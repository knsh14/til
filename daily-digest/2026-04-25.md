# デイリーダイジェスト — 2026-04-25

## 本日のハイライト

本日は土曜日のため arxiv 新着論文は配信なし（RSS 仕様で `skipDays` に Saturday/Sunday が指定されている）です。注目トピックは GitHub アクティビティから横断的に選定しました。

1. **[commaai/openpilot の panda submodule を pip パッケージへ置換](https://github.com/commaai/openpilot/pull/37908)** (commaai/openpilot) — `commaai/panda#2391`（pip パッケージ化）と `#2392`（wheel への資材同梱）と連動する三本立て PR 群。長年の bounty issue `panda#2239` を解決する見込みで、リポジトリ間の依存整理として影響範囲が広い。
2. **[Stateful Terminal Continuity (STC) 実装](https://github.com/microsoft/vscode/pull/312482)** (microsoft/vscode) — ターミナルプロセスを VS Code ウィンドウから切り離す大型機能 PR (+1010 / −8、23 ファイル)。永続バックグラウンドデーモンが scrollback ごと保持し、ウィンドウリロード・クラッシュ・アップデートをまたいでセッションを引き継ぐ。
3. **[BYOK で Claude Opus 4.7 の tool_search サポート](https://github.com/microsoft/vscode/pull/312491)** (microsoft/vscode) — `modelSupportsToolSearch` の正規表現が版数サフィックス付き BYOK モデル ID にマッチしない不具合の修正。`claude-opus-4-7@1.0.0` などを救うため、ファミリー単位の `startsWith` チェックに置換してマージ済み。
4. **[Neovim 0.12 リリース直前の LSP 整流化](https://github.com/neovim/neovim/pull/39391)** (neovim/neovim) — `vim.lsp.resolve_config` の本流化 PR `#39391` と古いドラフト `#33578` のクローズが一斉進行。`supports_method` 自己マップ対応 `#39383` のマージとバックポート、LuaRef リーク修正、`ttyfast` 復活も並行。
5. **[uv の S3 署名 URL リダクション](https://github.com/astral-sh/uv/pull/19146)** (astral-sh/uv) — `DisplaySafeUrl` が機密クエリパラメータ知識を持って verbose ログ漏洩を防ぐセキュリティ修正をマージ。続編 `#19147` で reqwest エラー経路までリダクションを拡張中。
6. **[uv で xz サポート完全削除のドラフト](https://github.com/astral-sh/uv/pull/19143)** (astral-sh/uv) — `#18927`（zip 経路の bz/lzma/xz 削除）に積む形で tarball 経路まで含めた xz 削除を狙う `breaking` ラベル付きドラフト。0.12 取り込み判断待ち。
7. **[ghostty libghostty を Linux/Windows から macOS にクロスコンパイル](https://github.com/ghostty-org/ghostty/pull/12417)** (ghostty-org/ghostty) — `pkg/apple-sdk` を Zig 内蔵 macOS ヘッダにフォールバックさせる整備。`CombineArchivesStep` も Linux ツーリングで動くよう更新し、CI で検証してマージ済み。
8. **[comma 3X の Kernel panic in WLAN driver 解析進展](https://github.com/commaai/openpilot/issues/35788)** (commaai/openpilot) — Starlink の短時間断で再現可能と判明した WLAN ドライバ起因クラッシュ。HTT TX 記述子が unmapped な HTT fragment-descriptor ページを指すと同じ SMMU フォールト fsynr が再現できる詳細解析が追記された。

---

## arxiv 新着論文

> 詳細: [arxiv/2026-04-25.md](../arxiv/2026-04-25.md)

本日（2026-04-25、土曜日）は arxiv RSS の `skipDays` 指定により全カテゴリ 0 件です。`https://export.arxiv.org/rss/cs.AI` を直接確認しても `<channel>` 内に `item` は含まれていませんでした。次回の新着公開は月曜日（2026-04-27）の予定です。

---

## GitHub アクティビティ

> 詳細: [github-digest/2026-04-25.md](../github-digest/2026-04-25.md)

合計 232 件（PR 148 / Issue 84）を 9 リポジトリから取得しました。

### commaai/openpilot

- **[panda submodule を pip 配布の panda 依存に置換](https://github.com/commaai/openpilot/pull/37908)** (`#37908`) — `.gitmodules` から panda を外し、`pyproject.toml` に `pandacan` を追加して in-tree でファームウェアをビルドする構成へ移行。process replay は 0 changed / 66 passed。
- **[NetworkManager 撤廃 Wi-Fi スタック (Draft)](https://github.com/commaai/openpilot/pull/37902)** (`#37902`) — `wpa_supplicant` ctrl-socket + `udhcpc` + `dnsmasq` への置換。UI 再起動を跨ぐデーモン引き継ぎを含む +4584 / −1599 の大規模パッチ。
- **[AGNOS 18.1 マージ](https://github.com/commaai/openpilot/pull/37895)** (`#37895`) — rootfs +13 MB の 3.7G。`#37891`（mici サーマル上限引き上げ）と併せて `#37737` をクローズ。
- **[jotpluggler のパース 2 倍高速化](https://github.com/commaai/openpilot/pull/37904)** (`#37904`) — 動的 capnp パースをビルド時生成の静的パーサに置換し、`generate_event_extractors.py` を新設してマージ。

### commaai/opendbc

- **[hypothesis 依存を削減](https://github.com/commaai/opendbc/pull/3357)** (`#3357`) — Ford / Hyundai / Toyota / 共通テストで `hypothesis` ベースのプロパティテストを `opendbc/testing.py` に置換。541 セグメントの Car behavior report で挙動差分なし。
- **[pycapnp のピン解除](https://github.com/commaai/opendbc/pull/3358)** (`#3358`) — `pyproject.toml` と `uv.lock` から pin を外す 2 行マージ。
- **[Honda 2026 コミュニティモデル追加](https://github.com/commaai/opendbc/pull/3356)** (`#3356`) — Acura RDX 2026 と Honda Pilot 2026 を `extra_cars.py` に登録。

### commaai/panda

- **[pip package 適正化 (openpilot submodule 置換用)](https://github.com/commaai/panda/pull/2391)** (`#2391`) — `__init__.py` への `INCLUDE_PATH` 追加と PEP 561 用 `py.typed` の設置。C・ヘッダ・リンカスクリプト・証明書を `pyproject.toml` の `package-data` に網羅登録し、版数を `0.0.11` に更新。
- **[wheel 同梱用 SCons パス修正](https://github.com/commaai/panda/pull/2392)** (`#2392`) — 明示的なワイルドカード globbing で非 Python 資材を確実に wheel に含め、site-packages からの絶対パス解決と `CPPPATH` 注入で out-of-tree ビルドを成立させる。

### neovim/neovim

- **[feat(lsp): resolve lsp config](https://github.com/neovim/neovim/pull/39391)** (`#39391`) — 古いドラフト `#33578` をクローズし、`vim.lsp.resolve_config` を本流化する整流化 PR。Neovim 0.12 リリース直前の LSP 集中改修。
- **[fix(lsp): supports_method の自己マップ対応](https://github.com/neovim/neovim/pull/39383)** (`#39383`) — マージ済み、バックポート `#39385` も完了。
- **[backport: stack-buffer-overflow 修正](https://github.com/neovim/neovim/pull/39393)** (`#39393`) — `fix(channel): stack-buffer-overflow with exit during connection` (`#39387`) のバックポート。

### nvim-telescope/telescope.nvim

- **[docs: File Picker のデフォルト挙動議論](https://github.com/nvim-telescope/telescope.nvim/issues/470)** (`#470`) — `.gitignore` 配下や hidden / binary ファイルの検索手段の発見性を改善すべきという議論を経てクローズ。

### ghostty-org/ghostty

- **[Flatpak: バス切断時にセッション終了](https://github.com/ghostty-org/ghostty/pull/12427)** (`#12427`) — Ghostty クラッシュ時、Flatpak Session Helper でスポーンしたコマンドを一緒に終了させる修正。
- **[libghostty: SIMD 付きでも libc 非依存ビルド (Draft)](https://github.com/ghostty-org/ghostty/pull/12414)** (`#12414`) — simdutf を fork して 6 個ほどの libc 関数を差し替え可能にし、C++ モジュールを別コンパイル単位に分離する +888 / −313 の大改修。
- **[macOS Korean IME preedit 取消バグの Vouch 発動](https://github.com/ghostty-org/ghostty/issues/11461)** (`#11461`) — `jcollie` が `!vouch @seyoungjeong` で投稿者を Vouch リストに招き、修正実装のフェーズへ移行。

### astral-sh/uv

- **[uninstalled パッケージの依存問い合わせ要望](https://github.com/astral-sh/uv/issues/19140)** (`#19140`) — PEP 658 の `.metadata` を sdist まで拡張する案、`pip install --dry-run` 代替案、PyPI 全体 SHA256 を Astral 側でホストする案など 6 件の議論。
- **[Reqwest エラー内 URL リダクション (Draft)](https://github.com/astral-sh/uv/pull/19147)** (`#19147`) — `#19146` の続編で `crates/uv-client/src/error.rs` を 144 行追加するなど 7 ファイル改修中。
- **[UV_PYTHON_PLATFORM 環境変数の追加](https://github.com/astral-sh/uv/pull/19142)** (`#19142`) — `--python-platform` 持ちコマンドに対応する環境変数を追加し `#15241` を解決。

### microsoft/vscode

- **[Anthropic Messages API: last two messages キャッシュ戦略 (Draft)](https://github.com/microsoft/vscode/pull/312472)** (`#312472`) — マージ済み末尾 2 メッセージへ `cache_control` を置く方式を、ヒューリスティックの代替として実験追加。`chat.anthropic.cacheBreakpoints.lastTwoMessages` フラグでゲートする。
- **[Chronicle セッションストアの cwd / assistant_response 充填](https://github.com/microsoft/vscode/pull/312293)** (`#312293`) — 常に空だった項目を `vscode.workspace.workspaceFolders[0]` などの consumer 側から埋める修正。OTel producer は変更しない方針。
- **[ターミナルタブの上下移動と一括 kill](https://github.com/microsoft/vscode/pull/309084)** (`#309084`) — `Move Terminal Tab Up/Down` と `Kill Terminals Below` コマンドを追加（`#309068`）。
- **[BYOK の認証なし利用 (Draft)](https://github.com/microsoft/vscode/pull/312207)** (`#312207`) — エアギャップ環境で BYOK 経由の chat を可能にする 19 ファイル改修。

### qmk/qmk_firmware

- **[Ergo‑L キーコード追加](https://github.com/qmk/qmk_firmware/pull/25766)** (`#25766`) — フランス語 `Ergo‑L` レイアウト用キーコードと sendstring を追加（`keymap_ergol.h` 新規 +154、`sendstring_ergol.h` +100）。投稿者がレビュー催促中。
- **[Ploopy Adept のハイブリッドスクロール (Draft)](https://github.com/qmk/qmk_firmware/pull/26171)** (`#26171`) — ドラッグスクロールに「短押しトグル / 長押しホールド」を `PLOOPY_DRAGSCROLL_HOLD_THRESHOLD_MS` 境界で実装。
