# デイリーダイジェスト 2026-06-23

## 本日のハイライト

本日 2026-06-23（火曜日）は arxiv の全 16 カテゴリで新着論文がなかったため、ハイライトは GitHub アクティビティから選定した。

1. **[Add `Hls` to `xterm-ghostty` terminfo so tmux auto-detects OSC 8 hyperlink passthrough](https://github.com/ghostty-org/ghostty/issues/13062)** (GitHub ghostty-org/ghostty)：terminfo の `Hls` 欠如を突き止めた詳細な報告。tmux 配下で OSC 8 ハイパーリンクが消える不具合の原因で、ターミナルが持つ capability を multiplexer へ伝える仕組みに関わる問題として注目された。
2. **[Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693)** (GitHub astral-sh/uv)：dirhash で wheel アーカイブをコンテンツアドレス指定する大型パフォーマンス機能。複数メンテナのレビューと CodSpeed の 6.88% 退行警告を巡り議論が続く。
3. **[feat(cmdwin): implement cmdwin as a normal buf+win](https://github.com/neovim/neovim/pull/40325)** (GitHub neovim/neovim)：コマンドラインウィンドウを通常のバッファとウィンドウとして実装し直す 65 ファイル規模の大規模リファクタ。特殊処理を排除する破壊的変更。
4. **[Chrysler: reliable fuzzy fingerprinting](https://github.com/commaai/opendbc/pull/3496)** (GitHub commaai/opendbc)：Chrysler 系のファジー FW マッチングを約 22 か月の実フィールドデータで検証し、認識率 80% かつ誤認識ゼロの strip=4 を採用した。失敗時は安全に no match へ縮退する。
5. **[Remove distro PAT](https://github.com/microsoft/vscode/pull/322052)** (GitHub microsoft/vscode)：7 日強制ローテートに備え、長寿命の distro PAT を全ビルドから廃止する 44 ファイル規模のセキュリティ対応。マージ済み。
6. **[webrtc: replace aiohttp with stdlib http server](https://github.com/commaai/openpilot/pull/38226)** (GitHub commaai/openpilot)：WebRTC デーモンの HTTP サーバを aiohttp から標準ライブラリへ置き換え、依存ツリーを `uv.lock` から完全に削除した。libdatachannel の試行や `openpilot/` への構造集約と合わせ、依存削減が進む。

---

## arxiv 新着論文

> 詳細: [arxiv/2026-06-23.md](../arxiv/2026-06-23.md)

本日は対象 16 カテゴリすべてで新着論文がなかった。
フィードのメタデータ上の公開日付は本日付で正しく、`<item>` 要素自体が存在しないため、取得スクリプトの不具合ではなく arxiv 側の一時的な空配信と考えられる。

---

## GitHub アクティビティ

> 詳細: [github-digest/2026-06-23.md](../github-digest/2026-06-23.md)

### sunnypilot/sunnypilot
- **[[bot] Update Python packages](https://github.com/sunnypilot/sunnypilot/pull/1866)** (`#1866`)：直近 24 時間は依存更新ボットによる Python パッケージ更新 1 件のみで、機能や修正の動きはなかった。

### commaai/openpilot
- **[webrtc: replace aiohttp with stdlib http server](https://github.com/commaai/openpilot/pull/38226)** (`#38226`)：WebRTC デーモンの HTTP サーバを標準ライブラリ実装へ移行し、aiohttp と依存群（yarl や multidict など）を `uv.lock` から完全に削除した。マージ済み。
- **[move top level dirs into openpilot/](https://github.com/commaai/openpilot/pull/38206)** (`#38206`)：トップレベルの各ディレクトリを `openpilot/` 配下へ集約する 738 ファイル規模のリネーム。`first_party` 化の試みと並ぶ構造改修で、本 PR はクローズ済み。

### commaai/opendbc
- **[Chrysler: reliable fuzzy fingerprinting](https://github.com/commaai/opendbc/pull/3496)** (`#3496`)：strip 長 2〜6 を実フィールドデータで掃引し、認識率 80% かつ誤認識ゼロの strip=4 を採用したファジー FW マッチング。過去 3 回の試みの誤った前提を是正した。
- **[2021 Subaru Ascent (Canada) - Vehicle not recognized](https://github.com/commaai/opendbc/issues/3494)** (`#3494`)：カナダ仕様の 2021 Subaru Ascent が CAN トラフィックはあるのに未認識で Dashcam Mode に留まるバグ報告。FW 指紋追加のトリアージ待ち。

### commaai/panda
- 直近 24 時間の更新はなかった。

### neovim/neovim
- **[feat(cmdwin): implement cmdwin as a normal buf+win](https://github.com/neovim/neovim/pull/40325)** (`#40325`)：cmdwin を通常のバッファとウィンドウとして実装し直す 65 ファイル規模の大規模リファクタ。破壊的変更を伴い、コードベース全体の特殊処理を排除する。
- **[feat(:restart)!: no bang restores session and window layout](https://github.com/neovim/neovim/pull/40321)** (`#40321`)：`:restart` でセッションとウィンドウレイアウトを復元し、従来動作を新設の `:restart!` へ移す破壊的変更。1 件 APPROVED 済みでメンテナの最終判断待ち。

### nvim-telescope/telescope.nvim
- **[Fix: replace usage of `fnameescape()` with `bufadd()` approach](https://github.com/nvim-telescope/telescope.nvim/pull/3682)** (`#3682`)：括弧を含むパスのファイルを開けない長年のバグ（#2446）を、`fnameescape` から `bufadd()` 方式への置き換えで修正する。Neovim 側の推奨に沿った対応。

### ghostty-org/ghostty
- **[Add `Hls` to `xterm-ghostty` terminfo](https://github.com/ghostty-org/ghostty/issues/13062)** (`#13062`)：tmux 配下で OSC 8 ハイパーリンクが消える原因を terminfo の `Hls` 欠如と特定した詳細な再現報告。非メンテナ起票のため自動クローズされた。
- **[core: fix PageList.scroll using wrong type for row offset](https://github.com/ghostty-org/ghostty/pull/13048)** (`#13048`)：行オフセットの型を u16 から usize へ変更し、65535 行を超えるとスクロールが失敗する不具合を修正した。数百万行規模で顕在化する。マージ済み。

### astral-sh/uv
- **[Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693)** (`#19693`)：dirhash による wheel アーカイブのコンテンツアドレス指定という大型パフォーマンス機能。CodSpeed が 6.88% の退行を警告し、複数メンテナのレビューで議論が続く。
- **[Reapply "Fix transparent Python upgrades in project environments"](https://github.com/astral-sh/uv/pull/19928)** (`#19928`)：pre-commit-uv を壊して一度リバートされた透過的 Python アップグレード修正を、上流の修正確認後に再適用した。マージ済み。

### microsoft/vscode
- **[Remove distro PAT](https://github.com/microsoft/vscode/pull/322052)** (`#322052`)：7 日強制ローテートに備え、長寿命の distro PAT を全ビルドから廃止する 44 ファイル規模のセキュリティ対応。マージ済み。
- **[Revert "Sandbox: force execution in sandbox..."](https://github.com/microsoft/vscode/pull/322476)** (`#322476`)：モデル昇格が壊れているため導入した「サンドボックス内強制実行」をリバートし、1.126 へチェリーピックした。Agent Host や Chat 関連の改修が活動の大半を占める。

### qmk/qmk_firmware
- **[keyboards/ploopyco: Fix scroll wheel freeze on trackball_thumb](https://github.com/qmk/qmk_firmware/pull/26286)** (`#26286`)：ノイズスパイクでエンコーダの境界が限界値まで広がるとデッドロックし、スクロールが無反応になる。境界を 10ms ごとに中心へ減衰させるタイマーで数秒後に自動回復させる修正。
- **[Fix VERBOSE make flag](https://github.com/qmk/qmk_firmware/pull/26289)** (`#26289`)：`VERBOSE` を任意の値（`false` でも）で有効化してしまうバグを修正し、不要になった `-s` 付与も整理した。新規キーボード追加 PR（Athena 1800 など）も多数進行中。
