# GitHub ダイジェスト — 2026-08-11

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[feat(options)!: schema, "dict" options, messages](https://github.com/neovim/neovim/pull/40924)** (neovim/neovim) — 辞書形式オプションをスキーマ中心に作り替える破壊的変更がマージされた。`vim.opt.diffopt:get()` の戻り値がリストからマップへ変わったため Gitsigns の nightly CI が壊れたと報告されており、下流への影響が既に出ている。
2. ![PR](icons/git-pull-request.svg) **[agentHost: make the orchestrator own session enumeration and chat lifecycle](https://github.com/microsoft/vscode/pull/329633)** (microsoft/vscode) — 60 ファイル・+12989/-4611 という直近 24 時間で最大の変更で、Agent Host のマルチチャット所有権リファクタを完了させる。セッションの同一性、列挙、ライフサイクル、復元を Agent Host へ一元化する。
3. ![PR](icons/git-pull-request.svg) **[Enable PGO for Linux x86-64 uv releases](https://github.com/astral-sh/uv/pull/21001)** (astral-sh/uv) — uv のリリースビルドに PGO を導入する取り組みで、Windows、macOS ARM64、Linux ARM64 向けの 3 本が同時に動いている。配布バイナリの性能に直結する変更である。
4. ![PR](icons/git-pull-request.svg) **[font/shaper: add RTL shaping for Arabic and Hebrew](https://github.com/ghostty-org/ghostty/pull/11079)** (ghostty-org/ghostty) — Unicode 双方向アルゴリズムを導入し、アラビア語とヘブライ語が正しい接合形と視覚順で描画されるようにする 1802 行の追加。レビューではベンチマークの不足と、RTL の母語話者による検証の必要性が論点になっている。
5. ![PR](icons/git-pull-request.svg) **[Enhance AI network access control for integrated browser](https://github.com/microsoft/vscode/pull/328880)** (microsoft/vscode) — 統合ブラウザについて AI のネットワークドメインポリシーの適用範囲を、サブリソース、ワーカー、スクリーンショット、ポップアップまで広げる 2553 行の変更。直近 24 時間で最も明確なセキュリティ案件である。

---

## sunnypilot/sunnypilot

> 直近 24 時間は横方向制御の新機能と modeld まわりの実装が動いており、あわせて依存パッケージの自動更新 PR が出ている。

### ![PR](icons/git-pull-request.svg) [Lateral: Road Edge Lane Change Controller](https://github.com/sunnypilot/sunnypilot/pull/1085) `#1085`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 車両が路肩に近いときに車線変更を抑止する RoadEdgeLaneChangeController を追加する PR である。モデル出力の確率しきい値（エッジ 0.35、レーンライン 0.2）から路肩を検出し、誤検知を避けるため 1 秒の反応遅延を挟んだうえで、検出した側への車線変更をブロックする。左右のブロックフラグは新設の `modelDataV2SP` メッセージで配信する。

**直近のアクティビティ:** process replay diff レポートは 66 件パス、差分 0 で通過している。UI プレビューも mici と big の双方で動画が一致した。sourcery-ai[bot]はレビューを試みたが、週あたりの diff 文字数の上限に達したため実行できなかった旨のみを返している。

**変更ファイル:** 14 ファイル、+330/-10。新規の `openpilot/sunnypilot/selfdrive/controls/lib/relc.py`（+92）が中心で、`cereal/custom.capnp`、`desire_helper.py`、`modeld.py`、`selfdrived.py`、車線変更設定の UI レイアウトに手が入っている。

**次のアクション:** レビュー待ちである。sourcery-ai のレート制限により自動レビューが未実施のため、人手での確認が必要となる。

---

### ![PR](icons/git-pull-request.svg) [modeld_v2: Support eGpu](https://github.com/sunnypilot/sunnypilot/pull/1894) `#1894`
**ステータス:** Open | **ラベル:** なし

**概要:** modeld_v2 で外付け GPU を利用できるようにする PR である。PR 本文の記述はない。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** 15 ファイル、+381/-244。`openpilot/selfdrive/modeld/modeld.py`（+137/-47）と SConscript まわりの変更が中心で、`compile_modeld.py` は削除されている。あわせて tinygrad モデルをビルドする GitHub Actions ワークフロー 3 本を更新している。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open | **ラベル:** prebuilt, car

**概要:** Hyundai/Kia/Genesis の ccNC（非 HDA2）対応を取り込む PR である。本文は対応する opendbc 側の PR へのリンクのみで、変更は opendbc サブモジュールの参照更新にとどまる。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** `opendbc_repo` の 1 ファイルのみ、+1/-1。

**次のアクション:** 参照先の opendbc 側 PR がマージされるのを待つ状態である。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/sunnypilot/sunnypilot/pull/1905) `#1905`
**ステータス:** Closed | **ラベル:** bot

**概要:** repo-maintenance から自動生成された Python 依存パッケージの更新 PR である。本文には仮想環境の総容量が 611M であることと、`zensical` 77M、`gcc_arm_none_eabi` 43M、`sympy` 30M といった容量上位の内訳、および `uv pip tree` の出力が添えられている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージされずにクローズされている。

**変更ファイル:** `uv.lock` の 1 ファイルのみ、+256/-259。

**次のアクション:** クローズ済みのため対応は不要である。

---
## commaai/openpilot

> 直近 24 時間は camerad の堅牢性向上（BPS クロック、IFE のラグ耐性、フレーム完全性テスト）が集中的に進み、あわせて cereal サービスの大規模リネームや locationd のプロセス統合といった構造整理が動いている。

### ![PR](icons/git-pull-request.svg) [camerad: force BPS clock to max](https://github.com/commaai/openpilot/pull/38539) `#38539`
**ステータス:** Merged | **ラベル:** なし

**概要:** BPS のエラーによる camerad の不調を修正する変更である。Issue `#38270` を解決する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `openpilot/system/camerad/cameras/spectra.cc` の 1 ファイルのみ、+1/-1。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [camerad: improve IFE robustness to lags](https://github.com/commaai/openpilot/pull/38548) `#38548`
**ステータス:** Merged | **ラベル:** なし

**概要:** カメライベントの処理に 1 秒以上の遅延が生じると IFE パイプラインが停止する問題への対応である。Issue `#38541` を解決する。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** 2 ファイル、+17/-13。`spectra.cc`（+14/-11）と `spectra.h`（+3/-2）である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [camerad: test frame integrity](https://github.com/commaai/openpilot/pull/38608) `#38608`
**ステータス:** Open | **ラベル:** なし

**概要:** カメラのフレーム完全性を検証するテストを追加する PR である。PR 本文には OX03C10 と OS04C10 の各センサについて、IFE と BPS それぞれの出力動画が表形式で添付されている。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** 3 ファイル、+114/-1。`system/camerad/test/test_camerad.py`（+103/-1）が中心で、`ox03c10.cc`（+7）と `os04c10.cc`（+4）にも追記している。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Rename "live" cereal services](https://github.com/commaai/openpilot/pull/38601) `#38601`
**ステータス:** Merged | **ラベル:** なし

**概要:** cereal サービス名から `live` 接頭辞を外し、内容を表す名前へ改める大規模リネームである。`liveTracks` を `radarTracks` へ、`liveCalibration` を `extrinsicsCalibration` へ、`liveTorqueParameters` を `lateralTorqueParameters` へ、`liveDelay` を `lateralDelay` へ、`livePose` を `deviceMotion` へ、`liveParameters` を `vehicleParameters` へ変更する。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。マージ済みである。

**変更ファイル:** 64 ファイル、+461/-461。`cereal/log.capnp`、`cereal/services.py`、`common/mock/` 配下、`selfdrive/car/card.py` など広範囲に及ぶ。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Merge paramsd, torque, and lagd into estimatorsd](https://github.com/commaai/openpilot/pull/38603) `#38603`
**ステータス:** Open | **ラベル:** なし

**概要:** locationd 配下で個別に動いていた paramsd、torqued、lagd を単一の estimatord へ統合する PR である。PR 本文の記述はない。

**直近のアクティビティ:** process replay diff レポートのみで、46 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** 10 ファイル、+103/-161。新規の `locationd/estimatord.py`（+94）に集約し、`lagd.py`（-39）、`paramsd.py`（-41）、`torqued.py`（-40）を削減している。process replay 側の設定も更新している。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [replace submodules with packages](https://github.com/commaai/openpilot/pull/38602) `#38602`
**ステータス:** Open | **ラベル:** なし

**概要:** git submodule をパッケージ依存へ置き換える PR である。本文には未完了項目として、editable install の整備、ビルドグラフの適正化、デバイス上での扱い（更新時に `uv sync` を走らせるか）が挙げられている。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** 35 ファイル、+179/-234。`.gitmodules`（-15）を空にし、`msgq_repo` と `opendbc_repo` の submodule 参照を除去、`SConstruct`（+24/-28）と `launch_chffrplus.sh` を調整している。

**次のアクション:** 本文の TODO が未消化のため、実装の継続が必要である。

---

### ![PR](icons/git-pull-request.svg) [Sleep shadow mode](https://github.com/commaai/openpilot/pull/38578) `#38578`
**ステータス:** Merged | **ラベル:** autonomy

**概要:** ドライバーモニタリングの眠気推定モデルを更新する PR である。作者は投稿時点でバッチ正規化に小さな不具合が残っており週末に修正すると述べていた。

**直近のアクティビティ:** TheConverseEngineer が、この PR はモデル本体のみを更新するものであり、sleep ヘッドと眠気確率のログ出力は既存だが、旧モデルは意味のあるポリシーを組めるだけの能力を持たない概念実証にとどまっていたと説明した。adeebshihadeh は `policy.py` への追加が漏れているのか、それとも sleep を既存の出力に畳み込んだのかと質問している。あわせて commaci-public がモデルリプレイの参照成果物を提示し、process replay diff は 66 件パス、差分 0 で通過している。

**変更ファイル:** `selfdrive/modeld/models/dmonitoring_model.onnx` の 1 ファイルのみ、+2/-2。

**次のアクション:** マージ済みだが、adeebshihadeh の `policy.py` に関する質問は未解決である。

---

### ![PR](icons/git-pull-request.svg) [ui: fix inverted DM head tracking on RHD](https://github.com/commaai/openpilot/pull/38607) `#38607`
**ステータス:** Merged | **ラベル:** ui

**概要:** 右ハンドル車でドライバーモニタリングの頭部追従が左右反転していた不具合の修正である。mici のドライバー状態描画が `driverStateV2` から自前で姿勢を計算する方式から `driverMonitoringState.visionPolicyState.pose` を読む方式へ変わった際、配信側の yaw が右ハンドル向けに既に反転済み（`policy.py:269`）であるにもかかわらず UI 側が無条件に符号を反転させていたことが原因である。左ハンドルでは影響が出ないため、リファクタが無害に見えていた。

**直近のアクティビティ:** sshane がレビューコメントを 2 件付けている（本文なし）。process replay diff は 66 件パス、差分 0 である。

**変更ファイル:** `selfdrive/ui/mici/onroad/driver_state.py` の 1 ファイルのみ、+2/-2。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [sync after a build](https://github.com/commaai/openpilot/pull/38606) `#38606`
**ステータス:** Merged | **ラベル:** なし

**概要:** scons がビルド成果物を fsync しないため、ビルド直後に電源が落ちると成果物が壊れたまま sconsign 上は有効と記録され続ける問題への対応である。作者はビルド後に電源を切ることで再現したと述べている。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** `system/manager/build.py` の 1 ファイルのみ、+2/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [wifi: replace NetworkManager with wpa_supplicant](https://github.com/commaai/openpilot/pull/38330) `#38330`
**ステータス:** Open | **ラベル:** なし

**概要:** Wi-Fi 管理を NetworkManager から wpa_supplicant ベースへ置き換える大規模な PR である。Issue `#37752` を解決し、`#37902` の継続にあたる。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** 25 ファイル、+6493/-1714。新規の `common/nm_keyfile.py`（+41）やハードウェア層のテスト追加に加え、mici の network レイアウトと `wifi_ui.py` が追随している。

**次のアクション:** 規模が大きくレビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [ui diff: Chunk frame diffs and group in report with thumbnails](https://github.com/commaai/openpilot/pull/37328) `#37328`
**ステータス:** Open | **ラベル:** なし

**概要:** リプレイ動画のフレームハッシュを `difflib` で適切に差分比較し、変更のまとまりごとにグループ化して HTML レポートへ表示する PR である。フレームの変更、追加、削除を示し、各グループの差分を個別に確認できる。従来どおりクリップ全体の差分も見られるが、チャンク単位の差分が入ったことで全体差分が今後も必要かは検討の余地があるとしている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿し、TheSecurityDev がレビューコメントを 2 件付けている。

**変更ファイル:** 3 ファイル、+352/-77。`selfdrive/ui/tests/diff/diff_template.html`（+185/-49）と `diff.py`（+166/-26）が中心である。

**次のアクション:** レビュー対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Fix process replay manager state](https://github.com/commaai/openpilot/pull/38604) `#38604`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** process replay のマネージャ状態を修正する小さな変更である。PR 本文の記述はない。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** `selfdrive/test/process_replay/migration.py` の 1 ファイルのみ、+2/-2。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [tools: setup.sh isn't for AGNOS](https://github.com/commaai/openpilot/pull/38588) `#38588`
**ステータス:** Merged | **ラベル:** なし

**概要:** `tools/setup.sh` が AGNOS 上での実行を想定していないことを明示するガードを追加する変更である。PR 本文の記述はない。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。人によるコメントはない。

**変更ファイル:** `tools/setup.sh` の 1 ファイルのみ、+8/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/commaai/openpilot/pull/38599) `#38599`
**ステータス:** Merged | **ラベル:** bot

**概要:** repo-maintenance から自動生成された Python 依存パッケージの更新 PR である。本文には仮想環境の総容量が 500M であることと、`gcc_arm_none_eabi` 43M、`sympy` 30M、`numpy` 30M といった容量上位の内訳が添えられている。

**直近のアクティビティ:** process replay diff レポートのみで、66 件パス、差分 0 である。マージ済みである。

**変更ファイル:** 5 ファイル、+43/-42。`uv.lock`（+38/-38）が中心で、`opendbc_repo` と `tinygrad_repo` の参照更新、`diff_report.yaml`、`migration.py` を含む。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [bump tinygrad](https://github.com/commaai/openpilot/pull/38526) `#38526`
**ステータス:** Closed | **ラベル:** なし

**概要:** tinygrad の参照を更新する PR である。作者は現時点でブロックされていると述べており、理由として tinygrad 側の PR がピクル可能な `TinyJit` クラスをデコレータ関数へ置き換えたため、既存のコンパイル済みモデル PKL を復元できなくなった点を挙げている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージされずにクローズされている。

**変更ファイル:** `tinygrad_repo` の 1 ファイルのみ、+1/-1。

**次のアクション:** クローズ済みである。上流側の PKL 互換性の問題が解消されるのを待つ必要がある。

---

### ![PR](icons/git-pull-request.svg) [dataset and reports Generated](https://github.com/commaai/openpilot/pull/38600) `#38600`
**ステータス:** Closed | **ラベル:** なし

**概要:** リポジトリの大半のファイルを削除する内容の PR である。PR 本文の記述はなく、1176 ファイルにわたり 140,267 行を削除するのみとなっている。誤って作成されたものとみられる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージされずにクローズされている。

**変更ファイル:** 1176 ファイル、+0/-140267。`.editorconfig`、`.gitattributes`、Issue テンプレート、ワークフロー定義など、リポジトリ全体に及ぶ削除である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Feature: op jupyter](https://github.com/commaai/openpilot/issues/38544) `#38544`
**ステータス:** Open | **ラベル:** PC

**概要:** openpilot のログを事前設定なしで調べたいという要望である。悪い運転の原因が測位、モデル、制御のどれにあるのかを切り分けたいが、そのために `apt` や `uv`、素性の知れない bash を実行したくないと述べている。Ubuntu 26.04 でも Windows XP でも作業できることを望み、`ssh comma` から仮想環境に入る UX 案を示している。

**直近のアクティビティ:** tealtwo が、誰もが comma connect と jupyterlab を使えるという前提に対し、自分は jupyterlab の使い方を知らないと応じている。

**次のアクション:** 要望の検討段階である。

---

### ![Issue](icons/issue-opened.svg) [LKAS dash faults and no bootkick: STM brown-out?](https://github.com/commaai/openpilot/issues/38488) `#38488`
**ステータス:** Open | **ラベル:** needs testing, hardware

**概要:** RAV4 を数日放置したあと始動したところ、カメラ関連のダッシュエラーが出て comma four が bootkick しなかったという報告である。デバイスを挿し直すと復帰し、以降の走行では問題なかった。根本原因は未特定だが、comma power の不良により STM がブラウンアウト状態に陥り、CAN バスのリレーを駆動してバスを開いた可能性が疑われている。放置から約 15 分でデバイスの電源が落ちていた点も、comma power の不良を裏づける材料として挙げられている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** needs testing のラベルが付いており、再現と原因の切り分けが必要である。

---

### ![Issue](icons/issue-opened.svg) [Loss of steering control (Bolt EV 2022)](https://github.com/commaai/openpilot/issues/38605) `#38605`
**ステータス:** Closed | **ラベル:** bug

**概要:** 高速道路で車線変更中に engaged 状態から「TAKE CONTROL IMMEDIATELY」が出たという報告である。直後に再度 engage できたとされ、該当ルートと nightly-dev というバージョンが示されている。

**直近のアクティビティ:** adeebshihadeh が、PR `#38464` で修正されるはずであり報告者のコミットはその直前のものだと回答し、あわせて LLM が生成した長大な文章を Issue に貼らないよう求めている。simontheflutist は panda に言及し、ログの単調時刻に基づく事象の時系列分析を折りたたみで添えている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [camerad: malfunction due to BPS errors](https://github.com/commaai/openpilot/issues/38270) `#38270`
**ステータス:** Closed | **ラベル:** bug

**概要:** BPS のエラーにより camerad が不調になるという報告である。本文は該当する 4 本のルート識別子と、カメラ不調時のログを添付したファイルで構成されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。PR `#38539` により解決してクローズされている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [camerad: CAM-ISP: Reg upd ack with no waiting request](https://github.com/commaai/openpilot/issues/38541) `#38541`
**ステータス:** Closed | **ラベル:** bug

**概要:** CAM-ISP で待機中のリクエストがないにもかかわらずレジスタ更新の ack が返るという報告である。本文は該当する多数のルート識別子の列挙で構成されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。PR `#38548` により解決してクローズされている。

**次のアクション:** クローズ済みのため対応は不要である。

---
## commaai/opendbc

> 直近 24 時間は Hyundai の ccNC ポートや Honda の車種追加といったプラットフォーム対応と、デッドコード削除や共通関数の切り出しといった整理が並んでいる。60 日間の無活動による stale 警告が付いた PR もある。

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/commaai/opendbc/pull/1575) `#1575`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, car safety

**概要:** ccNC を搭載する Hyundai/Kia 車のうち HDA II を持たない構成に対応する PR である。対象は Hyundai Kona 2024-25、Kona Hybrid 2024、Kona Electric 2024、Santa Cruz 2025、Sonata 2024-25、Sonata Hybrid 2024-25、Tucson Hybrid 2025、Kia K4 2025、K5 2025、Sorento と幅広い。

**直近のアクティビティ:** sshane が CHANGES_REQUESTED のレビューを出している（本文なし）。

**変更ファイル:** 15 ファイル、+417/-11。`opendbc/car/hyundai/fingerprints.py`（+119）と `hyundaicanfd.py`（+105/-1）、`values.py`（+72/-3）が中心で、`carcontroller.py`、`carstate.py`、`interface.py`、テストにも手が入っている。

**次のアクション:** sshane の指摘への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Honda: Acura ADX](https://github.com/commaai/opendbc/pull/2878) `#2878`
**ステータス:** Open | **ラベル:** car, honda, stale

**概要:** Acura ADX を追加する PR である。制御はレーダーレスの Honda HR-V と同一で、2025 年式と 2026 年式のフィンガープリントを個別に取得している。ダッシュカムのルートで純正の最大操舵トルクが 3840 であることを確認済みで、30 分および 60 分の走行を経た操舵パラメータの学習と微調整も完了している。

**直近のアクティビティ:** github-actions[bot]が 60 日間活動がないため 7 日後に自動クローズする旨の stale 警告を出している。あわせて car behavior report が HONDA_ACCORD や HONDA_CIVIC_BOSCH など複数車種の 154 セグメントを対象に実行されている。

**変更ファイル:** 6 ファイル、+34/-3。`opendbc/car/honda/fingerprints.py`（+24）が中心で、`values.py`、`interface.py`、`tests/routes.py`、`torque_data/override.toml` を更新し、`extra_cars.py` から 1 行削除している。

**次のアクション:** stale による自動クローズを避けるため、レビューまたは作者からの状況更新が必要である。

---

### ![PR](icons/git-pull-request.svg) [car: common function for delayed factory cruise canceling](https://github.com/commaai/opendbc/pull/3336) `#3336`
**ステータス:** Open | **ラベル:** car, gm, hyundai, toyota

**概要:** 純正クルーズの遅延キャンセル処理を、車種ごとに散らばった実装から共通関数へ切り出す PR である。Issue `#1173` を解決する可能性があるとしつつ、検証は未了と記されている。carcontroller 側は 1 行に集約済みである。

**直近のアクティビティ:** sshane がレビューコメントを付けている（本文なし）。

**変更ファイル:** 4 ファイル、+17/-14。`opendbc/car/interfaces.py`（+5）に共通関数を置き、GM、Hyundai、Toyota の各 `carcontroller.py` から重複を除いている。

**次のアクション:** 実車での検証と、sshane のレビューへの対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Remove dead code](https://github.com/commaai/opendbc/pull/3670) `#3670`
**ステータス:** Merged | **ラベル:** can, car, chrysler, ford, tesla, car safety

**概要:** 複数のモジュールにまたがる未使用コードを削除する PR である。PR 本文の記述はない。

**直近のアクティビティ:** TheSecurityDev と sshane がレビューコメントを付けている（いずれも本文なし）。マージ済みである。

**変更ファイル:** 13 ファイル、+14/-62。`opendbc/can/dbc.py`（+13/-19）が中心で、`docs.py`（-8）、`fordcan.py`、`chryslercan.py`、`tesla/carstate.py`、`psa/carstate.py`、`logreader.py`、safety のテストから不要な記述を除いている。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Remove mass from CarSpecs](https://github.com/commaai/opendbc/pull/3676) `#3676`
**ステータス:** Closed | **ラベル:** car, body, chrysler, ford, gm, honda, hyundai, mazda, nissan, subaru, tesla, toyota, volkswagen, rivian

**概要:** `CarSpecs` から質量の項目を取り除く PR である。PR 本文の記述はない。ほぼ全メーカーの `values.py` に横断的に及ぶ変更となっている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージされずにクローズされている。

**変更ファイル:** 19 ファイル、+207/-213。`hyundai/values.py`（+57/-57）、`honda/values.py`（+31/-31）、`gm/values.py`（+17/-17）など各メーカーの定義と、`opendbc/car/__init__.py`、`interfaces.py` に及ぶ。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [VW MEB: Platform support](https://github.com/commaai/opendbc/pull/1696) `#1696`
**ステータス:** Closed | **ラベル:** car, volkswagen, car safety

**概要:** Volkswagen の ID シリーズをはじめとする MEB プラットフォーム車への対応を追加する PR である。infiniteCable の貢献に基づくとされ、ハーネスや純正 Travel Assist でのルート取得、safety レイヤーの課題が TODO として並んでいた。CAN イグニッションの必要性の確認や、comma がダッシュカム扱いを解除するまで DEBUG に留める対応は完了済みとされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージされずにクローズされている。

**変更ファイル:** 13 ファイル、+513/-15。`volkswagen/carstate.py`（+65/-3）、`carcontroller.py`（+48/-10）、新規の `mebcan.py`（+34）が中心である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3675) `#3675`
**ステータス:** Merged | **ラベル:** bot

**概要:** `uv.lock` の依存関係を毎週更新する自動 PR である。

**直近のアクティビティ:** car behavior report が実行され、挙動の変化は検出されなかった。マージ済みである。

**変更ファイル:** `uv.lock` の 1 ファイルのみ、+115/-115。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Hard to use testing infrastructure after pytest removal](https://github.com/commaai/opendbc/issues/3401) `#3401`
**ステータス:** Closed | **ラベル:** なし

**概要:** pytest を外したあとのテスト実行手順が使いにくいという指摘である。`./opendbc/car/tests/test_car_interfaces.py` は何も実行せず、`pytest` 経由では unittest 記法のためクラッシュし、`unittest-parallel -p` ではテストが 0 件になる。正しく動かすにはファイル名をパターンとして渡す長いコマンドが必要で補完も効かず、しかも単一プロセスで pytest より遅いと述べている。opendbc が unittest で openpilot が pytest という不統一にも触れている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---
## commaai/panda

> 直近 24 時間の動きは 1 件のみで、リポジトリを Python パッケージとして配布できるよう構成を整理する PR が出ている。

### ![PR](icons/git-pull-request.svg) [package panda](https://github.com/commaai/panda/pull/2413) `#2413`
**ステータス:** Open | **ラベル:** なし

**概要:** panda をパッケージとして配布できるようにする構成変更の PR である。PR 本文の記述はない。ルート直下の `__init__.py` を削除し、`MANIFEST.in` を新設するなど、パッケージ化に向けたファイル配置の組み替えが中心となっている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 44 ファイル、+118/-72。`MANIFEST.in`（新規）、`Dockerfile`、`SConscript`、`.gitignore`、`README.md` に加え、`__init__.py`（-13）の削除と `board/` 配下の調整を含む。

**次のアクション:** レビュー待ちである。

---
## neovim/neovim

> 直近 24 時間はオプションのスキーマ化という破壊的変更がマージされ、その影響でプラグイン側の CI が壊れた報告も届いている。あわせて `vim.async` や conceal 対応 `'wrap'` といった大型 PR のレビューと、vim-patch の取り込みが平常運転で進んでいる。

### ![PR](icons/git-pull-request.svg) [feat(options)!: schema, "dict" options, messages](https://github.com/neovim/neovim/pull/40924) `#40924`
**ステータス:** Merged | **ラベル:** build, spell, diff, breaking-change, options

**概要:** 辞書形式のオプションの解析が煩雑だった問題に対し、スキーマを軸に整理する破壊的変更である。`_core/options.lua` の独自構造ビルダーを廃し、`'guicursor'` などを除く非プリミティブなオプションすべてにスキーマを定義して、`opt:get()` の改善と統一的で詳細なエラーメッセージを実現する。この `opt:get()` が将来の `vim.o()` の基礎になるとしている。

**直近のアクティビティ:** lewis6991 が、この変更で Gitsigns の nightly CI が壊れたと報告した。`vim.opt.diffopt:get()` が従来はリストを返していたのに対しマップを返すようになり、Gitsigns はリストとして読むためオプションが何も見えず外部 git へフォールバックしてしまうという内容である。justinmk は `news.txt` に記載済みであることを示している。あわせて justinmk がレビューコメントを 3 件付けている。

**変更ファイル:** 61 ファイル、+1924/-1254。`runtime/lua/vim/_core/options.lua`（+6/-148）の削減と、`src/gen/gen_api_dispatch.lua`、`src/gen/gen_eval_files.lua` の生成まわりの変更が中心である。

**次のアクション:** マージ済みだが、下流プラグインへの影響が出ているため周知が必要である。

---

### ![PR](icons/git-pull-request.svg) [feat(lua): add vim.async](https://github.com/neovim/neovim/pull/34473) `#34473`
**ステータス:** Open | **ラベル:** lua, async, AI assisted 🤖

**概要:** タイマー、ジョブ、libuv ハンドルなどコールバック駆動の処理を扱う `vim.async` を追加する PR である。現状は順序付け、待機、キャンセルのために利用者が自前でコルーチンのラッパーやタスク管理、後始末の規則を組む必要があり、非同期の制御フローを共有したりテストしたりしにくいという問題に対応する。

**直近のアクティビティ:** bfredl がドキュメントを通読し、協調的かつ構造化されたマルチタスクという設計に概ね同意すると述べた。あわせて yield の挙動を手作業で検証し、理解が正しいことを確認したとしている。lewis6991、clason、justinmk もレビューコメントを付けている。

**変更ファイル:** 18 ファイル、+4924/-7。新規の `runtime/doc/lua-async.txt`（+599）を含む大規模な追加である。

**次のアクション:** レビューの継続が必要である。

---

### ![PR](icons/git-pull-request.svg) [feat: conceal-aware 'wrap'](https://github.com/neovim/neovim/pull/40897) `#40897`
**ステータス:** Open | **ラベル:** marks, conceal, AI assisted 🤖

**概要:** `'wrap'` が有効なとき、conceal で隠されたセルを含む行が conceal 前の桁位置で折り返してしまう問題への対応である。表示上は隠れているのに行の画面高さは元のままで、カーソル、`screenpos()`、`wincol()`、マウス、`gj`/`gk` がいずれも conceal 前の桁を使っていた。

**直近のアクティビティ:** freddiehaddad が `concealcursor=n` のときのビジュアル選択でテキスト配置が期待と異なる点を指摘し、その後 `conceallevel=3` でビジュアルモードのスクロール時に妙な挙動を見つけたと報告した。あわせてエッジケースが見つかりにくくなっており進捗は良好だと述べている。また `blink.cmp` 向けに関数コメントのソフトブレークを除去するフックを書き、conceal 対応の折り返しでドキュメントが正しく再整形されることを画像付きで示した。pappasam も議論に加わり、barrettruth と zeertzjq がレビューコメントを付けている。

**変更ファイル:** 31 ファイル、+2843/-206。`runtime/lua/vim/treesitter/highlighter.lua`（+93/-24）を含み、treesitter とマークまわりに広く及ぶ。

**次のアクション:** 報告されたビジュアルモードのエッジケースへの対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [feat(uri): add nvim:// URI scheme for 'Open in Editor' link](https://github.com/neovim/neovim/pull/38006) `#38006`
**ステータス:** Open | **ラベル:** なし

**概要:** ブラウザ、ファイルマネージャ、エラーレポータといった外部ツールからの「Open in Editor」連携のために `nvim://` URI スキームを追加する PR である。形式は `nvim://open?file={path}[&line={n}][&column={n}][&server={addr}]` で、この PR では `open` のみを扱うが、パーサは将来の拡張に備えて `action` フィールドを保持する。Issue `#34102` を解決する。

**直近のアクティビティ:** superatomic、justinmk、calebdw がレビューコメントを付けている。

**変更ファイル:** 13 ファイル、+554/-13。新規の `runtime/lua/vim/_core/uri.lua`（+134）が中心で、`starting.txt`（+34）や `lua.txt`（+37）のドキュメントも追加している。

**次のアクション:** レビュー対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): deduplicate push and pull diagnostics](https://github.com/neovim/neovim/pull/40781) `#40781`
**ステータス:** Open | **ラベル:** lsp

**概要:** 同一の LSP クライアントが push と pull の双方で同じ診断を報告した場合に重複を除去する PR である。あわせて、そのクライアントとバッファに対する pull プロバイダがちょうど 1 つのときは、push された診断を pull のストリームへ流し込む。Issue `#40774` と `#29927` を解決する。

**直近のアクティビティ:** mfussenegger が CHANGES_REQUESTED を出している。クライアントが今必要な診断だけを要求できる点で pull のほうがアーキテクチャ的に適しているという、PR 本文の主張を引用する形のレビューである。

**変更ファイル:** 2 ファイル、+207/-1。`runtime/lua/vim/lsp/diagnostic.lua`（+143/-1）とテストである。

**次のアクション:** mfussenegger の指摘への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [feat(wasm): track and fix ongoing issues + add WASM build workflow](https://github.com/neovim/neovim/pull/41186) `#41186`
**ステータス:** Open | **ラベル:** なし

**概要:** WASM 対応の検証中に見つかった不具合の修正を追跡する PR である。あわせて、生成された成果物を検証するビルドスクリプトとワークフローを追加する。

**直近のアクティビティ:** clason が、公式のビルド成果物については binaryen の `wasm-opt` を工程に加えることを検討すべきだとコメントした。tree-sitter での経験から、その場合は前段の最適化を省くほうが有利になりうるとも述べている。bfredl と Rawan10101 もレビューコメントを付け、github-advanced-security[bot]もコメントしている。

**変更ファイル:** 6 ファイル、+227/-7。新規の `.github/workflows/build_wasm.yml`（+68）と `build_wasm.sh`（+57）が中心で、`release.yml`（+47/-2）と `src/wasm/` 配下を更新している。

**次のアクション:** `wasm-opt` の導入検討とレビュー対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [feat(prompt): prompt_appendbuf() appends to prompt buffer](https://github.com/neovim/neovim/pull/37763) `#37763`
**ステータス:** Merged | **ラベル:** なし

**概要:** プロンプトバッファではプロンプト行より上にテキストを挿入することが推奨されており、`:` マークを使えば実現できるものの、最善の方法が分かりにくいという問題への対応である。プロンプトバッファへ追記する専用 API を提供する。

**直近のアクティビティ:** justinmk が、Nvim 0.12 にはプロンプトバッファに関する他の変更もあり vimspector 側の追随が必要だと述べ、例として複数行入力への対応を挙げた。zoumi は vimspector のコンソールバッファがプロンプトバッファであり `buf.append()` を使っていると述べ、pynvim の `buf.append()` が壊れたのか、0.11 では動いて 0.12 で動かないと質問している。justinmk は `buf.append` はプロンプトバッファとは無関係だと回答した。

**変更ファイル:** 12 ファイル、+167/-8。`runtime/doc/vimfn.txt`（+25）と `runtime/lua/vim/_meta/vimfn.lua`（+22）を含む。

**次のアクション:** マージ済みだが、zoumi が報告した vimspector 側の挙動については切り分けが続いている。

---

### ![PR](icons/git-pull-request.svg) [fix(plines): 'linebreak' ignores inline virtual text width](https://github.com/neovim/neovim/pull/41227) `#41227`
**ステータス:** Open | **ラベル:** なし

**概要:** `linebreak` が有効なとき、単語が収まるかの判定にバッファ内のテキストしか測らず、LSP のインレイヒントなどインライン仮想テキストの幅を勘定に入れていない問題への修正である。その結果、短く見える単語が行に残され、隣の仮想テキストが 2 行にまたがってしまう。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 4 ファイル、+60/-4。`src/nvim/plines.c`（+38/-2）が中心で、`drawline.c`、`plines.h`、装飾のテストを含む。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix(diff): resync cursorbind after undo changes the diff #41250](https://github.com/neovim/neovim/pull/41275) `#41275`
**ステータス:** Open | **ラベル:** diff, AI assisted 🤖

**概要:** `do_check_cursorbind()` が、現在のウィンドウとカーソル位置が前回から変わっていない場合に早期リターンする点に起因する不具合の修正である。undo は削除された行を復元しつつカーソルを前回記録した位置へ戻すことがあり、その間に対向の diff ウィンドウが対応する行は変わってしまう。Issue `#41250` を解決する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル、+43/-1。`src/nvim/move.c`（+7/-1）と `cursorbind_spec.lua`（+36）である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix(cursorbind): update cursorbind after diff changes](https://github.com/neovim/neovim/pull/41265) `#41265`
**ステータス:** Closed | **ラベル:** なし

**概要:** `#41275` と同じ問題に対する先行の修正案である。diff の変更はウィンドウとカーソル位置のどちらも変えずに cursorbind の状態を無効化しうるため、undo 後に古い diff 情報が使われてしまうと説明している。

**直近のアクティビティ:** zeertzjq が、追加された条件は意味をなさないと思うとコメントしている。クローズされている。

**変更ファイル:** 2 ファイル、+50/-1。`src/nvim/move.c`（+3/-1）と `diff_spec.lua`（+47）である。

**次のアクション:** クローズ済みである。同じ問題は `#41275` で対応が続いている。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): remove blank virtual line after codeLens resolve failure](https://github.com/neovim/neovim/pull/41248) `#41248`
**ステータス:** Open | **ラベル:** lsp, AI assisted 🤖

**概要:** `codeLens/resolve` が失敗したとき、未解決のレンズが描画済みとして残り、空のプレースホルダ仮想行が次の `textDocument/codeLens` 要求まで表示され続ける問題への修正である。失敗した解決要求に対応する未解決レンズのみを取り除き、その行の最後のレンズだった場合は仮想行も消す。

**直近のアクティビティ:** barrettruth が jdrouhard に言及するコメントを残している。

**変更ファイル:** 2 ファイル、+98/-3。`runtime/lua/vim/lsp/codelens.lua`（+10/-3）と `codelens_spec.lua`（+88）である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): set title with TERM=wezterm](https://github.com/neovim/neovim/pull/41272) `#41272`
**ステータス:** Open | **ラベル:** tui, AI assisted 🤖

**概要:** `TERM` が WezTerm を示していて terminfo に `tsl` と `fsl` がない場合に、TUI が端末のタイトルを更新しない問題への修正である。`wezterm` 系を検出し、欠けているタイトル関連の capability だけを WezTerm の OSC 0/ST シーケンスで補う。未知の端末の扱いは変えない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル、+17/-0。`src/nvim/tui/tui.c`（+6）と `tui_spec.lua`（+11）である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.fs): support URIs in `slug()`](https://github.com/neovim/neovim/pull/41241) `#41241`
**ステータス:** Open | **ラベル:** lua, filesystem, ci:skip-news

**概要:** `vim.fs.slug()` が `term://foo//123:bash` のような URI を扱えず、ターミナルの永続化などの呼び出し側がスキームを自前で除く必要があった問題への対応である。`normalize()` の前に生の入力から `scheme://` を検出し、`=uri-<scheme>-` という接頭辞へ置き換える。

**直近のアクティビティ:** superatomic がレビューコメントを 2 件、justinmk が 1 件付けている。

**変更ファイル:** 3 ファイル、+17/-3。`runtime/lua/vim/fs.lua`（+11/-3）とドキュメント、テストである。

**次のアクション:** レビュー対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [refactor(defaults): edit global cwd on `1-`](https://github.com/neovim/neovim/pull/41254) `#41254`
**ステータス:** Merged | **ラベル:** なし

**概要:** ディレクトリバッファでは既にバッファローカルの CWD にいるため `1-` が何もしないという問題への対応である。解決した CWD によって挙動が変わるのは直感的でないとして、`1-` がグローバルな CWD を開くようにする。

**直近のアクティビティ:** nathanzeng が PR のタイトルと説明を更新し、CI の失敗は無関係に見えると述べた。barrettruth が感謝のコメントを残している。justinmk、superatomic、nathanzeng がレビューコメントを付けている。

**変更ファイル:** 3 ファイル、+10/-2。`runtime/plugin/dir.lua`、`runtime/doc/plugins.txt`、`dir_spec.lua` である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [fix(l10n): repair Traditional Chinese message catalog](https://github.com/neovim/neovim/pull/41273) `#41273`
**ステータス:** Open | **ラベル:** localization, AI assisted 🤖

**概要:** 繁体字中国語のカタログに未翻訳が 12 件、古い fuzzy 訳が 13 件、printf のプレースホルダ不一致が 7 件あり、一部の診断が空欄または不正確になって厳密な gettext 形式検証も通らない問題への修正である。未訳の補完と fuzzy 訳の修正を行う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/po/zh_TW.UTF-8.po` の 1 ファイルのみ、+50/-47。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [docs: misc, :bcd, slug()](https://github.com/neovim/neovim/pull/41113) `#41113`
**ステータス:** Merged | **ラベル:** なし

**概要:** ドキュメントの雑多な更新をまとめた PR である。PR 本文の記述はない。

**直近のアクティビティ:** justinmk がレビューコメントを 3 件付けている。マージ済みである。

**変更ファイル:** 19 ファイル、+226/-222。`runtime/doc/editing.txt`（+24/-2）、`api.txt`、`lsp.txt` などで、`runtime/autoload/README.txt`（-25）を削除している。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/41258) `#41258`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** サウンド関連や正規表現まわりを中心とする多数の vim パッチについて、Nvim では該当なしとする記録を `version.c` へ反映する PR である。

**直近のアクティビティ:** janlazo が、popup 系のパッチを除外してきた理由として、Nvim が独自実装を拡張する際の参考情報は `runtime/doc/` に頼る方針だったと説明した。justinmk はこれを理解したうえで、機能の欠落に気付いたときは高レベルの Issue を立てるべきだが、対象は主要なものに限り、`popup_create` の `tabpage` 引数のような細かなパラメータは対象外にしたいと述べている。

**変更ファイル:** `src/nvim/version.c` の 1 ファイルのみ、+29/-42。

**次のアクション:** 方針の議論が続いており、マージ前に整理が必要である。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): n/a popup_ api](https://github.com/neovim/neovim/pull/41270) `#41270`
**ステータス:** Open | **ラベル:** build, vim-patch

**概要:** `#41258` のコメントを受けた後続の PR で、popup 系 API を該当なしとして記録する。作者はもう少し追加したいがやり過ぎない範囲に留めたいとして、ドラフト状態にしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル、+6/-1。`scripts/vim_na_files.txt`、`vim_na_hunks_c.txt`、`vim_na_hunks_h.txt`、`vim_na_hunks_vim.txt`、`vim-patch.sh` である。

**次のアクション:** ドラフトのため、作者による追記の完了を待つ状態である。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0933: u_read_undo() leaks the file name when the undo file owner differs](https://github.com/neovim/neovim/pull/41269) `#41269`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted 🤖

**概要:** undo ファイルの所有者がテキストファイルおよび現在のユーザーと異なる場合、`u_read_undo()` が `u_get_undo_file_name()` で確保したファイル名を解放しないまま戻るというメモリリークの修正である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `src/nvim/undo.c` の 1 ファイルのみ、+1/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0932: NFA engine fallback can double free the compiled program](https://github.com/neovim/neovim/pull/41268) `#41268`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted 🤖

**概要:** 自動選択の正規表現エンジンが `vim_regexec_string()` でバックトラッキングエンジンへフォールバックする際、置き換え後のプログラムをコンパイルする前に既存のコンパイル済みプログラムを解放しており、メモリ不足でパターンの保存に失敗すると二重解放になりうる問題の修正である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `src/nvim/regexp.c` の 1 ファイルのみ、+7/-2。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:6163d99: runtime(doc): Improve :help expr-number, mention unary +/-](https://github.com/neovim/neovim/pull/41267) `#41267`
**ステータス:** Merged | **ラベル:** documentation, vim-patch

**概要:** `:help expr-number` の記述を改善する vim パッチの取り込みである。整数と浮動小数点の双方の説明で単項の `+` と `-` に触れ、既存の `0o` や `0x` に合わせて 2 進数の `0b` のタグを追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `runtime/doc/vimeval.txt` の 1 ファイルのみ、+7/-3。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:2e8c81e: runtime(strace): highlight stack trace as comment](https://github.com/neovim/neovim/pull/41266) `#41266`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** strace が `-k`（`--stack-trace`）でシステムコールごとに出力する完全なスタックトレースを、コメントとしてハイライトする vim パッチの取り込みである。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `runtime/syntax/strace.vim` の 1 ファイルのみ、+3/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [ci: bump github/codeql-action from 4.37.5 to 4.37.6 in the github-actions group across 1 directory](https://github.com/neovim/neovim/pull/41274) `#41274`
**ステータス:** Open | **ラベル:** ci, dependencies, github_actions

**概要:** dependabot による `github/codeql-action` の 4.37.5 から 4.37.6 への更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/codeql.yml` の 1 ファイルのみ、+2/-2。

**次のアクション:** マージ待ちである。

---

### ![Issue](icons/issue-opened.svg) [Regression: sending long strings to terminal extremely slow (Windows)](https://github.com/neovim/neovim/issues/41262) `#41262`
**ステータス:** Closed | **ラベル:** performance, clipboard, terminal

**概要:** Windows でターミナルへ十分に長い文字列を送ると数秒かかり、操作が実用に耐えないという報告である。遅さは文字列長に強く依存し、報告者は計算量が二乗規模のアルゴリズムを疑っている。

**直近のアクティビティ:** ienapliss が、Issue を立てる前に十分検証しなかったとして謝罪し、以前は問題なかった 0.8 を含む複数の旧バージョンを試したところ、自身の MinGW 環境では同様に影響を受けたと報告した。justinmk は問題が出る最も古いバージョンを尋ね、PR `#39773` を試せると有用だと述べている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [EditorConfig `{num..num}` pattern is not rejected](https://github.com/neovim/neovim/issues/41264) `#41264`
**ステータス:** Open | **ラベル:** runtime, plugin-editorconfig

**概要:** `[foo{1..1}]` のような EditorConfig の記述が、仕様上は `num1` が `num2` より小さいことを要求するため不正であるにもかかわらず、拒否されないという指摘である。

**直近のアクティビティ:** Freed-Wu が `[foo{2..10}]` の例を示し、`editorconfig.lua:285` で `Vim:E944: Reverse range in charac` として正規表現の解析に失敗するエラーが出ると報告している。

**次のアクション:** パターン検証の実装が必要である。

---

### ![Issue](icons/issue-opened.svg) [vim.pos, vim.range: elegant way to use `Range:has` with exclusive end bound](https://github.com/neovim/neovim/issues/41225) `#41225`
**ステータス:** Open | **ラベル:** lua

**概要:** カーソルが `vim.Range` の内側にあるかを調べる際、`vim.Range:has()` が終端を含むため、範囲の直後にカーソルがある位置まで含めてしまうという指摘である。終端の桁がカーソルの桁と一致することが原因である。

**直近のアクティビティ:** TheLeoP が、以前に `vim.Pos` で終端を含む場合と含まない場合を区別しようとしたときはコードが極めて煩雑になり、その種の論理を `vim.Pos` の内側に持たせるのは筋が悪いという結論に至ったと述べている。

**次のアクション:** API 設計の議論が続いている。

---

### ![Issue](icons/issue-opened.svg) [API: control virt_lines_above, viewport, screen lines, scroll](https://github.com/neovim/neovim/issues/16166) `#16166`
**ステータス:** Open | **ラベル:** api, ui, marks

**概要:** 仮想行やビューポート、画面行、スクロールを制御する API に関する長期の Issue である。

**直近のアクティビティ:** justinmk が、過去のコメントで方針は合意済みだとしつつ、新しい API を追加する前に `winrestview({ topfill=... })` で何が足りないのかを示す必要があると述べた。これまでは既存の手段で満足されてきたという認識も示している。mcauley-penney は `scrolloffpad` の作業に戻り、バッファ末尾側は既存の論理を流用できたがバッファ先頭側の実装に取り組んでいると報告した。

**次のアクション:** 新規 API の必要性についての論証が求められている。

---
## nvim-telescope/telescope.nvim

> 直近 24 時間に新しい提案や実装はなく、いずれも過去の Issue 2 件がクローズされた動きのみである。

### ![Issue](icons/issue-opened.svg) [fd/rg failed to spawn process](https://github.com/nvim-telescope/telescope.nvim/issues/1679) `#1679`
**ステータス:** Closed | **ラベル:** bug

**概要:** `find_file` や `live_grep` を実行するとプロセスの起動に失敗してエラーになるという報告である。報告環境は Ubuntu 20.04 と NVIM v0.7.0-dev で、`checkhealth telescope` は telescope 用のヘルスチェックが見つからないというエラーを返していた。

**直近のアクティビティ:** markediez が、`brew install rg` で ripgrep を入れたところ動作するようになったと報告している。外部コマンドの不足が原因だったことを示す内容である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Find files equivalent for SCP?](https://github.com/nvim-telescope/telescope.nvim/issues/804) `#804`
**ステータス:** Closed | **ラベル:** enhancement, wontfix

**概要:** netrw のようにファイルを閲覧できるビューアがあるかという要望である。

**直近のアクティビティ:** booth-w が、今も予定はないのかと確認するコメントを寄せている。

**次のアクション:** wontfix ラベル付きでクローズ済みのため、対応の予定はない。

---
## ghostty-org/ghostty

> 直近 24 時間は macOS のクリップボード確認やサロゲートペア入力といった入力まわりの修正と、GTK 側の設定編集やメモリ解放の整理が並行して進んでいる。RTL シェーピングやペイン間の入力ブロードキャストなど、規模の大きい機能 PR のレビューも動いている。

### ![PR](icons/git-pull-request.svg) [macos: defer OSC52 clipboard read confirmations until focused](https://github.com/ghostty-org/ghostty/pull/13744) `#13744`
**ステータス:** Open | **ラベル:** os/macos

**概要:** クリップボード読み取りの確認シートが即座に表示されてフォーカスを奪う問題への対応である。作者はこれが DoS 攻撃を含む悪用に使えるほか、フォーカス変更時に OSC52 を発行するプログラム（一部の Neovim 設定で観測された）でフォーカスとシートのループを引き起こしていたと述べている。修正後は、サーフェスが非フォーカスならベルを鳴らし、次にフォーカスされた時点で確認を出す。Issue `#10077` を解決する。

**直近のアクティビティ:** bo2themax は APPROVED を出しつつ、確認をサーフェスのオーバーレイへ移せばウィンドウコントローラ側で確認する必要がなくなるという案を後続 PR で試すとしている。一方で別途 CHANGES_REQUESTED も出しており、クリップボードの読み書きを繰り返すスクリプトでは依然として別タブからフォーカスを奪うと動画付きで報告した。mitchellh は同様のスクリプトでは再現しなかったとして再試行するとコメントしている。

**変更ファイル:** 7 ファイル、+367/-163。`BaseTerminalController.swift`（+164/-72）と新規の `Ghostty.ClipboardConfirmationRequest.swift`（+114）が中心で、`GhosttyPackage.swift`（-50）を削減している。

**次のアクション:** bo2themax が報告したフォーカス奪取の再現ケースへの対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [macos,gtk: add broadcast support to sync across panes](https://github.com/ghostty-org/ghostty/pull/13745) `#13745`
**ステータス:** Open | **ラベル:** なし

**概要:** ターミナルペイン間で入力を同期するブロードキャスト機能を実装する PR である。Issue `#3227`、`#11797`、`#12832` の要望に応えるもので、グループの定義はマウスとキーボードの双方から行え、設定可能なキーバインドで最大 10 個の同期グループを扱える。グループ化されたペインは色付きの枠線で識別する。

**直近のアクティビティ:** bo2themax が CHANGES_REQUESTED を出し、結果自体は良いとしつつ core、gtk、macOS の 2〜3 本の PR へ分割するよう勧めている。分割したほうがレビューしやすく、段階的にマージされやすいという理由である。

**変更ファイル:** 15 ファイル、+900/-12。新規の `BroadcastGroups.swift`（+155）が中心で、`SurfaceView_AppKit.swift`（+64/-8）、`Ghostty.App.swift`（+28）、`include/ghostty.h`、`src/Surface.zig` に及ぶ。

**次のアクション:** レビュアーの求めに応じた PR の分割が必要である。

---

### ![PR](icons/git-pull-request.svg) [font/shaper: add RTL shaping for Arabic and Hebrew](https://github.com/ghostty-org/ghostty/pull/11079) `#11079`
**ステータス:** Open | **ラベル:** font

**概要:** アラビア語とヘブライ語のテキストが、シェーピングを LTR に強制しているために字が繋がらず順序も逆転して描画される問題への対応である。Unicode 双方向アルゴリズム（UAX #9）を用いた双方向シェーピングを導入し、RTL のスクリプトが正しい接合形と視覚順で描画されるようにする。対象はシェーピングのみで、カーソル位置、選択、ターミナルモデル、プロトコルの変更は意図的に範囲外としている。

**直近のアクティビティ:** guysoft は実際に試して動作したと報告しつつ、句読点の位置が揃わない点は `auto-ltr` の設定で回避できると述べた。asibahi はベンチマークが欠けている点に触れ、Ghostty の性能を損なわないことの確認と、RTL の母語話者による検証が望ましいという議論を引用している。pluiedev は CHANGES_REQUESTED を出し、Fribidi のような外部ライブラリではなく純粋な Zig のライブラリを使った点が判断の決め手になったと述べた。DiaaEddin もレビューコメントを 2 件付けている。

**変更ファイル:** 10 ファイル、+1802/-298。`src/font/shaper/coretext.zig`（+729/-57）が中心で、新規の `bidi_helpers.zig`、ビルド定義、`build.zig.zon` に及ぶ。

**次のアクション:** ベンチマークの実施と、pluiedev の指摘への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [core/gtk: allow editing Ghostty config in a Ghostty window](https://github.com/ghostty-org/ghostty/pull/11905) `#11905`
**ステータス:** Merged | **ラベル:** gtk

**概要:** `open_config` キーバインドを拡張し、`$EDITOR` または `$VISUAL` で指定したエディタを使って Ghostty の設定を新しい Ghostty ウィンドウ内で編集できるようにする PR である。

**直近のアクティビティ:** 00-kat は翻訳ファイルがマージを妨げないようにするための APPROVED であり、コード自体は読んでいないと明記している。bo2themax は macOS で検証し、`keybind = cmd+/=open_config:new_window` が `keybind = cmd+/=open_config` と同様に動作したと報告した。trag1c も APPROVED を出している。

**変更ファイル:** 45 ファイル、+4076/-3327。大半は各言語の `po` ファイルの更新で、`include/ghostty.h`（+9）を含む。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [gtk: add window title renaming](https://github.com/ghostty-org/ghostty/pull/10999) `#10999`
**ステータス:** Open | **ラベル:** os/linux, gtk

**概要:** GTK 側でウィンドウタイトルの変更に対応する PR である。Issue `#10469` を GTK について解決する。

**直近のアクティビティ:** tristan957 は GTK 側の変更は問題ないとコメントした。00-kat は翻訳ファイルについて APPROVED を出している。bo2themax も APPROVED を出しつつ、macOS がいつ対応するか不明なため `unsupportedActionKeys` の配列にコメントを添えるべきだと述べている。

**変更ファイル:** 44 ファイル、+3961/-3554。大半は各言語の `po` ファイルの更新で、`include/ghostty.h` と `Ghostty.Command.swift` を含む。

**次のアクション:** bo2themax が求めた macOS 向けの注記の追加が必要である。

---

### ![PR](icons/git-pull-request.svg) [config/edit: better handling of existing paths](https://github.com/ghostty-org/ghostty/pull/13736) `#13736`
**ステータス:** Merged | **ラベル:** なし

**概要:** 設定ファイルを編集する際の既存パスの扱いを改善する PR である。既存のファイルが見つかった場合はディレクトリやファイルの作成を試みずにパスを返し、パスが存在しない場合はシンボリックリンクを辿ったうえで先にディレクトリの有無を確認する。

**直近のアクティビティ:** vancluever が、Issue `#13714` を修正し PR `#13734` に取って代わるものだと説明した。既に取得済みの情報をより丁寧に使う点が `#13734` との違いであり、ファイルの存在が分かっていればその判定自体を省くとしている。jcollie は CHANGES_REQUESTED のあと APPROVED を出し、vancluever もコメントを付けている。

**変更ファイル:** `src/config/edit.zig` の 1 ファイルのみ、+50/-20。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [config: fix open_config when the config directory is a symlink](https://github.com/ghostty-org/ghostty/pull/13734) `#13734`
**ステータス:** Closed | **ラベル:** なし

**概要:** 設定ディレクトリがシンボリックリンクのとき `open_config` が何も起こさない問題への修正である。ダイアログもエディタも出ず、標準エラーに `error getting config file path: error.NotDir` が残るだけだった。原因は `openPath` がファイルを開く前に設定ディレクトリを作成する点にあり、パスの構成要素が既に存在する場合に `createDirPath` が失敗していた。

**直近のアクティビティ:** tristan957 が、PR `#13736` を採用するためクローズすると述べた。あわせて、人間とやり取りするなら大量の生成文を投げつけないよう求めるコメントも残している。

**変更ファイル:** `src/config/edit.zig` の 1 ファイルのみ、+12/-2。

**次のアクション:** クローズ済みである。同じ問題は `#13736` で解決している。

---

### ![PR](icons/git-pull-request.svg) [terminal/kitty: use fixed table for control keys](https://github.com/ghostty-org/ghostty/pull/13729) `#13729`
**ステータス:** Open | **ラベル:** なし

**概要:** kitty グラフィクスの制御キー解決に使っていた、アリーナ上のハッシュマップを固定長配列へ置き換える最適化である。poop による計測として、CPU サイクル 173M から 171M（-1.2%）、命令数 519M から 517M（-0.5%）、ピーク RSS 11.9MB から 11.8MB、キャッシュミス 356K から 312K（-12.4%）を挙げている。

**直近のアクティビティ:** Uzaaft は pluiedev から `std.EnumMap` を教わって試したものの、コード生成の結果はかえって悪かったと報告した。あわせて、キーが 30 個ほどなのでテーブルを 200 バイト超から 128 バイト程度へ縮められるが、switch 文が読みにくくなり保守の手間が増えると述べ、コマンドフィールドへ直接パースする案にも触れている。jcollie もレビューコメントを付けている。

**変更ファイル:** `src/terminal/kitty/graphics_storage.zig` の 1 ファイルのみ、+26/-1。

**次のアクション:** テーブルサイズと可読性のどちらを取るかの判断と、レビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [macOS: support decoding the surrogate pair with UnicodeHexInput](https://github.com/ghostty-org/ghostty/pull/13737) `#13737`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS の Unicode Hex Input でサロゲートペアを復号できるようにする PR である。作者は、関連する discussion に対する完全な修正ではないと断っている。

**直近のアクティビティ:** mitchellh が、Ghostty は生の UTF-8 を解釈できるのだから UTF-8 で符号化して Ghostty 側に復号させられないのかと疑問を呈した。bo2themax はそれも試し、サロゲートを正しく復号してサーフェスへ送ったが上手くいかなかったと応じている。

**変更ファイル:** `macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift` の 1 ファイルのみ、+55/-2。

**次のアクション:** 根本的な解決方法の検討が続いている。

---

### ![PR](icons/git-pull-request.svg) [macOS: rework for #12712 and #13645](https://github.com/ghostty-org/ghostty/pull/13717) `#13717`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** `#12712` で導入された `needleSelection` に起因するクラッシュへの対応である。クラッシュは macOS 15 の `readPasteboardNeedle` で最も多く発生し、`objectWillChange` の発火の仕方が異なることが原因とみられる。macOS 26 と 27 では再現しにくいため、自分たちの側で保証すれば足りるという判断である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** 5 ファイル、+45/-69。`OSSurfaceView.swift`（+17/-34）が中心で、テストの整理も含む。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [gtk: fix `+new-window -e <command>` inheriting forced shell integration](https://github.com/ghostty-org/ghostty/pull/13741) `#13741`
**ステータス:** Open | **ラベル:** gtk

**概要:** `+new-window -e <command>` が、実行中の GTK アプリケーションから強制シェル統合の設定を引き継いでしまう問題への修正である。`-e` 以降の引数をシェル検出が必要な明示的コマンドとして印を付け、その印を既存のコマンド上書きとともに新しいサーフェスへ伝播させる。

**直近のアクティビティ:** jcollie が CHANGES_REQUESTED を出し、これは正しい方針ではないと述べている。利用者が `ghostty +new-window` や `ghostty +new-tab` の CLI で `--shell-integration` を指定でき、それが渡るようにすべきだという意見である。

**変更ファイル:** 5 ファイル、+53/-0。`src/apprt/gtk/class/surface.zig`（+30）が中心で、`application.zig`、`window.zig`、`tab.zig`、`split_tree.zig` に及ぶ。

**次のアクション:** jcollie の提案する方針への作り直しの検討が必要である。

---

### ![PR](icons/git-pull-request.svg) [apprt/gtk: add WeakRef.deinit and use it at teardown sites](https://github.com/ghostty-org/ghostty/pull/13732) `#13732`
**ステータス:** Merged | **ラベル:** gtk

**概要:** `WeakRef(T)` が `set` と `get` しか持たないため、解放が `set(null)` という通常の代入と区別の付かない書き方になっていた問題への対応である。所有メモリの解放前に必ず実行する必要があるという要件が `class/inspector_window.zig` のコメントにしか書かれておらず、型の利用者の目に入らない点も指摘している。GLib が文書化する `g_weak_ref_clear` へ委譲する `deinit` を追加する。Issue `#13713` を解決する。

**直近のアクティビティ:** jcollie が CI の通過を条件に APPROVED を出している。

**変更ファイル:** 5 ファイル、+21/-4。`src/apprt/gtk/weak_ref.zig`（+17）が中心で、各 teardown 箇所を 1 行ずつ置き換えている。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [gtk: free hotkeys memory on app teardown](https://github.com/ghostty-org/ghostty/pull/13727) `#13727`
**ステータス:** Merged | **ラベル:** gtk

**概要:** vicinae-hotkey プロトコルに対応する Wayland コンポジタでグローバルキーバインドを設定していると、デバッグビルドで終了時に DebugAllocator がメモリリークを報告する問題への修正である。原因は `Hotkeys.entries` の ArrayList が解放されないことにある。作者は、このリストはアプリ終了まで保持されるべきものなので実害はないが、毎回エラーが出るのは避けたいと述べている。

**直近のアクティビティ:** jcollie が CI の通過を条件に APPROVED を出し、pluiedev も APPROVED を出している。

**変更ファイル:** `src/apprt/gtk/winproto/wayland/Hotkeys.zig` の 1 ファイルのみ、+2/-1。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [nix: update nixpkgs-unstable](https://github.com/ghostty-org/ghostty/pull/13678) `#13678`
**ステータス:** Open | **ラベル:** なし

**概要:** fontconfig 2.18 を取り込むために nixpkgs-unstable を更新する PR である。現状は 2.17 にリンクしているため、2.18 向けの設定ファイルを使うと `invalid constant used` という fontconfig の警告が標準エラーに出る。

**直近のアクティビティ:** tristan957 が APPROVED を出している。

**変更ファイル:** 3 ファイル、+22/-22。`flake.lock`（+14/-14）、`flake.nix`、`build.zig.zon.nix` である。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [feat: add `cursor-style-unfocused` configuration option](https://github.com/ghostty-org/ghostty/pull/9630) `#9630`
**ステータス:** Closed | **ラベル:** なし

**概要:** 非フォーカス時のカーソル形状を設定する `cursor-style-unfocused` を追加する PR である。`#2818` のレビューで示された方針に沿った実装で、型は `terminal.CursorStyle`、既定値は現在の挙動を保つ `block_hollow` としている。ウィンドウが非フォーカスのときはカーソルを常に表示し、設定された形状を用いる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージされずにクローズされている。

**変更ファイル:** 6 ファイル、+822/-25。`src/font/shaper/harfbuzz.zig`（+747/-18）が大半を占め、`src/config/Config.zig`、`src/renderer/cursor.zig` を含む。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [i18n - Latvian translation of command palette, typo fixes and more natural translations](https://github.com/ghostty-org/ghostty/pull/11663) `#11663`
**ステータス:** Open | **ラベル:** translations

**概要:** ラトビア語訳をコマンドパレット周辺まで広げ、誤字の修正とより自然な言い回しへの調整を行う PR である。作者は `#11641` のマージ後に進められるとしている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿し、EriksRemess がコメントを 2 件付けている。

**変更ファイル:** `po/lv.po` の 1 ファイルのみ、+197/-178。

**次のアクション:** 前提となる `#11641` のマージを待つ状態である。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13743) `#13743`
**ステータス:** Merged | **ラベル:** なし

**概要:** jcollie の discussion コメントを受けて VOUCHED リストへ dave92082 を追加する PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `.github/VOUCHED.td` の 1 ファイルのみ、+1/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13739) `#13739`
**ステータス:** Merged | **ラベル:** なし

**概要:** jcollie の discussion コメントを受けて VOUCHED リストへ PRIHLOP を追加する PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。マージ済みである。

**変更ファイル:** `.github/VOUCHED.td` の 1 ファイルのみ、+1/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Unicode Hex Input produces �� instead of emoji on macOS](https://github.com/ghostty-org/ghostty/issues/13733) `#13733`
**ステータス:** Open | **ラベル:** os/macos, needs-confirmation, input

**概要:** macOS の Unicode Hex Input で、Option キーを押しながら Unicode 値を打って文字を入力する際の不具合報告である。単一の UTF-16 値で表せる文字は正しく入力できるが、サロゲートペアが必要な絵文字では置換文字になってしまう。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** needs-confirmation のラベルが付いており、再現の確認が必要である。対応する修正の試みが PR `#13737` で進んでいる。

---

### ![Issue](icons/issue-opened.svg) [open_config fails when the config directory is a symlink](https://github.com/ghostty-org/ghostty/issues/13714) `#13714`
**ステータス:** Open | **ラベル:** gui

**概要:** 設定ディレクトリがシンボリックリンクのとき、`ctrl+,`（`open_config`）を押しても何も起こらないという報告である。設定を dotfiles リポジトリで管理していると生じる構成である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR `#13736` がマージ済みのため、動作確認のうえクローズできる見込みである。

---

### ![Issue](icons/issue-opened.svg) [apprt/gtk: give WeakRef(T) a deinit, so "clear this in dispose" is visible at the use site](https://github.com/ghostty-org/ghostty/issues/13713) `#13713`
**ステータス:** Closed | **ラベル:** gtk

**概要:** `src/apprt/gtk/weak_ref.zig` の `WeakRef(T)` が `set` と `get` しか公開しておらず、`deinit` も `clear` もないため、解放が `set(null)` と書かれて通常の代入と区別が付かないという指摘である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。PR `#13732` により解決してクローズされている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [macOS: background flicker when focusing inactive tab after OS theme change](https://github.com/ghostty-org/ghostty/issues/7939) `#7939`
**ステータス:** Open | **ラベル:** os/macos, gui

**概要:** システムのテーマが変わるとアクティブなタブの背景は即座に追随するが、非アクティブなタブの背景色は変わらないため、そのタブへ切り替えた瞬間に色が急変するという報告である。

**直近のアクティビティ:** ArttuLai が、OS のテーマ変更時に限らず Ghostty をコールドローンチするたびにちらつきが出ると報告している。環境は macOS 15.7.8 と Ghostty 1.3.1 で、設定でライトとダークの両テーマを定義したあとに気付いたとしている。

**次のアクション:** 再現条件が広がったため、原因の調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [CLI: support opening new tabs in an existing window](https://github.com/ghostty-org/ghostty/issues/12136) `#12136`
**ステータス:** Closed | **ラベル:** cli

**概要:** 既存の Ghostty ウィンドウに対してコマンドラインから新しいタブを開きたいという要望である。報告者は複数のセッションをタブごとに開いており、再起動後に復元するスクリプトを使っているが、現状は `osascript` で `Cmd+T` の打鍵を模倣するしかなく、アクセシビリティ権限が必要で壊れやすいと述べている。

**直近のアクティビティ:** anombyte93 が、Linux と GTK でも同じ要望が当てはまるが現状の経路では実現できていないとして、セッションピッカーが実行中のインスタンスへ依頼できるべきだという観点から補足している。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [macOS: Add ability to change split order](https://github.com/ghostty-org/ghostty/issues/1525) `#1525`
**ステータス:** Closed | **ラベル:** gui

**概要:** 分割の順序を入れ替えるアクションが欲しいという要望である。現状は一度作った分割の順序を変える手段がないとし、kitty の `move_window` を例に挙げている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---
## astral-sh/uv

> 直近 24 時間は各プラットフォーム向けの PGO 有効化と、Simple API の解析やアーカイブの内容アドレス化といった性能改善の PR が目立つ。あわせて Renovate による依存更新が多数動いている。

### ![PR](icons/git-pull-request.svg) [Enable PGO for Linux x86-64 uv releases](https://github.com/astral-sh/uv/pull/21001) `#21001`
**ステータス:** Open | **ラベル:** releases

**概要:** uv のリリースビルドに PGO を導入する PR で、Linux x86-64 から着手する。既存の manylinux コンテナ内で計装済みの `uv` と `uvx` のバイナリをビルドし、コールドキャッシュとウォームキャッシュの双方でワークロードを実行してプロファイルを収集する構成である。

**直近のアクティビティ:** codspeed-hq[bot]がフレームグラフを生成できなかったと報告しつつ、性能に変化はないと結論している。zanieb がレビューコメントを 2 件付けている。

**変更ファイル:** 2 ファイル、+573/-0。新規の `scripts/build_uv_pgo.py`（+563）が中心で、`build-release-binaries.yml` に 10 行を追加している。

**次のアクション:** zanieb のレビューへの対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Enable PGO for Windows x86-64 uv releases](https://github.com/astral-sh/uv/pull/21003) `#21003`
**ステータス:** Open | **ラベル:** releases

**概要:** `#21001` の方針に沿って Windows x86-64 のリリースでも PGO を有効にする PR である。既存の静的 CRT 構成は維持する。

**直近のアクティビティ:** codspeed-hq[bot]が、性能に変化はないと報告している。

**変更ファイル:** 2 ファイル、+45/-1。`build-release-binaries.yml`（+43）と `scripts/build_uv_pgo.py` である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Enable PGO for macOS ARM64 uv releases](https://github.com/astral-sh/uv/pull/21002) `#21002`
**ステータス:** Open | **ラベル:** releases

**概要:** `#21001` の方針に沿って macOS ARM64 のリリースでも PGO を有効にする PR である。

**直近のアクティビティ:** codspeed-hq[bot]が、性能に変化はないと報告している。

**変更ファイル:** `.github/workflows/build-release-binaries.yml` の 1 ファイルのみ、+15/-1。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Enable PGO for Linux ARM64 uv releases](https://github.com/astral-sh/uv/pull/21004) `#21004`
**ステータス:** Open | **ラベル:** releases

**概要:** `#21001` の方針に沿って Linux ARM64 のリリースでも PGO を有効にする PR である。PGO を有効にするためネイティブの ARM64 ランナーへ移行する点も含む。

**直近のアクティビティ:** codspeed-hq[bot]が、性能に変化はないと報告している。

**変更ファイル:** `.github/workflows/build-release-binaries.yml` の 1 ファイルのみ、+12/-2。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Deduplicate binary payloads with archive manifests](https://github.com/astral-sh/uv/pull/19694) `#19694`
**ステータス:** Open | **ラベル:** なし

**概要:** 内容アドレス化されたホイールのアーカイブが、展開済みツリー全体としてしか重複排除できなかった問題への対応である。同じネイティブライブラリを含む別々のホイールが、その `.so`、`.pyd`、`.dll`、`.dylib` のペイロードをアーカイブごとに個別保存していた。

**直近のアクティビティ:** astral-sh-bot[bot]が、`charlie/dirhash-archives` を基準としてテストが 9 件追加されたと報告している。codspeed-hq[bot]は性能に変化はないと結論している。

**変更ファイル:** 34 ファイル、+1225/-118。`crates/uv-cache/` 配下を中心に広範囲へ及ぶ。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693) `#19693`
**ステータス:** Open | **ラベル:** enhancement, performance, preview

**概要:** 展開済みのホイールが `archive-v0` の下にランダム生成の ID で保存されていたため、異なるキャッシュ経由で到達した同じホイールを重複して保存しうるという問題への対応である。決定的なディレクトリハッシュを計算して内容アドレス化する。

**直近のアクティビティ:** codspeed-hq[bot]が、このマージにより性能が 6.42% 悪化し、2 件のベンチマークが退行すると報告している。astral-sh-bot[bot]はテストが 5 件追加されたと報告した。charliermarsh と woodruffw がレビューコメントを付けている。

**変更ファイル:** 21 ファイル、+1294/-303。`crates/uv-cache/src/lib.rs`（+26/-1）などキャッシュ層が中心である。

**次のアクション:** 報告された性能退行への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [add `uv_extract::dirhash` a new scheme and implementation for hashing directory trees](https://github.com/astral-sh/uv/pull/20737) `#20737`
**ステータス:** Merged | **ラベル:** なし

**概要:** ディレクトリツリーをハッシュする新しい方式と実装を `uv_extract::dirhash` として追加する PR である。作者は `#19693` をこの方式の上に作り直す予定だとし、charliermarsh と woodruffw の意見を得るため独立したドラフトとして出したと述べている。

**直近のアクティビティ:** oconnor663 と charliermarsh がレビューコメントを付けている。マージ済みである。

**変更ファイル:** 10 ファイル、+995/-0。`crates/uv-extract/` 配下の新規実装が中心である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Replace astral-tokio-tar with tar-codec](https://github.com/astral-sh/uv/pull/19979) `#19979`
**ステータス:** Open | **ラベル:** performance, internal

**概要:** 読み書きの双方の経路で astral-tokio-tar を tar-codec へ置き換えるプレビュー機能を追加する PR である。

**直近のアクティビティ:** woodruffw が、翌朝に最後の見直しをしてからマージする予定だとコメントしている。astral-sh-bot[bot]はテストが 18 件追加されたと報告した。woodruffw 自身のレビューコメントも 2 件ある。

**変更ファイル:** 31 ファイル、+1623/-362。`Cargo.lock`（+233/-21）とベンチマークまわりを含む。

**次のアクション:** woodruffw による最終確認を経てマージされる見込みである。

---

### ![PR](icons/git-pull-request.svg) [Deserialize PypiFile/PyxFile directly](https://github.com/astral-sh/uv/pull/21041) `#21041`
**ステータス:** Open | **ラベル:** performance

**概要:** 2 つの最適化を行う PR である。中間型の `PypiFileWire` を経ずに目的の型へ直接デシリアライズして中間確保を減らし、あわせて `requires-python` の小さな文字列の確保も避ける。

**直近のアクティビティ:** codspeed-hq[bot]が、このマージにより性能が 6.02% 悪化すると報告している。ただし実行環境が異なる旨の注記も付いている。

**変更ファイル:** `crates/uv-pypi-types/src/simple_json.rs` の 1 ファイルのみ、+141/-101。

**次のアクション:** 報告された性能退行の検証が必要である。

---

### ![PR](icons/git-pull-request.svg) [Parse PyPI Simple API JSON responses directly](https://github.com/astral-sh/uv/pull/21040) `#21040`
**ステータス:** Closed | **ラベル:** なし

**概要:** PyPI Simple API の応答が Serde を通じて中間の `Vec<PypiFileWire>` へデシリアライズされ、そこから各要素を `PypiFile` へ変換していた点を、直接解析へ変える PR である。配布物が数千件ある packages では、この余分な走査と汎用の JSON ディスパッチが負担になるとしている。

**直近のアクティビティ:** charliermarsh が、Codex に実装させたものの実際にはあまり効果がないと考えているとして、遅延条件ごとの CPU 削減率を示す計測表をコメントした。codspeed-hq[bot]も性能に変化はないと報告している。クローズされている。

**変更ファイル:** 5 ファイル、+809/-3。`crates/uv-pypi-types/src/simple_json.rs`（+281/-1）が中心である。

**次のアクション:** クローズ済みである。同じ方向の最適化は `#21041` で継続している。

---

### ![PR](icons/git-pull-request.svg) [Add Simple API deserialization benchmarks](https://github.com/astral-sh/uv/pull/21042) `#21042`
**ステータス:** Open | **ラベル:** internal, benchmarks

**概要:** Simple API のデシリアライズについてベンチマークを追加する PR である。`#21041` から基準値を確立するために切り出したものだと説明している。

**直近のアクティビティ:** codspeed-hq[bot]がフレームグラフを生成できなかったと報告している。

**変更ファイル:** 3 ファイル、+108/-0。新規の `crates/uv-bench/benches/uv_pypi_types.rs`（+99）が中心である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Preserve consecutive Python minor-version exclusions](https://github.com/astral-sh/uv/pull/21045) `#21045`
**ステータス:** Merged | **ラベル:** bug

**概要:** `requires-python` の正規化時に連続するワイルドカード除外が捨てられていた問題の修正である。隣接する範囲の隙間からは除外が 1 つしか生じないという前提が原因で、バージョン指定子の再構成時に除外された各マイナーバージョンをすべて保持するようにする。

**直近のアクティビティ:** astral-sh-bot[bot]がテストの 1 件追加を報告し、codspeed-hq[bot]はフレームグラフを生成できなかったと報告している。zanieb が APPROVED を出してマージされた。

**変更ファイル:** 2 ファイル、+99/-18。`crates/uv-pep440/src/version_specifier.rs`（+27/-18）とロックのテスト（+72）である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Reject conflicts between packages and their own extras](https://github.com/astral-sh/uv/pull/21038) `#21038`
**ステータス:** Open | **ラベル:** なし

**概要:** パッケージとその extra の間の競合指定を拒否する PR である。extra を選べば必ずパッケージ自体も選ばれるためで、解決の開始前およびロックファイルの書き出し前に、競合の検証段階でこれを確認する。

**直近のアクティビティ:** astral-sh-bot[bot]が、テストの 1 件追加と 1 件削除を報告している。

**変更ファイル:** 2 ファイル、+55/-199。`crates/uv-pypi-types/src/conflicts.rs`（+18）と、ロックのテスト（+37/-199）である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Handle ENAMETOOLONG on macOS when clearing cache entries](https://github.com/astral-sh/uv/pull/20933) `#20933`
**ステータス:** Open | **ラベル:** cache

**概要:** `uv cache clean` の実行時に、キャッシュディレクトリが `PATH_MAX` を超える極端に長い絶対パスを含む場合の対応である。Issue `#15569` を修正する。

**直近のアクティビティ:** zanieb がレビューコメントを 2 件付けている。

**変更ファイル:** 4 ファイル、+202/-8。`crates/uv-cache/src/removal.rs`（+65/-8）と、新規テストの `cache_long_paths.rs`（+131）である。

**次のアクション:** zanieb のレビューへの対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix(resolver): normalize trailing slash on remote index URLs during lockfile validation](https://github.com/astral-sh/uv/pull/20638) `#20638`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv sync --locked` や `uv lock --check` によるロックファイル検証が、末尾のスラッシュだけが違うリモートインデックス URL を別のインデックスとして扱い、不要な再解決を招く問題への修正である。

**直近のアクティビティ:** Sanjays2402 がレビューコメントを付けている。codspeed-hq[bot]はフレームグラフを生成できなかったと報告している。

**変更ファイル:** 3 ファイル、+112/-26。`crates/uv-resolver/src/lock/mod.rs`（+106/-17）が中心である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Suggest a newer patch release when an unbuilt Python version is requested](https://github.com/astral-sh/uv/pull/21046) `#21046`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv venv --python 3.11.0` が「No interpreter found for Python 3.11.0」という一般的なエラーで失敗する問題への対応である。実際の理由は python-build-standalone が `3.11.0` のような初期のパッチリリースをビルドしていないことにあるため、より新しいパッチリリースを提案する。Issue `#16869` を解決する。

**直近のアクティビティ:** yashs33244 が、`#16869` で zanieb が提案した新しいパッチの案内を実装したとして確認を求めている。copilot-pull-request-reviewer[bot]は依頼者がクォータ上限に達したためレビューできなかったと返している。

**変更ファイル:** 4 ファイル、+55/-2。`crates/uv-python/src/downloads.rs`（+33）が中心である。

**次のアクション:** zanieb によるレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Give a specific error when a PEP 723 closing tag has trailing whitespace](https://github.com/astral-sh/uv/pull/20944) `#20944`
**ステータス:** Open | **ラベル:** なし

**概要:** PEP 723 のブロックが `# /// ` のように末尾に空白を含む形で閉じられていると、閉じタグを見つけられず「opening tag without a closing tag」という一般的なエラーになり、真の原因を示せない問題への対応である。Issue `#10918` を解決する。

**直近のアクティビティ:** yashs33244 が、`#10918` で charliermarsh が言及した、より良いエラーメッセージを末尾空白のケースについて追加したとして確認を求めている。copilot-pull-request-reviewer[bot]はエラーによりレビューできなかったと返している。

**変更ファイル:** `crates/uv-scripts/src/lib.rs` の 1 ファイルのみ、+20/-0。

**次のアクション:** charliermarsh によるレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Fix uv init layout for stubs packages](https://github.com/astral-sh/uv/pull/19671) `#19671`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv init --package foo-stubs` が `uv_build` の想定するスタブ専用パッケージの構成を生成するようにする PR である。`src/foo-stubs/__init__.pyi` を作り、スタブ専用パッケージには実行時のコンソールスクリプトのエントリポイントを作らない。Issue `#19663` を修正する。

**直近のアクティビティ:** teamleaderleo が他のバックエンドを調べた結果を共有し、Poetry はソースルートの指定が必要で、Flit は 4 でスタブ専用パッケージに対応していると報告している。zanieb もレビューコメントを付けている。

**変更ファイル:** 2 ファイル、+78/-3。`crates/uv/src/commands/project/init.rs`（+23/-3）とテストである。

**次のアクション:** レビュー対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [Use mimalloc v3](https://github.com/astral-sh/uv/pull/21014) `#21014`
**ステータス:** Open | **ラベル:** なし

**概要:** メモリアロケータを mimalloc v3 へ更新する PR である。Ruff 側の同種の変更と同じ内容で、Issue `#19189` をクローズする。

**直近のアクティビティ:** zanieb が、Ruff と uv は大きく異なるためこのプロジェクト向けのベンチマークが必要だと述べ、過去の調査では説得力がなく、設定では相殺しにくいメモリ使用量の退行が見られたと `#19743` を挙げて説明した。chirizxc は自分の環境での Ruff のメモリ使用量について反論している。charliermarsh は APPROVED を出している。

**変更ファイル:** `crates/uv-performance-memory-allocator/Cargo.toml` の 1 ファイルのみ、+1/-1。

**次のアクション:** uv 向けのベンチマークによる検証が求められている。

---

### ![PR](icons/git-pull-request.svg) [Stabilize publish index readiness checks](https://github.com/astral-sh/uv/pull/21050) `#21050`
**ステータス:** Open | **ラベル:** なし

**概要:** アップロード直後の TestPyPI が一時的に整合しない simple-index の応答を返すことがあり、publish のスモークテストが失敗しうる問題への対応である。現在の `wait_for_index` は両方の成果物を含む最初の応答で先へ進むが、後続の `uv publish --check-url` が依然として失敗しうる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `scripts/publish/test_publish.py` の 1 ファイルのみ、+24/-14。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Temporarily disable Artifactory registry integration](https://github.com/astral-sh/uv/pull/21048) `#21048`
**ステータス:** Open | **ラベル:** ci-flake

**概要:** Artifactory レジストリ連携が資格情報の更新を要するため 7 月 31 日から `401 Unauthorized` で失敗し続けている問題への暫定対応である。必須レジストリの一覧と両方の認証バックエンドから Artifactory を一時的にコメントアウトし、他の構成の検証を続けられるようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル、+10/-7。`.github/workflows/test-integration.yml`（+8/-6）と `scripts/registries-test.py` である。

**次のアクション:** 資格情報の更新後に元へ戻す必要がある。

---

### ![PR](icons/git-pull-request.svg) [Disable profiling for wall-time CodSpeed benchmarks](https://github.com/astral-sh/uv/pull/21049) `#21049`
**ステータス:** Open | **ラベル:** なし

**概要:** 実時間ベンチマークのワークフローが CodSpeed のプロファイラでフレームグラフを収集しており、ジョブの所要時間を延ばしうるため、実時間の実行ではこの任意のプロファイリングを無効にして CI 時間への影響を確認する PR である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/bench.yml` の 1 ファイルのみ、+2/-0。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [chore: add Hermes Agent configuration](https://github.com/astral-sh/uv/pull/21013) `#21013`
**ステータス:** Closed | **ラベル:** bot:ai-policy-comment

**概要:** 既存の `.cursor/` や `.claude/` と同様に、AI エージェント向けのガイドラインと自動整形フックを収めた `.hermes/` を追加する PR である。

**直近のアクティビティ:** woodruffw が Ruff 側の同種の PR に触れ、長期的にはエージェント設定の種類を増やすのではなく減らしたいと述べた。astral-sh-bot[bot]は、コーディングでの AI 利用は認めるがメンテナとのやり取りに使うべきではないとして AI ポリシーの確認を求めている。クローズされている。

**変更ファイル:** 3 ファイル、+120/-0。`.hermes/rules.md`（+92）、`hooks.json`（+25）、`README.md` である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Update actions/setup-python action to v7](https://github.com/astral-sh/uv/pull/20874) `#20874`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `actions/setup-python` の v6.3.0 から v7.0.0 へのメジャー更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 8 ファイル、+33/-33。各ワークフロー定義の参照を更新している。

**次のアクション:** メジャー更新のため、影響確認のうえマージ判断が必要である。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate toml to v1.1.4](https://github.com/astral-sh/uv/pull/21028) `#21028`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `toml` の 1.1.3+spec-1.1.0 から 1.1.4 へのパッチ更新である。

**直近のアクティビティ:** EliteTK が、報告されていた 7% の性能低下は再現できず、実際には 1% 程度だとコメントした。TOML 解析自体の低下幅はより大きいが、既に実行時間の大きな割合を占めていないとも述べている。

**変更ファイル:** `Cargo.lock` の 1 ファイルのみ、+4/-4。

**次のアクション:** 性能影響の確認を経てマージ判断となる。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate toml_parser to v1.1.3](https://github.com/astral-sh/uv/pull/21029) `#21029`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `toml_parser` の 1.1.2+spec-1.1.0 から 1.1.3 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock` の 1 ファイルのみ、+2/-2。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate hyper to v1.11.0](https://github.com/astral-sh/uv/pull/20903) `#20903`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `hyper` の 1.10.1 から 1.11.0 へのマイナー更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock` の 1 ファイルのみ、+11/-11。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate base64 to 0.23.0](https://github.com/astral-sh/uv/pull/20901) `#20901`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `base64` の 0.22.1 から 0.23.0 へのマイナー更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル、+5/-5。`Cargo.lock` と `Cargo.toml` である。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate async-compression to v0.4.43](https://github.com/astral-sh/uv/pull/20888) `#20888`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `async-compression` の 0.4.19 から 0.4.43 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock` の 1 ファイルのみ、+32/-16。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate async-trait to v0.1.91](https://github.com/astral-sh/uv/pull/20703) `#20703`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `async-trait` の 0.1.89 から 0.1.91 へのパッチ更新である。0.1.92 が保留として控えている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock` の 1 ファイルのみ、+3/-3。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate test-log to v0.2.21](https://github.com/astral-sh/uv/pull/19721) `#19721`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `test-log` の 0.2.20 から 0.2.21 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock` の 1 ファイルのみ、+6/-6。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update taiki-e/install-action action to v2.85.7](https://github.com/astral-sh/uv/pull/21030) `#21030`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `taiki-e/install-action` の v2.85.6 から v2.85.7 へのパッチ更新である。v2.85.11 が保留として控えている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 6 ファイル、+10/-10。各ワークフロー定義の参照を更新している。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update digital-blueprint/gitlab-pipeline-trigger-action action to v1.4.3](https://github.com/astral-sh/uv/pull/20886) `#20886`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `digital-blueprint/gitlab-pipeline-trigger-action` の v1.4.0 からのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/ci.yml` の 1 ファイルのみ、+1/-1。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update aws-actions/configure-aws-credentials action to v6.2.3](https://github.com/astral-sh/uv/pull/20884) `#20884`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `aws-actions/configure-aws-credentials` の v6.2.2 から v6.2.3 へのパッチ更新である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test-integration.yml` の 1 ファイルのみ、+1/-1。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update open-security-tools/ost-simple-sts action to v0.0.4](https://github.com/astral-sh/uv/pull/21020) `#21020`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `open-security-tools/ost-simple-sts` の v0.0.2 から v0.0.4 へのパッチ更新である。

**直近のアクティビティ:** ewdurbin が APPROVED を出してマージされた。

**変更ファイル:** `.github/workflows/sync-python-releases.yml` の 1 ファイルのみ、+1/-1。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Interpreter discovery aborts on a PATH candidate that fails to execute (pyenv shim, exit 127) instead of skipping it](https://github.com/astral-sh/uv/issues/21047) `#21047`
**ステータス:** Open | **ラベル:** なし

**概要:** インタプリタの探索中に `PATH` 上の `python` が実行に失敗すると、他の使えない候補と同様に読み飛ばされるのではなく、探索全体が致命的エラーで中断されるという報告である。探索の早い段階に壊れた実行ファイルが 1 つあるだけで全体が止まる。

**直近のアクティビティ:** michaelpaesold が、回避策として pyenv に python を提供する別バージョンへのフォールバックを設定する方法を挙げている。あわせて、その分析は Claude Code によるもので自分は専門家ではないと率直に断りつつ、切り分けへの協力は申し出ている。

**次のアクション:** 実行に失敗した候補を読み飛ばす挙動への修正が必要である。

---

### ![Issue](icons/issue-opened.svg) [--find-links with a relative path in a requirements file fails with "relative URL without a base" (regression since 0.12.x)](https://github.com/astral-sh/uv/issues/21016) `#21016`
**ステータス:** Open | **ラベル:** bug

**概要:** requirements ファイル内の `--find-links` に相対パスを書くと解決に失敗するという回帰報告である。`./` の有無や、`uv pip compile` と `uv pip install -r` のどちらを使うかにかかわらず `Invalid URL` になる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 0.12.x 以降の回帰として調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Double `requires-python` exclusion gets ignored](https://github.com/astral-sh/uv/issues/21036) `#21036`
**ステータス:** Closed | **ラベル:** bug

**概要:** Python 3.10 と 3.13 のみを対象とするために `requires-python` へ `>=3.10, !=3.11.*, !=3.12.*, <3.14` と書いたところ、`uv.lock` が `>=3.10, <3.14` へ単純化してしまうという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。PR `#21045` により解決してクローズされている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [uv venv creates the same python.exe and pythonw.exe](https://github.com/astral-sh/uv/issues/19226) `#19226`
**ステータス:** Open | **ラベル:** bug

**概要:** uv が導入した Python で `python.exe` と `pythonw.exe` の内容が同一になるという報告である。報告者は両ファイルの sha256 が一致することを示している。

**直近のアクティビティ:** dertzi が、手動の `uv venv` に限らず、プログラムから生成した仮想環境でも再現すると報告している。

**次のアクション:** 再現条件が広がったため、調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [UV aborts package download on linux. works on windows](https://github.com/astral-sh/uv/issues/18735) `#18735`
**ステータス:** Open | **ラベル:** bug

**概要:** Linux で requirements からの導入時に、ホイールのダウンロードが約 120 秒でタイムアウトするという報告である。報告者はファイル自体は関係ないとみている。

**直近のアクティビティ:** gpadiolleau が、`tool.uv.sources` で PyTorch のインデックスを指定して `torch` を取得する際に同じ問題に遭遇したと報告している。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [How to make simple API responses cachable with etags](https://github.com/astral-sh/uv/issues/21039) `#21039`
**ステータス:** Closed | **ラベル:** question

**概要:** devpi-server で Simple API の応答に ETag 対応を追加しようとしたところ、uv が `WARN Server returned unusable 304` を出すという質問である。

**直近のアクティビティ:** fschulze が、304 応答にヘッダが欠けていたことが原因だったとして、200 と 304 の双方で `ETag` と `Cache-Control: must-revalidate` を返すようにしたところ問題なく動くようになったと報告し、解決している。woodruffw は `cached_client.rs` の実装から推測される原因を尋ねていた。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Consider using the system SSL library, i.e., OpenSSL instead of `rusttls` / `ring`](https://github.com/astral-sh/uv/issues/11595) `#11595`
**ステータス:** Open | **ラベル:** enhancement, compatibility

**概要:** `native-tls` フラグがネイティブの SSL を有効にすると思われがちだが、実際には rustls とその依存の ring でネイティブ証明書を読み込むだけであり、機能が限られるという指摘である。

**直近のアクティビティ:** insanitybit が、コンプライアンス上の義務があり OpenSSL の設定を標準化したいとして、対応する暗号スイートを明示できることの重要性を述べた。tiran は自身の概念実証を正式な PR にする用意があるとしつつ、Rust の初心者なので CI と作法について助力が必要だと申し出ている。michael-o は FreeBSD の移植担当者に関心があるかもしれないと言及した。

**次のアクション:** 方針の議論が続いている。

---

### ![Issue](icons/issue-opened.svg) [uv add/sync not respecting --active flag?](https://github.com/astral-sh/uv/issues/17780) `#17780`
**ステータス:** Open | **ラベル:** question

**概要:** Dev Container のビルド過程で使う Dockerfile 内で、`VIRTUAL_ENV` が未設定などの条件下において uv が `--active` フラグを尊重しないように見えるという報告である。

**直近のアクティビティ:** Xueziq が、既に有効化済みの仮想環境の内側で `uv add --active` を使うと、その仮想環境が予期せず作り直され、異なる Python バージョンが使われると報告している。

**次のアクション:** 挙動の切り分けが必要である。

---

### ![Issue](icons/issue-opened.svg) [Is it possible to add the option to rename a project to UV?](https://github.com/astral-sh/uv/issues/11795) `#11795`
**ステータス:** Open | **ラベル:** enhancement, wish

**概要:** プロジェクト名を変更したい場合に `pyproject.toml`、`src/project_name` フォルダ、内部の import をすべて直す必要があるとして、リネーム用のオプションを求める要望である。

**直近のアクティビティ:** tanishku が新規貢献者として着手できる Issue を尋ねたのに対し、woodruffw は、この件は自明でなく uv の多くの箇所に触れるため初めての貢献には向かないと回答している。

**次のアクション:** 実装の要否を含め検討段階である。

---

### ![Issue](icons/issue-opened.svg) [Accept `pylock.toml` as a constraint file](https://github.com/astral-sh/uv/issues/13031) `#13031`
**ステータス:** Open | **ラベル:** enhancement, cli

**概要:** `uv pip install flask -c pylock.toml` のように `pylock.toml` を制約ファイルとして受け付けてほしいという要望である。

**直近のアクティビティ:** notatallshaw が、自身で依存解決器を開発する中で、一度ロックして使い回せる点から `#13060` のほうがより正しい方針だという結論に至りつつあるとコメントしている。

**次のアクション:** 方針の議論が続いている。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate が自動生成、更新している依存関係ダッシュボードである。本文はプラットフォームの制限で切り詰められている旨の注記を含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 自動管理のため個別の対応は不要である。

---
## microsoft/vscode

> 直近 24 時間は Agent Host まわりの大規模なリファクタと worktree 関連の修正が集中し、あわせて SimonSiefke による各ビューのメモリリーク修正が続いている。1.132.0 リリース後のチャットとコミットメッセージ生成に関する不具合報告も多い。
>
> **注記:** 取得上限（PR 50 件 / Issue 50 件）に達しているため、本セクションは `updated_at` の新しい順に 100 件のみを収録している。

### ![PR](icons/git-pull-request.svg) [agentHost: make the orchestrator own session enumeration and chat lifecycle](https://github.com/microsoft/vscode/pull/329633) `#329633`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host のマルチチャット所有権リファクタを完了させる PR である。セッションの同一性、永続レジストリ、列挙、チャットカタログ、ライフサイクルの順序、復元、削除を Agent Host が一元的に持つようにする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿し、anthonykim1 のレビューは DISMISSED となっている。

**変更ファイル:** 60 ファイル、+12989/-4611。`MULTI_CHAT_ARCHITECTURE.md` の追加を含む、Agent Host 全体に及ぶ大規模な変更である。

**次のアクション:** 規模が大きくレビューの継続が必要である。

---

### ![PR](icons/git-pull-request.svg) [sessions: add worktree merge changeset operation](https://github.com/microsoft/vscode/pull/330242) `#330242`
**ステータス:** Open | **ラベル:** なし

**概要:** worktree セッション向けにネイティブの Agent Host `Merge Changes` 操作を追加する PR である。親のチェックアウトへ安全にマージし、既存のマージ状態を保ちつつブランチの変化に追随する。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で TylerLeonhardt へ通知し、copilot-pull-request-reviewer[bot]が安全性検査を含む概要レビューを投稿している。

**変更ファイル:** 40 ファイル、+1344/-80。`agentHostChangesetOperationService.ts`、`agentHostGitService.ts`、`sessionState.ts` などである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Improve copy and paste architecture across agent and editor window](https://github.com/microsoft/vscode/pull/329455) `#329455`
**ステータス:** Open | **ラベル:** なし

**概要:** エージェントウィンドウとエディタウィンドウをまたぐコピーとペーストの構造を改善する PR である。PR 本文はテンプレートのままとなっている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]はエラーによりレビューできなかったと返している。dmitrivMS がレビューコメントを付けている。

**変更ファイル:** 34 ファイル、+1399/-349。`src/vs/base/common/network.ts` や `src/vs/sessions/` 配下に及ぶ。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Update to ESLint 10](https://github.com/microsoft/vscode/pull/330197) `#330197`
**ステータス:** Open | **ラベル:** なし

**概要:** ESLint を 10 へ更新し、新しいルールと機能に合わせてコードを調整する PR である。オプショナルチェーンの用法の更新と context のメソッド呼び出しの修正を含み、直接の問題は生じていないとしている。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で lszomoru へ通知し、copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 36 ファイル、+801/-467。`.eslint-plugin-local/` 配下のローカルルールが中心である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [agentHost: watch every git repo root for multi-root changeset freshness](https://github.com/microsoft/vscode/pull/330172) `#330172`
**ステータス:** Open | **ラベル:** なし

**概要:** マルチルートワークスペースで、副次フォルダ内のファイルを編集してもセッション一覧の変更量チップが更新されない問題への対応である。変更セットのファイル監視が一部のルートにしか張られていなかったため、すべての git リポジトリルートを監視する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 3 ファイル、+596/-68。`agentHostChangesetCoordinator.ts` と `agentHostChangesetFileMonitorCoordinator.ts` である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Add cache-aware history manager for customendpoint providers](https://github.com/microsoft/vscode/pull/330232) `#330232`
**ステータス:** Open | **ラベル:** なし

**概要:** 自己ホストの OpenAI 互換エンドポイント（DeepSeek、DS4、ディスク KV を持つ vLLM や llama.cpp）向けに、キャッシュを意識した会話履歴マネージャを追加する PR である。プロンプトの接頭辞をバイト単位で保つ設計としている。

**直近のアクティビティ:** microsoft-github-policy-service[bot]が stevescot に対して CLA への同意を求めている。copilot-pull-request-reviewer[bot]も概要レビューを投稿している。

**変更ファイル:** 4 ファイル、+521/-1。新規の `cacheAwareHistoryManager.ts` とそのテストが中心である。

**次のアクション:** CLA への対応とレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [agentHost: perf: stop re-deriving worktree roots on every listing](https://github.com/microsoft/vscode/pull/329932) `#329932`
**ステータス:** Closed | **ラベル:** なし

**概要:** Agents ウィンドウの再読み込みでセッション情報が失われうる問題への性能改善である。Issue `#329880` を修正する。`#329925` の代替案であり、両者を並べて比較したうえで一方を採用すべきだと本文に明記されている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が、git のサブプロセス呼び出しの繰り返しを避ける最適化として概要レビューを投稿している。クローズされている。

**変更ファイル:** 9 ファイル、+724/-77。`agentHostGitService.ts` と `worktreeIsolation.ts` が中心である。

**次のアクション:** クローズ済みである。競合案との比較の結果、採用されなかった。

---

### ![PR](icons/git-pull-request.svg) [agentHost: resolve primary worktree roots without spawning git](https://github.com/microsoft/vscode/pull/329925) `#329925`
**ステータス:** Closed | **ラベル:** なし

**概要:** セッション列挙時に git のサブプロセスを繰り返し起動する処理を置き換える PR である。Issue `#329880` を修正する。本文には、競合する実装が存在しそちらのほうが良い選択肢になりうるという注意書きが置かれている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。クローズされている。

**変更ファイル:** 8 ファイル、+465/-42。`agentHostGitService.ts` と `worktreeIsolation.ts` が中心である。

**次のアクション:** クローズ済みである。

---

### ![PR](icons/git-pull-request.svg) [agentHost: fix: never treat a git directory as a repository root](https://github.com/microsoft/vscode/pull/329949) `#329949`
**ステータス:** Closed | **ラベル:** なし

**概要:** サブモジュールのチェックアウトから作った worktree 分離セッションで、worktree がリポジトリ自身の git ディレクトリ（`.git/modules/...`）の内側に作られてしまう問題への修正である。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が、Agent Host が git のメタデータディレクトリをリポジトリとして扱わないようにする変更として概要レビューを投稿している。クローズされている。

**変更ファイル:** 3 ファイル、+100/-11。`agentService.ts` と `worktreeIsolation.ts`、そのテストである。

**次のアクション:** クローズ済みである。

---

### ![PR](icons/git-pull-request.svg) [agentHost: use model-specific Codex reasoning efforts](https://github.com/microsoft/vscode/pull/330220) `#330220`
**ステータス:** Open | **ラベル:** なし

**概要:** Codex ハーネスの thinking レベル選択が、すべてのモデルに固定の一覧を当てはめていた問題への修正である。各モデルが公表する推論能力に基づいて選択肢を出すようにする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 6 ファイル、+132/-17。`reasoningEffort.ts`、`codexAgent.ts`、`codexSessionConfigKeys.ts` とテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Handle unpushed commits in Agent Host PR lookup](https://github.com/microsoft/vscode/pull/330224) `#330224`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host の PR 検索で、未プッシュのコミットに対して GitHub が返す 422 `No commit found for SHA` を想定内の応答として扱う修正である。Issue `#330072` を修正し、VS Code Sweeper のレビューを起点としている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 2 ファイル、+76/-2。`agentHostOctoKitService.ts` とそのテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in run and debug view](https://github.com/microsoft/vscode/pull/330233) `#330233`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** 実行とデバッグのビューをパネルへ移動して位置をリセットすると、イベントリスナが残って古いビューがメモリに保持される問題への修正である。プログレス関連のスコープを破棄する。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で benibenj へ通知した。copilot-pull-request-reviewer[bot]が概要レビューを投稿し、SimonSiefke もレビューコメントを付けている。

**変更ファイル:** 2 ファイル、+156/-5。`compositePart.ts` とそのテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in search view](https://github.com/microsoft/vscode/pull/330240) `#330240`
**ステータス:** Open | **ラベル:** なし

**概要:** 検索ビューを別の位置へ移動すると、composite のプログレスリスナから古いビューへ到達可能なまま残る問題への修正である。あわせて、破棄されていなかったコンテキスト行のトグルも破棄する。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で benibenj へ通知した。copilot-pull-request-reviewer[bot]が概要レビューを投稿し、SimonSiefke もレビューコメントを付けている。

**変更ファイル:** 4 ファイル、+207/-9。`compositePart.ts`、`searchWidget.ts` と各テストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in source control view](https://github.com/microsoft/vscode/pull/330241) `#330241`
**ステータス:** Open | **ラベル:** なし

**概要:** ソース管理ビューをアクティビティバーとパネルの間で移動すると、古いビューがメモリに残る問題への修正である。composite が取り除かれる際にプログレスのオブジェクトを破棄する。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で benibenj へ通知した。copilot-pull-request-reviewer[bot]が概要レビューを投稿し、SimonSiefke もレビューコメントを付けている。

**変更ファイル:** 3 ファイル、+157/-6。`compositePart.ts`、`scmViewPane.ts` とテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Revert new TAS endpoint changes](https://github.com/microsoft/vscode/pull/330175) `#330175`
**ステータス:** Merged | **ラベル:** candidate, release-cherry-pick

**概要:** `#329653` と `#329736` を差し戻し、VS Code 本体と Copilot 拡張の双方について新しい TAS assignments エンドポイント（`/api/v1/assignments`）の統合を撤回する PR である。

**直近のアクティビティ:** vs-code-engineering[bot]が、マージ時に `release/1.133` へ自動で cherry-pick される旨を通知した。DonJayamanne が APPROVED を出してマージされている。

**変更ファイル:** 17 ファイル、+104/-672。`copilotToken.ts`、`baseTelemetryService.ts` と各 package 定義である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Revert new TAS endpoint changes](https://github.com/microsoft/vscode/pull/330180) `#330180`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** `#330175` を `main` から cherry-pick したものである。内容は TAS assignments エンドポイント統合の差し戻しである。

**直近のアクティビティ:** vijayupadya が APPROVED を出してマージされている。

**変更ファイル:** 17 ファイル、+104/-672。`#330175` と同一である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [chore: bump electron@42.8.1](https://github.com/microsoft/vscode/pull/330244) `#330244`
**ステータス:** Open | **ラベル:** なし

**概要:** Electron ランタイムを 42.8.1 へ更新する PR である。同梱の Node.js も更新される。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 7 ファイル、+96/-96。`.npmrc`、`build/checksums/electron.txt`、`nodejs.txt`、`cgmanifest.json` などである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [chore: bump @github/copilot to 1.0.79](https://github.com/microsoft/vscode/pull/330228) `#330228`
**ステータス:** Open | **ラベル:** copilot-sdk-auto-upgrade

**概要:** `@github/copilot` を 1.0.79-9 から 1.0.79 へ更新する自動 PR である。

**直近のアクティビティ:** vs-code-engineering[bot]が製品ビルドをキューに入れ、サイクル 1 で成功したと報告している。copilot-pull-request-reviewer[bot]は完全なエージェント検証を実行できなかったと注記している。

**変更ファイル:** 4 ファイル、+74/-74。`package.json` と `package-lock.json`（本体と remote）である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Enhance modern UI with uppercase view headers](https://github.com/microsoft/vscode/pull/330238) `#330238`
**ステータス:** Open | **ラベル:** なし

**概要:** Modern UI のビューヘッダとタブについて、従来の大文字表記を復元できるようにする PR である。ビューヘッダやサイドバーの見た目を利用者が調整できる設定を追加する。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で benibenj へ通知し、copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 5 ファイル、+117/-9。`workbench.contribution.ts`、`fontRamp.css`、`styleOverrides.contribution.ts` とテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Refactor floating panel margins for UI consistency](https://github.com/microsoft/vscode/pull/330236) `#330236`
**ステータス:** Open | **ラベル:** なし

**概要:** フローティングパネルのレイアウトについて、カード間の間隔を 6px から 4px へ変更し、水平マージンの計算方法を整理する PR である。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で benibenj へ通知し、copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 6 ファイル、+94/-45。`floatingPanels.css`、`activitybarPart.ts`、`paneCompositePart.ts`、`layoutService.ts` などである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [feat: model metadata and pricing support for BYOK models](https://github.com/microsoft/vscode/pull/329400) `#329400`
**ステータス:** Open | **ラベル:** なし

**概要:** BYOK のカスタムエンドポイントについて、`description`、`icon`、`maxTokens` といったモデルのメタデータと価格情報に対応する PR である。Issue `#325237` の 4 本のうち最後の 1 本にあたる。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 3 ファイル、+59/-5。`byokProvider.ts`、`customEndpointProvider.ts`、`package.json` である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix: show Preview as icon in turn changes summary](https://github.com/microsoft/vscode/pull/328582) `#328582`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** 展開したターン変更のファイル一覧に青い「Preview」というテキストリンクが表示されていた問題への修正である。`#324929` はラベルなしのアイコン操作を意図していたが、そうなっていなかった。

**直近のアクティビティ:** benibenj がマージ競合の解消と before/after のスクリーンショットを求め、cipheraxat が競合を解消して push したうえで比較画像を添えて応じている。copilot-pull-request-reviewer[bot]も概要レビューを投稿している。

**変更ファイル:** 9 ファイル、+94/-21。`chatChangesSummaryPart.ts` と PR 用の画像アセットである。

**次のアクション:** benibenj による再確認が必要である。

---

### ![PR](icons/git-pull-request.svg) [fix: keep markdown preview copy button pinned during horizontal scroll](https://github.com/microsoft/vscode/pull/329808) `#329808`
**ステータス:** Open | **ラベル:** なし

**概要:** Markdown プレビューのコードブロックに付くコピーボタンが、スクロールコンテナである `<pre>` の内側に置かれていたため、横スクロール時に固定されない問題への修正である。Issue `#329805` を修正する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 2 ファイル、+22/-5。`markdown.css` と `preview-src/index.ts` である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix: guard stale showEndForLine index in sticky scroll rendering (fixes #325642)](https://github.com/microsoft/vscode/pull/325646) `#325646`
**ステータス:** Open | **ラベル:** errors-fix, *error-fix-driving

**概要:** スティッキースクロールのウィジェット描画中に `ViewModelLinesFromProjectedModel.modelPositionIsVisible` から `TypeError: Cannot read properties of undefined (reading 'isVisible')` が投げられる問題への修正である。

**直近のアクティビティ:** vs-code-engineering[bot]が、保護対象ファイルの変更を含むため PR ではなく代替のレビュー用 Issue（`#330246` と `#330235`）が作られた旨を 2 度通知している。copilot-pull-request-reviewer[bot]は bot が作成した PR を自動レビューできないと返している。

**変更ファイル:** `src/vs/editor/contrib/stickyScroll/browser/stickyScrollWidget.ts` の 1 ファイルのみ、+1/-1。

**次のアクション:** 保護対象ファイルの扱いのため、人手でのレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump actions/setup-node from 6.5.0 to 7.0.0](https://github.com/microsoft/vscode/pull/329352) `#329352`
**ステータス:** Open | **ラベル:** dependencies, github_actions

**概要:** dependabot による `actions/setup-node` の 6.5.0 から 7.0.0 への更新である。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が、完全なエージェント検証を実行できなかったと注記しつつ概要レビューを投稿した。lramos15 が APPROVED を出している。

**変更ファイル:** 13 ファイル、+25/-25。各 GitHub Actions ワークフローの参照更新である。

**次のアクション:** マージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump actions/setup-node from 6.5.0 to 7.0.0](https://github.com/microsoft/vscode/pull/326144) `#326144`
**ステータス:** Open | **ラベル:** dependencies, github_actions

**概要:** `#329352` と同内容の dependabot による `actions/setup-node` の更新 PR である。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]は bot が作成した PR を自動レビューできないと返し、TylerLeonhardt のレビューは DISMISSED となっている。

**変更ファイル:** 13 ファイル、+25/-25。各ワークフローの参照更新である。

**次のアクション:** 重複する `#329352` との整理が必要である。

---

### ![Issue](icons/issue-opened.svg) [Agent: submit / act on feedback buttons broken in 1.131.0](https://github.com/microsoft/vscode/issues/328359) `#328359`
**ステータス:** Open | **ラベル:** info-needed, agents-window

**概要:** 1.131.0 でフィードバックを submit してもエージェントが動かないという回帰報告である。報告環境は macOS である。

**直近のアクティビティ:** stevenh が、1.132.0 では改善したものの依然として失敗し、前夜も 2 つのセッションが固まったと報告した。benibenj は新しいリリースで試すよう促し、駄目な場合は動画の提供を求めている。

**次のアクション:** 再現手順と動画の提供を待つ状態である。

---

### ![Issue](icons/issue-opened.svg) [Agent: act on feedback processes all comments](https://github.com/microsoft/vscode/issues/328360) `#328360`
**ステータス:** Open | **ラベル:** polish, agents-window

**概要:** フィードバックへの対応時に、対象を絞らず全コメントを処理してしまうという指摘である。

**直近のアクティビティ:** benibenj が、良い指摘であり対応を検討すると応じている。

**次のアクション:** 対応方針の検討段階である。

---

### ![Issue](icons/issue-opened.svg) [Webview resources stream corrupted (every 256 KiB chunk delivered twice) when a second-instance CLI folder open lands during startup; corrupted response is then cached](https://github.com/microsoft/vscode/issues/330243) `#330243`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 1.123.0（Chromium 148）以降、起動中に 2 つ目のインスタンスから CLI でフォルダを開くと、そのウィンドウの webview が `https://file+.vscode-resource.vscode-cdn.net/...` 経由で読み込むリソースがすべて壊れるという報告である。256 KiB のチャンクが二重に配信され、壊れた応答がそのままキャッシュされる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Long commands are truncated](https://github.com/microsoft/vscode/issues/326286) `#326286`
**ステータス:** Open | **ラベル:** なし

**概要:** モデルが実行しようとしているコマンドライン全体を以前は確認できたのに、現在は一部しか見えないという報告である。部分的にしか見えないコマンドをそのまま実行しようとする点も指摘されている。

**直近のアクティビティ:** pmarkus が、サンドボックス外でコマンドを実行しようとする場合にのみ起きるように見えると画像付きで述べ、1.132.0 でも同じ問題が出ていると報告している。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Chat: Support contributable chat context resources](https://github.com/microsoft/vscode/issues/271104) `#271104`
**ステータス:** Open | **ラベル:** feature-request, api, api-proposal, chat

**概要:** チャットに添付できるコンテキストリソースを拡張側から提供できるようにしてほしいという要望である。例として Pull Request を挙げ、モックを添えている。

**直近のアクティビティ:** lppedd が、自分たちの用途はコーディングではないため利用者がエディタタブを開かずに Copilot を使う傾向があると補足した。また回避策には懐疑的だとして、顧客が多数のメインフレーム接続を設定しうる事情を挙げている。

**次のアクション:** API 提案としての検討が続いている。

---

### ![Issue](icons/issue-opened.svg) [VS Code windows can get stuck with a black screen if 2 simultaneous "new chat response" messages occur](https://github.com/microsoft/vscode/issues/329482) `#329482`
**ステータス:** Open | **ラベル:** windows, editor-rendering

**概要:** 2 つの「New chat response」メッセージがほぼ同時に発生すると、ウィンドウが黒画面のまま固まることがあるという報告である。

**直近のアクティビティ:** sean-mcmanus が再現手順を特定したとして、アクセシビリティのシグナル設定に由来する 2 つのメッセージがほぼ同時に届いたときに起きると deepak1556 へ報告している。

**次のアクション:** 再現手順が判明したため、調査が進められる状態である。

---

### ![Issue](icons/issue-opened.svg) [Custom Endpoint model is missing from Chats agent session model picker](https://github.com/microsoft/vscode/issues/329815) `#329815`
**ステータス:** Open | **ラベル:** なし

**概要:** カスタムエンドポイントのモデルが、チャットのエージェントセッションのモデル選択に現れないという報告である。環境は VS Code 1.132.0 と Windows 11 である。

**直近のアクティビティ:** nicezic が、サードパーティの言語モデル API プロバイダを使った場合にも近い事象を Windows と WSL2 で再現できると報告している。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [github.copilot.git.repository not populated for self-hosted GitHub Enterprise Server remotes with custom domains](https://github.com/microsoft/vscode/issues/330239) `#330239`
**ステータス:** Open | **ラベル:** なし

**概要:** 独自ドメインを使う自己ホスト型 GitHub Enterprise Server のリモートに対して、`github.copilot.git.repository` が設定されないという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [[regression] GitHub Copilot chat up arrow deletes current chat content.](https://github.com/microsoft/vscode/issues/329764) `#329764`
**ステータス:** Open | **ラベル:** なし

**概要:** チャット入力で上矢印キーを押すと、編集中のテキストが過去の送信内容で置き換わるという回帰報告である。

**直近のアクティビティ:** lramos15 が、報告内容を十分に理解できていないとして自身が試した手順を示し、カーソルがプロンプトの先頭へ移動するだけだったと述べている。

**次のアクション:** 再現手順の擦り合わせが必要である。

---

### ![Issue](icons/issue-opened.svg) [Error on generating commit message by copilot](https://github.com/microsoft/vscode/issues/328072) `#328072`
**ステータス:** Open | **ラベル:** なし

**概要:** ソース管理ビューのアイコンを押してもコミットメッセージが生成されず、応答がないという報告である。

**直近のアクティビティ:** venkatesh-webonise が 1.132.0 でも発生するとしつつ、設定から instructions を外すと動作すると報告した。radCircle114514 は公式の解決策が示されないため別の手段を使い始めたと述べている。

**次のアクション:** instructions が関与している可能性が示されており、調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Cursor goes white (on white background)](https://github.com/microsoft/vscode/issues/330208) `#330208`
**ステータス:** Open | **ラベル:** なし

**概要:** コードウィンドウ内でマウスカーソルを動かしていると、白い背景の上でカーソルが白くなり見えなくなることがあるという報告である。

**直近のアクティビティ:** AbdullahHAK が、ハードウェアアクセラレーションを無効にすると解消することからその方面が原因だと示唆し、benqcst はその回避策で問題がすぐ解決したと報告している。

**次のアクション:** ハードウェアアクセラレーション周辺の調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Enable bypassing SSL validation and signature verification failures in integrated browser](https://github.com/microsoft/vscode/issues/330064) `#330064`
**ステータス:** Open | **ラベル:** info-needed

**概要:** 統合ブラウザで SSL 検証と署名検証の失敗を回避できるようにしてほしいという要望である。

**直近のアクティビティ:** ganondev が、Edge ではユーザー証明書の選択ダイアログが出て、それを取り消さないと先へ進めないと述べた。jruales は Chrome や Edge で同じ URL を開いたときの様子を録画して共有するよう求めている。

**次のアクション:** 追加情報の提供を待つ状態である。

---

### ![Issue](icons/issue-opened.svg) [Small Flicker in input box with mcp and harness(?) elements](https://github.com/microsoft/vscode/issues/330065) `#330065`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットの入力ボックスで、MCP やハーネスの要素に関連するとみられる細かなちらつきが出るという報告である。起動時のコマ送りでわずかに 1 フレームだけ確認できたとされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Markdown preview show cached file when switching to another tab and then switching back](https://github.com/microsoft/vscode/issues/271233) `#271233`
**ステータス:** Open | **ラベル:** bug, markdown-ext

**概要:** Markdown のプレビューを開いた状態で別のタブへ切り替えて戻ると、古い内容が表示されるという報告である。

**直近のアクティビティ:** jgillard が、Markdown ファイルを編集するエージェントを使う場面でも同じ問題に遭遇し、更新後の内容をプレビューに反映できないと報告している。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [Add setting to disable code block copy buttons in Markdown preview](https://github.com/microsoft/vscode/issues/330201) `#330201`
**ステータス:** Open | **ラベル:** なし

**概要:** Markdown プレビューのコードブロックに付くコピーボタンを無効にする設定を求める要望である。

**直近のアクティビティ:** yhatt が、Marp for VS Code 拡張では v3.6.1 で既に対処済みだと情報提供している。

**次のアクション:** 設定の追加要否の検討段階である。

---

### ![Issue](icons/issue-opened.svg) [Git: Clone silently does nothing after previously cloned repository is deleted](https://github.com/microsoft/vscode/issues/327092) `#327092`
**ステータス:** Open | **ラベル:** bug

**概要:** 以前クローンしたリポジトリを削除したあと、クローン操作が何の反応もなく終わるという報告である。すべての拡張を無効にしても再現するとされている。

**直近のアクティビティ:** phoehnel が 1.129.1 で同様の症状を確認したとし、あわせて同じリポジトリを UI から二度目にクローンする手段がないことにも触れている。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [[flaky test] Flaky test: Arrays > removeFastWithoutKeepingOrder (src/vs/base/test/common/arrays.test.ts)](https://github.com/microsoft/vscode/issues/330245) `#330245`
**ステータス:** Open | **ラベル:** flaky-test, pr-test-failure-triage

**概要:** `Arrays > removeFastWithoutKeepingOrder` のテストが、既存または共有の CI 失敗のトリアージの一環として無効化されるという記録である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 根本原因の調査と再有効化が必要である。

---

### ![Issue](icons/issue-opened.svg) [Disable flaky test: Arrays > removeFastWithoutKeepingOrder](https://github.com/microsoft/vscode/issues/330246) `#330246`
**ステータス:** Open | **ラベル:** agentic-workflows, test-disabled, pr-test-failure-triage

**概要:** `removeFastWithoutKeepingOrder` のテストを `test.skip(...)` で無効化し、失敗元へのリンクを説明コメントとして添える対応である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 根本原因の調査後に再有効化が必要である。

---

### ![Issue](icons/issue-opened.svg) [[flaky test] Flaky test: Arrays > removeFastWithoutKeepingOrder (src/vs/base/test/common/arrays.test.ts)](https://github.com/microsoft/vscode/issues/330234) `#330234`
**ステータス:** Closed | **ラベル:** flaky-test, pr-test-failure-triage

**概要:** 同じテストについて、`Compile & Hygiene` ジョブの CI 失敗が当該テストで起きているように見えたため無効化したという記録である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [test: disable flaky removeFastWithoutKeepingOrder in arrays.test.ts](https://github.com/microsoft/vscode/issues/330235) `#330235`
**ステータス:** Closed | **ラベル:** agentic-workflows, test-disabled, pr-test-failure-triage

**概要:** 同じテストを `test.skip` で無効化し、発端へのリンクを説明コメントとして添える対応である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [visual studio code are not run because i am using html not run browser](https://github.com/microsoft/vscode/issues/330237) `#330237`
**ステータス:** Open | **ラベル:** info-needed

**概要:** HTML を使っているとブラウザが開かず動作しない、という趣旨の報告である。記述は断片的で、環境は VS Code 1.132.0 と Windows である。

**直近のアクティビティ:** vs-code-engineering[bot]が、基本的な情報が欠けており報告ガイドラインに沿っていないと自動で応答している。

**次のアクション:** 追加情報の提供を待つ状態である。

---

### ![Issue](icons/issue-opened.svg) [Provide first time user affordance to toggle secondary side bar visibility in chat](https://github.com/microsoft/vscode/issues/259855) `#259855`
**ステータス:** Closed | **ラベル:** feature-request, getting-started, workbench-copilot

**概要:** 新規利用者に対し、チャットの既定の表示設定を変更する方法や Copilot を隠す方法を案内するヒントを出すべきかという提案である。

**直近のアクティビティ:** xtqqczze が、パネルの既定の表示状態も設定可能にしてほしいとして別の Issue に言及している。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Unable to connect to tunnel after updating service remotely](https://github.com/microsoft/vscode/issues/324152) `#324152`
**ステータス:** Closed | **ラベル:** *duplicate

**概要:** Remote Tunnel Access のサービスをリモートで更新したあとトンネルへ接続できないという、別の報告に付随して立てられた Issue である。

**直近のアクティビティ:** alexdima が、完全な再現手順とエラー詳細を含む主たる報告が別にあるとして、そちらへ集約する旨を述べている。

**次のアクション:** 重複としてクローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Settings for Python comments and max suggestion length.](https://github.com/microsoft/vscode/issues/317105) `#317105`
**ステータス:** Closed | **ラベル:** *duplicate

**概要:** Copilot のインライン補完について、`#` を打ったあとに補完を出さない設定と、補完の最大長を指定する設定を求める要望である。

**直近のアクティビティ:** hediet が、Copilot の補完を控えめにする取り組みを追う別の Issue で扱われているとして、重複としてクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---
### ![PR](icons/git-pull-request.svg) [Enhance AI network access control for integrated browser](https://github.com/microsoft/vscode/pull/328880) `#328880`
**ステータス:** Open | **ラベル:** security, browser-integration

**概要:** 統合ブラウザについて、AI のネットワークドメインポリシーを適用範囲を広げて強制する PR である。サブリソース、ワーカー、メインフレームのナビゲーション、スクリーンショット、ページ要約、ポップアップ、遅延読み込みのページを対象とする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを 2 件付けている。

**変更ファイル:** 19 ファイル、+2553/-76。`browserView.ts`、`browserSession.ts` と API テストである。

**次のアクション:** セキュリティに関わるためレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [MCP servers UI in the Agents window: make state legible and controls direct](https://github.com/microsoft/vscode/pull/330207) `#330207`
**ステータス:** Open | **ラベル:** なし

**概要:** AI Customizations エディタの MCP サーバ一覧が状態を十分に伝えていなかった問題への対応である。状態が右端の操作欄に色だけのアイコンとして描かれ、しかも一部の状態でしか表示されていなかった。

**直近のアクティビティ:** github-actions[bot]がスクリーンショットの差分レポートを投稿し、copilot-pull-request-reviewer[bot]と ulugbekna がレビューコメントを付けている。

**変更ファイル:** 9 ファイル、+2039/-382。`embeddedMcpServerDetail.ts`、`enablementSwitch.ts`、`AI_CUSTOMIZATIONS.md` などである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [MCP: Preserve Launch Working Directories Across Hosts](https://github.com/microsoft/vscode/pull/330223) `#330223`
**ステータス:** Open | **ラベル:** chat-mcp

**概要:** MCP の既定の作業ディレクトリを、プロセス起動の境界まで URI のまま保持する PR である。これにより、実際にプロセスを動かす OS 上でパスが変換される。Issue `#320167` を修正する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要レビューを投稿している。

**変更ファイル:** 38 ファイル、+1318/-331。`clientPluginCustomizationMeta.ts`、`claudeAgentSession.ts`、`claudeSdkOptions.ts` などである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Fix Agent Host per-turn change attribution](https://github.com/microsoft/vscode/pull/329715) `#329715`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** Agent Host のターンごとの変更サマリーが、ターンの合間に加えられたリポジトリの変更を次のエージェント応答に帰属させてしまう問題への修正である。長期間動かした実セッションで再現したとされている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]と dmitrivMS がレビューコメントを付けている。

**変更ファイル:** 12 ファイル、+867/-32。`agentHostCheckpointService.ts` と `agentHostChangesetService.ts` が中心である。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Feature/alt click close other tabs](https://github.com/microsoft/vscode/pull/328975) `#328975`
**ステータス:** Open | **ラベル:** なし

**概要:** エディタタブの閉じるボタンを Alt を押しながらクリックすると、そのタブ以外をすべて閉じる JetBrains 風の操作を追加する PR である。Issue `#329251` をクローズする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを 2 件付けている。

**変更ファイル:** 5 ファイル、+529/-10。`editorActions.ts`、`multiEditorTabsControl.ts` とテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Preserve relaunch arguments across updates](https://github.com/microsoft/vscode/pull/324275) `#324275`
**ステータス:** Merged | **ラベル:** bug, install-update, windows

**概要:** 更新をまたいで再起動時の引数を保持する PR である。Issue `#322663` を修正する。

**直近のアクティビティ:** dmitrivMS が開発ビルドで再検証したと報告している。copilot-pull-request-reviewer[bot]と deepak1556 もレビューコメントを付けている。マージ済みである。

**変更ファイル:** 4 ファイル、+399/-15。`updateRelaunchArguments.ts`、`updateService.win32.ts`、`build/win32/code.iss` である。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Update grammars](https://github.com/microsoft/vscode/pull/330193) `#330193`
**ステータス:** Merged | **ラベル:** なし

**概要:** 各言語の TextMate 文法定義を最新へ更新する PR である。PR 本文の記述はない。

**直近のアクティビティ:** thernstig が、次のリリースノートで触れる価値があるとコメントしている。benibenj が APPROVED を出してマージされた。

**変更ファイル:** 29 ファイル、+10536/-9789。C++、C# などの `tmLanguage.json` と `cgmanifest.json` である。

**次のアクション:** マージ済みである。リリースノートへの記載が提案されている。

---

### ![PR](icons/git-pull-request.svg) [testing: add TestRunProfile priority](https://github.com/microsoft/vscode/pull/330211) `#330211`
**ステータス:** Open | **ラベル:** なし

**概要:** 互換性のあるプロファイル間で自動選択するための `TestRunProfile.priority` を提案 API として追加する PR である。自動選択は引き続き既定のプロファイルを優先する。Issue `#330204` に関連する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けている。

**変更ファイル:** 7 ファイル、+132/-7。`extensionsApiProposals.ts`、`extHostTesting.ts` とテストである。

**次のアクション:** 提案 API としてのレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in extensions view](https://github.com/microsoft/vscode/pull/330210) `#330210`
**ステータス:** Open | **ラベル:** なし

**概要:** 拡張機能ビューをパネルへ移動して位置をリセットすると、イベントリスナが残って古いビューがメモリに保持される問題への修正である。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で benibenj へ通知し、benibenj が APPROVED を出している。

**変更ファイル:** 2 ファイル、+156/-5。`compositePart.ts` とそのテストである。

**次のアクション:** 承認済みでマージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in references view](https://github.com/microsoft/vscode/pull/330191) `#330191`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `EditorHighlights.dispose()` が可視エディタから装飾を消す一方で `TextEditorDecorationType` の登録を残しており、装飾のルールが保持され続ける問題への修正である。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けている。

**変更ファイル:** `extensions/references-view/src/highlights.ts` の 1 ファイルのみ、+1/-0。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Avoid unhandled rejections in cancellable promise races](https://github.com/microsoft/vscode/pull/330215) `#330215`
**ステータス:** Open | **ラベル:** bug, browser-integration

**概要:** 統合ブラウザの遅延処理を複数回再開したあと、共有プロセスで同一の未捕捉例外が 4 件記録された事象への対応である。キャンセル可能な Promise の競合において未処理の reject が生じないようにする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けている。

**変更ファイル:** 2 ファイル、+94/-3。`src/vs/base/common/async.ts` とそのテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [chat - avoid logging canceled prompt providers as errors](https://github.com/microsoft/vscode/pull/330199) `#330199`
**ステータス:** Merged | **ラベル:** bug, chat

**概要:** 拡張のプロンプトファイルプロバイダから返る想定内の `CancellationError` を、失敗として記録しないようにする PR である。実際の障害は引き続き記録する。

**直近のアクティビティ:** benibenj が APPROVED を出し、copilot-pull-request-reviewer[bot]もレビューコメントを付けてマージされた。

**変更ファイル:** 2 ファイル、+42/-2。`extensionPromptFileService.ts` とテストである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Clarify onboarding prompt options](https://github.com/microsoft/vscode/pull/330186) `#330186`
**ステータス:** Merged | **ラベル:** なし

**概要:** リポジトリに紐づく最初のプロンプト候補について、Issue や Pull Request のタイトルを主たる内容として前面に出し、操作や件数のメタデータの見せ方を揃える PR である。

**直近のアクティビティ:** github-actions[bot]がスクリーンショットの差分レポートを投稿し、benibenj と copilot-pull-request-reviewer[bot]がレビューコメントを付けてマージされた。

**変更ファイル:** 8 ファイル、+78/-16。`newSessionPromptOptions.css`、`SESSIONS.md`、`SKILL.md` などである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Clarify onboarding prompt options](https://github.com/microsoft/vscode/pull/330213) `#330213`
**ステータス:** Open | **ラベル:** cherry-pick-artifact

**概要:** `#330186` を `release/1.133` から cherry-pick したものである。

**直近のアクティビティ:** github-actions[bot]がスクリーンショットの差分レポートを投稿し、benibenj が APPROVED を出している。

**変更ファイル:** 8 ファイル、+78/-16。`#330186` と同一である。

**次のアクション:** 承認済みでマージ待ちである。

---

### ![PR](icons/git-pull-request.svg) [Add surface.border property to various color themes for modern UI support](https://github.com/microsoft/vscode/pull/330216) `#330216`
**ステータス:** Merged | **ラベル:** なし

**概要:** Modern UI の要素との整合を高めるため、複数のカラーテーマへ `surface.border` を導入する PR である。

**直近のアクティビティ:** dmitrivMS が APPROVED を出し、copilot-pull-request-reviewer[bot]もレビューコメントを付けてマージされた。

**変更ファイル:** 12 ファイル、+36/-12。`abyss-color-theme.json`、`dark_vs.json`、`light_modern.json` などである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Add editor.border color variable and update related styles for modern UI](https://github.com/microsoft/vscode/pull/330227) `#330227`
**ステータス:** Open | **ラベル:** なし

**概要:** Modern UI の見た目を整えるため、エディタの境界線用の色変数を新設し、関連するスタイルをその変数へ揃える PR である。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けている。

**変更ファイル:** 4 ファイル、+30/-2。`theme.ts`、`editorBorder.css`、`vscode-known-variables.json` などである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [Modern UI: Refactor tab action background styles for improved hover effects](https://github.com/microsoft/vscode/pull/330209) `#330209`
**ステータス:** Merged | **ラベル:** なし

**概要:** タブ操作の背景についてホバー時の見た目を整理し、各状態のスタイルを改善する PR である。

**直近のアクティビティ:** benibenj が APPROVED を出し、copilot-pull-request-reviewer[bot]もレビューコメントを付けてマージされた。

**変更ファイル:** `src/vs/workbench/contrib/styleOverrides/browser/media/tabs.css` の 1 ファイルのみ、+39/-0。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Fix update tooltip clipping in narrow windows](https://github.com/microsoft/vscode/pull/330200) `#330200`
**ステータス:** Merged | **ラベル:** bug, install-update

**概要:** 幅の狭いウィンドウで更新のツールチップが見切れる問題への修正である。ホバーのコンテナに合わせて縮むようにし、十分な余地があるときは従来の幅を保つ。

**直近のアクティビティ:** benibenj が APPROVED を出し、copilot-pull-request-reviewer[bot]もレビューコメントを付けてマージされた。

**変更ファイル:** `updateTooltip.css` の 1 ファイルのみ、+2/-2。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Add colors to pull request picker](https://github.com/microsoft/vscode/pull/330218) `#330218`
**ステータス:** Merged | **ラベル:** なし

**概要:** Pull Request の選択 UI に色を付ける PR である。PR 本文の記述はない。

**直近のアクティビティ:** benibenj が APPROVED を出し、copilot-pull-request-reviewer[bot]もレビューコメントを付けてマージされた。

**変更ファイル:** 3 ファイル、+28/-1。`pullRequestPicker.css`、`pullRequestPicker.ts` とテストである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Allow to create "local worktree" session from PR for cloud agents too](https://github.com/microsoft/vscode/pull/330205) `#330205`
**ステータス:** Merged | **ラベル:** なし

**概要:** クラウドエージェントについても、Pull Request からローカルの worktree セッションを作れるようにする PR である。PR 本文の記述はない。

**直近のアクティビティ:** dmitrivMS が APPROVED を出し、copilot-pull-request-reviewer[bot]もレビューコメントを付けてマージされた。

**変更ファイル:** 4 ファイル、+16/-9。`pullRequestPicker.ts`、`SESSIONS_LIST.md`、`SKILL.md` などである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Ensure Copilot uses pull request templates](https://github.com/microsoft/vscode/pull/330222) `#330222`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot による Pull Request の説明生成が、リポジトリの PR テンプレートを確実に使うようにする PR である。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けている。

**変更ファイル:** 2 ファイル、+43/-0。`pullRequestDescriptionPrompt.tsx` とそのテストである。

**次のアクション:** レビュー待ちである。関連する不具合が Issue `#330221` として報告されている。

---

### ![PR](icons/git-pull-request.svg) [agentHost: Emit user message telemetry to CTS](https://github.com/microsoft/vscode/pull/329961) `#329961`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host から利用者メッセージのテレメトリを CTS へ送出する PR である。PR 本文はテンプレートのままとなっている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]と amunger がレビューコメントを付けている。

**変更ファイル:** 3 ファイル、+29/-3。`agentHostTelemetryReporter.ts`、`agentSideEffects.ts` とテストである。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [update md editor to latest](https://github.com/microsoft/vscode/pull/330194) `#330194`
**ステータス:** Merged | **ラベル:** なし

**概要:** Markdown エディタのコンポーネントを最新へ更新する PR である。PR 本文はテンプレートのままとなっている。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付け、benibenj のレビューは DISMISSED となっている。マージ済みである。

**変更ファイル:** 5 ファイル、+18/-9。`markdown-editor-src/` 配下の `editor.ts`、`syntaxHighlighter.ts` などである。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump @vscode/markdown-editor from 0.0.2-52 to 0.0.2-54 in /extensions/markdown-language-features](https://github.com/microsoft/vscode/pull/329945) `#329945`
**ステータス:** Closed | **ラベル:** javascript, dependencies

**概要:** dependabot による `@vscode/markdown-editor` の 0.0.2-52 から 0.0.2-54 への更新である。

**直近のアクティビティ:** dependabot[bot]が、対象が既に最新のため不要になったとしてクローズしている。

**変更ファイル:** 2 ファイル、+5/-5。`package.json` と `package-lock.json` である。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) ["Generate with Copilot" will intermittently not use a PR Template](https://github.com/microsoft/vscode/issues/330221) `#330221`
**ステータス:** Open | **ラベル:** なし

**概要:** Pull Request の作成時に「Generate with Copilot」を押しても、PR テンプレートが使われないことが断続的にあるという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR `#330222` で対応が進んでいる。

---

### ![Issue](icons/issue-opened.svg) [Regression: Modern UI rounded corners break edge scrollbar dragging behavior](https://github.com/microsoft/vscode/issues/329329) `#329329`
**ステータス:** Open | **ラベル:** bug, modernization

**概要:** Modern UI の角丸により、画面端でのスクロールバーのドラッグ操作が壊れるという回帰報告である。

**直近のアクティビティ:** hawkticehurst が、副サイドバーを閉じたときのエディタ周辺の隙間のことかと確認し、BobYue-01 がそのとおりだと応じたうえで、この最適化はより広く適用できるはずだと述べている。

**次のアクション:** 対応範囲の検討が続いている。

---

### ![Issue](icons/issue-opened.svg) [Preserve option to keep current UI: The so-called Modern UI is disgusting and objectively inferior](https://github.com/microsoft/vscode/issues/326146) `#326146`
**ステータス:** Open | **ラベル:** modernization

**概要:** Modern UI の外観と操作性が劣るとして、従来の UI を維持する選択肢を残すよう求める要望である。タブが小さくなり、余白が無駄に増えている点を挙げている。

**直近のアクティビティ:** 1101-Grills-Oren が、Modern UI は改善だと考えており、エディタの各部の間隔が広がった点を評価する意見を引用しつつコメントしている。

**次のアクション:** 意見が分かれており、方針の検討が続いている。

---

### ![Issue](icons/issue-opened.svg) [New "Modern UI" broke some of the workbench colorCustomizations](https://github.com/microsoft/vscode/issues/328910) `#328910`
**ステータス:** Closed | **ラベル:** modernization

**概要:** Modern UI により `workbench.colorCustomizations` の一部が効かなくなったという報告である。

**直近のアクティビティ:** mrleemurray が、現在の Insiders と main では修正済みであることを確認したと報告している。報告時のビルドは修正を含む PR より前のものだった。

**次のアクション:** 修正済みのためクローズされている。

---

### ![Issue](icons/issue-opened.svg) [[Agents] Gap above File Explorer list in single-pane view](https://github.com/microsoft/vscode/issues/329910) `#329910`
**ステータス:** Open | **ラベル:** bug, agents-window

**概要:** Agents ウィンドウを単一ペイン表示にしたとき、ファイルエクスプローラの一覧の上に想定外の空白が出るという報告である。再現手順も添えられている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [agents window duplicated file name ?](https://github.com/microsoft/vscode/issues/330008) `#330008`
**ステータス:** Closed | **ラベル:** なし

**概要:** Agents ウィンドウでファイル名が重複して見えるという報告である。

**直近のアクティビティ:** anthonykim1 が `sessions.layout.singlePaneDetailPanel` と `workbench.editor.showTabs` の設定を示し、sandy081 は単一タブモードでは想定どおりの挙動であり、上のラベルはアクティブなエディタタブのタイトルだと説明している。

**次のアクション:** 仕様どおりとしてクローズされている。

---

### ![Issue](icons/issue-opened.svg) [Extensions in custom --extensions-dir are unexpectedly deleted after VS Code updates](https://github.com/microsoft/vscode/issues/322663) `#322663`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, unreleased

**概要:** `--extensions-dir` で独自のディレクトリを指定して起動していると、VS Code の更新後に拡張機能が消えるという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。PR `#324275` により解決してクローズされている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Pop Up window for the delete is appearing only for less than second, unable to delete the files or folders .](https://github.com/microsoft/vscode/issues/330039) `#330039`
**ステータス:** Open | **ラベル:** なし

**概要:** 削除の確認ダイアログが 1 秒未満しか表示されず、ファイルやフォルダを削除できないという報告である。

**直近のアクティビティ:** Shaurav-Vora が自身の 1.132.0 では再現しないと報告し、AmanMehta22 が拡張機能をすべて無効にしても発生するとして詳細を追記している。

**次のアクション:** 環境差の切り分けが必要である。

---

### ![Issue](icons/issue-opened.svg) [cannot move the window](https://github.com/microsoft/vscode/issues/330229) `#330229`
**ステータス:** Open | **ラベル:** なし

**概要:** ウィンドウを掴んで動かす余地がなく移動できないという、強い調子の報告である。環境は VS Code 1.132.0 である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Modern UI のタイトルバー周辺として調査が必要である。

---

### ![Issue](icons/issue-opened.svg) [F*** this GitHub Authentication built-in module, please?](https://github.com/microsoft/vscode/issues/330217) `#330217`
**ステータス:** Open | **ラベル:** なし

**概要:** 組み込みの GitHub 認証モジュールに対する不満を述べた報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 具体的な事象の切り分けが必要である。

---

### ![Issue](icons/issue-opened.svg) [[flaky test] Flaky test: 'removeFastWithoutKeepingOrder' in src/vs/base/test/common/arrays.test.ts](https://github.com/microsoft/vscode/issues/330225) `#330225`
**ステータス:** Closed | **ラベル:** flaky-test, pr-test-failure-triage

**概要:** PR `#325646` の `Compile & Hygiene` ジョブが失敗し、同ジョブの再実行でも失敗したという記録である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Disable flaky test 'removeFastWithoutKeepingOrder' in arrays.test.ts](https://github.com/microsoft/vscode/issues/330226) `#330226`
**ステータス:** Closed | **ラベル:** agentic-workflows, test-disabled, pr-test-failure-triage

**概要:** 当該テストを `test.skip` へ切り替えて無効化し、説明のインラインコメントを添える対応である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [[flaky test] Flaky test: Arrays > removeFastWithoutKeepingOrder (src/vs/base/test/common/arrays.test.ts)](https://github.com/microsoft/vscode/issues/330230) `#330230`
**ステータス:** Closed | **ラベル:** flaky-test, pr-test-failure-triage

**概要:** 同じテストを、既存の CI 失敗として無効化する旨の記録である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Disable flaky test: Arrays > removeFastWithoutKeepingOrder](https://github.com/microsoft/vscode/issues/330231) `#330231`
**ステータス:** Closed | **ラベル:** agentic-workflows, test-disabled, pr-test-failure-triage

**概要:** 同じテストを `test.skip` で無効化し、説明のインラインコメントを添える対応である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [test: disable removeFastWithoutKeepingOrder (shared CI failure, #325646)](https://github.com/microsoft/vscode/issues/330219) `#330219`
**ステータス:** Closed | **ラベル:** agentic-workflows, test-disabled, pr-test-failure-triage

**概要:** 同じテストを、`#325646` で顕在化した共有の CI 失敗として `test.skip` で無効化する対応である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。クローズ済みである。

**次のアクション:** クローズ済みのため対応は不要である。同じテストについて 8 件の Issue が重複して立てられている。

---

### ![Issue](icons/issue-opened.svg) [Extension Signature Verification Failed: Python Preview](https://github.com/microsoft/vscode/issues/326945) `#326945`
**ステータス:** Closed | **ラベル:** bug, info-needed, extension-signature

**概要:** Python Preview 拡張の署名検証に失敗するという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、`info-needed` のまま追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Figma MCP opens link in agent window browser - misleading next step](https://github.com/microsoft/vscode/issues/328574) `#328574`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Figma の MCP サーバを使ってフレームやページのリンクを含むチャットを送ると、期待した動作ではなくエージェントウィンドウのブラウザでリンクが開かれ、次の手順が分かりにくいという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Non-Native Provider Tool Integration Failures](https://github.com/microsoft/vscode/issues/328632) `#328632`
**ステータス:** Closed | **ラベル:** info-needed, model-byok

**概要:** GitHub Copilot 以外のプロバイダを使うと、Copilot のツールが `undefined` のプロパティ参照エラーで失敗するという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Use the Wrong Model](https://github.com/microsoft/vscode/issues/328676) `#328676`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Gemini のモデルを選んでいるのに GPT-5 mini が応答するという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Question mark is at the beginning of a sentence and not at the end](https://github.com/microsoft/vscode/issues/328726) `#328726`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 疑問符が文末ではなく文頭に置かれてしまうという、画像のみの報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Voice: click on sample voice in onboarding widget doesn't trigger sound](https://github.com/microsoft/vscode/issues/328786) `#328786`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** オンボーディングのウィジェットでサンプル音声をクリックしても音が鳴らず、どの声を使うか選べないという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [no response](https://github.com/microsoft/vscode/issues/328798) `#328798`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 動作し続けるだけで何の応答も返らないという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [It does not execute commands from the chat; it stops.](https://github.com/microsoft/vscode/issues/328816) `#328816`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** チャットからのコマンド実行が途中で止まるという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [I'm sorry, I can't generate a response. Please try again. Error code: A9999](https://github.com/microsoft/vscode/issues/328894) `#328894`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 応答を生成できないというエラー（コード A9999）が出るという報告である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [New Modern UI is uncomfortable](https://github.com/microsoft/vscode/issues/328918) `#328918`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Modern UI が見慣れた配色を変えてしまい使いづらいという意見である。

**直近のアクティビティ:** vs-code-engineering[bot]が、追加情報が得られなかったとして自動でクローズしている。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![Issue](icons/issue-opened.svg) [Remote wsl conection](https://github.com/microsoft/vscode/issues/329745) `#329745`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** WSL のリモート接続が動作せず、WSL のフォルダを開けないという報告である。

**直近のアクティビティ:** 直近 24 時間のコメントなし。`info-needed` のままクローズされている。

**次のアクション:** クローズ済みのため対応は不要である。

---
## qmk/qmk_firmware

> 直近 24 時間はキーボード追加 PR のレビュー対応が中心で、OS へ物理レイアウトを申告する HID フィーチャーレポートの追加など core への提案も動いている。30 日間の無活動による自動クローズも複数発生した。

### ![PR](icons/git-pull-request.svg) [Add HID feature report for extended keyboard attrs](https://github.com/qmk/qmk_firmware/pull/26280) `#26280`
**ステータス:** Open | **ラベル:** core, documentation, python, cli, dd

**概要:** HUTRR42c に基づき、キーボードの物理レイアウト（ISO、ANSI、KS、JIS、ABNT）と印字キーセットのロケール（IETF BCP 47 準拠の `en-US` や `de-CH-1996` など）を OS へ申告する Get_Report(FEATURE) を追加する PR である。作者は、これが `bCountryCode` が果たせなかった役割を担いうるとし、機器の接続時に適切なキーボードレイアウトを選択する用途に使える可能性を挙げている。ユーザー空間からの照会も可能で、作者自身の Rust プログラムを例示している。

**直近のアクティビティ:** TorstenSchmitz が Python 側のコードを修正して整えたと報告した。drashna は APPROVED を出す一方、zvecr は CHANGES_REQUESTED を出しており、TorstenSchmitz からもコメントが 2 件付いている。

**変更ファイル:** 14 ファイル、+492/-45。`tmk_core/protocol/chibios/usb_report_handling.c`（+59/-4）、`lib/python/qmk/info.py`（+54）、`lib/python/qmk/cli/generate/config_h.py`（+24）が中心で、スキーマ定義とドキュメントも更新している。

**次のアクション:** zvecr の CHANGES_REQUESTED への対応が必要である。

---

### ![PR](icons/git-pull-request.svg) [[Keyboard] Add Keyboardio Model 100](https://github.com/qmk/qmk_firmware/pull/26397) `#26397`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** Keyboardio Model 100 のサポートを追加する PR である。Model 01 と同じ分割ハードウェア構成で、両半分が ATtiny のキースキャナとなり、メイン MCU が I2C で通信する。ワイヤプロトコル、スキャナのアドレス、64 個の LED 配置は共通のため、カスタムマトリクスと LED ドライバは Model 01 から流用している。メイン MCU は ATmega32U4 から GD32F303CG（ARM Cortex-M4）へ変わるが、mlego/m65/rev2 の前例に倣って高密度基板版の STM32F103 としてビルドするため、GD32 専用の ChibiOS ポートは不要としている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 13 ファイル、+913/-0。`keyboards/keyboardio/model100/` 配下を新規追加し、`keyboard.json`（+282）、`keymaps/default/keymap.c`（+125）、`matrix.c`（+110）、`leds.c`（+101）を含む。

**次のアクション:** レビュー待ちである。

---

### ![PR](icons/git-pull-request.svg) [[Keyboard] Add Akko 5075B Plus](https://github.com/qmk/qmk_firmware/pull/26395) `#26395`
**ステータス:** Open | **ラベル:** invalid, core, keyboard, keymap, documentation, crippled-firmware

**概要:** ロータリーエンコーダとキーごとの RGB を備える 75% キーボード Akko 5075B Plus の有線 USB 構成に対応する PR である。ベンダーがソースを公開していないため、マトリクスのピン配置、ダイオードの向き、LED 配置は純正ファームウェアのイメージから復元し、エンコーダのピンと LED の電源ゲートは実機のプロービングで特定したとしている。作者は、似た名前で流通する `akko/5075` とは別の基板だと注記している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 10 ファイル、+383/-1。`keyboards/akko/5075b_plus/` 配下を新規追加し、`keyboard.json`（+260）が中心である。あわせて `docs/drivers/ws2812.md` にも追記している。

**次のアクション:** invalid と crippled-firmware のラベルが付いており、方針の確認とレビューが必要である。

---

### ![PR](icons/git-pull-request.svg) [Add license headers to splitkb/aurora glcdfont.c files](https://github.com/qmk/qmk_firmware/pull/26382) `#26382`
**ステータス:** Merged | **ラベル:** keyboard

**概要:** `qmk lint` はキーボードのディレクトリツリー内の全コードファイルにライセンスヘッダを要求するが、Aurora の 3 つの `glcdfont.c` には付いていなかった。`lib/python/qmk/cli/lint.py` の `_get_code_files()` が lint 対象の親ディレクトリを遡って `*.c` と `*.h` を拾うため、リビジョンディレクトリの 1 つ上にあるこれらのファイルが対象に含まれ、`qmk lint --strict` が失敗していた。この PR はヘッダを追加してこれを解消する。

**直近のアクティビティ:** Diaoul が感謝のコメントを寄せている。zvecr は CHANGES_REQUESTED のあと APPROVED を出し、drashna も APPROVED を出してマージされた。

**変更ファイル:** 3 ファイル、+48/-0。corne、lily58、sweep の各 `glcdfont.c` にそれぞれ 16 行を追加している。

**次のアクション:** マージ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [Tbkmini dual os keymap](https://github.com/qmk/qmk_firmware/pull/26396) `#26396`
**ステータス:** Closed | **ラベル:** keymap

**概要:** bastardkb/tbkmini と crkbd に対して、2 つの OS を切り替えて使うためのカスタムキーマップを追加する PR である。PR 本文はテンプレートのままで、説明は記入されていない。

**直近のアクティビティ:** zvecr が `#22724` への参照のみをコメントしている。クローズされている。

**変更ファイル:** 6 ファイル、+1380/-0。`bastardkb/tbkmini/keymaps/custom/keymap.c`（+734）と `crkbd/keymaps/custom/keymap.c`（+538）が大半を占める。

**次のアクション:** クローズ済みのため対応は不要である。

---

### ![PR](icons/git-pull-request.svg) [add constance_tkl keyboard](https://github.com/qmk/qmk_firmware/pull/25778) `#25778`
**ステータス:** Closed | **ラベル:** keyboard, keymap, stale

**概要:** aiglatsonstudio の constance_tkl キーボードを追加する PR である。default、default_hs、default_sd の 3 つのキーマップを含み、インジケータ LED のみでスイッチ LED は持たない。主要コントローラは STM32F072 である。

**直近のアクティビティ:** github-actions[bot]が、30 日間活動がないため自動クローズしたと通知している。それまでに zvecr が CHANGES_REQUESTED を 3 回出し、patipan36991 もコメントを寄せていた。

**変更ファイル:** 6 ファイル、+536/-0。`solder/keyboard.json`（+310）と `hotswap/keyboard.json`（+106）が中心である。

**次のアクション:** 自動クローズ済みである。再開するには作者からの状況更新と指摘への対応が必要となる。

---

### ![PR](icons/git-pull-request.svg) [Add Electra 80 PCB](https://github.com/qmk/qmk_firmware/pull/25596) `#25596`
**ステータス:** Closed | **ラベル:** keyboard, keymap, stale

**概要:** Electra 80 PCB のファームウェアを追加する PR である。PR 本文はテンプレートに沿った最小限の記述にとどまる。

**直近のアクティビティ:** github-actions[bot]が、30 日間活動がないため自動クローズしたと通知している。それまでに drashna と zvecr が CHANGES_REQUESTED を出し、PluskbID と zvecr のコメントもあった。

**変更ファイル:** 5 ファイル、+412/-0。`hotswap/keyboard.json` と `solder/keyboard.json`（各 +143）、`electra80.c`（+59）を新規追加している。

**次のアクション:** 自動クローズ済みである。再開するには指摘への対応が必要となる。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open | **ラベル:** needs doc, in progress, core, keyboard, help wanted, keymap, needs testing, documentation, breaking_change, python, cli, feature, CI, no-ci, dd

**概要:** `#11567` で提案された XAP の実装を追跡する長期の作業用 PR である。作業中の位置づけで、スコーピング文書へのリンクが本文に置かれている。

**直近のアクティビティ:** t-8ch と tzarc がレビューコメントを付け、jock070 が APPROVED を出している（いずれも本文なし）。

**変更ファイル:** `builddefs/xap.mk`（+44）や照明系の定数定義（`rgb_matrix_0.0.1.hjson` +175、`rgblight_0.0.1.hjson` +130、`led_matrix_0.0.1.hjson` +82）、スキーマ定義など、ビルド基盤とデータ定義に広く及ぶ。

**次のアクション:** WIP のため実装の継続とテストが必要である。breaking_change のラベルが付いており、取り込みには慎重な検討を要する。

---
