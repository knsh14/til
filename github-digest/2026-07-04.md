# GitHub ダイジェスト — 2026-07-04


## 本日のハイライト

1. ![Issue](icons/issue-opened.svg) **[apply_patch PostToolUse hook fires before file is actually edited](https://github.com/microsoft/vscode/issues/315926)** (microsoft/vscode) — PostToolUse フックがファイル書き込み前に発火し、formatter などがプレ編集の内容を読んでサイレントなデータ損失を起こすと複数ユーザーが再現報告した高深刻度の不具合。
2. ![PR](icons/git-pull-request.svg) **[Support transitive pre-release dependencies](https://github.com/astral-sh/uv/pull/19993)** (astral-sh/uv) — 推移的なプレリリース依存の解決というリゾルバ中核の破壊的変更で、8 件の Issue を一挙にクローズする大型 PR。代替案 #20098 と方針が競合している。
3. ![Issue](icons/issue-opened.svg) **[Run MetaDrive simulation test in GitHub Actions](https://github.com/commaai/openpilot/issues/30693)** (commaai/openpilot) — 無料 Actions ランナー上で MetaDrive シミュレーションを実時間実行する bounty。`JPL11` がレンダリングを 2.5→25fps に改善したが modeld 推論が約 10 倍不足という詳細プロファイリングを報告し、活発な議論が続く。
4. ![PR](icons/git-pull-request.svg) **[feat(treesitter): highlight code inside diff hunks](https://github.com/neovim/neovim/pull/40225)** (neovim/neovim) — diff ハンク内のコードを Tree-sitter でシンタックスハイライトする 839 行追加の大型新機能。複数レビュアーによるレビューが継続中。
5. ![PR](icons/git-pull-request.svg) **[Rivian: remove 0.5 scaling on radar yRel](https://github.com/commaai/opendbc/pull/3511)** (commaai/opendbc) — Hyundai から流用され残っていた係数が全 radar 点の横位置 (yRel) を半分にしていたバグの修正。この期間で実質的にマージされた数少ないコード修正。

---



## sunnypilot/sunnypilot

> 直近は車種移植（HKG の ccNC ポート）と MADS の挙動修正・テスト追加が進行中で、いずれも Open のまま。特に MADS 関連では Pause モードでの停車時再有効化を防ぐ修正とテスト整備が動いている。

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open | **ラベル:** car

**概要:** HKG 車種向けに ccNC（non-HDA2）対応を移植する PR。本文には関連する opendbc 側の PR（sunnypilot/opendbc#94）へのリンクのみが記載されている。現状は Draft の状態。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** opendbc_repo の 1 ファイルのみ変更で、追加 1 行・削除 1 行（サブモジュール参照の更新とみられる）。

**次のアクション:** Draft のため、実装・レビューを進めて Ready for review に切り替える必要がある。

---

### ![PR](icons/git-pull-request.svg) [MADS: add tests and prevent re-enable at standstill in Pause mode](https://github.com/sunnypilot/sunnypilot/pull/1871) `#1871`
**ステータス:** Open | **ラベル:** なし

**概要:** Pause モードのブレーキ設定時に MADS で発生する問題を修正する PR。停車時（standstill）に再有効化されてしまう挙動を防ぎ、あわせてテストを追加する。community.sunnypilot.ai のフォーラム報告（brake setting is pause の issue）を修正対象としている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル変更で、追加 243 行・削除 1 行。sunnypilot/mads/mads.py を 1 行修正し、新規テスト sunnypilot/mads/tests/test_mads_steering_mode.py を 242 行追加している。

**次のアクション:** Open かつレビュー未着のため、レビューとマージ判断が必要。

---

## commaai/openpilot

> 直近は 2026-07-03 に大量の stale/draft PR がまとめてクローズされ、CI 保守（#38280 によるクローズ済みブランチ自動削除の導入）とリポジトリ整理が活動の中心。実質的に議論が活発なのは、MetaDrive シミュレーションを GitHub Actions で走らせる bounty issue #30693 のみで、詳細なプロファイリング報告が寄せられている。

### ![Issue](icons/issue-opened.svg) [Run MetaDrive simulation test in GitHub Actions](https://github.com/commaai/openpilot/issues/30693) `#30693`
**ステータス:** Open | **ラベル:** good first issue, CI / testing, bounty

**概要:** MetaDrive シミュレーションテストを、無料の OSS 向け Actions ランナー上でリアルタイム（openpilot 内の走行 1 分 = CI の 1 分）かつ 20 回以上連続で安定して通るよう実装する bounty。qlog・rlog・カメラファイルを artifact としてアップロードすることが要件。

**直近のアクティビティ:** `stmr` が着手可否を問い合わせ。`JPL11` が `ubuntu-24.04`（4 vCPU, LLVMpipe ソフトウェアレンダリング）での詳細なプロファイリング結果を報告。レンダリングは MSAA/HDR バッファや `ShaderTerrainMesh` 描画がボトルネックで、CI 限定パッチにより 2.5→25 fps へ改善済みだが、modeld の推論が約 500ms/frame（2Hz）で約 10 倍不足しており、CI 用の軽量モデルチェックポイントか低レート許容かをメンテナに判断を仰いでいる。

**次のアクション:** レンダリング側の PR は準備済み。推論側の方針（軽量モデル採用・レート緩和・8 コアランナー利用のいずれか）についてメンテナの判断待ち。

---

### ![Issue](icons/issue-opened.svg) [Missing route data](https://github.com/commaai/openpilot/issues/38178) `#38178`
**ステータス:** Open | **ラベル:** bug

**概要:** 長距離ルートの先頭部分（約 216 セグメント）が欠落するという不具合報告。Release ビルドで発生し、該当ルート `66edd99c9b094257/0000003d--e56f99e3cb/217` が提示されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** メンテナによる再現確認と原因調査が必要。

---

### ![Issue](icons/issue-opened.svg) [Calibration not Starting](https://github.com/commaai/openpilot/issues/38191) `#38191`
**ステータス:** Closed | **ラベル:** bug

**概要:** comma 4（Hyundai Tucson 2023）で openpilot をインストールしたがキャリブレーションが始まらず、ホーム画面のままになるという相談。

**直近のアクティビティ:** `adeebshihadeh` が comma.ai/support でチケットを開くよう案内してクローズ。

**次のアクション:** クローズ済み。サポート窓口での対応へ誘導済み。

---

### ![Issue](icons/issue-opened.svg) [Driver monitoring false positives in v0.11.1 make openpilot repeatedly disengage](https://github.com/commaai/openpilot/issues/38249) `#38249`
**ステータス:** Closed | **ラベル:** なし

**概要:** comma 3 の v0.11.1 で、ドライバーが前方を注視していてもドライバーモニタリングが「脇見」を誤検知し、頻繁にディスエンゲージ/切断されて実質的に使い物にならないという報告。類似 issue（#35776, #37047 など）も挙げられている。

**直近のアクティビティ:** `adeebshihadeh` が「セグメントを提供してもらえれば調査したい」としつつ、無活動のためクローズ。

**次のアクション:** クローズ済み。ログセグメント提供があれば再調査の余地あり。

---

### ![Issue](icons/issue-opened.svg) [ui: poll_input_events called from background thread instead of main UI thread](https://github.com/commaai/openpilot/issues/36613) `#36613`
**ステータス:** Closed | **ラベル:** bug

**概要:** Raylib の入力ポーリングはメイン/UI スレッドからのみ呼ぶべきだが、`MouseState` が背景スレッドから `poll_input_events()` を呼んでおり、スレッドセーフでないためレース・イベント欠落・未定義動作を招く。`SUPPORT_CUSTOM_FRAME_CONTROL` 有効化と入力処理のメインスレッド移行を提案。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Add Back Red Panda Support (Feature Request)](https://github.com/commaai/openpilot/issues/38251) `#38251`
**ステータス:** Closed | **ラベル:** PC

**概要:** #37217 で non-cabana の red panda 向け USB サポートが削除され、`tools/webcam` の Webcam サポートや comma 3 での CAN FD 対応が事実上不可能になったとして復活を求める要望。

**直近のアクティビティ:** `adeebshihadeh` が「multi-panda サポートを戻すことはないだろうが、USB サポートは将来的に戻す可能性がある」とコメント。

**次のアクション:** クローズ済み。USB サポート復活は将来検討の余地あり。

---

### ![Issue](icons/issue-opened.svg) [can't install custom branch with `/`](https://github.com/commaai/openpilot/issues/36480) `#36480`
**ステータス:** Closed | **ラベル:** bug

**概要:** `installer.comma.ai/greatgitsby/fix/api-retry` のようにブランチ名に `/` を含むカスタムブランチをインストールしようとすると、ブランチが公開されているにもかかわらず「invalid software」でダウンロード失敗する不具合。

**直近のアクティビティ:** `adeebshihadeh` が「Should be good now!」と修正済みを報告してクローズ。

**次のアクション:** クローズ済み（修正完了）。

---

### ![PR](icons/git-pull-request.svg) [controlsd: gate enabled on selfdrived validity](https://github.com/commaai/openpilot/pull/36081) `#36081`
**ステータス:** Open | **ラベル:** ignore stale, stale

**概要:** `controlsd.py` で `enabled` 状態を selfdrived の有効性でゲートするドラフト PR。本文なし、`selfdrive/controls/controlsd.py` を +8/-7 変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/controls/controlsd.py` 1 ファイルを +8/-7 変更。

**次のアクション:** ドラフト作業中（stale）。

---

### ![PR](icons/git-pull-request.svg) [ci: delete closed PR branches automatically](https://github.com/commaai/openpilot/pull/38280) `#38280`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** クローズされた PR のブランチを自動削除する CI ワークフローを追加。`.github/workflows/repo-maintenance.yaml` を +45/-1 変更。

**直近のアクティビティ:** CI のリプレイ差分レポート: 66 passed / 0 changed / 0 errors。

**変更ファイル:** `.github/workflows/repo-maintenance.yaml` 1 ファイルを +45/-1 変更。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [usbgpu single jit](https://github.com/commaai/openpilot/pull/37267) `#37267`
**ステータス:** Closed | **ラベル:** なし

**概要:** usbgpu を単一 JIT で動かすドラフト PR。`op switch usbgpu_single_jit` と comma tinygrad リモートのパッチ適用が前提で、test vs. compile/onnx 比較や「speed nan」デバッグが未消化のチェックリストとして残る。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/modeld/modeld.py`（+224/-58）を中心に 12 ファイルを +282/-100 変更。

**次のアクション:** クローズ済み（ドラフトのまま）。

---

### ![PR](icons/git-pull-request.svg) [cabana in jotpluggler](https://github.com/commaai/openpilot/pull/37702) `#37702`
**ステータス:** Closed | **ラベル:** stale

**概要:** jotpluggler に cabana・地図・エンゲージメントタイムライン・テキストログなどを統合する大規模な「dreaming」ドラフト PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `app_cabana.cc`（+2507）や `app_runtime.cc`（+1418）など 40 ファイルを +15,972/-195 の大規模変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [a better remote body teleoperation experience](https://github.com/commaai/openpilot/pull/37732) `#37732`
**ステータス:** Closed | **ラベル:** なし

**概要:** comma body V1/V2 向けの遠隔操作体験を刷新する大規模ドラフト PR。body 専用レイアウト・顔アニメーション・QR コード経由の 1 回接続、webrtcd の映像/音声チャネルや HTTPS・CORS 対応、soundd への `soundRequest` サービス追加、athenad の新 RPC、旧 `tools/bodyteleop/` の全削除などを含む。

**直近のアクティビティ:** `sshane` が CHANGES_REQUESTED でレビュー、`stefpi` が 2 件コメント。以降 stale 化しクローズ。

**変更ファイル:** `webrtcd.py`（+325/-25）や body UI 関連の新規ファイルなど 37 ファイルを +1,586/-807 変更。

**次のアクション:** クローズ済み（ドラフト）。opendbc/panda/teleoprtc/connect の関連 PR に依存していた。

---

### ![PR](icons/git-pull-request.svg) [body v1 updater](https://github.com/commaai/openpilot/pull/37763) `#37763`
**ステータス:** Closed | **ラベル:** body, stale

**概要:** comma body (v1) のフィンガープリント時にブートローダ移行で SBU ラインが落ちるため ignition ラインをスプーフし、0x1 フラッシュメッセージを届かせるため safety mode を BODY に設定するドラフト PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/pandad/pandad.cc`（+32/-2）ほか 4 ファイルを +36/-4 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [Action grid](https://github.com/commaai/openpilot/pull/37772) `#37772`
**ステータス:** Closed | **ラベル:** stale

**概要:** モデル出力に action grid を導入する PR。本文はテンプレートのみでコメントなし。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `drive_helpers.py`（+28/-1）やモデル onnx など 7 ファイルを +45/-8 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [Modeld single jit2 host recurrent](https://github.com/commaai/openpilot/pull/37801) `#37801`
**ステータス:** Closed | **ラベル:** stale

**概要:** modeld を単一 JIT 化しリカレント状態をホスト側で持つリファクタ PR。`compile_modeld.py` を新設し `compile_warp.py` を削除。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `compile_modeld.py`（+398 新規）ほか 6 ファイルを +663/-296 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [OP model 9](https://github.com/commaai/openpilot/pull/37805) `#37805`
**ステータス:** Closed | **ラベル:** autonomy

**概要:** 運転モデルを model 9 に更新する PR。onnx モデル 3 ファイルの差し替えのみ。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `driving_off_policy.onnx` など 3 ファイルを +6/-6 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [jp: add dark mode](https://github.com/commaai/openpilot/pull/37917) `#37917`
**ステータス:** Closed | **ラベル:** tools, stale

**概要:** jotpluggler にダークモードを追加する PR。`style.cc`/`style.h` を新設。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `style.cc`（+109 新規）ほか 11 ファイルを +194/-103 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [Show offroad alert for ignored uploads](https://github.com/commaai/openpilot/pull/37930) `#37930`
**ステータス:** Closed | **ラベル:** stale

**概要:** アップロードが無視された際にオフロードアラートを表示する PR（#36534 をクローズ）。

**直近のアクティビティ:** `adeebshihadeh` と `sshane` が計 3 件のレビューコメントを残したのち stale 化しクローズ。

**変更ファイル:** `alerts_offroad.json`・`prime_state.py` など 3 ファイルを +16/-0 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [Revert "Revert "DM: Lancia Delta HF Integrale model (#37696)""](https://github.com/commaai/openpilot/pull/37976) `#37976`
**ステータス:** Closed | **ラベル:** stale

**概要:** #37971 のリバートをさらにリバートし、ドライバーモニタリング（DM）モデルを再導入するドラフト PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `dmonitoring_model.onnx`・`policy.py` など 8 ファイルを +24/-53 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [ui: real kernel touch event timestamps](https://github.com/commaai/openpilot/pull/38019) `#38019`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** カーネルの実タッチイベントのタイムスタンプを用い、ポーリングを廃止するドラフト PR。将来はタッチ処理スレッドをメインより低優先度にできる旨のメモあり。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `system/ui/lib/application.py` 1 ファイルを +99/-5 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [replace pyserial with serial.py](https://github.com/commaai/openpilot/pull/38032) `#38032`
**ステータス:** Closed | **ラベル:** なし

**概要:** 依存の `pyserial` を自前の `common/serial.py` に置き換える PR。`pyproject.toml`/`uv.lock` から依存を削除し、modem・GPS 系の各所を差し替え。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `common/serial.py`（+262 新規）ほか 9 ファイルを +276/-26 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [dreaming about a clean root](https://github.com/commaai/openpilot/pull/38036) `#38036`
**ステータス:** Closed | **ラベル:** stale

**概要:** リポジトリのルートを整理し `common/`・`selfdrive/` などを `openpilot/` 配下へ移す構想のドラフト PR。実現にはサブモジュールの Python パッケージ化などが必要と本文で明記。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 大量のリネームとワークフロー削除を含む 1,138 ファイルを +92/-1,236 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [unused param IsEngaged](https://github.com/commaai/openpilot/pull/38038) `#38038`
**ステータス:** Closed | **ラベル:** なし

**概要:** 未使用パラメータ `IsEngaged` を削除するドラフト PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `params_keys.h`・`hardwared.py` の 2 ファイルを +0/-9 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [fix dm model build deps](https://github.com/commaai/openpilot/pull/38046) `#38046`
**ステータス:** Closed | **ラベル:** なし

**概要:** DM モデルのビルド依存関係を修正する PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/modeld/SConscript` 1 ファイルを +5/-5 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [modeld: pkl tensor inputs](https://github.com/commaai/openpilot/pull/38056) `#38056`
**ステータス:** Closed | **ラベル:** なし

**概要:** modeld の入力を pkl テンソル化し、`ModelState` 初期化を qcom 上で約 3 秒→約 2 秒に短縮する PR。pkl サイズは約 1MB 増だが、AMD でのコンパイラ設定やキュー配置デバイスの管理が不要になる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `compile_modeld.py`・`modeld.py` の 2 ファイルを +29/-18 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [modeld: zero copy npy inputs](https://github.com/commaai/openpilot/pull/38075) `#38075`
**ステータス:** Closed | **ラベル:** なし

**概要:** modeld の npy 入力をゼロコピー化するドラフト PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `compile_modeld.py`・`modeld.py` の 2 ファイルを +8/-4 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [degrade stream on metered connections](https://github.com/commaai/openpilot/pull/38082) `#38082`
**ステータス:** Closed | **ラベル:** なし

**概要:** 従量制（metered）ネットワーク接続時にライブストリームの解像度とビットレートを下げるドラフト PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `loggerd.h`（+53/-8）や `webrtcd.py` など 6 ファイルを +85/-21 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [Compile modeld warps with symbolic camera sizes](https://github.com/commaai/openpilot/pull/38078) `#38078`
**ステータス:** Closed | **ラベル:** stale

**概要:** modeld の warp をカメラサイズをシンボリックにしてコンパイルするドラフト PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `compile_modeld.py`（+77/-44）など 6 ファイルを +168/-85 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [split warp release](https://github.com/commaai/openpilot/pull/38088) `#38088`
**ステータス:** Closed | **ラベル:** なし

**概要:** warp のリリースビルドを分割するドラフト PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `compile_modeld.py`（+49/-45）など 4 ファイルを +70/-73 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [debug hil](https://github.com/commaai/openpilot/pull/38084) `#38084`
**ステータス:** Closed | **ラベル:** なし

**概要:** HIL ビルドのデバッグ用ドラフト PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/modeld/compile_modeld.py` 1 ファイルを +1/-1 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [modeld: always build big model for release](https://github.com/commaai/openpilot/pull/38097) `#38097`
**ステータス:** Closed | **ラベル:** なし

**概要:** リリースでは常に big model をビルドするドラフト PR。usbgpu 接続 CI デバイスで usbgpu 有無両方のテストをどう走らせるかが未解決課題として残る。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `SConstruct`・`SConscript` の 2 ファイルを +3/-3 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [modeld/SConscript: depend on tg HEAD + git status](https://github.com/commaai/openpilot/pull/38094) `#38094`
**ステータス:** Closed | **ラベル:** stale

**概要:** `scons --tree` の出力を読みやすくするため、tinygrad の全ファイルではなく HEAD + git status に依存させるドラフト PR。ビルド対象・依存・実行内容を把握しやすくする狙い。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/modeld/SConscript` 1 ファイルを +5/-4 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [usbgpu: fallback on small model](https://github.com/commaai/openpilot/pull/38098) `#38098`
**ステータス:** Closed | **ラベル:** なし

**概要:** usbgpu 利用時に small model へフォールバックする仕組みを追加するドラフト PR。`routerd.py` や `big_modeld.py` を新設し、イベント・UI・process_replay も対応。

**直近のアクティビティ:** `adeebshihadeh` と `Armandpl` が各 1 件のレビューコメントを残したのちクローズ。

**変更ファイル:** `routerd.py`（+53 新規）や `modeld.py` など 14 ファイルを +172/-36 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [sound requests from webrtc for body](https://github.com/commaai/openpilot/pull/38106) `#38106`
**ステータス:** Closed | **ラベル:** なし

**概要:** body 向けに webrtc からのサウンドリクエストを受け付けるドラフト PR。新 `soundRequest` サービス経由で soundd がアラート音を再生。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `cereal/log.capnp`・`soundd.py` など 4 ファイルを +23/-5 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [webrtc: REMB-based livestream bitrate controller](https://github.com/commaai/openpilot/pull/38189) `#38189`
**ステータス:** Open | **ラベル:** なし

**概要:** `LivestreamBitrateController` のパケットロスヒューリスティックを、ブラウザの REMB（Receiver Estimated Maximum Bitrate）に基づく制御へ置き換えるドラフト PR。`RTCRtpSender` を monkey-patch して REMB フィードバックを取得し、推定値が下回ればビットレートを下げ、安定後に再上昇を試みる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `system/webrtc/webrtcd.py` 1 ファイルを +57/-42 変更。

**次のアクション:** ドラフト作業中（Open）。

---

### ![PR](icons/git-pull-request.svg) [fix: webjoystick connection from firefox](https://github.com/commaai/openpilot/pull/38130) `#38130`
**ステータス:** Closed | **ラベル:** tools

**概要:** Firefox での mDNS 問題を解決するため STUN サーバーを追加し、カメラを wideRoad に切り替える webjoystick 接続の修正 PR。

**直近のアクティビティ:** `adeebshihadeh` が CHANGES_REQUESTED でレビューしたのちクローズ。

**変更ファイル:** `controls.js`・`webrtc.js`・`web.py` の 3 ファイルを +6/-4 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [usbgpu: don't materialize big model weights in RAM](https://github.com/commaai/openpilot/pull/38138) `#38138`
**ステータス:** Closed | **ラベル:** なし

**概要:** big model の重みを RAM 上に展開せずに扱うドラフト PR。`file_chunker.py` と `compile_modeld.py` を中心に変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `compile_modeld.py`（+81/-37）など 4 ファイルを +125/-56 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [modeld: simple usbgpu fallback](https://github.com/commaai/openpilot/pull/38169) `#38169`
**ステータス:** Closed | **ラベル:** なし

**概要:** usbgpu の簡易フォールバックを実装するドラフト PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `modeld.py`・`events.py` など 4 ファイルを +32/-1 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [move lat/long maneuvers into selfdrive/](https://github.com/commaai/openpilot/pull/38197) `#38197`
**ステータス:** Closed | **ラベル:** なし

**概要:** 横方向/縦方向マニューバ関連のツールを `tools/` から `selfdrive/tuning/` へ集約するリファクタ PR。README や `maneuver_helpers.py` を新設し、joystick/maneuvers 系を移動。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/tuning/` への移動・新設を含む 17 ファイルを +260/-287 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [desire cleanups](https://github.com/commaai/openpilot/pull/38188) `#38188`
**ステータス:** Closed | **ラベル:** なし

**概要:** `desire_helper.py` のクリーンアップ PR。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/controls/lib/desire_helper.py` 1 ファイルを +18/-41 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix vendored ffmpeg linking](https://github.com/commaai/openpilot/pull/38231) `#38231`
**ステータス:** Closed | **ラベル:** なし

**概要:** vendored ffmpeg のリンクを修正するドラフト PR（codex 生成ブランチ）。`SConstruct` と各 SConscript の x264 リンク周りを調整。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `SConstruct` など 5 ファイルを +14/-13 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [webrtcd: remove MediaStreamError](https://github.com/commaai/openpilot/pull/38269) `#38269`
**ステータス:** Closed | **ラベル:** なし

**概要:** aiortc 1.10.1 でトップレベル import でなくなった `MediaStreamError` を削除し、デッドコードの `readyState` も除去する PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `openpilot/system/webrtc/device/video.py` 1 ファイルを +0/-4 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [guard on exit](https://github.com/commaai/openpilot/pull/37421) `#37421`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** `wifi_manager.py` の終了処理にガードを追加する PR。本文なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `system/ui/lib/wifi_manager.py` 1 ファイルを +17/-6 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [WifiManager: fix more race conditions v2](https://github.com/commaai/openpilot/pull/37419) `#37419`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** #37411 の代替として、WifiManager のレース修正方法を書き直すクリーンアップ目的のドラフト PR。状態スレッドで長時間 DBus 呼び出しを避けるための `_user_epoch` チェックへの依存を、将来のメンテナが意識しなくて済むよう整理する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `wifi_manager.py`（+127/-122）とテストの 2 ファイルを +182/-169 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [support all the linuxes!](https://github.com/commaai/openpilot/pull/37382) `#37382`
**ステータス:** Closed | **ラベル:** tools, stale

**概要:** musl 以外の各種 Linux ディストリ（Arch・Fedora・Debian・Ubuntu 20.04/24.04）をサポートする PR。Docker スモークテストと QEMU での Desktop ビルドテストを追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `setup_dependencies.sh` や新規スクリプトなど 12 ファイルを +203/-87 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [jotpluggler: improved seeking](https://github.com/commaai/openpilot/pull/37735) `#37735`
**ステータス:** Closed | **ラベル:** tools

**概要:** jotpluggler のシーク動作を改善する PR。本文なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `session.cc`・`sidebar.cc` など 4 ファイルを +52/-22 変更。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [dlataccel 0](https://github.com/commaai/openpilot/pull/37745) `#37745`
**ステータス:** Closed | **ラベル:** autonomy

**概要:** 横加速度ノイズを除いた特殊モデルを試すドラフト PR。onnx モデル 2 ファイルの差し替えのみで本文はテンプレート。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `driving_policy.onnx`・`driving_vision.onnx` の 2 ファイルを +2/-2 変更。

**次のアクション:** クローズ済み（ドラフト）。

---

### ![PR](icons/git-pull-request.svg) [modeld: increase model smooth](https://github.com/commaai/openpilot/pull/37724) `#37724`
**ステータス:** Closed | **ラベル:** stale

**概要:** モデルの antilag を目的に、`timestampEof` の平均 50ms 遅延やモデル予測の遅延を `DT_MDL` 加算で補正する PR。`modeld.py` の 1 行変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/modeld/modeld.py` 1 ファイルを +1/-1 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [cabana: imgui rewrite](https://github.com/commaai/openpilot/pull/37638) `#37638`
**ステータス:** Closed | **ラベル:** stale

**概要:** cabana を Qt から ImGui へ書き換える大規模ドラフト PR。将来的な PlotJuggler 置き換えへの第一歩と位置づけられている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `imgui/app.cc`（+2540 新規）など 119 ファイルを +11,391/-10,170 の大規模変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [updated: use configured remote instead of hardcoded origin](https://github.com/commaai/openpilot/pull/37720) `#37720`
**ステータス:** Closed | **ラベル:** stale

**概要:** リモート名が `origin` でないフォークでの更新失敗を解消するため、`branch.<name>.remote` の git config からリモート名を取得し、未設定時は `origin` にフォールバックする PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `system/updated/updated.py` 1 ファイルを +12/-4 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [Another beep](https://github.com/commaai/openpilot/pull/37706) `#37706`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** 新しいビープ音（distracted プロンプト）を追加する PR。`make_beeps.py` と soundd を変更。本文はテンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `make_beeps.py`（+38/-2）や wav・`soundd.py` など 4 ファイルを +44/-4 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [locationd: PoseSmoother](https://github.com/commaai/openpilot/pull/37699) `#37699`
**ステータス:** Closed | **ラベル:** stale

**概要:** `masked_symmetric_moving_average` を `lagd.py` から `helpers.py` へ移し、`Pose` 測定の対称ガウス平滑化を行う再利用可能な `PoseSmoother` を導入するリファクタ PR。

**直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` が概要レビュー（2 ファイル・2 コメント生成）を投稿。以降 stale 化しクローズ。

**変更ファイル:** `locationd/helpers.py`（+52/-1）と `lagd.py` の 2 ファイルを +53/-14 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [ui: fade in path and lane lines](https://github.com/commaai/openpilot/pull/37545) `#37545`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** 走行パスとレーンラインをフェードインさせる UI PR。本文なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/ui/mici/onroad/model_renderer.py` 1 ファイルを +21/-20 変更。

**次のアクション:** クローズ済み（stale）。

---

### ![PR](icons/git-pull-request.svg) [mici ui: new firehose menu](https://github.com/commaai/openpilot/pull/37674) `#37674`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** mici UI に新しい firehose メニューを実装するドラフト PR（デザイン検討中）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `selfdrive/ui/mici/layouts/settings/firehose.py` 1 ファイルを +54/-142 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [mici ui: fix velocity jitter](https://github.com/commaai/openpilot/pull/37657) `#37657`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** スクロール速度計算で、直近サンプルが異常に高い場合のオーバーシュートを抑えるため平滑化を導入するドラフト PR。従来 4300 px/s だった速度を 2568 px/s に是正する例が示されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `system/ui/lib/application.py`（+105/-4）と `scroll_panel2.py` の 2 ファイルを +115/-12 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [mici ui: add increased size assets](https://github.com/commaai/openpilot/pull/37645) `#37645`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** mici UI 向けに拡大サイズのアセットを追加するドラフト PR。Figma の整理待ちの旨のメモあり。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** mici アセット PNG や `onboarding.py`（+85/-6）など 46 ファイルを +224/-102 変更。

**次のアクション:** クローズ済み（ドラフト・stale）。

---

### ![PR](icons/git-pull-request.svg) [ui: VBoxLayout](https://github.com/commaai/openpilot/pull/37598) `#37598`
**ステータス:** Closed | **ラベル:** ui, stale

**概要:** UI ウィジェットに `VBoxLayout` を導入するリファクタ PR。本文なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `settings/device.py`・`widgets/layouts.py` の 2 ファイルを +46/-42 変更。

**次のアクション:** クローズ済み（stale）。

---

## commaai/opendbc

> 直近は Honda 系を中心とした新規車種の fingerprint 追加 PR が数多く滞留しており、CI の車両挙動レポートは軒並みパスしているものの人的レビュー待ちの状態が続いている。一方で 6/15〜6/27 に投稿された安全機構やバウンティ関連の複数 PR は、メンテナから「LLM 生成と区別がつかない」として一括クローズされた。

### ![PR](icons/git-pull-request.svg) [Add fingerprint Cadillac XT6 Sport 2023](https://github.com/commaai/opendbc/pull/3512) `#3512`
**ステータス:** Open | **ラベル:** car, gm

**概要:** 2023 Cadillac XT6 Sport（SDGM）を BearTech SDGM B ハーネス経由でサポート追加する PR。DBC は Cadillac XT4 と同系統で、固有の固定 CAN fingerprint を持ち他プラットフォームとの衝突がないことを確認済み。約 145 マイルの走行から推定した torque 横制御（latAccelFactor 0.9、friction 0.14、steerRatio 15.0）を設定し、縦制御は他の SDGM 車と同様にストックを使用する。

**直近のアクティビティ:** CI の車両挙動レポート: GMC_ACADIA / GMC_YUKON の 20 セグメントで 0 changed / 20 passed / 0 errors。ボットがコントリビューションガイド確認のチェックリスト（レビュー準備が整うまで draft にする等）を案内している。

**変更ファイル:** 5 ファイルを変更、+15 / -1 行。gm の fingerprints.py・interface.py・values.py、tests/routes.py、torque_data/override.toml を小規模に修正。

**次のアクション:** Open のためメンテナのレビュー待ち。投稿者は SASCM モジュール導入済みで long control 作業の可否も問い合わせており、方針の回答が必要。

---

### ![PR](icons/git-pull-request.svg) [Honda - Add 2024 - 25 Civic SI fingerprint](https://github.com/commaai/opendbc/pull/3334) `#3334`
**ステータス:** Open | **ラベル:** car, honda, fingerprint

**概要:** 2024 および 2025 の Honda Civic SI の fingerprint を追加する PR。それぞれの検証用ルートが提示されている。

**直近のアクティビティ:** Jiming-C が、2024 のルートは自身の車両のもので 8S102-T38-AA10 が fingerprint され 2024 Civic Si 6MT でクリーンに engage することを確認したとコメント。必要ならファームウェアダンプや追加ルートの提供も申し出ている。

**変更ファイル:** 1 ファイルのみ、+2 / -0 行。honda/fingerprints.py への追記のみ。

**次のアクション:** Open のためメンテナのレビュー・マージ判断待ち。ユーザーからの実車確認は得られている。

---

### ![PR](icons/git-pull-request.svg) [Rivian: remove 0.5 scaling on radar yRel](https://github.com/commaai/opendbc/pull/3511) `#3511`
**ステータス:** Merged | **ラベル:** car, rivian

**概要:** Rivian の radar パーサーが Hyundai から流用された際に残っていた yRel への 0.5 係数を削除する PR。Rivian DBC では角度が度単位（0.1°/bit）、距離がメートル単位のため、yRel = -sin(azimuth)·dist と係数なしで計算すべきで、0.5 は全 radar 点の y 位置を半分にしてしまっていた。0.5 係数は AZIMUTH が 0.2°/bit の Hyundai でのみ必要と説明。

**直近のアクティビティ:** CI の車両挙動レポート: RIVIAN_R1 の 10 セグメントで 0 changed / 10 passed / 0 errors。

**変更ファイル:** 1 ファイル、+1 / -1 行。rivian/radar_interface.py の一行修正。

**次のアクション:** 既に master へマージ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Honda - 2025 Civic Thailand fingerprint](https://github.com/commaai/opendbc/pull/3519) `#3519`
**ステータス:** Open | **ラベル:** car, honda, fingerprint

**概要:** タイ仕様の 2025 Honda Civic の fingerprint を追加する PR。検証用ルートが 1 件提示されている。

**直近のアクティビティ:** CI の車両挙動レポート: Honda 系 17 車種の 154 セグメントで 0 changed / 154 passed / 0 errors。mrajaypalsingh1-coder がコントリビューションへの感謝コメントを残している。

**変更ファイル:** 1 ファイルのみ、+5 / -0 行。honda/fingerprints.py への追記のみ。

**次のアクション:** Open のためメンテナのレビュー・マージ判断待ち。

---

### ![PR](icons/git-pull-request.svg) [Honda - Acura RDX 22-26 release support](https://github.com/commaai/opendbc/pull/3316) `#3316`
**ステータス:** Open | **ラベル:** car, honda

**概要:** Acura RDX を release サポートへ引き上げる PR。ステアリングをファクトリー値 3840 へ引き上げ、対応年式を 2026 まで拡張（fingerprint 変更なし）、2025 は NHTSA EPS リコール完了を検証できないため adeeb の判断で除外し、より広範な走行を含む testroute に更新する。

**直近のアクティビティ:** mrajaypalsingh1-coder が「引き続き関心があり進捗を追っているので open のままにしてほしい」とコメント。CI の車両挙動レポート: Honda 系 17 車種の 154 セグメントで 0 changed / 154 passed / 0 errors。

**変更ファイル:** 5 ファイル、+17 / -8 行。extra_cars.py、honda の interface.py・values.py、tests/routes.py、torque_data/override.toml を修正。

**次のアクション:** Open のためメンテナのレビュー待ち。2025 除外の扱いを含めマージ判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Honda: 2015 CRV South Africa](https://github.com/commaai/opendbc/pull/2933) `#2933`
**ステータス:** Open | **ラベル:** car, honda

**概要:** 南アフリカ仕様の 2015 Honda CRV をサポートする PR。この車両は USA/EU の CRV と異なるレガシーなギアボックスを持つが、それ以外は EU CRV のコード構造に沿っている。testroute が提示されている。

**直近のアクティビティ:** CI の車両挙動レポート: Honda 系 17 車種の 154 セグメントで 0 changed / 154 passed / 0 errors。jyoung8607 が、#2457 でギアボックスメッセージを統合した際 Acura RDX が唯一の例外として DBC を分離したのは暫定的な hack だったと指摘。レガシーメッセージを参照する車種が増えてきたため、UDS ファームウェアを HONDA_CRV へ集約する抽象化の見直しに近く着手したいので、それまで open のままにしてほしいとしている。

**変更ファイル:** 5 ファイル、+19 / -3 行。honda の fingerprints.py・interface.py・values.py、tests/routes.py、torque_data/substitute.toml を修正。

**次のアクション:** Open だがメンテナ側のギアボックス抽象化リファクタリング待ちで保留状態。その完了後にマージ検討。

---

### ![PR](icons/git-pull-request.svg) [Honda: Acura Integra 2023-25](https://github.com/commaai/opendbc/pull/2432) `#2432`
**ステータス:** Open | **ラベル:** DBC signals, car, honda, car safety

**概要:** 放棄された PR 1722 をベースに、Acura Integra 2023-25 をサポートし 2025 の fingerprint を追加する PR。ENGINE_DATA メッセージを持たないため abs_sensor 0x20E（周期的な距離計測）を用い、距離変化から速度を算出、連続メッセージの値が不変であることで vehicle_moving を判定するよう再実装している。50Hz あたり平均 1 ユニット増加が約 4kph に対応し、abs_sensor 輪が carspeed_wheels に整合することを解析で確認済み。ハーネスは Comma Bosch B。

**直近のアクティビティ:** CI の車両挙動レポート: Honda 系 17 車種の 154 セグメントで 0 changed / 154 passed / 0 errors。人的レビューはまだ付いていない。

**変更ファイル:** 11 ファイル、+143 / -9 行と比較的大きい。carstate.py、fingerprints.py、safety/modes/honda.h（+43/-6）、safety テスト、honda_bosch_radarless.dbc など car・safety 両面に及ぶ変更。

**次のアクション:** Open のためメンテナのレビュー待ち。safety コードと低速処理ロジックの慎重なレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Honda - Bump 2026 Ridgeline](https://github.com/commaai/opendbc/pull/3502) `#3502`
**ステータス:** Open | **ラベル:** car, honda

**概要:** Honda Ridgeline の対応年式を 2026 へ引き上げる小規模 PR。検証用ルートが 1 件提示されている。

**直近のアクティビティ:** CI の車両挙動レポート: Honda 系 17 車種の 154 セグメントで 0 changed / 154 passed / 0 errors。

**変更ファイル:** 2 ファイル、+3 / -1 行。honda/fingerprints.py と values.py を修正。

**次のアクション:** Open のためメンテナのレビュー・マージ判断待ち。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3513) `#3513`
**ステータス:** Merged | **ラベル:** car, bot

**概要:** commaci-public ボットによる uv.lock 依存関係の週次更新 PR。

**直近のアクティビティ:** CI の車両挙動レポート: 変更検出なし（No changes detected）。

**変更ファイル:** 2 ファイル、+99 / -97 行。car/__init__.py と uv.lock を更新（大半は uv.lock のロック内容差分）。

**次のアクション:** 既に master へマージ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Honda: Fit (Taiwan) 2021/2024/2025](https://github.com/commaai/opendbc/pull/3113) `#3113`
**ステータス:** Open | **ラベル:** car, honda

**概要:** 台湾仕様の Honda Fit 2021/2024/2025 をサポートする PR。ファクトリー最大 LKAS は 3840。この車両は issue #1147 の影響を受けるが、その修正を #3114 でテスト済みと説明。alphalongitudinal 有効時に 5km/h 未満で（おそらくブレーキ由来の）ハプティックフィードバックが出るが、release では alphalong=off のため影響せず、調査は将来の PR に先送りしている。

**直近のアクティビティ:** cmtsij が jyoung8607 にレビューを依頼するコメントを投稿。

**変更ファイル:** 5 ファイル、+43 / -2 行。honda の fingerprints.py（+24）・interface.py・values.py、tests/routes.py、torque_data/override.toml を修正。

**次のアクション:** Open のためレビュー待ち。指名された jyoung8607 のレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Kia EV9 lateral safety controls](https://github.com/commaai/opendbc/pull/3488) `#3488`
**ステータス:** Closed | **ラベル:** car, hyundai, car safety

**概要:** issue #1175（$1,000 バウンティ）を解決する PR として、hyundai_canfd safety モジュールに LFA2 の角度ベースステアリング安全検証を追加し、Kia EV9 の CAN-FD ネットワークに安全チェックと制限を適用する。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 2 ファイル、+37 / -4 行。hyundai/values.py と safety/modes/hyundai_canfd.h を修正。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。バウンティ課題は未解決のまま。

---

### ![PR](icons/git-pull-request.svg) [safety: add ignition branch coverage tests](https://github.com/commaai/opendbc/pull/3436) `#3436`
**ステータス:** Closed | **ラベル:** car safety

**概要:** #2557 の一部として、ignition の CAN 処理に対する分岐カバレッジテストを追加する PR。プロダクションの安全ロジックは変更せず、GM・Rivian・Tesla・Mazda・Volkswagen MEB について誤ったバス/DLC の ignition メッセージが無視される経路（ignition.h の ignored-path 分岐）を検証する。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 5 ファイル、+33 / -0 行。safety/tests 配下の各車種テスト（test_gm.py 等）にテストケースを追加。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [hyundai: reclassify availability-toggle button as main](https://github.com/commaai/opendbc/pull/3386) `#3386`
**ステータス:** Closed | **ラベル:** car, hyundai

**概要:** cruise availability 状態が反転した際に、曖昧な Hyundai の cruise ボタンを mainCruise として再分類し、上位ロジックが availability トグルを resume 要求として扱わないようにする PR。再分類ヘルパー向けの回帰テストも含む。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 2 ファイル、+33 / -0 行。hyundai/carstate.py と tests/test_hyundai.py を修正。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [safety: speed mismatch invalidates rx checks](https://github.com/commaai/opendbc/pull/3499) `#3499`
**ステータス:** Closed | **ラベル:** car safety

**概要:** これまで `controls_allowed = false` のみを設定していた 2 つの速度源間の速度不一致（`speed_mismatch_check`）を、#2234 の要望どおり他の rx フォルトと同様に safety rx チェック無効（`safety_rx_checks_invalid`）としても扱うようにする PR。`speed_mismatch` と `rx_msgs_invalid` を永続フラグで追跡し、`safety_tick()`（1Hz）と `speed_mismatch_check()`（メッセージ毎）の両方で両者の OR を再計算することで、正常な速度メッセージが実フォルトをマスクしないようにする。テストは 3196 件パス、安全コード 100%行カバレッジ維持と報告。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 6 ファイル、+22 / -1 行。safety の declarations.h・safety.h、テスト用 common.py・libsafety・safety.c、test_tesla.py を修正。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。Tesla の符号付き/符号なし速度比較の件は元 issue に残課題として記載あり。

---

### ![PR](icons/git-pull-request.svg) [Chrysler: reliable fuzzy fingerprinting (#1092)](https://github.com/commaai/opendbc/pull/3496) `#3496`
**ステータス:** Closed | **ラベル:** car, chrysler

**概要:** Chrysler/Jeep/Ram/Dodge 向けにファジー FW マッチングを追加し、新しいファームウェアリビジョンが正確な DB エントリなしで識別されるようにする PR（#1092 対応、#3018 の分析を発展）。過去 3 回の試み（#2928, #3018, #3329）が想定したファームウェア末尾 2 文字リビジョン説は誤りで、約 22 ヶ月分の実ファームウェアで検証したところ strip=2 は 20% しか正しく識別できなかった。strip 長 2〜6 を全 PLATFORM_CODE_ECUS（combinationMeter, abs, fwdRadar, eps）一致条件で掃引し、実世界再現率 80%かつ誤識別ゼロの strip=4 を採用。失敗時は no match へ安全に劣化し exact-match にフォールバックする。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 3 ファイル、+214 / -1 行。chrysler/tests の新規テスト（test_chrysler.py +151）と values.py（+63）を追加・修正。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。投稿者自身も n=5 のサンプル数の小ささを課題として明記していた。

---

### ![PR](icons/git-pull-request.svg) [Rivian cruise set speed tracking from steering stalk signals](https://github.com/commaai/opendbc/pull/3489) `#3489`
**ステータス:** Closed | **ラベル:** car, rivian

**概要:** issue #1833（$1,000 バウンティ）を解決する PR として、CAN Bus 0（pt）の VDM_AdasStalk メッセージ信号を解析し、Rivian 車両でステアリングストークベースの cruise set speed トラッキングを実装する。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 1 ファイルのみ、+36 / -2 行。rivian/carstate.py を修正。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。バウンティ課題は未解決のまま。

---

### ![PR](icons/git-pull-request.svg) [Kia EV9 lateral port](https://github.com/commaai/opendbc/pull/3509) `#3509`
**ステータス:** Closed | **ラベル:** car, hyundai

**概要:** Kia EV9（2024-25）を CAN-FD EV プラットフォームとして Hyundai/Kia ポートに追加する PR。EV6 や Ioniq 5 と同じ E-GMP アーキテクチャで、values.py に KIA_EV9（質量 2650kg、ホイールベース 3.1m、steerRatio 16.1、EV フラグ）を、fingerprints.py に fwdRadar(0x7d0)/fwdCamera(0x7c4) のプレースホルダ fingerprint を追加。テストルート未取得のため non_tested_cars に登録し、torque params は Ioniq 5 の値をベースにしている。既存テストは全てパスと報告。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。

**変更ファイル:** 4 ファイル、+18 / -0 行。hyundai の fingerprints.py・values.py、tests/routes.py、torque_data/params.toml を修正。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。fingerprint がプレースホルダで実車ファームウェア未取得の点も投稿者が明記していた。

---

### ![PR](icons/git-pull-request.svg) [Add 2017 Lexus RC F support with 7-byte PCM_CRUISE](https://github.com/commaai/opendbc/pull/3032) `#3032`
**ステータス:** Closed | **ラベル:** DBC signals, car, toyota, car safety

**概要:** 非標準の 7 バイト PCM_CRUISE メッセージ(0x1D2)を使う 2017 Lexus RC F をサポートする PR。8 バイトではなく 7 バイトで信号位置がシフト（CRUISE_STATE bit47、CANCEL_REQ bit40、CHECKSUM bit55）するため `SHORT_PCM_CRUISE` フラグを新設。専用 DBC（toyota_lexus_rc_2017_pt.dbc）、LEXUS_RC_2017 プラットフォーム設定（EPS scale 77）、fingerprint、toyota.h の `TOYOTA_SHORT_PCM_RX_CHECKS` と 7 バイト 0x1D2 の TX ハンドリング、torque マッピングを追加。本文には未チェックのテスト計画が残る。

**直近のアクティビティ:** メンテナ adeebshihadeh が「LLM 生成と区別がつかない」としてクローズ。過去には sshane が 1/15 に 3 件の（本文なしの）レビューコメントを付けていた。

**変更ファイル:** 7 ファイル、+116 / -4 行。toyota の values.py・fingerprints.py、新規 DBC、safety/modes/toyota.h（+30）、test_toyota.py などを修正。draft 状態だった。

**次のアクション:** Closed（マージされず）。この PR での追加対応は不要。

---

## commaai/panda

> 直近24時間の更新はありません。

---

## neovim/neovim

> 直近は zeertzjq による vim-patch の移植と justinmk 主導の cmdwin 周りのリファクタリングが活発で、treesitter による diff ハイライトや LSP・remote 機能、GSoC の WASM 対応など多方面の PR/Issue が並行して動いている。

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0781: tests: Test_fuzzy_completion_bufname_fullpath() creates unnecessary dir](https://github.com/neovim/neovim/pull/40565) `#40565`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** Vim パッチ 9.2.0781 の移植。`Test_fuzzy_completion_bufname_fullpath()` がファイル名と同名の不要なディレクトリを作成していた問題を修正し、ファイルの親ディレクトリのみを作成するように変更する。vim/vim#20695 に対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `test/old/testdir/test_cmdline.vim` の 1 ファイルのみで、追加 1 行・削除 1 行の軽微な変更。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [docs(vimeval.txt): Update vimeval.txt](https://github.com/neovim/neovim/pull/40563) `#40563`
**ステータス:** Open | **ラベル:** documentation

**概要:** `vimeval.txt` のサブリスト（部分リスト）に関する説明の文言が分かりにくいという指摘に対し、インデックスが重なった場合に空リストになる旨の記述を書き換える draft PR。「second item」がリストを指すのかインデックスを指すのか曖昧だった点を修正しようとしている。

**直近のアクティビティ:** zeertzjq が、これはインデックスが重なるケースではなく、`List2[0:-99999]` のようにインデックスがリストの境界を超えるケースについての記述だと指摘した。

**変更ファイル:** `runtime/doc/vimeval.txt` の 1 ファイルで追加 2 行・削除 2 行。

**次のアクション:** draft かつレビュー指摘があるため、著者は指摘を踏まえて記述を再検討する必要がある。

---

### ![PR](icons/git-pull-request.svg) [refactor(test): unify XDG path handling (cmake vs zig)](https://github.com/neovim/neovim/pull/40522) `#40522`
**ステータス:** Open | **ラベル:** build, refactor, test

**概要:** テストにおける XDG パスの扱いを cmake ビルドと zig ビルドで統一するリファクタリング。#40381 を修正する。パス設定を `test/runner.lua` 側に集約し、`RunTests.cmake` や `run_tests.zig`、各 spec からの重複記述を削減する。

**直近のアクティビティ:** justinmk が Approve 済み。

**変更ファイル:** 7 ファイルで追加 29 行・削除 37 行。`test/runner.lua` に処理を集約（+17/-3）し、`RunTests.cmake`・`run_tests.zig`・複数の spec ファイルから重複を削除。

**次のアクション:** Approve 済みのため、マージに向けた最終確認・取り込み待ち。

---

### ![Issue](icons/issue-opened.svg) [`:argdelete {pattern}` is not correctly updating the "argument list pointer" (argidx)](https://github.com/neovim/neovim/issues/40564) `#40564`
**ステータス:** Open | **ラベル:** なし

**概要:** `:argdelete {pattern}`（パターン指定）で引数リストから要素を削除すると、`argidx()` が正しく更新されず無効なインデックスが残るバグ報告。特に最後の引数を削除すると値が 1 つずれ、以降 `:argument` が E165/E163 エラーになる。`:[range]argdelete`（範囲指定）は正しく動くため、パターン指定版のみの問題と見られる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Open のまま。再現手順は詳細に記載されており、メンテナによるトリアージと修正が必要。

---

### ![PR](icons/git-pull-request.svg) [fix(filetype): detect directory buffer names](https://github.com/neovim/neovim/pull/40552) `#40552`
**ステータス:** Open | **ラベル:** filetype

**概要:** `vim.filetype.match()` がファイルシステムの stat を行わずにディレクトリバッファを安価に認識できるようにする修正。ディレクトリのバッファ名（フルネーム）を末尾スラッシュ付きにすることで、通常の filetype 判定経路を通す。#40529 に関連。セッションや ShaDa のバッファリスト復元との互換性のため、それらと対応テストも更新している。

**直近のアクティビティ:** justinmk と著者 barrettruth の間で相互にコメントのやり取りがあった（レビュー進行中）。

**変更ファイル:** 8 ファイルで追加 78 行・削除 6 行。中心は `src/nvim/buffer.c`（+39/-1）で、`runtime/lua/vim/filetype.lua`、`mksession_spec.lua` や `filetype_spec.lua` などのテストも更新。

**次のアクション:** レビュー中。指摘への対応と CI 通過を経てマージ判断へ。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): highlight code inside diff hunks](https://github.com/neovim/neovim/pull/40225) `#40225`
**ステータス:** Open | **ラベル:** treesitter

**概要:** `filetype=diff` の diff ファイル内のコードを treesitter で構文ハイライトする新機能 `diffhl`。各ハンクの旧側・新側を sigil を除いた有効なソースコードに再構成してからそれぞれの言語でパースし、ephemeral extmark を使ってビューポート内の可視行のみを再描画することで大きな diff でも高速に動作する。オプトアウト式のランタイムプラグインとして `{g,b}:diffhl` で制御する設計。

**直近のアクティビティ:** echasnovski・clason・zeertzjq らを交えたレビューが継続中で、著者 barrettruth が複数回コメント対応している（直近 24 時間内にも著者コメントあり）。

**変更ファイル:** 5 ファイルで追加 839 行・削除 0 行。新規追加の `runtime/lua/nvim/diffhl.lua`（+415）と `test/functional/treesitter/diffhl_spec.lua`（+393）が中心で、`plugins.txt`・`news.txt`・`ftplugin/diff.lua` も追加・更新。

**次のアクション:** 大規模な新機能でレビュー継続中。設計・実装への指摘対応を経て採否判断へ。

---

### ![Issue](icons/issue-opened.svg) [Make :make async](https://github.com/neovim/neovim/issues/40561) `#40561`
**ステータス:** Closed | **ラベル:** なし

**概要:** `:make` がコマンド完了まで UI をブロックするため、`:terminal` やジョブを使って（tpope の vim-dispatch のように）非同期化してほしいという要望。

**直近のアクティビティ:** msaher が #28881 の重複であると指摘し、Issue はクローズされた。

**次のアクション:** 重複としてクローズ済みのため対応不要。議論は #28881 に集約。

---

### ![PR](icons/git-pull-request.svg) [feat(json): add generic JSON-RPC connection support](https://github.com/neovim/neovim/pull/38525) `#38525`
**ステータス:** Open | **ラベル:** lsp

**概要:** これまで LSP 専用だった JSON-RPC クライアントを汎用化し、`vim.lsp.rpc` から汎用 RPC クライアント `vim.json.rpc` を抽出する PR。MCP や ACP など JSON-RPC ベースの新プロトコルを Neovim が活用できるようにするのが狙い。Transport → メッセージフレーミング → JSON-RPC Peer → LSP Client という階層構成で、ロードマップの複数項目は既に別 PR で完了済み。

**直近のアクティビティ:** 直近 24 時間のコメントはないが、3 月以降 TheLeoP・justinmk・teto・ofseed らによる長期の詳細レビューが継続している（エラーハンドリング変更に関する著者メモなど）。

**変更ファイル:** 9 ファイルで追加 781 行・削除 304 行。新規の `runtime/lua/vim/json/rpc.lua`（+422）と `_meta.lua`（+117）を追加し、`runtime/lua/vim/lsp/rpc.lua` を大幅に整理（+58/-304）、`lua.txt` も +140 更新。

**次のアクション:** 長期レビュー中。残る「エラーコールバックの明確化」など未完了ロードマップ項目への対応が必要。

---

### ![PR](icons/git-pull-request.svg) [cmdwin: *dark ritual*](https://github.com/neovim/neovim/pull/40463) `#40463`
**ステータス:** Open | **ラベル:** terminal, DO NOT MERGE

**概要:** #40407 のコメントで示された構想を実装し、式レジスタ（`<C-r>= C-f`）や `input()` からの cmdwin 起動をサポートする draft PR。多数の古い Issue/PR（#1496, #10842, #22374, #12103, #1716, #6233 など）のクローズを狙う。`run_in_terminal` の仕組みを新設している。

**直近のアクティビティ:** msaher が「`<C-f>` は本来ノーマルモードで始まるべきだが、この PR ではインサートモードで始まる。意図的か？」と質問。bfredl は既に Approve（画像付き）している。

**変更ファイル:** 31 ファイルで追加 1216 行・削除 71 行と大規模。新規 `runtime/lua/vim/_core/run_in_terminal.lua`（+181）や `test/functional/terminal/run_in_terminal_spec.lua`（+278）、`src/nvim/os/pty_proc_unix.c`（+122）、`cmdwin.lua`（+126/-31）などが中心。

**次のアクション:** DO NOT MERGE かつ draft。挙動に関する質問への回答と実装確定が必要で、当面マージはしない方針。

---

### ![PR](icons/git-pull-request.svg) [feat(pack): add 'packlockfile' option](https://github.com/neovim/neovim/pull/40562) `#40562`
**ステータス:** Open | **ラベル:** ux-visibility, packages

**概要:** `vim.pack` のロックファイルの場所を設定できる `'packlockfile'` オプションを追加する PR。`'shadafile'` に倣い、空文字なら何もせず、非空値でデフォルトのロックファイルパスを上書きする。#36078 を解決する。オプション名や値には強いこだわりはないと著者が述べている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 10 ファイルで追加 151 行・削除 11 行。`test/functional/plugin/pack_spec.lua`（+95）が中心で、`options.lua`・`option_vars.h`・`pack.lua`・`options.txt`・`pack.txt` などオプション定義・ドキュメントを更新。

**次のアクション:** レビュー待ち。オプション名・仕様（NONE による無効化の是非など）についての合意形成が必要。

---

### ![Issue](icons/issue-opened.svg) [cmdwin freedom](https://github.com/neovim/neovim/issues/40407) `#40407`
**ステータス:** Open | **ラベル:** refactor, cmdline-mode

**概要:** #40325 の後続として cmdwin 周りのリファクタリング項目を追跡する Issue。`CHECK_CMDWIN` の削除（#40484 で完了）、`BUFLOCK_OK`/`LOCK_OK` の統一（完了）、`nvim__cmdwin_set` の除去、式レジスタ・`input()` からの cmdwin 対応（#40463）、`'scrollbind'` の考慮などをチェックリスト化している。

**直近のアクティビティ:** msaher が、`input()` 中の `<C-f>` ヘビーユーザーとして機能喪失を懸念し、cmdwin 全体を復活させずに履歴編集・閲覧を実現する `vim.ui.input` 代替実装案（Lua コード付き）を提示した。

**次のアクション:** Open のトラッキング Issue。残チェック項目の実装継続と、`<C-f>`/`input()` 対応方針（#40463 のアプローチ vs 代替案）の合意が必要。

---

### ![Issue](icons/issue-opened.svg) [build(wasm): Add WebAssembly/Emscripten build target [Tracking issue]](https://github.com/neovim/neovim/issues/39987) `#39987`
**ステータス:** Open | **ラベル:** gsoc, wasm-webassembly, platform:web

**概要:** GSoC 2026 として WASM/Emscripten を Neovim の公式ビルドターゲットに追加し、ブラウザ上で完全動作させる作業のトラッキング Issue（#35567 に対応）。ブラウザで構文ハイライト・Lua・プラグイン・ヘルプ・xterm.js 端末を備えた `nvim.wasm` の生成を目標とする。Month 1 のビルドパイプライン・libuv スタブ・各種シンボルスタブは概ね完了している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Month 2 後半以降のランタイムファイルの MEMFS バンドル、msgpack RPC 動作検証、xterm.js フロントエンド実装などが今後の作業。

---

### ![Issue](icons/issue-opened.svg) [Implement `--remote-wait`](https://github.com/neovim/neovim/issues/24788) `#24788`
**ステータス:** Open | **ラベル:** remote

**概要:** `:h --remote-wait` が「Nvim ではまだ未サポート」となっている問題。`neovim --remote` は起動直後に終了してしまうため git のエディタとして使えず（空コミットメッセージで中断される）、`--remote-wait` のサポートが求められている。

**直近のアクティビティ:** rafaelcl292 が対応ブランチをリベースしたと報告し、justinmk に #37951 の再オープンを提案。着地に向けて協力する意向を示した。

**次のアクション:** 関連 PR #37951 の再オープン・レビュー再開が必要。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): make --remote wait by default with +async opt-out](https://github.com/neovim/neovim/pull/37951) `#37951`
**ステータス:** Closed | **ラベル:** remote, AI assisted 🤖

**概要:** #24788 を解決するため、`--remote` を既定でブロッキング（開いたバッファがアンロードされるまで待機）にし、非ブロッキングの明示的オプトアウトとして `+async` を追加する PR。quit 系コマンドは `rpcnotify` で送ってチャネルクローズ時のリクエスト競合を回避する。mhinz/neovim-remote なしで git エディタ用途を実現できる。

**直近のアクティビティ:** 著者 rafaelcl292 が、現状の実装では旧来の `--remote-*` 形式が実質削除され `--remote-wait`/`--remote-tab` などの接尾辞が未知として拒否される点を認め、後方互換のエイリアスとして残す方向でドキュメントとともに見直すと表明した。

**変更ファイル:** 12 ファイルで追加 824 行・削除 239 行。`test/functional/core/remote_spec.lua`（+549/-92）が大きく、`runtime/lua/vim/_core/editor.lua`（+137/-47）、`remote.txt`・`deprecated.txt`・`main.c` などを更新。

**次のアクション:** 現在 Closed。#24788 のコメントによれば再オープンが検討されており、旧形式の互換対応とドキュメント整備を経ての再挑戦が見込まれる。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add nvim_win_resize()](https://github.com/neovim/neovim/pull/40520) `#40520`
**ステータス:** Open | **ラベル:** api

**概要:** ウィンドウリサイズ時にどの辺（anchor）から領域を取るか選べる新 API `nvim_win_resize(win, {height, width, anchor})` を追加する PR（#6645 参照）。従来はリサイズ時に下・右のウィンドウから先に領域を取るが、この API で上・左方向へ伸ばすことも可能になる。既定 anchor は既存の `nvim_win_set_height()`/`nvim_win_set_width()` と同等の挙動を再現する。

**直近のアクティビティ:** zeertzjq と justinmk が多数のレビューコメントを付け、著者 XiaowenHu96 が対応。justinmk は「最後の数点対応後で LGTM。oldtest の問題が直る前提で、数日は他のコメントを待つ」として Approve した。

**変更ファイル:** 17 ファイルで追加 499 行・削除 174 行。`test/functional/api/window_spec.lua`（+172/-60）、`src/nvim/api/window.c`（+66/-39）、`src/nvim/window.c`、`api/deprecated.c`（+48）などを更新。

**次のアクション:** 実質 Approve 済み。oldtest の問題修正と追加コメントへの対応を経てマージへ。

---

### ![Issue](icons/issue-opened.svg) [Blink after exit](https://github.com/neovim/neovim/issues/40560) `#40560`
**ステータス:** Closed | **ラベル:** duplicate

**概要:** 0.12.3-4 へのアップグレード後、urxvt/xterm でブロックカーソルが nvim 終了後に点滅するようになったという報告。`$TERM` が `rxvt-unicode-256color`/`rxvt-256color` のときに発生し、`xterm-256color` に変えると回避できるが、`mutt` 等では回避策が効かないためバグとして報告された。

**直近のアクティビティ:** zeertzjq が #40249 で修正されそうだと指摘。報告者 marcvs は master からビルドし直すと `$TERM` に関係なく点滅すると追記した。Issue は duplicate としてクローズされた。

**次のアクション:** 重複としてクローズ済み。修正は #40249 側で追跡。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.{0770,0771}](https://github.com/neovim/neovim/pull/40559) `#40559`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted 🤖

**概要:** Vim パッチ 9.2.0770 と 9.2.0771 の移植。`dict_add_dict()`/`dict_add_list()` が失敗経路によって渡された dict/list を解放したりしなかったりと所有権が不整合で、呼び出し側（window.c 等）が失敗時に unref すると二重解放を起こす問題を修正。失敗時は渡されたオブジェクトを未変更のまま呼び出し側が所有し、成功時のみ参照を取るよう契約を統一する。vim/vim#20668 に関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `src/nvim/eval/typval.c` の 1 ファイルで追加 8 行・削除 2 行。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [test(treesitter): hide Lua parsers portably](https://github.com/neovim/neovim/pull/40558) `#40558`
**ステータス:** Closed | **ラベル:** test, treesitter

**概要:** treesitter テストで Lua パーサーを移植性のある形で隠す draft PR。ただし内容は diffhl 関連（#40225 と同じ `diffhl.lua`/`diffhl_spec.lua` の追加）で、誤ったベースブランチに対して開かれていた。

**直近のアクティビティ:** 著者 barrettruth が「誤ったベースブランチに対して開いたため」とコメントしてクローズした。

**変更ファイル:** 5 ファイルで追加 840 行・削除 0 行だが、ベースブランチ誤りのためクローズ。

**次のアクション:** クローズ済み。正しいベースで開き直す想定。

---

### ![Issue](icons/issue-opened.svg) [TUI: `--listen` startup error can be lost before `[Process exited 1]`](https://github.com/neovim/neovim/issues/40557) `#40557`
**ステータス:** Open | **ラベル:** test, tui, terminal, ci

**概要:** AI で作成されたバグ報告。`:terminal` 内で `--listen` の起動失敗（アドレス使用中）で短命の Nvim プロセスが status 1 で終了する際、`nvim: Failed to --listen: address already in use:` という致命的 stderr 行が端末バッファに表示されず `[Process exited 1]` だけが見えることがある。#34365 と同系統で、#38027 の修正が #38175 で revert された後の PTY 終了後出力欠落問題（#37982）に該当する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Open。プロセス終了マーカーより前に最終 PTY 出力を確実に読み取って描画する修正が必要で、CI の該当テスト（tui_spec.lua）の不安定さ解消が求められる。

---

### ![Issue](icons/issue-opened.svg) [regression: `:restart` breaks `nvim -d`](https://github.com/neovim/neovim/issues/40551) `#40551`
**ステータス:** Open | **ラベル:** bug-regression, lifecycle

**概要:** `:restart` 後に nvim が `-d`（diff モード）で設定されたオプション（`'diff'`, `'scrollbind'`, `'cursorbind'`, `'nowrap'` など）を忘れるリグレッション。#40321（コミット 845b66dd）で混入したと bisect で特定されている。

**直近のアクティビティ:** 報告者 e-kwsm は `:restart!` なら期待通り動くと確認。nathanzeng は `mksession`→`nvim -S` でも diff モードが復元されない例を挙げ、diff モード時は `:restart!` を使う案や `:restart!` が `-d` を落とすべきかを問うている。

**次のアクション:** Open。`:restart`（`!` なし）でコマンドライン由来のオプションを保持すべきか、diff モード時の挙動をどうするかの設計判断と修正が必要。

---

### ![Issue](icons/issue-opened.svg) [guidelines for plugins to handle sessions (`lua-plugin-sessions` help tag)](https://github.com/neovim/neovim/issues/40336) `#40336`
**ステータス:** Open | **ラベル:** editor-state

**概要:** プラグインがセッション復元時に自身の状態をどう復元すべきかの明確なガイドラインがないという Issue。(1) バッファ復元（例: fugitive）と (2) 内部データ復元（例: nvim-dap のブレークポイント）の 2 シナリオを挙げ、`SessionLoadPost` の利用や `'sessionoptions'` の globals 方式、サイドカーファイル方式などの利点・欠点を整理。完了条件として `lua-plugin-sessions` ヘルプタグの新設を求めている。

**直近のアクティビティ:** alex-courtis が URI バッファ方式が妥当で、nvim-tree も nvim-dap と同様に URI でバッファ種別を判別できると指摘。igorlfs は内部状態復元にも正当なユースケース（nvim-dap のブレークポイント、nvim-dap-view の watch 式）があると反論した。

**次のアクション:** Open。ベストプラクティスを記した新ヘルプタグの作成に向け、URI 方式・globals 方式などの推奨方針の議論を継続。

---

### ![Issue](icons/issue-opened.svg) [Python Tree-sitter becomes extremely slow on repeated f-strings](https://github.com/neovim/neovim/issues/40553) `#40553`
**ステータス:** Closed | **ラベル:** treesitter

**概要:** コミット b44c2bdd 以降、連続した f-string リテラルを多数含む Python ファイルを treesitter 有効で開くと極端に遅くなる（7 行で約 5 秒、8 行で 1 分以上ハング）バグ報告。match limit が撤廃されたことが背景だが、根本原因は Python の treesitter injection クエリにある。

**直近のアクティビティ:** barrettruth が再現を確認。Neovim は今後 treesitter クエリを管理しない（nvim-treesitter はアーカイブ済み）ため、python injection の無効化やパーサーマネージャの利用を案内しつつ、`runtime/queries/python/injections.scm` の修正 diff（`string_start`/`string_end` と `interpolation` を明示する形）を提示した。Issue はクローズされた。

**次のアクション:** クローズ済み。回避策と修正クエリが提示済みで、ユーザー側でのクエリ差し替えや injection 無効化で対応する。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40519) `#40519`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** CI ボット（github-actions[bot]）による `version.c` の自動更新 draft PR。vim-patch 9.2.0753（GTK GUI の遅延再描画が `'lazyredraw'` でスキップされる）と 9.2.0757（zsh でプロンプトのため test_popupwin が失敗）のエントリを反映する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `src/nvim/version.c` の 1 ファイルで追加 29 行・削除 31 行。

**次のアクション:** ボット生成の draft。対象パッチの N/A 判定・整理を経て取り込み判断へ。

---

### ![PR](icons/git-pull-request.svg) [fix(cmdwin): cmdwin is scrollbinded](https://github.com/neovim/neovim/pull/40556) `#40556`
**ステータス:** Open | **ラベル:** なし

**概要:** `'scrollbind'` が有効なウィンドウから cmdwin を開くと cmdwin まで scrollbind されてしまう問題の修正。旧来の cmdwin は `'noscrollbind'` を設定していたため、それに倣って `'noscrollbind'` を設定する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `runtime/lua/vim/_core/cmdwin.lua` の 1 ファイルで追加 1 行のみの小さな修正。

**次のアクション:** レビュー待ち。#40407 の cmdwin リファクタリング系の一環として取り込み判断へ。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): fix semantic tokens reset_timer() usage](https://github.com/neovim/neovim/pull/40554) `#40554`
**ステータス:** Open | **ラベル:** lsp

**概要:** semantic tokens の `reset_timer()` が client state の存在を確認せずに呼ばれ、`debounce_request()`→`send_request()`→`reset_timer()` の遅延実行時に client_state が既に消えていると nil 上でタイマーを参照してしまう問題の修正。`reset_timer` が state を直接受け取るようにして nil を排除し、state 存在チェックのガード内に呼び出しを移す。さらにクライアント detach 時にタイマーをリセットしてダングリングを防ぐ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `runtime/lua/vim/lsp/semantic_tokens.lua` の 1 ファイルで追加 9 行・削除 11 行。

**次のアクション:** レビュー待ち。nil デリファレンスのバグ修正として取り込み判断へ。

---

### ![Issue](icons/issue-opened.svg) [When can Neovim get rid of all Vimscript code?](https://github.com/neovim/neovim/issues/40555) `#40555`
**ステータス:** Closed | **ラベル:** なし

**概要:** Neovim にまだ大量の Vimscript コードが残っているのが受け入れがたく、Vimscript を除去すればコードがきれいになるのではないか、という趣旨の投稿。

**直近のアクティビティ:** barrettruth が、話したいなら discussion を立てるよう促し、既に何度も議論されている話題であること、互換性を保ちつつ Vimscript の新規追加を最小化しようとしている現状を説明した。Issue はクローズされた。

**次のアクション:** クローズ済み。議論は discussion/既存 Issue へ誘導。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0763: tests: style issue in test_plugin_netrw](https://github.com/neovim/neovim/pull/40543) `#40543`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** Vim パッチ 9.2.0763 の移植。v9.2.0761 後に生じた test_plugin_netrw のスタイル上の問題を修正し、新規作成したディレクトリのクリーンアップと一貫した連結演算子の使用を行う。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `test/old/testdir/test_plugin_netrw.vim` の 1 ファイルで追加 2 行・削除 2 行。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): detect N/A C files based on diff](https://github.com/neovim/neovim/pull/40473) `#40473`
**ステータス:** Open | **ラベル:** build, vim-patch

**概要:** Nvim に無関係（N/A）な Vim の C 関数を列挙した `vim_na_cfuncs.txt`（`src/proto/*.proto` ベース）を新設し、`git diff` の C シンボル検出を使って diff から N/A な C ファイルを判定するビルドスクリプト改善。差分が C シンボルを含まない、あるいはファイル移動のみの vim-patch を N/A として自動スキップし、レビュアーの手間を減らすのが狙い。v9.0.0515 以降の N/A パッチを検出できる。

**直近のアクティビティ:** 直近 24 時間のコメントなし（著者 janlazo が自己コメントを 1 件付与）。

**変更ファイル:** 2 ファイルで追加 64 行・削除 12 行。`scripts/vim-patch.sh`（+25/-12）を改修し、`scripts/vim_na_cfuncs.txt`（+39）を新規追加。

**次のアクション:** レビュー待ち。git のバージョン依存性や検出精度についての確認を経て取り込み判断へ。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> kitty graphics のストレージ刷新やクロスプラットフォームのビルド修正（Android の 64-bit atomics、Windows/MSVC の CRT リンク）が進む一方、macOS 固有の不具合対応と、vouch 制度による非承認ユーザーの PR/Issue 自動クローズが目立つ。

### ![Issue](icons/issue-opened.svg) [Dependency in build.zig.zon missing from build.zig.zon.txt](https://github.com/ghostty-org/ghostty/issues/13175) `#13175`
**ステータス:** Closed | **ラベル:** なし

**概要:** Arch Linux の *ghostty* パッケージメンテナが、`build.zig.zon` にあるある依存が `build.zig.zon.txt` に対応エントリを持たないと報告。この txt はビルド前にキャッシュを事前取得する `fetch-zig-cache.sh` ヘルパースクリプトが使うため、Git HEAD ビルド（AUR の `ghostty-git`）が失敗している。両ファイルの同期を求めている。

**直近のアクティビティ:** `ghostty-vouch[bot]` が、非メンテナは Issue を作成できず先に discussion を立てる必要があると通知し、この Issue は自動クローズされた。

**次のアクション:** メンテナ以外は Issue を立てられないため、報告者は discussion での再提起が必要。

---

### ![PR](icons/git-pull-request.svg) [kitty/gfx: fix build on targets without 64-bit atomics](https://github.com/ghostty-org/ghostty/pull/13174) `#13174`
**ステータス:** Merged | **ラベル:** なし

**概要:** bdc0b6c19 で追加された世代カウンタがプロセスグローバルな `std.atomic.Value(u64)` で、32-bit までしか atomic をサポートしない `arm-linux-androideabi` などで Zig コンパイラが 64-bit atomic を拒否し、libghostty-vt の Android ビルドが壊れていた。`nextGeneration` のカウンタを comptime で選択し、64-bit ターゲットはロックフリーの atomic、32-bit ターゲットは mutex で保護した u64 にフォールバックする。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `src/terminal/kitty/graphics_storage.zig` 1 ファイルのみ（+32 / -5）。

**次のアクション:** 既にマージ済みで対応不要。

---

### ![PR](icons/git-pull-request.svg) [kitty/gfx: add generation stamps, delete transmit time](https://github.com/ghostty-org/ghostty/pull/13173) `#13173`
**ステータス:** Merged | **ラベル:** vt, libghostty

**概要:** kitty graphics の画像ストレージに世代カウンタを導入し、内容の変更（画像の transmit/replace、placement の追加・削除）ごとにストレージへ新しいスタンプを割り当てる。同一画像 ID・同一寸法の再送を検出できなかった問題と、スクロールやリサイズでも立ってしまう dirty フラグの曖昧さを解消する。`Image.transmit_time` を完全に置き換え、eviction 順序やレンダラのテクスチャ鮮度チェックも世代値に一本化。delete が実際に何かを削除したときだけ mutation を記録するようにした。

**直近のアクティビティ:** 作者 `mitchellh` が、複数の新しい下流 libghostty-vt レンダラを開発中に発見した問題で、これにより kitty graphics の実装がより堅牢かつ高性能になると補足。

**変更ファイル:** 8 ファイル（+733 / -60）。`src/terminal/kitty/graphics_storage.zig`（+241）、`src/terminal/c/kitty_graphics.zig`（新規実装 262 行）、`include/ghostty/vt/kitty_graphics.h`、`example/c-vt-kitty-graphics/src/main.c` など。

**次のアクション:** 既にマージ済みで対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix: set max window clamp to current monitor size](https://github.com/ghostty-org/ghostty/pull/13171) `#13171`
**ステータス:** Open | **ラベル:** なし

**概要:** #7984 の修正。GTK がウィンドウを開いたディスプレイ基準で自身をクランプしていた問題に対し、現在のディスプレイからサイズを計算し、GTK 任せにせず明示的にウィンドウサイズを設定する。調査・修正には Claude Code（Opus 4.7）を利用したと明記。

**直近のアクティビティ:** `jcollie` が CHANGES_REQUESTED。Gnome 50 では新規ウィンドウが最小サイズで開き、`window-width`・`window-height` 設定が完全に無視されるため、この状態では受け入れられないと指摘。

**変更ファイル:** `src/apprt/gtk/class/window.zig` 1 ファイル（+31 / -0）。

**次のアクション:** レビューで変更要求が出ており、設定値を尊重するよう作者の修正対応が必要。

---

### ![PR](icons/git-pull-request.svg) [macOS: only read file urls for new-terminal services](https://github.com/ghostty-org/ghostty/pull/13169) `#13169`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS の Services 設定側で既にガードされているものの、new-terminal services で実際に必要な file URL のみを読むようにガードを追加する小さな変更。

**直近のアクティビティ:** `jparise` が APPROVED（コメント本文なし）。

**変更ファイル:** `macos/Sources/Features/Services/ServiceProvider.swift` 1 ファイル（+1 / -1）。

**次のアクション:** 承認済みのためマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [macOS: read string contents per pasteboard item](https://github.com/ghostty-org/ghostty/pull/13170) `#13170`
**ステータス:** Open | **ラベル:** os/macos

**概要:** #4956 由来のリグレッションを修正し、pasteboard の item ごとに文字列内容を読むようにする。`getOpinionatedStringContents` を変更する前に Claude がテストを先に書いたと AI Disclosure に記載。

**直近のアクティビティ:** 作者 `bo2themax` 自身が COMMENTED（本文なし）のレビューを付けている。

**変更ファイル:** 2 ファイル（+235 / -7）。`macos/Sources/Helpers/Extensions/NSPasteboard+Extension.swift`（+12 / -6）と、テスト `macos/Tests/NSPasteboardTests.swift`（+223）。

**次のアクション:** レビュー待ちの状態。

---

### ![PR](icons/git-pull-request.svg) [macOS: only read fileURL from clipboard](https://github.com/ghostty-org/ghostty/pull/13153) `#13153`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** #13152 の修正で、#4956 由来のリグレッション対応。クリップボードから fileURL のみを読むようにする。

**直近のアクティビティ:** 作者 `bo2themax` が「`isFileURL` チェックを削除すべきか検討する」とセルフコメント（チェック済み）。その後クローズされ、後継の #13170 に引き継がれた形。

**変更ファイル:** `macos/Sources/Helpers/Extensions/NSPasteboard+Extension.swift` 1 ファイル（+2 / -2）。

**次のアクション:** クローズ済み。対応は後継 PR #13170 に移行。

---

### ![Issue](icons/issue-opened.svg) [macOS: key events should stop repeating when press & hold is enable](https://github.com/ghostty-org/ghostty/issues/13168) `#13168`
**ステータス:** Open | **ラベル:** os/macos

**概要:** ghostty は Terminal.app 同様にキーリピートを通すため意図的に `ApplePressAndHoldEnabled` を無効化している。ユーザーがこれを再度有効化すると、アクセント入力ウィンドウは正しく表示されるものの、キーがリピートし続けてしまう。Terminal.app の挙動に合わせるべきという要望。

**直近のアクティビティ:** 作者 `bo2themax` が discussion #10669 の暫定回避策へのリンクを補足。

**次のアクション:** press & hold 有効時にリピートを止める実装対応が必要。

---

### ![PR](icons/git-pull-request.svg) [fix(build): Fix MSVC shared library CRT linking in libghostty-vt](https://github.com/ghostty-org/ghostty/pull/13151) `#13151`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows/MSVC で `libghostty-vt` を共有ライブラリとしてビルドすると、`linkLibC()` が `msvcrt.lib` しかリンクせず、DLL ビルドに必要な `vcruntime.lib` と `ucrt.lib` が欠けて失敗する。共有ライブラリ+MSVC ABI を検出し、Windows SDK の UCRT パスを解決して `libvcruntime`・`libucrt` を明示的にリンクする。`GhosttyLib.zig` の既存修正に揃える方針。

**直近のアクティビティ:** `pluiedev` が #13166 で手動 vouch したとコメント。`VectorPeak` が、自分はメンテナを名乗っておらず GPT 生成の回答も手動レビューしていると弁明するコメントを投稿。

**変更ファイル:** `src/build/GhosttyLibVt.zig` 1 ファイル（+31 / -0）。

**次のアクション:** 作者が vouch されたため、レビューとマージ判断待ち。

---

### ![Issue](icons/issue-opened.svg) [macos: toggling fullscreen increase window height](https://github.com/ghostty-org/ghostty/issues/2479) `#2479`
**ステータス:** Open | **ラベル:** os/macos, contributor friendly, needs-confirmation

**概要:** `Cmd + Ctrl + F` で非ネイティブフルスクリーンを繰り返しトグルすると、非フルスクリーン時のウィンドウ高さが毎回わずかに増えていく。`macos-titlebar-style = tabs` と `macos-non-native-fullscreen` 有効時に発生する。

**直近のアクティビティ:** `densukesf` が詳細な根本原因分析を投稿。単一タブ+`macos-titlebar-style = tabs`+非ネイティブフルスクリーン時のみ発生し、`NonNativeFullscreen.exit()` で `window.toolbar = savedState.toolbar` が AppKit の `_growWindowReshapeContentAndToolbarView` 経由で内部的に `setFrame` を呼び、高さが +8 される（`_distanceFromToolbarBaseToTitlebar` が toolbar 高 40 − offset 32 = 8 を返す）ことが原因と LLDB トレースとデコンパイル結果で特定。`Fullscreen.swift` で toolbar 復元後に `setFrame(...savedState.contentFrame...)` を呼ぶパッチを提案。

**次のアクション:** 提案されたパッチのレビューと、needs-confirmation ラベルの再現確認が必要。

---

### ![PR](icons/git-pull-request.svg) [Fix MSVC shared library CRT linking in libghostty-vt](https://github.com/ghostty-org/ghostty/pull/13164) `#13164`
**ステータス:** Closed | **ラベル:** なし

**概要:** vouch を受けたことを受けて #13151 を再提出した PR。内容は #13151 と同じ MSVC 共有ライブラリの CRT リンク修正。

**直近のアクティビティ:** `ghostty-vouch[bot]` が未 vouch ユーザーのため自動クローズ。その後 `00-kat` が「再オープンされた #13151 に取って代わられた（superseded）」とコメント。

**変更ファイル:** `src/build/GhosttyLibVt.zig` 1 ファイル（+31 / -0）。

**次のアクション:** クローズ済み。対応は #13151 に集約。

---

### ![PR](icons/git-pull-request.svg) [Manually vouch `eunos-1128` & denounce `VectorPeak`](https://github.com/ghostty-org/ghostty/pull/13166) `#13166`
**ステータス:** Merged | **ラベル:** なし

**概要:** #13163 を受け、`eunos-1128` を手動で vouch し `VectorPeak` を denounce する変更。`!denounce` コマンドが手動ターゲット指定時に OP を BAN すべきではないという課題にも言及。

**直近のアクティビティ:** `00-kat` が APPROVED（本文なし）。

**変更ファイル:** `.github/VOUCHED.td` 1 ファイル（+2 / -1）。

**次のアクション:** 既にマージ済みで対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13165) `#13165`
**ステータス:** Merged | **ラベル:** なし

**概要:** `pluiedev` の discussion コメント（#13163）を契機に `ghostty-vouch[bot]` が自動生成した PR。`eunos-1128` を denounce する VOUCHED リスト更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `.github/VOUCHED.td` 1 ファイル（+1 / -0）。

**次のアクション:** 既にマージ済みで対応不要。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump docker/build-push-action from 7.2.0 to 7.3.0](https://github.com/ghostty-org/ghostty/pull/13143) `#13143`
**ステータス:** Merged | **ラベル:** dependencies, github_actions

**概要:** Dependabot による GitHub Actions 依存の更新で、`docker/build-push-action` を 7.2.0 から 7.3.0 へバンプ。esbuild バンドルでの名前保持や各種依存のバージョンアップを含む。

**直近のアクティビティ:** `jcollie` が APPROVED（本文なし）。

**変更ファイル:** `.github/workflows/test.yml` 1 ファイル（+1 / -1）。

**次のアクション:** 既にマージ済みで対応不要。

---

### ![PR](icons/git-pull-request.svg) [Feat: Add workspace support](https://github.com/ghostty-org/ghostty/pull/13162) `#13162`
**ステータス:** Closed | **ラベル:** なし

**概要:** トップレベルの workspace サポートを追加する大規模な PR（README 参照）。GTK の workspace サイドバー UI や設定・キーバインド追加を含む。

**直近のアクティビティ:** `ghostty-vouch[bot]` が、作者が未 vouch であることを理由に自動クローズ。

**変更ファイル:** 29 ファイル（+1515 / -295）。`src/apprt/gtk/class/window.zig`（+337 / -208）、新規 `src/apprt/gtk/class/workspace.zig`（+360）、openspec 配下の多数の spec ファイル、`src/config/Config.zig`・`src/input/Binding.zig` などを含む。なお `PR_NOT_CREATED.txt` という不審なファイルの追加も含まれている。

**次のアクション:** vouch 未取得のため自動クローズ済み。再提出には vouch が必要。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump dorny/paths-filter from 4.0.1 to 4.0.2](https://github.com/ghostty-org/ghostty/pull/13159) `#13159`
**ステータス:** Merged | **ラベル:** dependencies, github_actions

**概要:** Dependabot による GitHub Actions 依存の更新で、`dorny/paths-filter` を 4.0.1 から 4.0.2 へバンプ。コンテナジョブでの git dubious ownership エラー回避や、古い git 互換のための `rev-parse` 利用などの修正を含む。

**直近のアクティビティ:** `jcollie` が APPROVED（本文なし）。

**変更ファイル:** 2 ファイル（+3 / -3）。`.github/workflows/release-tip.yml` と `.github/workflows/test.yml`。

**次のアクション:** 既にマージ済みで対応不要。

---

### ![PR](icons/git-pull-request.svg) [input: kitty should not encode modifier-only key unless "report all"](https://github.com/ghostty-org/ghostty/pull/326) `#326`
**ステータス:** Merged | **ラベル:** なし

**概要:** kitty キーボードプロトコルにおいて、修飾キーのみのキーは「report all」が有効なときにだけ送信されるべき、という修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし（2023 年作成の古い PR で updated_at のみ更新）。

**変更ファイル:** `src/input/KeyEncoder.zig` 1 ファイル（+37 / -1）。

**次のアクション:** 既にマージ済みで対応不要。

---

## astral-sh/uv

> 直近は charliermarsh を中心に、推移的なプレリリース依存の解決（#19993 とその代替案 #20098）と Simple API パースや site-packages スキャンなどの性能最適化 PR が活発。加えて、エラーメッセージ改善やヒント追加といった小さな UX 修正 PR が多数投稿されている。

### ![PR](icons/git-pull-request.svg) [Support transitive pre-release dependencies](https://github.com/astral-sh/uv/pull/19993) `#19993`
**ステータス:** Open | **ラベル:** enhancement, breaking

**概要:** 従来はパッケージがプレリリースを使えるかを解決前に確定させる必要があり、推移的メタデータから `foo>=2.0.0b1` のような要求が現れてもプレリリースが有効化されなかった。本 PR は推移的なプレリリース要求を PubGrub のプロキシパッケージで表現し、安定版を優先しつつ枯渇時にプレリリースを検討するようにする。あわせて `--prerelease=explicit` は廃止され `if-necessary-or-explicit` のエイリアスになる。

**直近のアクティビティ:** charliermarsh が Damian との議論を要約し、本方式でも nab 方式でも順序次第で安定版が pin される場合はあるが、「解が存在すればエラーにせず見つける」ことを保証すると説明。bot の test-inventory では新規テスト 34 件・削除 1 件を報告。CodSpeed は 6.61% の性能低下（`resolve_warm_airflow` -7.07% など 2 件のリグレッション）を警告。

**変更ファイル:** 60 ファイルで +3801/-778。`crates/uv-resolver/src/candidate_selector.rs`（+105/-23）や `resolver/mod.rs`（+104/-37）、`pubgrub/package.rs`（+43）などリゾルバ中核を改修。多数の `pip_install_scenarios.rs`（+1429/-305）やシナリオ TOML の追加でプレリリース挙動を検証。

**次のアクション:** Open。CodSpeed の性能低下をレビュー・許容判断するか改善が必要。breaking 変更のためレビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [Reduce Simple API parsing CPU](https://github.com/astral-sh/uv/pull/20104) `#20104`
**ステータス:** Open（draft）| **ラベル:** performance

**概要:** 大きな Simple API レスポンスでは同じ `requires-python` 値が多数のファイルに繰り返し現れるが、従来は個別にパースしファイル名で全体ソートしていた。本 PR は distinct な `requires-python` 文字列をレスポンスごとに一度だけパースして `VersionSpecifiers` をインターンし、グルーピング後に各バージョンの wheel/sdist リストのみソートして決定性を保つ。合成 10 万ファイル応答でパース・グループ・アーカイブが 238.4ms→188.3ms（21%高速化）などの改善を報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 9 ファイルで +313/-151。中心は `crates/uv-pypi-types/src/simple_json.rs`（+256/-136）。`uv-client/src/html.rs` や `registry_client.rs`、`uv-cache/src/lib.rs` なども小規模に修正。

**次のアクション:** Open（draft）。ドラフト解除とレビューを待つ状態。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate（Mend.io）が自動生成する依存関係更新のダッシュボード Issue。放置扱いの依存 33 件、スケジュール待ちの Docker/GitHub Actions/Rust クレート更新、ステータスチェック待ちや編集ブロック中の PR などを一覧化している。Hidden Unicode 文字がリポジトリ内で検出された旨の警告も含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Open（bot 管理）。人手対応は不要で、必要な更新はチェックボックス操作で反映する運用。

---

### ![PR](icons/git-pull-request.svg) [Reduce VersionSpecifiers parsing allocations](https://github.com/astral-sh/uv/pull/20105) `#20105`
**ステータス:** Merged | **ラベル:** performance

**概要:** `VersionSpecifiers::from_str` は全制約を growable な `Vec` に集めてソートし boxed slice に変換していたが、`requires-python` の多くは単一節のため無駄な予備容量とソートが発生していた。本 PR は空・単一節のコレクションを直接構築し、区切りを一度数えて多節入力が正確な最終サイズを確保するようにする。プロファイルビルドで単一節が 129ns→74ns（-43%）に改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし（マージ済み）。

**変更ファイル:** 2 ファイルで +35/-6。`crates/uv-pep440/src/version_specifier.rs`（+19/-5）と、3 形状をカバーするベンチ `crates/uv-bench/benches/uv.rs`（+16/-1）。

**次のアクション:** Merged。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Reduce site-packages scan allocation overhead](https://github.com/astral-sh/uv/pull/20087) `#20087`
**ステータス:** Merged | **ラベル:** performance

**概要:** `SitePackages::from_interpreter` を、各パスを `BTreeSet` に挿入する方式から、候補パスを `Vec` に集めてソートする方式へ変更する。インデックス化前にソートするため決定性は保たれつつ、`site-packages` 走査時のツリー挿入オーバーヘッドを回避する。

**直近のアクティビティ:** レビューで zsol が Approve（「Nice, thanks!」）。

**変更ファイル:** 1 ファイルで +61/-25。`crates/uv-installer/src/site_packages.rs` のみ。

**次のアクション:** Merged。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Support transitive pre-release dependencies with admission ranges](https://github.com/astral-sh/uv/pull/20098) `#20098`
**ステータス:** Open（draft）| **ラベル:** なし

**概要:** #19993 の代替案。packaging#1311 と nab v0.0.6 で採用された「クリップ済みプレリリース admission」モデルに従い、各 PubGrub バージョン範囲に論理バージョン集合とオプションのプレリリース admission 領域を持たせる。プレリリースを名指す要求は自身の範囲内でのみ admission を与え、交差・和で領域を再クリップし、補集合は admission を消去して除外がプレリリースを有効化しないようにする。

**直近のアクティビティ:** bot の test-inventory が新規テスト 25 件・削除 1 件を報告。CodSpeed は 5.53% の性能低下（`resolve_warm_jupyter_universal` などのリグレッション 2 件）を警告。

**変更ファイル:** 62 ファイルで +3895/-642。新規 `crates/uv-resolver/src/pubgrub/range.rs`（+411）を中心に `candidate_selector.rs`（+120/-34）、`prerelease.rs`（+26/-116）、`error.rs`（+50/-64）などを改修。シナリオテスト群も追加。

**次のアクション:** Open（draft）。#19993 とどちらを採用するかの方針決定と、性能低下の評価が必要。

---

### ![PR](icons/git-pull-request.svg) [Update Rust toolchain to 1.96.1](https://github.com/astral-sh/uv/pull/20103) `#20103`
**ステータス:** Merged | **ラベル:** なし

**概要:** uv と `uv-build` の pin された Rust ツールチェーンを 1.96.1 に戻す。Cargo 1.96.0 が一時的な HTTP エラーを致命的に扱う問題（#19953）のため 1.95 に一時ダウングレードしていたが、Rust 1.96.1 で Cargo の HTTP クライアントの再試行・タイムアウト欠落が修正されたためアップグレードする。

**直近のアクティビティ:** 直近 24 時間のコメントなし（マージ済み）。

**変更ファイル:** 2 ファイルで +2/-2。`crates/uv-build/rust-toolchain.toml` と `rust-toolchain.toml` のバージョンピンのみ。

**次のアクション:** Merged。対応不要。

---

### ![Issue](icons/issue-opened.svg) [`uv pip compile --universal` does not use the correct lower bound](https://github.com/astral-sh/uv/issues/20102) `#20102`
**ステータス:** Open | **ラベル:** bug

**概要:** `pynwb==4.0.0` と `dandi==0.76.5` を `uv pip compile --generate-hashes --universal` でコンパイルすると、生成された requirements.txt が Python 3.11 on Linux で動作しない。`--python-version 3.11` を追加で渡した場合のみ動作する。verbose ログでは uv が上位フォルダの `.venv` を見て Python バージョンを決めているように見え、`--universal` が最小 Python バージョンを正しく下限に取っていない疑いがある。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Open。メンテナによる原因調査（`--universal` 時の下限 Python バージョン決定ロジックの確認）が必要。

---

### ![PR](icons/git-pull-request.svg) [Discover extensionless shebang scripts in `uv workspace list --scripts`](https://github.com/astral-sh/uv/pull/20099) `#20099`
**ステータス:** Open（draft）| **ラベル:** なし

**概要:** `uv workspace list --scripts` は `.py` と `.pyw` のみを探索し、拡張子なしのシェバン付き PEP 723 スクリプトを見逃していた。本 PR は拡張子なしファイルも探索対象に含め、各ファイルの先頭 2 バイトを読み `#!` でなければ中断し、シェバン候補は NUL か不正 UTF-8 まで逐次読み込む。ベンチではページキャッシュ排除時に 84.6ms 対 77.8ms の増加を示す。

**直近のアクティビティ:** bot の test-inventory が新規テスト 3 件を報告。

**変更ファイル:** 2 ファイルで +113/-11。`crates/uv/src/commands/workspace/list.rs`（+103/-9）と対応テスト。

**次のアクション:** Open（draft）。ドラフト解除とレビューを待つ状態。

---

### ![PR](icons/git-pull-request.svg) [Follow-up from #16542](https://github.com/astral-sh/uv/pull/16749) `#16749`
**ステータス:** Open | **ラベル:** なし

**概要:** #16542 のレビュー指摘に対するフォローアップ。`--python-downloads-json-url` のテストを `network.rs` に追加すること、および `CachedClient` を使うことがチェックリストとして掲げられ、いずれも対応済みとされている。

**直近のアクティビティ:** konstin と MeitarR がそれぞれ 2 回ずつレビューコメント（本文は空でスレッド上の指摘）を投稿。

**変更ファイル:** 8 ファイルで +96/-38。中心は `crates/uv-python/src/downloads.rs`（+69/-22）で、`discovery.rs` や各 python コマンドも小規模に修正。

**次のアクション:** Open。レビューコメントへの対応を確認し、マージ判断を要する。

---

### ![Issue](icons/issue-opened.svg) [Make importlib.metadata.packages_distributions work with uv build backend](https://github.com/astral-sh/uv/issues/16423) `#16423`
**ステータス:** Open | **ラベル:** external, build-backend

**概要:** uv build backend でビルドしたパッケージに対し `importlib.metadata.packages_distributions` が正しく動作しない。setuptools を backend にすると動くが、uv では top-level 名が供給されないため機能しない。報告者は当初バグと考えたが、editable install では信頼できないと公式ドキュメントにあるため機能要望寄りと整理している。

**直近のアクティビティ:** bunny-therapist が、pyproject.toml に明示指定した場合のみ uv build が import-names/import-namespaces を METADATA に書く現状を報告し、意図された最終形か・ドキュメント整備を求めるコメント。

**次のアクション:** Open。想定挙動（明示指定が必須か）の方針決定とドキュメント整備が必要。

---

### ![Issue](icons/issue-opened.svg) [Implement PEP 794 "Import Name Metadata" in uv_build](https://github.com/astral-sh/uv/issues/16435) `#16435`
**ステータス:** Open | **ラベル:** enhancement, build-backend

**概要:** 受理された PEP 794（Import Name Metadata）とそのフィールドを uv の build backend でサポートすべき、という機能要望。

**直近のアクティビティ:** bunny-therapist が、pyproject.toml に `import-names` を明示指定しないと機能しない点を指摘し、`tool.uv.build-backend.module-name` との役割の重複や、uv-build が自動で populate できないか、使い方の明確化を求めるコメント。

**次のアクション:** Open。`import-names` と `module-name` の関係整理と使用方法のドキュメント化・実装方針の検討が必要。

---

### ![PR](icons/git-pull-request.svg) [fix: allow uv tool uninstall to remove tools with corrupt receipts](https://github.com/astral-sh/uv/pull/20101) `#20101`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv-receipt.toml` が壊れると `uv tool list` は `uv tool uninstall <name>` を提案するが、その uninstall は壊れた receipt のパースに失敗して実行できなかった。本 PR は壊れた receipt を欠損 receipt と同様に扱い、警告を出して環境ディレクトリを削除するようにする。`get_tool_receipt` の `Result` 全体で分岐し、`Ok(None)` と `Err(_)` の両方で `remove_environment` を試みる。#19630 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイルで +23/-18。`crates/uv/src/commands/tool/uninstall.rs` のみ。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![Issue](icons/issue-opened.svg) [Enterprise CA `UnsupportedCriticalExtension` TLS Failure](https://github.com/astral-sh/uv/issues/19903) `#19903`
**ステータス:** Open | **ラベル:** external

**概要:** 企業の SSL インスペクション環境で、critical な `X509v3 Certificate Policies`（anyPolicy）拡張を持つインスペクション中間証明書に対し、uv が `UnsupportedCriticalExtension` で HTTPS 接続に失敗する。同じチェーンは OpenSSL・curl・Git・ブラウザでは受理される。rustls/webpki がハンドシェイク検証で当該 critical 拡張を拒否するため、`--system-certs` や `UV_SYSTEM_CERTS` では回避できない。

**直近のアクティビティ:** hohwille が `UnknownIssuer` の類似エラーで影響を受けていると報告し、どの証明書が問題かを特定できるよう証明書 identity をログ出力する改善を要望。

**次のアクション:** Open。rustls/webpki の証明書検証挙動への対応方針検討と、エラー出力の改善が必要。

---

### ![Issue](icons/issue-opened.svg) [gui-scripts entry point doesn't persist when launched via `uv run` on Windows (works when trampoline exe invoked directly)](https://github.com/astral-sh/uv/issues/20100) `#20100`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows で `gui-scripts` エントリポイントを `uv run <name>` 経由で起動すると終了コード 0 でエラーもないが、GUI プロセスが持続せずウィンドウが残らない。生成された trampoline exe（`.venv\Scripts\<name>.exe`）を直接起動すると正常動作する。`uv run <name>` が trampoline 直接起動と異なる方法で GUI サブシステムプロセスの生存を追跡・待機している可能性を示唆。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Open。`uv run` による gui-scripts 起動時のプロセス待機挙動の調査が必要。

---

### ![PR](icons/git-pull-request.svg) [Avoid shared temporary directories for Windows Python launchers](https://github.com/astral-sh/uv/pull/20019) `#20019`
**ステータス:** Closed | **ラベル:** windows

**概要:** Windows の Python ランチャー構築は共有の `%TEMP%` に一時ディレクトリを作り、そこで書き込み・更新・読み戻しをした後、宛先脇に別の一時ファイルを書いてアトミック置換していた。本 PR はバイト列返却の Python ランチャー API を廃し、宛先脇の一時ファイルへ直接構築する。作成専用パスは no-clobber 永続化、置換パスは既存のリトライ付きアトミック永続化を用い、完全な PE ランチャー構築後にのみ宛先へ触れる。

**直近のアクティビティ:** zsol が「共有一時ディレクトリの何が問題か、中断時に宛先を汚す懸念がある」とレビュー。charliermarsh は「目的は冗長なラウンドトリップの回避」で、`%TEMP%` 構築→読み戻し→再書き込みを 1 回の宛先脇構築に置き換えるものだと説明。

**変更ファイル:** 3 ファイルで +48/-64。`crates/uv-trampoline-builder/src/lib.rs`（+24/-46）、`uv-python/src/managed.rs`、`uv-fs/src/lib.rs`。

**次のアクション:** Closed。クローズ済みのため対応不要（必要なら再検討）。

---

### ![PR](icons/git-pull-request.svg) [Avoid blocking the async runtime when creating temporary files](https://github.com/astral-sh/uv/pull/20089) `#20089`
**ステータス:** Open | **ラベル:** なし

**概要:** 一時ファイル作成はファイルオープンを伴いブロックしうる（#19429）。複数の async パスがブロッキングな `tempfile` API を使い tokio ランタイムを止める可能性があるため、`uv-fs` に `spawn_blocking` 経由で `tempfile_in` を実行する `tempfile_in_async` ヘルパーを追加し、`write_atomic` などの async 呼び出し箇所を置き換える。実際にランタイム上で動く箇所のみ変換し、同期パスは据え置く。

**直近のアクティビティ:** bot の test-inventory が新規テスト 1 件（`tempfile_in_async_creates_file_in_directory`）を報告。

**変更ファイル:** 2 ファイルで +45/-5。`crates/uv-fs/src/lib.rs`（+37/-2）と `uv-distribution/src/distribution_database.rs`。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![Issue](icons/issue-opened.svg) [Performance issues when using multiple git+ssh dependencies](https://github.com/astral-sh/uv/issues/15746) `#15746`
**ステータス:** Open | **ラベル:** bug, external, network

**概要:** git+ssh 依存を約 4 件以上使うと接続が失敗する。報告者は原因を GitHub 側と推測しつつ、他者の参考のため記録している。同一 git リポジトリの複数サブディレクトリを依存に指定するモノレポ構成で発生。

**直近のアクティビティ:** ngnpope が「ssh 限定ではない」とし、同一コミットを複数パッケージが参照する https 版の requirements.txt を提示して同様の問題を報告。

**次のアクション:** Open。ssh 以外でも再現するとの情報を踏まえ、同一リポジトリへの並行接続挙動の調査が必要。

---

### ![PR](icons/git-pull-request.svg) [fix: show lock mismatch error even with --quiet](https://github.com/astral-sh/uv/pull/20097) `#20097`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv sync --dev --locked --quiet` や `uv lock --locked --quiet` 実行時、ロックファイル更新が必要という重大なエラーが `--quiet` で `printer.stderr()` が無効化されるため黙って抑制されていた。本 PR は `sync.rs` と `lock.rs` のロック不一致エラーで `printer.stderr()` を `stderr_important()` に変更し、quiet モードでもエラー出力を保持する。#19326 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 3 ファイルで +3/-4。`project/lock.rs`、`project/sync.rs`、`printer.rs`。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [fix: detect when `pyproject.toml` is a directory and show a clear error](https://github.com/astral-sh/uv/pull/20096) `#20096`
**ステータス:** Open | **ラベル:** なし

**概要:** `pyproject.toml` が通常ファイルでなくディレクトリとして存在する場合、uv は読み取りを試みて `Is a directory (os error 21)` という分かりにくい OS エラーで失敗していた。本 PR はワークスペース探索時にこれを検出し「`pyproject.toml` at ... is a directory, expected a file」という明確なエラーを表示する。`WorkspaceErrorKind::PyprojectTomlIsDirectory` を追加。#14584 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイルで +72/-7。`crates/uv-workspace/src/workspace.rs`（+47/-7）と統合テスト。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [fix(uvx): show correct flag name in error when --from path is invalid](https://github.com/astral-sh/uv/pull/20095) `#20095`
**ステータス:** Open | **ラベル:** なし

**概要:** `uvx --from ./nonexistent-path some-command` のように不正なローカルパスを渡すと、エラーメッセージが `--from` ではなく誤って `--with` を表示していた。本 PR は `--from` が指定されたかを確認し、エラーコンテキストで正しいフラグ名を使う。#8757 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイルで +23/-1。`crates/uv/src/commands/tool/run.rs`（+5/-1）とテスト。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [fix: detect uv lockfile passed to -r flag and show actionable error](https://github.com/astral-sh/uv/pull/20094) `#20094`
**ステータス:** Open | **ラベル:** なし

**概要:** ユーザーが誤って uv.lock を `-r` に渡した（`uv pip install -r uv.lock`）場合に、`RequirementsTxt::parse_impl` で早期にファイル形式を検出し、`uv sync` や `uv lock` を促す明確なエラーを表示する。#16192 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイルで +52/-0。`crates/uv-requirements-txt/src/lib.rs` のみ。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [Add `package/command` shorthand to `uvx` and `uv tool run`](https://github.com/astral-sh/uv/pull/20091) `#20091`
**ステータス:** Open | **ラベル:** enhancement

**概要:** パッケージが別名のコマンドを提供する場合、従来は `uvx --from httpie http` のように `--from` が必要だった。本 PR は `<package>/<command>` 短縮記法を両エントリポイントに追加し、`uvx httpie/http` で `httpie` を選び `http` を実行できるようにする。スラッシュ 1 個・左辺が有効なパッケージ名・右辺がパス区切りを含まない非空コマンド、という条件で、明示的 `--from` や URL、ネストパスは従来のパースを維持する。

**直近のアクティビティ:** bot の test-inventory が新規テスト 3 件を報告。

**変更ファイル:** 4 ファイルで +112/-2。`crates/uv/src/commands/tool/run.rs`（+54）、`uv-cli/src/lib.rs`、テスト、`docs/guides/tools.md`。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [fix: hint at uv init when pyproject.toml is not found](https://github.com/astral-sh/uv/pull/20093) `#20093`
**ステータス:** Open | **ラベル:** なし

**概要:** プロジェクトディレクトリ外で `uv add` や `uv sync` を実行した際のエラーに、`uv init` や getting-started ガイドへの案内を追加する。pip から移行するユーザー向けに `WorkspaceErrorKind::MissingPyprojectToml` のメッセージを拡充。#12988 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 8 ファイルで +12/-12。`crates/uv-workspace/src/workspace.rs`（+1/-1）の本体変更に加え、メッセージ変更に伴う各テストのスナップショット更新。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![PR](icons/git-pull-request.svg) [fix: hint supported Python request formats when executable name not found](https://github.com/astral-sh/uv/pull/20092) `#20092`
**ステータス:** Open | **ラベル:** なし

**概要:** `foobar` や `@311` など認識できない Python リクエストは `ExecutableName` として扱われ、インタプリタが見つからないと理由だけのエラーになっていた。本 PR は `PythonNotFound::Display` を修正し、`ExecutableName` の場合に `3.12`、`python3.12`、`cpython-3.12`、`pypy3.8` などサポートされる書式例をエラーに付加する。#4401 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 3 ファイルで +9/-2。`crates/uv-python/src/discovery.rs`（+7）とテストのスナップショット更新。

**次のアクション:** Open。レビューとマージ判断を要する。

---

### ![Issue](icons/issue-opened.svg) [Allow opt-in reuse of isolated build environments for native-build backends (scikit-build-core, meson-python)](https://github.com/astral-sh/uv/issues/20090) `#20090`
**ステータス:** Open | **ラベル:** enhancement

**概要:** uv は PEP 517 ビルドごとに新しい一時パスへ使い捨ての isolated venv を作る。setuptools 等では妥当だが、scikit-build-core や meson-python のように CMake/Ninja・Meson を駆動する backend では、絶対パスを保持するネイティブビルドキャッシュ（CMakeCache.txt 等）が venv パスの変化で毎回無効化される。結果として編集・反復的な C 拡張開発でほぼ完全な再構成・再ビルドコストが発生する。isolated build 環境の opt-in な再利用を求める機能要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Open。ネイティブビルド backend 向けの build 環境再利用のオプトイン設計の検討が必要。

---

### ![Issue](icons/issue-opened.svg) [uv-build includes empty directories in bdist/sdist](https://github.com/astral-sh/uv/issues/19404) `#19404`
**ステータス:** Open | **ラベル:** bug

**概要:** uv-build が bdist/sdist に空ディレクトリを含めてしまい（setuptools は含めない）、bit-for-bit の再現性が得られない。ブランチ切り替えやディレクトリ改名で `__pycache__` だけ残った空ディレクトリが対象。空ディレクトリを追加して `uv build` し、`tar tf`/`unzip -l` で確認すると再現する。

**直近のアクティビティ:** hexbinoct が、これは #19437（5 月 17 日マージ）で既に修正済みで、現在の main では包含ファイルの祖先でない限りディレクトリエントリをスキップするためクローズしてよさそうとコメント。

**次のアクション:** Open。#19437 で修正済みか確認のうえクローズ判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/20069) `#20069`
**ステータス:** Open（draft）| **ラベル:** なし

**概要:** Python リリースの自動更新 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイルで +12/-12。`crates/uv-python/download-metadata.json` のみ。

**次のアクション:** Open（draft）。自動更新の内容を確認しマージ判断を要する。

---

## microsoft/vscode

> 直近の活動は Agent Host / Agents ウィンドウ（Copilot CLI・Claude・Codex エージェント）と Sessions 周りの新機能・リファクタリングに集中しており、BYOK モデル対応やチャット描画のバグ修正、Modern UI 向け style-override も多数進行中。ユーザー報告では拡張機能の署名検証失敗や Copilot 補完フィードバック、CLI セッションの不具合が目立つ。

### ![PR](icons/git-pull-request.svg) [sessions: single-pane detail-panel layout for the Agents window](https://github.com/microsoft/vscode/pull/324257) `#324257`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウ向けに、詳細パネル（auxiliary bar）をエディタ部内にドッキングして単一のタブバーで統合表示する実験的レイアウトを、新設定 `sessions.layout.singlePaneDetailPanel`（既定 off）の背後に追加する。`DockedAuxiliaryBarController` や `SinglePaneAuxiliaryBarPart`、multi-diff の `SessionChangesEditor`、`MenuId.EditorTabsBarAddTab` による「+」タブなどを導入。設定 off 時は既存レイアウトを byte-for-byte 不変に保つよう、subclass と起動時登録で分岐している。

**直近のアクティビティ:** github-actions[bot]が 176 件のスクリーンショット差分を報告し、CODENOTIFY で @benibenj に通知。copilot-pull-request-reviewer[bot]がレビュー概要をコメント、roblourens のレビューは DISMISSED、sandy081 が複数コメントを付与している。

**変更ファイル:** 37 ファイル・+2018 / -184。`src/vs/sessions/browser/` 配下のパーツやコントローラ、`editorPart.ts`・`editorGroupView.ts` などエディタ部のコアと LAYOUT.md 系ドキュメントが対象。

**次のアクション:** レビュー指摘の反映と実験設定の検証を経てマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Extension Signature Verification Failed: Magic Racket (and other extensions)](https://github.com/microsoft/vscode/issues/324273) `#324273`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 1.125.0（Linux）で anthropic.claude-code・openai.chatgpt・evzen-wybitul.magic-racket など複数拡張のインストール時に `SignatureVerificationInternal: Signature verification failed with 'UnknownError' error.`（Internal Code: 159）が発生し、拡張をインストールできない。sharedProcess で uncaught exception となっている。

**直近のアクティビティ:** vs-code-engineering[bot]が、最新安定版 1.127.0 への更新を促す定型コメントを投稿。

**次のアクション:** 最新版での再現確認と署名検証エラーの原因調査が必要。

---

### ![PR](icons/git-pull-request.svg) [Fix skill invocation attribution for subagents](https://github.com/microsoft/vscode/pull/324272) `#324272`
**ステータス:** Open | **ラベル:** なし

**概要:** #324271 の修正。`skill.invoked` イベントが subagent 由来の場合に `parentToolCallId` が欠落し、スキル呼び出しがトップレベルチャットに表示されてしまう問題を修正する。event レベルの `agentId` マッピングで subagent スコープを解決し、start/ready/complete アクションをそのスコープへ振り分け、未知の subagent ID のイベントは破棄する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント。`copilotAgentSession.ts` の合成ツールコールへの subagent 帰属付与を確認している。

**変更ファイル:** 2 ファイル・+69 / -4。`copilotAgentSession.ts` と対応するテストのみ。

**次のアクション:** レビュー完了後にマージ。

---

### ![Issue](icons/issue-opened.svg) [Extension Signature Verification Failed: Vue (Official), Codex & Claude](https://github.com/microsoft/vscode/issues/324180) `#324180`
**ステータス:** Open | **ラベル:** なし

**概要:** #324273 と同種の署名検証失敗。vue.volar・anthropic.claude-code・openai.chatgpt など複数拡張のインストール時に `Signature verification failed` が発生する。

**直近のアクティビティ:** cameron-git が「同じ問題」と報告、dylantf と TurnrDev が 1.127.0（および各自の最新版）でも継続すると報告。vs-code-engineering[bot]は最新版更新を促す定型コメントを投稿している。

**次のアクション:** 最新版でも再現しているため、署名検証エラーの根本調査が必要。

---

### ![Issue](icons/issue-opened.svg) [Skill invocations from subagents appear in the top-level chat](https://github.com/microsoft/vscode/issues/324271) `#324271`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** subagent がスキルを呼び出すと、その呼び出しが subagent ではなくトップレベルチャットに表示される。ライブの `skill.invoked` イベント経路が影響を受け、復元済み履歴では正しく帰属される。#324272 で対応中。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 対応 PR #324272 のマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Agent host: run `!`-prefixed chat messages as terminal commands](https://github.com/microsoft/vscode/pull/324270) `#324270`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットで `!` から始まるメッセージをモデルに送らず端末コマンドとして実行する「bang command」機能を追加し、ホスト注入コマンド処理を `LocalChatCommandRegistry` へリファクタリングする。加えて host-injected turn（`!command` / `/rename`）の永続化、peer-chat の truncation ルーティング修正などを含む。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメントし、bang command と registry リファクタの内容を要約。

**変更ファイル:** 26 ファイル・+1980 / -439。`agentHostBangCommand.ts`・`localCommands/` 配下・`agentSideEffects.ts`・`sessionDataService.ts` などが対象。

**次のアクション:** 規模が大きいためレビュー精査後にマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Copilot CLI sessions don't appear in the Agents window session list](https://github.com/microsoft/vscode/issues/319415) `#319415`
**ステータス:** Open | **ラベル:** なし

**概要:** 端末で開始した Copilot CLI セッション（`copilot`）が、Agents ウィンドウの Sessions サイドバーに表示されない。通常の VS Code Chat の Sessions パネルには表示されるため、両者でセッション管理のバックエンドが分かれていることが原因と見られる。

**直近のアクティビティ:** SlothCroissant が +1 し、CLI と Agents UI・サイドバーチャットでセッションが統一されるべきだと主張している。

**次のアクション:** セッション管理バックエンドの統一方針の検討が必要。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/324269) `#324269`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** Copilot 補完に関するフィードバック issue だが、再現手順や具体的な問題内容がほぼ空欄で、補完テキストと診断情報のみが記載されている。

**直近のアクティビティ:** vs-code-engineering[bot]が、issue 報告ガイドラインに沿った情報補完を求める定型コメントを投稿。

**次のアクション:** 報告者による情報追記待ち。

---

### ![PR](icons/git-pull-request.svg) [Include proxy config source in telemetry event](https://github.com/microsoft/vscode/pull/324056) `#324056`
**ステータス:** Merged | **ラベル:** なし

**概要:** 拡張ホストの patched `@vscode/proxy-agent` から `resolveProxyByURL` を公開し、Copilot 拡張の fetcher テレメトリで、解決されたプロキシ種別に加えて「どのプロキシ設定ソースが結果を決めたか」もイベントに含める。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、pwang347 が APPROVED。

**変更ファイル:** 7 ファイル・+49 / -37。`proxyResolver.ts`・`loggingActions.ts` と package(-lock).json 群。

**次のアクション:** マージ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [[CopilotCLISession] Unexpected generated prompt structure.](https://github.com/microsoft/vscode/issues/323696) `#323696`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 1.126.0（Windows 11）で Copilot Agent セッションに簡単なプロンプトを送ると即座に `[CopilotCLISession] Unexpected generated prompt structure.` で失敗する。通常の Copilot Chat は正常だが Agent モードのみ失敗し、.copilot フォルダのリセットでも解消しない。

**直近のアクティビティ:** Alessandrolosco・juhe970・DenzelDCB が 1.127.0 を含む環境で同一エラーを報告している。

**次のアクション:** Agent モードのプロンプト生成の不具合調査が必要。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat extension causes extension host to constantly restart](https://github.com/microsoft/vscode/issues/318595) `#318595`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Chat 拡張を有効化すると全 worktree の読み込みが始まり、巨大な Claude Code セッションの読み込みでハングして拡張ホストが再起動を繰り返す。`github.copilot.chat.claudeAgent.enabled: false` にしても発生する。過去の #292644 等と関連。

**直近のアクティビティ:** eamodio が数か月継続していると強く訴え、Copilot 有効時に SCM が全 worktree を強制ロードしてハングすると報告している。

**次のアクション:** 大規模セッション読み込み時のハングの根本調査が必要。

---

### ![PR](icons/git-pull-request.svg) [Automations: Integration](https://github.com/microsoft/vscode/pull/324167) `#324167`
**ステータス:** Open | **ラベル:** なし

**概要:** automations v0（4 部構成）の最終 PR で、システムを製品 UI へ組み込む。Customizations サイドバー・概要タイル・ツリービュー・管理エディタに automations を配線し、`experimental`・`advanced` タグでインサイダー限定表示にしてチームフィードバックを収集する。UX は初期段階で後続でリファクタ予定。

**直近のアクティビティ:** github-actions[bot]が 6 件のスクリーンショット差分を報告。copilot-pull-request-reviewer[bot]が複数回レビュー（16/16、13/13 ファイル）、benvillalobos がコメント。

**変更ファイル:** 24 ファイル・+344 / -41。`sessions/contrib/automations/` 配下と `copilotAgentSession.ts`・`mapSessionEvents.ts` など。

**次のアクション:** レビュー指摘反映後にマージ。

---

### ![Issue](icons/issue-opened.svg) [Worktree information is getting updated only after first turn](https://github.com/microsoft/vscode/issues/322529) `#322529`
**ステータス:** Open | **ラベル:** bug, regression, agent-host

**概要:** Agents ウィンドウで VS Code / copilot（agent host）と worktree オプションを選んでプロンプトを送ると、セッション一覧・ヘッダー・ファイルエクスプローラ・changes ビューの worktree 情報が最初のターン後にしか反映されない。拡張ホスト版の copilot セッションと比べたリグレッション。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** worktree 情報の初期反映タイミングの修正が必要。

---

### ![Issue](icons/issue-opened.svg) [apply_patch PostToolUse hook fires before file is actually edited](https://github.com/microsoft/vscode/issues/315926) `#315926`
**ステータス:** Open | **ラベル:** chat-hooks

**概要:** `apply_patch` ツールの `PostToolUse` フックが、パッチ後の内容がディスクに書き込まれる前に発火するため、フックから読むと編集前の古い内容が見える。formatter やチェッカーなど PostToolUse 消費者が壊れる。v1.119.0 で発生したと見られる。

**直近のアクティビティ:** bugrasan が同一問題でデータ損失が起きていると報告し、`create_file`・`replace_string_in_file`・`multi_replace_string_in_file`・`apply_patch` すべてで発生する「高深刻度・サイレントなデータ損失」だと指摘している。

**次のアクション:** フック発火順序（書き込み後発火）の修正が必要。深刻度が高い。

---

### ![Issue](icons/issue-opened.svg) [BYOK provider still consumes GitHub Copilot AI Credits through "Other models"](https://github.com/microsoft/vscode/issues/324268) `#324268`
**ステータス:** Open | **ラベル:** なし

**概要:** BYOK プロバイダ（OpenCode Zen）を設定しても、エージェントが内部的に「Other models」を呼び出すため GitHub Copilot の AI Credits を消費してしまう。BYOK 設定時は明示的にユーザーが選ばない限り GitHub ホストのモデルを呼ばず、AI Credits を消費しないよう求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** BYOK 時の内部モデル呼び出しとクレジット消費の挙動見直しが必要。

---

### ![PR](icons/git-pull-request.svg) [Agents - animated changes summary action](https://github.com/microsoft/vscode/pull/324266) `#324266`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウの Changes ビューにある「View All Changes」の追加/削除行数を、値変化時にアニメーション表示するようリファクタする。`ChangesDiffStatsActionItem` 内の集計・描画ロジックを再利用可能な `ChangesSummaryWidget` に抽出し、2 つの `AnimatedCounterWidget` を組み合わせる。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント。

**変更ファイル:** 3 ファイル・+251 / -47。`changesSummaryWidget.ts`・`changesView.ts` と対応 CSS。

**次のアクション:** レビュー完了後にマージ。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/324267) `#324267`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** 補完テキスト `justify-content: center;` のみが記載され、問題の詳細が空欄の Copilot 補完フィードバック issue。

**直近のアクティビティ:** vs-code-engineering[bot]が情報補完を求める定型コメントを投稿。

**次のアクション:** 報告者による情報追記待ち。

---

### ![PR](icons/git-pull-request.svg) [agentHost: stream system notifications during active turns](https://github.com/microsoft/vscode/pull/323382) `#323382`
**ステータス:** Open | **ラベル:** なし

**概要:** ライブの Agent Host ターン observer を通じて `SystemNotification` レスポンス部を転送し、既存の履歴/再接続変換を再利用して通知を progress メッセージとして描画する。再接続時に復元済みの通知は重複しないようシードする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメントし、AHP トランスクリプト再構築での `system.notification` 復元の扱いを要約。

**変更ファイル:** 3 ファイル・+52 / -4。`agentHostSessionHandler.ts`・`stateToProgressAdapter.ts` とテスト。

**次のアクション:** レビュー完了後にマージ。

---

### ![Issue](icons/issue-opened.svg) [Sessions: Terminal and command output is not visible when the agent runs commands, showing "The terminal is no longer available"](https://github.com/microsoft/vscode/issues/302109) `#302109`
**ステータス:** Open | **ラベル:** ux

**概要:** Agent Sessions の UX 調査で、エージェントが端末コマンドを実行した後に出力が見えず「The terminal is no longer available」「No preview available.」と表示される問題が判明。開発者は別途端末を開いて手動で実行する必要があり、エージェント駆動ワークフローの意義が損なわれている。

**直近のアクティビティ:** davemcom が問題の背景考察と、`Claude Extension` + `TMUX` を使った回避策（端末を transient にしつつログを見る）を詳細に共有している。

**次のアクション:** 端末出力/プレビューの可視性改善の設計検討が必要。

---

### ![PR](icons/git-pull-request.svg) [chat: avoid relayout while hidden](https://github.com/microsoft/vscode/pull/324264) `#324264`
**ステータス:** Merged | **ラベル:** なし

**概要:** `ChatViewPane` のチャットウィジェットが非表示の間は relayout をスキップし、detached や `display: none` の DOM に対して動的高さのチャット行が測定される（`offsetHeight === 0`）のを防ぐ。再表示時には通常のレイアウトパスを維持する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、benvillalobos が APPROVED。

**変更ファイル:** 1 ファイル・+4 / -0。`chatViewPane.ts` にガードを追加。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Fix chat progressive rendering after hidden updates](https://github.com/microsoft/vscode/pull/324260) `#324260`
**ステータス:** Merged | **ラベル:** なし

**概要:** レンダラーが非表示の間にチャット応答が更新され続けた場合の progressive rendering のリグレッション修正。非表示チャットが初めて表示された際、蓄積済みの応答 Markdown を即座に描画して「追いつく」ようにする。render cursor で未描画と部分描画を区別する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、benvillalobos が APPROVED。

**変更ファイル:** 2 ファイル・+23 / -3。`chatListRenderer.ts` とテスト。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [sessions: add running-subagents status chip above the chat input](https://github.com/microsoft/vscode/pull/324222) `#324222`
**ステータス:** Merged | **ラベル:** なし

**概要:** チャット入力欄の上に、現在表示中のチャットに実行中の subagent がある間だけ表示される一時的なステータスチップを追加する（#324199 のフォローアップ）。1 件なら「Running subagent: <title>」、複数なら「N subagents running」を表示し、アイドル時は完全に非表示。クリックで実行中 subagent のメニューを開ける。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、sandy081 がコメント、alexdima が APPROVED。

**変更ファイル:** 3 ファイル・+191 / -4。`chatView.ts`・`sessionRunningSubagentsControl.ts` と CSS。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [sessions: update changes view file sections](https://github.com/microsoft/vscode/pull/324247) `#324247`
**ステータス:** Merged | **ラベル:** なし

**概要:** Changes ビューの外部セッションファイルセクションを「Other Files」にリネームして行の見た目を本体の changes 一覧に揃える。Checks と File Changes を内容に合わせて縮め、Other Files に残り領域を割り当てる split サイズ調整と、空 File Changes に 140px の最小高さを設定。全セクションを網羅するコンポーネントフィクスチャも追加。

**直近のアクティビティ:** github-actions[bot]が 4 件のスクリーンショット差分を報告。copilot-pull-request-reviewer[bot]がレビュー概要をコメント、alexdima が APPROVED。

**変更ファイル:** 7 ファイル・+602 / -33。`changesView.ts`・`sessionFilesWidget.ts`・`sessionFilesViewModel.ts` などとフィクスチャ。

**次のアクション:** マージ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/324265) `#324265`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** 補完テキスト `print("Ayudantía: ", ayudantia)` に対し、報告者（スペイン語）が補完機能の無効化方法を尋ねている Copilot 補完フィードバック issue。

**直近のアクティビティ:** vs-code-engineering[bot]が本文を英訳した AI Translation コメントと、最新版更新を促す定型コメントを投稿している。

**次のアクション:** トリアージと、補完無効化方法の案内が必要。

---

### ![PR](icons/git-pull-request.svg) [Unify worktree isolation across agents](https://github.com/microsoft/vscode/pull/324246) `#324246`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot・Codex・Claude 各エージェントの worktree セッション隔離を共有コントローラ（`worktreeIsolation.ts`）に抽出し、フォルダ/worktree 隔離を一貫させる。各エージェントクラスを共有コントローラ利用にリファクタし、ユニットテストを追加する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]は「diff が大きすぎてレビューできない」と返答している。

**変更ファイル:** 12 ファイル・+1353 / -438。`worktreeIsolation.ts`・`agentBranchNameGenerator.ts` と各エージェント（claude/codex/copilot）およびテスト。

**次のアクション:** 変更が大きいため分割またはレビュー体制の調整が必要。

---

### ![PR](icons/git-pull-request.svg) [Support PDF inputs for BYOK endpoints](https://github.com/microsoft/vscode/pull/323836) `#323836`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Responses / Messages API を使う BYOK モデルに PDF 入力サポートを追加する。proposed の language-model capabilities と Custom Endpoint モデル設定に `fileInputMimeTypes` を追加し、`application/pdf` を広告。raw↔VS Code メッセージ変換で PDF データを保持し、Anthropic `document` ブロックへ変換する。Chat Completions では PDF 入力は無効のまま。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要と、19/21 ファイルをレビューし 1 件のコメントを生成した旨をコメント。

**変更ファイル:** 21 ファイル・+281 / -25。`byok/` 配下の provider/converter・`fileVariable.tsx`・`languageModelAccessPrompt.tsx` など。

**次のアクション:** トリアージとレビュー完了後にマージ判断。

---

### ![Issue](icons/issue-opened.svg) [[BUG] enabledPlugins from managed settings remove existing plugins in user environment](https://github.com/microsoft/vscode/issues/324263) `#324263`
**ステータス:** Open | **ラベル:** new release

**概要:** GHEC の managed-settings.json で `enabledPlugins` を宣言すると、ユーザーが既にインストール済みのプラグインが上書き削除されてしまう（LinkedIn が報告）。`enabledPlugins` のプラグインは、deny list がない限り既存プラグインに追加される形であるべき。報告者はまだ自身では再現できていない。

**直近のアクティビティ:** gpadak が @joshspicer・@digitarald に cc している。

**次のアクション:** 再現確認とマージ挙動（上書きではなく追加）の修正検討が必要。

---

### ![Issue](icons/issue-opened.svg) [i have not create new folder in that vs code](https://github.com/microsoft/vscode/issues/324262) `#324262`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** VS Code 1.127.0（Windows）でフォルダやファイルを追加・作成できないという趣旨の報告だが、記述が不明瞭で詳細情報が不足している。

**直近のアクティビティ:** vs-code-engineering[bot]が情報補完を求める定型コメントを投稿。

**次のアクション:** 報告者による再現手順・情報の追記待ち。

---

### ![Issue](icons/issue-opened.svg) [macOS: Ctrl+F and Ctrl+P intercepted by workbench when typing in webview text input, breaking Cocoa text navigation](https://github.com/microsoft/vscode/issues/320435) `#320435`
**ステータス:** Open | **ラベル:** macos, webview

**概要:** macOS では `Ctrl+F`（前進）・`Ctrl+P`（前行）がシステムレベルの Cocoa テキストナビゲーションだが、webview 拡張パネルのテキスト入力内ではワークベンチがこれらを横取りしてカーソルが動かない。Claude Code・Cline・Continue・OpenCode などのチャット入力で発生。focus が webview 内にあるときはキーを webview へ渡すべき。

**直近のアクティビティ:** dvdrtrgn が 1.127.0 / macOS 26.5.1 でも再現し、`Ctrl+A/E/B/N` は動くが `Ctrl+F/Ctrl+P` のみ飲み込まれると詳細報告。anthropics/claude-code#66291 にもクロス報告している。

**次のアクション:** webview 入力での該当キーのパススルー対応の検討が必要。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot GPT-5.5 (Xhigh) frequently regenerates or duplicates output](https://github.com/microsoft/vscode/issues/320532) `#320532`
**ステータス:** Open | **ラベル:** なし

**概要:** GPT-5.5 Xhigh で複雑な推論を要する場合、長い応答が停止した後にまるごと消えて再生成される、あるいはほぼ同一の応答ブロックが同一ターン内に二重生成される異常が頻発する。プロバイダ側かストリーミング API への Copilot の応答処理のバグかは不明。

**直近のアクティビティ:** garretwilson が v1.127 でも継続すると報告。GPT 5.5 で cancel・edit・再送すると進行中の出力を途中から拾ってしまうと具体例を挙げている。

**次のアクション:** ストリーミング応答の再生成/重複の原因切り分けが必要。

---

### ![Issue](icons/issue-opened.svg) [Not able to connect remote connection(SSH) through the VS code.](https://github.com/microsoft/vscode/issues/324259) `#324259`
**ステータス:** Open | **ラベル:** info-needed

**概要:** VS Code 1.127.0（Windows）で SSH によるリモート接続ができないという報告。詳細情報やログが不足している。

**直近のアクティビティ:** vs-code-engineering[bot]が情報補完を求める定型コメントを投稿。

**次のアクション:** 報告者によるログ・詳細の追記待ち。

---

### ![Issue](icons/issue-opened.svg) [SSH session stuck on client tools](https://github.com/microsoft/vscode/issues/324261) `#324261`
**ステータス:** Open | **ラベル:** なし

**概要:** ログはオフラインで共有。根本原因は 3 つのバグの連鎖と分析されている。(1) nested-chat の unsubscribe が誤ってクライアントをセッション全体から除去（`protocolServerHandler.ts:1468-1492`）、(2) provider が除去済みクライアントのツールを保持（`agentSideEffects.ts:158-177` が `SessionActiveClientRemoved` を反映しない）、(3) リトライが permission 登録前に完了する race（`copilotAgentSession.ts:997-999`）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 特定済みの 3 バグ連鎖に対する修正が必要。

---

### ![Issue](icons/issue-opened.svg) [Input needed -> no UI for input shown, session stuck](https://github.com/microsoft/vscode/issues/323861) `#323861`
**ステータス:** Open | **ラベル:** info-needed

**概要:** 入力が求められている状態なのに入力用 UI が表示されず、セッションが停止する。本文はスクリーンショットのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 再現手順・詳細情報の収集が必要。

---

### ![Issue](icons/issue-opened.svg) [[html] completions: better handle unclosed string literals](https://github.com/microsoft/vscode/issues/273226) `#273226`
**ステータス:** Closed | **ラベル:** bug, help wanted, html, verification-found

**概要:** `<th><input type="che</th>` でカーソルを `che` の後に置き、Ctrl+Space で「checkbox」を選ぶと後続テキストが削除され `<th><input type="checkbox` になる。HTML 補完が未クローズの文字列リテラルを適切に扱えていない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Fix bugs related to setting the right Chat model. (Fixes Issue #323765)](https://github.com/microsoft/vscode/pull/323767) `#323767`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットでエラー後の「Try Again」時に別モデルを選択できない問題（#323765）を修正。調査でモデル選択状態の受け渡しに複数の不整合が判明したため、`getSelectedModelRequestOptions()` に集約し、選択モデル ID とそのスコープ設定を常に一緒に保持するようにした。resend や confirmation 経路で ID のみ渡すとスコープ設定が失われていた。

**直近のアクティビティ:** Giuspepe が「Fixes #323765」とコメント。Giuspepe・alexdima・roblourens が APPROVED。copilot-pull-request-reviewer[bot]は依頼者のクォータ上限でレビュー不可と返答。

**変更ファイル:** 9 ファイル・+26 / -18。`chatWidget.ts`・`chatListWidget.ts`・`chatTitleActions.ts`・`chatSetupProviders.ts` など。

**次のアクション:** 承認済み。マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Guard agent-SDK release build against missing native optional-dep packages](https://github.com/microsoft/vscode/pull/324255) `#324255`
**ステータス:** Open | **ラベル:** なし

**概要:** #324064 が PR-check の node_modules キャッシュで守るネイティブ optional 依存（`@openai/codex-<target>`・`@anthropic-ai/claude-agent-sdk-<target>`）の欠落チェックを、リリースパイプライン（`produce.ts` → `buildOne`）にも適用する。npm が optional 依存欠落でも exit 0 になるため、バイナリ欠落の tar が CDN に上がりユーザー実行時に初めて失敗する事態を防ぐ。`findMissingNativeOptionalDep` を再利用する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント。

**変更ファイル:** 2 ファイル・+91 / -34。`build/agent-sdk/package.ts`・`checkNativeOptionalDeps.ts`。

**次のアクション:** レビュー完了後にマージ。

---

### ![Issue](icons/issue-opened.svg) [Integrated terminal automatically sends 'Y' when terminating node batch script](https://github.com/microsoft/vscode/issues/142341) `#142341`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, terminal-conpty, unreleased, terminal-input

**概要:** Windows の統合端末で concurrently 実行中に Ctrl+C を押すと「Terminate batch job (Y/N)?」に対して `Y` が自動送信され、最初のスクリプトのみ終了して残り出力が次行のプロンプトに紛れ込む。Windows Terminal では発生しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [VSCode freezes when opening a terminal](https://github.com/microsoft/vscode/issues/239838) `#239838`
**ステータス:** Closed | **ラベル:** important, freeze-slow-crash-leak, candidate, terminal, gpu, unreleased

**概要:** VS Code 1.97.0（Windows）で、更新後に TypeScript プロジェクトフォルダを開いて端末を開くとフリーズする。全拡張無効でも再現する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Native context menu hijacks custom one in webview editor](https://github.com/microsoft/vscode/issues/239862) `#239862`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, electron, confirmed, regression, unreleased

**概要:** VS Code 1.97.0 で Git Graph の項目を右クリックすると、カスタムコンテキストメニューではなくネイティブのテキスト操作メニューのみが表示される webview エディタのリグレッション。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) ["Open with Code" old context menu option missing from Explorer on version 1.103.0](https://github.com/microsoft/vscode/issues/260389) `#260389`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, regression, unreleased

**概要:** VS Code 1.103.0（Windows）でインストール時に「Add 'Open with Code'」を有効にしても、Explorer のディレクトリ右クリックメニューに旧来の「Open with Code」項目が表示されないリグレッション。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Automatic synchronization with Windows High Contrast mode](https://github.com/microsoft/vscode/issues/269406) `#269406`
**ステータス:** Closed | **ラベル:** bug, editor-theming, insiders-released

**概要:** Windows のハイコントラストモード（Alt+Shift+PrintScreen）に VS Code のテーマが自動追従してほしいという要望。他の Windows アプリの多くは自動適応するが VS Code はしない。#324025 で対応済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（insiders 反映済み）。

---

### ![Issue](icons/issue-opened.svg) [Chat session picker: No picker in restored chat editor](https://github.com/microsoft/vscode/issues/275289) `#275289`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased

**概要:** #274938 のテスト中に発見。CCA チャットエディタを開いてエージェントピッカーが見えている状態で VS Code をリロードすると、チャットエディタは復元されるがエージェントピッカーが表示されない。新規チャットエディタを作ると表示され、新規カスタムエージェントも含まれる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Telemetry event `inline.done` isn't send for v2 inline chat sessions](https://github.com/microsoft/vscode/issues/277422) `#277422`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased

**概要:** inline chat v1 と v2 の性能を `inline.done` テレメトリで比較したいが、v2 ではこのイベントが送出されない。コアと拡張の両方でコード変更が必要。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [InlineChatController2 creates widget eagerly](https://github.com/microsoft/vscode/issues/283017) `#283017`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased, inline-chat

**概要:** `InlineChatController2` がウィジェットを eager に生成しており、わずかな性能低下に加え、実験的な `inlineChat.notebookAgent` ではウィジェット生成時にモデルが未設定で正しい location 情報を渡せない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Git Source Control Graph tree doesn't expand for some entries](https://github.com/microsoft/vscode/issues/286776) `#286776`
**ステータス:** Closed | **ラベル:** bug, candidate, scm, unreleased

**概要:** Git Source Control Graph のツリーで一部のエントリが展開されない（コミットファイルが多い場合に発生）。バグ項目のハッシュをクリックすると `Git: The "path" argument must be of type string. Received undefined` エラーが出る。VS Code 1.108（Windows 10）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Backport fix for CVE-2026-2441 from chromium](https://github.com/microsoft/vscode/issues/295264) `#295264`
**ステータス:** Closed | **ラベル:** bug, upstream, verified, candidate, chromium, unreleased

**概要:** Chromium の CVE-2026-2441 の修正を electron 経由でバックポートする対応。参照コミットは electron/electron a229dbf。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（セキュリティバックポート対応済み）。

---

### ![Issue](icons/issue-opened.svg) [autopilot mode should allow curl in terminal](https://github.com/microsoft/vscode/issues/303174) `#303174`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** `chat.tools.global.autoApprove: true` かつ Autopilot モードでも、エージェントに `curl` を実行させると失敗する。YOLO モードでも端末の `curl` がブロックされているように見え、明確なエラーメッセージや対処法が示されない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Terminal commands should be auto approved when invoked from `ask_questions` tool](https://github.com/microsoft/vscode/issues/309589) `#309589`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** `ask_questions` ツールから呼ばれた端末コマンドは、ユーザーが既に回答を確認しているため（特に `Enter` 送信について）改めて確認プロンプトを出すべきではない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [`send_to_terminal` progress message repeats first question when accepting all defaults in interactive commands](https://github.com/microsoft/vscode/issues/309608) `#309608`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, terminal-chat-tools

**概要:** `npm init` など対話コマンドで `askQuestions` カルーセルの全デフォルトを受け入れると、各 `send_to_terminal` の progress メッセージが正しい質問ではなく常に最初の質問（例: "Package name?"）を表示する。原因は `_getQuestionContextForTerminal` のマッチング。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [[Error] unhandlederror-PerfSampleError: by <<renderer>> in getBoundingClientRect](https://github.com/microsoft/vscode/issues/313733) `#313733`
**ステータス:** Closed | **ラベル:** bug, error-telemetry, insiders-released, recent-regression, stable-anomaly, ai-generated, ai-analyzed

**概要:** `getDomNodePagePosition` → `isMousePositionWithinElement` → hover コントローラ経由で `getBoundingClientRect` が呼ばれ `PerfSampleError` が発生するエラーテレメトリ。hover ウィジェットの DOM ノードが document から切断された状態で呼ばれることが原因。#324216 で対応済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（insiders 反映済み）。

---

### ![Issue](icons/issue-opened.svg) [`Insert Final New Line` does not work if last line only contains spaces](https://github.com/microsoft/vscode/issues/314720) `#314720`
**ステータス:** Closed | **ラベル:** bug, insiders-released

**概要:** 最終行がスペースのみで最後に改行がない場合、`Insert Final New Line` コマンドが最終改行を追加できない。#324194 で対応済み。

**直近のアクティビティ:** aiday-mar・RedCMD が、最終行末尾に文字がある場合の EOF 改行の意味やホワイトスペースのみの行の扱いについて議論している（過去の議論）。

**次のアクション:** クローズ済み（insiders 反映済み）。

---

### ![Issue](icons/issue-opened.svg) [CLI reports broken pty.node when sandbox is enabled](https://github.com/microsoft/vscode/issues/319377) `#319377`
**ステータス:** Closed | **ラベル:** bug, unreleased, agent-sandbox

**概要:** #319218 のテスト中、Linux で sandbox を有効化して秘密ファイルの再読み込みを依頼すると、sandbox/権限メッセージではなく「ネイティブモジュール（pty.node）が壊れている」という内部エラーが返る。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Block auto-approval of file-write redirection to ~ and % paths](https://github.com/microsoft/vscode/issues/320660) `#320660`
**ステータス:** Closed | **ラベル:** bug, unreleased

**概要:** ファイル書き込みリダイレクトの自動承認ガードを、`~`（POSIX チルダ展開）や `%`（Windows 環境変数展開）を含む宛先パスにも拡張する。いずれも `posix.isAbsolute` / `win32.isAbsolute` では絶対パスと判定されないため。bash・pwsh 向けの回帰テストを追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（セキュリティ関連の自動承認ガード強化）。

---

### ![Issue](icons/issue-opened.svg) [Agents Window: Clarify Worktree Ownership in Delete Dialog](https://github.com/microsoft/vscode/issues/324220) `#324220`
**ステータス:** Closed | **ラベル:** polish, insiders-released, agents-window

**概要:** Agents ウィンドウで複数セッションを選んで削除する際、削除される worktree がどのセッションのものか分からない。ダイアログで明示すべき。#324237 で対応済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（insiders 反映済み）。

---

### ![Issue](icons/issue-opened.svg) [Steering toolbar is rendered too much to the right](https://github.com/microsoft/vscode/issues/324252) `#324252`
**ステータス:** Closed | **ラベル:** bug, ux, unreleased

**概要:** Agents ウィンドウで steering メッセージを送ると、steering ツールバーが大きく右にずれて描画される。#324254 で修正。

**直近のアクティビティ:** justschen が #324087 が関連で、修正 PR を出したとコメントしている。

**次のアクション:** クローズ済み（修正 PR #324254 でマージ）。

---

### ![PR](icons/git-pull-request.svg) [Disable GitHub Copilot utility models by default for BYOK models](https://github.com/microsoft/vscode/pull/323907) `#323907`
**ステータス:** Merged | **ラベル:** なし

**概要:** BYOK チャットモデル選択時、チャットタイトルや要約などの内部 utility フローが Copilot アクセスなしでも GitHub Copilot モデルへ暗黙にフォールバックしていたのを停止する。`chat.utilityModel` / `chat.utilitySmallModel` で自前モデルを指定するか、新設定 `chat.useCopilotModelsForUtilityModels` で旧挙動を維持できる。

**直近のアクティビティ:** LogicDaemon が、utility モデル未選択時に応答が失敗する・llama.cpp でモデル再ロードのオーバーヘッドが大きいとし「メインと同じモデルを使うオプション」を要望。copilot-pull-request-reviewer[bot]のレビューと、benvillalobos・DonJayamanne が APPROVED、MohammadKani がコメント。

**変更ファイル:** 8 ファイル・+216 / -54。`languageModelAccess.ts`・`utilityModelContribution.ts`・`byokUtilityModel.contribution.ts` など。

**次のアクション:** マージ済み。要望（同一モデル利用オプション）はフォローアップ検討。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/324258) `#324258`
**ステータス:** Open | **ラベル:** info-needed

**概要:** 補完テキスト `let = [1, 2, 3, ...]` のみが記載された、詳細不足の Copilot 補完フィードバック issue。

**直近のアクティビティ:** vs-code-engineering[bot]が情報補完を求める定型コメントと、最新版更新を促すコメントを投稿。

**次のアクション:** 報告者による情報追記待ち。

---

### ![PR](icons/git-pull-request.svg) [fix agents window misplaced steering hover](https://github.com/microsoft/vscode/pull/324254) `#324254`
**ステータス:** Merged | **ラベル:** なし

**概要:** #324252 の修正。メッセージ行（`.interactive-item-container`）が幅制限・中央寄せなのに位置決めコンテキストになっておらず、絶対配置の `.request-hover` ツールバーが全幅要素基準で解決されて右にずれていた問題を CSS で修正する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、alexdima が APPROVED。

**変更ファイル:** 2 ファイル・+13 / -1。`style.css` と LAYOUT.md。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [sessions: add setting to show empty default groups (Pinned, Chats) in sessions list](https://github.com/microsoft/vscode/pull/324251) `#324251`
**ステータス:** Merged | **ラベル:** なし

**概要:** 新設定 `sessions.list.showEmptyDefaultGroups`（既定 `true`）を追加し、Pinned / Chats の既定グループが空でも表示されるかを制御する。`true` は現行動作（プレースホルダ行を表示して発見性を維持）、`false` はセッションが入るまで空セクションを隠す。設定変更でライブ再描画する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、alexdima が APPROVED。

**変更ファイル:** 3 ファイル・+34 / -4。`sessionsList.ts`・`sessions.contribution.ts` と SESSIONS_LIST.md。

**次のアクション:** マージ済み。追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [Agents Window: Unable to continue archived session due to deleted worktree](https://github.com/microsoft/vscode/issues/324248) `#324248`
**ステータス:** Open | **ラベル:** agents-window

**概要:** アーカイブ済みセッションを再開しようとしたが、worktree が既に削除されているためエラーで続行できず、フォークも機能しない。良いコンテキストを再利用したいのに逃げ道がない状態。

**直近のアクティビティ:** roblourens が「アーカイブ済みセッションは read-only にすべきで、続行したければ復元して worktree を再作成する方式が良い（そうしないと worktree が再作成されたまま消えない）」とコメント。

**次のアクション:** アーカイブセッションの read-only 化と復元フローの設計検討。

---

### ![PR](icons/git-pull-request.svg) [Style overrides: Add border-radius and padding to terminal tab entries](https://github.com/microsoft/vscode/pull/324241) `#324241`
**ステータス:** Merged | **ラベル:** なし

**概要:** 統合端末のタブエントリに border-radius と padding を追加する見た目調整。`styleOverrides` contribution 内で `.style-override` クラス配下として端末自身のスタイルに上乗せする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet・alexdima が APPROVED。

**変更ファイル:** 2 ファイル・+13 / -0。`roundedCorners.css`・`tabs.css`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [fix: hide hover when mouse down not on hover](https://github.com/microsoft/vscode/pull/324233) `#324233`
**ステータス:** Open | **ラベル:** なし

**概要:** #245728 の修正。ドラッグでコード選択中にエディタの content hover ツールチップが残り/再表示されて選択が隠れる問題を、`ContentHoverController._onEditorMouseMove()` にマウス押下中かつ hover 保持領域外なら hover を隠して計算をキャンセルする early return を追加して修正する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、alexdima が APPROVED。

**変更ファイル:** 1 ファイル・+6 / -0。`contentHoverController.ts`。

**次のアクション:** 承認済み。マージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Markdown preview does not fallback monospace font in code block if `editor.fontFamily` does not have `monospace` and font does not exist](https://github.com/microsoft/vscode/issues/324256) `#324256`
**ステータス:** Open | **ラベル:** new release

**概要:** `editor.fontFamily` に存在しないフォントを設定し `monospace` フォールバックがない場合、Markdown プレビューのコードブロックが monospace にフォールバックせず serif になる。コードエディタは monospace に解決できるのに、プレビューのコードブロック/インラインコードで不整合が生じる。

**直近のアクティビティ:** siemieniuk（報告者）が、コードエディタとプレビューのグローバルな不整合がバグ報告の主動機で、インラインコードでも同様だと補足している。

**次のアクション:** プレビューのコードブロックへの monospace フォールバック追加の検討。

---

### ![Issue](icons/issue-opened.svg) [Error - Failed during initial scan: Could not find ripgrep binary in ZooCode extension after update vscode](https://github.com/microsoft/vscode/issues/318704) `#318704`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 1.122（Ubuntu 20.04）へ deb で更新後、ZooCode 拡張が ripgrep バイナリを見つけられず初期スキャンに失敗する。更新で `rg` の配置先が変わり、旧場所を参照する拡張が壊れると見られる。

**直近のアクティビティ:** abaron が、`ripgrep-universal\bin` に移動した `rg` を旧 `ripgrep\bin` にコピーする一時回避策（Windows での Roo Code 事例）を紹介している。

**次のアクション:** ripgrep バイナリ配置変更に伴う拡張互換性の検討（拡張側更新までの回避策あり）。

---

### ![Issue](icons/issue-opened.svg) [Message appears all the time "The MCP server GitHub requires authentication. Authenticate?"](https://github.com/microsoft/vscode/issues/324253) `#324253`
**ステータス:** Open | **ラベル:** papercut :drop_of_blood:

**概要:** AHP Copilot ハーネスにメッセージを送るたびに「The MCP server GitHub requires authentication. Authenticate?」が毎回表示される。VS Code 1.128.0-insider。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** MCP サーバ認証プロンプトの繰り返し表示の修正が必要。

---

### ![Issue](icons/issue-opened.svg) [Async subagents can be blocked](https://github.com/microsoft/vscode/issues/323762) `#323762`
**ステータス:** Closed | **ラベル:** bug, important, agent-host

**概要:** セッションが async subagent の完了待ちで停止し、subagent が完了しない。events.jsonl の末尾が未解決の `permission.requested` になる。メインセッションが unsubscribe→idle→AH メモリから evict され、subagent 完了時に起こそうとしても evict 済みで機能しないのが原因。#323738 に類似。

**直近のアクティビティ:** roblourens が回避策 PR #323824 と、上流の真の修正（github/copilot-agent-runtime#11900）を共有している。

**次のアクション:** クローズ済み（回避策と上流修正あり）。

---

### ![Issue](icons/issue-opened.svg) [Agents Window: Switching workspace doesn't switch branch picker](https://github.com/microsoft/vscode/issues/323468) `#323468`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** あるワークスペースで選択したブランチが、別ワークスペースに切り替えても同じブランチ名で事前選択され、その名前が切り替え先に存在しない場合がある。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** ワークスペース切り替え時のブランチピッカー同期の修正が必要。

---

### ![Issue](icons/issue-opened.svg) [Restart to Update not working on Mac](https://github.com/microsoft/vscode/issues/310927) `#310927`
**ステータス:** Open | **ラベル:** bug, install-update, macos

**概要:** macOS（Apple Silicon）で「Restart to apply the latest update」を押すと VS Code が閉じるだけで再起動せず、更新が適用されない。手動で開き直すと同じ通知が再表示される。自己上書き/再起動の権限不足が疑われる。

**直近のアクティビティ:** msridhar が 1.125.1（Universal）でも継続すると報告。Update ボタンで終了するが再起動せず、手動再起動でも旧バージョンのままとのこと。

**次のアクション:** macOS のアプリ内更新（自己上書き・再起動）の不具合調査が必要。

---

### ![Issue](icons/issue-opened.svg) [An error of Github Copilot Chat](https://github.com/microsoft/vscode/issues/324008) `#324008`
**ステータス:** Open | **ラベル:** new release

**概要:** VS Code 1.127.0（Windows）で GitHub Copilot にチャットしようとすると常に `[CopilotCLISession] Unexpected generated prompt structure.` が表示される。#323696 と同種。

**直近のアクティビティ:** guillaume-roderick-iaggbs が +1 している。

**次のアクション:** #323696 と同根の Agent モードプロンプト生成不具合の調査。

---

### ![Issue](icons/issue-opened.svg) [Setting update.mode to "none" does not stop automatic updates](https://github.com/microsoft/vscode/issues/289144) `#289144`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, insiders-released

**概要:** リモートサーバ接続のため特定バージョン（1.100）を使いたいのに、`update.mode: "none"` と `update.enableWindowsBackgroundUpdates: false` を設定しても数日ごとに自動更新されてしまう。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（insiders 反映済み）。

---

### ![PR](icons/git-pull-request.svg) [Add missing window.title completion items](https://github.com/microsoft/vscode/pull/323456) `#323456`
**ステータス:** Open | **ラベル:** なし

**概要:** `${activeEditorLanguageId}`・`${profileName}`・`${focusedView}` は `window.title` の設定説明に記載され `windowTitle.ts` で解決されるのに、`settingsDocumentHelper.ts` の補完リストに欠けていて IntelliSense に出なかった。この 3 つを補完候補に追加する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント（2/2 ファイル、新規コメントなし）、NaufalK25 がコメント。

**変更ファイル:** 2 ファイル・+33 / -0。`settingsDocumentHelper.ts` とテスト。

**次のアクション:** レビュー完了後にマージ。

---

### ![PR](icons/git-pull-request.svg) [Fix raw markdown shown in image carousel caption](https://github.com/microsoft/vscode/pull/320754) `#320754`
**ステータス:** Merged | **ラベル:** なし

**概要:** #307446 の修正。画像カルーセルのキャプションに生 Markdown（例: `Viewed image [](file:///…/homepage.png)`）が表示される問題を、`chatImageExtraction.ts` でメッセージを `string | IMarkdownString` のまま保持し、表示層の `toCaptionText()`（`stripIcons(renderAsPlaintext(...))`）で表示テキストへ変換して修正する。

**直近のアクティビティ:** stasyu2009-ux が無関係な C++ コード片を投稿。copilot-pull-request-reviewer[bot]のレビュー、arun-357 が複数コメント、mjbvz・dmitrivMS が APPROVED。

**変更ファイル:** 4 ファイル・+60 / -9。`chatImageCarouselService.ts`・`chatImageExtraction.ts` とテスト。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [updates rspack](https://github.com/microsoft/vscode/pull/324201) `#324201`
**ステータス:** Merged | **ラベル:** なし

**概要:** Component Explorer の rspack を v1 から v2 にアップグレードし、メモリ使用量を削減。フィクスチャを `out`（.js）ではなく `src`（.ts）から読み込むよう変更する。

**直近のアクティビティ:** github-actions[bot]が「スクリーンショット変更なし」を報告。copilot-pull-request-reviewer[bot]のレビュー、aeschli は DISMISSED、Giuspepe・benibenj が APPROVED。

**変更ファイル:** 6 ファイル・+701 / -3838。`build/rspack/` 配下と component-fixtures ワークフローなど。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [adds agentEditorComments proposed API for markdown editor](https://github.com/microsoft/vscode/pull/324207) `#324207`
**ステータス:** Merged | **ラベル:** なし

**概要:** 新しい proposed API `agentEditorComments` を追加。webview で文書を描画するカスタムエディタなどが、あるリソースに対するワークベンチの agent/session コメントストアを観測・寄与できるようにする。ext-host 側実装・RPC プロトコル・DTO を配線する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のレビュー、chrmarti は DISMISSED、benibenj が APPROVED。

**変更ファイル:** 10 ファイル・+385 / -0。`extHostAgentEditorComments.ts`・`mainThreadAgentEditorComments.ts`・`vscode.proposed.agentEditorComments.d.ts` など。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [sessions: identify session in worktree delete prompt](https://github.com/microsoft/vscode/pull/324237) `#324237`
**ステータス:** Merged | **ラベル:** なし

**概要:** #324220 の修正。Agents ウィンドウの削除フローから Copilot CLI セッションラベルを拡張ホストの削除処理へ渡し、dirty-worktree の強制削除プロンプトに所有セッションラベルと worktree パスを表示する。ラベル受け渡しの provider テストも追加。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で @lszomoru に通知。copilot-pull-request-reviewer[bot]のレビュー、hediet が APPROVED。

**変更ファイル:** 13 ファイル・+74 / -26。`copilotCLIChatSessions.ts`・git 拡張の `api1.ts`・`repository.ts`・`git.d.ts` など。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [feat: glob icon theme support](https://github.com/microsoft/vscode/pull/311376) `#311376`
**ステータス:** Open | **ラベル:** なし

**概要:** #177650・#12493 の対応。ファイルアイコンテーマの関連付けに glob パターン（`*` ワイルドカード）サポートを追加し、`*.test.ts`・`webpack.*`・`.env.*` などを `fileNames` 等で使えるようにする。破壊的変更なし。`getIconClasses()` を `getFileIconInfo()` にリネームし、`data-` 属性を介した CSS 属性セレクタ照合を全コンテキストで有効化する。

**直近のアクティビティ:** manyifire が「#177650 と #12493 を同時に修正するのか」と質問。copilot-pull-request-reviewer[bot]のレビューと、PKief が多数コメント。

**変更ファイル:** 34 ファイル・+710 / -118。`getFileIconInfo.ts`・`iconLabel.ts`・`labels.ts`・`suggestWidgetRenderer.ts` など広範。

**次のアクション:** PKief のレビュー指摘反映後にマージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix: remove all manual folding ranges when selection is empty](https://github.com/microsoft/vscode/pull/304793) `#304793`
**ステータス:** Open | **ラベル:** なし

**概要:** `editor.removeManualFoldingRanges` を空選択（カーソルのみ）で実行したとき、カーソルが手動 folding 範囲上なら最内の範囲のみ削除（#212599）、範囲外なら文書内の全手動範囲を削除（#266597）する。テキスト選択時は既存挙動（交差する範囲のみ削除）を維持する。

**直近のアクティビティ:** manyifire が同時修正かを質問。aeschli が CHANGES_REQUESTED、yogeshwaran-c がコメント。

**変更ファイル:** 2 ファイル・+120 / -2。`foldingModel.ts` とテスト。

**次のアクション:** aeschli の変更要求への対応が必要。

---

### ![PR](icons/git-pull-request.svg) [Feature: Implement concatenation on Enter for string literals](https://github.com/microsoft/vscode/pull/320286) `#320286`
**ステータス:** Open | **ラベル:** なし

**概要:** #83196 の対応。文字列リテラルの途中で Enter を押すと、言語ごとに適応して構文的に有効な連結された 2 つの部分文字列へ自動分割する機能を追加する（C++ なら `"hel"` + `"lo world"`）。既存の Enter 検出ロジックとトークン処理で文字列リテラル内を判定する。

**直近のアクティビティ:** clarafdomingos（Clara と Vasco）が alexdima に、ボットの提案の一部に対応したが機能に無関係と判断した残項目について方針確認を求めている。copilot-pull-request-reviewer[bot]のレビューあり。

**変更ファイル:** 19 ファイル・+585 / -13。各言語の `language-configuration.json`・`cursorTypeEditOperations.ts`・`languageConfiguration.ts` など。

**次のアクション:** メンテナからの方針レビューとレビュー指摘対応が必要。

---

### ![PR](icons/git-pull-request.svg) [copilot: force temperature=1 and top_p=0.95 for Kimi models](https://github.com/microsoft/vscode/pull/324232) `#324232`
**ステータス:** Merged | **ラベル:** なし

**概要:** Kimi モデル（`kimi-k2.6`・`kimi-k2.7-code`）向けに、送信リクエストで常に `temperature = 1`・`top_p = 0.95` を上書き送信する（Moonshot 推奨、temperature 0 でループが増えるため）。既存の `gemini-3` ブロックに倣い `customizeCapiBody` で `isKimiFamily` ゲート下に実装。両 Kimi 系のテストを追加。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet が APPROVED。

**変更ファイル:** 2 ファイル・+52 / -1。`chatEndpoint.ts` とテスト。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Style override: Refactor scrollbar size management and add slider options](https://github.com/microsoft/vscode/pull/324240) `#324240`
**ステータス:** Open | **ラベル:** なし

**概要:** UI フレームワーク全体の既定スクロールバーサイズをグローバルに制御する仕組みを導入し、明示指定していない要素にのみ動的反映する。`DEFAULT_SCROLLBAR_SIZE`・`setGlobalDefaultScrollbarSize` と変更イベントを追加し、`horizontalSliderSize`/`verticalSliderSize` オプションも拡張。Modern UI は既定を 8px に縮小する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント。

**変更ファイル:** 2 ファイル・+44 / -2。`scrollableElement.ts`・`styleOverrides.contribution.ts`。

**次のアクション:** レビュー完了後にマージ。

---

### ![PR](icons/git-pull-request.svg) [Agents - add review/unreview operation to the multi-file diff editor](https://github.com/microsoft/vscode/pull/324218) `#324218`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents ウィンドウの multi-file diff エディタ（Branch Changes）にファイル単位の Mark as Reviewed / Unreviewed 操作を追加する。レビュー済み状態は agent host 側で session-private の合成 git ref（`refs/agents/<sid>/reviewed`）としてモデル化し、reviewed-tree の内容と一致すればレビュー済みと判定する。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY で @sandy081 に通知。copilot-pull-request-reviewer[bot]のレビュー、benibenj が複数 DISMISSED の後に APPROVED。

**変更ファイル:** 33 ファイル・+1568 / -62。`agentHostReviewService.ts`・`agentHostReviewOperationProvider.ts`・multiDiffEditor 系など。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [fix: checking position is valid before accessing line content](https://github.com/microsoft/vscode/pull/324235) `#324235`
**ステータス:** Open | **ラベル:** なし

**概要:** #283293 のクラッシュ修正。`TextAreaEditContext` の `getScreenReaderContent` が、モデル変更後の古い/範囲外 position で行内容にアクセスし `getLineContent` が `BugIndicatingError('Illegal value for lineNumber')` を投げる問題を、現在の view-model 行数に対する境界チェックを 3 箇所に追加して修正する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet が APPROVED（およびコメント）。

**変更ファイル:** 1 ファイル・+9 / -0。`textAreaEditContext.ts`。

**次のアクション:** 承認済み。マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Set HTTP_PROXY and HTTPS_PROXY from user / OS config](https://github.com/microsoft/vscode/pull/324209) `#324209`
**ステータス:** Merged | **ラベル:** なし

**概要:** agent host にプロキシ解決の配線を追加し、Copilot SDK サブプロセスが VS Code のプロキシ設定（設定/環境変数）と、ローカル時は Electron セッション（システムプロキシ/PAC）から導出した正確な `HTTP_PROXY`/`HTTPS_PROXY` を継承できるようにする。renderer→agent host の逆 RPC チャネルで `resolveProxy` を利用する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet が APPROVED。

**変更ファイル:** 17 ファイル・+356 / -10。`agentHostProxyResolver.ts`・`copilotApiService.ts`・`copilotAgent.ts` と多数のテスト。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Style override: Update badge overflow property and title padding](https://github.com/microsoft/vscode/pull/324234) `#324234`
**ステータス:** Merged | **ラベル:** なし

**概要:** #324002 の対応。Modern UI で、パネルタブがコンパクトなアイコンのみ表示のとき通知/コンポジットバッジが視覚的にクリップされないよう overflow を調整し、タイトルの padding を更新する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet が APPROVED。

**変更ファイル:** 2 ファイル・+6 / -0。`activityBar.css`・`padding.css`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Style override: Add border-radius to update indicator in action bar](https://github.com/microsoft/vscode/pull/324231) `#324231`
**ステータス:** Merged | **ラベル:** なし

**概要:** Modern UI の「Rounded Corners」style-override に、タイトルバーの更新インジケータ（「Update」ボタン）が controls-tier 半径（`--vscode-cornerRadius-small`, 4px）を使うルールを追加する。`.style-override` ゲート下でのみ適用。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet が APPROVED。

**変更ファイル:** 1 ファイル・+6 / -0。`roundedCorners.css`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [fix: restore color theme when detecting high contrast theme](https://github.com/microsoft/vscode/pull/324025) `#324025`
**ステータス:** Merged | **ラベル:** なし

**概要:** #269406 の修正。`window.autoDetectColorScheme` が無効でも VS Code が OS のハイコントラスト変更に追従するようにする。従来は当該設定が有効なときのみテーマを再適用していたため、Windows ハイコントラスト切り替えに反応しなかった。

**直近のアクティビティ:** aeschli が CHANGES_REQUESTED の後 APPROVED。copilot-pull-request-reviewer[bot]のレビューあり。

**変更ファイル:** 2 ファイル・+5 / -1。`workbenchThemeService.ts`・`themeConfiguration.ts`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Style overrides: Remove redundant scroll shadow styles](https://github.com/microsoft/vscode/pull/324228) `#324228`
**ステータス:** Merged | **ラベル:** なし

**概要:** Modern UI の「Scroll Shadows」style-override を整理し、リストとパネルエディタの上端（`::before`）グラデーションオーバーレイを削除して下端（`::after`）フェードのみ残す。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、hediet が APPROVED。

**変更ファイル:** 1 ファイル・+0 / -12。`scrollShadows.css`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Blocked sessions cleanup](https://github.com/microsoft/vscode/pull/324230) `#324230`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents ウィンドウ（`vs/sessions`）のクリーンアップ。blocked-sessions 検出ロジックを注入型の `IBlockedSessionsService` シングルトンから、`SessionsTitleBarWidget` が生成・所有するプレーンな `BlockedSessions` モデルへ変換する。「Allow」承認フローも、承認に安定 ID（`agentSessionApprovalId`）を持たせて強化する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のレビュー、hediet がコメント後に APPROVED。

**変更ファイル:** 10 ファイル・+146 / -65。`blockedSessions.ts`・`sessionsTitleBarWidget.ts`・`agentSessionApprovalModel.ts` など。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [a11y: Update theme selection and hover colors for better visibility](https://github.com/microsoft/vscode/pull/324191) `#324191`
**ステータス:** Merged | **ラベル:** なし

**概要:** #320723 の対応。組み込みの「Light 2026」「Dark 2026」既定テーマで、選択・フォーカス・hover 状態の視認性を改善する（オートコンプリート/suggest リスト項目のフォーカス表示のコントラスト対応）。従来のブランドブルーの色調を中立オーバーレイに置き換える。

**直近のアクティビティ:** github-actions[bot]が「スクリーンショット変更なし」を報告。copilot-pull-request-reviewer[bot]のレビュー、alexr00 が APPROVED。

**変更ファイル:** 2 ファイル・+11 / -12。`2026-dark.json`・`2026-light.json`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [Fix forwarded ports status bar item to toggle the ports view](https://github.com/microsoft/vscode/pull/320090) `#320090`
**ステータス:** Open | **ラベル:** なし

**概要:** #320086 の修正。転送ポートのステータスバー項目をクリックしたとき、従来は view にフォーカスするだけだったのを、既に表示中なら閉じる本来のトグル動作（Problems ステータス項目と同様）に置き換える。定数を common/browser に抽出する。

**直近のアクティビティ:** thernstig と alexr00 が、既存の自動生成コマンド「View: Toggle Ports」(`~remote.forwardedPortsContainer`) との挙動差について議論。alexr00 が 2 度 CHANGES_REQUESTED し、既存コマンド ID の再利用を提案している。

**変更ファイル:** 2 ファイル・+26 / -2。`remote.contribution.ts`・`remoteExplorer.ts`。

**次のアクション:** 既存コマンド再利用の提案への対応が必要。

---

### ![PR](icons/git-pull-request.svg) [Fix IPC serialization of non-JSON values](https://github.com/microsoft/vscode/pull/324012) `#324012`
**ステータス:** Open | **ラベル:** なし

**概要:** JSON 表現が undefined になるトップレベル値（関数・シンボル等）の IPC シリアライズ修正。従来は `JSON.stringify(data)` の結果（undefined）を `VSBuffer.fromString` に渡し `Buffer.from(undefined)` で例外になり得た。既存の Undefined preset を使うようにし、回帰テストを追加する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント。

**変更ファイル:** 2 ファイル・+14 / -1。`ipc.ts` とテスト。

**次のアクション:** レビュー完了後にマージ。

---

### ![PR](icons/git-pull-request.svg) [Style overrides: Adjust title bar height and padding for better alignment](https://github.com/microsoft/vscode/pull/324197) `#324197`
**ステータス:** Merged | **ラベル:** なし

**概要:** Modern UI（`.style-override`）で、パートのタイトルバーをよりコンパクトに揃える。`.style-override` モードでタイトルバー高さを 35px から 32px に下げ、CSS（`padding.css`）と `PartLayout`（`part.ts`）のロジックを同期させる。余分なマージンの除去や間隔調整も行う。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のレビュー、aeschli は DISMISSED、chrmarti が APPROVED。

**変更ファイル:** 4 ファイル・+38 / -15。`part.ts`・`padding.css`・`activityBar.css`・`floatingPanels.css`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [fix: deleting whitespace](https://github.com/microsoft/vscode/pull/324211) `#324211`
**ステータス:** Open | **ラベル:** なし

**概要:** #245055・#259145 の修正。`Ctrl+Del`（deleteWordRight）が、複数カーソルの整列シナリオで空白の連なりを削除する際にコメントマーカー（`//`）まで誤削除するリグレッションを、`WordOperations._deleteWordRightWhitespace` の空白ヒューリスティック境界を調整して修正する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、chrmarti は DISMISSED。

**変更ファイル:** 2 ファイル・+2 / -2。`cursorWordOperations.ts` とテスト。

**次のアクション:** レビュー完了後にマージ。

---

### ![PR](icons/git-pull-request.svg) [Improve performance of fs operations in extension host](https://github.com/microsoft/vscode/pull/324070) `#324070`
**ステータス:** Open | **ラベル:** なし

**概要:** 拡張ホストの `vscode.workspace.fs` の fast-path を最適化。scheme が有効化済みなら、各 FS 操作前の `$ensureActivation(uri.scheme)` の IPC ラウンドトリップを省くことで、多数の小さなファイル操作でのオーバーヘッドを削減する。多数のネストしたディレクトリの小ファイル IO で顕著な改善が見込まれる。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、DavyLandman（報告者）がコメント。

**変更ファイル:** 1 ファイル・+18 / -8。`extHostFileSystemConsumer.ts`。

**次のアクション:** レビュー完了後にマージ判断。

---

### ![PR](icons/git-pull-request.svg) [feat: restore asar for node_modules](https://github.com/microsoft/vscode/pull/324084) `#324084`
**ステータス:** Open | **ラベル:** なし

**概要:** #228064・#319428 の修正、#269500 対応。ESM 環境でパッケージ済みアプリの ASAR サポートを復元し、`node_modules.asar`（JS）と `node_modules.asar.unpacked`（native/WASM）からのモジュール/アセット解決を再有効化する。適切なペイロードの unpack と不要バイナリ（特に `@microsoft/mxc-sdk`）のトリムも行う。

**直近のアクティビティ:** github-actions[bot]がスクリーンショット差分（88 件追加等）を報告。copilot-pull-request-reviewer[bot]がレビュー概要をコメント。

**変更ファイル:** 31 ファイル・+506 / -57。`gulpfile.vscode.ts`・`copilot.ts`・`nodePtyShim.ts`・`ripgrepShim.ts` などビルド/パッケージ系。

**次のアクション:** レビュー完了後にマージ判断。

---

### ![PR](icons/git-pull-request.svg) [early return if dom node is not connected](https://github.com/microsoft/vscode/pull/324216) `#324216`
**ステータス:** Merged | **ラベル:** なし

**概要:** #313733 の修正。content hover ウィジェットの DOM ノードが document から切断された状態で `getBoundingClientRect`（`getDomNodePagePosition` → `isMousePositionWithinElement` 経由）が呼ばれ `PerfSampleError` になる問題に、`isConnected` ガードを追加して切断済み hover を「マウス非該当」として扱う。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、Giuspepe が APPROVED。

**変更ファイル:** 1 ファイル・+1 / -1。`contentHoverController.ts`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [fix(agentHost): resolve fork anchor UUID for subagent-synthesized first turns](https://github.com/microsoft/vscode/pull/324193) `#324193`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Claude agent host の replay で、subagent セッションが最初のターンから始まる場合に fork anchor UUID の解決が失敗するバグを修正する。subagent トランスクリプトには user プロンプトがなく、`ReplayBuilder` が最初の `assistant` メッセージ UUID をターン ID に合成するため、`user-text` エンベロープしか照合しない `resolveForkAnchorUuid` が undefined を返していた。`assistant` メッセージも照合するよう更新する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]は「どのファイルもレビューできなかった」と返答（diff 情報が 0/0 のため）。

**変更ファイル:** JSON 上は変更ファイル情報が欠落（+0 / -0）。本文では `claudeReplayMapper.ts` とテストが対象。

**次のアクション:** トリアージと diff 反映の確認、レビュー完了後にマージ。

---

### ![PR](icons/git-pull-request.svg) [fix: making insertFinalNewLine work if last line contains whitespaces](https://github.com/microsoft/vscode/pull/324194) `#324194`
**ステータス:** Merged | **ラベル:** なし

**概要:** #314720 の修正。最終行が空白のみでも `Insert Final New Line` が最終改行を挿入できるよう、従来の「空/空白のみの最終行なら early-return」ロジックを削除して無条件で動作させる。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント、aeschli が APPROVED。

**変更ファイル:** 1 ファイル・+0 / -8。`insertFinalNewLineCommand.ts`。

**次のアクション:** マージ済み。追加対応なし。

---

### ![PR](icons/git-pull-request.svg) [fix: match uppercase query characters in fuzzyContains](https://github.com/microsoft/vscode/pull/324047) `#324047`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `fuzzyContains` が target を小文字化する一方でクエリは元の大小文字のまま比較するため、クエリの大文字が決してマッチしない不具合を修正する（例: `fuzzyContains('Explorer', 'E')` が false）。`viewQuickAccess` ピッカーが生フィルタを渡すため大文字入力でビューを絞り込めなかった。クエリも小文字化して両側を大小無視にする。

**直近のアクティビティ:** spokodev が CLA 同意し、ループを `queryLower.length` で回すよう修正して İ→i̇ のケースの回帰テストを追加。copilot-pull-request-reviewer[bot]のレビュー、dbaeumer が CHANGES_REQUESTED（Copilot レビュー参照）。

**変更ファイル:** 2 ファイル・+10 / -2。`strings.ts` とテスト。

**次のアクション:** dbaeumer の変更要求への対応が必要。

---

### ![PR](icons/git-pull-request.svg) [Task API migration - CloudAgentBackend interface + TaskApiBackend implementation](https://github.com/microsoft/vscode/pull/313395) `#313395`
**ステータス:** Closed | **ラベル:** なし

**概要:** VS Code の Cloud Agent を sweagentd の Jobs API から Mission Control の Task API へ移行する。Jobs API は毎タスクで PR が必要で PR 作成に 30 秒ブロックするが、Task API は即時タスク作成・PR なしタスク・イベントストリーミング・途中 steering をサポートする。2,400 行の `CopilotCloudSessionsProvider` を再実装せず、`CloudAgentBackend` インターフェース背後にバックエンド固有ロジックを抽出する。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビュー概要をコメント。

**変更ファイル:** 9 ファイル・+1192 / -565。`cloudAgentBackend.ts`・`taskApiBackend.ts`・`jobsApiBackend.ts`・`copilotCloudSessionsProvider.ts` など。

**次のアクション:** クローズ済み（マージされず）。追加対応なし。

---

## qmk/qmk_firmware

> 直近は Community Module のユニットテスト対応や CLI 改善、tap-hold 周りのバグ修正・ドキュメント整備など core と開発者体験に関わる PR が中心。長期停滞している XAP や新規キーボード追加 PR も動きがあり、activity は develop ブランチ向けの機能追加が目立つ。

### ![PR](icons/git-pull-request.svg) [Support unit testing for Community Modules.](https://github.com/qmk/qmk_firmware/pull/26307) `#26307`
**ステータス:** Open | **ラベル:** core, documentation, python, cli, community_module

**概要:** Community Modules 向けにユニットテストの探索・コンパイル・実行を行う仕組みを QMK のテスト基盤に追加する。各モジュールの `modules/{module_name}/tests/{suite}/` にテストを置き、Google Test を使って `make test:community_module:{module_path}` の構文で実行できる。`qmk/hello_world` モジュール用のテスト例も同梱している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 13 ファイルを変更（+373 / -36）。Makefile や builddefs 配下のテストリスト（`community_modules_testlist.mk` を新規追加）、`lib/python/qmk/cli/generate/` 配下の Python CLI、`docs/features/community_modules.md`、`hello_world` モジュールのテスト一式が対象。

**次のアクション:** レビュー待ち。core への影響が大きいためメンテナのレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Require `--keyboard` for some CLI subcommands](https://github.com/qmk/qmk_firmware/pull/26308) `#26308`
**ステータス:** Open | **ラベル:** python, cli

**概要:** `--keyboard` を必要とする一部の CLI サブコマンドで、キーボード未指定時に `TypeError`（`generate-community-modules-h` で NoneType エラー）が出て落ちる問題への対策。関連する複数コマンドを見直し、必要な箇所で `--keyboard` を必須化するよう修正した。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 3 ファイルを変更（+17 / -37）。`community_modules.py`、`dfu_header.py`、`info_json.py` の各 generate CLI が対象で、差し引きで行数を削減している。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Add Ergo‑L keycodes and sendstring](https://github.com/qmk/qmk_firmware/pull/25766) `#25766`
**ステータス:** Open | **ラベル:** core, documentation, awaiting review, dd

**概要:** フランス語向け Ergo‑L レイアウトのキーコードと sendstring を追加する。`qmk generate-keycode-extras --lang ergol` で `keymap_ergol.h` を生成している。

**直近のアクティビティ:** xaltsc が、以前の自身の PR (#25047) への言及とともに、プレフィックスに（通常ギリシャ語を指す）`EL` を選んだ理由を質問した。以前 drashna が APPROVED 済み。

**変更ファイル:** 4 ファイルを変更（+782 / -0）。`keycodes_ergol_0.0.1.hjson`（+527）と `keymap_ergol.h`、`sendstring_ergol.h` を新規追加し、`reference_keymap_extras.md` を更新。

**次のアクション:** プレフィックス命名についての質問への回答が必要。承認済みだがマージ前の調整余地あり。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Need a way to configure unicode tap code delay](https://github.com/qmk/qmk_firmware/issues/26304) `#26304`
**ステータス:** Open | **ラベル:** enhancement, help wanted

**概要:** 一部アプリで `tap_code()` のキー検出のために `TAP_CODE_DELAY` を 15〜20ms 以上にする必要があるが、それが unicode キー（7〜8 キーをタップ）の送出を著しく遅くし、連続入力で取りこぼしが起きる。unicode タップ用に別の遅延を使えるようにしたいという要望。解決案として `UNICODE_TAP_DELAY` の追加（案 1、投稿者の推奨）か `send_char()` を weak 化する案（案 2）を提示し、実装前に方針合意を求めている。

**直近のアクティビティ:** 投稿者 NapOli1084 が補足。問題のアプリはゲームで unicode は使わず、WinCompose と unicode map を用いて記号やフランス語のアクセント文字を入力しており、Windows のキーボードレイアウト切替の煩わしさを避けるためにこの構成を使っていると説明した。

**次のアクション:** メンテナによる採用方針（案 1／案 2）の判断待ち。合意が取れれば投稿者が PR を作成予定。

---

### ![PR](icons/git-pull-request.svg) [Use `memmove` for overlapping copies in RGB/LED matrix last-hit tracker](https://github.com/qmk/qmk_firmware/pull/26306) `#26306`
**ステータス:** Open | **ラベル:** core

**概要:** RGB/LED matrix の last-hit tracker にある、重なり合うメモリ領域への `memcpy` が問題となるバグの修正。Gentoo-GCC が既定で有効化する `_FORTIFY_SOURCE=2` 環境で 9 打鍵後にロックアップする現象を調査し、`rgb_matrix.c` と `led_matrix.c` の該当箇所を特定。安全に重複領域を扱える `memmove` に置き換えた。issue #20525 を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイルを変更（+8 / -8）。`quantum/led_matrix/led_matrix.c` と `quantum/rgb_matrix/rgb_matrix.c` がそれぞれ 4 行ずつ置換。

**次のアクション:** レビュー待ち。core のバグ修正のためメンテナ確認が必要。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open | **ラベル:** needs doc, in progress, core, keyboard, help wanted, keymap, needs testing, documentation, breaking_change, python, cli, feature, CI, no-ci, dd

**概要:** #11567 に基づく XAP（eXtensible Application Protocol）の実装を追跡するための作業中（draft）PR。スコーピング文書に沿った大規模な実装で、長期にわたり進行中。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に t-8ch と tzarc のコメント、2024 年に jock070 の APPROVED あり）。

**変更ファイル:** 96 ファイルを変更（+7548 / -26）。`data/xap/` の各バージョン hjson、`quantum/xap/` のハンドラ群、`lib/python/qmk/xap/` および `xap_client` の Python 実装、各種テンプレートやドキュメント、USB プロトコル（chibios/lufa/vusb）まで広範に及ぶ。

**次のアクション:** draft のため継続開発中。ドキュメント・テスト整備が未完で、breaking_change を含むため慎重な進行が必要。

---

### ![PR](icons/git-pull-request.svg) [[Docs] Tap-hold doc improvements and suggest an HRMs starter recipe](https://github.com/qmk/qmk_firmware/pull/26284) `#26284`
**ステータス:** Merged | **ラベル:** documentation

**概要:** tap-hold 関連ドキュメントの改善。`tap_hold.md` で tap-hold 設定が影響するキー（`MT`, `LT`, `TT`, `OSL`, `OSM`, `SH_T`, `SH_TT`）を明示し、Tap Dance は対象外である点を注記。home row mods の入門レシピ（urob 風のタイマー依存を減らす構成）を提示し、`feature_layers.md` で `TT` のトグルに Quick Tap が必要な点を明確化、`tap_dance.md` に mod tap インターセプトという代替案を追記した。

**直近のアクティビティ:** filterpaper が APPROVED（マージにつながる承認）。過去に drashna と getreuer のコメント、drashna の APPROVED あり。

**変更ファイル:** 4 ファイルを変更（+34 / -4）。`feature_layers.md`、`features/tap_dance.md`、`mod_tap.md`、`tap_hold.md` の各ドキュメントが対象（`tap_hold.md` が +25 と最多）。base ブランチは master。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Clear weak mods before processing queued keypresses in tap-hold handling](https://github.com/qmk/qmk_firmware/pull/26214) `#26214`
**ステータス:** Open | **ラベル:** core

**概要:** tap-hold 処理でバッファされたイベントが `action_exec()` を経由せず処理されるため、前の打鍵で残った weak mods が後続の打鍵に影響してしまうバグの修正。LT でレイヤーを有効化し tapping term 内に修飾付きキーコードと非修飾キーコードを連続入力した際に発生（例: `!=` を打とうとして `!+` になる）。バッファされたレコードを処理する前に weak mods をクリアする修正を実装した。

**直近のアクティビティ:** 直近 24 時間のコメントなし（drashna が APPROVED 済み）。

**変更ファイル:** 2 ファイルを変更（+75 / -3）。`quantum/action_tapping.c`（+16 / -3）と、`tests/tap_hold_configurations/default_mod_tap/test_tap_hold.cpp` にテストを追加（+59）。

**次のアクション:** 承認済み。マージに向けたメンテナの最終確認待ち。

---

### ![PR](icons/git-pull-request.svg) [[Keyboard] Add Mithril](https://github.com/qmk/qmk_firmware/pull/26212) `#26212`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale

**概要:** カスタム分割 ortholinear キーボード Mithril（片手 4x6 + サム 3 キー、per-key RGB matrix）の追加。ATmega32U4／Caterina ブートローダを使用し、左右はシリアルで通信する。ハードウェア設計はオープンソースで公開されている。

**直近のアクティビティ:** github-actions[bot]が 45 日間活動がないため PR を stale として自動マーク（30 日後にクローズ予定）。過去に zvecr から複数回 CHANGES_REQUESTED、drashna からも CHANGES_REQUESTED が出ている。

**変更ファイル:** 7 ファイルを追加（+359 / -0）。`keyboards/mithril/` 配下の `config.h`、`info.json`、default／sanfeps キーマップ、`readme.md`、`rev1/keyboard.json` が対象。base ブランチは master。

**次のアクション:** 投稿者による変更要求への対応が必要。放置するとクローズされるためステータス更新が求められる。

---

### ![PR](icons/git-pull-request.svg) [feat: add framed uart support](https://github.com/qmk/qmk_firmware/pull/26198) `#26198`
**ステータス:** Open | **ラベル:** core, stale

**概要:** バイト単位の UART 上にパケットリンク層を追加する PR。TX キュー、タイムアウト時のリトライ付き ACK 待ち、RX フレーマを備える。フレーム開始検出・完了判定・整合性検証・ACK 照合・ACK 送信・ディスパッチのプロトコルコールバックを呼び出し側が提供する。`UART_FRAME_DRIVER_REQUIRED` でオプトインでき、既定は無効で既存キーボードへの影響はない。

**直近のアクティビティ:** github-actions[bot]が 45 日間活動がないため stale として自動マーク（30 日後にクローズ予定）。

**変更ファイル:** 3 ファイルを変更（+245 / -0）。`builddefs/common_features.mk` の変更と、`drivers/uart_frame.c`（+210）・`drivers/uart_frame.h`（+30）の新規追加。

**次のアクション:** レビュー待ちだが stale 化しているため、投稿者のステータス更新かメンテナのレビューが必要。

---
