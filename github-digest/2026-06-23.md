# GitHub ダイジェスト — 2026-06-23

## 本日のハイライト

1. ![Issue](icons/issue-opened.svg) **[Add `Hls` to `xterm-ghostty` terminfo so tmux auto-detects OSC 8 hyperlink passthrough](https://github.com/ghostty-org/ghostty/issues/13062)** (ghostty-org/ghostty) — tmux 配下で OSC 8 ハイパーリンクが消える根本原因を terminfo の `Hls` 欠如として詳細な再現実験付きで特定した良質な報告（非メンテナ起票のため自動クローズ）。
2. ![PR](icons/git-pull-request.svg) **[Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693)** (astral-sh/uv) — dirhash による wheel アーカイブのコンテンツアドレス指定という大型パフォーマンス機能で、複数メンテナのレビューと CodSpeed の 6.88% 退行警告を巡り議論が活発。
3. ![PR](icons/git-pull-request.svg) **[feat(cmdwin): implement cmdwin as a normal buf+win](https://github.com/neovim/neovim/pull/40325)** (neovim/neovim) — cmdwin を通常バッファ化する 65 ファイル規模の大規模リファクタで、破壊的変更を伴いコードベース全体の特殊処理を排除する重要提案。
4. ![PR](icons/git-pull-request.svg) **[Chrysler: reliable fuzzy fingerprinting](https://github.com/commaai/opendbc/pull/3496)** (commaai/opendbc) — Chrysler 系のファジー FW マッチングを ~22 か月の実フィールドデータで検証し、strip=4 で 80% の認識率と誤認識ゼロを示した実証的な車両ポート PR。
5. ![PR](icons/git-pull-request.svg) **[Remove distro PAT](https://github.com/microsoft/vscode/pull/322052)** (microsoft/vscode) — 7 日強制ローテートに備え長寿命 distro PAT を全ビルドから廃止する大規模セキュリティ対応（44 ファイル）でマージ済み。

---

## sunnypilot/sunnypilot

> 直近 24 時間の更新は依存更新ボットによる 1 件のみで、機能・修正系の動きはなかった。

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/sunnypilot/sunnypilot/pull/1866) `#1866`
**ステータス:** Open | **ラベル:** bot

**概要:** repo-maintenance による自動 PR で、Python パッケージ群を更新し `uv.lock` を同期する。本文には `.venv` の容量内訳（合計 1.2G、panda3d/ffmpeg/av などが上位）と `uv pip tree` の全依存ツリーが添付されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv.lock` の 1 ファイルのみ（+162 / -164）。

**次のアクション:** CI 確認とマージ待ち。

---

## commaai/openpilot

> WebRTC スタックの依存削減（aiohttp 廃止、libdatachannel 試行）とリポジトリ構造の大規模リネーム（`openpilot/` 配下への集約、first_party 化）が中心。lane change / desire 周りの簡素化や RL モデル更新も進行中。

### ![PR](icons/git-pull-request.svg) [tools/lib: add auto_camera_source for multi-source camera file resolution](https://github.com/commaai/openpilot/pull/38103) `#38103`
**ステータス:** Open | **ラベル:** tools, stale

**概要:** ログ向けの `auto_source()` に相当するカメラ版 `auto_camera_source()` を追加し、fcamera/dcamera/ecamera/qcamera のマルチソース解決とフォールバックを可能にする。`logreader.py:149` の TODO を解消し、`comma_api_source()` を 4 種のカメラファイルに対応させる。12 件のテストを追加。

**直近のアクティビティ:** github-actions ボットが「24 日間活動なし、7 日後に自動クローズ」と通知。

**変更ファイル:** 3 ファイル（+162 / -5）。`tools/lib/file_sources.py`・`logreader.py`・`tests/test_logreader.py`。

**次のアクション:** stale 化しており、レビューがなければ自動クローズ予定。

---

### ![PR](icons/git-pull-request.svg) [Simplify lane change logic](https://github.com/commaai/openpilot/pull/38229) `#38229`
**ステータス:** Open | **ラベル:** なし

**概要:** `desire_helper.py` の lane change ロジックを簡素化する（+16 / -46）。本文は PR テンプレートのままで具体的な説明は未記入。process replay では差分なし（66 passed）。

**直近のアクティビティ:** process replay diff レポートが「0 changed, 66 passed」を報告。

**変更ファイル:** 1 ファイル（+16 / -46）。`openpilot/selfdrive/controls/lib/desire_helper.py`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [desire cleanups](https://github.com/commaai/openpilot/pull/38188) `#38188`
**ステータス:** Closed | **ラベル:** なし

**概要:** `desire_helper.py` のクリーンアップ（+18 / -41）。#38229 と同系統の整理で、本文は未記入。マージされず Closed。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+18 / -41）。`selfdrive/controls/lib/desire_helper.py`。

**次のアクション:** クローズ済み（#38229 に引き継がれたとみられる）。

---

### ![PR](icons/git-pull-request.svg) [try libdatachannel](https://github.com/commaai/openpilot/pull/38228) `#38228`
**ステータス:** Open | **ラベル:** なし

**概要:** WebRTC 実装で libdatachannel を試す変更。`uv.lock` から大量の依存（-159）を削除し、`webrtcd.py` などを簡素化する。本文は空。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 6 ファイル（+45 / -209）。`system/webrtc/webrtcd.py`・`device/video.py`・`uv.lock`・`teleoprtc_repo` ほか。

**次のアクション:** 実験的 PR。検証とレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/commaai/openpilot/pull/38227) `#38227`
**ステータス:** Merged | **ラベル:** bot

**概要:** repo-maintenance による Python パッケージ自動更新。`uv.lock` と各サブモジュール参照（opendbc/panda/tinygrad）、`docs/CARS.md`、`orientation.py` を更新。

**直近のアクティビティ:** model replay プロットの参照コメントと、process replay diff「0 changed, 66 passed」が投稿された。

**変更ファイル:** 6 ファイル（+172 / -177）。`uv.lock`（+165/-167）が中心。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Full RL](https://github.com/commaai/openpilot/pull/38164) `#38164`
**ステータス:** Open | **ラベル:** autonomy

**概要:** 運転モデル `driving_supercombo.onnx` を更新する RL 関連 PR（+2 / -2、実体は ONNX バイナリ差し替え）。本文は PR テンプレートのまま。

**直近のアクティビティ:** process replay diff レポートが「0 changed, 66 passed」を報告。

**変更ファイル:** 1 ファイル。`openpilot/selfdrive/modeld/models/driving_supercombo.onnx`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [webrtc: replace aiohttp with stdlib http server](https://github.com/commaai/openpilot/pull/38226) `#38226`
**ステータス:** Merged | **ラベル:** なし

**概要:** `webrtcd.py` の HTTP サーバを aiohttp から標準ライブラリ実装へ置き換え、aiohttp とその依存ツリー（aiohappyeyeballs・aiosignal・frozenlist・multidict・yarl など）を `uv.lock` から完全に削除する。

**直近のアクティビティ:** adeebshihadeh が stefpi に検証・レビュー・マージを依頼。process replay diff は「0 changed, 66 passed」。

**変更ファイル:** 3 ファイル（+164 / -215）。`webrtcd.py`（+164/-47）、`pyproject.toml`、`uv.lock`（-167）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [try first_party](https://github.com/commaai/openpilot/pull/38225) `#38225`
**ステータス:** Open | **ラベル:** なし

**概要:** サブモジュール群（msgq・opendbc・panda・rednose・teleoprtc・tinygrad）を `first_party/` ディレクトリ配下へリネームし、各種ビルド・CI・パス参照を更新する構造変更。本文は空。

**直近のアクティビティ:** model replay プロットの参照コメントと、process replay diff「0 changed, 66 passed」が投稿された。

**変更ファイル:** 35 ファイル（+61 / -53）。`.gitmodules`、各 `.github/workflows/*`、`SConstruct`、`pyproject.toml`、サブモジュールのリネームなど。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [move cereal/ into nested openpilot](https://github.com/commaai/openpilot/pull/38222) `#38222`
**ステータス:** Closed | **ラベル:** なし

**概要:** `cereal/` を `openpilot/cereal/` 配下へ移動し、全コードベースの import パスを更新する大規模リネーム。本文は空。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 205 ファイル（+276 / -275）。大半が import パス更新とファイルリネーム。

**次のアクション:** クローズ済み（後続の構造変更に統合されたとみられる）。

---

### ![PR](icons/git-pull-request.svg) [move top level dirs into openpilot/](https://github.com/commaai/openpilot/pull/38206) `#38206`
**ステータス:** Closed | **ラベル:** なし

**概要:** トップレベルの各ディレクトリ（common など）を `openpilot/` 配下へ移動する大規模リネーム。本文は空。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 738 ファイルのリネーム/移動（+0 / -3）。

**次のアクション:** クローズ済み。

---

## commaai/opendbc

> Chrysler のファジーフィンガープリンティングの実証的な再挑戦が目立つほか、Tesla PartyCAN の DBC 同期や BYD Sealion 7 ポート（stale 化）が進行中。Subaru Ascent の認識不可バグ報告も新規に挙がっている。

### ![PR](icons/git-pull-request.svg) [Chrysler: reliable fuzzy fingerprinting (#1092)](https://github.com/commaai/opendbc/pull/3496) `#3496`
**ステータス:** Open | **ラベル:** car, chrysler

**概要:** Chrysler/Jeep/Ram/Dodge 向けのファジー FW マッチングを追加し、正確な DB エントリなしで新ファームウェアを識別できるようにする。過去 3 回の試み（#2928・#3018・#3329）が末尾 2 文字をリビジョン接尾辞と仮定していたのは誤りで、opendbc のフィンガープリントボット履歴（~22 か月の実データ）で検証した結果 strip=2 では 20% しか正しく識別できなかった。strip 長 2〜6 を全 PLATFORM_CODE_ECUS の一致条件付きで掃引し、strip=4 が 80% の認識率かつ誤認識ゼロで最良だったため採用。失敗時は no match に安全に縮退する。

**直近のアクティビティ:** 作成者 Acley143 が、@sshane の要望に応え実 Chrysler FP PR に対する検証をしたこと、サンプル数 n=5 がリポジトリ分割（2024 年 8 月）以降の履歴に制限される点を率直に説明。car behavior report は「40 segments、0 changed, 40 passed」。contributing 確認を促す自動レビューも付いた。

**変更ファイル:** 3 ファイル（+214 / -1）。`opendbc/car/chrysler/values.py`（+63）、新規テスト `tests/test_chrysler.py`（+151）。

**次のアクション:** レビュー待ち。マージ前に追加の歴史的データでサンプル拡充の余地あり。

---

### ![PR](icons/git-pull-request.svg) [Tesla: PartyCAN dbc sync](https://github.com/commaai/opendbc/pull/3301) `#3301`
**ステータス:** Open | **ラベル:** DBC signals

**概要:** Tesla の PartyCAN DBC を整理する。SW 2026.8.3 に存在しないメッセージを削除し、Tesla が PartyCAN にミラーする Vehicle DBC のメッセージと同期、加速度センサーやブレーキトルクなど既知のメッセージをマージする。

**直近のアクティビティ:** car behavior report が「TESLA_MODEL_3/MODEL_Y、20 segments、0 changed, 20 passed」を報告。

**変更ファイル:** 1 ファイル（+89 / -29）。`opendbc/dbc/tesla_model3_party.dbc`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [BYD Sealion 7 port](https://github.com/commaai/opendbc/pull/3352) `#3352`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, can, car, stale, car safety

**概要:** BYD Sealion 7 サポートの初期 PR。bukapilot と BYD ATTO 3 の PR（#3337）をベースにしており、CAN メッセージ/シグナルの重複度が高く同一プラットフォーム基盤とみられる。現状は openpilot で dashcam モード/速度表示が動作し、今後アクティブモードと安全性チェック、リプレイルート追加を進める予定。

**直近のアクティビティ:** github-actions ボットが「60 日間活動なし、7 日後に自動クローズ」と通知。

**変更ファイル:** 20 ファイル（+1000 / -1）。新規 `opendbc/car/byd/` 一式、`byd_sealion_7.dbc`（+200）・`byd_atto3.dbc`（+193）、安全性モジュール `safety/modes/byd.h`、テストなど。

**次のアクション:** stale 化。活動がなければ自動クローズ予定。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3495) `#3495`
**ステータス:** Merged | **ラベル:** CI / testing, bot

**概要:** commaci-public による週次の `uv.lock` 依存更新。`.github/workflows/tests.yml` も併せて更新。

**直近のアクティビティ:** car behavior report が「No changes detected」を報告。

**変更ファイル:** 2 ファイル（+118 / -88）。`uv.lock`（+114/-84）と `tests.yml`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [2021 Subaru Ascent (Canada) - Vehicle not recognized - FW versions attached](https://github.com/commaai/opendbc/issues/3494) `#3494`
**ステータス:** Open | **ラベル:** bug, car

**概要:** 2021 Subaru Ascent（カナダ仕様、Comma 4 + Subaru Harness A、openpilot nightly-dev）で車両が認識されず Dashcam Mode に留まる。VIN プレフィックス `4S4WMAAD` と、fwdCamera/engine/transmission/eps/abs の各ファームウェアバージョンを添付。CAN トラフィックは存在するが未認識のため、このファームウェアセットのサポート追加または調査を求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ファームウェア指紋の追加可否をトリアージする必要がある。

---

## commaai/panda

> 直近 24 時間の更新はありません。

---

## neovim/neovim

> vim-patch のポーティングと runtime 更新が活発で複数マージされる一方、`:restart` のセッション復元や cmdwin を通常バッファ化する大規模リファクタなど breaking-change を伴う機能 PR の議論が集中している。UI2 / cmdline / terminal / job-control 周りの修正も継続的に進行中。

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40372) `#40372`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** marvim ボットによる自動生成 PR で、`src/nvim/version.c` の vim-patch 適用状況を更新する。timer_create 用の `-lrt` 検出、test_terminal の job 待機、GTK4 のマウスイベント処理、zsh 環境での test_terminal3 失敗（9.2.0700/0701/0704/0706）に関する patch エントリを記録している。draft 状態。

**直近のアクティビティ:** janlazo が、virtual text 関連の追加 vim-patch 候補（9.0.0547/0718/0723/0962/0975）をコメントで列挙した。

**変更ファイル:** `src/nvim/version.c` の 1 ファイルのみ（+30 / -45）。

**次のアクション:** draft 解除とレビュー待ち。追加で挙がった patch の取り込み判断も必要。

---

### ![PR](icons/git-pull-request.svg) [feat(:restart)!: no bang restores session and window layout](https://github.com/neovim/neovim/pull/40321) `#40321`
**ステータス:** Open | **ラベル:** breaking-change

**概要:** `:restart` の挙動を変更し、bang なしではセッションとウィンドウレイアウトを復元、従来動作は新設の `:restart!` に割り当てる破壊的変更。`v:startreason` に新しい値を追加し、`ZR` を `:restart!` にマッピングする。Lua 実装は mini.sessions（MIT ライセンス）から移植したもので、#38940 をクローズする。AI（Codex）支援。

**直近のアクティビティ:** 作成者 nathanzeng がバックポート可否を問い、oil.nvim 利用者に破壊的挙動が出る点を指摘。レビューは justinmk・superatomic・echasnovski・zeertzjq らが多数の COMMENTED を重ね、最終的に superatomic が APPROVED を出した。

**変更ファイル:** 12 ファイル（+183 / -57）。`runtime/lua/vim/_core/ex_cmd.lua`（+62）、`test/functional/terminal/tui_spec.lua`（+82/-39）が中心で、ドキュメント（editing/gui/vvars.txt）や `src/nvim/ex_docmd.c`・`normal.c` も更新。

**次のアクション:** 1 件 APPROVED 済みだが breaking-change のためメンテナの最終判断・バックポート方針の確定待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(statusline): defer redraw in aucmd context](https://github.com/neovim/neovim/pull/40309) `#40309`
**ステータス:** Open | **ラベル:** なし

**概要:** `nvim_exec_autocmds({ buf = ... })` は `aucmd_prepbuf()` を通じて一時的に `curwin`/`curbuf` を対象バッファのウィンドウへ切り替えるが、その間に autocmd が statusline/winbar の再描画を要求すると、`%!` statusline が `g:statusline_winid` と `win_getid()` の比較で非カレントの対象ウィンドウをアクティブと誤認する。本 PR は `aucmd_prepbuf()` が実際にウィンドウを切り替えた状況を追跡し、その間は statusline/winbar の再描画を遅延させて `aucmd_restbuf()` 復帰後に評価させる。

**直近のアクティビティ:** kristijanhusak が「期待通り動作する」とコメント。justinmk・barrettruth・zeertzjq・luukvbaal の間で多数の COMMENTED レビューによる詳細な議論が続いている。

**変更ファイル:** 4 ファイル（+125 / -5）。`src/nvim/statusline.c`・`autocmd.c`・`autocmd.h` のコア実装に加え、`test/functional/ui/statusline_spec.lua`（+98）でテストを追加。

**次のアクション:** レビューコメントへの対応継続中。承認待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): keep cmdwin cursor on cmdline hide](https://github.com/neovim/neovim/pull/40364) `#40364`
**ステータス:** Closed | **ラベル:** ui2

**概要:** `cmdheight=0` 時、UI2 はコマンドライン表示中に実際の `'cmdheight'` を一時的に上げ、非表示時に戻す。この復元処理が `<c-f>` で cmdwin を開く際にも走り、`splitkeep=screen` が古い画面範囲を保持するためカーソルが過去の履歴へ移動してしまう。解決として cmdwin がアクティブな間は `'splitkeep'` を強制しないようにし、#40303 をクローズしようとした。

**直近のアクティビティ:** justinmk が「'splitkeep' 関連なら #40325 で直るかも、確認できるか」と提案。作成者 barrettruth が「そちらで直った」とし #40303 のクローズを #40324 側で対応するようリマインドして本 PR をクローズした。

**変更ファイル:** 2 ファイル（+21 / -1）。`runtime/lua/vim/_core/ui2/cmdline.lua` と `test/functional/ui/cmdline2_spec.lua`。

**次のアクション:** クローズ済み。修正は #40325 に委ねられた。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): attribute `TermResponse` to source channel](https://github.com/neovim/neovim/pull/40330) `#40330`
**ステータス:** Open | **ラベル:** defaults, tui

**概要:** #40175 がリモート UI 向けの attach 時ターミナル探索を追加したが、端末応答に識別子が無いため per-UI 状態の実装に進めない。本 PR は `TermResponse` イベントデータに RPC ソースチャンネルを含め、`vim.tty.request()` 呼び出し側がそのチャンネルでフィルタできるようにする前提的な変更。オプションやカラースキームの per-UI 化はまだ行わない。受理されれば後続でビルトインの端末探索/クエリを監査しチャンネルフィルタを適用する予定。

**直近のアクティビティ:** justinmk が複数の COMMENTED レビューをしたうえで APPROVED を出した。その後 barrettruth が応答コメントを返している。

**変更ファイル:** 8 ファイル（+140 / -79）。`runtime/lua/vim/_core/defaults.lua`（+32/-28）、`runtime/lua/vim/tty.lua`、`src/nvim/autocmd.c`・`api/events.c`、`test/functional/api/ui_spec.lua`（+90/-45）など。

**次のアクション:** APPROVED 済み。マージ可否の最終確認待ち。

---

### ![PR](icons/git-pull-request.svg) [feat(lua): add job and detached modes to vim.system](https://github.com/neovim/neovim/pull/40267) `#40267`
**ステータス:** Open | **ラベル:** lua

**概要:** #39552 の解決を狙う（前回の試み #40209）。現在 `vim.system()` は生の `uv.spawn`（mode='process'）のみ対応で、`SystemObj:kill()` が子孫プロセスを終了できず孤児プロセスツリーが残る。Neovim 内部の job-control 層（jobstart/jobstop）を使う mode='job' と、teardown 所有権なしにプロセスグループを kill する mode='detached' を追加し、既定の mode='process' は従来動作を維持する。draft 状態。

**直近のアクティビティ:** justinmk と作成者 ManojPanda3 がそれぞれ COMMENTED レビューを 1 件ずつ交わしている。

**変更ファイル:** 11 ファイル（+601 / -280）。`runtime/lua/vim/_core/system.lua`（+179/-253）の大幅書き換えに加え、`src/nvim/api/vim.c`（+104）、新規 `src/nvim/jobs.c`・`jobs.h`、`channel.c`、ドキュメント・テストを更新。

**次のアクション:** draft 解除と継続レビュー。設計の合意形成が必要。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): save state file with `:write`](https://github.com/neovim/neovim/pull/40097) `#40097`
**ステータス:** Open | **ラベル:** terminal, gsoc

**概要:** ターミナルバッファの内容とメタデータはプロセス終了後に失われる問題に対処する。`:w name.txt` のプレーンテキスト保存は通常のバッファ書き込みとして既存挙動を維持し、`:w name.mpack` では `.mpack` 拡張子でレンダリング済みターミナル状態をメタデータ付き msgpack としてエクスポートする。`cwd` の信頼性が課題として残る。#40017 関連。GSoC プロジェクト。

**直近のアクティビティ:** justinmk と zeertzjq が多数の COMMENTED レビューをし、作成者 Willaaaaaaa が継続的に応答。直近も 6/22〜6/23 にかけてやり取りが続いている。

**変更ファイル:** 12 ファイル（+620 / -80）。新規 `src/nvim/terminal_encode.c`（+281）・`terminal_defs.h`（+84）・`runtime/lua/vim/_core/terminal.lua`（+66）、`src/nvim/terminal.c`（+84/-77）、`ex_cmds.c`、テスト・ドキュメントなど。

**次のアクション:** レビュー反映継続中。`cwd` 問題の解消が必要。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0702: :windo and :tabdo create an extra window with 'winfixbuf'](https://github.com/neovim/neovim/pull/40358) `#40358`
**ステータス:** Merged | **ラベル:** has:vim-patch, window

**概要:** `'winfixbuf'` 設定時に `:windo`/`:tabdo` が余分なウィンドウを作る問題の修正。`ex_listdo()` はバッファ変更コマンドが切り替えられるよう `'winfixbuf'` を回避するため新規ウィンドウを分割するが、`:windo`/`:tabdo` はカレントウィンドウのバッファを変えないためその処理をスキップする。#40355 をクローズ。

**直近のアクティビティ:** justinmk が CONTRIBUTING.md の AI 支援作業ガイドラインを参照し、PR 説明に AI 由来の冗長な記述が多い点を指摘。zeertzjq が APPROVED した。

**変更ファイル:** 2 ファイル（+31 / -15）。`src/nvim/ex_cmds2.c`（+4/-1）と `test/old/testdir/test_winfixbuf.vim`（+27/-14）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Window with local `winfixbuf` is duplicated by `:tabdo wincmd =`](https://github.com/neovim/neovim/issues/40355) `#40355`
**ステータス:** Closed | **ラベル:** has:vim-patch, bug-vim, options, window

**概要:** カレントウィンドウで window-local オプション `winfixbuf` が有効なとき、`:tabdo wincmd =` が同じバッファを表示する追加ウィンドウを予期せず作成する。`wincmd =` はウィンドウサイズの均等化のみを行うべきで新規ウィンドウを作るべきではない。v0.12.3 で発生し、Vim 9.2.650 でも同様の挙動。

**直近のアクティビティ:** ShivaPriyanShanmuga が根本原因は共有の `ex_listdo()` にあり vim/vim#14301 と同根と説明、まず Vim 上流（vim/vim#20600）で修正してから vim-patch ポートとして持ち込むと表明。justinmk は CONTRIBUTING.md の確認を促した。

**次のアクション:** クローズ済み（#40358 のマージで解決）。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0705: :delete # silently fails to update "# and clobbers "0](https://github.com/neovim/neovim/pull/40371) `#40371`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** `:delete #` が `"#` レジスタの更新に失敗し `"0` を上書きする問題の修正。`"#` を `"/` と同様に扱い、`:let` と `setreg()` でのみ書き込み可能とする。vim/vim#20592 をクローズ。Doug Kearns との共著。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+30 / -14）。`src/nvim/register.c`、`runtime/doc/change.txt`、`test/old/testdir/test_excmd.vim`・`test_registers.vim`、`test/functional/legacy/eval_spec.lua`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: runtime file updates](https://github.com/neovim/neovim/pull/40370) `#40370`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** 複数の runtime ファイル更新をまとめた vim-patch。fennel の `'comments'` への `;` コメントリーダー追加、beancount の非 ASCII 勘定科目名サポート、dtrace のアクションブロック前の probe ハイライト対応（`BEGIN{ ... }` 形式の認識）など。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 3 ファイル（+7 / -4）。`runtime/ftplugin/fennel.vim`、`runtime/syntax/beancount.vim`、`runtime/syntax/dtrace.vim`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: nvim_open_term, terminal jobs, misc](https://github.com/neovim/neovim/pull/40214) `#40214`
**ステータス:** Open | **ラベル:** なし

**概要:** justinmk による広範なドキュメント更新 PR。`nvim_open_term`、ターミナル job、その他の項目に関する記述を整理する。API・terminal・options・LSP などのドキュメントと、それらの生成元 Lua ファイルを横断的に修正している。draft 状態。

**直近のアクティビティ:** justinmk と zeertzjq が COMMENTED レビューを交わしている。直近 24 時間内の新規コメントはなく、最新更新は 6/22 の同期によるもの。

**変更ファイル:** 25 ファイル（+443 / -274）。`runtime/doc/api.txt`（+65/-33）、`runtime/doc/vimfn.txt`、`runtime/doc/terminal.txt`（+55）、`src/nvim/api/vim.c`、`src/nvim/eval.lua`、各 LSP モジュールなど doc とその生成ソースが中心。

**次のアクション:** draft 解除とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [feat(cmdwin): implement cmdwin as a normal buf+win](https://github.com/neovim/neovim/pull/40325) `#40325`
**ステータス:** Open | **ラベル:** AI assisted 🤖

**概要:** cmdwin（`q:` のコマンドラインバッファ）がコードベース全体で特殊扱いを必要とする制約を解消するため、cmdwin を通常のバッファ＋ウィンドウとして再実装する大規模変更。`<c-r>=` や検索などの再帰的「inception」サポートを廃止する代わりに、`'winfixbuf'`・`bufhidden=wipe`・`buftype=nofile` を持つ通常ウィンドウとして実装し、プラグインやコアが cmdwin を特別扱いしなくて済むようにする。垂直セパレータのマウスドラッグや cmdwin 内 inccommand が無償で動くようになる。#40312・#30340 を修正。破壊的変更として、insert モードからの式レジスタ cmdline や `input()`/`inputlist()` 内での cmdwin、マクロ/マッピングでの cmdwin 利用が壊れる可能性がある。draft 状態。

**直近のアクティビティ:** justinmk 自身による COMMENTED レビューが 3 件あるのみ。直近 24 時間内の新規コメントはなく、最新更新は 6/22。

**変更ファイル:** 65 ファイル（+1185 / -1548）と大規模。`src/nvim/ex_getln.c`（+73/-290）の大幅削減、新規 `runtime/lua/vim/_core/cmdwin.lua`（+225）、`src/nvim/ex_cmds.lua`（+318/-307）、多数の C ソースとテストの整理（削除された `test/functional/legacy/cmdwin_spec.lua` など）。

**次のアクション:** draft のため設計レビューと合意形成が必要。破壊的変更の評価待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(folds): use single column spill indicator in foldcolumn](https://github.com/neovim/neovim/pull/40368) `#40368`
**ステータス:** Open | **ラベル:** folds

**概要:** `fdc` が 2 以上のとき `foldcolumn` のオーバーフローが fold レベルを表す不要な数字を表示する挙動を改善する。最初の列に省略された fold セパレータ数を示す単一のスピルインジケータを表示するよう変更する。Vim と挙動が変わるが、稀なケースであること、オーバーフロー表示がより一般的な UI パターンであること、`fill_foldcolumn` ロジックの簡素化と `fi_low_level`→`fi_num_folds` への置き換えになることを理由に妥当だとしている。

**直近のアクティビティ:** justinmk が `feat` 扱いと news 記載が必要と回答し、ロジック簡素化を歓迎しつつ「SGTM、他のメンバーの意見も聞きたい」とコメントした。

**変更ファイル:** 7 ファイル（+71 / -39）。`src/nvim/drawline.c`（+22/-24）、`src/nvim/fold.c`、`fold_defs.h`、`runtime/doc/fold.txt` ほか、`test/functional/ui/fold_spec.lua`（+35）。

**次のアクション:** 他メンバーのレビュー待ち。`feat` への変更と news 追記が必要。

---

### ![PR](icons/git-pull-request.svg) [feat(statusline)!: allow %= in item groups, scope %< to item groups](https://github.com/neovim/neovim/pull/40369) `#40369`
**ステータス:** Open | **ラベル:** statusline, breaking-change

**概要:** アイテムグループ内で区切りマーカー `%=` が無視される問題（Neovim 0.12 で ruler 幅指定にグループ構文を多重利用したことによる #39984 の回帰）を解消し、グループ内で `%=` を使えるようにする新機能。あわせて切り詰めマーカー `%<` をアイテムグループにスコープし、グループは既定で先頭で切り詰め、各グループごとに切り詰め位置を制御できるようにする破壊的変更。例として既定 statusline の ruler 式が `%18(%l,%c%V%=\ %P%)` と書けるようになる。#39984 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+378 / -246）。`src/nvim/statusline.c`（+246/-236）が中心で、`runtime/doc/options.txt`・`src/nvim/options.lua` と `test/unit/statusline_spec.lua`（+117）。

**次のアクション:** レビュー待ち。breaking-change のため慎重な評価が必要。テスト追加方法の課題も残る。

---

### ![PR](icons/git-pull-request.svg) [ci: bump msys2/setup-msys2 from 2.31.1 to 2.32.0 in the github-actions group across 1 directory](https://github.com/neovim/neovim/pull/40324) `#40324`
**ステータス:** Merged | **ラベル:** ci, dependencies, github_actions

**概要:** Dependabot による依存更新で、GitHub Actions グループの `msys2/setup-msys2` を 2.31.1 から 2.32.0 へ引き上げる。新版はベースディストリビューションを 20260611 に更新し、ncc から rolldown へのバンドル移行や SBOM の同梱を含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+1 / -1）。`.github/workflows/test_windows.yml`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): vim.on_key callbacks can tell if key will be consumed](https://github.com/neovim/neovim/pull/40089) `#40089`
**ステータス:** Open | **ラベル:** lua, mappings

**概要:** `vim.on_key` コールバックは、先行コールバックや `getchar()` によって既に処理・破棄されたキーを無視したくても、その状況を知る術がなかった。本 PR は `vim.on_key` コールバックに `discard` 引数を追加し、コールバックがキーを破棄した場合や `getchar()` に消費されてメインループに渡らない場合に true を設定する。さらにコールバックを登録の新しい順に実行する。#40010 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 6 ファイル（+119 / -55）。`runtime/lua/vim/_core/editor.lua`（+41/-38）、`runtime/doc/lua.txt`、`src/nvim/getchar.c`・`lua/executor.c`、`test/functional/lua/vim_spec.lua`（+45）。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(cmdline): encode no prompt in cmdline_show.hl_id](https://github.com/neovim/neovim/pull/40052) `#40052`
**ステータス:** Merged | **ラベル:** なし

**概要:** `cmdline_show` イベントで空のプロンプトとプロンプト無しを区別できない問題を、プロンプト無しのとき `cmdline_show.hl_id` を -1 に設定することで解消する。あわせて UI2 で空の `input()` の前のメッセージが表示されない問題を、非ブール型プロンプトをダイアログウィンドウへ振り分けることで修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 6 ファイル（+44 / -30）。`runtime/doc/api-ui-events.txt`（+26/-21）、`runtime/lua/vim/_core/ui2/cmdline.lua`、`src/nvim/ex_getln.c`、`test/functional/ui/cmdline2_spec.lua`・`screen.lua`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [ui2: previous search count result reappears in cmdline after <C-l>](https://github.com/neovim/neovim/issues/40354) `#40354`
**ステータス:** Closed | **ラベル:** ui2

**概要:** ui2 有効時、`<C-l>`（既定）で検索結果が一旦消えても、その後のキー操作（h/l などのカーソル移動）で前回の検索カウント結果が cmdline に再表示される。手動で `:nohlsearch` を実行した場合はカーソル移動で再表示されない。NVIM v0.13.0-dev で再現。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み（#40367 のマージで解決）。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): clear search_count after clearing the screen](https://github.com/neovim/neovim/pull/40367) `#40367`
**ステータス:** Merged | **ラベル:** ui2

**概要:** 画面クリア時に「last」の virtual text がクリアされず、重複カウンタの virtual text が 1 より増えない問題を修正する。画面クリア時に「last」virtual text をクリアし、過去のコミットで失われた代入を復元する。#40354 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル（+29 / -2）。`runtime/lua/vim/_core/ui2/messages.lua` と `test/functional/ui/messages2_spec.lua`（+25）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [`nvim_exec2()` accepts args (like `nvim_exec_lua`)](https://github.com/neovim/neovim/issues/40159) `#40159`
**ステータス:** Open | **ラベル:** api, vimscript, has:workaround, lua

**概要:** `nvim_exec_lua` は args を受け取れるが `nvim_exec2()` は受け取れず、Vimscript へ引数を渡す手軽な方法がない（`nvim_call_function()` 以外）。シリアライズの往復なしに buffer-local 変数を更新するなどのユースケースに対し、`nvim_exec2` が `nvim_exec_lua` 同様の args リストを受け取り `v:args` 経由で参照できること、結果に `result` フィールド（`v:result`）を含めることを提案している。

**直近のアクティビティ:** dqnne が「良さそう、PR に着手する」と表明。justinmk は `nvim_call_function`/`nvim_call_dict_function` を `nvim_exec2` のラッパーとして内部実装し直す案を示し、`_call_function` の排除や Vimscript 呼び出し経路の統一に言及した。

**次のアクション:** dqnne による PR 作成待ち。

---

### ![Issue](icons/issue-opened.svg) [Missing "/" when DIR name contains parenthesis (depending on 'isfname')](https://github.com/neovim/neovim/issues/23530) `#23530`
**ステータス:** Closed | **ラベル:** closed:invalid, bug-vim, filesystem, options, editor

**概要:** パスに括弧を含むファイル（例 `app\(login)\_layout.tsx`）を開くと、パスセパレータが欠落して `app(login)\_layout.tsx` として開かれ、ファイルが空になる。Windows で `(`/`)` が `isfname` に含まれないことに起因する。

**直近のアクティビティ:** lmgraf と justinmk が活発に議論。justinmk は `fnameescape` ではなく `'isfname'` の影響を受けない `bufadd()`/`bufload()`（例 `vim.fn.bufadd(vim.fs.normalize(...))` 後に `:buffer`）の使用を推奨し、nvim-tree.lua / telescope.nvim もこの方式を採るべきと回答。プラグインがどうファイルを開いているかが論点となっている。

**次のアクション:** クローズ済み（invalid）。プラグイン側での `bufadd()` 利用が推奨され、推奨方法のドキュメント化が検討されている。

---

### ![PR](icons/git-pull-request.svg) [job-control: do system()/:! in this process-group](https://github.com/neovim/neovim/pull/8389) `#8389`
**ステータス:** Open | **ラベル:** job-control

**概要:** `system()` や `:!` を Neovim 自身のプロセスグループ内で実行するようにする変更で、#8217 をクローズする。2018 年作成の長期 PR。

**直近のアクティビティ:** justinmk が、関連議論が #39552 にあること（そちらにはコメントしないよう注記）を述べ、再訪する予定だが深い検討が必要だとコメントした。

**変更ファイル:** 10 ファイル（+134 / -36）。`src/nvim/event/process.c`・`process.h`、`os/process.c`・`os/shell.c`、`test/helpers.lua`（+55）、`test/functional/core/job_spec.lua`（+27）など。

**次のアクション:** 設計の再検討が必要。長期保留中。

---

### ![PR](icons/git-pull-request.svg) [fix(process): avoid inherited stdio for Windows jobs](https://github.com/neovim/neovim/pull/40074) `#40074`
**ステータス:** Open | **ラベル:** job-control

**概要:** #40009（0.12.2 で表面化）の修正。Windows で埋め込みサーバが `AttachConsole(ATTACH_PARENT_PROCESS)` で親ターミナルのコンソールを借用した結果、サーバの stdio ハンドルがそのコンソール（CON）を指し、チャンネル job がそれを継承して libuv が `CREATE_NO_WINDOW` なしで spawn してしまう。CON に直接書く job（例 gutentags）が再描画まで TUI を上書きする。Windows 専用の `stdio_noinherit` フラグを追加し、設定時は子の stdin/stderr に継承ハンドルでなく `UV_CREATE_PIPE` を使うことで `CREATE_NO_WINDOW` を可能にし CON 書き込みの漏れを防ぐ。AI 支援。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+78 / -5）。`src/nvim/event/libuv_proc.c`（+40/-5）、`channel.c`、`event/defs.h`・`proc.h`、`test/functional/terminal/tui_spec.lua`（+35）。

**次のアクション:** レビュー継続中（justinmk・zeertzjq の COMMENTED 済み）。承認待ち。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40348) `#40348`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** marvim ボットによる `src/nvim/version.c` の自動更新。vim.ico の最適化、ccfilter.c のバッファオーバーフロー、text property virtual text での境界外読み取り（9.2.0679、セキュリティ）、ヘルプタグ再生成などの vim-patch エントリを記録する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+34 / -23）。`src/nvim/version.c`。

**次のアクション:** マージ済み。

---

## nvim-telescope/telescope.nvim

> パスに括弧を含むファイルを開けない長年のバグ（#2446）に対し、`fnameescape` を `bufadd()` 方式へ置き換える修正 PR が提出され、Neovim 側の推奨に沿った対応として議論が進んでいる。

### ![PR](icons/git-pull-request.svg) [Fix: replace usage of `fnameescape()` with `bufadd()` approach when instantiating a new buffer](https://github.com/nvim-telescope/telescope.nvim/pull/3682) `#3682`
**ステータス:** Open | **ラベル:** なし

**概要:** `fnameescape` の使用を廃し、`vim.fs.normalize()` + `bufadd()`/`bufload()` による新バッファ生成方式へ置き換える。`isfname` に依存する `fnameescape` は括弧をエスケープできず #2446 を引き起こすため、Neovim メンテナ推奨の方式に切り替える。`vim.cmd` の文字列形式をテーブル形式にし、`vim.fs.normalize` を使うことで `\` 区切りの問題も回避する。#2446 を修正し #1171 の残課題にも対応。

**直近のアクティビティ:** justinmk が COMMENTED レビューを 1 件実施。作成者は自身が Neovim の専門家ではない旨を明記し、慎重なレビューを依頼している。

**変更ファイル:** 2 ファイル（+7 / -4）。`lua/telescope/actions/set.lua` と `lua/telescope/from_entry.lua`。

**次のアクション:** レビュー対応中。`drop`/`tab drop` や Linux での追加検証が残っている。

---

### ![PR](icons/git-pull-request.svg) [ci: bump actions/checkout from 6 to 7](https://github.com/nvim-telescope/telescope.nvim/pull/3683) `#3683`
**ステータス:** Open | **ラベル:** dependencies, github_actions

**概要:** Dependabot による依存更新で、`actions/checkout` を v6 から v7 へ引き上げる。v7 では fork PR のチェックアウトブロックや ESM 化、Node.js 24 対応などが含まれる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 4 ファイル（+5 / -5）。`.github/workflows/` 配下の ci/docgen/lint/release ワークフロー。

**次のアクション:** レビュー・マージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Unable to preview and open files with parentheses in the path](https://github.com/nvim-telescope/telescope.nvim/issues/2446) `#2446`
**ステータス:** Open | **ラベル:** bug, Windows

**概要:** SvelteKit の advanced layout group（`(group)` を含むパス）など、括弧を含むパス配下のファイルを telescope でプレビュー・オープンできない。パスセパレータの `/` が欠落し空バッファが開かれる。Windows で `(`/`)` が `isfname` に含まれないことに起因する長年の問題。

**直近のアクティビティ:** lmgraf が、これは telescope ではなく Neovim 側の問題だと整理し、推奨修正 PR（#3682）を提出したことを報告。回避策として telescope ロード前に `vim.o.isfname = vim.o.isfname .. ",(,)"` を設定する方法も共有している。

**次のアクション:** #3682 のマージで解消見込み。

---

## ghostty-org/ghostty

> macOS 27（Golden Gate）beta 対応のタブ/タイトルバー修正と追跡 Issue、PageList のスクロール不具合（u16 オーバーフロー）修正、Kitty ドラッグ&ドロッププロトコルのパーサ実装が進行。VOUCHED リストや依存更新ボットの動きも多い。

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13072) `#13072`
**ステータス:** Merged | **ラベル:** なし

**概要:** ghostty-vouch ボットによる VOUCHED リスト更新で、@nilsherzig を vouch（信任）に追加する。discussion コメントを契機に自動生成された PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+1 / -0）。`.github/VOUCHED.td`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Meta: macOS Golden Gate (v27)](https://github.com/ghostty-org/ghostty/issues/13001) `#13001`
**ステータス:** Open | **ラベル:** os/macos

**概要:** 次期 macOS Golden Gate（v27、2026 年 6 月 8 日発表）対応の追跡 Issue。Ghostty は v27 でおおむね問題なく動作するが、軽微な視覚的不具合があり得るとし、新機能の適合が主作業。beta 利用者は tip リリースの使用を推奨している。

**直近のアクティビティ:** bo2themax と dj-skn が、`macos-titlebar-style = tabs` 時に macOS 27 beta 2 でタブ/タイトルバーのコントロールが右上で潰れて表示される不具合を議論。tip ビルド（1.3.2-main）でも再現すると報告され、bo2themax は別 Issue/discussion での扱いを促してこのメタ Issue を整理しようとしている。

**次のアクション:** 各不具合は個別 Issue/discussion へ切り出し。v27 正式リリースに向け追跡継続。

---

### ![PR](icons/git-pull-request.svg) [core: fix PageList.scroll using wrong type for row offset](https://github.com/ghostty-org/ghostty/pull/13048) `#13048`
**ステータス:** Merged | **ラベル:** なし

**概要:** `PageList.scroll` で残り行数の型を `CellCountInt=u16` から `usize` に変更する修正。行オフセットが `maxInt(u16)=65535` を超えるとスクロールが失敗していた。#13000（GTK アプリで巨大なスクロールバック時にスクロールバー中央クリックが効かない）を修正。数百万行規模で顕在化する。AI 不使用と明記。

**直近のアクティビティ:** mitchellh が APPROVED（「Thank you!」）。

**変更ファイル:** 1 ファイル（+10 / -16）。`src/terminal/PageList.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macOS: fix tabs frame on macOS 27 beta 2](https://github.com/ghostty-org/ghostty/pull/13069) `#13069`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** macOS 27 beta 2 でのタブフレーム表示を修正する。あわせて正式リリース後も挙動が維持されるか確認するためのフォローアップ Issue（#13070）を作成。

**直近のアクティビティ:** mitchellh が「動的ディスパッチ化して Golden Gate 用タブのサブクラスを作るべきか」と提案しつつ現状のままマージ。bo2themax は正式リリース前に行う価値があるか不明としフォローアップ側で言及するとした。

**変更ファイル:** 1 ファイル（+3 / -1）。`macos/.../TitlebarTabsTahoeTerminalWindow.swift`。

**次のアクション:** マージ済み。フォローアップは #13070 で追跡。

---

### ![Issue](icons/issue-opened.svg) [Check whether the `tabs` style works correctly after the first release](https://github.com/ghostty-org/ghostty/issues/13070) `#13070`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS 27 beta 2 でタブバーのリネームが行われているため、正式リリース後に `macos-titlebar-style = tabs` が正しく動作するか確認するためのフォローアップ Issue。Xcode の UI テストで既存のタブ関連 UI を検証できる。動的ディスパッチ化と Golden Gate 用タブサブクラス化の検討も TODO に含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 正式リリース後に検証を実施。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump actions/checkout from 6.0.3 to 7.0.0](https://github.com/ghostty-org/ghostty/pull/13044) `#13044`
**ステータス:** Merged | **ラベル:** dependencies, github_actions

**概要:** Dependabot による `actions/checkout` の v6.0.3 → v7.0.0 更新。多数のワークフローに反映。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 9 ファイル（+68 / -68）。`.github/workflows/` 配下の test/release/nix/vouch 系ワークフロー。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update iTerm2 colorschemes](https://github.com/ghostty-org/ghostty/pull/13055) `#13055`
**ステータス:** Merged | **ラベル:** dependencies

**概要:** github-actions ボットによる iTerm2-Color-Schemes の上流リリース取り込み（release-20260615）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+12 / -12）。`build.zig.zon` 系と `flatpak/zig-packages.json`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump softprops/action-gh-release from 3.0.0 to 3.0.1](https://github.com/ghostty-org/ghostty/pull/13059) `#13059`
**ステータス:** Merged | **ラベル:** dependencies, github_actions

**概要:** Dependabot による `softprops/action-gh-release` の v3.0.0 → v3.0.1 更新（依存更新中心のメンテナンスリリース）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+6 / -6）。`.github/workflows/release-tip.yml`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13065) `#13065`
**ステータス:** Merged | **ラベル:** なし

**概要:** ghostty-vouch ボットによる VOUCHED リスト更新で、@schickling-assistant を denounce（信任取り消し）する。discussion コメントを契機に自動生成。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+1 / -0）。`.github/VOUCHED.td`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Kitty dnd parser](https://github.com/ghostty-org/ghostty/pull/13029) `#13029`
**ステータス:** Open | **ラベル:** なし

**概要:** 新しい Kitty ドラッグ&ドロッププロトコルのパーサを Ghostty 向けに実装する（#12852 の discussion を受けた作業）。まずアクションを接続せずパーサのみを実装する方針で、`kitty_clipboard_protocol.zig` の形式を踏襲。テスト作成と初期の構造設計に Claude Opus 4.8（Claude Code）を使用したと開示。

**直近のアクティビティ:** 作成者 ajr-khll が「GTK 向けに端末へのドラッグ&ドロップ（into）を実装完了、端末からの drag out は未実装」と報告。レビューは jcollie・mitchellh が APPROVED 済み（コメント増を希望する指摘あり）。

**変更ファイル:** 14 ファイル（+1237 / -3）。新規 `osc/parsers/kitty_dnd_protocol.zig`（+372）・`termio/kitty_dnd.zig`（+244）、`apprt/gtk/class/surface.zig`（+340）、`termio/stream_handler.zig`（+147）など。

**次のアクション:** 承認済みだがアクション接続と drag out 実装が今後の課題。コメント追記の指摘あり。

---

### ![Issue](icons/issue-opened.svg) [Add `Hls` to `xterm-ghostty` terminfo so tmux auto-detects OSC 8 hyperlink passthrough](https://github.com/ghostty-org/ghostty/issues/13062) `#13062`
**ステータス:** Closed | **ラベル:** なし

**概要:** tmux 配下では、クライアント端末の terminfo に拡張機能 `Hls` がないと OSC 8 ハイパーリンクがサイレントに破棄される。`xterm-ghostty` terminfo は `Ms`（OSC 52 クリップボード）など多くの拡張ケーパビリティを持つ一方 `Hls` を欠いている。`src/terminfo/ghostty.zig` に tmux 標準値の `Hls` を追加すれば、ローカルでも SSH 先でも tmux が自動でハイパーリンクパススルーを検出できるようになる。3 つの隔離した tmux サーバでの比較実験（A: 素の状態で 0 件、B: Hls 追加で通過、C: 手動設定で通過）で根本原因を `Hls` 欠如に特定している。

**直近のアクティビティ:** ghostty-vouch ボットが「非メンテナはまず discussion を作成する必要がある」として自動クローズ。

**次のアクション:** クローズ済み（自動）。discussion 化のうえ `Hls` 追加の検討が望ましい。

---

## astral-sh/uv

> コンテンツアドレス指定キャッシュ（dirhash によるアーカイブ重複排除）や `uv check` のロック済み ty バージョン利用など大型機能の開発が活発で、同時に Windows でのトランポリン実行ファイル更新失敗や `uv lock --upgrade` のリグレッション、ワークスペース source override のリグレッションといったバグ報告・修正が並行して進んでいる。

### ![Issue](icons/issue-opened.svg) [failt to create venv in windows](https://github.com/astral-sh/uv/issues/19873) `#19873`
**ステータス:** Closed | **ラベル:** bug, windows, needs-mre

**概要:** Windows 11 上で `uv venv --python ../path/to/python --seed venv` を実行すると、シードパッケージ（`wheel-0.47.0`）のインストールに失敗する。原因は `uv-trampoline-*.exe` に対する Windows PE リソースの更新失敗（`os error -2147024786`「系统无法打开指定的设备或文件」）で、トランポリン実行ファイルの生成直後の書き換えがアンチウイルスや EDR に妨害されているとみられる。

**直近のアクティビティ:** ottokruse が、これは #15968 の重複ではないのではと指摘しつつ、企業 VDI 環境で `uv tool install --force mistral-vibe` が uv 0.11.23 で失敗する（`pywin32` の PE リソース更新失敗、`os error -2147024891`）事例を報告。uv 0.9.8 ではツール環境のインストール自体は成功するが `.local\bin` に shim がコピーされないという回避策込みの比較も共有している。

**次のアクション:** needs-mre ラベルが付いており再現可能な最小例の提供待ち。AV/Defender 干渉前提でトランポリン書き込み処理の堅牢化を検討する必要がある。

---

### ![Issue](icons/issue-opened.svg) [install.ps1 on Windows: Failed to download uv 0.11.23](https://github.com/astral-sh/uv/issues/19956) `#19956`
**ステータス:** Open | **ラベル:** bug

**概要:** Windows 10 で `install.ps1`（および `uv-installer.ps1`）を実行すると、`releases.astral.sh` と `github.com` の両方からのダウンロードに失敗し「failed to download binaries」で終わる。一方、ブラウザからは同じ URL（`uv-x86_64-pc-windows-msvc.zip`）を問題なくダウンロードできる。報告者はエラー時にダウンロード URL 全体を表示するよう提案している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 原因調査（ダウンロード失敗の切り分け）と、提案されている完全な URL 表示によるエラーメッセージ改善の検討。

---

### ![Issue](icons/issue-opened.svg) [uv.lock for workspaces](https://github.com/astral-sh/uv/issues/17351) `#17351`
**ステータス:** Open | **ラベル:** question

**概要:** ワークスペースで `uv sync --all-packages --all-groups` を実行すると全体ビルド用の `uv.lock` は得られるが、各ライブラリ個別の `uv.lock` は生成されない。そのためワークスペース内ライブラリを別の場所で再利用する際にバージョンを固定したロックファイルが無く、再ビルドで異なるバージョンになるリスクがあるとして、これが意図的な挙動か質問している。

**直近のアクティビティ:** CarlossShi が回避策を共有。`[tool.uv.workspace].members` から一時的にパッケージを除外し、そのディレクトリで `uv lock` を実行して個別の `uv.lock` を生成・コミットしてからワークスペースに戻す、という手順で独立開発とワークスペースメンバーの両立をしているが理想的ではないと述べている。

**次のアクション:** ワークスペースメンバー個別ロックファイルのサポート方針について、メンテナの判断待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix CLI upgrade precedence with configured packages](https://github.com/astral-sh/uv/pull/19955) `#19955`
**ステータス:** Open | **ラベル:** なし

**概要:** `upgrade-package` が設定されている場合に CLI のアップグレード戦略の優先順位を復元する修正。`Upgrade` リファクタで導入された `combine` 実装が、優先度の低い設定側の戦略を正としてしまい、`uv lock --upgrade` が全パッケージではなく設定に列挙されたパッケージのみをアップグレードしていたリグレッション（#19954）を修正する。プロジェクトおよび pip 設定のスナップショットも更新している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-configuration/src/package_options.rs`（+7 / -4）と `crates/uv/tests/sync/show_settings.rs`（+70 / -56）の 2 ファイル。テストスナップショットの更新が変更の大半を占める。

**次のアクション:** ドラフト状態のためレビュー依頼前の仕上げ・CI 確認待ち。

---

### ![PR](icons/git-pull-request.svg) [Deduplicate binary payloads with archive manifests](https://github.com/astral-sh/uv/pull/19694) `#19694`
**ステータス:** Open | **ラベル:** なし

**概要:** これまで抽出済みの wheel アーカイブはツリー全体しか重複排除できず、同じネイティブライブラリ（`.so`/`.pyd`/`.dll`/`.dylib`）を含む別の wheel はそれぞれ別コピーを保持していた。本 PR はネイティブライブラリのペイロードを BLAKE3 コンテンツダイジェストと実行ビットをキーとする `archive-files-v0` バケットに分離し、各アーカイブが wheel 相対パスから共有オブジェクトへのマニフェストを持つ。インストール時はマニフェストを読んで対象環境へリンクし、既定では可能ならハードリンクを使う。キャッシュのクリーニング・プルーニングや競合検出もマニフェスト対応に拡張される。#19693 にスタックされている。

**直近のアクティビティ:** codspeed-hq[bot]が 2 件コメント。ベースブランチ（`charlie/dirhash-archives`）比較ではパフォーマンス変化なし（13 ベンチマーク無影響）と報告する一方、フレームグラフ生成は内部エラーで失敗している。人間によるレビューコメントはまだない。

**変更ファイル:** 32 ファイル（+1237 / -115）。`crates/uv-distribution/src/distribution_database.rs`（+273）、`crates/uv-install-wheel/src/linker.rs`（+185）、`crates/uv-cache/src/lib.rs`（+143）、新規 `crates/uv-install-wheel/src/archive_file_manifest.rs`（+97）、`crates/uv/tests/pip_install/pip_install.rs`（+289）などが中心。

**次のアクション:** ドラフトかつ #19693 にスタックされているため、まず土台 PR のマージ待ち。並行してレビューが必要。

---

### ![Issue](icons/issue-opened.svg) [Why is the `-o` alias for the `--outdated` flag to `pip list` not implemented?](https://github.com/astral-sh/uv/issues/19921) `#19921`
**ステータス:** Open | **ラベル:** question

**概要:** `pip list -o` で古いパッケージを確認していたユーザーが、`uv pip list` に同等の短縮形が無いことを指摘。`uv pip` は pip/pip-tools の drop-in 代替と謳われているため、`uv pip list -o` を `--outdated` と同じ動作で追加してほしいと要望している。

**直近のアクティビティ:** woodruffw が、`-o` を省いた当初の理由は互換レイヤー外の uv 独自コマンドのフラグと衝突するためだと説明。報告者の jonathandung は該当コメントを見落としていたと認めつつ、pip+venv から移行するユーザーには歓迎される追加だろうと述べている。

**次のアクション:** `-o` の衝突を踏まえた上で互換性向上として実装するかどうか、メンテナの方針判断待ち。

---

### ![PR](icons/git-pull-request.svg) [Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693) `#19693`
**ステータス:** Open | **ラベル:** enhancement, performance, preview

**概要:** これまで抽出済み wheel はランダム生成 ID で `archive-v0` に保存され、別キャッシュ経由だと同じ wheel が複数回保存され得た。本 PR は ZIP wheel 抽出時に決定的なディレクトリダイジェスト（dirhash）を計算してアーカイブ ID に用いる。ダイジェストは正規化パス・ファイルサイズ・実行ビット・内容・空ディレクトリを捉え、ZIP エントリ順や無関係なメタデータを無視する。バージョン付き BLAKE3 から 18 バイトを取り 24 文字の URL-safe 文字列にエンコードする。先行 PR #16816 はアーカイブ全体をハッシュ化していたが、本 PR は並列化と将来の大容量ファイル重複排除（#19694）を可能にするため内容ごとにハッシュ化している。`content-addressed-cache` プレビュー機能でゲートされたオプトイン。

**直近のアクティビティ:** charliermarsh が dirhash レビューを woodruffw に、セキュリティレビューを EliteTK に、全体方針を zanieb に依頼。charliermarsh と woodruffw による COMMENTED レビューが計 3 件。codspeed-hq[bot]は main 比で `unzip_wheel_many_files` と `prepare_wheel_many_files` の 2 ベンチが退行し合計 6.88% のパフォーマンス低下を警告。astral-sh-bot[bot]はテストが 6 件追加されたと報告している。

**変更ファイル:** 25 ファイル（+1763 / -316）。新規 `crates/uv-extract/src/dirhash/seek.rs`（+796）と `dirhash/mod.rs`（+349）、`archive_path.rs`（+88）、`crates/uv-extract/src/stream.rs`（+190/-96）、`crates/uv-distribution/src/distribution_database.rs`（+153/-55）が中核。

**次のアクション:** dirhash・セキュリティ・全体方針の各レビュー進行中。CodSpeed が指摘したパフォーマンス退行への対応または acknowledge が必要。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate（renovate[bot]）が自動生成・更新する依存関係ダッシュボード Issue。検出された Renovate 更新と依存関係の一覧、長期間更新の無い「放棄された依存関係」（cargo の `arcstr`、`byteorder`、`dunce` など 33 件）などを掲載する管理用 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし（本文が bot により自動更新された）。

**次のアクション:** Renovate による自動管理が継続。個別の依存更新 PR の取り込みは各 PR 側で対応。

---

### ![Issue](icons/issue-opened.svg) [`uv lock --upgrade` does not work if `upgrade-package` is set in `pyproject.toml`](https://github.com/astral-sh/uv/issues/19954) `#19954`
**ステータス:** Open | **ラベル:** bug

**概要:** 常に最新に保ちたい内部パッケージを `upgrade-package` に登録していると、`uv lock --upgrade`（や `uv sync --upgrade`）を実行してもその 1 パッケージ以外は何もアップグレードされない。コマンドライン引数は upgrade-package を尊重しつつ全体をアップグレードするはずだという期待に反する挙動で、0.10.8（#18226 の修正）から発生し始めたとトレースされている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正 PR #19955 が作成済みでクローズ予定。PR のマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Reapply "Fix transparent Python upgrades in project environments"](https://github.com/astral-sh/uv/pull/19928) `#19928`
**ステータス:** Merged | **ラベル:** bug

**概要:** pre-commit-uv を壊していたために一度リバートされていたコミット（#19925）を再度リバートし、プロジェクト環境における透過的な Python アップグレードの挙動を再有効化する。pre-commit-uv 側が修正済み（tox-dev/pre-commit-uv#152）になったため再適用が可能になった。

**直近のアクティビティ:** zanieb が APPROVED。Gankra が、修正が pre-commit-uv の最新リリース（4.2.2）に含まれていることを確認したとコメントしている。

**変更ファイル:** `crates/uv-virtualenv/src/virtualenv.rs`（+5 / -1）と `crates/uv/tests/python/python_upgrade.rs`（+83 / -1）の 2 ファイル。テストの追加が中心。

**次のアクション:** マージ済み。対応完了。

---

### ![PR](icons/git-pull-request.svg) [Downgrade to Rust 1.95](https://github.com/astral-sh/uv/pull/19953) `#19953`
**ステータス:** Merged | **ラベル:** internal

**概要:** Rust 1.96 には `cargo` のバグがあり、本来リトライされるべき一時的/プロトコル内エラーが致命的エラーとして表面化する。特に HTTP/2 接続が不安定な GitHub Actions で問題になるため、Rust ツールチェーンを 1.95 へダウングレードする。1.96.1 か 1.97 のリリース後に再度引き上げる予定。

**直近のアクティビティ:** Gankra が APPROVED し、「1.96 の機能にはまだ依存していないようなので妥当」とコメントしている。

**変更ファイル:** `crates/uv-build/rust-toolchain.toml` と `rust-toolchain.toml`（各 +1 / -1）の 2 ファイルのみ。機能的変更なし（NFC）。

**次のアクション:** マージ済み。Rust 1.96.1/1.97 リリース後の再引き上げが今後の課題。

---

### ![Issue](icons/issue-opened.svg) [`--no-sync` for `uv run` via configuration parameter](https://github.com/astral-sh/uv/issues/12429) `#12429`
**ステータス:** Open | **ラベル:** configuration, needs-decision

**概要:** `uv run` のデフォルト挙動を設定で変更したい、特に `--no-sync` をプロジェクトのデフォルトにしたいという要望。`uv.toml` や `pyproject.toml` での設定方法がドキュメントに見当たらないため、将来的なサポート可否を質問している。

**直近のアクティビティ:** tigerhawkvok が、自分も `uv run` 専用の `no_sync` 設定オプションを望むと賛同。`upgrade_package` を設定しており同期が非常に遅くなるため、そのケース以外は同期したいと述べている。

**次のアクション:** needs-decision ラベルが付いており、`uv run` 限定の `no-sync` 設定オプションを追加するかメンテナの判断待ち。

---

### ![PR](icons/git-pull-request.svg) [Use locked ty versions in uv check](https://github.com/astral-sh/uv/pull/19884) `#19884`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv check` は現在、プロジェクトのロック済み開発依存が別バージョンや別ソースの `ty` を選んでいても、uv 組み込みの互換範囲からスタンドアロンの `ty` 実行ファイルを解決している。本 PR は `uv.lock` の該当 `dev` 依存グループが `ty` を選ぶ場合にその正確なロック済みパッケージを実行する。`--no-sync` や除外された `dev` グループでは、プロジェクト環境を変更せずロック済み `ty` のサブグラフのみをキャッシュ環境にマテリアライズする。ty バージョン選択の優先順位は `TY` 実行ファイル上書き → `--ty-version` → `uv.lock` の該当 `ty` → uv 既定の制約範囲、となる。#19907・#19909・#19892 に分割・依存している。

**直近のアクティビティ:** zanieb と zsol を中心に COMMENTED レビューが計 40 件と非常に活発に議論が進行中（承認はまだ）。astral-sh-bot[bot]はベースブランチ比でテストが 5 件追加・4 件削除されたと報告している。

**変更ファイル:** 8 ファイル（+478 / -9）。`crates/uv/tests/project/check.rs`（+246）、`crates/uv-resolver/src/lock/mod.rs`（+103）、`crates/uv/src/commands/project/check.rs`（+69）、新規 `tool_from_lock.rs`（+54）が中心。

**次のアクション:** 大量のレビューコメントへの対応が必要。依存 PR（#19907 など）のマージ後に取り込み。

---

### ![PR](icons/git-pull-request.svg) [Use Packse in `uv check` tests](https://github.com/astral-sh/uv/pull/19913) `#19913`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv check` のプロジェクト lock/sync テストで、ライブ PyPI 依存（`anyio`・`iniconfig` や不要な `--exclude-newer` カットオフ）を既存の Packse フィクスチャに置き換え、ローカルインデックスを明示指定して決定的なパッケージメタデータを使うようにする。宣言済み依存と隔離プロジェクトのカバレッジを `test-r2` シャードへ移動。`TY` 実行ファイルの優先順位テストは実物の `ty` 実行ファイルが必要なため `test-pypi` ゲートのまま残す。

**直近のアクティビティ:** zanie-oai・zsol・zanieb による COMMENTED レビューに加え、zanieb が APPROVED 済み。

**変更ファイル:** 3 ファイル（+157 / -109）。`crates/uv/tests/project/check.rs`（+104/-109）が中心で、`pip_install_scenarios.rs`（+37）と新規シナリオ TOML を追加。

**次のアクション:** zanieb の承認済み。残りのレビューコメント対応後マージ可能。

---

### ![PR](icons/git-pull-request.svg) [Lock scrolls used in credentialed workflows](https://github.com/astral-sh/uv/pull/19950) `#19950`
**ステータス:** Open | **ラベル:** なし

**概要:** レジストリ統合と Python リリース同期のワークフローは、認証情報を持つジョブ内で PEP 723 スクリプトを実行している。直接の要件は制約されていたが、uv が実行時に直接・推移的依存を解決するため、依存パッケージから新たに公開されたコードがクラウド/レジストリ認証情報やリポジトリトークンにアクセスし得る状態だった。本 PR は認証情報投入前に `uv sync --locked` で環境を同期し、`uv run --locked --no-build` でスクリプトを実行することで、各環境を既存依存に限定する。両方のレジストリ認証モードおよびスケジュール/手動の Python リリース更新フローは変更しない。

**直近のアクティビティ:** zanie-oai・zsol・zanieb による COMMENTED レビューが計 3 件（承認はまだ）。

**変更ファイル:** 6 ファイル（+125 / -6）。`.github/workflows/` 配下 2 ファイルの修正に加え、新規ロックファイル `fetch-download-metadata.py.lock`（+83）・`registries-test.py.lock`・`sync-python-version-constants.py.lock` を追加。

**次のアクション:** セキュリティ強化目的の変更でレビュー進行中。レビューコメント対応待ち。

---

### ![Issue](icons/issue-opened.svg) [uv installs x86_64 Python on arm64 Windows](https://github.com/astral-sh/uv/issues/12906) `#12906`
**ステータス:** Open | **ラベル:** question

**概要:** GitHub の Windows 11 ARM ランナー（`windows-11-arm`）上で `uv python install 3.9` を実行すると、ホストが arm64 にもかかわらず x86 版 Python がダウンロードされる。パッケージビルドは通るがテスト実行時にアーキテクチャ不一致で失敗する。最小再現ワークフローと CI 実行ログが添付されている。

**直近のアクティビティ:** Avasam が GitHub CI での回避策を共有。`setup-uv` の `python-version` で OS が arm 終端なら `aarch64`、そうでなければ `x86_64` をサフィックスとして付与する matrix 設定例を提示し、「aarch64」は Linux 系の呼称ながら機能すると述べている。

**次のアクション:** arm64 Windows でのデフォルト Python アーキテクチャ選択ロジックの修正方針について検討が必要。

---

### ![PR](icons/git-pull-request.svg) [Add support for xonsh venv activation](https://github.com/astral-sh/uv/pull/19740) `#19740`
**ステータス:** Open | **ラベル:** なし

**概要:** xonsh シェル向けの venv アクティベーションをサポートする。`activate.xsh` を追加し、xonsh から仮想環境を有効化できるようにする。#19606 をクローズする。

**直近のアクティビティ:** EliteTK が、非リロケータブル版に対応する意味があるか疑問を呈し、powershell と同様にリロケータブルのみで良いのではとレビューコメント。qaijuang による COMMENTED レビューが計 4 件あり、設計面の議論が進行中。

**変更ファイル:** 4 ファイル（+150 / -7）。新規 `crates/uv-virtualenv/src/activator/activate.xsh`（+102）に加え、`crates/uv-fs/src/path.rs`（+30/-4）、`virtualenv.rs`、`tests/python/venv.rs` を修正。

**次のアクション:** 非リロケータブル対応の要否などレビュー指摘への対応待ち。

---

### ![Issue](icons/issue-opened.svg) [Regression in workspace source override between uv 0.11.19 and 0.11.20](https://github.com/astral-sh/uv/issues/19916) `#19916`
**ステータス:** Closed | **ラベル:** bug

**概要:** uv 0.11.19 と 0.11.20 以降の間で、スタンドアロンインストールも可能なワークスペースメンバーに関するリグレッションが発生し、`Workspace members must be declared as workspace sources` エラーが出る。ワークスペースルートとメンバーの間に（メンバーではない）`pyproject.toml` が挟まる構成で再現し、`override-dependencies` を明示しても解消しない。#18311 や #18954 に関連する可能性が指摘されている。

**直近のアクティビティ:** j-helland が konstin の質問に回答。規制上の理由で多数のリポジトリを単一モノレポに統合できず、エンジニア向けに uv ワークスペースを動的生成する独自ワークフローのため、ワークスペースルートとメンバーの間に非メンバーの `pyproject.toml` が存在する事情を説明している。

**次のアクション:** Closed。リグレッションの修正対応がなされたとみられる。

---

### ![PR](icons/git-pull-request.svg) [Generate PEP 740 attestations](https://github.com/astral-sh/uv/pull/19489) `#19489`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv publish` に PEP 740 アテステーションの「生成」初期サポートを追加する。これによりユーザーは `attest-action` 等で事前/帯域外にアテステーションを生成する必要がなくなり、`uv publish` が自動で行う。WIP でマージ準備未了。`chrono` を `jiff` に置き換える依存トリミングなど上流調整 TODO は完了済みだが、`--no-attestations` 以外の適切な CLI フラグ整備が残っている。#15618 をクローズ予定。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 15 ファイル（+1241 / -230）。`Cargo.lock`（+352）、`crates/uv-publish/src/lib.rs`（+449/-183）、新規クレート `crates/uv-pep740`（`types.rs` +259、`sign.rs` +55 など）が中心。

**次のアクション:** ドラフトかつ WIP。CLI フラグ設計の確定とユニット/統合テストの追加が必要。

---

### ![Issue](icons/issue-opened.svg) [Provide a way for `uv lock` to ignore and replace existing and/or broken `uv.lock` files](https://github.com/astral-sh/uv/issues/16348) `#16348`
**ステータス:** Open | **ラベル:** bug

**概要:** git マージ競合などで `uv.lock` が壊れて有効な TOML でなくなると、`uv lock` や `uv lock --upgrade` がエラーになる。既存ロックファイルを無視して再生成する `--replace`/`--recreate` などのオプションを設けるか、マージ競合時でも優雅に `uv.lock` を再作成できる方法が欲しいという提案。

**直近のアクティビティ:** philipp-horstenkamp が、単純な上書きではなく構造化されたフォールバック戦略を選べる `--resolve-strategy` 引数を提案。無効/競合したロックファイルに遭遇した際に `HEAD`（マージ先の安定ブランチ）を構造的アンカーとして使う `target`/`head` をデフォルト推奨とし、`source`/`merge_head` なども挙げて、pre-commit フックでの自動化を念頭に詳述している。

**次のアクション:** `--replace` 系オプションあるいは `--resolve-strategy` の採否・設計についてメンテナの方針判断待ち。

---

### ![Issue](icons/issue-opened.svg) [Cannot add packages from internal index, uv skips all versions. Pip works fine.](https://github.com/astral-sh/uv/issues/19952) `#19952`
**ステータス:** Open | **ラベル:** bug

**概要:** 社内 devpi インスタンスを使う環境で、pip は `PIP_INDEX_URL` で問題なく動作するが、`UV_DEFAULT_INDEX` を設定しても uv はパッケージのどのバージョンも見つけられず「No solution found」になる。`uv add [package-name] -vv` の詳細ログでは見つけた全バージョンをスキップしているが理由が示されていない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** バージョンがスキップされる原因の調査が必要（インデックスのメタデータや互換性判定の確認）。ログを基にした切り分けが求められる。

---

### ![PR](icons/git-pull-request.svg) [Remove `insecure` API from uv-fastid](https://github.com/astral-sh/uv/pull/19951) `#19951`
**ステータス:** Merged | **ラベル:** internal

**概要:** #19949 に続き、`uv-fastid` の `insecure` API が未使用になったため削除する。`fastrand` の問題（smol-rs/fastrand#130）解決後に再追加を検討する余地はあるが、CSPRNG のコストはここではほとんど問題にならないとしている。

**直近のアクティビティ:** woodruffw と konstin による COMMENTED レビュー、konstin の APPROVED を経てマージ。astral-sh-bot[bot]はテスト `test_insecure` が 1 件削除されたと報告している。

**変更ファイル:** 4 ファイル（+4 / -25）。`crates/uv-fastid/src/lib.rs`（+1/-23）が中心で `insecure` 実装を削除。機能的変更なし（NFC）。

**次のアクション:** マージ済み。対応完了。

---

### ![PR](icons/git-pull-request.svg) [Add locked subgraph materialization primitives](https://github.com/astral-sh/uv/pull/19907) `#19907`
**ステータス:** Open | **ラベル:** internal

**概要:** プロジェクト対応のツール実行では、リゾルバに同じ判断を再度させずに `uv.lock` で既に選択済みの正確なパッケージサブグラフをインストールする必要がある。本 PR はその後続作業（#19884）のための前提として、狭いマテリアライズとインストールのプリミティブを追加する。ツール実行や `uv run --with` の挙動自体は変更しない。

**直近のアクティビティ:** zanieb と charliermarsh を中心に COMMENTED レビューが計 7 件、charliermarsh が APPROVED。承認コメントでは、以前「任意のルートからのインストールは不可でワークスペースメンバーである必要がある」と結論したはずだが包括的な議論を見つけられない、と過去経緯への疑問を呈している。

**変更ファイル:** 7 ファイル（+1104 / -183）。`crates/uv-resolver/src/lock/installable.rs`（+902/-177）が変更の大半を占め、`install_target.rs`（+60）、`environment.rs`（+43）などが続く。

**次のアクション:** charliermarsh が承認済み。後続 PR（#19884）の土台として、残りのレビュー観点を整理しマージへ。

---

### ![PR](icons/git-pull-request.svg) [Fix relocatable `activate.fish` and broaden Fish version support](https://github.com/astral-sh/uv/pull/19856) `#19856`
**ステータス:** Merged | **ラベル:** なし

**概要:** リロケータブル仮想環境向けに生成される `activate.fish` が Bash 風の `$(...)` コマンド置換で `VIRTUAL_ENV` を導出していた 2 つの問題を修正する。(1) `$(...)` は Fish 3.4 以降のみのため 3.4 未満では構文エラーになり `uv venv --relocatable` が使えない、(2) Fish の `(...)` 置換はサブシェルで実行されないため式中の `cd` が呼び出し元シェルの作業ディレクトリを変えてしまう。Fish ネイティブの `(...)` 置換と `realpath` の使用に切り替え、全サポート対象 Fish バージョンで動作し呼び出し元のディレクトリも変えないようにした。

**直近のアクティビティ:** EliteTK が「良いがマージ前に対処すべき点がいくつかある」とコメント。decajoin による COMMENTED レビューが計 4 件あり、これらの指摘対応を経てマージされた。

**変更ファイル:** `crates/uv-virtualenv/src/virtualenv.rs`（+1 / -1）と `crates/uv/tests/python/venv.rs`（+3 / -1）の 2 ファイル。Fish ネイティブ形式を検証するテスト更新を含む。

**次のアクション:** マージ済み。対応完了。

---

### ![PR](icons/git-pull-request.svg) [Switch to free runners when on a fork](https://github.com/astral-sh/uv/pull/18359) `#18359`
**ステータス:** Open | **ラベル:** test:extended

**概要:** python-build-standalone と同様の方式で、ランナーが free 指定かどうかに基づいて plan 時に使用するランナーを生成し、uv の CI をフォーク上でほぼ全面的に実行できるようにする。CodSpeed の walltime ベンチはカスタムランナーが必要なためスキップし、一部のタイムアウトを延長する。これにより基盤ランナーのラベルが抽象化され、Depot と GitHub の命名体系を混在させる必要がなくなる。

**直近のアクティビティ:** zsol と zanieb による COMMENTED レビューが計 2 件（承認はまだ）。

**変更ファイル:** 13 ファイル（+545 / -76）。`.github/workflows/` 配下の多数のワークフロー（`ci.yml` +70、`build-dev-binaries.yml` など）の修正に加え、新規 `.github/runners.json`（+55）、`scripts/ci/resolve-ci-runners.py`（+147）、そのテスト（+73）を追加。

**次のアクション:** CI 基盤変更のためレビュー継続中。レビューコメント対応待ち。

---

### ![PR](icons/git-pull-request.svg) [Avoid archive id collisions](https://github.com/astral-sh/uv/pull/19949) `#19949`
**ステータス:** Merged | **ラベル:** bug

**概要:** ruff のテストスイート実行で `ArchiveId` の衝突とみられる事象が観測された。同じパッケージをインストールする 2 プロセスが完全に同時に開始されると発生し得るため、セキュア RNG（CSPRNG）を使って衝突を防止する。

**直近のアクティビティ:** woodruffw が APPROVED。コメントでは、2 プロセスが同時起動しても `fastrand` のグローバルシードには一意のスレッド ID が含まれるはずだと疑問を呈しつつ、`Thread::id` が OS レベルのスレッド ID と 1 対 1 対応する保証がないため `fastrand` 側のバグの可能性があると分析。konstin はスレッド ID がプロセス相対であることを指摘し、CSPRNG への切り替えに合意している。

**変更ファイル:** `crates/uv-cache/src/archive.rs` と `crates/uv-distribution/src/source/revision.rs`（各 +1 / -1）の 2 ファイル。RNG をセキュアなものへ切り替える小規模修正。

**次のアクション:** マージ済み。`insecure` API の削除は後続 #19951 で完了。

---

### ![Issue](icons/issue-opened.svg) [Add an option to suppress the "excluded packages" footer comment in `uv pip compile`](https://github.com/astral-sh/uv/issues/19948) `#19948`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv pip compile` で `--no-emit-package` により解決グラフに残るパッケージを出力から除外すると、TXT ファイルに「The following packages were excluded from the output:」という情報フッターが無条件に付与される。これが原因で `dependabot` と `uv pip compile` がこのコメントの追加・削除を繰り返してしまう。`--no-emit-package-comment` のようなフラグや `--no-annotate` でこれを抑制できるようにしてほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** フッターコメントを抑制するオプション（新規フラグまたは `--no-annotate` でのカバー）を追加するか、メンテナの判断待ち。

---

### ![Issue](icons/issue-opened.svg) [Is there a better way to install dependencies in an older Airflow Docker Container?](https://github.com/astral-sh/uv/issues/19919) `#19919`
**ステータス:** Open | **ラベル:** question

**概要:** `apache/airflow:2.4.1-python3.8` を基にした Dockerfile で、既存の Pipenv/pip 構成から uv へ移行したいが、`pip install` が拡張イメージで行う動作を再現できなかったという質問。`$PATH` をいじって `~/.local` にインストールさせる方法や、既存 airflow インストールに venv を依存させる方法を試したが断念し、`uv lock --check` → `uv export --format requirements.txt` → `pip install -r requirements.txt` で代替している。pip に依存せず、`uv.lock` 通りに同一パッケージをインストールするより良い方法を求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 既存環境（`~/.local`）への重ね合わせインストールに関するガイダンス提示、またはドキュメント改善の検討。

---

### ![Issue](icons/issue-opened.svg) [exclude-newer should have an extra flag to allow it to skip over packages that don't have publish date information.](https://github.com/astral-sh/uv/issues/12449) `#12449`
**ステータス:** Open | **ラベル:** enhancement, configuration

**概要:** 現状 `exclude-newer` は、パッケージのいずれかに公開日情報が無いと要件更新に失敗するため有用性が大きく損なわれている。公開日情報を持たないパッケージをスキップできる追加フラグが欲しいという要望。

**直近のアクティビティ:** jakob-keller が、`UV_EXCLUDE_NEWER` 環境変数と CLI パラメータがブール値 `true`/`false` を含む全範囲の値をサポートすべきという点について、#19934 で対応済みとコメントしている。

**次のアクション:** #19934 で関連対応が入ったため、本 Issue で求められた公開日なしパッケージのスキップ機能が満たされたか確認が必要。

---

## microsoft/vscode

> microsoft/vscode の直近 24 時間は Agents Window / Agent Host（Copilot CLI・Claude 連携）と Chat 周辺の機能改善・バグ修正が圧倒的多数で、エンドゲーム（1.126.0）に向けたサンドボックス機能のリバートやクレジット上限バナー、リモート再接続の安定化、ビルドの長期 PAT 廃止といったインフラ系の動きも目立つ。Issue 側は Copilot completion feedback の自動投稿やクラッシュ・黒画面など環境依存の不具合報告が多い。

### ![Issue](icons/issue-opened.svg) [Custom agent frontmatter is not automatically loaded on VSCode startup.](https://github.com/microsoft/vscode/issues/321765) `#321765`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 再起動後にカスタムエージェントを選択しても、frontmatter で設定した model や tools がチャット UI に確実に読み込まれない不具合。`user-invocable: false` のエージェントが UI に残り、サブエージェントの呼び出しにも影響する。対象の `.agent.md` を開いて無変更で再保存すると初めて設定が反映される、というワークアラウンドが報告されている。

**直近のアクティビティ:** 報告者 phebing-slashwhy が同僚でも再現すると補足し、個別保存以外の回避策がないか確認を求めている。

**次のアクション:** 再現確認とトリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Keeps crashing while trying to close one particular opened file](https://github.com/microsoft/vscode/issues/322257) `#322257`
**ステータス:** Open | **ラベル:** なし

**概要:** Linux（CachyOS/Arch、Wayland+KDE）環境で、変更のある特定ファイルのタブを閉じようとすると VS Code がクラッシュする。`DialogService#prompt`（保存確認ダイアログ）表示直後に crashpad の `tag not found` / `read out of range` エラーが出力される。

**直近のアクティビティ:** Saucistophe が同一ログで再現すると報告し、新規/既存ファイルを 1 文字変更してタブを閉じる手順を提示。#321312 のワークアラウンドが有効だったと追記している。

**次のアクション:** 重複（#321312）の可能性を含めトリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Switching to Agent mode from Plan erases prompt](https://github.com/microsoft/vscode/issues/316908) `#316908`
**ステータス:** Open | **ラベル:** なし

**概要:** Plan モードで入力途中のプロンプトがある状態で Agent モードに切り替えると、入力したプロンプトが消去され代わりに "Start implementation" が送信されてしまう。消えたプロンプトは履歴にも残らない。モード切替で送信が走らないこと、プロンプトが消えないことが期待値。

**直近のアクティビティ:** zadorski が拡張 0.53.1 / VS Code 1.125.1（WSL2）でも依然再現すると報告し、再現用 JSON を添付している。

**次のアクション:** 修正対応待ち。

---

### ![PR](icons/git-pull-request.svg) [chat: enhance draft input restoration logic to use session history model](https://github.com/microsoft/vscode/pull/322514) `#322514`
**ステータス:** Open | **ラベル:** なし

**概要:** リモート（外部提供）チャットセッションの再読み込み時に、未送信のドラフト入力をセッションメタデータから復元する処理を改善する Draft PR。復元するドラフトの `selectedModel` を、古い永続化値ではなくセッション履歴から導出したモデルで上書きする。該当テストの再有効化と「履歴のモデルが優先される」ケースのテスト追加を含む。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要コメントを投稿（指摘 0 件）。

**変更ファイル:** `chatServiceImpl.ts`（+12/-2）と `chatService.test.ts`（+45/-2）の 2 ファイル。

**次のアクション:** Draft のため作業継続中。

---

### ![PR](icons/git-pull-request.svg) [Recover from stale reconnection token after server replacement](https://github.com/microsoft/vscode/pull/317774) `#317774`
**ステータス:** Open | **ラベル:** なし

**概要:** `--enable-remote-auto-shutdown` での長時間スリープ後にリモートサーバーが置き換わり、`Unknown reconnection token` で再接続が拒否され Reload Window プロンプトに落ちる問題を透過的に回復させる。`ErrorMessage` に任意の `reasonCode: ConnectionRejectionReason` を追加し、不明トークン拒否時に `ManagementPersistentConnection` がトークンをローテートして `PersistentProtocol` セッションをリセット・再ハンドシェイクする（最大 3 回）。Management 接続のみが対象で、Extension Host の透過回復は #317980 で追跡。

**直近のアクティビティ:** 著者 EhabY が @alexdima @roblourens にメンションし、リベース完了・レビュー可能と連絡。copilot bot による複数回のレビューと EhabY による多数の返信コメントが交わされている。

**変更ファイル:** `ipc.net.ts` / `ipc.ts` / `remoteAgentConnection.ts`（+110/-13）/ `remoteExtensionHostAgentServer.ts` ほか計 7 ファイル（+399/-51）。テストも厚く追加。

**次のアクション:** メンテナ（alexdima/roblourens）によるレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Add config to split xtab diff patches via per-patch diff](https://github.com/microsoft/vscode/pull/322438) `#322438`
**ステータス:** Open | **ラベル:** なし

**概要:** diff-patch 応答形式で各編集が `-`/`+` の連続ブロックとして出力され、`resolveEdit` が変更箇所周辺の文脈行まで含めた粗い `LineReplacement` を作る問題に対処する。新設の実験設定 `chat.advanced.inlineEdits.xtabProvider.diffPatch.splitOnDiff`（既定 off）を有効化すると、各パッチを行 diff して最小限の `LineReplacement` に分割し、より局所的な提案にする。同期版 `DefaultLinesDiffComputer` を使い `maxComputationTimeMs: 100` で打ち切り、タイムアウト時は従来挙動にフォールバック。

**直近のアクティビティ:** aeschli が APPROVED。

**変更ファイル:** `xtabPatchResponseHandler.ts`（+71/-6）やテスト含む計 6 ファイル（+179/-9）。

**次のアクション:** 承認済み、マージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Let extensions add custom options to the main "Session Target" dropdown menu](https://github.com/microsoft/vscode/issues/321519) `#321519`
**ステータス:** Open | **ラベル:** triage-needed, stale

**概要:** Agents Window の Session Target ドロップダウン（Local/Cloud/Copilot CLI）に拡張機能から独自オプションを追加できるようにする要望。現状カスタムローカルエージェントや独立 CLI ハーネスは `.agent.md` のサブツールとして登録するしかなく、Copilot モデルがプロンプト解析のため先に起動する「Double Model Invocation」によるレイテンシ・トークン浪費・システムプロンプト干渉が問題だとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ待ち（stale ラベル付き）。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat renders `./...` inside inline code as a parent-folder inline reference](https://github.com/microsoft/vscode/issues/322508) `#322508`
**ステータス:** Open | **ラベル:** triage-needed, new release

**概要:** Copilot Chat の応答レンダリングで、インラインコード内の `./...`（例: `go test ./...`）が親フォルダへのインライン参照として描画されてしまう不具合。Windows 11 / VS Code 1.125.1、UnifyChatProvider 経由の GPT-5.5 で再現。親フォルダ配下にネストしたワークスペースで発生しやすい。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix `handleEndOfLifetime` `supersededBy` tracking for inline completions](https://github.com/microsoft/vscode/pull/320143) `#320143`
**ステータス:** Open | **ラベル:** なし

**概要:** インライン補完が新しい候補で置き換えられた際、拡張ホストの `handleEndOfLifetime` コールバックに渡る `reason.supersededBy` が誤って古い項目自身を指す不具合を修正。`IdentifiableInlineCompletion` が `idx` のみで `pid` を持たず、`mapReason` が古いリストの `completions.pid` にフォールバックしていたのが原因。DTO に `pid: number` を追加して解決する。

**直近のアクティビティ:** hediet が "Thanks!" と APPROVED、dmitrivMS も APPROVED。copilot bot もレビュー済み。

**変更ファイル:** `mainThreadLanguageFeatures.ts` / `extHost.protocol.ts` / `extHostLanguageFeatures.ts` の 3 ファイル（+3/-1）。

**次のアクション:** 承認済み、マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Renamed "Apply Update..." command to "Apply Update from File...".](https://github.com/microsoft/vscode/pull/322504) `#322504`
**ステータス:** Merged | **ラベル:** なし

**概要:** Windows 向け開発者コマンドの名称を「Apply Update...」から「Apply Update from File...」に変更し、ディスク上のインストーラ選択が必要であることを明確にする。#264648 の対応。

**直近のアクティビティ:** dmitrivMS と justschen が APPROVED。copilot bot もレビュー。

**変更ファイル:** `update.contribution.ts` の 1 ファイル（+2/-2）のみ。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [sessions: make Sessions Part the high-priority layout view](https://github.com/microsoft/vscode/pull/322513) `#322513`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents Window の Sessions Part を `LayoutPriority.High` に設定し、横方向レイアウトの余剰幅を吸収する唯一の柔軟ビューにする。`proportionalLayout: false` のグリッドで横方向チェーンに High が存在せず、Low の Auxiliary Bar が右側全体を Low に引き下げてセッションリスト（サイドバー）がウィンドウ幅の半分近くまで広がる回帰を修正。`LAYOUT.md` にも優先度モデルを文書化。

**直近のアクティビティ:** ulugbekna が APPROVED、copilot bot がレビューコメント。vs-code-engineering[bot]が @benibenj への CODENOTIFY を投稿。

**変更ファイル:** `LAYOUT.md`（+15）と `sessionsPart.ts`（+1/-1）の 2 ファイル。

**次のアクション:** 承認済み、マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [:lipstick: correct command capitalization in /causedByExtension response](https://github.com/microsoft/vscode/pull/298925) `#298925`
**ステータス:** Open | **ラベル:** なし

**概要:** トリアージコマンド `/causedByExtension` が生成するコメント内の `Help: Start extension bisect` を `Help: Start Extension Bisect` に修正する軽微な表記統一。

**直近のアクティビティ:** sandy081 が「コマンド名に合わせた軽微な修正、LGTM」と APPROVED、dmitrivMS も APPROVED。

**変更ファイル:** `.github/commands.json` の 1 ファイル（+1/-1）。

**次のアクション:** 承認済み、マージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Clarify how to use `Developer: Apply Update`](https://github.com/microsoft/vscode/issues/264648) `#264648`
**ステータス:** Closed | **ラベル:** install-update, polish, windows, unreleased

**概要:** `Developer: Apply Update` コマンドが、新バージョンの .exe をダウンロードして選択する必要があることがタイトルから分かりにくいという指摘。`Developer: Update VS Code from file...` のような名称が望ましいと提案していた。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #322504 のマージにより解決）。

**次のアクション:** 解決済み・クローズ。

---

### ![PR](icons/git-pull-request.svg) [agentHost: cancel pending model-refresh retry on shutdown](https://github.com/microsoft/vscode/pull/322498) `#322498`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot エージェントの model-refresh リトライをティアダウン中に安全にする修正。`_refreshModels` の指数バックオフリトライタイマーが `super.dispose()` でしか解除されず、`AgentService.shutdown()` 経路では到達しないため、ティアダウン後にタイマーが発火して Copilot CLI サブプロセスを再生成しうる問題を解決する。`shutdown()` 冒頭でリトライをキャンセルし、`_refreshModels` を `_shutdownPromise` でガードする。

**直近のアクティビティ:** dmitrivMS が APPROVED、copilot bot もレビュー。

**変更ファイル:** `copilotAgent.ts`（+60/-3）とテスト（+120）の 2 ファイル。

**次のアクション:** 承認済み、マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Phase 16: editable Claude customization resolution via disk scan](https://github.com/microsoft/vscode/pull/322501) `#322501`
**ステータス:** Open | **ラベル:** なし

**概要:** Claude エージェントホストセッションのカスタマイズ（agents/skills/slash commands/rules/MCP servers）を、SDK ペイロードからスタブ生成するのではなくファイルシステムを走査して実体の編集可能な `file:` URI で解決する Draft PR（Phase 16）。`~/.claude/**` と `<cwd>/.claude/**` を走査するディスクスキャンリゾルバを追加し、発見した agent/skill/command を開くとユーザー実体ファイルが開くようにする。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント。

**変更ファイル:** 大規模（計 23 ファイル、+2489/-635）。`claudeSessionCustomizationDiscovery.ts`（+391）など新規スキャン群を追加し、旧 `claudeSdkCustomizationBundler.ts` などを削除。多数のテストも追加。

**次のアクション:** Draft のため作業継続中。

---

### ![PR](icons/git-pull-request.svg) [chat: add full path + branch tooltip to agent-host folder picker chip](https://github.com/microsoft/vscode/pull/322502) `#322502`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Chat の副入力ツールバーにあるエージェントホスト Folder ピッカーチップ（`AgentHostFolderPickerActionItem`）にホバーを追加し、フォルダのフルパスと現在の git ブランチ（SCM リポジトリにマップされる場合）を表示する Draft PR。#322139 の対応で、セッション開始後に無効化されたチップがフォルダ名のみでツールチップなしの状態を解消する。`ISCMService` と `IHoverService` を注入。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント。

**変更ファイル:** `agentHostFolderPickerActionItem.ts` の 1 ファイル（+68/-0）。

**次のアクション:** Draft のため作業継続中。

---

### ![PR](icons/git-pull-request.svg) [Wire up universal tool_instructions for agent host prompts](https://github.com/microsoft/vscode/pull/322507) `#322507`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot CLI エージェントホストに、モデル非依存の `tool_instructions` システムプロンプトガイダンスの配線を追加する。セッション内にクライアントツールが存在するかでゲートされ、具体的なツールごとの命令行はまだ登録せず後続 PR で対応する。起動時に解決したシステムメッセージをログ出力（要約は info、全文は trace）。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]が概要と指摘 1 件のレビューを投稿。

**変更ファイル:** `toolInstructions.ts`（+121 新規）/ `promptRegistry.ts`（+65）/ `copilotSessionLauncher.ts` ほか計 7 ファイル（+363/-9）。

**次のアクション:** レビュー対応・マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Validate predicted line for cross-file cursor jumps in NES](https://github.com/microsoft/vscode/pull/322437) `#322437`
**ステータス:** Merged | **ラベル:** なし

**概要:** エラーテレメトリで `CurrentDocument#lineWithCursor: cursor is out of bounds`（`BugIndicatingError`）が 56 倍に急増した偽陽性バグを修正。NES のクロスファイルカーソルジャンプ経路（`XtabProvider.handleCrossFilePrediction`）がモデルの予測行番号を対象ドキュメントの行数と照合せずに使っていたのが原因で、同一ファイル経路に既にあるガードをクロスファイル経路にも適用する。

**直近のアクティビティ:** aeschli が APPROVED、copilot bot もレビュー。

**変更ファイル:** `xtabProvider.ts`（+9/-1）とテスト（+55）の 2 ファイル。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [[Regression] [Error] [GitHub.copilot-chat] unhandlederror-CurrentDocument#lineWithCursor: cursor is out of bounds](https://github.com/microsoft/vscode/issues/318579) `#318579`
**ステータス:** Closed | **ラベル:** bug, error-telemetry, unreleased, recent-regression, extension, ai-generated, ai-analyzed

**概要:** `cursor: 337, doc line count: 332` のようにカーソルが行数を超えて `BugIndicatingError` がグローバル未処理エラーテレメトリに上がる回帰。スタックは `handleCrossFilePrediction` → `lineWithCursor` 経由で、クロスファイルカーソルジャンプのロールアウトと相関する。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #322437 で修正・クローズ）。

**次のアクション:** 解決済み・クローズ。

---

### ![Issue](icons/issue-opened.svg) [Scroll wheel click will not attempt to close a tab](https://github.com/microsoft/vscode/issues/322419) `#322419`
**ステータス:** Open | **ラベル:** feature-request, agents-window

**概要:** Agents Window のタブをマウスホイールクリック（中クリック）で閉じられない。ブラウザでは標準的な挙動であり同様に動くことを期待していた、という #322285 のテスト中に見つかった要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [New chat issue title changes twice](https://github.com/microsoft/vscode/issues/322416) `#322416`
**ステータス:** Open | **ラベル:** agents-window

**概要:** 新規チャットのタイトルが 2 回変わり、1 回目の方がユーザー入力に忠実だが 2 回目はエージェントの意図寄りに変わる、という #322285 テスト中の報告。動的に変わると思ったが 2 回で止まり、意図的な挙動か疑問を呈している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 仕様確認・トリアージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Allow disabling AuthenticationProvider extension for workspace if not used by Settings Sync](https://github.com/microsoft/vscode/pull/320415) `#320415`
**ステータス:** Open | **ラベル:** なし

**概要:** #269367 の修正。Settings Sync が認証プロバイダー拡張に依存していない場合に、ワークスペース単位でその拡張を無効化できるよう、拡張有効化の検証とメッセージングを調整する。

**直近のアクティビティ:** sandy081 が「1.126 エンドゲーム進行中のため、適切なレビューとベイク時間を確保すべく 1.127.0 に割り当てた」とコメント。gjsjohnmurray は `/assign @sandy081` を含む複数コメント。

**変更ファイル:** `extensionEnablementService.ts` の 1 ファイル（+11/-6）。

**次のアクション:** 1.127.0 でのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix: terminal progress display overflow](https://github.com/microsoft/vscode/pull/322041) `#322041`
**ステータス:** Open | **ラベル:** なし

**概要:** ターミナルパネルを右側に移動した際、`single-terminal-tab` アクションアイテム（アイコン+タイトル+進捗）がパネルタイトルバーに描画され、`justify-content: flex-end` と `flex: 0 1 auto` により幅広ラベルが左方向にオーバーフローしてコンポジットバー上に飛び出す問題を修正。`terminal.css` を調整する。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューなし）。

**変更ファイル:** 計 8 ファイル（+61/-335）。`terminal.css`（+22）と `terminalInstance.ts` のほか、AGENTS.md 系ドキュメントの大量削除（`AGENT_HOST_SESSIONS_PROVIDER.md` -186 など）を含む点に注意。

**次のアクション:** レビュアー割り当て・レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Remove distro PAT](https://github.com/microsoft/vscode/pull/322052) `#322052`
**ステータス:** Merged | **ラベル:** なし

**概要:** PAT が 7 日ごとに強制ローテートされる方針に伴い、長寿命の `github-distro-mixin-password` PAT を製品ビルド全体から完全に削除する。private GitHub アクセスは Monaco GitHub App のインストールトークン、プリビルドバイナリは Azure Artifacts universal feeds、レート制限対象の公開ダウンロードは `github-token-code-oss` トークンに置き換える。残る参照はテストフィクスチャのみ。

**直近のアクティビティ:** copilot bot が「Not ready to approve」と非 main ブランチでの distro チェック失敗/スキップを懸念。joaomoreno が返信し、dbaeumer が APPROVED。

**変更ファイル:** 大規模（計 44 ファイル、+898/-647）。Azure Pipelines 系 yml 多数、`gulpfile.reh.ts`、新規 `build/lib/azureFeed.ts`、`package-lock.json`（+275/-392）など。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Co-Pilot View: Can we have a "Don't show again" for the "Credit limit reached"?](https://github.com/microsoft/vscode/issues/322488) `#322488`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot ビューの "Credit limit reached" 通知を恒久的に非表示にする「Don't show again」を求める要望。閉じても繰り返し表示されるため通知自体を止めたいとしている。

**直近のアクティビティ:** gjsjohnmurray が「#321949 で解決するかもしれない」とコメント。

**次のアクション:** #321949 の動向と合わせてトリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Ability to disable "Credit Limit Reached" banner](https://github.com/microsoft/vscode/issues/321278) `#321278`
**ステータス:** Open | **ラベル:** なし

**概要:** 追加利用予算を持つユーザーに対し、エージェント入力ボックスの "Credit Limit Reached" バナーが 1 日に何度も表示される。別の上限（"Budget Limit Reached"）に達するまでは一度きりの表示にするか、無効化手段が欲しいという要望。

**直近のアクティビティ:** gjsjohnmurray が「#321949 で解決するかもしれない」とコメント。

**次のアクション:** #321949 の動向と合わせてトリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Support Github Enterprise with Copilot CLI](https://github.com/microsoft/vscode/issues/316511) `#316511`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 1.120.0 以降、GHE.com アカウントでサインインして Agents Window から Copilot CLI を選びプロンプトを送る際に動作しない不具合。GitHub Enterprise 環境での Copilot CLI サポートを求めている。

**直近のアクティビティ:** EricKY26 が同一問題を報告し、公式ドキュメントの導線から `agents-window` ラベルを付けるべきと提案。

**次のアクション:** ラベル付け・トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Spinners do not reflect the state of peer chats](https://github.com/microsoft/vscode/issues/322418) `#322418`
**ステータス:** Open | **ラベル:** bug, agents-window

**概要:** Sessions ビューで、複数タブのチャットがあっても最初のタブからのみスピナー/作業中状態が表示され、タブ自体のスピナーも最初のタブにしか出ない、という #322285 テスト中のバグ報告。動画添付あり。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正対応・トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Gemma models return reasoning in `<thought>` tags – UI does not format/hide them](https://github.com/microsoft/vscode/issues/321918) `#321918`
**ステータス:** Open | **ラベル:** なし

**概要:** Google AI API 経由の Gemma モデルが推論を `<thought>...</thought>` タグで返すが、拡張は `<think>`/`<thinking>` のみを処理対象とするため `<thought>` の中身が生テキストとして表示されチャット UI が乱れる。`<thought>` も同様に非表示または折りたたみ表示にすべきという要望。

**直近のアクティビティ:** ltomuta が同一セッション内で `<thought>` が解析されたりされなかったりすると報告し、スクリーンショットを添付。

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Move modelConfiguration to be part of selectedModel](https://github.com/microsoft/vscode/issues/322512) `#322512`
**ステータス:** Open | **ラベル:** debt

**概要:** `ISerializableChatModelInputState` の `modelConfiguration` プロパティを `selectedModel` の中に移動するリファクタ提案。モデルに関する情報はすべて `selectedModel` に含めるべきで、両者が片方だけ値を持つ不整合状態を避けたいとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 技術的負債としての対応検討待ち。

---

### ![Issue](icons/issue-opened.svg) [Model Reasoning is incorrect in new Sessions in Agent Window](https://github.com/microsoft/vscode/issues/322511) `#322511`
**ステータス:** Open | **ラベル:** bug, agents-window

**概要:** Agent Window で GPT 5.5 Max 1M を選択してプロンプトを送りセッション開始後、チャット入力のモデル構成が GPT 5.5 Medium 1M など誤った値になる不具合。#321405 と類似。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正対応・トリアージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix OS_ID parsing from /etc/os-release](https://github.com/microsoft/vscode/pull/321293) `#321293`
**ステータス:** Open | **ラベル:** なし

**概要:** #232159 の修正。`check-requirements-linux.sh` が `/etc/os-release` の `ID="rocky"` のようにクオート付き値を正しく解析できず Rocky 8.5 などで devcontainer セットアップが失敗する問題を、POSIX 標準の `.`（source）演算子で読み込むように修正する。

**直近のアクティビティ:** copilot bot が「レビュー対象ファイルなし」とコメント。

**変更ファイル:** `check-requirements-linux.sh` の 1 ファイル（+1/-1）。

**次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/322510) `#322510`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** Copilot 補完フィードバックの自動生成 Issue。補完テキスト `function createFarewell(name) {` のみが記載され、具体的な問題や再現手順の記述がない。Windows / VS Code 1.125.1、Copilot 拡張 0.53.1。

**直近のアクティビティ:** vs-code-engineering[bot]が情報不足のためガイドライン参照を促す定型コメントを投稿。

**次のアクション:** 報告者による情報補足待ち。

---

### ![Issue](icons/issue-opened.svg) [Toggle Terminal Panel Shortcut disappear/ Does not Work](https://github.com/microsoft/vscode/issues/322350) `#322350`
**ステータス:** Open | **ラベル:** new release

**概要:** 1.125.1 へのアップデート後、ターミナルの開閉に使っていた `Ctrl + `` ` ` ショートカットが設定から消え、デフォルトにリセットしても何も設定されない、という Linux 環境での不具合報告。

**直近のアクティビティ:** 報告者 Arthur-Wagner-GH が翌朝、設定変更やアップデートなしに自然に直ったと追記しつつ、バグであったことは確かと述べている。

**次のアクション:** 再現性が低く、追加調査・トリアージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix PHP highlighting for comment-like strings](https://github.com/microsoft/vscode/pull/317058) `#317058`
**ステータス:** Open | **ラベル:** なし

**概要:** #204065 の修正。PHP 文法を更新し、`/*` で始まるクオート文字列がクオート付き正規表現として扱われないようにする。正規表現向けのクオート文字列ルールがコメント風の開始をスキップしつつ、有効なクオート正規表現のハイライトは維持する。カラー化フィクスチャも追加。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント。

**変更ファイル:** 計 5 ファイル（+1526/-3）。大半は生成されたカラー化結果 `issue-204065_php.json`（+1490）で、実質変更は `php.tmLanguage.json`（+3/-3）。

**次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Add a "+" new file button on the Tab bar](https://github.com/microsoft/vscode/issues/322509) `#322509`
**ステータス:** Open | **ラベル:** なし

**概要:** タブバー上部に新規ファイル作成用の「+」ボタンを追加する要望。サイドバーを開く手間を減らし、分割表示でも作業しやすくなるとし、Windows 11 にも同様の機能があると例示している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) ["Delegate to cloud agent" uses wrong branch](https://github.com/microsoft/vscode/issues/303677) `#303677`
**ステータス:** Open | **ラベル:** なし

**概要:** デフォルト以外のブランチで作業中にクラウドエージェントへ委譲すると、クラウド側が main ブランチで作業してしまい現在のブランチの内容を認識しない不具合。シームレスな継続が期待値。

**直近のアクティビティ:** andrerom が再発を報告し、Copilot に `main`（既定 `dev`）を使うよう指示しても効かないと述べ、並列 PR 作業のためにも早期解決を希望している。

**次のアクション:** トリアージ・修正対応待ち。

---

### ![Issue](icons/issue-opened.svg) [Can't turn on Chat Session Sync (`chat.sessionSync.enabled`)](https://github.com/microsoft/vscode/issues/320335) `#320335`
**ステータス:** Open | **ラベル:** なし

**概要:** `chat.sessionSync.enabled` 設定がチェックボックス未チェックなのに「Enabled」と表示され、チェックボックスが無効化されてクリックできず、組織ポリシーがないのに「Managed by organization」通知が出る不具合。実際にはセッション同期が有効になっていない。

**直近のアクティビティ:** TobiasDraven が DeepSeekv4 利用・グループポリシーなしの Windows 11 / VS Code 1.125 で同一問題を報告。

**次のアクション:** トリアージ・修正対応待ち。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Revert forceFistExecutionInSandbox](https://github.com/microsoft/vscode/pull/322482) `#322482`
**ステータス:** Closed | **ラベル:** cherry-pick-artifact

**概要:** `release/1.126` からの #322477 のチェリーピック（自動生成）。`forceFirstExecutionInSandbox` 関連の変更をリバートするもの。

**直近のアクティビティ:** vs-code-engineering[bot]が @anthonykim1 への CODENOTIFY を投稿。copilot bot はレビューエラー、DonJayamanne が APPROVED。最終的にクローズ。

**変更ファイル:** sandbox / chatAgentTools 系の計 12 ファイル（+20/-274）。`runInTerminalTool.test.ts`（-138）など主に削除。

**次のアクション:** クローズ済み（別経路で対応）。

---

### ![Issue](icons/issue-opened.svg) [Add Support for PDF File Parsing in GitHub Copilot Context Addition](https://github.com/microsoft/vscode/issues/265968) `#265968`
**ステータス:** Open | **ラベル:** upstream, chat

**概要:** GitHub Copilot がコンテキスト追加時に PDF ファイルを扱えず、仕様書や論文などの内容にアクセスできない制約に対し、PDF からのテキスト自動抽出（ネイティブ PDF 解析）のサポートを求める要望。

**直近のアクティビティ:** cveld が、ビジョン機能がプレビューで管理者にブロックされている場合に Python スクリプトへ切り替えるなど、Copilot 自身が能力を理解してメタ的に応答できると良いとコメント。

**次のアクション:** upstream 対応待ち。

---

### ![Issue](icons/issue-opened.svg) [1.126.0 Endgame](https://github.com/microsoft/vscode/issues/321852) `#321852`
**ステータス:** Open | **ラベル:** endgame-plan

**概要:** 1.126.0 リリースに向けたエンドゲーム計画 Issue。チューリッヒ時間月曜朝までのテスト計画項目作成、マイルストーンの整理、`bug`/`verification-needed` への検証手順追加など、チームの作業スケジュールを定めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** エンドゲーム手順に沿った検証・クローズ作業の進行。

---

### ![Issue](icons/issue-opened.svg) [Meta: Sorry, no response was returned](https://github.com/microsoft/vscode/issues/253126) `#253126`
**ステータス:** Open | **ラベル:** bug, capi, claude

**概要:** Agent モード（Claude 3.5）で複数アクションを依頼すると、一部完了後にランダムなタイミングで「Sorry, no response was returned.」エラーが返る不具合のメタ Issue。

**直近のアクティビティ:** sanny-io がローカル ollama の gemma4:e4b でも Agent モードで同様に発生すると報告し、`shouldContinue=false` のログを添付。

**次のアクション:** 調査継続。

---

### ![Issue](icons/issue-opened.svg) [Ctrl+Shift+I always activates GitHub Copilot tab instead of last active chat extension (no Copilot license)](https://github.com/microsoft/vscode/issues/321885) `#321885`
**ステータス:** Closed | **ラベル:** なし

**概要:** `Ctrl+Shift+I`（Open Chat (Agent)）が、Copilot ライセンスがなく別のチャット拡張（例: Claude Code）が直前にアクティブでも常に GitHub Copilot Chat タブを開いてしまい、Claude Code タブをクリックすると Copilot タブが消えてちらつく不具合。

**直近のアクティビティ:** 報告者 cveld が roblourens の指摘どおり、`ctrl+shift+i` を `claude-vscode.sidebar.open` にバインドするキーバインド設定で解決したと報告しクローズ。

**次のアクション:** ワークアラウンドにより解決・クローズ。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Allow configuring Copilot agent/skill/instruction discovery paths via settings](https://github.com/microsoft/vscode/issues/322506) `#322506`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot エージェントファイル探索のうち、workbench 側（`.agent.md`）は `chat.agent.fileLocations` で設定可能だが、エージェントホスト側（`sessionCustomizationDiscovery.ts` の SDK セッション探索）はパスがハードコードされており変更できない。モノレポや独自構成のチームが探索先を指定できるよう、設定での構成可能化を求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [使用过程出现整个界面"黑屏"](https://github.com/microsoft/vscode/issues/322493) `#322493`
**ステータス:** Open | **ラベル:** ai-translated, new release

**概要:** VS Code を開いて他プロセス/アプリに切り替えると画面が「黒画面」になり、最大化/最小化では復帰するが内部領域クリックでは復帰しない不具合（状況 1）。使用中にも偶発的に同様の黒画面が発生する（状況 2）。Windows 11 / Intel UHD Graphics 620 環境。

**直近のアクティビティ:** qkhorizon・Wangkiwa・stevenkang2020-hue らが同様の症状（特にウィンドウ切替時）を報告。vs-code-engineering[bot]が英訳を投稿。

**次のアクション:** GPU 関連としてトリアージ・調査待ち。

---

### ![Issue](icons/issue-opened.svg) [Keep system awake during agent actions](https://github.com/microsoft/vscode/issues/276546) `#276546`
**ステータス:** Open | **ラベル:** feature-request, chat

**概要:** Copilot Chat のエージェントが長時間処理中にシステムがスリープしてネットワークが切れ、セッションが中断される問題に対し、アクティブなエージェント実行中はホストのスリープを一時的に抑止する機能の要望。

**直近のアクティビティ:** dunxuan が同機能を必要とし、長いセッション中の途中スリープが困ると報告。

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/322503) `#322503`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** Copilot 補完フィードバックの自動生成 Issue。JavaScript の画像スライドショーらしき補完テキストのみで、問題内容や再現手順の記述がない。Windows / VS Code 1.125.1、Copilot 拡張 0.53.1。

**直近のアクティビティ:** vs-code-engineering[bot]が情報不足の定型コメントを投稿。

**次のアクション:** 報告者による情報補足待ち。

---

### ![Issue](icons/issue-opened.svg) [VS Code does not work properly with multiple monitors](https://github.com/microsoft/vscode/issues/304693) `#304693`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code ウィンドウを複数モニターにまたがって表示すると、追加モニター側の内容が更新されない不具合。メインモニターのスクロールに追従しない、検索ダイアログが第 2 モニターでは更新されない（選択は変わるのにテキスト表示が数秒遅れる）などの症状。

**直近のアクティビティ:** eraserewind123 が Windows 11 で同様、とくに横幅が必要な diff ウィンドウで発生すると報告。

**次のアクション:** トリアージ・調査待ち。

---

### ![Issue](icons/issue-opened.svg) [Keyboard shortcut for Custom Editor not working on VS Code for the web](https://github.com/microsoft/vscode/issues/322505) `#322505`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code for the Web で、Shift キーを含むカスタムエディタ向けショートカット（例: `markdown.togglePreview` の Shift+Cmd/Ctrl+V）が機能しない。コマンドパレットからは動作する。Web 版では Shift キー入力が Alt キーのように誤って解釈されツールバーアイコンが切り替わる挙動も確認されている。Safari/macOS・Edge/Windows 双方で再現。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ・調査待ち。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/322500) `#322500`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** Copilot 補完フィードバックの自動生成 Issue。HTML の `link rel="stylesheet"` 補完テキストのみで、問題の説明や再現手順がない。Windows / VS Code 1.125.1、Copilot 拡張 0.53.1。

**直近のアクティビティ:** vs-code-engineering[bot]が情報不足の定型コメントを投稿。

**次のアクション:** 報告者による情報補足待ち。

---

### ![Issue](icons/issue-opened.svg) [Custom file save dialog keyboard shortcuts inconsistent with macOS](https://github.com/microsoft/vscode/issues/119913) `#119913`
**ステータス:** Open | **ラベル:** bug, under-discussion, dialogs

**概要:** untitled ファイルを `cmd+w` で閉じる際、macOS ネイティブダイアログでは Enter=保存・Space=破棄だが、カスタムダイアログでは Enter も Space も両方保存になってしまう不整合。

**直近のアクティビティ:** icemac が `Don't save` ボタンへフォーカスが戻る挙動を取り戻したいと要望し、以前はそうだったがバージョンアップでキー操作が増えたとして opt-in オプションを希望している。

**次のアクション:** 議論継続（under-discussion）。

---

### ![Issue](icons/issue-opened.svg) [Git - Implement Git Interactive Rebase Editor](https://github.com/microsoft/vscode/issues/260637) `#260637`
**ステータス:** Open | **ラベル:** feature-request, scm

**概要:** GitLens が持つインタラクティブ rebase エディタを VS Code 本体の Git 機能としても追加してほしいという要望。GitLens の中でこの機能だけ使いたい（他機能はオフにしたい）という需要を背景にしている。

**直近のアクティビティ:** MurzNN が、実装されるまでの回避策として GitLens の他機能を `settings.json` で無効化する設定を共有。

**次のアクション:** トリアージ・実装検討待ち。

---

### ![Issue](icons/issue-opened.svg) [Add tooltip to workspace folder name in Copilot Chat](https://github.com/microsoft/vscode/issues/322139) `#322139`
**ステータス:** Open | **ラベル:** info-needed, agent-host

**概要:** Copilot Chat で無効化されたワークスペースフォルダ名にツールチップがなく、何を表すか分からないという指摘。PR #322502 がこの対応にあたる。

**直近のアクティビティ:** DonJayamanne が最新版で再現できないと確認を求め、vritant24 が複数フォルダのワークスペースでエージェントホストセッション中に再現すると VS Code 1.126.0-insider のバージョン情報付きで回答。

**次のアクション:** 再現確認のうえ #322502 で対応中。

---

### ![Issue](icons/issue-opened.svg) [Bearer token authentication for custom language model endpoints](https://github.com/microsoft/vscode/issues/322499) `#322499`
**ステータス:** Open | **ラベル:** なし

**概要:**「Bring your own language model key」機能で、カスタムエンドポイントが組み込み Azure プロバイダーと同様の柔軟さで Bearer トークン認証できない。リクエストごとに動的生成した Bearer トークンを `Authorization: Bearer <token>` ヘッダで送る仕組みを求める要望で、組織管理の AI ゲートウェイ経由の OpenAI 互換モデル認証を想定している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ・機能検討待ち。

---

### ![PR](icons/git-pull-request.svg) [AH: use plan widget instead of questions carousel when exiting plan mode](https://github.com/microsoft/vscode/pull/322495) `#322495`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host の「plan モード終了」フローで、質問カルーセルの代わりに既存のチャット plan review ウィジェットを使い、実装に進む前にユーザーがプランを確認できるようにする Draft PR。スクリーンショット付き。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント。

**変更ファイル:** `agentHostSessionHandler.ts`（+184）/ `agentHostPlanReview.ts`（+27 新規）/ `copilotAgentSession.ts` ほか計 6 ファイル（+463/-23）。テストも追加。

**次のアクション:** Draft のため作業継続中。

---

### ![PR](icons/git-pull-request.svg) [chat: allow chat.pluginLocations in workspace settings](https://github.com/microsoft/vscode/pull/315123) `#315123`
**ステータス:** Open | **ラベル:** なし

**概要:** `chat.pluginLocations` のスコープを `ConfigurationScope.MACHINE` から `WINDOW` に変更し、ワークスペース設定（`.vscode/settings.json`）で指定可能にする。`restricted: true` は維持しワークスペーストラストで引き続きゲート。類似の customization 系設定（hookFilesLocations 等）がすべて WINDOW スコープなのに対しこの設定だけ MACHINE だった不整合を解消する。

**直近のアクティビティ:** saar-e-l が「これも必要」とコメント。著者 nalmiron もレビューコメント。copilot bot もレビュー。

**変更ファイル:** `chat.shared.contribution.ts` の 1 ファイル（+6/-2）。

**次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Test Plan: Run sandboxed before requesting terminal access elevation](https://github.com/microsoft/vscode/issues/322403) `#322403`
**ステータス:** Open | **ラベル:** testplan-item, blocked

**概要:** プレビュー設定 `chat.agent.sandbox.forceFirstExecutionInSandbox`（既定無効）のテスト計画項目。有効時、昇格アクセスを要求するフォアグラウンド `run_in_terminal` をまず既存サンドボックス制限で実行し、非ゼロ終了コードで失敗した場合にのみ昇格リトライの承認をユーザーに求める挙動を検証する。

**直近のアクティビティ:** jruales が「dileepyavan が変更をリバートし次イテレーションで再投入する」と報告。TylerLeonhardt は当該設定で 2 回目のサンドボックス確認が出ず最初と同様に失敗するため、この TPI は実質ブロック状態だとコメント。

**次のアクション:** 機能リバート（#322476/#322482）により次イテレーションへ持ち越し（blocked）。

---

### ![Issue](icons/issue-opened.svg) [Request models.list failed with 429 "too many requests" when refreshing models in local agent host](https://github.com/microsoft/vscode/issues/322429) `#322429`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** ローカルエージェントホストでのモデルリフレッシュ時に `models.list` が 429「too many requests」で失敗し、モデルセレクタが消える不具合。リフレッシュは成功するか graceful にバックオフ再試行し、セレクタは維持されるべきとしている。ログには Claude 系モデル 7 件取得後に 429 エラーが続く様子が記録されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** バックオフ/リトライ対応・トリアージ待ち（関連 PR として #322498）。

---

### ![Issue](icons/issue-opened.svg) [Integrated terminal automatically sends 'Y' when terminating node batch script](https://github.com/microsoft/vscode/issues/142341) `#142341`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, terminal-conpty, unreleased, terminal-input

**概要:** Windows で `concurrently` 実行中に `Ctrl+C` を押すと、`Terminate batch job (Y/N)?` に対し打鍵していない `Y` が自動送信され、最初のスクリプトのみ終了して残りの出力がプロンプトに混入する不具合。Windows Terminal では発生しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [VSCode freezes when opening a terminal](https://github.com/microsoft/vscode/issues/239838) `#239838`
**ステータス:** Closed | **ラベル:** important, freeze-slow-crash-leak, candidate, terminal, gpu, unreleased

**概要:** 全拡張を無効にしてもターミナルを開くと VS Code がフリーズする不具合（1.97.0、Electron 32 系）。GPU 関連が疑われている。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [Native context menu hijacks custom one in webview editor](https://github.com/microsoft/vscode/issues/239862) `#239862`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, electron, confirmed, regression, unreleased

**概要:** webview エディタ（例: Git Graph）で右クリックすると、カスタムコンテキストメニューではなくネイティブのテキスト操作メニューが表示される回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) ["Open with Code" old context menu option missing from Explorer on version 1.103.0](https://github.com/microsoft/vscode/issues/260389) `#260389`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, regression, unreleased

**概要:** 1.103.0 でインストール時に「Open with Code」を有効化しても、Windows エクスプローラのフォルダ/空白領域の右クリックメニューに当該オプションが出ない回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [Chat session picker: No picker in restored chat editor](https://github.com/microsoft/vscode/issues/275289) `#275289`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased

**概要:** CCA チャットエディタを開いて VS Code をリロードするとエディタは復元されるがエージェントピッカーが表示されない不具合。新規チャットエディタを作ると新規追加したカスタムエージェントを含むピッカーが出る、という #274938 テスト中の報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [Telemetry event `inline.done` isn't send for v2 inline chat sessions](https://github.com/microsoft/vscode/issues/277422) `#277422`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased

**概要:** インラインチャット v1 と v2 の性能比較に使う `inline.done` テレメトリイベントが v2 では送信されない不具合。コア側と拡張側双方のコード変更が必要とされていた。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [InlineChatController2 creates widget eagerly](https://github.com/microsoft/vscode/issues/283017) `#283017`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased, inline-chat

**概要:** `InlineChatController2` がウィジェットを先行（eager）生成しており、わずかな性能低下に加え、実験的な `inlineChat.notebookAgent` ではウィジェット生成時にモデル未設定で正しい location 情報を渡せない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [Git Source Control Graph tree doesn't expand for some entries](https://github.com/microsoft/vscode/issues/286776) `#286776`
**ステータス:** Closed | **ラベル:** bug, candidate, scm, unreleased

**概要:** Git Source Control Graph のツリーで、コミットファイルが多い場合などに一部エントリが展開されない不具合（1.108、Windows 10）。

**直近のアクティビティ:** 直近 24 時間のコメントなし（修正済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [Backport fix for CVE-2026-2441 from chromium](https://github.com/microsoft/vscode/issues/295264) `#295264`
**ステータス:** Closed | **ラベル:** bug, upstream, verified, candidate, chromium, unreleased

**概要:** Chromium の CVE-2026-2441 修正を Electron 経由でバックポートするセキュリティ対応 Issue。該当の electron コミットを参照している。

**直近のアクティビティ:** 直近 24 時間のコメントなし（対応済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [autopilot mode should allow curl in terminal](https://github.com/microsoft/vscode/issues/303174) `#303174`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** `chat.tools.global.autoApprove: true` の Autopilot モードでも `curl` の実行が失敗し、明確なエラーや対処法が示されないという不具合。YOLO モードでも `curl` がブロックされているのではと指摘していた。

**直近のアクティビティ:** 直近 24 時間のコメントなし（対応済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [Terminal commands should be auto approved when invoked from `ask_questions` tool](https://github.com/microsoft/vscode/issues/309589) `#309589`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** ユーザーが `ask_questions` ツールで回答を確定済みなのに、`Enter` 送信などについて再度確認プロンプトが出るのは不要、という不具合。

**直近のアクティビティ:** 直近 24 時間のコメントなし（対応済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![Issue](icons/issue-opened.svg) [`send_to_terminal` progress message repeats first question when accepting all defaults in interactive commands](https://github.com/microsoft/vscode/issues/309608) `#309608`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, terminal-chat-tools

**概要:** `npm init` のような対話コマンドで `askQuestions` カルーセルの全デフォルトを受け入れると、各 `send_to_terminal` の進捗メッセージが毎回最初の質問（"Package name?"）を表示してしまい、対応する質問が出ない不具合。

**直近のアクティビティ:** 直近 24 時間のコメントなし（対応済みとしてクローズ）。

**次のアクション:** 解決済み・クローズ（unreleased）。

---

### ![PR](icons/git-pull-request.svg) [Update customizations sidebar content and behavior](https://github.com/microsoft/vscode/pull/322497) `#322497`
**ステータス:** Open | **ラベル:** なし

**概要:** #321166 の修正。Agents Window の Customizations サイドバーのリスト項目をクリックしても対応するモーダルページが開かないバグに対処する。Checks セクションの UI 挙動に合わせ、Customizations セクションを折りたたみ式から可変サイズに変更し、Overview ページへ直接アクセスする「Overview」項目を追加、ラベルフォントサイズを 1px 縮小する。

**直近のアクティビティ:** github-actions[bot]がスクリーンショット差分レポート（14 件変更）を投稿。copilot bot もレビュー。

**変更ファイル:** `sessionsView.ts`（+89/-13）/ `customizationsToolbar.contribution.ts`（+78/-24）/ `aiCustomizationShortcutsWidget.ts` ほか計 7 ファイル（+308/-241）。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix: guard against missing default agent in chat sendRequest (fixes #321962)](https://github.com/microsoft/vscode/pull/321969) `#321969`
**ステータス:** Open | **ラベル:** errors-fix, *error-fix-driving

**概要:** `ChatService.sendRequest` が `getDefaultAgent(...)` の結果を非 null アサーション（`!`）で扱うため、location/mode に一致するアクティブな既定エージェントがない場合に `undefined` が `data.agent` に入り、下流の `repopulateAgentCommand` で `TypeError: Cannot read properties of undefined (reading 'metadata')` を引き起こす問題を修正。プロデューサ側でアサーションを除去し、既定エージェントがなければリクエストを reject する。#321962 と #321961 を修正。

**直近のアクティビティ:** copilot bot が複数回レビューエラー。roblourens と alexdima が APPROVED。

**変更ファイル:** `chatServiceImpl.ts` の 1 ファイル（+5/-1）。

**次のアクション:** 承認済み、マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Feat: Add copy button to fenced code blocks in Markdown preview](https://github.com/microsoft/vscode/pull/322274) `#322274`
**ステータス:** Open | **ラベル:** なし

**概要:** Markdown プレビューのフェンス付きコードブロックに、GitHub の Markdown 同様のコピー用ボタンを追加する。ボタンはホバー時に表示され、クリックで内容をクリップボードへコピー、アイコンが 2 秒間 `copy` から `check` に変わる。morphdom によるライブプレビュー更新後も再注入される。

**直近のアクティビティ:** 著者 hariharjeevan がレビュアー割り当てを依頼。copilot bot が 2 回レビュー（指摘 3 件）。

**変更ファイル:** `markdown.css`（+39）/ `preview-src/index.ts`（+58）/ `documentRenderer.ts` の 3 ファイル（+99/-0）。

**次のアクション:** レビュアー割り当て・レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix: suppress expected EPIPE error on graceful client disconnect](https://github.com/microsoft/vscode/pull/322398) `#322398`
**ステータス:** Open | **ラベル:** なし

**概要:** リモートクライアントの正常切断のたびにサーバーが `Unexpected SIGPIPE` を error ログに出す問題を修正。`PersistentProtocol.sendDisconnect()` がクライアント切断後にソケットへ flush し、Linux で `EPIPE` が `onUnexpectedError` 経由でログされていた。`flush()` を EPIPE を握りつぶし他は再 throw する catch で包む（`ProtocolWriter.dispose()` の既存パターンに合わせる）。#322341 を修正。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント。

**変更ファイル:** `ipc.net.ts` の 1 ファイル（+9/-1）。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix a simple typo](https://github.com/microsoft/vscode/pull/321203) `#321203`
**ステータス:** Open | **ラベル:** なし

**概要:** 単純なタイポ修正を意図した PR だが、差分が空（変更ファイル 0、+0/-0）になっている。

**直近のアクティビティ:** copilot bot がクオータ上限到達でレビュー不可のコメントののち、1 ファイル 1 件指摘のレビューを 2 回投稿している。

**変更ファイル:** 変更ファイルなし（0 ファイル）。

**次のアクション:** 差分が空のため、内容修正かクローズの確認が必要。

---

### ![PR](icons/git-pull-request.svg) [AH: fix thinking headers getting reloaded every single time](https://github.com/microsoft/vscode/pull/322489) `#322489`
**ステータス:** Merged | **ラベル:** なし

**概要:** エージェントホストセッションで「thinking」（推論）ブロックのヘッダーがリロードのたびに再生成される問題を修正。reasoning パートに安定 ID を付与し、既存の非ローカルタイトルキャッシュをその ID でキー付けする（従来はツールコールを持つブロックのみ機能）。

**直近のアクティビティ:** DonJayamanne が APPROVED、copilot bot もレビュー。

**変更ファイル:** `chatThinkingContentPart.ts`（+37/-26）/ `agentHostSessionHandler.ts` ほか計 5 ファイル（+102/-31）。テスト追加。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [add hover on permissions picker](https://github.com/microsoft/vscode/pull/322486) `#322486`
**ステータス:** Merged | **ラベル:** なし

**概要:** チャットの権限/承認ピッカー（workbench チャット UI と Sessions ウィンドウ双方）にホバー/ツールチップを追加し、プロバイダー固有のオーバーライドフックも含める。

**直近のアクティビティ:** DonJayamanne が APPROVED、copilot bot もレビュー。

**変更ファイル:** `agentHostChatInputPicker.ts`（+56/-9）/ `permissionPicker.ts`（+25/-2）ほか計 7 ファイル（+144/-20）。テスト追加。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix color picker copy button copying accessibility placeholder (#322335)](https://github.com/microsoft/vscode/pull/322484) `#322484`
**ステータス:** Open | **ラベル:** なし

**概要:** #322335 の修正。ホバーのコピーボタンが各 hover participant の `getAccessibleContent` を返り値としてコピーするが、カラーピッカーでは「There is a color picker here.」というスクリーンリーダー用プレースホルダーが返り、それがクリップボードに入っていた。選択中のカラー表現ラベル（なければ CSS 整形した現在色）を返すように修正し、テストも追加する。

**直近のアクティビティ:** 著者 zaewc が CLA 同意コメント。copilot bot がレビュー。

**変更ファイル:** `hoverColorPickerParticipant.ts`（+1/-2）と新規テスト（+38）の 2 ファイル。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [WIP: chat session handling in Copilot CLI extension](https://github.com/microsoft/vscode/pull/316170) `#316170`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot CLI 拡張のチャットセッション処理を、イベントリスナー更新・キャッシュ機構導入・パフォーマンス計測改善で見直す WIP Draft PR。非推奨メソッドの削除とテスト更新を含む。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `copilotCLIChatSessionsContribution.ts`（+348/-197）ほか計 8 ファイル（+362/-224）。

**次のアクション:** クローズ済み（WIP のまま打ち切り）。

---

### ![PR](icons/git-pull-request.svg) [Filter agents with target:vscode from Background Agent dropdown](https://github.com/microsoft/vscode/pull/282654) `#282654`
**ステータス:** Closed | **ラベル:** なし

**概要:** Background Agent が `target: vscode` のカスタムエージェントを表示し、VS Code 固有ツールに依存するエージェントを外部プロバイダーに露出していた問題に対処する Draft PR。`mainThreadChatSessions.ts` に `IPromptsService` を注入し、`target === 'vscode'` のエージェントを除外するフィルタを追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューなしでクローズ）。

**変更ファイル:** `mainThreadChatSessions.ts`（+34/-3）とテスト（+88）の 2 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Cherry-pick: disable `ChatConfiguration.ExitAfterDelegation` into release/1.109](https://github.com/microsoft/vscode/pull/296266) `#296266`
**ステータス:** Closed | **ラベル:** なし

**概要:** #296149 の `release/1.109` へのチェリーピック（Draft）。`ChatConfiguration.ExitAfterDelegation` の既定値を `true`→`false` に変更し、リクエスト委譲後のチャットパネル自動終了を既定で無効化する。

**直近のアクティビティ:** 直近 24 時間のコメントなし（クローズ）。

**変更ファイル:** `chat.contribution.ts` の 1 ファイル（+1/-1）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Copilot CLI - refactor agent handling to use custom agents service and improve agent resolution logic](https://github.com/microsoft/vscode/pull/309251) `#309251`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot CLI のエージェント探索/解決を、`vscode.chat.getCustomAgents` を裏付けとする専用のカスタムエージェントサービスに移行し、DI 配線を更新する Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `copilotCli.ts`（+59/-43）と `chatSessions.ts` の 2 ファイル（+63/-43）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(chat): implement input state caching and management for chat sessions](https://github.com/microsoft/vscode/pull/309348) `#309348`
**ステータス:** Closed | **ラベル:** なし

**概要:** 既に開いているセッションでオプション変更がなくてもプロンプト送信のたびに拡張側 `getChatSessionInputState` が呼ばれる冗長な挙動（#288457 関連）に対し、セッションリソース URI をキーにした拡張ホスト側のキャッシュを実装する Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `extHostChatSessions.ts`（+111/-6）ほか計 4 ファイル（+197/-6）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix web_fetch tool call showing empty content when expanded](https://github.com/microsoft/vscode/pull/311315) `#311315`
**ステータス:** Closed | **ラベル:** なし

**概要:** `web_fetch` ツールコールが実行中に展開しても URL が省略され空表示になる問題（VS Code 拡張・Agents App 双方で表示処理が欠如）を修正する Draft PR。`copilotCLITools.ts` で `formatWebFetchInvocation()` を使い「Fetching {url}」「Fetched {url}」を表示し、`copilotToolDisplay.ts` に `WebFetch` ケースを追加する。

**直近のアクティビティ:** DonJayamanne が CHANGES_REQUESTED で「truncate も `appendEscapedMarkdownInlineCode` も不要、`args.url` をそのまま使え」と指摘。copilot-swe-agent[bot]がコメント。最終的にクローズ。

**変更ファイル:** `copilotCLITools.ts`（+10/-1）/ `copilotToolDisplay.ts`（+19）ほか計 3 ファイル（+34/-1）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(chat session picker): add action bar actions and improve dropdown handling](https://github.com/microsoft/vscode/pull/310064) `#310064`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッションピッカーのドロップダウンに専用のアクションバーセクションを追加し、コマンド前のセパレータ有無などドロップダウン処理を改善する Draft PR（Before/After スクリーンショット付き）。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `chatSessionPickerActionItem.ts` の 1 ファイル（+39/-23）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [style: fix button dropdown styles in chat plan review container](https://github.com/microsoft/vscode/pull/311341) `#311341`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャット plan review UI の `monaco-button-dropdown` ボタンで、セグメント間の高さが不揃いになる問題を修正する Draft PR。#311327 の対応。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `chatPlanReview.css` の 1 ファイル（+22/-0）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [chore: populate AGENTS.md with full coding agent instructions](https://github.com/microsoft/vscode/pull/311888) `#311888`
**ステータス:** Closed | **ラベル:** なし

**概要:** `AGENTS.md` が `.github/copilot-instructions.md` を指すだけのスタブで、慣習的に `AGENTS.md` を読む AI コーディングエージェント（Claude、Codex 等）がプロジェクト固有のガイダンスを取得できない問題に対し、`copilot-instructions.md` の全内容を `AGENTS.md` へ反映する Draft PR。タイポ「repeadedly」→「repeatedly」も修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューなしでクローズ）。

**変更ファイル:** `AGENTS.md`（+149/-2）と `.github/copilot-instructions.md` の 2 ファイル（+150/-3）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [git: enhance repository handling for trusted workspaces](https://github.com/microsoft/vscode/pull/305514) `#305514`
**ステータス:** Closed | **ラベル:** なし

**概要:** Git 拡張の `GitFileSystemProvider` のリポジトリ解決ロジックを改善し、ワークスペースがトラスト済みの場合の git スキーム URI（特に worktree 関連）をより適切に扱う Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `fileSystemProvider.ts` の 1 ファイル（+23/-0）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [WIP: Add autorun for state updates in Response class](https://github.com/microsoft/vscode/pull/303927) `#303927`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャット応答モデルで、外部報告されたツール呼び出しの内部 `state` 変化時に応答更新をトリガーするよう調整する WIP Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `chatModel.ts` の 1 ファイル（+8/-0）。

**次のアクション:** クローズ済み（WIP のまま）。

---

### ![PR](icons/git-pull-request.svg) [feat: add modeId and permissionLevel to chat session interfaces and implementations](https://github.com/microsoft/vscode/pull/301923) `#301923`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッション履歴にチャットモードとツール自動承認の権限レベルというリクエストごとのメタデータ（`modeId`、`permissionLevel`）を追加し、提案 API サーフェスを通じて再ハイドレートできるようにする Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `chatServiceImpl.ts`（+32/-3）/ `constants.ts`（+25）ほか proposed API 含む計 9 ファイル（+81/-5）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(chat): add supportsHandOffs capability to chat session agents](https://github.com/microsoft/vscode/pull/298816) `#298816`
**ステータス:** Closed | **ラベル:** なし

**概要:** `IChatAgentAttachmentCapabilities` に `supportsHandOffs` を追加し、コーディングエージェントにロックされていても suggest-next ウィジェットを表示できるようにする Draft PR。スキーマ登録も行う。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューなしでクローズ）。Copilot コーディングエージェントが作成し、ファイアウォールで一部接続がブロックされた旨が本文に記載。

**変更ファイル:** `chatWidget.ts`（+3/-2）/ `chatSessions.contribution.ts`（+4）/ `chatAgents.ts` の 3 ファイル（+8/-2）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Hide manage model picker action item when required](https://github.com/microsoft/vscode/pull/296876) `#296876`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットモデルピッカーの「Manage Models...」アクション項目の表示を条件制御する `canManageModels` パラメータを追加する Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `chatModelPicker.ts`（+8/-6）とテスト（+51）の 2 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Save dirty files before Copilot CLI edits](https://github.com/microsoft/vscode/pull/282657) `#282657`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot CLI が未保存変更のあるファイルを編集すると stale な内容に適用されうる問題に対し、ダーティファイル検出時にユーザーへ確認を出して保存後に編集する Draft PR。`editFileTool.ts` に `ITextFileService` を追加し `textFileModel.isDirty()` で判定する。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューなしでクローズ）。

**変更ファイル:** `editFileTool.ts`（+31）と新規テスト（+108）の 2 ファイル（+139/-0）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Initialize GITHUB_COPILOT_INTEGRATION_ID in CopilotAgent](https://github.com/microsoft/vscode/pull/320922) `#320922`
**ステータス:** Closed | **ラベル:** なし

**概要:** エージェントホストの `CopilotAgent` 構築時に `GITHUB_COPILOT_INTEGRATION_ID` 環境変数を初期化する Draft PR。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメント（その後クローズ）。

**変更ファイル:** `copilotAgent.ts`（+12/-1）とテスト（+5/-2）の 2 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add chat quota trajectory nudge](https://github.com/microsoft/vscode/pull/320683) `#320683`
**ステータス:** Open | **ラベル:** なし

**概要:** `chatQuotaTrajectoryNudge` 処理（treatment）の背後で、Copilot Chat のクオータ消費ペースに関するナッジを追加する。月間使用量が 10〜35% かつ予測使用量が 1 日 4.5% 以上の対象 Pro/Pro+/Max ユーザーに表示し、表示/クローズ/クリックのテレメトリとテストを追加。チャット入力通知のリンク色も調整する。

**直近のアクティビティ:** rfeltis、pwang347、cwebster-99 の間で多数のレビューコメントがやり取りされている（活発なレビュー進行中）。copilot bot もレビュー。

**変更ファイル:** `chatQuotaNotification.ts`（+253/-3）/ `chatQuotaNotification.test.ts`（+544/-27）ほか計 4 ファイル（+812/-32）。

**次のアクション:** レビュー対応継続中。

---

### ![PR](icons/git-pull-request.svg) [Fix: CancellationToken not propagating to `provideLanguageModelChatResponse` on chat stop](https://github.com/microsoft/vscode/pull/319098) `#319098`
**ステータス:** Open | **ラベル:** なし

**概要:** 停止ボタンがプロバイダーの `CancellationToken` にキャンセルを伝播しない問題（#291713、#277872）を修正。原因は (1) `$startChatRequest` の RPC キャンセルハンドラがプロバイダー返却前に promise 解決で即削除される、(2) MainThread と ExtensionHost でリクエスト ID が異なり単一 CTS で橋渡しできない、の 2 点。両プロセスにローカル CTS を導入し新規 `$cancelLanguageModelChatRequest` RPC で接続、破棄時のリスナークリーンアップも行う。

**直近のアクティビティ:** tamuratak が `console.warn` 除去を報告。vritant24 が PR チェックの失敗と最後の copilot レビュー対応を依頼し、CHANGES_REQUESTED で保守性向上とユニットテスト追加を要求している。copilot bot も複数回レビュー。

**変更ファイル:** `cancellableRequestMap.ts`（+55 新規）/ `mainThreadLanguageModels.ts`（+34/-5）/ `extHostLanguageModels.ts` ほか計 7 ファイル（+291/-13）。テスト追加。

**次のアクション:** PR チェック修正とレビュー指摘対応待ち。

---

### ![PR](icons/git-pull-request.svg) [Extract reusable loopback proxy server base](https://github.com/microsoft/vscode/pull/322415) `#322415`
**ステータス:** Merged | **ラベル:** なし

**概要:** エージェントホストの各プロキシ間で重複していたループバック HTTP サーバーのライフサイクル（127.0.0.1 バインド、nonce 生成、参照カウント付きハンドル、in-flight 追跡、teardown）を再利用可能な基底クラスに抽出し、`ClaudeProxyService` と `CodexProxyService` をその上にリファクタする。今後の BYOK 実装で同じ基底を共有するための布石。

**直近のアクティビティ:** TylerLeonhardt と vritant24 が複数レビューコメントをやり取りし、TylerLeonhardt が APPROVED。

**変更ファイル:** `loopbackProxyServer.ts`（+332 新規）/ `loopbackProxyServer.test.ts`（+618 新規）/ `claudeProxyService.ts`（+46/-231）ほか計 5 ファイル（+1035/-504）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Revert "Sandbox: force execution in sandbox as model elevation is broken.](https://github.com/microsoft/vscode/pull/322476) `#322476`
**ステータス:** Merged | **ラベル:** ~release-cherry-pick

**概要:** モデル昇格が壊れているため導入された「サンドボックス内で強制実行する」挙動（Run-In-Terminal チャットツールおよびターミナルサンドボックス基盤）をリバートする。マージ時に `release/1.126` へ自動チェリーピックされる。

**直近のアクティビティ:** vs-code-engineering[bot]が自動チェリーピックと @anthonykim1 への CODENOTIFY を通知。jruales が APPROVED、copilot bot もレビュー。

**変更ファイル:** sandbox / chatAgentTools 系の計 12 ファイル（+20/-274）。`runInTerminalTool.test.ts`（-138）など主に削除。

**次のアクション:** マージ済み（1.126 へチェリーピック）。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Update GPT-5.5 prompt instructions](https://github.com/microsoft/vscode/pull/322447) `#322447`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** `main` からの #322443 のチェリーピック。GPT-5.5 のプロンプト命令をベースプロンプトに統合し、実験固有のプロンプトバリアントと設定を削除、ツール検索サポートを含む GPT-5.5 の capability routing を更新する。

**直近のアクティビティ:** dileepyavan と jruales が APPROVED。copilot bot はレビューエラー。

**変更ファイル:** `gpt55Prompt.tsx`（+264/-26）/ `gpt55BasePrompt.tsx` ほか計 8 ファイル（+304/-206）。`gpt55LargePrompt.tsx`（-72）など実験用バリアントを削除。

**次のアクション:** マージ済み（release/1.126）。

---

### ![PR](icons/git-pull-request.svg) [chat: add typed scrollbar markers to the Chat View transcript](https://github.com/microsoft/vscode/pull/322312) `#322312`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Chat View のトランスクリプトに、Monaco の overview ruler に類する型付きスクロールバーマーカーオーバーレイを追加し、会話内の重要箇所（prompt/askQuestion/fileChange/compaction/error の 5 種）をスクロールバーから視認・ジャンプできるようにする。`IChatRequestViewModel` への `editedFileEvents` 追加や、`ListView`→`List`→`AbstractTree` を貫く最小限の読み取り専用 API、`ChatScrollbarPromptMarkerController` による幾何計算と重なり解決を含む。

**直近のアクティビティ:** vs-code-engineering[bot]が @joaomoreno・@benibenj への CODENOTIFY を投稿。著者 matthewcorven が複数レビューコメント、copilot bot が指摘 5 件のレビュー。

**変更ファイル:** 大規模（計 14 ファイル、+3052/-245）。新規 `chatScrollbarPromptMarkerController.ts`（+468）と同テスト（+976）、`chatListWidget.ts`（+408/-196）、`chatPromptNavigationActions.ts`（+589/-41）など。

**次のアクション:** トリアージとレビュー待ち（大型機能）。

---

## qmk/qmk_firmware

> 新規キーボード/マクロパッドの追加 PR が多数進行中（Athena 1800・TwentyPad・santy65v2・Macro2040 など）。Ploopy 光学エンコーダのスクロール不具合修正が 2 件マージ/承認され、`VERBOSE` make フラグや LED 重複マッピングの自動処理といったコア/CLI の改善も入っている。長期 WIP の XAP プロトコルも更新された。

### ![PR](icons/git-pull-request.svg) [Add KBDFans Athena 1800](https://github.com/qmk/qmk_firmware/pull/26290) `#26290`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** KBDFans Athena 1800（96%/1800 レイアウトの RP2040 キーボード、GC9107 128x128 SPI 表示付き）のメインライン QMK ポート。ベンダーは RP2040 の第 2 コアで GIF を再生する Vial ベースだが、本ポートはシングルコアで Quantum Painter により Caps/Num/レイヤー状態を表示する。シフトレジスタ方式のカスタムマトリクス、GP0 の WS2812 rgblight（コールドブート時の破損対策として LCD 電源レール GP17 を先行有効化）などを実装。実機検証済み。

**直近のアクティビティ:** 作成者 laszloth が、QMK に慣れており GIF 不要で C でのカスタマイズを好むためポートした経緯と、比較用ベンダーファームの URL を補足コメント。

**変更ファイル:** 10 ファイル（+435 / -0）。`keyboard.json`（+145）、`athena1800.c`、`matrix.c`、`switch_board.h` など新規一式。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Fixed 4 1](https://github.com/qmk/qmk_firmware/pull/26291) `#26291`
**ステータス:** Closed | **ラベル:** keyboard, keymap

**概要:** crkbd（Corne）向けの個人キーマップ（marleydepew/marleydepewv3）と OLED 関連ファイルを追加する PR。本文は PR テンプレートのままで説明は未記入。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 10 ファイル（+997 / -130）。`keyboards/crkbd/` 配下のキーマップ・`oled.h`・`crkbd.c` など。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [keyboards/ploopyco: Fix scroll wheel freeze on trackball_thumb using a decaying simple encoder](https://github.com/qmk/qmk_firmware/pull/26286) `#26286`
**ステータス:** Open | **ラベル:** keyboard

**概要:** Ploopy 光学エンコーダ（特に Thumb Trackball）でスクロールホイールが数分でジッター→無反応になる問題を修正する。`simple`/`default` エンコーダは MAX/MIN を動的追跡するが、ノイズスパイクで境界が限界値（AVR の 1023、RP2040 の 4095）に押し広げられると閾値が永久に広がりデッドロックする。`opt_encoder_simple.c` に 10ms ごとに境界を中心へ減衰させるタイマーを導入し、数秒でスパイクを「忘れて」電源再投入なしに自動回復させる。Thumb で `simple` エンコーダを既定にする。

**直近のアクティビティ:** drashna が「simple ドライバのみ影響するか、default/tiny にも影響するか」と質問し、作成者 trougnouf が「simple のみで、Thumb の既定を simple にする」と回答。drashna が APPROVED。なお直射日光下のフォトダイオード起因のスクロールは原理的に残るとも述べている。

**変更ファイル:** 2 ファイル（+38 / -12）。`ploopyco/common/opt_encoder_simple.c`（+37/-12）と新規 `trackball_thumb/rules.mk`。

**次のアクション:** draft 状態。承認済みだが作者が数日の実使用確認後に ready 化予定。

---

### ![PR](icons/git-pull-request.svg) [WIP: Add HID descriptor for extended keyboard attrs](https://github.com/qmk/qmk_firmware/pull/26280) `#26280`
**ステータス:** Open | **ラベル:** core, no-ci

**概要:** HID の USAGE(EXTENDED_KEYBOARD_ATTRIBUTES) 用 Feature レポートを追加し、bCountryCode に代わって OS にレイアウトを報告する基盤（HUTRR42c）を整備する。lufa と V-USB の get_report(FEATURE) 対応は実装済みで、ChibiOS は未対応。#26236 の部分対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+287 / -30）。`tmk_core/protocol/` 配下の `vusb.c`（+103）・`chibios/usb_report_handling.c`（+82）・`lufa/lufa.c`・`usb_descriptor.c/.h`。

**次のアクション:** WIP。ChibiOS 対応、設定マクロ整備、ドキュメント、実機テストが残る。

---

### ![PR](icons/git-pull-request.svg) [Fix VERBOSE make flag](https://github.com/qmk/qmk_firmware/pull/26289) `#26289`
**ステータス:** Open | **ラベル:** core, python, cli

**概要:** `VERBOSE` を `true` で有効化すると謳いつつ、実際には任意の値（"false" でも）で verbose になってしまうバグを修正する。QMK の Python ビルドは make に `VERBOSE=false` を渡しており本来 verbose 化していたが、ラッパーが付ける `-s` で打ち消されていた。フラグ修正に伴い不要になった `-s` 付与も削除する。

**直近のアクティビティ:** drashna が APPROVED。

**変更ファイル:** 4 ファイル（+3 / -6）。`Makefile`、`builddefs/build_keyboard.mk`・`build_test.mk`、`lib/python/qmk/build_targets.py`。

**次のアクション:** 承認済み。マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open | **ラベル:** needs doc, in progress, core, keyboard, help wanted, keymap, needs testing, documentation, breaking_change, python, cli, feature, CI, no-ci, dd

**概要:** XAP（eXtensible Application Protocol）の実装作業を追跡する長期 WIP PR（#11567 のトラッキング）。2021 年作成で、ファームウェア側の生成コード、Python クライアント、ドキュメント生成などを横断的に整備する大規模機能。

**直近のアクティビティ:** 直近 24 時間のコメントなし（updated_at は同期によるもの）。

**変更ファイル:** 96 ファイル（+7548 / -26）と非常に大規模。`data/xap/*.hjson`、`quantum/xap/` 一式、`lib/python/xap_client/`、`lib/python/qmk/xap/`、各プロトコル実装など。

**次のアクション:** 長期 WIP。ドキュメント・テスト・実機検証が継続課題。

---

### ![PR](icons/git-pull-request.svg) [Automatically handle duplicate matrix LED mappings](https://github.com/qmk/qmk_firmware/pull/26278) `#26278`
**ステータス:** Merged | **ラベル:** enhancement, keyboard, python, cli

**概要:** 複数 LED を 1 キーにマッピングする際に追加コードが必要だった問題を解消する。同一 `matrix` を持つ LED 定義があると先頭が無視されていたが、必要な `rgb_matrix_map_row_column_to_led_kb` / `led_matrix_map_row_column_to_led_kb` 実装を自動生成して「そのまま動く」ようにする。`LED_HITS_TO_REMEMBER` が小さすぎる場合の OOB 対策ガードも含む。

**直近のアクティビティ:** drashna が APPROVED。

**変更ファイル:** 3 ファイル（+58 / -19）。`lib/python/qmk/cli/generate/keyboard_c.py`（+57/-8）が中心、`keyboards/planck/light/` を調整。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ploopyco: fix bitwise typos in opt_encoder_tiny state definitions](https://github.com/qmk/qmk_firmware/pull/26288) `#26288`
**ステータス:** Merged | **ラベル:** bug, keyboard

**概要:** `opt_encoder_tiny.c` のステートマシン定義で、`EMIT_UP_MID`/`EMIT_DOWN_MID` がビット OR (`|`) ではなくビット AND (`&`) を使っていたタイポを修正する。`EMIT_UP`/`EMIT_DOWN`（0x10/0x80）と `START_MID`（3）の AND が 0（START）になり、ほぼ毎遷移で状態が早期リセットされてスクロールが効かなくなっていた。`|` に直すことでスクロール追従を回復する。`OPT_ENCODER_TYPE = tiny` 設定時のみ影響（既定ではない）。

**直近のアクティビティ:** drashna と zvecr が APPROVED。

**変更ファイル:** 1 ファイル（+2 / -2）。`keyboards/ploopyco/common/opt_encoder_tiny.c`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add "handwired" TwentyPad macro pad](https://github.com/qmk/qmk_firmware/pull/26264) `#26264`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** 自作の 20 キーマクロパッド TwentyPad を追加する。設定は `ez_maker/directpins/teensy_2` をベースに、ブートローダーとレイアウトを変更したもの。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビュー履歴では zvecr・tzarc・drashna が CHANGES_REQUESTED や COMMENTED を重ね、直近 6/22 に drashna が CHANGES_REQUESTED）。

**変更ファイル:** 3 ファイル（+94 / -0）。`keyboards/handwired/twentypad/` の `keyboard.json`・`keymap.json`・`readme.md`。

**次のアクション:** 変更要求への対応が必要。

---

### ![PR](icons/git-pull-request.svg) [Add keyboard santy65v2](https://github.com/qmk/qmk_firmware/pull/26276) `#26276`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** アンダーグロー照明と 2 つのロータリーエンコーダを備えた分割キーボード santy65v2 を追加する。`keyboard.json` を `qmk format-json` で整形し、lint を通している。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューは waffle87・drashna・zvecr と作者の間で複数往復。直近 6/22 に drashna が「dev board 指定でプロセッサ/ブートローダーが設定でき converter 対応も改善する」とコメント）。

**変更ファイル:** 6 ファイル（+219 / -0）。`keyboards/santy65v2/` の `keyboard.json`（+146）・`config.h`・`keymap.c`・`readme.md` など。

**次のアクション:** 変更要求への対応が必要。

---

### ![PR](icons/git-pull-request.svg) [Add SM Boards Macro2040](https://github.com/qmk/qmk_firmware/pull/26247) `#26247`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** RP2040 ベースの 7 キーマクロパッド SM Boards Macro2040 を追加する。AS5600 磁気角センサ、WS2812B RGB マトリクス、SSD1306 OLED、6 個のインジケータ LED を搭載。AS5600 は I²C0、OLED は I²C1 にあり `i2c_master` のバス競合を避けるためカスタム OLED ドライバを使用。AS5600 の角度を読み GP20/21 に A/B 直交パルスを合成して GP22/23 にループバックさせ、QMK 標準エンコーダドライバを利用する設計。row 3（GP11）検出のため `matrix_read_cols_on_row()` に `wait_us(100)` を入れている。VIA 対応は別途提出予定。

**直近のアクティビティ:** 直近 24 時間のコメントなし（レビューは zvecr・tzarc が CHANGES_REQUESTED を複数回。直近 6/22 に tzarc が CHANGES_REQUESTED）。

**変更ファイル:** 9 ファイル（+570 / -0）。`keyboards/sm_boards/macro2040/` の `macro2040.c`（+235）・`config.h`・`keyboard.json`・`readme.md` など。

**次のアクション:** 変更要求への対応が必要。

---
