# GitHub ダイジェスト — 2026-05-03

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Change default for git.addAICoAuthor to off](https://github.com/microsoft/vscode/pull/313931)** (microsoft/vscode) — VS Code 1.118 で AI Co-author を既定で挿入する変更（#310226）が大きな反発を招いた件のフォローアップ。既定値を `off` に戻し、`disableAIFeatures` 時は変更追跡も走らないようにする。
2. ![PR](icons/git-pull-request.svg) **[modem: register cellular DNS with systemd-resolved](https://github.com/commaai/openpilot/pull/37955)** (commaai/openpilot) — 自前の `modem.py` 移行後にセルラー DNS が NetworkManager 経由で設定されない不具合を修正。Wi-Fi 切断時にデバイスがインターネットに到達できなかった既知問題の解消。
3. ![PR](icons/git-pull-request.svg) **[feat(tui): apply terminfo overrides from $NVIM_TERMINFO](https://github.com/neovim/neovim/pull/39555)** (neovim/neovim) — 環境変数 `$NVIM_TERMINFO` で terminfo 定義を JSON で上書きできるようにする機能。Windows ユーザーが terminfo を活用できない問題（#37274）への対応で、長期的な terminfo 依存縮小に向けた布石。
4. ![PR](icons/git-pull-request.svg) **[Add experimental alt prompt for Claude Opus 4.7](https://github.com/microsoft/vscode/pull/313916)** (microsoft/vscode) — Anthropic の Opus 4.7 プロンプトガイドに沿った `Claude47OpusPrompt` を Copilot Chat 用に追加。実験設定でゲートし `claude-opus-4-7*` トラフィックをルーティング。
5. ![Issue](icons/issue-opened.svg) **[job control: vim.SystemObj:kill() leaves orphans](https://github.com/neovim/neovim/issues/39552)** (neovim/neovim) — `vim.system` で起動した子プロセスを `:kill()` してもプロセスグループに送らず孫プロセスが孤児化する問題。`jobstart`/`jobstop` には存在しない 5 件目の重複バグで、ジョブ制御層の一本化が議論されている。

---

## commaai/openpilot

> モデム関連の追従修正（DNS、SIM ホットスワップ、BPP エラー処理）と、UI のフレームレート改善が中心。`modem.py` 完全移行後の細かな取りこぼしを順次潰している。

### ![PR](icons/git-pull-request.svg) [modem: register cellular DNS with systemd-resolved](https://github.com/commaai/openpilot/pull/37955) `#37955`
**ステータス:** Open | **ラベル:** なし。

**概要:** Wi-Fi 切断時、`modem.py` が DNS サーバーを設定しないためネットワークリンクが張れていてもインターネットに繋がらない問題を修正。モデムが取得した DNS を systemd-resolved に登録する。

**直近のアクティビティ:** プロセスリプレイ差分は 0 changed/66 passed。adeebshihadeh が変更要求中、greatgitsby とレビュー中。

**変更ファイル:** `system/hardware/tici/modem.py` 1 ファイル、+38/-5 行。

**次のアクション:** adeebshihadeh の指摘に対応してマージ判断へ。

---

### ![PR](icons/git-pull-request.svg) [modem: robust identity read on SIM hot-swap](https://github.com/commaai/openpilot/pull/37954) `#37954`
**ステータス:** Merged | **ラベル:** なし。

**概要:** SIM ホットスワップ時に AT 識別子（CGSN/QCCID/CIMI/GMR）が空・エコー・非数字のまま SEARCHING に進む問題を修正。識別子が完全に揃うまで INITIALIZING に留まる。

**直近のアクティビティ:** Process replay 0 changed/66 passed。

**変更ファイル:** `system/hardware/tici/modem.py` のみ、+33/-21 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [add modem.py expected cpu usage](https://github.com/commaai/openpilot/pull/37951) `#37951`
**ステータス:** Merged | **ラベル:** CI / testing。

**概要:** `selfdrive/test/test_onroad.py` に `modem.py` の期待 CPU 使用量を追加するテスト調整。

**直近のアクティビティ:** Process replay 0 changed/66 passed。

**変更ファイル:** `selfdrive/test/test_onroad.py` 1 行追加。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [lpa: stop loading BPP once eUICC returns ProfileInstallResult](https://github.com/commaai/openpilot/pull/37890) `#37890`
**ステータス:** Merged | **ラベル:** なし。

**概要:** eUICC が BPP 中に終端 ProfileInstallResult (BF37) を返した時点で残り A3 子チャンクの送信を停止する。閉じたインストールセッションへの送信で SW=6A88 偽エラーが出て本来のエラーが隠れる問題を解消。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `system/hardware/tici/lpa.py` 1 ファイル、+3/-2 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [modem.py](https://github.com/commaai/openpilot/pull/37811) `#37811`
**ステータス:** Merged | **ラベル:** なし。

**概要:** 昨日のハイライトでも触れたモデム管理の全面 `modem.py` 移行 PR がマージ。NetworkManager がモデムを一切触らない構成に切替。

**直近のアクティビティ:** adeebshihadeh が「Great work!」とコメント、UI Preview と process replay の bot コメントが続く。

**変更ファイル:** `system/hardware/tici/modem.py`、`lpa.py`、`hardware.py` ほか 12 ファイル、+589/-492 行。

**次のアクション:** マージ済み。フォローアップとして #37954（identity 強化）と #37955（DNS）が継続中。

---

### ![Issue](icons/issue-opened.svg) [Replace ModemManager with modem.py](https://github.com/commaai/openpilot/issues/37277) `#37277`
**ステータス:** Closed | **ラベル:** refactor, bounty, comma three。

**概要:** ModemManager を ~200 行の Python に置換するバウンティ Issue。comma 3X / four 双方対応、Discord ユーザーで一定期間の試用合格が達成条件だった。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #37811 マージで完了し Issue クローズ。

---

### ![PR](icons/git-pull-request.svg) [ui: re-apply mici/AugmentedRoadView _calc_frame_matrix caching](https://github.com/commaai/openpilot/pull/37948) `#37948`
**ステータス:** Merged | **ラベル:** ui。

**概要:** deanlee の #36669（#36749 で revert）を再適用し、スワイプ時の同期ずれを修正。`video_transform` を `x=y=0` でキャッシュ、`ModelRenderer` 側で `rect.x/y` を加算する設計。

**直近のアクティビティ:** comma four 実機計測で `uiDebug.drawTimeMillis` p50 8.7ms→5.6ms、スワイプ中も 59fps を維持。

**変更ファイル:** `selfdrive/ui/mici/onroad/augmented_road_view.py`、`model_renderer.py` 2 ファイル。

**次のアクション:** マージ済み。`#37610`（comma four <60fps）の改善に寄与。

---

### ![PR](icons/git-pull-request.svg) [ui debugging](https://github.com/commaai/openpilot/pull/37950) `#37950`
**ステータス:** Closed | **ラベル:** なし。

**概要:** UI デバッグ系の改修 PR（capnp、shader、application 等）。+355/-41。

**直近のアクティビティ:** UI Preview に差分、process replay は問題なし。最終的に取り下げ・分割の方針か。

**変更ファイル:** `cereal/log.capnp`、`selfdrive/ui/mici/onroad/*` 等 10 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [ui: vectorize triangulate in shader_polygon](https://github.com/commaai/openpilot/pull/37947) `#37947`
**ステータス:** Closed | **ラベル:** ui。

**概要:** `shader_polygon.py` の `triangulate()` を numpy スライスでベクトル化。Python ループとリスト経由の往復オーバーヘッドを除去。

**直近のアクティビティ:** Process replay 0 changed/66 passed。

**変更ファイル:** `system/ui/lib/shader_polygon.py` 1 ファイル。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [comma four < 60 FPS while engaged](https://github.com/commaai/openpilot/issues/37610) `#37610`
**ステータス:** Open | **ラベル:** bug。

**概要:** comma four で engaged 中に FPS が 60 を割るリグレッション。#37078 関連が疑われている。

**直近のアクティビティ:** sshane が継続的に調査。modeld を kill してもタイミングがほぼ変わらず CPU 起因ではないと判断。スクロール時の崩れは #37948 と #37946（CPU 改善）で解消した。ただし、定常状態で 60fps を確保するに至っていない。

**次のアクション:** UI と modeld を含めた継続調査。

---

### ![Issue](icons/issue-opened.svg) [kernel panic on desk](https://github.com/commaai/openpilot/issues/37949) `#37949`
**ステータス:** Open | **ラベル:** なし。

**概要:** デスク上で onroad 中、複数回マネージャ再起動した後にカーネルパニックで再起動した事象。pstore の dmesg-ramoops-0 を添付。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ramoops の調査・再現確認。

---

### ![PR](icons/git-pull-request.svg) [ui: disable experimental mode and driving personality toggles](https://github.com/commaai/openpilot/pull/37403) `#37403`
**ステータス:** Open | **ラベル:** ui, stale。

**概要:** `CarParamsPersistent` 未設定時に Experimental Mode と Driving Personality トグルを操作不可に。"no longitudinal control" 挙動に揃える。

**直近のアクティビティ:** 24 日活動なしで stale 通知、自動クローズ予定。Process replay/UI Preview の bot コメントのみ。

**変更ファイル:** `selfdrive/ui/layouts/settings/toggles.py` 系 2 ファイル。

**次のアクション:** stale 解除のための再活動が必要。

---

### ![PR](icons/git-pull-request.svg) [ui: fix false "NO PANDA" flash on screen wake](https://github.com/commaai/openpilot/pull/37404) `#37404`
**ステータス:** Open | **ラベル:** ui, stale。

**概要:** スクリーン復帰時に一瞬「NO PANDA」が表示される問題の修正。

**直近のアクティビティ:** stale 化、sshane と andiradulescu のレビューコメント。

**変更ファイル:** `selfdrive/ui/ui_state.py` 1 ファイル、+1/-2 行。

**次のアクション:** 再活動して stale を解除する必要あり。

---

### ![PR](icons/git-pull-request.svg) [esim: comma four profile management UI](https://github.com/commaai/openpilot/pull/37844) `#37844`
**ステータス:** Open | **ラベル:** ui。

**概要:** comma four 向けに eSIM プロファイル管理 UI を MICI レイアウトに追加。`CellularManager` バックエンドで LPA 操作・プロファイル/IP ポーリングを別スレッド実行、楽観的 UI 更新。

**直近のアクティビティ:** Process replay と UI Preview の bot コメントのみ。

**変更ファイル:** `selfdrive/ui/mici/layouts/settings/network/*`、`system/ui/lib/cellular_manager.py` ほか 6 ファイル、+570/-7。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [longitudinal: use VehicleModel in limit_accel_in_turns](https://github.com/commaai/openpilot/pull/37540) `#37540`
**ステータス:** Open | **ラベル:** stale。

**概要:** `limit_accel_in_turns` の横加速度計算を簡易キネマティックから `VM.calc_curvature` に切替。highway 域で最大 48% の過大評価による不要な縦方向リミットを抑制。

**直近のアクティビティ:** stale 通知。jyoung8607 が LGTM、autonomy team のセカンドオピニオン待ち。Copilot reviewer がレビューコメント。

**変更ファイル:** `selfdrive/controls/lib/longitudinal_planner.py` 1 ファイル、+9/-6。

**次のアクション:** autonomy team の最終承認待ち。

---

### ![PR](icons/git-pull-request.svg) [ui diff: add multithreading and improve logging (split from #37328)](https://github.com/commaai/openpilot/pull/37474) `#37474`
**ステータス:** Open | **ラベル:** ui。

**概要:** UI 差分動画生成の並列化と frame hash の並列計算で、長尺動画でも処理時間短縮。`-nostdin` 追加、Path 型移行、ログ改善。

**直近のアクティビティ:** Copilot reviewer と TheSecurityDev のやり取り。

**変更ファイル:** `selfdrive/ui/tests/diff/diff.py` 1 ファイル、+59/-47。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [try removing hypothesis](https://github.com/commaai/openpilot/pull/37953) `#37953`
**ステータス:** Closed | **ラベル:** なし。

**概要:** hypothesis 依存削減の試行。`pyproject.toml`、`fuzzy_generation.py` 等を変更。

**直近のアクティビティ:** Process replay 0 changed/66 passed。

**変更ファイル:** `pyproject.toml`、`uv.lock` 等 6 ファイル、+160/-134。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [try python 3.14](https://github.com/commaai/openpilot/pull/37952) `#37952`
**ステータス:** Closed | **ラベル:** なし。

**概要:** Python 3.14 への移行試行。

**直近のアクティビティ:** adeebshihadeh が「pycapnp と metadrive がブロッカー」とコメント。

**変更ファイル:** `.python-version`、`pyproject.toml`、`uv.lock`。

**次のアクション:** 上流対応待ちで一旦クローズ。

---

### ![PR](icons/git-pull-request.svg) [🐛 LogicError: [$200 bounty] [Ford Q4] Ford Lightnings Harness Causes Error](https://github.com/commaai/openpilot/pull/37942) `#37942`
**ステータス:** Closed | **ラベル:** なし。

**概要:** バウンティを名乗って `fixes/` ディレクトリにファイルを置くだけの低品質 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み（無効）。

---

## commaai/opendbc

> 新規車種対応（Mazda CX-5 2025、Alfa Romeo Stelvio）と既存車種のバグ報告。

### ![PR](icons/git-pull-request.svg) [Add FW for 2025 Mazda CX-5](https://github.com/commaai/opendbc/pull/3373) `#3373`
**ステータス:** Open | **ラベル:** car, mazda, fingerprint。

**概要:** 2025 Mazda CX-5 の新しい FW フィンガープリントを `mazda/fingerprints.py` に追加。

**直近のアクティビティ:** car_diff CI が 17 segments で 0 changed / 17 passed。

**変更ファイル:** `opendbc/car/mazda/fingerprints.py` 1 ファイル、+2/-0。

**次のアクション:** メンテナレビューを経てマージへ。

---

### ![PR](icons/git-pull-request.svg) [FCA Giorgio: Alfa Romeo Stelvio](https://github.com/commaai/opendbc/pull/1251) `#1251`
**ステータス:** Open | **ラベル:** car, car safety。

**概要:** openpilot#32898 を引き継ぐ FCA Giorgio プラットフォーム（Alfa Romeo Stelvio/Giulia）対応。lateral acceleration torque controller の feed-forward が好相性で公式サポート可能性あり、ただし当面 dashcam モードでの提供を想定。

**直近のアクティビティ:** sshane と belm0 がコメントレビュー。UDS/VIN フィンガープリンティング、ギア位置信号特定などが TODO に残る。

**変更ファイル:** `opendbc/car/fca_giorgio/*`、`opendbc/safety/modes/fca_giorgio.h` ほか 17 ファイル、+501/-7。

**次のアクション:** TODO 残項目（UDS、ギア、ドライバートルクなど）の埋め込みとレビュー対応。

---

### ![Issue](icons/issue-opened.svg) [Ford Expedition 2022 Platinum with Blue Cruise.](https://github.com/commaai/opendbc/issues/3372) `#3372`
**ステータス:** Open | **ラベル:** bug, car。

**概要:** Ford Expedition 2022 で車線変更時、進入先車線にいる前方車両を無視してしまうという挙動報告。BSM 検知は問題なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ルート（11283eb47af132c4）からの再現確認。

---

## commaai/panda

> 直近24時間の更新はありません。

---

## neovim/neovim

> Lua API 拡張（vim.Pos のオフセット変換、`exepath` の shellslash 対応、prompt_appendbuf 改善）と TUI/terminfo・診断系の細かな修正、CI の PPA 解除が中心。LSP の `MessageStream` 抽象化や JSON 実装ドロップなど大型リファクタも進行中。

### ![PR](icons/git-pull-request.svg) [feat(pos): conversion between position and buffer offset](https://github.com/neovim/neovim/pull/39564) `#39564`
**ステータス:** Open | **ラベル:** なし。

**概要:** `vim.Pos` ↔ バッファのバイトオフセット変換 API を追加し、位置のソートやバイナリサーチを可能にする。`#36331` をクローズする可能性あり。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/pos.lua`、`test/functional/lua/pos_spec.lua` 等 4 ファイル、+91/-0。

**次のアクション:** メンテナレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [LSP codelens can trigger ModeChanged from Insert to Normal mode](https://github.com/neovim/neovim/issues/39567) `#39567`
**ステータス:** Open | **ラベル:** lsp。

**概要:** `runtime/lua/vim/lsp/codelens.lua` の `vim.cmd('normal! zb')` が Insert モード中に非同期コールバックから走ると、実際は Insert のままなのに `ModeChanged i:n` が発火する。tinymist 利用時にモード依存 UI が壊れる症状で発覚。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** codelens 側の zb 呼び出し方法を修正する必要あり。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39466) `#39466`
**ステータス:** Open | **ラベル:** vim-patch。

**概要:** `src/nvim/version.c` の vim-patch 一覧更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/version.c` のみ、+34/-46。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): get_node_text() should include trailing newline for buffer source](https://github.com/neovim/neovim/pull/39409) `#39409`
**ステータス:** Open | **ラベル:** treesitter, ci:backport release-0.12。

**概要:** `get_node_text()` がバッファソースで `end_col == 0` のときに改行を落としていた問題を修正。`#trim!` ディレクティブの暫定対応も削除。

**直近のアクティビティ:** dbalatero が rebase、justinmk と wookayin が APPROVED。CI の PPA 不通は #39557 で並行修正中。

**変更ファイル:** `runtime/lua/vim/treesitter*.lua` 系 5 ファイル、+38/-6。

**次のアクション:** PR タイトルを `should not drop trailing newline` 寄りに整えてマージへ。

---

### ![PR](icons/git-pull-request.svg) [build(zig): add formatc and lintc steps](https://github.com/neovim/neovim/pull/38546) `#38546`
**ステータス:** Open | **ラベル:** ci:skip-news。

**概要:** `build.zig` に `formatc`/`lintc` ステップを追加。uncrustify を deps に組み込む。

**直近のアクティビティ:** dchinmay2 と bfredl、justinmk のコメントレビューが多数（17 件）。

**変更ファイル:** `.clang-tidy`、`.clangd`、`build.zig`、`deps/uncrustify/*` ほか 11 ファイル、+491/-15。

**次のアクション:** レビュー指摘を反映。

---

### ![PR](icons/git-pull-request.svg) [feat(tui): apply terminfo overrides from $NVIM_TERMINFO](https://github.com/neovim/neovim/pull/39555) `#39555`
**ステータス:** Open | **ラベル:** tui。

**概要:** `$NVIM_TERMINFO` 環境変数で JSON ベースの terminfo 上書きを可能にする。Windows ユーザーの terminfo 利用問題（#37274）と、長期的な terminfo 依存縮小に向けた一歩。`#37475`（環境変数による override）を置き換える方針で再実装。

**直近のアクティビティ:** justinmk と kylesower がコメントレビュー多数。

**変更ファイル:** `runtime/lua/vim/tty.lua`、`src/nvim/tui/terminfo.c`、`tui.c` ほか 9 ファイル、+373/-33。

**次のアクション:** レビュー反映後マージへ。

---

### ![PR](icons/git-pull-request.svg) [fix: write `!` output while in binary mode not remove CR](https://github.com/neovim/neovim/pull/39558) `#39558`
**ステータス:** Open | **ラベル:** environment。

**概要:** バイナリモード中の `!` 出力で CR が落ちる #39424 の修正。

**直近のアクティビティ:** 616b2f がコメントレビュー。

**変更ファイル:** `src/nvim/os/shell.c`、`test/functional/vimscript/system_spec.lua`。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [feat(cmdline): show command description in floating window](https://github.com/neovim/neovim/pull/39559) `#39559`
**ステータス:** Open | **ラベル:** なし。

**概要:** wildmenu の隣にコマンド説明用フローティング窓を表示。ex コマンドに `desc` フィールドを追加し、`'wildmode'` で `popup` を有効化したときに表示する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/news.txt`、`runtime/doc/options.txt`、`src/nvim/api/command.c`、`src/nvim/cmdexpand.c`、`src/nvim/usercmd.c` ほか 17 ファイル、+1312/-9。

**次のアクション:** 大型変更のためレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [ui2: entering the pager fails if `<ESC>` is remapped to `:fclose`](https://github.com/neovim/neovim/issues/39463) `#39463`
**ステータス:** Open | **ラベル:** ui2。

**概要:** `<Esc>` を `:fclose` に再マップした状態で ui2 のページャに入ると `Invalid window id: 1004` エラー。`messages.lua:607` の `nvim_win_get_height` が落ちる。

**直近のアクティビティ:** wookayin が `git bisect` で `faa7c15b5a7…` (#39247) を first bad commit と特定。`feedkeys <Esc>` の使い方が根本原因で、#39462 が良い修正候補という指摘。

**次のアクション:** #39462 のレビュー進行待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(path): make exepath respect shellslash](https://github.com/neovim/neovim/pull/39541) `#39541`
**ステータス:** Open | **ラベル:** なし。

**概要:** Windows で `gx` が `cmd.exe` のフルパスを取得する際、スラッシュ正規化に失敗する問題（#39524）の修正。`exepath` の `slash_adjust` を revert して `shellslash` を尊重。

**直近のアクティビティ:** ntdiary がコメント、CI の落ちは無関係と説明。テスト追加は方針確定後。

**変更ファイル:** `src/nvim/eval/fs.c`、`exepath_spec.lua` 等 4 ファイル。

**次のアクション:** 解法合意後にテスト追加。

---

### ![PR](icons/git-pull-request.svg) [refactor(json)!: drop "eval" impl of JSON de/serialization](https://github.com/neovim/neovim/pull/39181) `#39181`
**ステータス:** Open | **ラベル:** refactor, breaking-change。

**概要:** `eval/decode.c` / `eval/encode.c` の JSON-Vimscript 実装をドロップする探索的 PR。Vimscript<=>JSON のフルラウンドトリップは利用者がほぼいないと判断。`#28683` 解決を狙う。

**直近のアクティビティ:** zeertzjq がコメントレビュー多数。

**変更ファイル:** `runtime/lua/vim/_core/vimfn.lua`、`src/nvim/channel.c`、`src/nvim/eval/decode.c` ほか 12 ファイル、+234/-1700。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.{0429,0430}](https://github.com/neovim/neovim/pull/39566) `#39566`
**ステータス:** Merged | **ラベル:** test, vim-patch。

**概要:** macos-15-intel ランナで連発する flaky な `Test_smoothscroll_incsearch` と MS-Windows での `Test_shortmess_F3` の安定化を vim から取り込み。screendump を assertion に置換。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test/old/testdir/test_options.vim`、`test_scroll_opt.vim`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(diagnostic): show diagnostic signs according to config](https://github.com/neovim/neovim/pull/39525) `#39525`
**ステータス:** Open | **ラベル:** diagnostic。

**概要:** `diagnostic.status` が `config.signs.text` を尊重せず `config.status.format` のみを見ていた問題を修正。

**直近のアクティビティ:** ofseed が前後経緯を補足、antonk52 と MariaSolOs が APPROVED/LGTM。

**変更ファイル:** `runtime/doc/diagnostic.txt`、`runtime/lua/vim/diagnostic.lua`。

**次のアクション:** マージへ。

---

### ![PR](icons/git-pull-request.svg) [fix(prompt): handle multi-element lists in prompt_appendbuf](https://github.com/neovim/neovim/pull/39550) `#39550`
**ステータス:** Open | **ラベル:** なし。

**概要:** `prompt_appendbuf` に複数要素のリストを渡したとき、最初の要素のみ連結され残りがプロンプトを置換してしまう不具合を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/eval/buffer.c`、`prompt_buffer_spec.lua`。

**次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Better repeat](https://github.com/neovim/neovim/issues/1025) `#1025`
**ステータス:** Open | **ラベル:** multicursor。

**概要:** `cw` を `dwi` で再マップしても `.` リピートで `i` 部分しか繰り返されない、`vim-repeat` の必要性をなくしたいという長年の要望 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン議論継続。

---

### ![Issue](icons/issue-opened.svg) [Bug_report_nvim](https://github.com/neovim/neovim/issues/39563) `#39563`
**ステータス:** Closed | **ラベル:** なし。

**概要:** テンプレ未記入の低品質バグ報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(jobs): always detach on unix](https://github.com/neovim/neovim/pull/39562) `#39562`
**ステータス:** Open | **ラベル:** なし。

**概要:** `vim.system`（`uv_spawn`）で `UV_PROCESS_DETACHED` を立てて子プロセスにプロセスグループを付与し、孫プロセスをまとめて kill 可能にする。`#39552` の解消が目的。

**直近のアクティビティ:** cairo55 が unix の detach 必要性を説明、ofseed が `vim._transport` の同様ロジックを参照。

**変更ファイル:** `runtime/lua/vim/_core/system.lua` 1 ファイル。

**次のアクション:** Windows 挙動の確認とジョブ制御層一本化の議論。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): make --remote wait by default with +async opt-out](https://github.com/neovim/neovim/pull/37951) `#37951`
**ステータス:** Open | **ラベル:** revisit-at-release, remote, AI assisted 🤖。

**概要:** `--remote` を既定で wait（バッファアンロードまでブロック）にし、`+async` で従来の非同期挙動を opt-out できるようにする。`mhinz/neovim-remote` 不要を狙う。

**直近のアクティビティ:** justinmk がコメントレビュー。

**変更ファイル:** `src/nvim/api/events.c`、`src/nvim/main.c`、`runtime/lua/vim/_core/editor.lua` ほか 9 ファイル、+872/-249。

**次のアクション:** リリース見直しタイミングまで継続レビュー。

---

### ![Issue](icons/issue-opened.svg) [job control: vim.SystemObj:kill() leaves orphans](https://github.com/neovim/neovim/issues/39552) `#39552`
**ステータス:** Open | **ラベル:** job-control, lua, architecture。

**概要:** `vim.system` で起動した子プロセスを `:kill()` してもプロセスグループへ信号が送られず、孫プロセスが孤児化する。`jobstart`/`jobstop` には存在しない。

**直近のアクティビティ:** justinmk が「`vim.system` と `jobstart` のジョブ制御層が二重なのは 5 件目の問題」と指摘。`vim.spawn` を内部 job-control 層に寄せるべきという議論。

**次のアクション:** 短期対処は #39562、長期は層の一本化。

---

### ![Issue](icons/issue-opened.svg) [vim.net as a full fledged http client](https://github.com/neovim/neovim/issues/38946) `#38946`
**ステータス:** Open | **ラベル:** lua, network。

**概要:** `vim.net.request` を GET 専用から拡張し、Python `requests` 風の `vim.net.get` / `post` などを提供する API 提案。

**直近のアクティビティ:** deathbeam が「`vim.system` コールバックでほぼ事足りるのでは」とのコメント、#39000 の参照あり。

**次のアクション:** デザイン議論継続。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump tree-sitter to fb348c349](https://github.com/neovim/neovim/pull/39442) `#39442`
**ステータス:** Merged | **ラベル:** build。

**概要:** tree-sitter の更新。長行で多量の captures を扱うパスのパフォーマンス改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build.zig.zon`、`cmake.deps/deps.txt`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ci(external): no need for neovim-ppa/stable](https://github.com/neovim/neovim/pull/39560) `#39560`
**ステータス:** Merged | **ラベル:** ci, target:release。

**概要:** `with-external-deps` ワークフローで neovim-ppa/stable 追加が timeout する問題を回避するため PPA 追加を削除。release-0.12 への手動バックポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test.yml` 1 行削除。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ci(external): no need for neovim-ppa/stable](https://github.com/neovim/neovim/pull/39557) `#39557`
**ステータス:** Merged | **ラベル:** ci, ci:backport release-0.12。

**概要:** master 側の同一修正。

**直近のアクティビティ:** jamessan が「Launchpad 側の問題、おそらく LLM スクレイパー由来」とコメント、neovim-backports[bot]のバックポート push 失敗、teto/justinmk が APPROVED。

**変更ファイル:** `.github/workflows/test.yml`。

**次のアクション:** マージ済み（バックポートも #39560 で完了）。

---

### ![PR](icons/git-pull-request.svg) [feat(ui): ctrl-z targets only the current UI](https://github.com/neovim/neovim/pull/30319) `#30319`
**ステータス:** Open | **ラベル:** ui, remote。

**概要:** `ctrl-z` を「現在の UI のみ」サスペンドするように変更。`:detach`/`[count]ctrl-z` で UI を分離可能。長期的なマルチクライアント体験の整備。

**直近のアクティビティ:** zeertzjq、justinmk のコメントレビュー継続。

**変更ファイル:** `src/nvim/api/ui.c`、`channel.c`、`tui.c` 等 11 ファイル、+117/-3。

**次のアクション:** TODO 残（`nvim --connect` 等）。

---

### ![PR](icons/git-pull-request.svg) [refactor(excmd): pass fargs to Lua for builtin cmds](https://github.com/neovim/neovim/pull/39528) `#39528`
**ステータス:** Merged | **ラベル:** refactor。

**概要:** `nlua_push_eap` でトークン化を行い、ビルトイン Lua ハンドラも `eap.fargs` を受け取れるように。`M.ex_lsp` の再パースを削除。

**直近のアクティビティ:** justinmk のコメントレビュー。

**変更ファイル:** `runtime/lua/vim/_core/ex_cmd.lua`、`src/nvim/lua/executor.c` ほか 8 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [refactor(lsp): message stream abstraction for message framing](https://github.com/neovim/neovim/pull/39000) `#39000`
**ステータス:** Open | **ラベル:** refactor, lsp。

**概要:** JSON-RPC からメッセージフレーミングを切り離す `vim.MessageStream` 抽象を導入。LSP 以外の JSON-RPC や `vim.net` での再利用を視野。

**直近のアクティビティ:** ofseed が `vim.iter` との違いを説明（`feed`/`on_read` 形式、エラーハンドリング差）、justinmk と lewis6991 が APPROVED 含むレビュー継続。

**変更ファイル:** `runtime/lua/vim/_transport.lua`、`runtime/lua/vim/lsp/rpc.lua`。

**次のアクション:** デザイン合意後にマージ。

---

### ![Issue](icons/issue-opened.svg) [`:restart` uses Nvim's current directory instead of shell's](https://github.com/neovim/neovim/issues/39239) `#39239`
**ステータス:** Open | **ラベル:** needs:discussion, lifecycle。

**概要:** `:restart` が現在の Nvim cwd を使ってしまい、相対パス引数を解決できない問題。

**直近のアクティビティ:** justinmk が `command_line_scan` のパース重複懸念を提起、Mrpaoo が `initialdir` グローバル変数を使う試作パッチを共有。

**次のアクション:** デザイン合意。

---

### ![PR](icons/git-pull-request.svg) [docs: document parameter `nargs` for nvim_create_user_command](https://github.com/neovim/neovim/pull/39540) `#39540`
**ステータス:** Open | **ラベル:** needs:response。

**概要:** `nvim_create_user_command` の `opts.nargs`/`cmd.nargs` の挙動をドキュメント化。

**直近のアクティビティ:** justinmk と bobbybobby のコメントレビュー。

**変更ファイル:** `runtime/doc/api.txt`、`api.gen.lua`、`api/command.c`。

**次のアクション:** 著者からの応答待ち。

---

### ![PR](icons/git-pull-request.svg) [feat(tui): allow env var overrides for terminfo capabilities](https://github.com/neovim/neovim/pull/37475) `#37475`
**ステータス:** Closed | **ラベル:** tui, needs:response。

**概要:** TUI 初期化時に `NVIM_T_*` 環境変数で terminfo 機能を上書きする提案。

**直近のアクティビティ:** justinmk が JSON 形式採用への懸念を再確認。

**次のアクション:** 後継として #39555（JSON ベース）が進行中、本 PR はクローズ。

---

### ![Issue](icons/issue-opened.svg) [crash in win_line .. grid_put_linebuf](https://github.com/neovim/neovim/issues/39075) `#39075`
**ステータス:** Open | **ラベル:** needs:repro, bug-crash, display。

**概要:** ホスト端末リサイズ後のクラッシュ。`grid_put_linebuf` で abort。

**直近のアクティビティ:** phanen が `ui2` 有効状態で同様バックトレースを再現したと報告。

**次のアクション:** 安定再現手順の絞り込み。

---

### ![PR](icons/git-pull-request.svg) [feat: keymap parse](https://github.com/neovim/neovim/pull/38636) `#38636`
**ステータス:** Open | **ラベル:** なし。

**概要:** key-chord をパースして modifiers / key / 正規化キーを返す API 追加。`#26575` の解決を狙う。

**直近のアクティビティ:** clason が「`vim.keycode` との違い・必要性」を質問、altermo らとのレビュー継続。

**変更ファイル:** `src/nvim/keycodes.c`、`mapping.c`、`runtime/lua/vim/_core/editor.lua` ほか 14 ファイル、+260/-35。

**次のアクション:** API 名と `vim.keycode` との差別化を整理。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): avoid triggering `__index` when judging if a table is a list](https://github.com/neovim/neovim/pull/39556) `#39556`
**ステータス:** Open | **ラベル:** lua。

**概要:** `vim.islist` で `__index` を持つテーブル判定が不安定だった問題を `rawget` で回避。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_core/shared.lua`、`vim_spec.lua`。

**次のアクション:** レビュー待ち。

---

## nvim-telescope/telescope.nvim

> 折りたたみによる結果表示崩れの修正と、古いタグ利用に伴うバグ報告のクローズ。

### ![PR](icons/git-pull-request.svg) [fix(ftplugin): disable folding in telescope results](https://github.com/nvim-telescope/telescope.nvim/pull/3656) `#3656`
**ステータス:** Merged | **ラベル:** なし。

**概要:** `shiftwidth=0`、`foldmethod=indent`、`foldlevelstart=0` の組み合わせで TelescopeResults がスクロール時に折り畳まれる #3629 を修正。`ftplugin/TelescopeResults.lua` で `nofoldenable` を設定。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `ftplugin/TelescopeResults.lua` 1 ファイル、+2/-0。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chore: update issue template url](https://github.com/nvim-telescope/telescope.nvim/pull/3658) `#3658`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Issue テンプレ内の質問用リンクを修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/ISSUE_TEMPLATE/config.yml`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [:Telescope find_files throwing an error when calling treesitter](https://github.com/nvim-telescope/telescope.nvim/issues/3657) `#3657`
**ステータス:** Closed | **ラベル:** bug。

**概要:** `attempt to call field 'ft_to_lang' (a nil value)` エラー。古い tag (0.1.8) と最新の Neovim を組み合わせていたのが原因。

**直近のアクティビティ:** clason が「ピン留めされたタグは古いまま、現行 Neovim と非互換」と回答。投稿者は適切なバージョン管理に切替予定。

**次のアクション:** クローズ済み（運用上の問題）。

---

### ![Issue](icons/issue-opened.svg) [`|` is not escaped in `grep_string`](https://github.com/nvim-telescope/telescope.nvim/issues/3659) `#3659`
**ステータス:** Closed | **ラベル:** bug。

**概要:** `grep_string` で `|` を検索すると ripgrep が OR と解釈し全行マッチしてしまう。

**直近のアクティビティ:** clason が「ripgrep が `|` を OR として解釈する仕様で、リテラル一致なら `\|`」と回答。MilkeeyCat が `__files.lua` の特殊文字エスケープ関数に `|` の置換が抜けていることを指摘。

**次のアクション:** ripgrep 仕様で wontfix。

---

### ![Issue](icons/issue-opened.svg) [Specific fold settings can mangle telescope results](https://github.com/nvim-telescope/telescope.nvim/issues/3629) `#3629`
**ステータス:** Closed | **ラベル:** bug。

**概要:** `shiftwidth=0` + `foldmethod=indent` で結果バッファが壊れる症状。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #3656 で修正・クローズ。

---

## ghostty-org/ghostty

> RTL/preedit/IME 周りの macOS 修正、メモリ最適化試行と撤回、Vouch 制度に伴う Issue/PR の自動クローズが目立つ。

### ![PR](icons/git-pull-request.svg) [Hint cold scroll back pages to allow OS reclaim under pressure](https://github.com/ghostty-org/ghostty/pull/12560) `#12560`
**ステータス:** Closed | **ラベル:** なし。

**概要:** スクロールバックの古い 1 万行を超えた領域に「OS が再利用してよい」ヒントを与え、長時間セッションでの RSS 増大（1k 行あたり 2.4MB）を抑制する案。`grow()` から `hintColdBoundary()` を呼ぶ実装。

**直近のアクティビティ:** 投稿者 agoodkind が「データロスのリグレッションが入っている」として自主クローズ。jcollie が「該当コードに詳しいレビュアーが見るのに時間がかかる」とコメント、ghostty-vouch[bot]が vouch 未取得のため自動クローズを通知。

**変更ファイル:** `src/terminal/PageList.zig`、`page.zig`、ベンチマーク 5 ファイル、+437/-0。

**次のアクション:** 別アプローチで再 PR 予定。

---

### ![Issue](icons/issue-opened.svg) [Add minimap support for terminal content](https://github.com/ghostty-org/ghostty/issues/12565) `#12565`
**ステータス:** Closed | **ラベル:** なし。

**概要:** 端末出力の右端にミニマップ表示を追加する Feature Request。

**直近のアクティビティ:** ghostty-vouch[bot]が「Issue は非メンテナ作成不可、Discussion を立ててください」として自動クローズ。

**次のアクション:** Discussion へ移行。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/12564) `#12564`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Discussion #12563 のコメントをトリガーとして agoodkind を vouch リストに追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/VOUCHED.td`、+1/-0。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add `middle-click-paste` config option](https://github.com/ghostty-org/ghostty/pull/12109) `#12109`
**ステータス:** Closed | **ラベル:** なし。

**概要:** `middle-click-paste` 設定を追加し、`selection`/`clipboard`/`false` を選べるようにする。

**直近のアクティビティ:** 00-kat が「#12443 と #12478 で代替済み」と指摘。

**変更ファイル:** `src/Surface.zig`、`src/config/Config.zig` ほか 3 ファイル。

**次のアクション:** クローズ済み（重複）。

---

### ![PR](icons/git-pull-request.svg) [Update iTerm2 colorschemes](https://github.com/ghostty-org/ghostty/pull/12562) `#12562`
**ステータス:** Open | **ラベル:** dependencies。

**概要:** iTerm2-Color-Schemes リリース 20260427-153600-5e4d1de への追従。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build.zig.zon`、`build.zig.zon.json`、`build.zig.zon.nix`、`build.zig.zon.txt`、`flatpak/zig-packages.json` 5 ファイル。

**次のアクション:** マージ判断。

---

### ![Issue](icons/issue-opened.svg) [minimal RTL support for single lines](https://github.com/ghostty-org/ghostty/issues/1442) `#1442`
**ステータス:** Open | **ラベル:** renderer。

**概要:** Arabic/Persian の文字シェイピングと右→左の文字順序が壊れている既知 Issue。

**直近のアクティビティ:** DiaaEddin の修正 PR #11079（昨日のハイライト）に対するビルド共有とフィードバック。Flimm が PR の作者を取り違える混乱に注意。

**次のアクション:** #11079 の議論継続。

---

### ![PR](icons/git-pull-request.svg) [macos: avoid replaying keys that commit preedit](https://github.com/ghostty-org/ghostty/pull/12547) `#12547`
**ステータス:** Merged | **ラベル:** os/macos。

**概要:** macOS で IME がマーク済みテキストを keyDown 中にコミットしたとき、AppKit が `insertText` で返す内容を「IME によるコミット」として扱い、元のキー（例: `ctrl+j` → LF）の再生を避ける。`Refs #10460`、`Related: #12518`。

**直近のアクティビティ:** mitchellh が「CI 通過後 OK」で APPROVED。

**変更ファイル:** `macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift` 1 ファイル、+22/-15。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Feature: Declarative workspace management via +split and +close CLI actions](https://github.com/ghostty-org/ghostty/issues/12556) `#12556`
**ステータス:** Closed | **ラベル:** なし。

**概要:** AppleScript ハックや tmux に頼らず Ghostty の split/close を CLI で宣言的に管理したい要望（AI agent 連携を意識）。

**直近のアクティビティ:** ghostty-vouch[bot]が Discussion 経由を案内し自動クローズ。

**次のアクション:** Discussion へ移行。

---

### ![PR](icons/git-pull-request.svg) [macOS: restore some menu shortcuts when terminal is not focused](https://github.com/ghostty-org/ghostty/pull/12522) `#12522`
**ステータス:** Open | **ラベル:** os/macos。

**概要:** `#10749` 修正。フォーカスが端末から外れたとき copy/paste/select all/undo/redo のメニューショートカットを復元する最小変更。第一レスポンダ・キーウインドウ変更時に再登録する方針。

**直近のアクティビティ:** mitchellh が CHANGES_REQUESTED「コンセプトは OK だがスタイルを整理してほしい」。

**変更ファイル:** `macos/Sources/App/macOS/AppDelegate.swift`、`MainMenu.xib`、`Ghostty.MenuShortcutManager.swift` ほか 5 ファイル、+131/-17。

**次のアクション:** スタイル整理して再レビュー。

---

### ![PR](icons/git-pull-request.svg) [libghostty: produced artifacts are static (no libc) even with SIMD features](https://github.com/ghostty-org/ghostty/pull/12414) `#12414`
**ステータス:** Closed | **ラベル:** libghostty。

**概要:** SIMD ありでも libghostty が libc 不要になるよう simdutf を fork、Zig で別コンパイルユニット化する大型変更。

**直近のアクティビティ:** mitchellh が「もう重要ではない」としてクローズ。

**変更ファイル:** `pkg/simdutf/*`、`example/c-vt-*` 32 ファイル、+888/-313。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [libghostty-vt: support building on macOS with Nix](https://github.com/ghostty-org/ghostty/pull/12548) `#12548`
**ステータス:** Merged | **ラベル:** libghostty。

**概要:** macOS (aarch64-darwin) 上で Nix を使って libghostty-vt をビルドできるようにする。

**直近のアクティビティ:** sandydoo と mitchellh のコメントレビュー。

**変更ファイル:** `build.zig`、`nix/libghostty-vt.nix`、+65/-27。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [SIGSEGV at startup on Fedora 44: bundled fontconfig + glycin SVG icon loader](https://github.com/ghostty-org/ghostty/issues/12554) `#12554`
**ステータス:** Closed | **ラベル:** なし。

**概要:** Ghostty 1.3.1 が Fedora 44 で起動時 SEGV。`gtk_window_realize_icon` → `glycin` (gdk-pixbuf SVG loader) → `fontconfig` 経路で発生。`#10568`/`#11152`（revert 済み）と同根の同梱 fontconfig 問題。

**直近のアクティビティ:** ghostty-vouch[bot]が Discussion 案内で自動クローズ。

**次のアクション:** Discussion 化。

---

## astral-sh/uv

> Renovate による自動依存更新と、PyTorch 2.11 関連ドキュメント・`uv python find --project` 実装、`UV_RUN_MAX_RECURSION_DEPTH` の設定リファクタが進行中。

### ![PR](icons/git-pull-request.svg) [Update cgr.dev/chainguard/python:latest-dev Docker digest to 24ab659](https://github.com/astral-sh/uv/pull/19230) `#19230`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker。

**概要:** Renovate による Chainguard Python コンテナの digest 更新（`c2ac411` → `24ab659`）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test-system.yml` 1 行。

**次のアクション:** 手動マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Resolve project environment in `uv python find --project` (#11990)](https://github.com/astral-sh/uv/pull/19244) `#19244`
**ステータス:** Open | **ラベル:** なし。

**概要:** `uv python find --project` でプロジェクト環境を解決するように実装。`#11990` の対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv/src/commands/python/find.rs`、`crates/uv/tests/it/python_find.rs`、+88/-17。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Resolve project environment in `uv python find --project` (#11990)](https://github.com/astral-sh/uv/pull/19243) `#19243`
**ステータス:** Closed | **ラベル:** なし。

**概要:** 同名 PR の前段。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv/src/commands/python/find.rs`、`python_find.rs`。

**次のアクション:** クローズ（#19244 で再提出）。

---

### ![PR](icons/git-pull-request.svg) [Update taiki-e/install-action action to v2.75.22](https://github.com/astral-sh/uv/pull/19172) `#19172`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker。

**概要:** Renovate による taiki-e/install-action の patch 更新（v2.75.10 → v2.75.22）。`tombi@latest`/`biome@latest` 等の更新を含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** GitHub Actions ワークフロー 4 ファイル。

**次のアクション:** 手動マージ。

---

### ![PR](icons/git-pull-request.svg) [Re-add `pytorch-triton-rocm` to PyTorch ROCm docs](https://github.com/astral-sh/uv/pull/19241) `#19241`
**ステータス:** Merged | **ラベル:** documentation。

**概要:** #19095 で削った `pytorch-triton-rocm` を ROCm ドキュメントに再追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/guides/integration/pytorch.md`、+9/-2。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update PyTorch documentation for PyTorch 2.11](https://github.com/astral-sh/uv/pull/19095) `#19095`
**ステータス:** Merged | **ラベル:** documentation。

**概要:** PyTorch 2.11 リリースに合わせて uv-torch backend、indexes、pyproject.rs などを更新。

**直近のアクティビティ:** Cloud0310 が ROCm 関連の依存問題（pytorch-triton-rocm が triton-rocm に依存して uv が拒否する）を補足。konstin が APPROVED。

**変更ファイル:** `crates/uv-distribution-types/src/index.rs`、`crates/uv-resolver/src/resolver/indexes.rs`、`crates/uv-torch/src/backend.rs` 系ほか 9 ファイル。+71/-67。

**次のアクション:** マージ済み（`#19241` でフォロー）。

---

### ![PR](icons/git-pull-request.svg) [Move parsing of `UV_RUN_MAX_RECURSION_DEPTH` to EnvironmentOptions](https://github.com/astral-sh/uv/pull/19240) `#19240`
**ステータス:** Open | **ラベル:** なし。

**概要:** `UV_RUN_MAX_RECURSION_DEPTH` 環境変数の解釈を clap から `EnvironmentOptions` に移し、`RunSettings::resolve` で args/env/default を統合する。`#14720` シリーズの一環。`str_to_bool` の非標準値コメントも更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-cli/src/lib.rs`、`crates/uv-settings/src/lib.rs`、`crates/uv-static/src/lib.rs`、`crates/uv/src/settings.rs`、+11/-3。

**次のアクション:** メンテナレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal。

**概要:** Renovate ダッシュボード Issue。abandoned 依存（39 件）が並ぶ。

**直近のアクティビティ:** 直近 24 時間のコメントなし（Renovate 自動更新）。

**次のアクション:** Renovate 管理。

---

### ![Issue](icons/issue-opened.svg) [global python default is overriding specific python version set in script](https://github.com/astral-sh/uv/issues/17717) `#17717`
**ステータス:** Open | **ラベル:** bug。

**概要:** `uv python pin --global 3.13` 後にスクリプトの `requires-python = ">=3.14"` が無視されグローバル既定が使われてしまう問題。プロジェクトでは期待通り、スクリプトのみで発生。

**直近のアクティビティ:** sharifhsn が「#18097 でクローズ済み」とコメント。

**次のアクション:** クローズ予定。

---

### ![Issue](icons/issue-opened.svg) [`uv publish` fails on 308 Permanent Redirect](https://github.com/astral-sh/uv/issues/17126) `#17126`
**ステータス:** Closed | **ラベル:** enhancement。

**概要:** `uv publish` で `https://test.pypi.org/legacy`（末尾スラッシュなし）へ送ると 308 でこける。リダイレクト追従の要望。

**直近のアクティビティ:** sharifhsn が「#17130 でクローズ」とコメント。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`uv pip install --group` fails when used with a subfolder](https://github.com/astral-sh/uv/issues/13571) `#13571`
**ステータス:** Open | **ラベル:** error messages。

**概要:** `uv pip install ./subdir --group x` が `error: File not found: pyproject.toml` で失敗。ディレクトリ移動後なら成功。

**直近のアクティビティ:** minusf が「`uv pip install -r ../pyproject.toml --group dev` でも `Failed to read dependency groups` が出る」と類似事例を共有。

**次のアクション:** 挙動の整理（pip との整合性）議論。

---

### ![Issue](icons/issue-opened.svg) [Automatically print the metadata file](https://github.com/astral-sh/uv/issues/19242) `#19242`
**ステータス:** Open | **ラベル:** enhancement。

**概要:** メタデータ表示用コマンドの追加要望（0.13.0 想定）。

**直近のアクティビティ:** zsol が「どのメタデータ？」と質問返し。

**次のアクション:** 仕様明確化を待つ。

---

### ![Issue](icons/issue-opened.svg) [uv --locked looks in the wrong place for lockfile when symlinked](https://github.com/astral-sh/uv/issues/19222) `#19222`
**ステータス:** Open | **ラベル:** bug。

**概要:** スクリプトをシンボリックリンク経由で起動すると `--locked` がリンクパス基準でロックファイルを探してしまい失敗する。

**直近のアクティビティ:** truffle-dev が `Pep723Script::read` の `std::path::absolute(file)?` がシンボリックリンクを解決していないこと、`LockTarget::lock_path` が同パスから `.lock` を導出していることを特定。

**次のアクション:** `canonicalize` 系へ置換する修正。

---

## qmk/qmk_firmware

> 新規キーボード追加 PR 中心、コアの combo コード最適化と、長期 WIP の XAP プロジェクト継続。

### ![PR](icons/git-pull-request.svg) [Add YMDK CV11 keyboard](https://github.com/qmk/qmk_firmware/pull/26182) `#26182`
**ステータス:** Open | **ラベル:** keyboard, keymap, python, cli。

**概要:** YMDK CV11 キーボード追加。`s245` の handwired キーボード調整含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `keyboards/ymdk/cv11/*`、`keyboards/handwired/sector245/*`、`lib/python/qmk/info.py` ほか 18 ファイル、+403/-4。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Minor combo code optimisation](https://github.com/qmk/qmk_firmware/pull/26073) `#26073`
**ステータス:** Merged | **ラベル:** core, awaiting review。

**概要:** combo 処理での冗長なインデックス初期化、オーバーラップスキャンの early break、デッドコード（`no_combo_keys_pressed`）削除、重複タイマーリセット解消。

**直近のアクティビティ:** stale 警告 → drashna と zvecr が APPROVED でマージ。

**変更ファイル:** `quantum/process_keycode/process_combo.c` 1 ファイル、+8/-9。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open | **ラベル:** core, breaking_change, feature ほか多数。

**概要:** XAP（クライアント・ファームウェア共通プロトコル）の実装トラッキング PR。`#11567` の本体実装。

**直近のアクティビティ:** t-8ch、tzarc がコメント、jock070 が APPROVED。

**変更ファイル:** `data/templates/xap/*`、`data/xap/*`、`builddefs/xap.mk` ほか 96 ファイル、+7548/-25。

**次のアクション:** 長期 WIP、継続的な作業。

---

### ![PR](icons/git-pull-request.svg) [Added keyboard pad9.](https://github.com/qmk/qmk_firmware/pull/26051) `#26051`
**ステータス:** Merged | **ラベル:** keyboard, keymap, stale。

**概要:** 3x3 + エンコーダ macropad の追加。

**直近のアクティビティ:** stale 通知後、drashna と zvecr の review iteration を経て APPROVED → マージ。

**変更ファイル:** `keyboards/pad9/*` 4 ファイル、+93/-0。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [add BitDeck10 macro keyboard](https://github.com/qmk/qmk_firmware/pull/26184) `#26184`
**ステータス:** Open | **ラベル:** keyboard, keymap。

**概要:** morisoba/BitDeck10 マクロキーボード追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `keyboards/morisoba/bitdeck10/*` 6 ファイル、+651/-0。

**次のアクション:** メンテナレビュー待ち。

---

## microsoft/vscode

> Copilot/Agent Host 関連の機能拡張（OTel トレーシング、Background Todo、Sandbox の AllowNetwork、Chronicle スキル化など）が大量に進行中。並行して、AI Co-author 既定値の差し戻し（#310226 → #313931）に対する強い反発、Claude Opus 4.7 のレートリミット変更、BYOK の API 非互換、Linux/macOS のクラッシュなど、ユーザー側の不満も多数。直近24時間で 100 件のアクティビティ（PR50/Issue50）。

### ![PR](icons/git-pull-request.svg) [Change default for git.addAICoAuthor to off](https://github.com/microsoft/vscode/pull/313931) `#313931`
**ステータス:** Open | **ラベル:** なし。

**概要:** `#310226` の差し戻しフォローアップ。`git.addAICoAuthor` の既定値を `off` に戻し、AI 機能無効時に変更追跡も走らないようにする。

**直近のアクティビティ:** ecki が「モデルバージョンの正確な記録・セッションログ参照などが揃えば既定 on でも構わない」と条件付き賛成、edenchazard が「フィードバックを聞いてくれてありがとう」、dmitrivMS が経緯と意図を整理。Copilot reviewer がコメント。

**変更ファイル:** `extensions/git/package.json`、`extensions/git/src/repository.ts`、`editTelemetryContribution.ts` の 3 ファイル。+11/-2。

**次のアクション:** コミュニティ反応を踏まえつつマージへ。

---

### ![PR](icons/git-pull-request.svg) [Default `git.addAICoAuthor` to `off`](https://github.com/microsoft/vscode/pull/313725) `#313725`
**ステータス:** Closed | **ラベル:** なし。

**概要:** 同主旨のコミュニティ提出 PR。

**直近のアクティビティ:** dmitrivMS が「engineering system が package.json 変更を弾くため別 PR で対応」と説明し #313931 へ誘導。Wren6991/typed-sigterm/edenchazard が反対意見・法的考察を投稿。

**変更ファイル:** `extensions/git/package.json`、`extensions/git/src/repository.ts`、+2/-2。

**次のアクション:** クローズ。#313931 が後継。

---

### ![PR](icons/git-pull-request.svg) [Enabling ai co author by default](https://github.com/microsoft/vscode/pull/310226) `#310226`
**ステータス:** Merged | **ラベル:** なし。

**概要:** AI co-author trailer を既定 on にしたマージ済み PR（炎上元）。

**直近のアクティビティ:** Vzor- が「再ライセンスへの法的影響がある詐欺的な振る舞い」と批判、複数ユーザーが他エディタ移行・revert 要求を表明。dmitrivMS が「`disableAIFeatures` 時に走るべきでなかった、AI 由来でない変更には付けない、テストカバレッジを増やしてから既定変更すべきだった」と謝意と修正方針を表明。

**変更ファイル:** `extensions/git/package.json`、`extensions/git/src/repository.ts`、+2/-2。

**次のアクション:** #313931 で revert 中。

---

### ![PR](icons/git-pull-request.svg) [Fix unwanted skill truncation](https://github.com/microsoft/vscode/pull/313749) `#313749`
**ステータス:** Merged | **ラベル:** なし。

**概要:** `chatSkills` のトラックがスキルツール無効時にも適用されてしまう問題を修正（#313263 解決）。

**直近のアクティビティ:** PEZ が修正を確認、rzhao271 が APPROVED。

**変更ファイル:** `src/vs/workbench/contrib/chat/common/promptSyntax/computeAutomaticInstructions.ts` 1 ファイル、+1/-1。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [The agent can't read the `chatSkills` provided by the extension](https://github.com/microsoft/vscode/issues/313263) `#313263`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released。

**概要:** 拡張提供の `chatSkills` パスが解決されず agent から読めない問題。`chatInstructions` は同形式で解決されていた。

**直近のアクティビティ:** pwang347 が `#313749` で検証済みとコメント。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Make Chronicle a Skill](https://github.com/microsoft/vscode/pull/313929) `#313929`
**ステータス:** Open | **ラベル:** なし。

**概要:** `/chronicle` インテント・専用プロンプトパイプラインを廃し、Skills 体系へ移行。`sessionStoreSqlTool` を standup/reindex 双方をカバーするよう拡張、Cloud (DuckDB) と Local (SQLite) の両バックエンド対応。

**直近のアクティビティ:** Copilot reviewer がコメント、screenshot diff の bot 出力。

**変更ファイル:** `extensions/copilot/assets/prompts/chronicle-*.prompt.md`、`extensions/copilot/src/extension/chronicle/*`、`sessionStoreSqlTool.ts` ほか 13 ファイル。+595/-829。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Sandbox: AllowNetwork option to allow outgoing traffic in sandbox mode](https://github.com/microsoft/vscode/pull/313912) `#313912`
**ステータス:** Open | **ラベル:** なし。

**概要:** ファイルシステム隔離は維持しつつ outbound ネットワークを許可する新サンドボックスモードを追加。`#313276` 修正。

**直近のアクティビティ:** Copilot reviewer がコメント、roblourens が DISMISSED。

**変更ファイル:** `src/vs/platform/networkFilter/*`、`platform/sandbox/common/settings.ts`、`chatAgentTools/*` ほか 25 ファイル。+228/-160。

**次のアクション:** roblourens の懸念整理後再レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add background todo agent](https://github.com/microsoft/vscode/pull/313911) `#313911`
**ステータス:** Open | **ラベル:** なし。

**概要:** Agent ターン中に todo リストを自動メンテする `BackgroundTodoDeltaTracker` + `BackgroundTodoProcessor` を追加。`copilot-fast` で別パスを走らせて `manage_todo_list` を呼び出し、メインモデルからの呼び出し依存を排除。

**直近のアクティビティ:** Copilot reviewer がコメント、screenshot diff の bot コメント。

**変更ファイル:** `extensions/copilot/src/extension/prompts/node/agent/backgroundTodo*` 系含む 17 ファイル、+2773/-13。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Implement Agent Host OTel tracing](https://github.com/microsoft/vscode/pull/313921) `#313921`
**ステータス:** Open | **ラベル:** なし。

**概要:** Agent Host に OpenTelemetry サポートを導入。chat フロントエンド → Agent Host → プロバイダ → Copilot SDK → hooks/tools の W3C トレーシング伝播、OTLP export、verbose スパン、Node async-local trace context を実装。

**直近のアクティビティ:** Copilot reviewer がコメント。

**変更ファイル:** `src/vs/platform/agentHost/common/otel/*`、`agentHostOTelService.ts`、`copilot/copilotAgent.ts` ほか 38 ファイル、+2819/-376。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [agentHost: use URI helpers for subagent sessions](https://github.com/microsoft/vscode/pull/313924) `#313924`
**ステータス:** Open | **ラベル:** なし。

**概要:** Subagent セッション URI のパースを毎回文字列化・再パースするのをやめ、URI オブジェクトのまま回す。idle eviction の URI 祖先チェックも整備、ネスト subagent の回帰テスト追加。

**直近のアクティビティ:** Copilot reviewer のコメント、screenshot diff bot。

**変更ファイル:** `platform/agentHost/common/state/sessionState.ts`、`agentService.ts`、`agentSessions/agentHost/*` ほか 11 ファイル。+325/-58。

**次のアクション:** レビュー進行。

---

### ![PR](icons/git-pull-request.svg) [Add experimental alt prompt for Claude Opus 4.7](https://github.com/microsoft/vscode/pull/313916) `#313916`
**ステータス:** Open | **ラベル:** なし。

**概要:** Anthropic の Opus 4.7 prompting guide に沿った `Claude47OpusPrompt` を追加。`github.copilot.chat.claude47OpusPrompt.enabled`（既定 false）でゲートし、`claude-opus-4-7*`/`claude-opus-4.7*` を新プロンプトへルーティング。Tool triggering、subagent fan-out、内部ナレーション禁止、`<skillUsage>` プリミティブなどを反映。

**直近のアクティビティ:** roblourens が APPROVED、Copilot reviewer がコメント。

**変更ファイル:** `prompts/node/agent/anthropicPrompts.tsx`、`platform/configuration/common/configurationService.ts` ほか 4 ファイル。+164/-1。

**次のアクション:** マージ間近。

---

### ![PR](icons/git-pull-request.svg) [Update remote dependencies](https://github.com/microsoft/vscode/pull/313923) `#313923`
**ステータス:** Open | **ラベル:** なし。

**概要:** REH (remote) パッケージに `@vscode/copilot-api@^0.3.0` を追加してサーバー側ビルドで利用可能にする。

**直近のアクティビティ:** Copilot reviewer がコメント、bpasero が APPROVED。

**変更ファイル:** `remote/package-lock.json`、`remote/package.json`。

**次のアクション:** マージへ。

---

### ![Issue](icons/issue-opened.svg) [BYOK chat providers receive an internal cache_control LanguageModelDataPart sentinel that's not in the public API](https://github.com/microsoft/vscode/issues/313920) `#313920`
**ステータス:** Open | **ラベル:** chat-ext-issue, new release。

**概要:** BYOK プロバイダ向けの tool-result メッセージへ、公開 API には存在しない内部 sentinel `cache_control` (`LanguageModelToolResultPart`) が混入する。tool-result を含むターンで毎回再現する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 公開 API のクリーンアップ。

---

### ![Issue](icons/issue-opened.svg) [chat.useCustomAgentHooks is "Unknown Configuration Setting" in 1.118.1 (regression from 1.117)](https://github.com/microsoft/vscode/issues/313846) `#313846`
**ステータス:** Open | **ラベル:** info-needed, new release。

**概要:** 1.118.1 で `chat.useCustomAgentHooks` 設定が「Unknown」となるリグレッション（Copilot Chat 拡張提供）。

**直近のアクティビティ:** pwang347 が「設定は削除済み、warning は期待通りで hook 自体は両方の経路で動作」と回答、サンプル agent.md を提示。

**次のアクション:** 報告者再現確認。

---

### ![Issue](icons/issue-opened.svg) [Hide Claude Opus 4.7 in Model Picker (Unavailable Featured Models, Upgrade to Copilot Pro+)](https://github.com/microsoft/vscode/issues/312960) `#312960`
**ステータス:** Open | **ラベル:** なし。

**概要:** Pro+ 以外で利用不可な Claude Opus 4.7 がモデルピッカーに常時表示される問題。設定で隠せない。

**直近のアクティビティ:** ganlvtech と DanTup が「再オープンして hide 可能にすべき」、`state.vscdb` を編集しても再注入される実装上の問題を指摘、スクリーンショット付き再現。

**次のアクション:** 再オープン検討。

---

### ![Issue](icons/issue-opened.svg) [Observing unexpected 15x Claude Opus 4.7, was 7.5x.](https://github.com/microsoft/vscode/issues/313763) `#313763`
**ステータス:** Open | **ラベル:** chat-ext-issue。

**概要:** 5/1 に Claude Opus 4.7 のレートが 7.5x → 15x に予告なく倍増したことへの不満と、需要連動の動的価格化提案。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 価格ポリシーの透明化要求。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat local models do not populate Context Window usage](https://github.com/microsoft/vscode/issues/313458) `#313458`
**ステータス:** Open | **ラベル:** chat-ext-issue。

**概要:** ollama / customoai プロバイダ経由のローカルモデル利用時、Context Window 使用量が 0 / max のまま埋まらない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** バックエンド側で usage を返すパスの整備。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat fails with OpenAI-compatible DeepSeek/Kimi models](https://github.com/microsoft/vscode/issues/313904) `#313904`
**ステータス:** Open | **ラベル:** なし。

**概要:** OpenAI 互換の DeepSeek v4 Pro / Kimi K2.6 で tool 利用時に `400 reasoning_content in the thinking mode must be passed back` エラー。

**直近のアクティビティ:** Dhakra92 が「sequentialthinking MCP で 100% 再現、DeepSeek の独自 function-calling 仕様が原因」と詳細補足。

**次のアクション:** プロバイダ別の reasoning_content ハンドリング検討。

---

### ![Issue](icons/issue-opened.svg) [error serializing chat for storage.](https://github.com/microsoft/vscode/issues/313925) `#313925`
**ステータス:** Open | **ラベル:** triage-needed。

**概要:** chat のストレージシリアライズでエラーが発生する報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 詳細情報待ち。

---

### ![Issue](icons/issue-opened.svg) [kill_terminal leads to system notification](https://github.com/microsoft/vscode/issues/313927) `#313927`
**ステータス:** Open | **ラベル:** なし。

**概要:** kill_terminal で冗長なシステム通知が出る、長コマンドの描画も崩れる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 通知抑制と描画修正。

---

### ![Issue](icons/issue-opened.svg) [IntelliSense suggestions disappear when GitHub Copilot is enabled (C#, Python)](https://github.com/microsoft/vscode/issues/313926) `#313926`
**ステータス:** Open | **ラベル:** new release。

**概要:** Copilot のゴーストテキストが IntelliSense ドロップダウンを隠す/出さなくなる問題。Copilot 無効化で復活。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 重複候補の優先制御見直し。

---

### ![Issue](icons/issue-opened.svg) [WebView browser panel gets stuck and becomes unclosable](https://github.com/microsoft/vscode/issues/313890) `#313890`
**ステータス:** Closed | **ラベル:** bug, browser-integration。

**概要:** 別プロジェクトを開いた後 WebView ブラウザパネルが固まって閉じられない。

**直近のアクティビティ:** kycutler が「Insiders で修正済み、次安定版で配布」、`Ctrl+Alt+/` のワークアラウンドを案内。ehisamed が確認。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [corrupted characters in the context menu entry](https://github.com/microsoft/vscode/issues/293711) `#293711`
**ステータス:** Open | **ラベル:** bug, install-update, windows, L10N。

**概要:** Windows のコンテキストメニュー項目が文字化けする。

**直近のアクティビティ:** chimes-of-freedom が「Insiders 版では問題なし」、dmitrivMS が PR を進める方針。

**次のアクション:** 安定版にも修正適用。

---

### ![PR](icons/git-pull-request.svg) [Browser: allow temporary certificate trust](https://github.com/microsoft/vscode/pull/299797) `#299797`
**ステータス:** Merged | **ラベル:** なし。

**概要:** 内蔵ブラウザで TLS 証明書エラー時に Chromium 風の「proceed anyway」を許可。hostname+fingerprint で 1 週間有効、セッション単位の信頼。

**直近のアクティビティ:** lramos15 APPROVED。

**変更ファイル:** `src/vs/platform/browserView/electron-main/browserSession*.ts`、`browserView.ts`、`siteInfoWidget.ts` ほか 10 ファイル、+1325/-80。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix Reveal in File Explorer not working in WSL](https://github.com/microsoft/vscode/pull/299492) `#299492`
**ステータス:** Merged | **ラベル:** なし。

**概要:** `revealFileInOS` が WSL の `vscode-remote://` URI で silently fail していた問題を修正。`\\wsl$\DistroName\...` の UNC パスを authority から構築。

**直近のアクティビティ:** alexr00 と lszomoru が APPROVED、Copilot reviewer がコメント。

**変更ファイル:** `src/vs/workbench/contrib/files/electron-browser/fileCommands.ts` ほか 2 ファイル、+32/-6。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update conpty to 1.25](https://github.com/microsoft/vscode/pull/301398) `#301398`
**ステータス:** Merged | **ラベル:** なし。

**概要:** node-pty を `^1.2.0-beta.10 → ^1.2.0-beta.12` に更新。`#241978` 対応。

**直近のアクティビティ:** anthonykim1 のコメント複数、justschen が APPROVED。

**変更ファイル:** `package*.json`、`remote/package*.json`、`childProcessMonitor.ts`、`terminalProcess.ts` ほか 7 ファイル、+34/-13。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Agent-host: Spawn from CLI and connect to remotes from Sessions](https://github.com/microsoft/vscode/pull/301140) `#301140`
**ステータス:** Merged | **ラベル:** なし。

**概要:** `--agent-host-port` / `--agent-host-path` を CLI に追加し、サーバー側で WebSocket エンドポイントを公開。Electron 環境とは異なる ESM 解決の調整も含む。

**直近のアクティビティ:** Copilot reviewer/connor4312/mjbvz/pwang347 のレビューを経てマージ。

**変更ファイル:** `cli/src/commands/agent_host.rs`、`agentHost/electron-browser/*`、`bootstrap-import.ts` ほか 82 ファイル、+5643/-826。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add chat perf markers](https://github.com/microsoft/vscode/pull/302491) `#302491`
**ステータス:** Merged | **ラベル:** なし。

**概要:** `ChatService.sendRequest` 経路にパフォーマンスマーカーを追加し、UI 送信から agent 起動までの timing を取得可能に。

**直近のアクティビティ:** roblourens APPROVED。

**変更ファイル:** `chat/common/chatService/chatServiceImpl.ts` ほか 7 ファイル、+38/-4。

**次のアクション:** マージ済み（Copilot 拡張側にも追加 TODO）。

---

### ![PR](icons/git-pull-request.svg) [Run action polish](https://github.com/microsoft/vscode/pull/302478) `#302478`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Agent Sessions の Run アクションに `submitLabel` 追加、タスク pin/設定対応、ラベル管理改善。`#302338`/`#302340` クローズ。

**直近のアクティビティ:** alexr00 APPROVED、roblourens DISMISSED、Copilot reviewer コメント。

**変更ファイル:** `src/vs/sessions/contrib/chat/browser/runScriptAction.ts` ほか 4 ファイル、+701/-187。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Sessions: Unified project picker and target mode picker](https://github.com/microsoft/vscode/pull/302375) `#302375`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Local/Cloud 別のラジオ + FolderPicker + RepoPicker を Project Picker に統合し、folder→Local、repo→Cloud のように推論。Target Picker は Worktree/Folder/Cloud の 3 モード。

**直近のアクティビティ:** bhavyaus APPROVED、bpasero/TylerLeonhardt DISMISSED、Copilot reviewer コメント。

**変更ファイル:** `sessions/contrib/chat/browser/projectPicker.ts`、`sessionTargetPicker.ts` ほか 14 ファイル、+758/-812。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Center agent feedback widget header on start line](https://github.com/microsoft/vscode/pull/302176) `#302176`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Agent feedback ウィジェットのヘッダがスタートラインの中心に揃うよう layout 計算を修正。

**直近のアクティビティ:** roblourens APPROVED。

**変更ファイル:** `src/vs/sessions/contrib/agentFeedback/browser/agentFeedbackEditorWidgetContribution.ts`、`agentFeedbackEditorWidget.css`、+4/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Allow pinning chat sessions in Copilot Chat](https://github.com/microsoft/vscode/pull/302163) `#302163`
**ステータス:** Closed | **ラベル:** なし。

**概要:** Copilot Chat のセッションをピン留めする機能。`pinned` 状態を `IAgentSessionState` に追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `agentSessions/*` ほか 9 ファイル、+165/-6。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [sessions - resolve by provider](https://github.com/microsoft/vscode/pull/302160) `#302160`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Agent Sessions の resolve をプロバイダ単位にし、遅いプロバイダが UI を止めないようにする。`#298507` 修正、`#300770` 改善。

**直近のアクティビティ:** sandy081 APPROVED、joshspicer DISMISSED、Copilot reviewer 多数のコメント。

**変更ファイル:** `agentSessionsModel.ts`、`chatSessionsService.ts` ほか 7 ファイル、+143/-54。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Sessions: telemetry should distinguish between the apps](https://github.com/microsoft/vscode/pull/302151) `#302151`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Sessions ウィンドウとデスクトップ本体を telemetry で識別できるよう `common.product` を `sessions/window` で切替。

**直近のアクティビティ:** sandy081 APPROVED。

**変更ファイル:** `src/vs/code/electron-main/app.ts`、`workbenchCommonProperties.ts` ほか 6 ファイル、+7/-6。

**次のアクション:** マージ済み（`#302132` 修正）。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump undici from 7.19.0 to 7.24.4 in /remote](https://github.com/microsoft/vscode/pull/301983) `#301983`
**ステータス:** Merged | **ラベル:** javascript, dependencies。

**概要:** undici を 7.19.0 → 7.24.4 に更新。fetch URL credentials の dispatch 修正等を含む。

**直近のアクティビティ:** rzhao271 APPROVED。

**変更ファイル:** `remote/package-lock.json`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [implements artifact view](https://github.com/microsoft/vscode/pull/302839) `#302839`
**ステータス:** Merged | **ラベル:** なし。

**概要:** chat 内に折りたたみ可能な session artifacts UI を実装、`ChatArtifactsService`（永続ストレージ + observer）と built-in `setArtifactsTool` を追加。

**直近のアクティビティ:** benibenj APPROVED。

**変更ファイル:** `chat/browser/widget/chatArtifactsWidget.ts`、`tools/chatArtifactsService.ts` ほか 13 ファイル、+738/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chore: add telemetry for network process lifetime](https://github.com/microsoft/vscode/pull/302836) `#302836`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Electron `network-process-launched`/`network-process-gone` をパブリック telemetry に流し、ネットワークプロセスの reliability/perf 分析に活用。

**直近のアクティビティ:** jrieken APPROVED、deepak1556 コメント。

**変更ファイル:** `src/vs/code/electron-main/app.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Enhance ListenerLeakError and ListenerRefusalError to include detailed debugging information](https://github.com/microsoft/vscode/pull/302835) `#302835`
**ステータス:** Merged | **ラベル:** なし。

**概要:** イベントリスナーのリーク/拒否エラーに詳細を追加しつつ、telemetry 集計用に `message` は安定文字列を維持。

**直近のアクティビティ:** dmitrivMS APPROVED。

**変更ファイル:** `src/vs/base/common/event.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [[TerminalSandboxing]: Include workspace folders as default in the sandbox config](https://github.com/microsoft/vscode/pull/302834) `#302834`
**ステータス:** Merged | **ラベル:** なし。

**概要:** ターミナルサンドボックス config に multi-root のワークスペースフォルダを `allowWrite` 既定に含める。`#302904` 修正。

**直近のアクティビティ:** jrieken APPROVED。

**変更ファイル:** `terminalSandboxService.ts` ほか 2 ファイル、+99/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add telemetry for update actions in title bar](https://github.com/microsoft/vscode/pull/302832) `#302832`
**ステータス:** Merged | **ラベル:** install-update。

**概要:** タイトルバー更新インジケータからのアクションに `workbenchActionExecuted` telemetry を追加。`#302815` 修正。

**直近のアクティビティ:** rzhao271 APPROVED。

**変更ファイル:** `update/browser/updateTitleBarEntry.ts`、+11/-6。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix - update session icon rendering logic](https://github.com/microsoft/vscode/pull/302824) `#302824`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Agent sessions のツリーアイコンで Local 以外は read indicator ではなくプロバイダアイコンを表示。

**直近のアクティビティ:** dmitrivMS APPROVED。

**変更ファイル:** `agentSessionsViewer.ts`、+5/-1。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Hide cancel button when displaying modified file confirmation](https://github.com/microsoft/vscode/pull/302821) `#302821`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Modified files 確認 UI のボタン重なりを解消するため Cancel ボタンを除去。

**直近のアクティビティ:** bpasero APPROVED。

**変更ファイル:** `chatModifiedFilesConfirmationSubPart.ts`、+0/-5。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat - update task labels for OSS compatibility](https://github.com/microsoft/vscode/pull/302819) `#302819`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Code - OSS / Sessions ワークフロー向けに `.vscode/tasks.json` のラベル整理。

**直近のアクティビティ:** benibenj APPROVED。

**変更ファイル:** `.vscode/tasks.json`、+11/-1。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix spinning icon for in-progress CI status checks in sessions window](https://github.com/microsoft/vscode/pull/302818) `#302818`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Sessions ウィンドウの CI ステータスで `Codicon.loading` のスピンが label 全体に伝播していたため、静的な `Codicon.clock` に置換。

**直近のアクティビティ:** bpasero APPROVED。

**変更ファイル:** `ciStatusWidget.ts`、+2/-2。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Ensure terminal instance exists before operations](https://github.com/microsoft/vscode/pull/302812) `#302812`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Sessions ウィンドウのターミナル管理で disposed/未追跡インスタンスへの move/show/close を防ぐガードを追加。

**直近のアクティビティ:** sandy081 APPROVED。

**変更ファイル:** `sessionsTerminalContribution.ts` ほか 2 ファイル、+118/-20。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Implement conditional blocks processing in release notes](https://github.com/microsoft/vscode/pull/302811) `#302811`
**ステータス:** Merged | **ラベル:** なし。

**概要:** リリースノート Markdown に build quality（Stable/Insiders）に応じた条件分岐ブロックを導入。

**直近のアクティビティ:** dmitrivMS と alexr00 APPROVED。

**変更ファイル:** `releaseNotesEditor.ts`、`releaseNotesRenderer.test.ts`、+172/-2。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chore - remove deprecated setting `chat.viewSessions.enabled`](https://github.com/microsoft/vscode/pull/302806) `#302806`
**ステータス:** Merged | **ラベル:** なし。

**概要:** 非推奨設定 `chat.viewSessions.enabled` をワークスペース settings から除去。

**直近のアクティビティ:** justschen APPROVED。

**変更ファイル:** `.vscode/settings.json`、-1。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update hover background color in 2026 Dark theme](https://github.com/microsoft/vscode/pull/302802) `#302802`
**ステータス:** Merged | **ラベル:** なし。

**概要:** 2026 Dark テーマの `list.hoverBackground` を視認性向上のために調整。

**直近のアクティビティ:** ulugbekna APPROVED。

**変更ファイル:** `extensions/theme-defaults/themes/2026-dark.json`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add shadow configuration setting and streamline layout behavior](https://github.com/microsoft/vscode/pull/302801) `#302801`
**ステータス:** Merged | **ラベル:** なし。

**概要:** ワークベンチの shadow 効果を制御する `workbench.shadows` 設定を導入。

**直近のアクティビティ:** ulugbekna APPROVED。

**変更ファイル:** `workbench/browser/layout.ts`、`media/style.css`、`editorgroupview.css` ほか 5 ファイル、+54/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat - add watcher exclusions for performance improvement](https://github.com/microsoft/vscode/pull/302800) `#302800`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Sessions ウィンドウ既定の `files.watcherExclude` を追加し、ファイル監視のオーバーヘッドを削減。

**直近のアクティビティ:** ulugbekna APPROVED。

**変更ファイル:** `sessions/contrib/configuration/browser/configuration.contribution.ts`、+6/-0。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Adds more fixtures](https://github.com/microsoft/vscode/pull/302840) `#302840`
**ステータス:** Merged | **ラベル:** なし。

**概要:** Component Explorer 用フィクスチャを追加（peek references、image carousel、chat input）。

**直近のアクティビティ:** roblourens APPROVED。

**変更ファイル:** `componentFixtures/*` 3 ファイル、+514/-0。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Test: UBB support](https://github.com/microsoft/vscode/issues/313933) `#313933`
**ステータス:** Open | **ラベル:** testplan-item。

**概要:** GitHub の usage-based billing 対応をテスト計画に追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** テストシナリオの記述。

---

### ![Issue](icons/issue-opened.svg) [Feat: UBB support](https://github.com/microsoft/vscode/issues/313932) `#313932`
**ステータス:** Closed | **ラベル:** feature-request, on-testplan。

**概要:** UBB 機能 Issue（テスト計画化）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ。

---

### ![Issue](icons/issue-opened.svg) [Support `XDG_CONFIG_HOME` for the `argv.json` file](https://github.com/microsoft/vscode/issues/162712) `#162712`
**ステータス:** Open | **ラベル:** feature-request, linux, workbench-os-integration。

**概要:** XDG Base Directory に従い `~/.vscode/` を `~/.config/vscode/` に移したい長期要望。

**直近のアクティビティ:** lgarron が「最近の `#311317` で `~/.vscode-shared` も追加され、ホームディレクトリのファイル作成をブロックしているマシンでは 1.118.0 以降 recently opened folders が読めなくなった」と報告。

**次のアクション:** XDG 準拠化方針の議論再開。

---

### ![Issue](icons/issue-opened.svg) [SSH connection](https://github.com/microsoft/vscode/issues/313143) `#313143`
**ステータス:** Open | **ラベル:** info-needed。

**概要:** SLURM クラスタなどメモリ制限がきつい環境で `Installing VS Code Server` が `RangeError: WebAssembly.instantiate(): Out of memory`（undici 起因）でループ。

**直近のアクティビティ:** beep-love が「~3.8GB 割当で発生、安全な必要量は？」と質問。

**次のアクション:** 必要メモリ量と対処法の調査。

---

### ![Issue](icons/issue-opened.svg) [Allow extensions to show progress associated with editor](https://github.com/microsoft/vscode/issues/295161) `#295161`
**ステータス:** Closed | **ラベル:** feature-request, api, workbench-editors。

**概要:** エディタ上部に進捗表示を出す API 追加要望。

**直近のアクティビティ:** vs-code-engineering[bot]が 60 日 20 upvote 未達で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot chat AwaitingInput Hook](https://github.com/microsoft/vscode/issues/298207) `#298207`
**ステータス:** Open | **ラベル:** feature-request, chat-hooks。

**概要:** Agent が user の入力待ちになった時点で発火する `AwaitingInput` フック追加要望。

**直近のアクティビティ:** vs-code-engineering[bot]による upvote 集計通知。

**次のアクション:** コミュニティ投票継続。

---

### ![Issue](icons/issue-opened.svg) [Clicking on a branch in the Repositories menu of Source Control should jump the Graph menu to that branch](https://github.com/microsoft/vscode/issues/298747) `#298747`
**ステータス:** Closed | **ラベル:** feature-request, scm。

**概要:** Repositories メニューで branch をクリックすると Graph メニューでそのブランチへ飛ぶ要望。

**直近のアクティビティ:** upvote 不足で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [request: don't open linked files in the preview tab group](https://github.com/microsoft/vscode/issues/298874) `#298874`
**ステータス:** Closed | **ラベル:** feature-request, markdown。

**概要:** Markdown プレビューからリンクを開くと preview タブ側に開くのをやめてほしい要望。

**直近のアクティビティ:** upvote 不足で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Add a suggestions pane](https://github.com/microsoft/vscode/issues/300886) `#300886`
**ステータス:** Open | **ラベル:** feature-request, suggest。

**概要:** 候補をパネルとして別領域に表示する案。

**直近のアクティビティ:** upvote 集計通知。

**次のアクション:** 投票継続。

---

### ![Issue](icons/issue-opened.svg) [Add an option to disable ghost inline auto complete for GitHub Copilot for comments specifically](https://github.com/microsoft/vscode/issues/313003) `#313003`
**ステータス:** Open | **ラベル:** なし。

**概要:** コメント内のみ Copilot ゴーストテキスト補完を抑止したい要望。

**直近のアクティビティ:** sam-segal-sndk が `editor.quickSuggestions` は inline 全体に効くため別軸が必要と補足。

**次のアクション:** 設計検討。

---

### ![Issue](icons/issue-opened.svg) [AHP: Show multiplier for models](https://github.com/microsoft/vscode/issues/313922) `#313922`
**ステータス:** Open | **ラベル:** agent-host。

**概要:** Agent Host ピッカーでモデルのレートマルチプライヤを表示する要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 仕様検討。

---

### ![Issue](icons/issue-opened.svg) [chandan adhikar](https://github.com/microsoft/vscode/issues/313868) `#313868`
**ステータス:** Open | **ラベル:** info-needed, triage-needed。

**概要:** 内容のないテンプレ未記入 issue。

**直近のアクティビティ:** vs-code-engineering[bot]と pwang347 が情報追加を依頼。

**次のアクション:** 情報待ち。

---

### ![Issue](icons/issue-opened.svg) [https://web.facebook.com/share/v/1BenZpytzS/](https://github.com/microsoft/vscode/issues/313875) `#313875`
**ステータス:** Open | **ラベル:** info-needed, triage-needed, new release。

**概要:** 本文がリンクのみの低品質 issue。

**直近のアクティビティ:** pwang347 が詳細追加を依頼。

**次のアクション:** 情報待ち。

---

### ![Issue](icons/issue-opened.svg) [While VS Code Insiders updating - Window is frozen](https://github.com/microsoft/vscode/issues/308674) `#308674`
**ステータス:** Open | **ラベル:** なし。

**概要:** Insiders 更新中、admin/elevated モードで起動した VS Code がフリーズ。

**直近のアクティビティ:** frozenprakash が「他の更新プロセスが elevated でないため」と推測。

**次のアクション:** 再現環境の調査。

---

### ![Issue](icons/issue-opened.svg) [Updated Download Speed is below 2g speed.](https://github.com/microsoft/vscode/issues/313930) `#313930`
**ステータス:** Open | **ラベル:** なし。

**概要:** 更新ダウンロードが 13kbps と極端に遅い。

**直近のアクティビティ:** vs-code-engineering[bot]が古いバージョンの利用を指摘。

**次のアクション:** 最新版での再確認。

---

### ![Issue](icons/issue-opened.svg) [Perceptron](https://github.com/microsoft/vscode/issues/313928) `#313928`
**ステータス:** Closed | **ラベル:** spam, new release。

**概要:** Inline Edits Debug 出力のみのスパム的 issue。

**直近のアクティビティ:** bot が spam として自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [report 4 / new problem / new report A / issue / new report 3](https://github.com/microsoft/vscode/issues/313914) `#313914`/`#313917`/`#313919`/`#313915`/`#313913`
**ステータス:** ほぼ Open or 自動クローズ | **ラベル:** triage-needed/spam。

**概要:** テンプレ未記入またはスパムの低品質 issue 群。

**直近のアクティビティ:** spam ラベルで自動クローズ、または triage-needed のまま放置。

**次のアクション:** triage 担当が処理。

---

### ![Issue](icons/issue-opened.svg) [Add Observability Dashboard for Project Health Metrics](https://github.com/microsoft/vscode/issues/313918) `#313918`
**ステータス:** Open | **ラベル:** なし。

**概要:** プロジェクトのビルド成功率、テスト結果、パイプライン実行時間などを集約するダッシュボード追加要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** triage。

---

### ![Issue](icons/issue-opened.svg) [Github Copilot Chat History not showing](https://github.com/microsoft/vscode/issues/298507) `#298507`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released。

**概要:** Copilot Chat の履歴が表示されなくなった。リモート SSH 側にも残っていないなど被害範囲広い既知バグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** `#302160` で修正、Insiders 配布。

---

### ![Issue](icons/issue-opened.svg) [Put Chat/Copilot in its own release notes section](https://github.com/microsoft/vscode/issues/251384) `#251384`
**ステータス:** Closed | **ラベル:** feature-request, docs-experience。

**概要:** リリースノートの Chat/Copilot 項目を独立セクションにしてほしい要望。

**直近のアクティビティ:** upvote 不足で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Doxygen "after members" syntax not recognized](https://github.com/microsoft/vscode/issues/248348) `#248348`
**ステータス:** Closed | **ラベル:** upstream, grammar, upstream-issue-linked。

**概要:** Doxygen の after-members 構文が文法ハイライトで認識されない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** upstream 解決待ち。

---

### ![Issue](icons/issue-opened.svg) [Help: VSCode source code packaging error with gulp4](https://github.com/microsoft/vscode/issues/241303) `#241303`
**ステータス:** Closed | **ラベル:** info-needed。

**概要:** ソースから gulp4 でパッケージング時のエラー報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Create my website](https://github.com/microsoft/vscode/pull/238016) `#238016`
**ステータス:** Closed | **ラベル:** triage-needed。

**概要:** 単に「my website」というファイルを追加するだけの低品質 PR。

**直近のアクティビティ:** AlphaOmega113 が APPROVED 投票（自演）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Correct type Identifiers for PowerShell files on macOS](https://github.com/microsoft/vscode/pull/237679) `#237679`
**ステータス:** Closed | **ラベル:** なし。

**概要:** macOS で PowerShell の UTI を追加する `#237199` 対応 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [stuck in neverending loop / tried to apply patch / language / No regresa nada / copilot can't use / bug / Enable Copilot super lag / verbose output](https://github.com/microsoft/vscode/issues/300080) 他
**ステータス:** Closed | **ラベル:** info-needed, triage-needed。

**概要:** Copilot 関連の低品質 issue 群（`#300080`/`#300072`/`#300071`/`#300068`/`#300067`/`#300058`/`#300027`/`#299972`）。多くがテンプレ未記入。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Agent](https://github.com/microsoft/vscode/pull/299878) `#299878`
**ステータス:** Closed | **ラベル:** なし。

**概要:** Agent ループのプロトタイプ。`src/vs/platform/agent/*` 配下に大規模追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 102 ファイル、+20573/-7。

**次のアクション:** クローズ済み（後続実装で発展）。

---

### ![PR](icons/git-pull-request.svg) [Add slight clarification to ChatParticipant in content provider API](https://github.com/microsoft/vscode/pull/300903) `#300903`
**ステータス:** Merged | **ラベル:** なし。

**概要:** `chatSessionsProvider.d.ts` の `ChatParticipant` の挙動説明を補強。

**直近のアクティビティ:** hediet と TylerLeonhardt APPROVED。

**変更ファイル:** `src/vscode-dts/vscode.proposed.chatSessionsProvider.d.ts`、+2/-1。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [[WIP] Update user profile page layout / [WIP] Update user profile page for better usability / [WIP] Hide customizations section in primary sidebar](https://github.com/microsoft/vscode/pull/300885) 他
**ステータス:** Closed | **ラベル:** なし。

**概要:** Copilot Coding Agent が "hi" の prompt から起動した低品質 WIP PR 群（`#300885`/`#300884`/`#302007`）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [test pr: do not merge](https://github.com/microsoft/vscode/pull/302479) `#302479`
**ステータス:** Closed | **ラベル:** なし。

**概要:** mime devDependency を 1.4.1 → 4.1.0 に上げるテスト PR（マージ禁止）。

**直近のアクティビティ:** Copilot reviewer のみ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix incorrect stale state during nodejs patch bump](https://github.com/microsoft/vscode/pull/302143) `#302143`
**ステータス:** Closed | **ラベル:** なし。

**概要:** Node.js パッチ bump で `node_modules is stale` 誤検出する問題の修正案。

**直近のアクティビティ:** Copilot reviewer のコメントのみ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [chat: remove stale languageModelStats storage migration](https://github.com/microsoft/vscode/pull/301963) `#301963`
**ステータス:** Closed | **ラベル:** なし。

**概要:** `LanguageModelStatsService` の旧キー削除マイグレーションコードを除去する案。

**直近のアクティビティ:** Copilot reviewer のコメント。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Week2 atif](https://github.com/microsoft/vscode/pull/301873) `#301873`
**ステータス:** Closed | **ラベル:** なし。

**概要:** リポジトリを「Nexora」へリブランド + `nexora-core` 拡張追加というフォーク混入の不適切 PR。`.vs/` 等の IDE 副産物が含まれる。

**直近のアクティビティ:** Copilot reviewer のみ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[Bug] `chat.disableAIFeatures` is removed from user profile without user approval](https://github.com/microsoft/vscode/issues/309947) `#309947`
**ステータス:** Open | **ラベル:** なし。

**概要:** Insiders で `chat.disableAIFeatures` がユーザー承認なく削除され、AI 機能が再有効化される。

**直近のアクティビティ:** glektarssza が「#310226 マージとその後の経緯を受けて VS Code から完全移行する」と表明。

**次のアクション:** 調査継続。

---

### ![Issue](icons/issue-opened.svg) [Chronicle: Add command to trigger session reindexing](https://github.com/microsoft/vscode/issues/312825) `#312825`
**ステータス:** Closed | **ラベル:** feature-request, chronicle。

**概要:** Chronicle セッションを手動 reindex するコマンド追加要望。

**直近のアクティビティ:** vijayupadya が「有効化済み」とコメント。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[macOS 26.3.1] VS Code Insiders 1.118.0 crashes on startup — network process crash loop (exitCode: 9)](https://github.com/microsoft/vscode/issues/312720) `#312720`
**ステータス:** Closed | **ラベル:** bug, verified, macos。

**概要:** macOS 26.3.1 (Tahoe) で Insiders 1.118.0 が network process の `exitCode: 9` クラッシュループで起動不能に。

**直近のアクティビティ:** deepak1556 が CI 検証手順を `#313013` で更新、dmitrivMS が 1.119 への moving を案内。

**次のアクション:** 1.119 で再検証。

---

### ![Issue](icons/issue-opened.svg) [Agents Insider always shows "Update Available"](https://github.com/microsoft/vscode/issues/310947) `#310947`
**ステータス:** Open | **ラベル:** info-needed, install-update, agents-app。

**概要:** Homebrew で更新後も Agents (Insider) が「Update Available」を出し続ける。

**直近のアクティビティ:** deepak1556 が「最新 Insiders は cross-app update 実装済みなので再確認を」と返信。

**次のアクション:** 報告者再検証。

---

## 取得状況サマリー

- 全 9 リポジトリで取得成功（エラーなし）。
- microsoft/vscode は updated_at 降順で 50 PR + 50 Issue の上限に到達。
- commaai/panda は直近 24 時間のアクティビティなし。
