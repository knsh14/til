# GitHub ダイジェスト — 2026-04-25

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[build(openpilot): replace panda submodule with pip-distributed panda dependency](https://github.com/commaai/openpilot/pull/37908)** (commaai/openpilot) — 長年 submodule で扱ってきた panda を pip パッケージ化された `pandacan` に置き換える PR。検証 PR `commaai/panda#2392` と組み合わせ、bounty issue `commaai/panda#2239` を解決する流れで CI もすべて緑。
2. ![PR](icons/git-pull-request.svg) **[wifi: no NetworkManager](https://github.com/commaai/openpilot/pull/37902)** (commaai/openpilot) — comma 機の Wi-Fi 管理を NetworkManager から `wpa_supplicant` の ctrl-socket 直接利用 + `udhcpc` + `dnsmasq` に丸ごと置換する 4584 行追加・1599 行削除の大規模 PR（Draft）。UI 再起動を跨いでデーモンを引き継ぐ仕組みやテザリング状態の引き取りロジックなど、`#37752` `#37662` を引き継ぐ刷新提案。
3. ![Issue](icons/issue-opened.svg) **[Kernel panic in WLAN driver](https://github.com/commaai/openpilot/issues/35788)** (commaai/openpilot) — `comma 3X` が走行後にフリーズしたという報告に、別ユーザーが Starlink 短時間断で再現でき、さらに HTT TX 記述子が unmapped な HTT fragment-descriptor ページを指すと同じ WLAN SMMU フォールトクラスが再現できることを突き止めた詳細解析が追記。
4. ![Issue](icons/issue-opened.svg) **[Feature Request: Support querying dependencies of uninstalled packages](https://github.com/astral-sh/uv/issues/19140)** (astral-sh/uv) — `pipgrip` 相当の「インストールせずに依存関係を引きたい」要望に対し、PEP 658 の `.metadata` を sdist まで拡張する案や `pip install --dry-run` の代替案、PyPI 全体の SHA256 を Astral 側でホストする案など 6 件のコメントで活発に議論。
5. ![PR](icons/git-pull-request.svg) **[refactor(json)!: drop C-impl JSON de/serialization の続編 — 各種 LSP 改善 PR 群](https://github.com/neovim/neovim/pull/39391)** (neovim/neovim) — `vim.lsp.resolve_config` 実装を本流化する `feat(lsp): resolve lsp config`（#39391）と、それを受けたドラフト統合作業 `#33578` のクローズ、`supports_method` の自己マップ対応 `#39383` のマージ＋バックポートなど、Neovim 0.12 リリース直前の LSP 関連の集中的な整流化が進行。

---

## commaai/openpilot

> panda の pip パッケージ化や `usbgpu` 系列の探索、AGNOS 18.1 リリースとサーマル余裕拡大、UI まわりのリファクタが並行進行。同時に 24 日無活動 PR の `stale` 自動クローズが大量に流れた一日。

### ![PR](icons/git-pull-request.svg) [build(openpilot): replace panda submodule with pip-distributed panda dependency](https://github.com/commaai/openpilot/pull/37908) `#37908`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `.gitmodules` から panda を外し、`pyproject.toml` に `pandacan` を CI 検証用の fork ブランチでピン止めして追加。`SConstruct` を書き換えて panda パスを Python パッケージのインストール場所から動的解決し、`SConscript` を `variant_dir='panda'` の `duplicate=0` で呼び出すことで site-packages を改変せずに in-tree でファームウェアをビルドする。

**直近のアクティビティ:** process replay diff report が 0 changed / 66 passed / 0 errors。レビューコメントは未着。

**変更ファイル:** `.gitmodules`、`SConstruct`、`panda` （submodule 削除）、`pyproject.toml`、`uv.lock` の 5 ファイルで +63 / −6。

**次のアクション:** 検証 PR `commaai/panda#2392` のマージと並行してメンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [wifi: no NetworkManager](https://github.com/commaai/openpilot/pull/37902) `#37902`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `#37752` の解決 PR、`#37662` の続編。NetworkManager の Wi-Fi 管理を、`wpa_supplicant` の ctrl-socket クライアント、`udhcpc`、`dnsmasq` の直接呼び出しに置換する。`start_new_session=True` でデーモンを起動し UI 再起動でも生き残らせ、`_init_wifi_state` は ctrl-socket の attach で既存デーモンを引き取る。NM の systemd `wpa_supplicant.service` に誤って噛みつかないよう `_our_wpa_supplicant_running` でゲート。

**直近のアクティビティ:** UI preview で mici / big の動画差分が検出されドラフト段階を示す。process replay diff report は 0 changed / 66 passed / 0 errors。

**変更ファイル:** `selfdrive/ui/mici/layouts/settings/network/` 配下や `system/networkd/` など 25 ファイル変更で +4584 / −1599 の大規模パッチ。

**次のアクション:** ドラフトのままレビュー前フィードバック収集。

---

### ![Issue](icons/issue-opened.svg) [Kernel panic in WLAN driver](https://github.com/commaai/openpilot/issues/35788) `#35788`
**ステータス:** Open | **ラベル:** (なし)

**概要:** comma 3X が駐車後にフリーズして再起動した障害報告で、bootlog に複数の kernel oops/bug を含む。最初は単発障害として登録されていたが、その後 Wi-Fi 接続中の短時間スループット中断（Starlink の遮蔽復帰時など）で高確率に再現する WLAN ドライバ起因のクラッシュと判明している。

**直近のアクティビティ:** `suburbazine` が Starlink 環境で短時間断（木立や uplink retracing）の最中に再現することを報告。`zappybiby` が、未マッピングの HTT fragment-descriptor ページを指す HTT TX 記述子の publish が同等の WLAN SMMU フォールトクラスと fsynr を再現することを実験的に示し、別フォローアップで「障害発生時には常に単一 SSID のみで roaming も発生していなかった」とロジック検証。

**次のアクション:** 引き続き再現条件の絞り込みと、特定された SMMU フォールト経路への対策実装。

---

### ![PR](icons/git-pull-request.svg) [bump opendbc](https://github.com/commaai/openpilot/pull/37907) `#37907`
**ステータス:** Merged | **ラベル:** car

**概要:** `opendbc_repo` の参照を更新する 1 行 PR。`commaai/opendbc#3305` の取り込みを行うリビジョン更新。

**直近のアクティビティ:** `sshane` が proc replay の出処を `opendbc#3305` と明示。process replay は 2 件変動（HYUNDAI 1 セグメント差分）64 passed / 0 errors のまま許容範囲としてマージ。

**変更ファイル:** `opendbc_repo`（+1 / −1）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [usbgpu](https://github.com/commaai/openpilot/pull/37906) `#37906`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** USB 接続 GPU でモデル推論を行う `usbgpu` 系列の最新ドラフト。GPU ファームウェアをコンパイル時にダウンロードする必要があり、ビルドに約 9 分を要する点が留意事項として明記。

**直近のアクティビティ:** Model Review コメントで `dmonitoring_model.onnx` のメトリクスが master と PR で同等であることを確認。

**変更ファイル:** `selfdrive/modeld/SConscript`、`compile_modeld.py`、`dmonitoringmodeld.py`、`modeld.py` ほか 9 ファイルで +82 / −45。

**次のアクション:** ビルド時間短縮と動作検証を経てドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [bang-bang fan controller](https://github.com/commaai/openpilot/pull/37905) `#37905`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `system/hardware/fan_controller.py` に bang-bang 方式のファン制御を追加する +14 / −0 の小さな実験 PR。起動時の熱スパイクとフリート定常状態でのファン動作を可視化したグラフを添付。

**直近のアクティビティ:** process replay diff report は 0 changed / 66 passed / 0 errors で挙動変化なし。

**変更ファイル:** `system/hardware/fan_controller.py` のみ。

**次のアクション:** 本番デプロイ前のレビューと実機検証。

---

### ![PR](icons/git-pull-request.svg) [jp: 2x faster parsing](https://github.com/commaai/openpilot/pull/37904) `#37904`
**ステータス:** Merged | **ラベル:** tools

**概要:** `tools/jotpluggler` の動的 capnp パースをビルド時生成の静的パーサに置き換えて 2 倍高速化。`generate_event_extractors.py` を新規追加し、`sketch_layout.cc` から 356 行を削除して書き換え。

**直近のアクティビティ:** process replay diff report は変動なし。

**変更ファイル:** `tools/jotpluggler/SConscript`、`generate_event_extractors.py`（新規 +327）、`sketch_layout.cc`（+34 / −356）など 4 ファイルで +380 / −356。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [comma four: retune thermal limits](https://github.com/commaai/openpilot/issues/37737) `#37737`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** comma four で comma 3X 由来のサーマル制限を流用しているため一部ユーザーで上限到達が増えており、`OFFROAD_DANGER_TEMP` の見直しと黄/赤バンド調整、`agnos-kernel-sdm845` 側のスロットリングポイント調整を行う作業項目をまとめていた issue。

**直近のアクティビティ:** 直近24時間のコメントなし。`#37891`（mici サーマル上限引き上げ）のマージと併せてクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [modeld: remove deprecated tg flags](https://github.com/commaai/openpilot/pull/37903) `#37903`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `selfdrive/modeld/SConscript` から非推奨となった tinygrad 関連フラグを除去する +5 / −9 のドラフト整理 PR。

**直近のアクティビティ:** process replay diff report は 0 changed / 66 passed / 0 errors。

**変更ファイル:** `selfdrive/modeld/SConscript` のみ。

**次のアクション:** ドラフト解除とレビュー。

---

### ![PR](icons/git-pull-request.svg) [ui: DM icon turns orange briefly after awareness drops](https://github.com/commaai/openpilot/pull/37826) `#37826`
**ステータス:** Open | **ラベル:** ui

**概要:** ドライバーモニタリングの awareness が一定値を下回った直後に DM アイコンを短時間オレンジ色に点灯させる UI 調整。`selfdrive/ui/mici/onroad/driver_state.py` のロジック追加と、対応するアイコン PNG の差し替えを含む。

**直近のアクティビティ:** UI preview で mici / big とも動画差分検出。`sshane` が COMMENTED レビューを 4 件残している。

**変更ファイル:** `selfdrive/assets/icons_mici/onroad/driver_monitoring/dm_cone.png`、`selfdrive/ui/mici/onroad/driver_state.py`（+16 / −3）。

**次のアクション:** sshane のレビューコメントへの応答待ち。

---

### ![PR](icons/git-pull-request.svg) [AGNOS 18.1](https://github.com/commaai/openpilot/pull/37895) `#37895`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** AGNOS のマイナーリリース 18.1 を取り込む PR。rootfs サイズは +13 MB の 3.7G、Wi-Fi/Modem/サウンド/Factory reset などのリリースチェックリストはすべて完了済み。

**直近のアクティビティ:** process replay diff report は 0 changed / 66 passed / 0 errors。

**変更ファイル:** `launch_env.sh`、`system/hardware/tici/agnos.json`、`all-partitions.json`、`system/loggerd/SConscript` の 4 ファイル（+32 / −31）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Raise mici thermal limits](https://github.com/commaai/openpilot/pull/37891) `#37891`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `OFFROAD_DANGER_TEMP` を 10℃ 引き上げて熱で onroad 移行をブロックされていた 90% 超のユーザーを救済し、`THERMAL_BANDS` を mici 向けに調整して onroad 移行時の熱ピークでも engage を維持できるようにする。`agnos-kernel-sdm845#110` と組み合わせて Tmax 105℃ の CPU/GPU スロットリング限界に対し約 10℃ のマージンを確保。

**直近のアクティビティ:** `adeebshihadeh` と `elkoled` がコメントレビュー。UI preview で動画差分が検出されたが許容範囲としてマージ。

**変更ファイル:** `cereal/log.capnp`、`selfdrive/selfdrived/selfdrived.py`、`selfdrive/ui/layouts/sidebar.py`、`system/hardware/hardwared.py` の 4 ファイル（+23 / −19）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [usbgpu single jit](https://github.com/commaai/openpilot/pull/37267) `#37267`
**ステータス:** Closed (Draft) | **ラベル:** (なし)

**概要:** USB 接続 GPU 上に 1 つの JIT で `modeld` を載せる試行ドラフト。`tinygrad_repo` のサブモジュール切り替えと `launch_openpilot.sh` の調整、`modeld.py` 大幅改修を含むが、コンパイルとの突合検証や「speed nan」のデバッグなどがチェックリストに残っていた。

**直近のアクティビティ:** 直近24時間のコメントなし。新しい `usbgpu` 系列に置き換わってクローズ。

**変更ファイル:** 12 ファイルで +282 / −100、特に `modeld.py` が +224 / −58 と大きい。

**次のアクション:** `#37906` 等の新ドラフトに引き継いでクローズ。

---

### ![PR](icons/git-pull-request.svg) [usbgpu](https://github.com/commaai/openpilot/pull/37853) `#37853`
**ステータス:** Closed (Draft) | **ラベル:** (なし)

**概要:** `usbgpu` の hack を整理する中間版ドラフト。big モデルの再エクスポート、コンパイルフラグからのデバイス指定整理、ファームウェアのランタイムダウンロード排除、speed nan デバッグなどが TODO に残っていた。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 8 ファイルで +86 / −82。

**次のアクション:** 新ドラフト `#37906` に統合してクローズ。

---

### ![PR](icons/git-pull-request.svg) [dmonitoringmodeld: keep warp output/model input tensor](https://github.com/commaai/openpilot/pull/37874) `#37874`
**ステータス:** Closed (Draft) | **ラベル:** (なし)

**概要:** `dmonitoringmodeld` で warp 出力をモデル入力テンソルとして保持して再利用するドラフト。`compile_modeld.py` の +6 / −3 と `dmonitoringmodeld.py` の +2 / −1 程度の小規模改修。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `selfdrive/modeld/compile_modeld.py`、`selfdrive/modeld/dmonitoringmodeld.py`。

**次のアクション:** 新しい usbgpu 系統と `#37896` に整理されてクローズ。

---

### ![PR](icons/git-pull-request.svg) [modeld, dmonitoringmodeld: don't assume single device](https://github.com/commaai/openpilot/pull/37896) `#37896`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** デバイスをコンパイル時に選び、ランタイムでは JIT から取得する形に書き換える前提整備 PR。usbgpu で QCOM 上で warp を走らせつつ AMD 上でモデルを走らせる構成のための前段。

**直近のアクティビティ:** `Armandpl` が「`get_jit_input_devices` 実装は脆い」と指摘し、tinygrad 側がテンソル配置先を常に把握する前提と分離すべきだとフィードバック。

**変更ファイル:** `selfdrive/modeld/SConscript` ほか 6 ファイル（+44 / −61）。

**次のアクション:** 設計を見直すためクローズ。

---

### ![Issue](icons/issue-opened.svg) [Improve dcam processing pipeline](https://github.com/commaai/openpilot/issues/36742) `#36742`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** dcam 画像処理パイプラインの改善トラッカー。BPS の linearization と gamma を済ませ、最後に IPE 立ち上げを伴う downscale を試みる、というチェックリストを管理していた。

**直近のアクティビティ:** 直近24時間のコメントなし。チェック項目がすべて済んだためクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [updated: use configured remote instead of hardcoded origin](https://github.com/commaai/openpilot/pull/37720) `#37720`
**ステータス:** Closed | **ラベル:** stale

**概要:** `system/updated/updated.py` がリモート名を `origin` でハードコードしていたため、フォークでリモート名が異なる場合に更新が失敗していた問題を修正し、`branch.<name>.remote` の git config から取得するように変更する PR。

**直近のアクティビティ:** 24日無活動の自動警告に続き、再度の自動コメントで非アクティビティのためクローズ。

**変更ファイル:** `system/updated/updated.py`（+12 / −4）。

**次のアクション:** 再開には re-open が必要。

---

### ![PR](icons/git-pull-request.svg) [modeld: increase model smooth](https://github.com/commaai/openpilot/pull/37724) `#37724`
**ステータス:** Closed | **ラベル:** stale

**概要:** モデル推論結果のスムージング設定を増やすことで model antilag を緩和する 1 行 PR。`timestampEof` が平均 50ms 遅れていたり、モデル予測が `livePose` に対し追加で約 50ms 遅れていたりという背景の修正試行。

**直近のアクティビティ:** 自動 stale クローズ。

**変更ファイル:** `selfdrive/modeld/modeld.py`（+1 / −1）。

**次のアクション:** 再開には re-open が必要。

---

### ![PR](icons/git-pull-request.svg) [test_models: fuzz openpilot TX against panda on replayed routes](https://github.com/commaai/openpilot/pull/37726) `#37726`
**ステータス:** Closed | **ラベル:** CI / testing, car, stale

**概要:** `selfdrive/car/tests/test_models.py` にリプレイされたルート CAN 上で `CarInterface` と panda safety を回し、openpilot が出す TX を `safety_tx_hook` と突合する fuzz カバレッジを追加。Toyota の角度制御リグレッションも accepted/blocked/recovered/inactive の LTA TX 経路を辿る形で導入。`#32425` の解決を意図。

**直近のアクティビティ:** 自動 stale クローズ。

**変更ファイル:** `selfdrive/car/tests/test_models.py`（+234 / 0）。

**次のアクション:** 再開には re-open が必要。

---

### ![PR](icons/git-pull-request.svg) [deleter: handle non-openpilot created files](https://github.com/commaai/openpilot/pull/37728) `#37728`
**ステータス:** Closed | **ラベル:** stale

**概要:** `system/loggerd/deleter.py` が log root に紛れ込んだ `.tar.gz` などの非ディレクトリエントリで `NotADirectoryError` を起こしてクラッシュする問題（`#30102`）を、`os.listdir` 周辺の `try/except` 化と symlink・通常ファイル個別ハンドリングで解消する PR。

**直近のアクティビティ:** 自動 stale クローズ（同様の修正を狙った別の PR `#37736` と並走していた）。

**変更ファイル:** `system/loggerd/deleter.py`（+26 / −4）と新規テスト `test_deleter.py`（+104 / 0）。

**次のアクション:** 再開には re-open が必要。

---

## commaai/opendbc

> Hyundai/Honda の車種追加と `pycapnp` の unpin、依存テスト基盤の整理が並行する一日。

### ![PR](icons/git-pull-request.svg) [HKG: Hyundai Santa Fe 2022 Fingerprint](https://github.com/commaai/opendbc/pull/3354) `#3354`
**ステータス:** Open | **ラベル:** car, hyundai, fingerprint

**概要:** `opendbc/car/hyundai/fingerprints.py` に Hyundai Santa Fe 2022 のフィンガープリントを 1 行追加。検証ルートとドングル ID 付きのコントリビュータ PR。

**直近のアクティビティ:** Car behavior report は 260 セグメントの再生で挙動差分なし。Bot レビューは PR をドラフトに戻すよう案内。

**変更ファイル:** `opendbc/car/hyundai/fingerprints.py`（+1 / 0）。

**次のアクション:** 既存メンテナによる承認待ち。

---

### ![PR](icons/git-pull-request.svg) [Max torque rate down test](https://github.com/commaai/opendbc/pull/2398) `#2398`
**ステータス:** Open (Draft) | **ラベル:** car safety

**概要:** `opendbc/safety/tests/common.py` に最大トルク減少率のテストを追加するドラフト。本来 safety 側で rate down がチェックされていない欠落を可視化する目的で、現状ではテストが意図的に失敗する状態。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `opendbc/safety/tests/common.py`（+16 / −1）。

**次のアクション:** safety 側の `rate down` チェック実装を待ってテストをパスさせる。

---

### ![PR](icons/git-pull-request.svg) [try to remove hypothesis](https://github.com/commaai/opendbc/pull/3357) `#3357`
**ステータス:** Open | **ラベル:** car, ford, hyundai, toyota

**概要:** `hypothesis` ベースのプロパティテストを使っている Ford / Hyundai / Toyota / 共通 car テストから依存を取り除き、`opendbc/testing.py` に置き換え用ヘルパーを追加するリファクタ。`+134 / −151` で実質減量。

**直近のアクティビティ:** Car behavior report は 541 セグメントを 4 メーカー横断で回し挙動差分なし。

**変更ファイル:** `opendbc/car/{ford,hyundai,toyota}/tests/test_*.py`、`opendbc/car/tests/test_car_interfaces.py`、`opendbc/testing.py` の 7 ファイル。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [unpin pycapnp](https://github.com/commaai/opendbc/pull/3358) `#3358`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `pyproject.toml` と `uv.lock` で固定していた `pycapnp` のピンを外す 2 行変更。

**直近のアクティビティ:** Car behavior report は変化なし。

**変更ファイル:** `pyproject.toml`、`uv.lock`（+2 / −2）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [DBC files don't follow the spec](https://github.com/commaai/opendbc/issues/151) `#151`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** リポジトリの DBC ファイルがオリジナルの CANdb++ Editor で開けない、というスペック追従の指摘 issue。多くのパーサは順序を強制しないが本家エディタはセクションの並びを強制するという背景説明付き。

**直近のアクティビティ:** 直近24時間のコメントなし。整理として古い issue がクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Honda - Additional 2026 community models](https://github.com/commaai/opendbc/pull/3356) `#3356`
**ステータス:** Merged | **ラベル:** car

**概要:** Acura RDX 2026（コミュニティ PR `#3316`）と Honda Pilot 2026（`#3330`）を追加 cars として登録する。`opendbc/car/extra_cars.py` 1 ファイル変更。

**直近のアクティビティ:** Car behavior report は変化なし。

**変更ファイル:** `opendbc/car/extra_cars.py`（+2 / −1）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Honda: Fit 2024 - 2025 (Taiwan)](https://github.com/commaai/opendbc/pull/3113) `#3113`
**ステータス:** Open | **ラベル:** car, honda

**概要:** 台湾仕様 Honda Fit 2024-25 のサポート追加。Factory max LKAS は 3840、`#1147` の影響を受けるが `#3114` の修正で対処済み。`alphalongitudinal` 有効時の 5km/h 以下で工場 ACC 非対応領域に触れる haptic feedback が見られるが、Ridgeline と Brazil City と同等で実走に影響なしと注記。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `opendbc/car/honda/{fingerprints.py,interface.py,values.py}`、`opendbc/car/tests/routes.py`、`opendbc/car/torque_data/override.toml` の 5 ファイル（+38 / −2）。

**次のアクション:** メンテナによる検証ルート確認とレビュー待ち。

---

## commaai/panda

> openpilot の panda submodule 撤去（`commaai/openpilot#37908`）と連動した pip パッケージ整備の 1 日。

### ![Issue](icons/issue-opened.svg) [Setup proper pip package](https://github.com/commaai/panda/issues/2239) `#2239`
**ステータス:** Open | **ラベル:** enhancement, bounty

**概要:** `pandacan` は既に pip パッケージとして公開されているが、ファームウェア等が含まれず `openpilot` の submodule を置き換えるには不十分。これらをすべて含めたパッケージにし、openpilot 側の検証 PR で CI を緑にすることがゴールの bounty issue。

**直近のアクティビティ:** `adwit-agg` が `commaai/panda#2392` と `commaai/openpilot#37908` を提出し、両方 CI 緑であることを報告。最近の bounty 試行が動かない bot 投稿が多いことに触れ、自身は実機検証済みであると強調。

**次のアクション:** `#2392` と `commaai/openpilot#37908` のレビューとマージで解決見込み。

---

### ![PR](icons/git-pull-request.svg) [build(panda): fix out-of-tree SCons paths and include firmware assets in wheel](https://github.com/commaai/panda/pull/2392) `#2392`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `include-package-data = true` ではなく明示的なワイルドカード globbing を使い、C ソース・ヘッダ・リンカ・SConscript などの非 Python 資材を `__init__.py` の有無に関わらず確実に wheel に含める。証明書・署名スクリプトの参照を相対パスから絶対パスに切り替え、site-packages にインストールされた状態でも src を解決できるようにし、out-of-tree で組み立てる際に GCC の `CPPPATH` に src 絶対パスを注入する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `SConscript`（+21 / −10）、`pyproject.toml`（+11 / −9）。

**次のアクション:** `commaai/openpilot#37908` の CI と合わせてレビュー。

---

### ![PR](icons/git-pull-request.svg) [setup: proper pip package for openpilot submodule replacement](https://github.com/commaai/panda/pull/2391) `#2391`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `#2239` の解決 PR。`__init__.py` に `INCLUDE_PATH`（`opendbc.INCLUDE_PATH` と同じパターン）を追加、PEP 561 用の `py.typed` マーカーを設置、`pyproject.toml` の `package-data` に C・ヘッダ・リンカスクリプト・証明書などのボードソースを網羅的に登録、版を `0.0.11` に更新。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `__init__.py`（+5）、`py.typed`（新規）、`pyproject.toml`（+5 / −3）。

**次のアクション:** `#2392` とレビュー統合。

---

## neovim/neovim

> 0.12 リリース直前の整流期。LSP の `supports_method` バグ修正（`#39383`）と一連のバックポート、`nvim_create_user_command` / `nvim_set_keymap` の `LuaRef` リーク修正、TUI の `ttyfast` 復活、Windows 側のパス正規化マージなどが集中。`vim.lsp.resolve_config` も再 PR 化。

### ![PR](icons/git-pull-request.svg) [docs(vim.ui): document an interface for `vim.ui.select` preview](https://github.com/neovim/neovim/pull/37360) `#37360`
**ステータス:** Open | **ラベル:** documentation, ui, revisit-at-release, lua, select-picker

**概要:** プラグインがファジーピッカーで詳細プレビューを提供したいというニーズに応え、`vim.ui.select` の `opts.preview_item` 関数を文書化する。`runtime/doc/lua.txt` と `runtime/lua/vim/ui.lua` を更新する +36 / 0 のドキュメント PR。

**直近のアクティビティ:** `MariaSolOs` と `echasnovski` がインライン COMMENTED レビュー（テキストなし）。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/lua/vim/ui.lua`。

**次のアクション:** レビュー対応とリリース前の見直し（`revisit-at-release`）。

---

### ![PR](icons/git-pull-request.svg) [feat(lua): make `vim.defer_fn` handle more execution conditions](https://github.com/neovim/neovim/pull/37618) `#37618`
**ステータス:** Open | **ラベル:** lua

**概要:** `vim.defer_fn` で「イベント発火時に一回だけ」のような遅延実行条件を扱えるようにする提案。`vim.pack` での lazy load でも `runtimepath` 増加の影響を抑える用途を想定。`vim/_core/editor.lua` で +81 / −16 と本体ロジックを大きく拡張。

**直近のアクティビティ:** `justinmk` が「`nvim_create_autocommand` にラッピング実行のオプションを追加するほうがクリーンで広く使えるのでは」と CHANGES_REQUESTED。`echasnovski` も COMMENTED レビュー。

**変更ファイル:** `runtime/doc/lua.txt`、`news.txt`、`runtime/lua/vim/_core/editor.lua`。

**次のアクション:** API デザインの差し替え可否を含めた再検討。

---

### ![PR](icons/git-pull-request.svg) [feat(keymap): add `force` to control whether to override present mapping](https://github.com/neovim/neovim/pull/36394) `#36394`
**ステータス:** Open | **ラベル:** lua

**概要:** プラグインがユーザーマッピングを上書きしないよう「既存マッピングが無いときだけ作成する」を簡単に表現できるよう、`vim.keymap.set` に `force` 相当のオプションを追加する。`vim.fn.maparg()` がバッファ局所/グローバルを区別しない問題への対処。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `runtime/doc/lua.txt`、`news.txt`、`runtime/lua/vim/keymap.lua`、テスト（+53 / −2）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add `fast` option to `vim.filetype.match()`](https://github.com/neovim/neovim/pull/30086) `#30086`
**ステータス:** Open | **ラベル:** performance, filetype

**概要:** `vim.filetype.match()` に `fast=true` オプションを追加して、テーブル検索より遅いパターンマッチと内容マッチをスキップ可能にする。多くの実環境で十分なカバレッジを保ちつつ起動・スキャン時の性能を改善する狙い。

**直近のアクティビティ:** `dundargoc`、`telemachus` から COMMENTED レビュー（テキストなし）。

**変更ファイル:** `runtime/doc/lua.txt`、`news.txt`、`runtime/lua/vim/filetype.lua`、`filetype/detect.lua`（+67 / −13）。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [feat(events): add `MacroEnter` and `MacroLeave` events](https://github.com/neovim/neovim/pull/24565) `#24565`
**ステータス:** Open | **ラベル:** events

**概要:** マクロ録画用イベントは存在するが再生開始/終了に対応するものがないため、`MacroEnter` / `MacroLeave` 自動コマンドを追加する。`reg_executing()` だけでは判定できない「マクロ中に実行されたくないマッピングを一時無効化する」ような用途を想定。

**直近のアクティビティ:** 直近24時間のコメントなし（COMMENTED レビューのみ）。

**変更ファイル:** `runtime/doc/{autocmd,news,repeat,vim_diff}.txt` など 9 ファイル（+62 / 0）。

**次のアクション:** API レビューと実装の合意形成。

---

### ![Issue](icons/issue-opened.svg) [smarter cmdline abbreviation](https://github.com/neovim/neovim/issues/39394) `#39394`
**ステータス:** Open | **ラベル:** mappings, cmdline-mode

**概要:** `cabbrev` は cmdline 内のどこにある対象でも置換するため意図せず `cmd x y` のような誤置換が起きる。コマンド先頭部分のみを置換する適切な API があれば、関連する数多くの拡張要望（コマンド中身の補正など）を一気に解決できる、という整理。

**直近のアクティビティ:** `echasnovski` が `mini.cmdline` の autocorrection コンセプトを参照として提示。

**次のアクション:** 仕様議論を進めて実装方針を固める。

---

### ![Issue](icons/issue-opened.svg) [unreliable test: `pack_spec.lua: after_each`: "EBUSY: resource busy or locked"](https://github.com/neovim/neovim/issues/39380) `#39380`
**ステータス:** Open | **ラベル:** test, ci, packages

**概要:** Windows CI の `pack_spec.lua` の `after_each` フックで `EBUSY: resource busy or locked` が発生し続けるフレーキーテスト。先に修正された `#39114` とは別ケースで、`Xtest_xdg_plugin` の cleanup が引っかかる。

**直近のアクティビティ:** `echasnovski` がタイムアウト延長か Windows CI スキップしか思いつかないとコメント。`justinmk` は `#39042` と同根で、`n.rmdir()` でも解決できなかったと記している。

**次のアクション:** Windows 限定でタイムアウト緩和もしくはスキップ条件追加の判断。

---

### ![PR](icons/git-pull-request.svg) [feat(server): return detailed info from serverlist({info=true})](https://github.com/neovim/neovim/pull/39392) `#39392`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `serverlist({info=true})` が返す情報を拡張し、ローカルソケットだけでなく PID やそのサーバが現インスタンス由来かどうかなどの情報を UI ツールに渡せるようにする。`#39378`（`--remote-ssh`）に引き渡すための前処理にあたる。

**直近のアクティビティ:** 投稿者 `szymonwilczek` が CI 緑化と TUI :restart のフレーキー再実行を経て準備完了と報告。

**変更ファイル:** `runtime/lua/vim/_core/server.lua`、`runtime/doc/{vimfn,news}.txt`、メタ生成 lua（+125 / −12）。

**次のアクション:** `justinmk` の最終レビュー。

---

### ![Issue](icons/issue-opened.svg) [vim.pack: expose pending updates via API](https://github.com/neovim/neovim/issues/39285) `#39285`
**ステータス:** Open | **ラベル:** api, packages

**概要:** `vim.pack.update()` がインタラクティブに保留中アップデートを表示する一方、機械可読 API がないため、ステータスライン更新カウンタやダッシュボードウィジェットなどを作れない。プログラマブルな取得 API を要望。

**直近のアクティビティ:** `rseichter` が同期コメントとして `#39370` を作っていたと共有。`haarisr` が実装に貢献したい旨を表明し、`echasnovski` は急がないが対応するという方針。

**次のアクション:** API 形状の合意と実装担当の確定。

---

### ![PR](icons/git-pull-request.svg) [ci: ensure full sanitizer logs are shown in CI](https://github.com/neovim/neovim/pull/39361) `#39361`
**ステータス:** Open | **ラベル:** ci, AI assisted

**概要:** ASAN が子プロセスでエラーを検出した際、テスト teardown でプロセスが先に殺されてログが切れるため、フルなスタックトレースが残らない問題を修正する。サニタイザ出力を stdout だけでなく確実に保存するよう CI ヘルパを変更する。

**直近のアクティビティ:** `zeertzjq` が「新解は `#39387` で実装した」と通知（`uv_stream` と `stream->uv.idle` が同アドレスである点や `close_cb()` のコメントを参照）。`justinmk` は関連コードのリーク可能性を指摘。

**変更ファイル:** `test/testutil.lua`（+51 / −20）。

**次のアクション:** `#39387` のマージを受けて閉じるか、追加修正を続けるか判断。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(channel): stack-buffer-overflow with exit during connection](https://github.com/neovim/neovim/pull/39393) `#39393`
**ステータス:** Merged | **ラベル:** target:release

**概要:** `#39387` を `release-0.12` 系列にバックポートする PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `src/nvim/event/socket.c`、`stream.c`、`test/functional/core/channels_spec.lua`（+45 / −17）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(channel): stack-buffer-overflow with exit during connection](https://github.com/neovim/neovim/pull/39387) `#39387`
**ステータス:** Merged | **ラベル:** channels-rpc, ci:backport release-0.12

**概要:** Nvim がソケット接続中に終了するとスタックバッファオーバーフローが起きる問題を修正。ハンドルを Stream に紐づけ、Stream 内部の `internal_close_cb` で「closed」状態を更新するよう変更。

**直近のアクティビティ:** バックポート PR `#39393` が自動生成。`zeertzjq` が、初期 probe 接続のハンドル `watcher->uv.pipe.handle` には Stream が紐付いていないという以前の指摘を訂正。`justinmk` から APPROVED。

**変更ファイル:** `src/nvim/event/socket.c`、`stream.c`、テスト（+45 / −17）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [unsetting 'guicursor' does not disable cursor shape change](https://github.com/neovim/neovim/issues/38987) `#38987`
**ステータス:** Open | **ラベル:** tui, has:plan, bug-regression, lifecycle, options

**概要:** Neovim 0.12 へのアップデートで `vim.opt.guicursor = ''` が効かなくなり、ターミナルから抜けたあともカーソル形状が block のままになるという回帰報告。

**直近のアクティビティ:** ユーザー間で意見が分かれ、`justinmk` が「これはバグトラッキングのために開けてある issue で、雑談コメントは要らない」と一蹴。

**次のアクション:** 元のリセット動作の復元実装。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): make code_action context.diagnostics optional](https://github.com/neovim/neovim/pull/39368) `#39368`
**ステータス:** Open | **ラベル:** needs:response, lsp, AI assisted

**概要:** `vim.lsp.buf.code_action` は `context.diagnostics` を省略しても `vim.diagnostic.get()` から推測できるが、型が `lsp.CodeActionContext` にマップされており LuaLS で必須欠損警告が出ていた。`code_action` 専用に `diagnostics` を任意とする型を定義する。`#39208` の修正。

**直近のアクティビティ:** `justinmk` が「`make doc` を実行して lint を直す必要あり」と指摘。

**変更ファイル:** `runtime/lua/vim/lsp/buf.lua`（+4 / −1）。

**次のアクション:** CI 修正と再レビュー。

---

### ![PR](icons/git-pull-request.svg) [perf(lsp): avoid unnecessary string allocations when parsing content-length](https://github.com/neovim/neovim/pull/39389) `#39389`
**ステータス:** Open | **ラベル:** lsp

**概要:** RPC クライアントが `Content-Length` を解析する際、`tostring + find` で新規文字列を確保していたのを `buf:ref()` でバイト単位走査して PUC Lua でも動くよう shim を補強する形で削減。

**直近のアクティビティ:** `ofseed` がベンチマークスクリプトを共有し旧 `tostring+find` と新 `ref+scan` の差分を提示。`strbuf:get(header_len)` の残り 1 件のアロケーションは consume パターンに合うので `#39000` で更に削減できると展望。`justinmk` が APPROVED。

**変更ファイル:** `runtime/doc/news.txt`、`runtime/lua/vim/_core/stringbuffer.lua`、`runtime/lua/vim/lsp/rpc.lua`（+56 / −23）。

**次のアクション:** マージ前最終チェック。

---

### ![PR](icons/git-pull-request.svg) [fix(path): normalize separator to slash on Windows](https://github.com/neovim/neovim/pull/37729) `#37729`
**ステータス:** Merged | **ラベル:** platform:windows, revisit-at-release

**概要:** Windows でパス区切り文字が混在することによる多数の不具合（`#32590` `#28384` `#25033` `#23639` 等）を解消するため、内部処理を常時スラッシュに正規化する大規模パッチ。表示用は維持しつつ意味用のパスは forward slash で揃える方針。

**直近のアクティビティ:** `justinmk` が「`TO_SLASH` のインプレース置換のいくつかは慎重に見直す必要あり」「テスト変更の中にはテストの主旨を無効化するものがある」と複数の COMMENTED レビュー。`ntdiary` も応答しつつ追加修正。

**変更ファイル:** `src/nvim/{arglist,cmdexpand,...}` ほか 41 ファイル（+201 / −214）。

**次のアクション:** マージ済み（リリース前の `revisit-at-release` フォロー要）。

---

### ![PR](icons/git-pull-request.svg) [build: add libghostty-vt](https://github.com/neovim/neovim/pull/38408) `#38408`
**ステータス:** Open (Draft) | **ラベル:** build

**概要:** Ghostty の VT コア `libghostty-vt` をオプションでビルド可能にする探索 PR。`-D ENABLE_GHOSTTY=ON` で有効化する。

**直近のアクティビティ:** `clason` が PR をリベースし libghostty を更新中であることをコメント。Zig ネイティブビルドへの統合は Neovim 側 0.16 への移行と Ghostty 側 0.14（推測）残留により当面ブロック中。

**変更ファイル:** `cmake.deps/CMakeLists.txt` ほか 14 ファイル（+72 / −9）。

**次のアクション:** Ghostty 側の Zig バージョン進捗待ち。

---

### ![PR](icons/git-pull-request.svg) [backport: test: add finally() to meta file](https://github.com/neovim/neovim/pull/39390) `#39390`
**ステータス:** Merged | **ラベル:** test, target:release

**概要:** `#39388` を 0.12 系列にバックポートする 3 行 PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `test/_meta.lua`（+3 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): handle self-mapped methods in supports_method](https://github.com/neovim/neovim/pull/39383) `#39383`
**ステータス:** Merged | **ラベル:** lsp, ci:backport release-0.12

**概要:** LSP クライアントが `supports_method` を判定する際、`shutdown` のような自己対応メソッド（サーバ capability に対応する項目を持たない）を誤って未対応とみなしていた。動的登録は正しいが静的登録で誤検知が起きていた問題を修正。

**直近のアクティビティ:** バックポート PR `#39385` が自動生成。`justinmk` が「読みやすくなって良い」とコメント。

**変更ファイル:** `runtime/lua/vim/lsp/client.lua`、`test/functional/plugin/lsp_spec.lua`（+17 / −4）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build(zig): run oldtests in zig builds (RETROMANIA HYPERDRIVE)](https://github.com/neovim/neovim/pull/39074) `#39074`
**ステータス:** Open (Draft) | **ラベル:** build, ci

**概要:** タイトルに反し本質は Zig 移行ではなく、Makefile・shell スクリプト・vimscript wrapper で構成されていた oldtests のラッパー一式を、`--headless` Nvim で `:terminal` 上に展開していた回りくどい構造を Lua ランナー 1 本に統合するリファクタ。

**直近のアクティビティ:** `justinmk` と `clason` が COMMENTED レビュー（テキストなし）。

**変更ファイル:** `test/old/runner.lua`（新規 +160）、ほか CI ワークフローと treesitter テスト（+186 / −9）。

**次のアクション:** ドラフト解除に向けた整理継続。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): resolve lsp config](https://github.com/neovim/neovim/pull/39391) `#39391`
**ステータス:** Open | **ラベル:** lsp

**概要:** クローズされた `#33578` を引き継ぎ、`vim.lsp.Config` から `vim.lsp.ClientConfig` へ解決するロジックを `vim.lsp.lua` に整理して再提案する +65 / −46 の PR。

**直近のアクティビティ:** `justinmk` と `TheRealLorenz` が COMMENTED レビュー（テキストなし）。

**変更ファイル:** `runtime/doc/lsp.txt`、`runtime/doc/news.txt`、`runtime/lua/vim/lsp.lua`。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(build): add `_core/time.lua` to gettext](https://github.com/neovim/neovim/pull/39384) `#39384`
**ステータス:** Merged | **ラベル:** build, localization

**概要:** `#39331` で `_core/time.lua` を xgettext に登録し忘れていたのを 1 行追加するフォローアップ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `src/nvim/po/CMakeLists.txt`（+1 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): add `vim.lsp.resolve_config`](https://github.com/neovim/neovim/pull/33578) `#33578`
**ステータス:** Closed (Draft) | **ラベル:** revisit-at-release, lsp

**概要:** `vim.lsp.Config` から `vim.lsp.ClientConfig` への変換 API を提案するドラフト。`vim.lsp.start` を手動呼び出す際の整合性向上を目的としていた。

**直近のアクティビティ:** `clason` が「クローズ済 PR には force push できないので新規 PR を開いてほしい」と指摘し、`TheRealLorenz` がリベース済みだが re-open できない旨を返答。

**次のアクション:** `#39391` に引き継いでクローズ。

---

### ![PR](icons/git-pull-request.svg) [fix(api): adjust VIsual position in nvim_buf_set_text](https://github.com/neovim/neovim/pull/30690) `#30690`
**ステータス:** Open (Draft) | **ラベル:** api

**概要:** `nvim_buf_set_text` で visual selection の前にテキストを挿入したとき、もう一方の端が同列に取り残されてカーソルだけ動く挙動を、`fix_cursor_cols()` に visual 端点同期ロジックを追加して修正する（`#29558` の対応）。

**直近のアクティビティ:** `justinmk` が COMMENTED レビュー（テキストなし）。

**変更ファイル:** `src/nvim/api/buffer.c`、テスト（+183 / −57）。

**次のアクション:** ドラフト解除と再レビュー。

---

### ![PR](icons/git-pull-request.svg) [test: add finally() to meta file](https://github.com/neovim/neovim/pull/39388) `#39388`
**ステータス:** Merged | **ラベル:** test, ci:backport release-0.12

**概要:** `test/_meta.lua` に `finally()` を追加するテスト整備。バックポート PR `#39390` も自動生成。

**直近のアクティビティ:** `justinmk` が APPROVED。

**変更ファイル:** `test/_meta.lua`（+3 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): handle optional diagnostics in CodeActionContext](https://github.com/neovim/neovim/pull/39366) `#39366`
**ステータス:** Open | **ラベル:** needs:response, lsp

**概要:** ドキュメントでは optional とされている `context.diagnostics` を実装側でも省略時に空テーブルにフォールバックさせ、空でない場合のみ条件分岐するよう書き換える。

**直近のアクティビティ:** `justinmk` が「対応する open issue はあるか、CI 失敗を見たか」と質問し、`rio767` は文書化された挙動への追従であり特定 issue とは紐付かないと回答。

**変更ファイル:** `runtime/lua/vim/lsp/buf.lua`（+6 / −4）。

**次のアクション:** CI 修正と紐付け issue の有無確認。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): implement native SSH orchestration (--remote-ssh)](https://github.com/neovim/neovim/pull/39378) `#39378`
**ステータス:** Open (Draft) | **ラベル:** remote, ux-visibility, AI assisted

**概要:** VSCode Remote SSH 相当（`#21635`）のリモート編集セッションを Neovim で扱えるようにする `--remote-ssh` 機構の本実装ドラフト。手動 bootstrap・トンネル・UI attach をオーケストレーションする。`runtime/doc/remote_ssh.txt` を新規追加し、`runtime/lua/vim/net/_remote.lua` を 484 行で新設。

**直近のアクティビティ:** `justinmk` が「まず `serverlist()` に `info` パラメータを足し、`:connect` の picker でそれを呼ぶ流れにすべき」と段階分割を提案。投稿者が並走 PR `#38454` の確認を進めると応答し、引数なし `:connect` でホストピッカーを出すデザインに同意。

**変更ファイル:** `runtime/doc/remote_ssh.txt`（新規 +139）、`runtime/lua/vim/net/_remote.lua`（新規 +484）ほか 11 ファイル（+1385 / 0）。

**次のアクション:** `serverlist()` 拡張（`#39392`）の取り込みを待ってから段階適用。

---

### ![Issue](icons/issue-opened.svg) [Double input for certain keys, like enter, backspace and tab when using kitty.](https://github.com/neovim/neovim/issues/32143) `#32143`
**ステータス:** Closed | **ラベル:** duplicate

**概要:** kitty 上で enter/backspace/tab 等が二重入力されるという 2025 年付近からの回帰報告。

**直近のアクティビティ:** 別ユーザーが「kitty keyboard protocol を無効化する init.lua スニペットで回避できた」と共有。重複として処理されクローズ。

**次のアクション:** 関連 issue でフォロー。

---

### ![Issue](icons/issue-opened.svg) [remove attributes from highlight groups](https://github.com/neovim/neovim/issues/16481) `#16481`
**ステータス:** Open | **ラベル:** has:plan, core, highlight

**概要:** `indent-blankline` のような virtual text を使うプラグインで、italic / underline などの GUI オプションを抑止しつつ他のハイライト要素を許容するために、ハイライトグループから属性だけを取り除けるようにする要望。

**直近のアクティビティ:** 30 日間返答待ちで bot が一旦クローズ。

**次のアクション:** 情報提供があれば再オープン。

---

### ![Issue](icons/issue-opened.svg) [restore 'ttyfast'](https://github.com/neovim/neovim/issues/1045) `#1045`
**ステータス:** Closed | **ラベル:** defaults, refactor

**概要:** `ttyfast` をデフォルト ON に戻し、`vim_is_fastterm` / `vim_is_vt300` のような古い判定を削除するという長年の願いをトラッキングしていた issue。

**直近のアクティビティ:** `ddevault` が「フォークの維持工数が大幅に減る」と謝意。`justinmk` が `#38699` のマージで `nottyfast` を使えると案内し、フォーク不要になったと共有。

**次のアクション:** 解決クローズ。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): handle optional diagnostics in CodeActionContext](https://github.com/neovim/neovim/pull/39369) `#39369`
**ステータス:** Closed | **ラベル:** lsp

**概要:** `#39366` の重複 PR。1 行修正版だった。

**直近のアクティビティ:** `justinmk` が「`#39366` の重複、人間か確認を」とコメントし、投稿者が「人間です」と返答。重複としてクローズ。

**次のアクション:** `#39366` を本筋として進める。

---

### ![PR](icons/git-pull-request.svg) [feat(ui): implement vim.ui.img api](https://github.com/neovim/neovim/pull/37914) `#37914`
**ステータス:** Open | **ラベル:** ui, revisit-at-release

**概要:** kitty graphics protocol 上に `vim.ui.img` 同期 API（`load` / `place` / `hide` / `get` / `images`）を実装する。先行 PR `#31399` のクリーン版で、`#30889` の解決を狙う。

**直近のアクティビティ:** `stevenxxiu` が「Tmux 対応コードが落とされる場合、`term_send()` を hook できる導線を残してほしい」と要望。投稿者 `chipsenkbeil` がレビュー指摘を反映した新コミットを 1 つにまとめたと報告。

**変更ファイル:** `runtime/lua/vim/ui/img.lua`（新規 +127）、`runtime/doc/lua.txt`（+76）など 7 ファイル（+656 / −1）。

**次のアクション:** `justinmk` 等の追加レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [refactor(lsp): message stream abstraction for message framing](https://github.com/neovim/neovim/pull/39000) `#39000`
**ステータス:** Open | **ラベル:** refactor, lsp

**概要:** JSON-RPC クライアントから LSP の base protocol レイヤ（メッセージフレーミング）を切り出し、`vim.MessageStream` 抽象として `vim/_transport.lua` を導入する整理。

**直近のアクティビティ:** `lewis6991` が APPROVED、`mfussenegger` と `ofseed` が COMMENTED レビュー。

**変更ファイル:** `runtime/lua/vim/_transport.lua`（rename +80 / −27）、`runtime/lua/vim/lsp/rpc.lua`（+74 / −71）。

**次のアクション:** マージ調整。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39257) `#39257`
**ステータス:** Open (Draft) | **ラベル:** vim-patch

**概要:** vim 本家の 9.2.0375 / 9.2.0376 / 9.2.0381 のパッチ取り込みに伴う `version.c` 更新ドラフト。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `src/nvim/version.c`（+85 / −93）。

**次のアクション:** ドラフト解除待ち。

---

### ![Issue](icons/issue-opened.svg) [Cursor hightlight attribute (hl_attr_define) not sent to remote UI (nvim-qt)](https://github.com/neovim/neovim/issues/19779) `#19779`
**ステータス:** Closed | **ラベル:** ui, channels-rpc, needs:response, ui-extensibility, highlight

**概要:** リモート UI（nvim-qt）にカーソルハイライト属性が送られないという 2022 年からの古い report。

**直近のアクティビティ:** 30 日無回答で bot がクローズ。

**次のアクション:** 情報提供で再オープン可能。

---

### ![Issue](icons/issue-opened.svg) [nvim doesn't resize to full terminal width upon opening](https://github.com/neovim/neovim/issues/29247) `#29247`
**ステータス:** Closed | **ラベル:** needs:response, lifecycle

**概要:** ターミナル幅にフィットせず開かれるという旧 issue。`needs:response` のまま停滞していた。

**直近のアクティビティ:** 30 日無回答で bot がクローズ。

**次のアクション:** 情報提供で再オープン可能。

---

### ![PR](icons/git-pull-request.svg) [test: fix load-time nvim_get_current_buf call in util_spec](https://github.com/neovim/neovim/pull/39365) `#39365`
**ステータス:** Merged | **ラベル:** test, ci:backport release-0.12

**概要:** `describe`-block ロード時に Nvim セッションが存在しない状態で `curbuf` を初期化していた箇所を、各呼び出し箇所で 0 を直接使う形に修正する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `test/functional/plugin/lsp/util_spec.lua`（+3 / −5）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: misc](https://github.com/neovim/neovim/pull/39256) `#39256`
**ステータス:** Open | **ラベル:** documentation

**概要:** `#35589` を解決するための雑多なドキュメント整理。`labeler_issue.yml` を含む 23 ファイルにわたる小修正の集合（+186 / −92）。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `.github/workflows/labeler_issue.yml`、`runtime/doc/{dev,dev_arch,dev_vimpatch}.txt` ほか。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(lsp): handle self-mapped methods in supports_method](https://github.com/neovim/neovim/pull/39385) `#39385`
**ステータス:** Merged | **ラベル:** lsp, target:release

**概要:** `#39383` を `release-0.12` 系列にバックポートする PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/lsp/client.lua`、テスト（+17 / −4）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): correctly detect supports_method after a refactor](https://github.com/neovim/neovim/pull/39379) `#39379`
**ステータス:** Closed | **ラベル:** lsp

**概要:** リファクタ後の `supports_method` 誤検知（メソッド名と等しい capability の場合は対応 capability なしを意味する）の修正試行。

**直近のアクティビティ:** `mochaaP` が `nvimtools/none-ls.nvim#331` で同問題を捕捉した経緯を共有し、リリース前にテストで捕まえたい旨を要望。`justinmk` がコメントをコードに残すよう促す。

**次のアクション:** `#39383` の本筋修正に統合されてクローズ。

---

### ![Issue](icons/issue-opened.svg) [plugin monitoring, active health check](https://github.com/neovim/neovim/issues/36403) `#36403`
**ステータス:** Open | **ラベル:** performance, diagnostic, ux-visibility, namespaces-scopes

**概要:** プラグイン起因の遅延（プロセス大量生成や LSP 誤設定で 100GB メモリを食う等）を可視化する低コスト・高価値な能動的モニタリング機構を追加する提案。`:checkhealth` を補完するアクティブヘルスチェックとして整理。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 設計案の議論。

---

### ![Issue](icons/issue-opened.svg) [Window path separator (slash) handling](https://github.com/neovim/neovim/issues/39382) `#39382`
**ステータス:** Open | **ラベル:** platform:windows, environment, system, filesystem

**概要:** Windows の `/` と `\` の混在問題を全面整理するメタ issue。内部処理は常に `/` を使い、表示用には可能な限り `/`、変換が必要なところでだけ `\` に戻すという方針を提示。

**直近のアクティビティ:** 直近24時間のコメントなし。`#37729` のマージと並行する形で議論を集約。

**次のアクション:** 残課題のサブタスク化と段階的実装。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter/extmark): support removing a conceal highlight](https://github.com/neovim/neovim/pull/35087) `#35087`
**ステータス:** Open | **ラベル:** treesitter

**概要:** `highlights.scm` で `@conceal` がついた範囲を上書きで解除する `@noconceal`（`@nospell` と同様の発想）を追加。`conceal` メタデータに `\f` を入れることでも同等の動作にする。`#35085` のクローズを意図。

**直近のアクティビティ:** `PeterCardenas` が CI を直し boolean アプローチに戻したのでレビュー再開可能と報告。

**変更ファイル:** `runtime/doc/{api,news,treesitter}.txt`、メタ生成 lua など 15 ファイル（+116 / −35）。

**次のアクション:** `zeertzjq` `justinmk` `clason` のレビュー継続。

---

### ![Issue](icons/issue-opened.svg) [refactor: path.c cleanup](https://github.com/neovim/neovim/issues/36112) `#36112`
**ステータス:** Open | **ラベル:** refactor, filesystem

**概要:** `path.c` 周辺で目的が重複する関数群を整理し、それぞれの差を文書化・統合することでバグ発見と次のパス処理改善（`#23xxx` 系列）を可能にする整理 issue。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 関数仕分けの担当割。

---

### ![Issue](icons/issue-opened.svg) [`vim.pack` options](https://github.com/neovim/neovim/issues/34772) `#34772`
**ステータス:** Open | **ラベル:** lua, options, packages

**概要:** `vim.pack` のグローバル設定（`job.threads` / `job.timeout`、`add` 構造体相当のデフォルト等）を可能にする提案。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 設定キーの合意。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(api): leak `preview` callback `LuaRef` in `nvim_create_user_command`](https://github.com/neovim/neovim/pull/39377) `#39377`
**ステータス:** Merged | **ラベル:** target:release

**概要:** `#39357` の `release-0.12` バックポート。

**直近のアクティビティ:** `justinmk` が「先の CI 失敗はマージ重なりが原因で `#39371` で修正済み」と注記。

**変更ファイル:** `src/nvim/api/command.c`、テスト（+22 / −1）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(api): `LuaRef` leak in `nvim_set_keymap` on LHS too long (>=66 bytes)](https://github.com/neovim/neovim/pull/39376) `#39376`
**ステータス:** Merged | **ラベル:** target:release

**概要:** `#39351` の `release-0.12` バックポート。LHS が 66 バイト以上のキーマップで `LuaRef` がリークする問題の修正を 0.12 系へ反映。

**直近のアクティビティ:** `justinmk` が CI 失敗のマージ重なりは `#39371` で解消済みと記載。

**変更ファイル:** `src/nvim/mapping.c`、テスト（+18 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(events): trigger WinScrolled during incsearch](https://github.com/neovim/neovim/pull/39308) `#39308`
**ステータス:** Open | **ラベル:** needs:response, events, AI assisted

**概要:** `'incsearch'` 中に検索パターン入力でウィンドウがスクロールしても `WinScrolled` が発火しない問題を、`update_screen()` のあとに `may_trigger_win_scrolled_resized()` を呼ぶことで修正する。`#21207` を解決。

**直近のアクティビティ:** `justinmk` が「自分はレビューしないので有識者を待つ、レビューが付かなければ自動クローズ」と通告。

**変更ファイル:** `src/nvim/ex_getln.c`、テスト（+27 / 0）。

**次のアクション:** 有識者の登場待ち。

---

### ![PR](icons/git-pull-request.svg) [backport test: curbuf initialized in describe-block](https://github.com/neovim/neovim/pull/39371) `#39371`
**ステータス:** Merged | **ラベル:** test, target:release

**概要:** `#39365` のバックポート。`describe`-block 初期化時に Nvim セッション無しで `curbuf` を取得していた箇所のテスト修正を 0.12 系へ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `test/functional/{ex_cmds/trust_spec.lua,lua/secure_spec.lua,plugin/lsp/util_spec.lua}`（+6 / −9）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(startup): restore ttyfast to control startup behavior](https://github.com/neovim/neovim/pull/38699) `#38699`
**ステータス:** Merged | **ラベル:** defaults, tui

**概要:** SSH 越し起動などでバックグラウンド検出に間に合わず警告が出る問題（`#38648`）を解消するため、`ttyfast` を復活させて bg 検出の有効/無効を制御できるようにする。起動時かつ user config 前なので環境変数で扱う方針。

**直近のアクティビティ:** `zeertzjq` と `gpanders` が COMMENTED レビュー（テキストなし）。

**変更ファイル:** `runtime/doc/{news,options,vim_diff}.txt`、`runtime/lua/vim/_core/defaults.lua` ほか 11 ファイル（+53 / −9）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [:nineties mode: disable various settings for slow terminals](https://github.com/neovim/neovim/issues/6293) `#6293`
**ステータス:** Open | **ラベル:** performance, tui

**概要:** 低速回線で `showcmd` `ruler` などが描画パフォーマンスを下げるため、`:nineties` 一発で諸機能を OFF にできるビルトイン提案。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** `ttyfast` 復活と合わせて再評価。

---

### ![Issue](icons/issue-opened.svg) [DECRQSS +q5463;524742;73657472676266;73657472676262$qm appears in terminal](https://github.com/neovim/neovim/issues/28776) `#28776`
**ステータス:** Closed | **ラベル:** platform:macos, tui

**概要:** 起動時に `+q5463;524742;...$qm` がターミナル左上に瞬間表示される問題（macOS リモート Mac で顕著）。

**直近のアクティビティ:** `justinmk` が `#38699` で対処されると示唆しクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [ui2: support disabling expansion of any messages](https://github.com/neovim/neovim/issues/39375) `#39375`
**ステータス:** Open | **ラベル:** ui2

**概要:** `_core/ui2/messages.lua` 内のロジックで非エラー系 message が `cmdheight` を超えるとき自動展開する挙動を、選択的に無効化したいという要望。`#39373` のデバッグ過程で見つかった構造課題。

**直近のアクティビティ:** `clason` の方針コメントへの言及や、`y9san9` の cmd ターゲット限定で展開可否を持つべきかの議論が続く。

**次のアクション:** メッセージ「routing」議論との合流。

---

### ![PR](icons/git-pull-request.svg) [fix(api): leak `preview` callback `LuaRef` in `nvim_create_user_command`](https://github.com/neovim/neovim/pull/39357) `#39357`
**ステータス:** Merged | **ラベル:** api, ci:backport release-0.12

**概要:** `nvim_create_user_command` の `preview` callback の `LuaRef` 所有権がオプション検証前に取られていたため、検証失敗時に解放されないリークを再現スクリプト付きで修正。

**直近のアクティビティ:** バックポート PR `#39377` が自動生成。

**変更ファイル:** `src/nvim/api/command.c`、テスト（+22 / −1）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(api): `LuaRef` leak in `nvim_set_keymap` on LHS too long (>=66 bytes)](https://github.com/neovim/neovim/pull/39351) `#39351`
**ステータス:** Merged | **ラベル:** api, ci:backport release-0.12

**概要:** LHS が 66 バイトを超えるキーマップを設定すると `LuaRef` がリークしていた問題を、ASan/LSan で日々運用していた投稿者が `:restarted` 後に発見し修正。

**直近のアクティビティ:** バックポート PR `#39376` が自動生成。

**変更ファイル:** `src/nvim/mapping.c`、テスト（+18 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): handle relative='editor' in make_floating_popup_options()](https://github.com/neovim/neovim/pull/39320) `#39320`
**ステータス:** Open | **ラベル:** lsp, AI assisted

**概要:** `vim.g.health = { style = 'float' }` で `:checkhealth` を `:help` バッファから実行すると、`relative='editor'` を渡しているのにカーソル相対のメトリクスから anchor を選んでしまい左上に表示されてしまう問題の修正。

**直近のアクティビティ:** `glepnir` と `justinmk` から COMMENTED レビュー。

**変更ファイル:** `runtime/lua/vim/lsp/util.lua`、テスト（+29 / −4）。

**次のアクション:** レビュー対応継続。

---

### ![Issue](icons/issue-opened.svg) [ui2: echoing very long message with `cmd={height=1}`](https://github.com/neovim/neovim/issues/39373) `#39373`
**ステータス:** Open | **ラベル:** ui2

**概要:** `ui2` の `cmd.height=1` 設定で `vim.cmd.echom` に長文を渡すと意図しない動作になるバグ。`height = 0.0001` まで下げると一見動くがそれもまた別の予期せぬ挙動を起こすという再現付き報告。

**直近のアクティビティ:** 投稿者が再現条件と回避策を共有。

**次のアクション:** ui2 メッセージ表示ロジック修正。

---

### ![Issue](icons/issue-opened.svg) [text-object for the entire buffer](https://github.com/neovim/neovim/issues/39209) `#39209`
**ステータス:** Open | **ラベル:** defaults, normal-mode, editor

**概要:** バッファ全体を対象とする text-object をデフォルトで導入する提案。現状 `ggVG` だとカーソルが動くため yank やフォーマットが不便。`ag`（"a global") などのキーが候補。

**直近のアクティビティ:** ユーザーが `ag`（"all global"）バインドを推奨し、`gg`/`G` と覚えやすいなど好みを共有。

**次のアクション:** デフォルトキー候補の合意。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): add `vim.lsp.inlay_hint.apply_action()`](https://github.com/neovim/neovim/pull/36219) `#36219`
**ステータス:** Open | **ラベル:** lsp

**概要:** `inlayHint` の `textEdits` や `location` 等の属性を活用する `vim.lsp.inlay_hint.apply_action()` を追加し、basedpyright 等が提供する追加情報をキーマップから扱えるようにする。`#36181` の解決を意図。

**直近のアクティビティ:** `ribru17` が「resolve が必要なケースのハンドリングが要る」とコメント、`Davidyz` も COMMENTED レビュー。

**変更ファイル:** `runtime/lua/vim/lsp/inlay_hint.lua`（+677 / −1）、`runtime/lua/vim/lsp/protocol.lua`、ドキュメント。

**次のアクション:** resolve 対応の実装と再レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(api): distinguish 0 from `nil` progress percent in nvim_echo](https://github.com/neovim/neovim/pull/39029) `#39029`
**ステータス:** Merged | **ラベル:** defaults, tui, messages, AI assisted

**概要:** `nvim_echo` の進捗パーセンテージで 0 と `nil` を区別し、`nil` の場合は「不確定」を表す OSC 9、0 の場合は zero progress を発出するよう改修。statusline 表記も差別化。

**直近のアクティビティ:** `justinmk` が複数の COMMENTED レビュー（テキストなし）。

**変更ファイル:** `runtime/lua/vim/_core/defaults.lua` ほか 8 ファイル（+27 / −16）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(charwise_range): initial implementation [no ci]](https://github.com/neovim/neovim/pull/32816) `#32816`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** バッククォート範囲（``` :`<,`> foo ``` `:`a,`b foo`）や `x.y` 範囲などコマンドに列情報を渡せる文字幅範囲（charwise range）の最初の実装ドラフト。`#22297` を解決対象に挙げる大規模変更（40 ファイル、+1564 / −399）。

**直近のアクティビティ:** `justinmk` から複数の COMMENTED レビュー。

**変更ファイル:** `runtime/doc/map.txt`、`runtime/lua/vim/_meta/api*` ほか 40 ファイル。

**次のアクション:** ドラフト段階で実装範囲の確定。

---

## nvim-telescope/telescope.nvim

> 直近24時間は古い request 系 issue のクローズが 1 件のみで動きは静か。

### ![Issue](icons/issue-opened.svg) [docs: File Picker: options to search also 1.gitignore, 2. hidden files, 3.binary files](https://github.com/nvim-telescope/telescope.nvim/issues/470) `#470`
**ステータス:** Closed | **ラベル:** question

**概要:** `.gitignore` 配下や hidden / binary ファイルも検索したいという要望。ripgrep の `-u` のように、Telescope の File Picker でも段階的に制限緩和する手段を文書化してほしいという提案。

**直近のアクティビティ:** ユーザー `RheaAyase` が「達成可能と言われても初心者には方法が分からず、設定の難しさが本質的問題」とコメントし、デフォルト挙動と発見性を改善すべきと指摘。整理として古い question issue がクローズ。

**次のアクション:** クローズ済み（議論は `nvim-mini` などの代替や README 改善に委ねられる）。

---

## ghostty-org/ghostty

> Flatpak 環境の安定化（クラッシュ時のセッション終了、GNOME 50 ランタイム移行）と libghostty のクロスコンパイル整備、Vouched ユーザー追加が中心。

### ![PR](icons/git-pull-request.svg) [flatpak: terminate session if Ghostty disconnects from bus](https://github.com/ghostty-org/ghostty/pull/12427) `#12427`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Ghostty がクラッシュしたとき、Flatpak Session Helper にスポーンしたコマンドも一緒に終了させるよう修正。投稿者が経験した「クラッシュで重い背景プロセスが残り続ける」問題への対処。

**直近のアクティビティ:** `pluiedev` から「1 点を除き良さそう」と APPROVED 寄りのレビュー。

**変更ファイル:** `src/os/flatpak.zig`（+14 / −1）。

**次のアクション:** 指摘 1 点の修正後マージ。

---

### ![Issue](icons/issue-opened.svg) [Ghostty crashes immediately when launched from GitHub Desktop via "Open in Ghostty"](https://github.com/ghostty-org/ghostty/issues/12430) `#12430`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** GitHub Desktop からの「Open in Ghostty」起動で Ghostty が一瞬表示後すぐクラッシュするというバグ報告。

**直近のアクティビティ:** `ghostty-vouch[bot]` が「非メンテナは issue を直接立てず discussion を使うルール（`#3558` / CONTRIBUTING.md 参照）」を案内して自動クローズ。

**次のアクション:** 投稿者が discussion で再投稿することを期待。

---

### ![PR](icons/git-pull-request.svg) [flatpak: update runtime to GNOME 50](https://github.com/ghostty-org/ghostty/pull/12428) `#12428`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Flatpak ランタイムを GNOME 50 に更新する PR。GTK は `4.20.4` → `4.22.2`、libadwaita は `1.8.5` → `1.9.0`。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `flatpak/com.mitchellh.ghostty-debug.yml`、`com.mitchellh.ghostty.yml`（+2 / −2）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [gtk/SurfaceScrolledWindow: wrap root child with another Adw.Bin](https://github.com/ghostty-org/ghostty/pull/12426) `#12426`
**ステータス:** Open | **ラベル:** (なし)

**概要:** GTK の既知不具合（テンプレートルートの scrolled_window が dispose 時に二重 free される）を、`ResizeOverlay` 等と同様に root child を `Adw.Bin` でラップして回避する。GNOME 49 (Gtk 4.20 / libadwaita 1.8.5) でのクラッシュを抑止。

**直近のアクティビティ:** 直近24時間のコメントなし。投稿者は Valgrind トレースを LLM 支援で解析しリポ内既知例から該当パターンを特定したと記載。

**変更ファイル:** `src/apprt/gtk/class/surface_scrolled_window.zig`、`ui/1.5/surface-scrolled-window.blp`（+10 / −5）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/12425) `#12425`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** discussion `#12308` のコメントトリガで `@alaviss` を Vouch リストに追加する 1 行 PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `.github/VOUCHED.td`（+1 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [input: add indexed goto_window:N with stable window slots](https://github.com/ghostty-org/ghostty/pull/12422) `#12422`
**ステータス:** Closed | **ラベル:** input

**概要:** `goto_window` バインディングに 1 ベースのスロット番号を受け付け、`#3950` のディスカッションに合わせて安定したウィンドウスロットを実装する大型 PR。`WindowSlotRegistry.swift` を新設し macOS と libghostty 双方を更新。

**直近のアクティビティ:** `ghostty-vouch[bot]` が、投稿者が Vouch リストに無いため自動クローズすると通告。

**変更ファイル:** `WindowSlotRegistry.swift`（新規 +121）、`include/ghostty.h` ほか 10 ファイル（+414 / −19）。

**次のアクション:** 投稿者の Vouch を取得後の再提出。

---

### ![PR](icons/git-pull-request.svg) [macOS: save window state before relaunching by Sparkle](https://github.com/ghostty-org/ghostty/pull/12419) `#12419`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** Sparkle による再起動の前にウィンドウ状態を保存する修正案。`#11304` の解決を意図。

**直近のアクティビティ:** 投稿者 `bo2themax` が「`invalidateRestorableState` 呼び出しを試した」と仮説検証。`mitchellh` が「動くと言えるのか？ 状態保存は async で確認手段がない」と CHANGES_REQUESTED。投稿者は「運頼み」と認めて方針見直し。

**変更ファイル:** `macos/Sources/Features/Update/UpdateDelegate.swift`（+6 / −1）。

**次のアクション:** 同期保証可能な仕組みでの再提案。

---

### ![PR](icons/git-pull-request.svg) [config: use Config to check key binding instead of App](https://github.com/ghostty-org/ghostty/pull/12415) `#12415`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `ghostty_app_key_is_binding` が App ではなく `config.keybind` を直接参照するよう変更し、ダミー設定でユニットテストを書きやすくする整理。

**直近のアクティビティ:** `mitchellh` が APPROVED コメントで「アプリとサーフェスが個別の設定を持てる前提を保ちつつこの形で問題ない」と承認。

**変更ファイル:** `include/ghostty.h`、`AppDelegate.swift`、`src/App.zig` ほか 5 ファイル（+22 / −25）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build: respect config.emit_xcframework for building libghostty-vt.xcframework on Darwin](https://github.com/ghostty-org/ghostty/pull/12267) `#12267`
**ステータス:** Merged | **ラベル:** libghostty

**概要:** `-Demit-lib-vt` のように生の C オブジェクト/ヘッダだけ要求している場合でも `xcodebuild -create-xcframework` がコンパイル時に強制実行されていた macOS の hard-coded ビルドを修正し、`emit-xcframework` フラグに従わせる。

**直近のアクティビティ:** `mitchellh` が一旦自分で引き取ろうとし、新フラグを足さず既存の `emit-xcframework` を再利用すべきと指摘。最終的に Swift サンプルから libc++ リンクを除去するリベース後 APPROVED でマージ。

**変更ファイル:** `.github/workflows/test.yml`、`build.zig`、`src/build/Config.zig`（+27 / −10）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [libghostty: enable cross-compiling macOS from Linux/Windows](https://github.com/ghostty-org/ghostty/pull/12417) `#12417`
**ステータス:** Merged | **ラベル:** libghostty

**概要:** Linux / Windows から macOS 向け `libghostty-vt` をクロスコンパイルできるよう、`pkg/apple-sdk` を Zig 内蔵 macOS ヘッダにフォールバックさせ、`CombineArchivesStep` を Linux ツーリングで動作するよう更新。CI でも検証。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `pkg/apple-sdk/build.zig`（+105 / −59）、CI ほか（+116 / −66）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Feature request: preserve soft-wrapped lines when copying (no forced newlines at visual wrap points)](https://github.com/ghostty-org/ghostty/issues/12420) `#12420`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** ソフトラップされた行をコピーすると視覚的折り返し位置に実際の `\n` が挿入される挙動について、論理行を保持する（あるいは少なくともオプション化する）よう要望。

**直近のアクティビティ:** `ghostty-vouch[bot]` が「非メンテナは discussion で起票してほしい」と案内し自動クローズ。

**次のアクション:** discussion に再投稿してもらう。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/12418) `#12418`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** issue `#11461` のコメントを根拠に `@seyoungjeong` を Vouch リストへ追加する 1 行 PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `.github/VOUCHED.td`（+1 / 0）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [macOS: Korean IME preedit cancelled (not committed) when pressing arrow keys](https://github.com/ghostty-org/ghostty/issues/11461) `#11461`
**ステータス:** Open | **ラベル:** os/macos, input

**概要:** macOS で韓国語 2-Set IME を使用中、Hangul 構成中に矢印キーや Delete を押すと preedit がコミットされず取り消されてしまうバグ。元 discussion `#11306` を経て issue 化。

**直近のアクティビティ:** `jcollie` が `!vouch @seyoungjeong` と Vouch を発動。

**次のアクション:** Vouch 済みコントリビュータによる修正実装。

---

### ![PR](icons/git-pull-request.svg) [libghostty: produced artifacts are static (no libc) even with SIMD features](https://github.com/ghostty-org/ghostty/pull/12414) `#12414`
**ステータス:** Open (Draft) | **ラベル:** libghostty

**概要:** SIMD 機能付きで `libghostty` をビルドしても libc 依存なしで成果物を作れるようにする。simdutf を fork して 6 個ほどの libc 関数を差し替え可能にし、`linkLibC()` の挙動の都合で C++ モジュールを別コンパイル単位に分離する 32 ファイルに渡る大改修。

**直近のアクティビティ:** 直近24時間のコメントなし（投稿者がブランチがまだ整理途中であると注記）。

**変更ファイル:** CMakeLists.txt、`example/c-vt-*` ビルド設定ほか 32 ファイル（+888 / −313）。

**次のアクション:** ドラフト整理とレビュー。

---

## astral-sh/uv

> セキュリティ寄りの URL リダクションと、xz/lzma/bz2 圧縮形式のサポート除去という breaking 変更、プラットフォーム依存（armv7、CPython リリース取得）まわりの整理が同時進行する一日。

### ![Issue](icons/issue-opened.svg) [Unable to use `uv self update` on armv7 host after 0.11.0](https://github.com/astral-sh/uv/issues/19137) `#19137`
**ステータス:** Open | **ラベル:** bug

**概要:** 0.11.0 以降、32-bit ARM ホスト (`armv7`) で `uv self update` が `armv7-unknown-linux-gnueabihfeabihf` というプラットフォーム名で見つからないと失敗するバグ。`aarch64` 上で 32-bit を動かす Debian Trixie コンテナで再現する。

**直近のアクティビティ:** `nooscraft` が「対応 PR のレビューを `@zanieb` にお願いしたい」と投稿。

**次のアクション:** 該当 PR のレビューとマージ。

---

### ![Issue](icons/issue-opened.svg) [Unable to use `uv` with Cookiecutter templates](https://github.com/astral-sh/uv/issues/8129) `#8129`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Cookiecutter テンプレート（変数置換前の `pyproject.toml`）を `uv` の strict バリデーションが拒否するため、Poetry からの移行が困難という長寿の issue。

**直近のアクティビティ:** `sobolevn` が同様に `wemake-django-template` で同じ問題に直面しており、`name = "{{ cookiecutter.project_name }}"` を一旦 valid な名前にリネームしてから戻すという回避が必要だったと共有。

**次のアクション:** テンプレート互換モード等の対応議論。

---

### ![Issue](icons/issue-opened.svg) [uv pip install doesn't work in TF memory card](https://github.com/astral-sh/uv/issues/12129) `#12129`
**ステータス:** Closed | **ラベル:** bug, error messages

**概要:** macOS で TF メモリカード上のプロジェクトに対して `uv venv` / `uv pip install -r requirements.txt` を行うと `Operation not supported (os error 45)` で失敗するという報告。ローカルストレージでは問題なし。

**直近のアクティビティ:** 別ユーザー `mohammadazeemwani` が「解決策はありましたか」と尋ねる程度で進展なくクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Support querying dependencies of uninstalled packages](https://github.com/astral-sh/uv/issues/19140) `#19140`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `pipgrip` 同様に「インストールせずに Python パッケージの依存関係を問い合わせる」機能要望。CI/CD の依存ツリー出力や事前確認に有用と主張。

**直近のアクティビティ:** `Mingliang-Liu` が「公式 PyPI 全体の SHA256 を Astral 側で hosting すれば数十 MB で済む」と試算を提示。`woodruffw` は「PEP 658 の `.metadata` を sdist まで拡張すれば一部の build を skip できるが dynamic な sdist は依然として実行が必要」「`pip install --dry-run` という代替経路もある」と多角的に応答。合計 6 件の議論で活発化。

**次のアクション:** PEP 658 拡張議論を待ちつつ、暫定 CLI として `uv pip install --dry-run` の使い勝手改善を検討。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate 由来の依存管理ダッシュボード issue。アバンダンド依存（37 件）など内部メンテナンス情報をまとめている。

**直近のアクティビティ:** 直近24時間のコメントなし（自動更新のみ）。

**次のアクション:** Renovate が自動更新。

---

### ![PR](icons/git-pull-request.svg) [Redact pre-signed upload URLs in verbose output](https://github.com/astral-sh/uv/pull/19146) `#19146`
**ステータス:** Merged | **ラベル:** bug

**概要:** `DisplaySafeUrl` に S3 署名で典型的な機密クエリパラメータの知識を持たせ、verbose ログから漏洩しないようにする。reqwest の生 url や AWS が返す署名付きエラーメッセージは依然として原文を出すが、後者は署名無効時しか出ないため許容と判断。

**直近のアクティビティ:** `zsol` と `woodruffw` の COMMENTED レビューを経てマージ。

**変更ファイル:** `crates/uv-redacted/src/lib.rs`（+106 / −7）、`crates/uv/tests/it/publish.rs`（+61 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Redact sensitive parts of URLs in reqwest errors](https://github.com/astral-sh/uv/pull/19147) `#19147`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `#19146` の続編。reqwest 起源のエラーメッセージにも機密 URL が漏れないようリダクションを拡張する。`crates/uv-client/src/error.rs` を 144 行追加するなど 7 ファイル改修。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `crates/uv-client/src/error.rs`、`crates/uv-publish/{lib.rs,trusted_publishing/*.rs}`（+378 / −41）。

**次のアクション:** ドラフト解除と `#19146` 後のレビュー。

---

### ![PR](icons/git-pull-request.svg) [Allow `apply-ci-snapshots.sh review` without ID](https://github.com/astral-sh/uv/pull/19149) `#19149`
**ステータス:** Open | **ラベル:** internal

**概要:** `apply-ci-snapshots.sh review` で run ID を渡さなくても動くよう、引数なしで `review` だけ呼べる挙動に直す nice-to-have な内部 PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `scripts/apply-ci-snapshots.sh`（+14 / −6）。

**次のアクション:** 内部レビュー。

---

### ![PR](icons/git-pull-request.svg) [Remove xz support entirely](https://github.com/astral-sh/uv/pull/19143) `#19143`
**ステータス:** Open (Draft) | **ラベル:** breaking

**概要:** `#18927`（zip 周りの bz/lzma/xz 削除）の上に積む形で、tarball 経路を含めて xz 解凍サポートを完全に削除する破壊的変更ドラフト。

**直近のアクティビティ:** `konstin` と `woodruffw` が COMMENTED レビュー（テキストなし）。

**変更ファイル:** `Cargo.lock`、`Cargo.toml`、`crates/uv-bin-install/src/lib.rs` ほか 24 ファイル（+471 / −215）。

**次のアクション:** 0.12 リリースに合わせて取り込みを判断。

---

### ![Issue](icons/issue-opened.svg) [Add support for customizing Astral mirror URLs](https://github.com/astral-sh/uv/issues/18525) `#18525`
**ステータス:** Open | **ラベル:** enhancement, configuration, network

**概要:** Ruff / CPython / `uv` 自体のリリース取得が `releases.astral.sh` に統一されたが、隔離環境やプロキシリポジトリ経由でしかアクセスできない場合に向けて、それらの URL を環境変数や設定で差し替えできるようにする要望。

**直近のアクティビティ:** `leodevian` が `#18503` 完了で uv 更新が容易になった点を称えつつ、GitHub 系環境変数の path 制約に触れて再要望。

**次のアクション:** mirror URL の設定設計。

---

### ![PR](icons/git-pull-request.svg) [fetch available CPython releases from the network](https://github.com/astral-sh/uv/pull/19148) `#19148`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** PBS の利用可能 CPython 版数メタデータを `releases.astral.sh` の ndjson から取得する。GitHub フォールバック、ベイクされたメタデータへのフォールバック、行単位ストリームでキャッシュ済み分の前置きなどを実装。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `crates/uv-python/src/downloads.rs`（+1271 / −80）ほか 18 ファイル（+2636 / −184）。

**次のアクション:** ドラフト整理とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add UV_PYTHON_PLATFORM environment variable](https://github.com/astral-sh/uv/pull/19142) `#19142`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `--python-platform` を持つコマンドに対して `UV_PYTHON_PLATFORM` 環境変数を追加し、共有環境変数カタログに登録、`uv sync` の統合テストでカバー。`#15241` を解決。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `crates/uv-cli/src/lib.rs`、`crates/uv-static/src/env_vars.rs`、`crates/uv/tests/it/sync.rs`（+60 / −11）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate webpki to v0.103.13 - autoclosed](https://github.com/astral-sh/uv/pull/19145) `#19145`
**ステータス:** Closed | **ラベル:** internal, security

**概要:** `webpki` の `0.103.12 → 0.103.13` パッチアップデート。GHSA-82j2-j2ch-gfr8 の DoS（不正な CRL BIT STRING で `bit_string_flags()` がパニック）対応版を取り込む。

**直近のアクティビティ:** Renovate が自動 close（恐らく上流対応で別形に置き換わったため）。

**変更ファイル:** `Cargo.lock`（+9 / −9）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate sha2 to 0.11.0](https://github.com/astral-sh/uv/pull/18870) `#18870`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** `sha2` を `0.10.8 → 0.11.0` に minor 更新する Renovate PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `Cargo.lock`、`Cargo.toml`（+112 / −48）。

**次のアクション:** 自動更新待ち。

---

### ![PR](icons/git-pull-request.svg) [Use a single codepath for extracting a .tar.zst wheel](https://github.com/astral-sh/uv/pull/19144) `#19144`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `.tar.zst` wheel の展開コードが 2 箇所に重複していた状態を統合し、欠けていたバリデーションを揃える。マージ後は `zstd` と `tar` への直接依存も不要になる副次効果あり。

**直近のアクティビティ:** `konstin` と `zsol` の COMMENTED レビュー、最終的に `konstin` が APPROVED でマージ。

**変更ファイル:** `crates/uv-extract/src/stream.rs`（−56）、`distribution_database.rs` ほか 5 ファイル（+11 / −69）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Remove bz/lzma/xz support from ZIP handling](https://github.com/astral-sh/uv/pull/18927) `#18927`
**ステータス:** Open | **ラベル:** breaking

**概要:** ZIP 取り扱いから bzip2/lzma/xz を削る breaking 変更。Python パッケージング標準ではないという背景に基づく長期方針（`#16911`）の一環で、tarball 側は別 PR `#19143` で扱う。0.12 リリースまでマージしない方針。

**直近のアクティビティ:** `woodruffw` と `konstin` が COMMENTED レビュー（テキストなし）。

**変更ファイル:** `Cargo.lock`、`Cargo.toml`、`crates/uv-bin-install/src/lib.rs` ほか 16 ファイル（+89 / −187）。

**次のアクション:** 0.12 マイルストーンまで保留。

---

### ![PR](icons/git-pull-request.svg) [uv audit: adverse status reporting](https://github.com/astral-sh/uv/pull/19128) `#19128`
**ステータス:** Open (Draft) | **ラベル:** enhancement, preview, uv audit

**概要:** PEP 792 のプロジェクトステータス情報（既に内部表現にある）を活用し、`uv audit` が「`hackme is quarantined`」のような adverse status を報告する。`#18506` の一部、`#15254` をクローズ。OSV に加え registry へのリクエストが増える。

**直近のアクティビティ:** CodSpeed bot が「マージで 5.02% パフォーマンス低下」と警告。

**変更ファイル:** `crates/uv-audit/src/service/project_status.rs`（新規 +139）など 11 ファイル（+699 / −26）。

**次のアクション:** パフォーマンス低下の確認と、CodSpeed 上での承認または改善。

---

### ![PR](icons/git-pull-request.svg) [Correct file extension from '.cer' to '.cert'](https://github.com/astral-sh/uv/pull/19141) `#19141`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** TLS ドキュメントの拡張子を `.cer` から `.cert` に修正するという 1 行ドキュ修正。

**直近のアクティビティ:** `woodruffw` が「`.cer` は X.509 / PKCS#7 用の正規拡張子なので修正不要」と CHANGES_REQUESTED し、クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Follow-up from #16542](https://github.com/astral-sh/uv/pull/16749) `#16749`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `#16542` のフォローアップ。リモート JSON 取得時に client retries を 0 に上書きしない、`network.rs` に `--python-downloads-json-url` のテストを追加、`CachedClient` を使う、というチェック項目を消化済み。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `crates/uv-python/src/downloads.rs`、`installation.rs`、`crates/uv/src/commands/python/{find,install,list}.rs` ほか 7 ファイル（+207 / −30）。

**次のアクション:** メンテナレビュー。

---

## microsoft/vscode

> 1.118 開発が活発で Copilot Chat 関連 PR とリファクタが大量に流入。Spam / `info-needed` の自動クローズ issue も多数で、その他は Endgame `1.117.0` のクローズ、BYOK Claude Opus 4.7 サポート追加、Stateful Terminal Continuity（STC）大型 PR の登場などが目立つ。

### ![PR](icons/git-pull-request.svg) [Fix flaky sandbox tests by stripping Chromium stderr noise](https://github.com/microsoft/vscode/pull/312527) `#312527`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** ヘッドレス Linux CI で Chromium / Electron が「Exiting GPU process due to errors during initialization」など GPU プロセスエラーをコマンド出力後に吐き出すと、最終行検査が DBus 行しか弾けないため sandbox テストがフレーキー化していた。`stripChromiumStderr` ヘルパーを追加してこれらをまとめて除去する。

**直近のアクティビティ:** `vs-code-engineering[bot]` がビルドキューに投入。

**変更ファイル:** 1 ファイル（+18 / −6）。

**次のアクション:** ビルド緑化後にレビュー。

---

### ![Issue](icons/issue-opened.svg) [Colors are washed and grayed out in vscode (Arch linux)](https://github.com/microsoft/vscode/issues/263092) `#263092`
**ステータス:** Closed | **ラベル:** bug

**概要:** Arch Linux で 1.116 系の VS Code の表示色が褪せて見えるという長寿のバグ報告。

**直近のアクティビティ:** `SaeedMuhaisen` が「Electron 39 → 40 へ更新しても変化なし」とトラブル状況を共有。Issue クローズ。

**次のアクション:** Electron 由来の Linux 表示問題として別 issue で追跡。

---

### ![Issue](icons/issue-opened.svg) [Copilot stuck evaluating/analyzing/planning](https://github.com/microsoft/vscode/issues/312526) `#312526`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Insider 1.118 で Copilot が「evaluating / analyzing / planning」状態のまま停止するというバグ報告。Electron 39.8.8、Chromium 142 の環境情報付き。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** トリアージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix false-positive input detection causing premature command termination](https://github.com/microsoft/vscode/pull/312392) `#312392`
**ステータス:** Merged | **ラベル:** ~requires-eval-assessment

**概要:** `detectsInputRequiredPattern` を 2 段階に分割し、`y/n`、パスワード、PowerShell オプション、`(END)`、press any key、括弧付きデフォルトなど確実な高信頼パターンを `_waitForIdle` の高速パスに、それ以外の幅広いヒューリスティックを別段に分けて誤検知（コマンドの早期打ち切り）を解消する。

**直近のアクティビティ:** ビルドキュー投入のみ。

**変更ファイル:** 2 ファイル（+82 / −10）。

**次のアクション:** Eval 評価キューでの効果測定。

---

### ![Issue](icons/issue-opened.svg) [BUG](https://github.com/microsoft/vscode/issues/312525) `#312525`
**ステータス:** Closed | **ラベル:** spam

**概要:** タイトル「BUG」のテンプレ未記入投稿。

**直近のアクティビティ:** `vs-code-engineering[bot]` がスパム/コミュニティガイドライン違反として自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [My limit is on 31.7% claude opus 4.7 is enabled but Opus 4.6 show upgrade](https://github.com/microsoft/vscode/issues/311446) `#311446`
**ステータス:** Open | **ラベル:** info-needed, triage-needed

**概要:** Copilot で Claude Opus 4.7 が有効なのにアップグレード表示が Opus 4.6 のままであるという報告。

**直近のアクティビティ:** 別ユーザーが「GPT 5.5 の rate は 7.5x なのか？」と冗談コメント。

**次のアクション:** モデルラベル整合の確認待ち。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat: Add fork button at the top of the conversation](https://github.com/microsoft/vscode/issues/312524) `#312524`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 会話の先頭にも fork ボタンを置いてほしいという機能要望。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Very lagging scrolling in diff view after a while](https://github.com/microsoft/vscode/issues/284483) `#284483`
**ステータス:** Open | **ラベル:** bug, diff-editor

**概要:** Git diff ビューを開いてしばらくすると 20fps 程度までスクロール性能が落ちる、Window Reload で復旧するパフォーマンス issue（1.107 系から継続）。

**直近のアクティビティ:** `xCirno1` が「進捗ある？ 注目度が足りない印象」とコメント。

**次のアクション:** 再現条件絞り込みとパフォーマンスチームのアサイン。

---

### ![Issue](icons/issue-opened.svg) [Test plan item: Eval assessment flow (`~requires-eval-assessment` label)](https://github.com/microsoft/vscode/issues/312430) `#312430`
**ステータス:** Open | **ラベル:** testplan-item

**概要:** `~requires-eval-assessment` ラベルが付いた PR を CI で自動ビルド・公開・ベンチマークする新パイプラインの動作確認テストプランアイテム。`microsoft/vscode-engineering#2510` を参照。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** anyOS の 3 環境テスト遂行。

---

### ![Issue](icons/issue-opened.svg) [Font rendering becomes jagged / aliased after VS Code 1.107.1 on Linux](https://github.com/microsoft/vscode/issues/285092) `#285092`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** EL8 系 Linux で 1.107.0 以降フォントレンダリングがジャギーになる回帰報告。`editor.fontFamily` 変更でも改善しない。

**直近のアクティビティ:** `SphenHe` が「1.118 で修正済み」とスクリーンショット付きで報告。クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Fork sessions](https://github.com/microsoft/vscode/issues/309024) `#309024`
**ステータス:** Open | **ラベル:** (なし)

**概要:** セッションを fork する機能の要望。

**直近のアクティビティ:** `peyremorgan` が「会話のチェックポイントごとに fork ボタンが既に存在する（ホバー時のみ表示）」とスクリーンショット付きで案内。

**次のアクション:** 既存 UI で要望が満たされているかの確認。

---

### ![Issue](icons/issue-opened.svg) [Session memory gets lost when forking a conversation](https://github.com/microsoft/vscode/issues/311020) `#311020`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Plan モードの `plan.md` 等のセッションメモリが fork 先の新セッションに引き継がれないというバグ。Copilot Chat Extension 0.44.1 / VS Code 1.116.0 で再現。

**直近のアクティビティ:** `peyremorgan` が「`#296370` の重複の可能性」と指摘。

**次のアクション:** 重複判定の確認とメモリ引き継ぎ実装。

---

### ![Issue](icons/issue-opened.svg) [VS Code freezes when Copilot agent runs `run-in-roblox` for headless Roblox tests](https://github.com/microsoft/vscode/issues/311275) `#311275`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Copilot Chat agent から `run-in-roblox` を実行すると、Roblox Studio が一瞬開く副作用と相まって Windows で VS Code が完全フリーズするという報告。

**直近のアクティビティ:** `claudiurazlet` が「1.117.0 で解消、関連は `#310740` `#309906`」と共有。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [TRY 1](https://github.com/microsoft/vscode/issues/312523) `#312523`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed, new release

**概要:** タイトル「TRY 1」、本文「TRY TRY TRY」のテスト投稿。

**直近のアクティビティ:** Bot が情報不足で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [مش متبع التعليمات](https://github.com/microsoft/vscode/issues/312522) `#312522`
**ステータス:** Open | **ラベル:** info-needed, triage-needed, ai-translated

**概要:** アラビア語で「指示に従わない」というタイトル、本文も「アドバイスだけくれて agent として動かない」という短いバグ報告。Copilot 拡張 0.33.5 の古い環境。

**直近のアクティビティ:** Bot が「VS Code 1.106.3 は古いので 1.117.0 に上げてください」と案内し、AI 翻訳結果を併記。

**次のアクション:** バージョンアップ後の再現確認。

---

### ![PR](icons/git-pull-request.svg) [Remove copilot](https://github.com/microsoft/vscode/pull/312521) `#312521`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Copilot 関連を削除すると主張する 3 ファイル PR。

**直近のアクティビティ:** `microsoft-github-policy-service[bot]` が CLA 同意コメントを促す。クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Allow Pin or Favorite Branches in Git Checkout Panel](https://github.com/microsoft/vscode/issues/285828) `#285828`
**ステータス:** Open | **ラベル:** feature-request, git

**概要:** Git: Checkout コマンドのブランチ一覧でピン留め / お気に入りを付けたいという要望。

**直近のアクティビティ:** `Diogo7Matias` が「PR を出したい」と表明。

**次のアクション:** 仕様合意後 PR 化。

---

### ![Issue](icons/issue-opened.svg) [VSCode caused my Ubuntu to crash.](https://github.com/microsoft/vscode/issues/297628) `#297628`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Ubuntu のクラッシュを VS Code 由来と報告。詳細情報なし。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Host becomes unresponsive during synchronous subprocess wait instead of timing out cleanly](https://github.com/microsoft/vscode/issues/305740) `#305740`
**ステータス:** Closed | **ラベル:** bug, info-needed, freeze-slow-crash-leak, triage-needed

**概要:** UI スレッドや event loop 系から外部プロセスを同期 wait しているコードが原因で、子プロセスがストールするとホストが応答停止するという resilience 上の指摘。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Crashing!](https://github.com/microsoft/vscode/issues/306866) `#306866`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed, stale

**概要:** タイトル `Crashing!` のテンプレ未記入クラッシュ報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [github.copilot.chat.agentDebugLog.fileLogging.enabled does not create debug-logs directory despite being set to true](https://github.com/microsoft/vscode/issues/307890) `#307890`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `github.copilot.chat.agentDebugLog.fileLogging.enabled = true` を設定しても `debug-logs` ディレクトリが作られないというバグ。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み（再現可能なら再オープン）。

---

### ![Issue](icons/issue-opened.svg) [ask for an account review for the “flagged and unable to make purchases” status.](https://github.com/microsoft/vscode/issues/308409) `#308409`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** アカウントの flagged ステータス見直しを求めるサポート問い合わせ（issue tracker 用途外）。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [feature request: add function like `languages.match()` for `Uri` in extension API](https://github.com/microsoft/vscode/issues/309002) `#309002`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 拡張 API に `Uri` 向けの `languages.match()` 同等関数を追加する要望。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み（再オープン可能）。

---

### ![Issue](icons/issue-opened.svg) [report](https://github.com/microsoft/vscode/issues/309040) `#309040`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** Copilot Chat の「Server error: 500」報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Integrated browser doesn't work with GitHub Copilot](https://github.com/microsoft/vscode/issues/310087) `#310087`
**ステータス:** Closed | **ラベル:** bug, info-needed, browser-integration

**概要:** 統合ブラウザが Copilot で `Target page, context or browser has been closed` エラーを返すバグ。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot is no longer working](https://github.com/microsoft/vscode/issues/310203) `#310203`
**ステータス:** Closed | **ラベル:** info-needed, chat

**概要:** Copilot が動作しないという情報不足の報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Keep failing to complete work](https://github.com/microsoft/vscode/issues/310386) `#310386`
**ステータス:** Closed | **ラベル:** info-needed, chat

**概要:** カスタム Copilot agent と VS Code 拡張で他システム連携している状況での失敗報告だが詳細不足。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Extension Signature Verification Failed: Unity](https://github.com/microsoft/vscode/issues/310727) `#310727`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** Unity 拡張で署名検証失敗。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Do Not Delete This! bug_report_template](https://github.com/microsoft/vscode/issues/310784) `#310784`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** テンプレートそのものを投稿してしまった起票ミス。

**直近のアクティビティ:** Bot が自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [An error occurs when opening the file access.log in VS Code](https://github.com/microsoft/vscode/issues/310921) `#310921`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `access.log` を開いたときのエラー報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [The extension restarts frequently](https://github.com/microsoft/vscode/issues/311002) `#311002`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** Chat 起動 / メッセージ送信 / Ask ↔ Agent 切替時に拡張が頻繁に再起動するという報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Wbt.clearMarks is not a function](https://github.com/microsoft/vscode/issues/311005) `#311005`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** `Wbt.clearMarks is not a function` エラーが繰り返し出るとフランス語で報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [after the update I just clicked on the agents drop-down in CoPilot.](https://github.com/microsoft/vscode/issues/311009) `#311009`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** アップデート後 agents ドロップダウンをクリックしたら問題が発生したという情報不足の報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Exceptionally Long Time Taken for Output Response](https://github.com/microsoft/vscode/issues/311033) `#311033`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** `GPT 5.4 mini` の Auto Mode で応答が止まるという報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [it blocks input](https://github.com/microsoft/vscode/issues/311062) `#311062`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** ネットワーク I/O 中に入力がブロックされ非同期動作になっていない、という報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [copilot chat not working](https://github.com/microsoft/vscode/issues/311108) `#311108`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** 「動かない」だけの情報不足な Copilot Chat 報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [error popup vscode](https://github.com/microsoft/vscode/issues/311114) `#311114`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** タイトルそのままの抽象的なエラーポップアップ報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Horendous random dissapearance of entire parts of my codebase intermittently.](https://github.com/microsoft/vscode/issues/311137) `#311137`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** Copilot 利用後にコードベースの一部が突然消えるという報告。restore 機能の不具合に起因していると推測している。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Chat model issue](https://github.com/microsoft/vscode/issues/311158) `#311158`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** タイトル通りの抽象的な chat model 関連の報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Double requests for credentials to Sign In](https://github.com/microsoft/vscode/issues/311162) `#311162`
**ステータス:** Closed | **ラベル:** info-needed, triage-needed

**概要:** サインイン時に資格情報を二重要求されるという報告。

**直近のアクティビティ:** Bot が `info-needed` 期限切れで自動クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Terminal: allow PowerShell 7.4+ constrained language audit mode to work with shell integrations](https://github.com/microsoft/vscode/pull/283178) `#283178`
**ステータス:** Open | **ラベル:** (なし)

**概要:** PowerShell 7.4 の Constrained Language Audit Mode（`$ExecutionContext.SessionState.LanguageMode` が `ConstrainedLanguage` を返すが実体は FullLanguage）に shell integration が対応するよう修正する PR。`#283151` を解決。

**直近のアクティビティ:** 投稿者 `skbtwiz` が `@anthonykim1` にレビューを依頼。COMMENTED レビュー 5 件付き。

**変更ファイル:** 1 ファイル（+13 / −1）。

**次のアクション:** メンテナレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [haven-g](https://github.com/microsoft/vscode/issues/312520) `#312520`
**ステータス:** Closed | **ラベル:** spam, chat-ext-issue

**概要:** タイトル「haven-g」のテンプレ空 issue。

**直近のアクティビティ:** Bot がスパム判定で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [haven](https://github.com/microsoft/vscode/issues/312519) `#312519`
**ステータス:** Closed | **ラベル:** spam, chat-ext-issue

**概要:** タイトル「haven」のテンプレ空 issue。

**直近のアクティビティ:** Bot がスパム判定で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [editor: preserve all multi-cursors through Copy Line Up/Down](https://github.com/microsoft/vscode/pull/312513) `#312513`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 同一行に 3 つ以上のマルチカーソルがあるとき Copy Line Up/Down で 2 つ以外が消える問題（`#309282`）の修正。`AbstractCopyLinesAction` でソース行を N 重に複製しない最適化が原因で全カーソルが残らなかった。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 3 ファイル（+104 / −4）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [editor: keep indentSize tied to tabSize after detect indentation](https://github.com/microsoft/vscode/pull/312515) `#312515`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `editor.detectIndentation` 有効時に「Change Tab Display Size」で `tabSize` だけが変わって実 indent は検出値のまま、というバグ（`#309962`）を `TextModel.detectIndentation` 周辺で修正する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2 ファイル（+41 / −1）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [chat: rank title matches above description matches in customizations list](https://github.com/microsoft/vscode/pull/312516) `#312516`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat / Agent Customizations 一覧の検索結果順序を、name（title）一致を description / filename / badge 一致より上位に揃える修正（`#309423`）。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 3 ファイル（+97 / −6）。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [1.117.0 Endgame](https://github.com/microsoft/vscode/issues/311006) `#311006`
**ステータス:** Closed | **ラベル:** endgame-plan

**概要:** 1.117.0 リリースの Endgame 計画 issue。

**直近のアクティビティ:** ユーザーが Endgame ガイドの中国語訳を引用してチェック手順を共有。リリース完了でクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Agent run_in_terminal fails with ENOPRO: No file system provider found in GitHub Codespaces](https://github.com/microsoft/vscode/issues/301804) `#301804`
**ステータス:** Open | **ラベル:** bug, web, chat-terminal

**概要:** Codespaces の Web 版 VS Code から Copilot Agent の `run_in_terminal` を実行すると `ENOPRO: No file system provider found` で失敗するという長年のバグ。

**直近のアクティビティ:** `smerschjohann` が「iPad Safari、Brave Browser など複数環境で再現する」と発生環境のリストを追記。

**次のアクション:** Web 版 VS Code 側のファイルシステムプロバイダ解決の修正。

---

### ![Issue](icons/issue-opened.svg) [runtime doesn't switch from planning to agent few times in a row](https://github.com/microsoft/vscode/issues/312518) `#312518`
**ステータス:** Closed | **ラベル:** chat-billing, new release

**概要:** Agent モード選択時でも planning に張り付き続け、トークンだけ消費してしまうという課金的にも痛い報告。

**直近のアクティビティ:** Bot が「meta issue `#252230` を参照」と案内しクローズ。

**次のアクション:** メタ issue で集約議論。

---

### ![Issue](icons/issue-opened.svg) [SubAgent stops showing activity in chat window and returns no response](https://github.com/microsoft/vscode/issues/312517) `#312517`
**ステータス:** Closed | **ラベル:** new release

**概要:** SubAgent が chat 上で activity を示さず無応答になるという 1.117.0 環境の報告。

**直近のアクティビティ:** Bot が「meta issue `#253126` を参照」と案内しクローズ。

**次のアクション:** メタ issue で集約議論。

---

### ![Issue](icons/issue-opened.svg) [Update](https://github.com/microsoft/vscode/issues/312514) `#312514`
**ステータス:** Closed | **ラベル:** spam

**概要:** 本文「ADD ISSUE DESCRIPTION HERE」のスパム issue。

**直近のアクティビティ:** Bot 自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [companion tab is not showing up](https://github.com/microsoft/vscode/issues/312510) `#312510`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** vscode.dev 環境で課題提出時に companion タブが現れないという報告（特定の教育系統合の用途と思われる）。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** triage と教育用統合の調査。

---

### ![Issue](icons/issue-opened.svg) [When Debugging ths up and breakpoint is added still when run the code pointer not stops at breakpoint.](https://github.com/microsoft/vscode/issues/312511) `#312511`
**ステータス:** Open | **ラベル:** triage-needed, new release

**概要:** 1.117.0 でブレークポイントを設定してもデバッグ時に停止しないというバグ報告。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** triage 待ち。

---

### ![Issue](icons/issue-opened.svg) [Support opening file with path enclosed in "" on Windows](https://github.com/microsoft/vscode/issues/312512) `#312512`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Windows で `"..."` で囲まれたパスでもファイルを開けるようにする要望。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** triage 待ち。

---

### ![Issue](icons/issue-opened.svg) [Frequent “Compacting conversation” loops causing extreme delays with reasoning models (Claude Sonnet 4.6)](https://github.com/microsoft/vscode/issues/309166) `#309166`
**ステータス:** Open | **ラベル:** info-needed

**概要:** Claude Sonnet 4.6 のような reasoning モデルで「Compacting conversation」状態に何度も突入し小タスクでも遅延が極端という報告。

**直近のアクティビティ:** `bhavyaus` が「1.116 か？ `backgroundCompaction` 設定は？ `largeToolResultsToDisk.enabled` は？」と diagnostic 質問を投げ、引き続き再現時のチャットログ共有を依頼。

**次のアクション:** 再現環境ログ提供待ち。

---

### ![Issue](icons/issue-opened.svg) [Agent gets stuck in Plan-mode state and fails to detect Start implementation](https://github.com/microsoft/vscode/issues/311940) `#311940`
**ステータス:** Open | **ラベル:** new release

**概要:** Plan モードで「Start implementation」と明示しても Plan に居座り続けるモード遷移バグ（GPT 5.3-Codex 利用時）。

**直近のアクティビティ:** `C0nstan` と `mneil` が「再起動しないと直らない」と複数の同症状を報告。

**次のアクション:** モード遷移検出の修正。

---

### ![Issue](icons/issue-opened.svg) [opensuse 15.6 nothing provides nodejs-vscode-tree-sitter-wasm = 0.1.4](https://github.com/microsoft/vscode/issues/312508) `#312508`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** openSUSE 15.6 の zypper リポジトリでパッケージ依存 `nodejs-vscode-tree-sitter-wasm = 0.1.4` を満たせずインストール失敗するというバグ報告。

**直近のアクティビティ:** Bot が「最新版 1.117.0 を試してください」と案内したのに対し、`kalymos` が「リポジトリで提供されている最新を使っており、これはインストーラ側のバグ」と反論。

**次のアクション:** RPM 依存記述の修正。

---

### ![PR](icons/git-pull-request.svg) [chat: avoid raw-text flicker for streaming HTML in markdown](https://github.com/microsoft/vscode/pull/312506) `#312506`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat 参加者がストリーミングする markdown に HTML（`<sup><span>...</span></sup>` 等）が含まれると、フォーマット完了前に生テキストがちらつく問題（`#239765`）。`fillInIncompleteTokens` がスライス済み段落を再字句解析する際の flicker を抑止する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2 ファイル（+109 / −1）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [chat: improve RTL handling in chat input and markdown content](https://github.com/microsoft/vscode/pull/312507) `#312507`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat 入力欄と markdown コンテンツの RTL 表示を改善。`dir=auto`、`unicode-bidi: plaintext`、`text-align: start` を適用し、コードブロックは `unicode-bidi: isolate-override` で LTR を強制（`#273696` の修正）。

**直近のアクティビティ:** COMMENTED レビュー 1 件。

**変更ファイル:** 3 ファイル（+12 / −1）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix: close zipfile handle and add error handler in zip reader](https://github.com/microsoft/vscode/pull/312503) `#312503`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `src/vs/base/node/zip.ts` の `read()` が yauzl で開いたファイルハンドルを閉じておらず、`once('error')` も登録していなかったため、ハンドルリークと不正 zip でのプロセスクラッシュが発生していた問題を修正する 600 行規模の改修。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 3 ファイル（+602 / −2）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [notebook: don't reveal previous cursor when clicking into markdown preview](https://github.com/microsoft/vscode/pull/312505) `#312505`
**ステータス:** Open | **ラベル:** (なし)

**概要:** notebook の markdown preview をクリックして編集に入ると、保存されたカーソル位置に戻ってしまう（`#232077`）。markdown preview のクリック時は `revealCursor` させないよう修正。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 1 ファイル（+8 / 0）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [webview: respect default localResourceRoots for custom editors](https://github.com/microsoft/vscode/pull/312492) `#312492`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `WebviewOptions.localResourceRoots` を未指定時にワークスペースフォルダ＋拡張インストールディレクトリを許可するというドキュメント通りの挙動を、custom editors にも適用する修正（`#282495`）。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 3 ファイル（+106 / 0）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [inlineCompletions: tighten keybinding spacing in NES gutter menu](https://github.com/microsoft/vscode/pull/312493) `#312493`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Next Edit Suggestions の gutter メニューで `Shift + Tab` キーバインドラベルの `+` の周りに余白が出てしまう（`#281478`）のを `KeybindingLabel` 共通コンポーネント側のスタイルで詰める修正。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 1 ファイル（+9 / −1）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [github: resolve SSH host aliases when classifying remotes](https://github.com/microsoft/vscode/pull/312494) `#312494`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `~/.ssh/config` の `Host gh-personal` のような github.com 用 alias 経由のリモート（`git@gh-personal:owner/repo.git`）を、GitHub 拡張の `getRepositoryFromUrl` が認識できるよう SSH 設定を解決する（`#283245`）。

**直近のアクティビティ:** CODENOTIFY が `@lszomoru` 宛にレビュー通知。

**変更ファイル:** `extensions/github/src/{extension,sshConfig}.ts` ほか 4 ファイル（+274 / −2）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [github: stably order repository quickpick results](https://github.com/microsoft/vscode/pull/312496) `#312496`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 「Clone from GitHub」/「Open Repository on GitHub」の quickpick で、入力ごとに GitHub Search API のランキングが変わり結果が再並び替えされてしまう（`#163603`）のを安定化する。

**直近のアクティビティ:** CODENOTIFY が `@lszomoru` 宛にレビュー通知。

**変更ファイル:** `extensions/github/src/{remoteSourceProvider.ts,...}` ほか 2 ファイル（+165 / −7）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Inline summarization: pass conversationId/turnId for prompt caching, disable WebSocket](https://github.com/microsoft/vscode/pull/312497) `#312497`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** インライン背景要約 `makeChatRequest2` 呼び出しに `turnId` と `conversationId` を渡し、Responses API の content-level prompt-prefix キャッシュが GPT-5.4 系で機能するよう改修。`useWebSocket` は明示 false。

**直近のアクティビティ:** COMMENTED レビュー 1 件。

**変更ファイル:** 1 ファイル（+4 / 0）。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [Fix "Open in VS Code" not opening workspace for Claude agent sessions](https://github.com/microsoft/vscode/pull/312468) `#312468`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Claude セッションから「Open in VS Code」した際にセッションは開くがワークスペースフォルダが開かれない問題（フォルダ URI 抽出が `CopilotCLISessionType.id` でしかゲートされていなかった）を修正。

**直近のアクティビティ:** APPROVED レビュー 2 件付き、マージ。

**変更ファイル:** `browser/openInVSCode.contribution.ts` ほか 3 ファイル（+37 / −4）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [round trip permission levels to extension](https://github.com/microsoft/vscode/pull/312479) `#312479`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** permission level を拡張に往復伝搬するための内部基盤 PR。7 ファイル、+167 / −7。

**直近のアクティビティ:** COMMENTED レビュー 1 件。

**変更ファイル:** 7 ファイル。

**次のアクション:** ドラフト整理。

---

### ![PR](icons/git-pull-request.svg) [BYOK: support tool_search for Claude Opus 4.7](https://github.com/microsoft/vscode/pull/312491) `#312491`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** BYOK Anthropic provider で Claude Opus 4.7 の `tool_search` をサポート。`modelSupportsToolSearch` が版数サフィックス付きの BYOK モデル ID（例 `claude-opus-4-7@1.0.0`）にマッチしなかった正規表現を、ファミリーごとの `startsWith` チェックに置換。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 3 ファイル（+19 / −14）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add 'last two messages' cache breakpoint strategy for Messages API](https://github.com/microsoft/vscode/pull/312472) `#312472`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** Anthropic Messages API のキャッシュブレークポイント配置戦略として、ヒューリスティックではなく最後にマージされた 2 メッセージに `cache_control` を置く方式を実験的に追加。`chat.anthropic.cacheBreakpoints.lastTwoMessages` フラグでゲート（`microsoft/vscode-internalbacklog#73`）。

**直近のアクティビティ:** COMMENTED レビュー 1 件。

**変更ファイル:** 6 ファイル（+380 / −18）。

**次のアクション:** Experiment 結果を見て本採用判断。

---

### ![PR](icons/git-pull-request.svg) [Don't add listener for webview webview resource load](https://github.com/microsoft/vscode/pull/312490) `#312490`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** webview の webview リソースロードに対し余分な listener を追加しないよう修正（`#312367`）。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 1 ファイル（+21 / −4）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add toolSearch to vscode toolset](https://github.com/microsoft/vscode/pull/312485) `#312485`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `vscode` の `languageModelToolSets` 貢献に `toolSearch` を明記し、`registerToolDefinition` の登録順タイミング問題に依存しない確実な紐付けを行う。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 1 ファイル（+1 / 0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Simplify subagent processing for Claude](https://github.com/microsoft/vscode/pull/312486) `#312486`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** API 側に `getSubagentMessages` が追加されたため、JSONL 手読みロジックを大幅に削減。「Task」ツールが「Agent」に名前変更されている点に対応し、Agents app 側のバグも合わせて修正する 15 ファイル改修。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 15 ファイル（+398 / −244）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: persist SCM repository visibility state across restarts](https://github.com/microsoft/vscode/pull/301029) `#301029`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Copilot worktree 等の `isHidden` リポジトリが `previousState` ロジックで visibility 復元を破壊する問題と、新規リポジトリのデフォルト可視化挙動を修正（`#301028`）。

**直近のアクティビティ:** CODENOTIFY が `@lszomoru` 宛に通知。

**変更ファイル:** `scmViewService.ts` など 2 ファイル（+237 / −18）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix enter/exit plan mode not reflecting in UI](https://github.com/microsoft/vscode/pull/312487) `#312487`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** plan モードへの enter/exit が一部の入力状態で UI に反映されないパイプラインの不備を修正。

**直近のアクティビティ:** APPROVED レビュー 3 件、マージ。

**変更ファイル:** 3 ファイル（+157 / −17）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [mobile: adjust context menu and toolbar positions/sizes.](https://github.com/microsoft/vscode/pull/312475) `#312475`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Phone レイアウトの 44px タッチターゲットルールが chat input ツールバーのボタンを 50px に押し広げ、隣接ドロップダウンラベルが 22px のままで視覚的にちぐはぐ、という問題を中心にコンテキストメニューと工具バーの位置・サイズを再調整。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 1 ファイル（+25 / −36）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix minor grammatical issues in README](https://github.com/microsoft/vscode/pull/312480) `#312480`
**ステータス:** Open | **ラベル:** (なし)

**概要:** README.md の文法と大文字小文字の細かい修正（`not only do we...we also` → `but we also`、`Cores` → `cores` など）。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 1 ファイル（+3 / −3）。

**次のアクション:** ドキュメンテーションメンテナの判断待ち。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): Implement Stateful Terminal Continuity (STC)](https://github.com/microsoft/vscode/pull/312482) `#312482`
**ステータス:** Open | **ラベル:** (なし)

**概要:** ターミナルプロセスのライフサイクルを VS Code ウィンドウから切り離し、永続的なバックグラウンドデーモンが裏で保持することで、ウィンドウリロード・クラッシュ・アップデートを跨いで scrollback ごとセッションを引き継げるようにする大型機能 PR（23 ファイル、+1010 / −8）。

**直近のアクティビティ:** COMMENTED レビュー 1 件、CODENOTIFY が `@anthonykim1` に通知。

**変更ファイル:** `src/vs/platform/terminal/node/ptyHostMain.ts` ほか。

**次のアクション:** アーキテクチャレビュー。

---

### ![PR](icons/git-pull-request.svg) [Enable BYOK without authentication](https://github.com/microsoft/vscode/pull/312207) `#312207`
**ステータス:** Open (Draft) | **ラベル:** model-byok

**概要:** エアギャップ環境で BYOK 経由の chat を可能にする改修（`microsoft/vscode-internalbacklog#7044` ほか）。

**直近のアクティビティ:** COMMENTED レビュー 5 件。

**変更ファイル:** 19 ファイル（+116 / −80）。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [chat: replace embedded MCP/plugin editors with compact detail widgets](https://github.com/microsoft/vscode/pull/312465) `#312465`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat Customizations 編集画面の split-pane 内で表示している `McpServerEditor` / `AgentPluginEditor` を、軽量な `EmbeddedMcpServerDetail` / `EmbeddedAgentPluginDetail` ウィジェットに置き換える整理。

**直近のアクティビティ:** Screenshot diff bot が UI スクリーンショットの差分を提示。COMMENTED レビュー 2 件。

**変更ファイル:** 6 ファイル（+508 / −50）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix input send button jumping](https://github.com/microsoft/vscode/pull/312476) `#312476`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** 入力送信ボタンのレイアウトジャンプを抑止。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 1 ファイル（+25 / −25）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: support XDG_CONFIG_HOME for argv.json](https://github.com/microsoft/vscode/pull/305206) `#305206`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Linux の `argv.json` を `~/.vscode/argv.json` 固定ではなく `$XDG_CONFIG_HOME/<dataFolderName>/argv.json` に従わせる修正（`#162712`）。

**直近のアクティビティ:** 投稿者 `maruthang` が main へのリベース完了を報告。

**変更ファイル:** 3 ファイル（+77 / 0）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [agentHost: configure plugins for remote agent hosts](https://github.com/microsoft/vscode/pull/312225) `#312225`
**ステータス:** Open | **ラベル:** (なし)

**概要:** リモート agent host が plugin 設定を所有できるよう、host-owned plugin customizations を AHP root config に永続化し、dispatch / side-effect 経路を通して live session の customization 状態を再 publish する大型 PR（51 ファイル、+3105 / −996）。

**直近のアクティビティ:** COMMENTED レビュー 5 件。

**変更ファイル:** 51 ファイル。

**次のアクション:** 設計レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix: resolve #305051 - fix listener leak on LanguageService.onDidChange](https://github.com/microsoft/vscode/pull/305186) `#305186`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 各 `LanguageSelection` が `LanguageService.onDidChange` に対し独自の `observableFromEvent` 購読を作成しており、開いているモデル数だけ listener が累積していたメモリリーク（`#305051`）を修正。

**直近のアクティビティ:** 投稿者がリベースを実施し、上流の `leakWarningThreshold: 200` mitigation との重なりを解消したと報告。COMMENTED レビュー 1 件。

**変更ファイル:** 2 ファイル（+112 / −6）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat: implement Copilot Context Pinning (Chat Breadcrumbs)](https://github.com/microsoft/vscode/pull/312350) `#312350`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** 長時間 Copilot セッションでのコンテキスト劣化緩和を狙う「Chat Breadcrumbs」機能（重要発言を永続バーにピン留め）の clean rebase 版（23 ファイル、+1010 / −8）。

**直近のアクティビティ:** CODENOTIFY が `@anthonykim1` に通知。

**変更ファイル:** 23 ファイル。

**次のアクション:** triage と機能レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(copilot): add defensive null checks for extension context subscriptions](https://github.com/microsoft/vscode/pull/311739) `#311739`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 拡張ホストレンダラがクラッシュしたあと、`extensionContext.subscriptions` を null チェックなしで参照することで起きるクラッシュ（`#311723`）を、optional chaining で防ぐ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2 ファイル（+6 / −2）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(terminal): use theme background for IME composition view](https://github.com/microsoft/vscode/pull/312119) `#312119`
**ステータス:** Open | **ラベル:** (なし)

**概要:** IME composition ビューがハードコード黒背景を使っていたため PowerShell ターミナル等で空白入力後に黒い矩形が残るバグ（`#308421`）を、テーマ変数 `--vscode-terminal-background` 利用に修正。

**直近のアクティビティ:** 投稿者 `rupeshkumar-555` がフィードバックを反映し、フォールバックを `transparent` に、色を別カスタマイズに変更したと共有。COMMENTED レビュー 1 件。

**変更ファイル:** 1 ファイル（+3 / −3）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Enabling ai co author by default](https://github.com/microsoft/vscode/pull/310226) `#310226`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** AI co-author をデフォルト有効化する 2 ファイル変更。

**直近のアクティビティ:** ユーザー `rgs2151` が「なぜデフォルトに？」と強い反対意見をコメント。APPROVED レビュー 2 件後マージ。

**変更ファイル:** 2 ファイル（+2 / −2）。

**次のアクション:** マージ済み（コミュニティへの説明が必要)。

---

### ![PR](icons/git-pull-request.svg) [fix: prevent duplicate Simple Browser panels on rapid show/restore race](https://github.com/microsoft/vscode/pull/312477) `#312477`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** ウィンドウリロード時に `show()` が連続呼びされて `_activeView` がセットされる前に複数 Simple Browser パネルが作られたり、`restore()` が新パネル存在時に旧パネルを蘇生させたりする race を `_simpleBrowserAlreadyOpen()` チェックで防ぐ。

**直近のアクティビティ:** CLA 同意のみ。

**変更ファイル:** 2 ファイル（+11 / −3）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Terminal: add move tab up/down and kill terminals below](https://github.com/microsoft/vscode/pull/309084) `#309084`
**ステータス:** Open | **ラベル:** (なし)

**概要:** ターミナルグループの並び替え用に `Move Terminal Tab Up` / `Down` と、アクティブグループ以下を一括 kill する `Kill Terminals Below` を追加（`#309068`）。

**直近のアクティビティ:** COMMENTED レビュー 5 件。

**変更ファイル:** 8 ファイル（+264 / −1）。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [sessions: fix account widget missing from titlebar after IntersectionObserver optimization](https://github.com/microsoft/vscode/pull/312469) `#312469`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `#312317` で導入した `MenuWorkbenchToolBar` の `IntersectionObserver` 最適化が原因で Agent Sessions デスクトップタイトルバーから account widget（avatar / sign-in）が消えていた問題を修正。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 1 ファイル（+46 / −45）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add authSessionAccountIcon proposed API and account avatar support](https://github.com/microsoft/vscode/pull/308654) `#308654`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 認証プロバイダがセッションアカウントのアイコン URL（プロフィールアバター等）を渡せるようにする proposed API `authSessionAccountIcon` を追加。`AuthenticationSessionAccountInformation.iconUrl` を導入。

**直近のアクティビティ:** CODENOTIFY が `@TylerLeonhardt` 宛に通知、COMMENTED レビュー 5 件。

**変更ファイル:** 9 ファイル（+105 / −10）。

**次のアクション:** Proposed API レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Chronicle: populate agent_name on sessions from invoke_agent spans](https://github.com/microsoft/vscode/pull/312300) `#312300`
**ステータス:** Closed (Draft) | **ラベル:** (なし)

**概要:** ローカル Chronicle ストアの `sessions.agent_name` が常に `'unknown'` のままだった原因（`gen_ai.agent.name` がフォールバックなしで読まれていた）を、`extractAgentName(span)` ピュア関数を導入して修正するドラフト。

**直近のアクティビティ:** 直近24時間のコメントなし、`#312293` 等に統合されてクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Few chronicle updates](https://github.com/microsoft/vscode/pull/312284) `#312284`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** Chronicle 周りの雑多な更新ドラフト。

**直近のアクティビティ:** Screenshot diff bot は変化なしと報告。COMMENTED レビュー 5 件。

**変更ファイル:** 14 ファイル（+338 / −61）。

**次のアクション:** ドラフト整理。

---

### ![PR](icons/git-pull-request.svg) [fix: populate cwd and assistant_response in Chronicle session store](https://github.com/microsoft/vscode/pull/312293) `#312293`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chronicle ローカルセッションストアで常に空だった `cwd` と `assistant_response` を、`vscode.workspace.workspaceFolders[0]` 等の consumer 側から埋める修正。OTel producer 側は変更しない方針で、残データギャップは schema の caveats として残す。

**直近のアクティビティ:** COMMENTED レビュー 4 件。

**変更ファイル:** 4 ファイル（+194 / −29）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [better autopilot wording + if no tools were called in the last round](https://github.com/microsoft/vscode/pull/312458) `#312458`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Autopilot ワーディング改善と「直近ラウンドでツールが呼ばれなかった場合は継続しない」という条件追加。

**直近のアクティビティ:** APPROVED レビュー 2 件、マージ。

**変更ファイル:** 2 ファイル（+37 / −10）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Document VS Code Selfhost Import Aid extension](https://github.com/microsoft/vscode/pull/312455) `#312455`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `VS Code Selfhost Import Aid` 拡張に README を追加し、用途・ワークスペース要件・issue 場所の説明を整える。アクティベーションとファイル監視を `src/vs` の TypeScript ソースワークスペースに絞る。

**直近のアクティビティ:** COMMENTED レビュー 4 件。

**変更ファイル:** 3 ファイル（+74 / −3）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Document VS Code Selfhost Test Provider extension](https://github.com/microsoft/vscode/pull/312459) `#312459`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `VS Code Selfhost Test Provider` 拡張への README 追加。virtual workspaces 非対応であることを明示し、コマンド・問題報告先を文書化。

**直近のアクティビティ:** COMMENTED レビュー 1 件。

**変更ファイル:** 2 ファイル（+83 / −1）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Lift customization items into a single observable model](https://github.com/microsoft/vscode/pull/312463) `#312463`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** Agents app のサイドバー Customizations ウィジェットが、実 Customizations editor とは別の discovery 経路（`customizationCounts.ts`）でセクション数を計算しているため `ChatSessionCustomizationProvider` 等の存在で乖離が生じていた問題を、単一 observable モデルに統合する大改修ドラフト（12 ファイル、+577 / −1560）。

**直近のアクティビティ:** Screenshot diff bot が 4 シーン分の変化を提示、COMMENTED レビュー 1 件。

**変更ファイル:** 12 ファイル。

**次のアクション:** ドラフト整理。

---

### ![PR](icons/git-pull-request.svg) [Fix quoted ID parsing in check-requirements-linux.sh](https://github.com/microsoft/vscode/pull/312466) `#312466`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `check-requirements-linux.sh` の `ID=([^"]+)` 正規表現がダブルクォート付き ID（`ID="rocky"` など Rocky / RHEL / CentOS / openSUSE）にマッチしない問題を、`/etc/os-release` を直接 source する形に変更して解消。

**直近のアクティビティ:** COMMENTED レビュー 1 件。

**変更ファイル:** 1 ファイル（+5 / −2）。

**次のアクション:** メンテナレビュー。

---

## qmk/qmk_firmware

> 自動 stale ボットがコミュニティキーボード PR とフィーチャー要望を整理する一日。レビュー指摘待ちが多く新規マージはなし。

### ![PR](icons/git-pull-request.svg) [Hybrid scroll for Ploopy Adept](https://github.com/qmk/qmk_firmware/pull/26171) `#26171`
**ステータス:** Open (Draft) | **ラベル:** keyboard

**概要:** Ploopy Adept のドラッグスクロールボタンに「短押しでトグル / 長押しでホールド」のハイブリッド挙動を実装。`PLOOPY_DRAGSCROLL_HOLD_THRESHOLD_MS` を境に押下時間に応じて挙動が変わる。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `keyboards/ploopyco/ploopyco.c`（+14 / 0）。

**次のアクション:** ドラフト解除とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add Ergo‑L keycodes and sendstring](https://github.com/qmk/qmk_firmware/pull/25766) `#25766`
**ステータス:** Open | **ラベル:** core, documentation, awaiting review, dd

**概要:** フランス語 `Ergo‑L` レイアウト用のキーコードと sendstring を追加。`qmk generate-keycode-extras --version latest --lang ergol` で生成された `keymap_ergol.h` を含む大型ドキュメンテーション PR。

**直近のアクティビティ:** 投稿者 `severindupouy` が「進行を止めているものはあるか、誰かレビューしてくれないか」とフォローアップを依頼。

**変更ファイル:** `data/constants/keycodes/extras/keycodes_ergol_0.0.1.hjson`（新規 +527）、`keymap_ergol.h`（+154）、`sendstring_ergol.h`（+100）、`reference_keymap_extras.md`（+1）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Anthony](https://github.com/qmk/qmk_firmware/pull/26170) `#26170`
**ステータス:** Closed | **ラベル:** keymap

**概要:** タイトル「Anthony」、`gopolar/gg86` キーマップとビルドルールを追加するだけのテンプレ未記入 PR。

**直近のアクティビティ:** 直近24時間のコメントなし。整理として close。

**変更ファイル:** `keyboards/gopolar/gg86/keymaps/uabbswag/{keymap.c,rules.mk}`（+218 / 0）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add sector245/s245_streamdeck keyboard](https://github.com/qmk/qmk_firmware/pull/26013) `#26013`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale, pr_checklist_pending

**概要:** RP2040 と 3x3 マトリクス + ロータリーエンコーダの S245 Streamdeck マクロパッドを追加する PR。

**直近のアクティビティ:** stale ボットが 45 日無活動を警告。`zvecr` がレビューで「キーボードの画像を提示してほしい」「`keyboards/handwired/sector245/s245/` に再配置し README のコンパイルコマンドを更新してほしい」と CHANGES_REQUESTED。

**変更ファイル:** `keyboard.json`、`keymaps/default/keymap.c`、`readme.md`（+92 / 0）。

**次のアクション:** レビュー指摘の反映と画像追加。

---

### ![PR](icons/git-pull-request.svg) [Adding keyboard - Keynet](https://github.com/qmk/qmk_firmware/pull/26022) `#26022`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale, pr_checklist_pending

**概要:** Atmega32u4 / Caterina ベースの分割オルソリニア（カラム staggered）キーボード「Keynet」の追加 PR（10 ファイル、+1116 / 0）。

**直近のアクティビティ:** stale ボットが警告。`lesshonor` と `zvecr` がレビューで「default keymap を pristine に保つ」「encoder map 機能を使う」など PR チェックリスト遵守を要求。

**変更ファイル:** `keyboards/handwired/keynet/keymaps/{default,light,max}/{keymap.c,rules.mk}` ほか。

**次のアクション:** レビュー指摘の反映。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Disable `SPECULATIVE_HOLD` during `LT`](https://github.com/qmk/qmk_firmware/issues/25906) `#25906`
**ステータス:** Closed | **ラベル:** enhancement, help wanted, stale

**概要:** ホームロウ mod（`LWIN_T(KC_A)` ほか）と同じキーに別レイヤで one-shot mod（`OS_LGUI` ほか）を割り当て、サムキー `LT(1, KC_SPC)` でレイヤ切替する構成のとき、`SPECULATIVE_HOLD` を `LT` 中だけ無効化したいという要望。

**直近のアクティビティ:** 30 日無活動でステイル自動クローズ。

**次のアクション:** クローズ済み（要望継続なら再オープン）。

---
