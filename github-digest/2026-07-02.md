# GitHub ダイジェスト — 2026-07-02

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[libghostty: fix utf-8 grapheme length overflow](https://github.com/ghostty-org/ghostty/pull/13145)** (ghostty-org/ghostty) — 7 バイトを超えるマルチスカラーのグラフェムクラスタで `u3` のバイト数アキュムレータがオーバーフローし、最適化ビルドで呼び出し側バッファを越えて書き込む恐れがあったメモリ安全性のバグを修正。境界ケースのテストも追加してマージ済み。
2. ![PR](icons/git-pull-request.svg) **[feat(:restart)!: no bang restores session and window layout](https://github.com/neovim/neovim/pull/40321)** (neovim/neovim) — `:restart` の既定挙動をセッション/ウィンドウレイアウト復元へ変更し、従来挙動を `:restart!` に移す破壊的変更がマージ。多くのユーザー要望に応えるもの。
3. ![PR](icons/git-pull-request.svg) **[Honda: stop disengagements caused by LKAS steering wheel nag](https://github.com/commaai/opendbc/pull/3498)** (commaai/opendbc) — Honda Bosch Radarless で純正 LKAS が有効化されると約 30 秒後に ACC が意図せず解除される問題を、SCM_BUTTONS を掌握して LKAS を自動無効化・ボタン抑止することで解消するセーフティ関連 PR。
4. ![PR](icons/git-pull-request.svg) **[Avoid redundant pyproject parsing](https://github.com/astral-sh/uv/pull/20076)** (astral-sh/uv) — 有効な `pyproject.toml` を 2 回パースしていた無駄を解消し、ワークスペース探索のベンチマークで最大 78%高速化。SIMD パース有効化(#20079)やリゾルバ改善と合わせ性能改善が集中。
5. ![Issue](icons/issue-opened.svg) **[2023 Bolt EV LT2 | LKAS Fault after 10 to 15 seconds](https://github.com/commaai/openpilot/issues/38275)** (commaai/openpilot) — 公式 openpilot と StarPilot の両方で 10〜15 秒後に LKAS フォルトが発生しエンゲージが解除される再現性のある不具合報告。工場出荷リセット後も再発しフォーク固有ではない。

---

## sunnypilot/sunnypilot

> HKG（現代・起亜）向けのレーダートラック表示や ccNC ポートといった車種対応 PR に加え、MADS のテスト追加とスタンドスティル時の再エンゲージ防止という制御修正が進行中。

### ![PR](icons/git-pull-request.svg) [HKG: Radar Tracks](https://github.com/sunnypilot/sunnypilot/pull/1723) `#1723`
**ステータス:** Open | **ラベル:** enhancement, ui, raylib

**概要:** 現代・起亜（HKG）車両のレーダートラックを UI 上に表示する機能を追加するドラフト PR。sp 拡張への移動、tizi/mici でのトグル、hda2 の異なるバスの処理、レーダー種別の自動検出などが TODO として残っている。replay 向けにレーダー種別自動検出やカスタムルート、レーダーヘルパー、レーダー UI などのツール群を大量に追加している。

**直近のアクティビティ:** github-actions[bot]の UI プレビューが mici と big の両方で「Videos differ!」（映像差分あり）を報告している。

**変更ファイル:** 16 ファイル変更（+1581/-1）。`tools/replay/ui_radar.py`（+912）や `auto_radar_detect.py`（+343）、`radar_helpers.py`（+209）などツール群が中心で、`selfdrive/ui/sunnypilot/onroad/radar_tracks.py` を新規追加。

**次のアクション:** TODO 項目（sp 拡張への移動、トグル、hda2 バス処理、レーダー種別自動検出）の完了とドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [MADS: add tests and prevent re-enable at standstill in Pause mode](https://github.com/sunnypilot/sunnypilot/pull/1871) `#1871`
**ステータス:** Open | **ラベル:** なし

**概要:** MADS のブレーキ設定が Pause のときの問題（community フォーラムの issue に対応）を修正する PR。スタンドスティル時に Pause モードで再エンゲージしないようにし、あわせて MADS のステアリングモードのテストを追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル変更（+243/-1）。`sunnypilot/mads/mads.py`（+1/-1）と、新規テスト `sunnypilot/mads/tests/test_mads_steering_mode.py`（+242）。

**次のアクション:** レビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open | **ラベル:** car

**概要:** 現代・起亜（HKG）の非 HDA2 車両に対する ccNC ポートのドラフト PR。関連する opendbc 側 PR（sunnypilot/opendbc#94）を参照している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル変更（+1/-1）。`opendbc_repo`（サブモジュール参照）のみ。

**次のアクション:** 依存する opendbc 側 PR の進行に合わせてドラフトを解除する。

---

## commaai/openpilot

> モデル関連（RL学習モデル、lebowski モデル、GPU ウェイト読み込み）のマージが活発で、横方向制御のチューニング可視化やドキュメント整合、CI 再有効化などの改善提案も出ている。一方で車両（Bolt EV）の LKAS フォルト報告や、長期無活動でクローズ予定の PR も見られる。

### ![PR](icons/git-pull-request.svg) [frame drops are never ok](https://github.com/commaai/openpilot/pull/38092) `#38092`
**ステータス:** Open | **ラベル:** stale

**概要:** フレームドロップを許容しないよう `selfdrive/selfdrived/selfdrived.py` を 1 行修正する PR。本文はテンプレートのみで具体的な説明は記載されていない。

**直近のアクティビティ:** github-actions[bot]が「24 日間活動がなく、あと 7 日で自動クローズされる」と通知した。

**変更ファイル:** `selfdrive/selfdrived/selfdrived.py` を 1 行追加・1 行削除で修正。

**次のアクション:** 自動クローズを避けるには、変更内容の説明追記やレビュー依頼など何らかの活動が必要。

---

### ![PR](icons/git-pull-request.svg) [feat: log latcontrol_torque low-speed proportional gain](https://github.com/commaai/openpilot/pull/38262) `#38262`
**ステータス:** Closed | **ラベル:** なし

**概要:** `latcontrol_torque` の低速時比例ゲインをログに出力する PR。`log.capnp` の `LateralTorqueState` に `kp` を追加し、コントローラから `pid_log.kp` を設定して、これまでメモリ上にしか存在しなかった速度補間後の `k_p` をチューニングや解析で観測できるようにする。低速で `kp` がログされることを検証するテストも追加。Issue #34907 に対応。

**直近のアクティビティ:** sshane が「これは Issue の意図と違う。トルク制御には別途、低速の動的係数があり、それがどこにもログされていない。ログを見てもオーバーシュートを引き起こす出力の一部がどこから来ているのか分からない」と指摘した。

**変更ファイル:** `openpilot/cereal/log.capnp`、`openpilot/selfdrive/controls/lib/latcontrol_torque.py`、`openpilot/selfdrive/controls/tests/test_latcontrol_torque_buffer.py` の 3 ファイルを変更。

**次のアクション:** クローズ済み。指摘された「低速の動的係数」を対象にログ出力を実装し直す必要がある。

---

### ![Issue](icons/issue-opened.svg) [latcontrol_torque: move low speed factor into logs](https://github.com/commaai/openpilot/issues/34907) `#34907`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** `latcontrol_torque` の低速係数（`kp` など）をログ化・プロット可能にする要望。この効果は非常に不透明で、例として低速でラグにより 0.2 m/s^2 の誤差が生じると誤差が非常に大きくなると説明されている。

**直近のアクティビティ:** sshane が「理想的ではないが、問題ではない」とコメントした。

**次のアクション:** クローズ済み。追加対応は不要と判断された。

---

### ![PR](icons/git-pull-request.svg) [desire: enter preLaneChange if blinker before engaged](https://github.com/commaai/openpilot/pull/38276) `#38276`
**ステータス:** Merged | **ラベル:** なし

**概要:** エンゲージ前にウィンカーが出ていた場合に `preLaneChange` 状態へ入るよう `desire_helper.py` を修正する PR。本文の記載はない。

**直近のアクティビティ:** github-actions[bot]のプロセスリプレイ差分レポートで「0 changed, 66 passed, 0 errors」と報告され、挙動変化なし。

**変更ファイル:** `openpilot/selfdrive/controls/lib/desire_helper.py` を 1 行追加・1 行削除で修正。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update README for driver monitoring model changes and formats](https://github.com/commaai/openpilot/pull/38274) `#38274`
**ステータス:** Closed | **ラベル:** autonomy

**概要:** ドライバーモニタリング（DM）の README を現行のモデル構成に合わせて更新する PR。過去に使われていた Qualcomm DSP の .dlc/SNPE パスの記述を削除し、現在の ONNX 由来 tinygrad pickle 推論に合わせる。本文では 2021 年の README 追加から 2024 年の GPU DM パス投入・即日リバート、SNPE からの移行、Tinygrad ランナー確立まで、ドキュメントが実態とずれた経緯が時系列で説明されている。

**直近のアクティビティ:** YassineYousfi が「README は削除した（コミット 4c5bef5）。最新でもなく有用でもない」とコメント。github-actions[bot]のプロセスリプレイは「0 changed, 66 passed, 0 errors」。

**変更ファイル:** `openpilot/selfdrive/modeld/models/README.md` を 25 行追加・26 行削除で修正。

**次のアクション:** クローズ済み。README 自体が削除されたため対応不要。

---

### ![PR](icons/git-pull-request.svg) [modeld: use correct intrinsics for extra transform when wide camera disabled](https://github.com/commaai/openpilot/pull/38239) `#38239`
**ステータス:** Merged | **ラベル:** なし

**概要:** `DISABLE_WIDE_ROAD` フラグ有効時に、big_img モデル入力でナローカメラに対して ecam の intrinsics を誤って使っていた問題を修正する PR。ワイドカメラが利用できない場合に適切な intrinsics を使うようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `openpilot/selfdrive/modeld/modeld.py` を 2 行追加・1 行削除で修正。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [re-enable MetaDrive simulator driving test](https://github.com/commaai/openpilot/pull/38272) `#38272`
**ステータス:** Closed | **ラベル:** なし

**概要:** GitHub Actions の無料 `ubuntu-24.04` ランナー上で MetaDrive シミュレータの走行テストを再有効化する PR。`test_metadrive_bridge.py` 用の `simulator driving` CI ジョブ追加、ジョブ内のみでの MetaDrive インストール、`pytest -n0` での実行、ヘッドレス CI 用ダミーオーディオ設定、`os.nice(19)` による CPU 負荷低減、カメラレンダリングのスロットリングなどを行う。フォークではフルワークフローが通り、信頼性向上の主因はプロセス優先度の低下だったと検証されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/tests.yaml`、`openpilot/selfdrive/selfdrived/selfdrived.py`、`openpilot/tools/sim/bridge/metadrive/metadrive_process.py`、`openpilot/tools/sim/bridge/metadrive/metadrive_world.py`、`openpilot/tools/sim/tests/test_sim_bridge.py` の 5 ファイルを変更。

**次のアクション:** クローズ済み。再有効化を進めるにはメンテナのレビュー・方針確認が必要。

---

### ![PR](icons/git-pull-request.svg) [Full RL](https://github.com/commaai/openpilot/pull/38164) `#38164`
**ステータス:** Open | **ラベル:** autonomy

**概要:** `driving_supercombo.onnx` を差し替える PR。本文はテンプレートのみで具体的な説明は記載されていない。

**直近のアクティビティ:** github-actions[bot]のプロセスリプレイは「0 changed, 66 passed, 0 errors」。Model Review では master と PR ブランチの `driving_supercombo.onnx` のレポートリンクが提示され、dmonitoring と big_driving は同一。いずれも直近 24 時間内のコメントではない。

**変更ファイル:** `openpilot/selfdrive/modeld/models/driving_supercombo.onnx` を 2 行追加・2 行削除で修正。

**次のアクション:** モデル差し替えの妥当性についてレビューと判断が必要。

---

### ![Issue](icons/issue-opened.svg) [2023 Bolt EV LT2 | LKAS Fault after 10 to 15 seconds on both StarPilot and official OpenPilot (Comma 4)](https://github.com/commaai/openpilot/issues/38275) `#38275`
**ステータス:** Open | **ラベル:** bug

**概要:** 2023 Bolt EV LT2 で、openpilot が正常にエンゲージし約 10〜15 秒間は完璧に動作するが、その後 LKAS フォルトが発生して openpilot が解除され、純正の運転支援（ACC・レーンキープ）も機能しなくなるという報告。StarPilot と公式 openpilot の両方で同一の挙動が再現され、Comma 4 の工場出荷リセット・再フラッシュ後も再発したため、フォーク固有ではないとしている。ハーネス／コネクタ、車両フィンガープリント／設定、CAN 通信、Comma 4 のハードウェアなど原因の切り分けについて助言を求めている。openpilot バージョンは 0.11.1、ルートも提示。なお StarPilot では手動で車両選択したが、openpilot では EUV として認識された点にも言及。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ログとルートを用いた原因調査（フィンガープリント誤認識や CAN 通信の確認）が必要。

---

### ![PR](icons/git-pull-request.svg) [lebowski model](https://github.com/commaai/openpilot/pull/38268) `#38268`
**ステータス:** Merged | **ラベル:** autonomy

**概要:** `big_driving_supercombo.onnx` を差し替える PR。本文はテンプレートのみで具体的な説明は記載されていない。

**直近のアクティビティ:** commaci-public がモデルリプレイのプロット（速度・desiredCurvature・leadsV3・laneLines・顔検出確率など）へのリンクを含むコメントを投稿。github-actions[bot]のプロセスリプレイは「0 changed, 66 passed, 0 errors」。いずれも直近 24 時間内のコメントではない。

**変更ファイル:** `openpilot/selfdrive/modeld/models/big_driving_supercombo.onnx` を 2 行追加・2 行削除で修正。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Reapply "usbgpu: never materialize model weights in RAM"](https://github.com/commaai/openpilot/pull/38273) `#38273`
**ステータス:** Merged | **ラベル:** なし

**概要:**「usbgpu: never materialize model weights in RAM」の変更を再適用する PR。本文はテンプレートのみで具体的な説明は記載されていない。

**直近のアクティビティ:** commaci-public がモデルリプレイのプロットリンクを含むコメントを投稿。github-actions[bot]のプロセスリプレイは「0 changed, 66 passed, 0 errors」。いずれも直近 24 時間内のコメントではない。

**変更ファイル:** `openpilot/common/file_chunker.py`、`openpilot/selfdrive/modeld/compile_modeld.py`、`openpilot/selfdrive/modeld/dmonitoringmodeld.py`、`openpilot/selfdrive/modeld/helpers.py`、`openpilot/selfdrive/modeld/modeld.py` の 5 ファイルを変更。

**次のアクション:** マージ済み。対応不要。

---

## commaai/opendbc

> Cadillac XT6 のフィンガープリント追加や Chrysler の fuzzy フィンガープリント、Rivian・Kia EV9 のバウンティ対応など車種サポートの PR が中心。加えて Honda Bosch Radarless の意図しない解除を防ぐセーフティ修正が進行中。

### ![PR](icons/git-pull-request.svg) [Add fingerprint Cadillac XT6 Sport 2023](https://github.com/commaai/opendbc/pull/3512) `#3512`
**ステータス:** Open | **ラベル:** car, gm

**概要:** 2023 Cadillac XT6 Sport（SDGM、BearTech SDGM B ハーネス経由）のフィンガープリントを追加する PR。Cadillac XT4 と同じ DBC ファミリで、固有の固定 CAN フィンガープリント（他プラットフォームとの衝突がないことを確認済み）を持つ。約 145 マイルの走行からライブ推定でトルク横制御をチューニング（latAccelFactor 0.9、friction 0.14、steerRatio 15.0）。他の SDGM 車と同様に純正ロンジチューディナルを使用。作者は SASCM モジュールも導入済みで、許されればロング制御にも取り組みたいと述べている。

**直近のアクティビティ:** github-actions[bot]の車挙動レポートが GMC_ACADIA・GMC_YUKON の 20 セグメントを「0 changed, 20 passed, 0 errors」で通過と報告。別途、コントリビューションチェックリストの案内レビューが自動投稿されている。

**変更ファイル:** 5 ファイル変更（+15/-1）。`opendbc/car/gm/fingerprints.py`、`interface.py`、`values.py`、`opendbc/car/tests/routes.py`、`opendbc/car/torque_data/override.toml`。

**次のアクション:** メンテナのレビューと、ロング制御対応可否の判断。

---

### ![PR](icons/git-pull-request.svg) [chrysler: reliable fuzzy fingerprinting](https://github.com/commaai/opendbc/pull/3508) `#3508`
**ステータス:** Closed | **ラベル:** car, chrysler

**概要:** Ford と同様に FW バージョンからベース部品番号をパースすることで、Chrysler の信頼性の高い fuzzy フィンガープリントを実装する PR。#1092 をクローズする狙い。

**直近のアクティビティ:** コントリビューションチェックリストの案内レビューが自動投稿されているのみ。

**変更ファイル:** 1 ファイル変更（+43/-1）。`opendbc/car/chrysler/values.py`。

**次のアクション:** クローズ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Rivian cruise set speed tracking from steering stalk signals](https://github.com/commaai/opendbc/pull/3491) `#3491`
**ステータス:** Closed | **ラベル:** car, rivian

**概要:** Issue #1833（1,000 ドルのバウンティ）を解決する PR。CAN Bus 0（pt）の VDM_AdasStalk メッセージ信号をパースして、Rivian 車両でステアリングストークベースのクルーズ設定速度トラッキングを実装する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル変更（+34/-2）。`opendbc/car/rivian/carstate.py`。

**次のアクション:** クローズ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Kia EV9 lateral safety controls](https://github.com/commaai/opendbc/pull/3490) `#3490`
**ステータス:** Closed | **ラベル:** car, hyundai, car safety

**概要:** Issue #1175（1,000 ドルのバウンティ）を解決する PR。hyundai_canfd セーフティモジュールに LFA2 の角度ベースのステアリング安全検証を追加し、Kia EV9 の CAN-FD ネットワーク上で安全チェックと制限を強制する。

**直近のアクティビティ:** コントリビューションチェックリストの案内レビューが自動投稿されているのみ。

**変更ファイル:** 2 ファイル変更（+37/-4）。`opendbc/car/hyundai/values.py`（+1）と `opendbc/safety/modes/hyundai_canfd.h`（+36/-4）。

**次のアクション:** クローズ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Honda: stop disengagements caused by LKAS steering wheel nag](https://github.com/commaai/opendbc/pull/3498) `#3498`
**ステータス:** Open | **ラベル:** car, honda, car safety

**概要:** Honda Bosch Radarless での「ランダムな解除」（openpilot#36015）に対処する PR。純正 LKAS を有効にするとステアリングホイールのタッチタイマー／nag により ACC が解除される問題があり、ドライバーが LKAS ボタンを押すと約 30 秒後に予期しない強制解除が起こる。この PR は、(1) エンゲージ中に純正 LKAS が有効なとき、カメラへ LKAS ボタン押下を送って LKAS を無効化し、(2) エンゲージ中はドライバーの LKAS ボタン押下がカメラへ届くのを遮断する。SCM_BUTTONS メッセージを掌握することで、カメラが openpilot からの LKAS ボタン押下を確実に受け付けるようにしている（従来のボタン連打では不安定だった）。

**直近のアクティビティ:** コントリビューションチェックリストの案内レビューが自動投稿されているのみ（人的なコメントは直近 24 時間なし）。

**変更ファイル:** 4 ファイル変更（+47/-8）。`opendbc/car/honda/carcontroller.py`（+23/-3）、`hondacan.py`、`opendbc/safety/modes/honda.h`（+13/-1）、`opendbc/safety/tests/test_honda.py`。

**次のアクション:** セーフティに関わる変更のため、メンテナによる慎重なレビュー。

---

## commaai/panda

> 直近24時間の更新はありません。

---

## neovim/neovim

> 直近24時間は `vim-patch` 系の取り込みが多数マージされ、`dir.lua`（ディレクトリブラウザ）の有効化・無効化の仕組みや `:restart` のセッション復元、`vim.fs` のエラー報告、cmdwin まわりのリファクタが活発に議論されている。パス正規化・Windows パス処理・補完（'autocompletedelay'）の改善も継続中。

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40519) `#40519`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** github-actions[bot]による自動生成の `src/nvim/version.c` 更新 PR。GTK GUI で 'lazyredraw' 時に遅延再描画がスキップされる問題や、zsh でプロンプト由来により test_popupwin が失敗するテスト問題などの vim-patch 記載を含む。ドラフト状態。

**直近のアクティビティ:** janlazo が、`included_patches` 配列でパッチ番号が 2171 のまま 2172 に更新されていない記載ミスを diff 付きで指摘した。この種のミスはまれで、前回は v8.1.0744（符号付き/符号なし文字列のコンパイラ警告）のときだったとコメント。

**変更ファイル:** `src/nvim/version.c` のみ（+22 / -24）。

**次のアクション:** 指摘されたパッチ番号のずれを修正し、ドラフトを解除する。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add nvim_win_resize()](https://github.com/neovim/neovim/pull/40520) `#40520`
**ステータス:** Open | **ラベル:** api

**概要:** #6645 に関連し、`nvim_win_resize(win, {height, width, anchor})` を追加する。ウィンドウをリサイズする際に、どの端（anchor）から領域を取るかを選べるようにし、上や左のウィンドウから領域を取って上方向・左方向に伸ばせるようにする。デフォルトの anchor は既存の `nvim_win_set_height()`/`nvim_win_set_width()` と同じ挙動になり、これらは deprecated 化される。

**直近のアクティビティ:** justinmk が最後の数点のコメント対応を条件に APPROVED（LGTM）。ただし oldtest の `Test_set_cursor` が失敗している点が未解決で、justinmk は「3 ジョブが同じテストで『フレーク』で落ちることはない。ウィンドウ挙動の変更が perl テストの依存するカーソル挙動に影響した可能性があり、バグ導入とは限らない。perl provider 側のバグの可能性もある」と説明。作者 XiaowenHu96 はローカルで再現できず数日後に再調査するとコメント。anchor 名は将来 "topleft" などへ拡張可能にする方針で合意。

**変更ファイル:** api ドキュメント（api.txt / deprecated.txt / news.txt）、`runtime/lua/vim/_meta/api.gen.lua`、`src/nvim/api/window.c`・`deprecated.c`・`keysets_defs.h`、`src/nvim/window.c`、テスト（window_spec.lua / deprecated_spec.lua）など計 17 ファイル（+499 / -174）。

**次のアクション:** `Test_set_cursor` 失敗の原因を切り分け、バグでなければテストをスキップする。他のレビュアーのコメントを数日待って対応後にマージ。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): mark 'tabpanel' files as N/A](https://github.com/neovim/neovim/pull/40538) `#40538`
**ステータス:** Open | **ラベル:** build, vim-patch

**概要:** 'tabpanel' 機能関連ファイルを `scripts/vim_na_files.txt` に N/A（対象外）として追記する。#34273 は再オープンされておらず、現状のコードベースとエコシステムを踏まえるとこの機能に C コードを割く価値があるか不明。Nvim は "autocmd_add()" 同様この機能を移植していないため CVE を回避できたとする。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zeertzjq が APPROVED）。

**変更ファイル:** `scripts/vim_na_files.txt` のみ（+2 / -0）。

**次のアクション:** マージ。もしこの機能を C コードベースに受け入れる場合は、Vim の最新状態を移植する必要がある。

---

### ![PR](icons/git-pull-request.svg) [fix(dir): move parent mapping to defaults](https://github.com/neovim/neovim/pull/40531) `#40531`
**ステータス:** Open | **ラベル:** defaults

**概要:** #40530 に関連。dir.lua のマッピングは autocmd の順序に結合しており上書きが難しいという問題に対し、マッピングを defaults へ移動する。また dir.lua が無効化されている場合は組み込みの `-` モーションへフォールバックし、未定義の対象へのマッピング作成を防ぐ。

**直近のアクティビティ:** zeertzjq が COMMENTED（レビューコメント）を 1 件残した。

**変更ファイル:** `runtime/doc/vim_diff.txt`、`runtime/lua/vim/_core/defaults.lua`、`runtime/plugin/dir.lua`、テスト（defaults_spec.lua / dir_spec.lua）の計 5 ファイル（+37 / -16）。

**次のアクション:** zeertzjq のレビューコメントに対応する。

---

### ![Issue](icons/issue-opened.svg) [vim.pack: survive/handle session restore](https://github.com/neovim/neovim/issues/40537) `#40537`
**ステータス:** Open | **ラベル:** editor-state, packages

**概要:** `vim.pack` の更新バッファが、セッション復元時（例: `:restart`）に不正な挙動をする。再現手順は `nvim --clean` → `:packupdate` → `:restart`。期待動作として、(1) セッションファイル読み込み時に `:packupdate` のウィンドウを削除するか、(2) バッファを再構築してシームレスにする（作者の推奨。ただしバッファ名にバッファ番号を含めない変更が必要になりそう）のいずれかを挙げる。関連 #1180、#40336。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** セッション復元時の更新バッファの扱い（削除または再構築）を設計・実装する。

---

### ![PR](icons/git-pull-request.svg) [fix(health): close floating window when focus leaves it](https://github.com/neovim/neovim/pull/40536) `#40536`
**ステータス:** Open | **ラベル:** ux-visibility

**概要:** フロート表示の `:checkhealth` がフォーカスを離れても閉じない問題を修正。`BufLeave` で閉じるようにし、`gO` で目次（ToC）の quickfix を開くときはスキップする。#39307 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/health.lua`（+15）とテスト `test/functional/plugin/health_spec.lua`（+7）の計 2 ファイル。

**次のアクション:** レビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:partial:9.2.0762: duplicated sub-option name check in :set completion](https://github.com/neovim/neovim/pull/40535) `#40535`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted 🤖

**概要:** `:set` 補完で同じサブオプション名のチェックが 10 回重複している問題に対し、そのチェックを新関数 `completing_value_for_subopt()` に抽出する（Shane Harper）。機能変更なし。closes vim/vim#20676。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/optionstr.c` のみ（+11 / -8）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: runtime file updates](https://github.com/neovim/neovim/pull/40534) `#40534`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** ランタイムファイルの更新をまとめて取り込む。(1) typst の ftplugin と syntax ファイルを改善（空白整形設定を indent から filetype plugin へ移し "typst_recommended_style" 設定で制御、browsefilter 設定、syntax 改善）。(2) screen の syntax をバージョン 5 対応まで更新（v5.0.0/v4.5.0 の新コマンド対応、廃止コマンドの deprecate、Braille ナビゲーションコマンドの分離、文字列エスケープや 8 進数の対応など）。(3) xml syntax ファイルの性能改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/autoload/typst.vim`、`runtime/ftplugin/typst.vim`、`runtime/indent/typst.vim`、`runtime/syntax/typst.vim`・`screen.vim`・`xml.vim`、`runtime/doc/filetype.txt` の計 7 ファイル（+509 / -623）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0761: runtime(netrw): Unix: unable to open '\' file](https://github.com/neovim/neovim/pull/40533) `#40533`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** Unix 上の netrw で `\` を含むファイルを開けない問題を、ディレクトリパターンの調整で修正（Manoj Panda）。#15439 を修正。closes vim/vim#20685。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/pack/dist/opt/netrw/autoload/netrw.vim`（+2 / -2）とテスト `test/old/testdir/test_plugin_netrw.vim`（+23）の計 2 ファイル。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Netrw can't open `\`](https://github.com/neovim/neovim/issues/15439) `#15439`
**ステータス:** Closed | **ラベル:** bug-vim, netrw

**概要:** netrw で `\` という名前のファイルを開けないというバグ報告（v0.5.0、Gentoo Linux、zsh 5.8）。`touch \\` でファイルを作り、nvim で `:E` → `j` で `\` に移動して開こうとしても何も起きない。期待動作は空ファイル `\` が開くこと。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40533 のマージに伴いクローズされた）。

**次のアクション:** 対応不要（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(ui): implement :detach! to disconnect other UIs](https://github.com/neovim/neovim/pull/39216) `#39216`
**ステータス:** Open | **ラベル:** ui, AI assisted 🤖

**概要:** #39213 をクローズする。`ex_detach()` には `:detach!` のスタブがあり `"bang (!) not supported yet"` でエラーになる。共有 Nvim サーバーから他の UI を切断する手段がない状態を解消し、`:detach!` は呼び出し元以外のすべての UI を切断してサーバーは動作継続・呼び出し元は接続維持する。UI が 1 つのときは no-op、UI 未接続時は `E5768: No UI attached` エラー。

**直近のアクティビティ:** 過去に justinmk・zeertzjq・作者 jasonwoodland の間で多数のレビューコメントのやり取りがある（最新は 2026-06-26）。直近 24 時間のコメントなし。

**変更ファイル:** api.txt / gui.txt / news.txt、`runtime/lua/vim/_core/server.lua`、`src/nvim/api/vim.c`、`src/nvim/errors.h`、`src/nvim/ex_docmd.c`、テスト（tui_spec.lua など）の計 11 ファイル（+123 / -26）。

**次のアクション:** 既存レビューコメントへの対応を継続する。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): detect N/A C files based on diff](https://github.com/neovim/neovim/pull/40473) `#40473`
**ステータス:** Open | **ラベル:** build, vim-patch

**概要:** `vim_na_cfuncs.txt` に Nvim にとって N/A な Vim の C 関数（`src/proto/*.proto` ベース）を列挙し、version.c は当面ホワイトリスト扱いにする。`git diff` の C シンボル検出（-U0 でハンクを C 関数シンボルに紐付けやすくする）に依存するため Git のバージョン要件が生じる可能性がある。v9.0.0515 以降の N/A パッチを検出できる想定。

**直近のアクティビティ:** justinmk が「ローカルツール限定であれば Git バージョンを気にする必要はない。nvim のビルドに必須でない限り問題なく、Git v2 は 10 年以上前のものだ」とコメント。janlazo は version.c のハンク検証のため `vim_na_cfuncs.txt` を配列スキャンに変える案を次の PR として提示、Git v2.0.0 が最低必要になりそうと述べる。

**変更ファイル:** `scripts/vim-patch.sh`（+25 / -12）と新規 `scripts/vim_na_cfuncs.txt`（+36）の計 2 ファイル。

**次のアクション:** version.c ハンク検証のための配列スキャンを次の PR で対応。ドキュメントファイルの N/A レビューを進める。

---

### ![PR](icons/git-pull-request.svg) [build(wasm): add missing platform stubs](https://github.com/neovim/neovim/pull/40306) `#40306`
**ステータス:** Merged | **ラベル:** gsoc

**概要:** LTO（Link Time Optimization）を有効化し、メモリ・スタック上限を調整、Asyncify と例外処理を有効化、未定義シンボルの厳格チェックを復活させてリンク時に依存欠落を検出できるようにする。加えて WASM 環境で利用できない libuv やプラットフォーム固有関数の Emscripten 互換代替を提供する `wasm_stubs.c` を導入する。

**直近のアクティビティ:** justinmk が「では BUILD.md にセクションを起こせるか、粗くてもよいので追記があると助かる」と依頼し、作者 Rawan10101 が「はい、了解」と応答。

**変更ファイル:** `build.zig`、`build.zig.zon`、新規 `contrib/test_wasm.html`（+68）、`src/nvim/CMakeLists.txt`、新規 `src/wasm_stubs.c`（+181）の計 5 ファイル（+282 / -11）。

**次のアクション:** BUILD.md に WASM ビルド手順のセクションを追加する（作者が対応予定）。

---

### ![PR](icons/git-pull-request.svg) [fix(dir): remove global parent mapping](https://github.com/neovim/neovim/pull/40530) `#40530`
**ステータス:** Closed | **ラベル:** なし

**概要:** dir.lua の無効化/有効化が分かりにくい問題への対応案。グローバルな `-` マッピングを削除し、ディレクトリを適切にディスパッチし（#40425）、dir.lua の「面」を autocmd `nvim.dir` のみにすることで、プラグイン作者は autocmd を削除するだけで無効化できるようにする狙い。

**直近のアクティビティ:** justinmk と作者 barrettruth の間で COMMENTED のやり取りが 3 件（2026-07-01）。この PR はクローズされ、後継として #40531 / #40529 などに分割された。

**変更ファイル:** `runtime/doc/plugins.txt`、`runtime/plugin/dir.lua`、`test/functional/plugin/dir_spec.lua` の計 3 ファイル（+4 / -22）。

**次のアクション:** 対応不要（クローズ済み。後続 PR に引き継がれた）。

---

### ![Issue](icons/issue-opened.svg) [Regression: Ruby Tree-sitter scrolling becomes very slow on repeated `A #` lines](https://github.com/neovim/neovim/issues/40517) `#40517`
**ステータス:** Open | **ラベル:** performance, has:plan, bug-regression, dependencies, treesitter

**概要:** tree-sitter 依存のバンプ後、Tree-sitter を有効にした Ruby バッファのスクロールが非常に遅くなる回帰。`A #` を 1000 行繰り返した合成バッファで `<C-f>` が非常に遅く、押しっぱなしにすると CPU 使用率が 100%に達する。Vim では同様の挙動は起きない（NVIM v0.13.0-dev-881+gc040f53dc1、Ubuntu 26.04）。

**直近のアクティビティ:** WillLillis が「回帰は tree-sitter/tree-sitter#5712 の変更が原因。変更自体は正しいが tree-sitter のクエリエンジンで状態爆発を招いた。Ruby のクエリに `.`（アンカー）を追加すれば容易に直せる（本来最初から付いているべきだった）」と diff 付きで説明。加えて tree-sitter/tree-sitter#5725 のクエリエンジン性能改善でいずれのクエリも使えるようになるとした。

**次のアクション:** `runtime/queries/ruby/highlights.scm` にアンカー `.` を追加する修正を取り込む。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.{0739,0748,0749,0750,0755,0759}: 'autocompletedelay' fixes](https://github.com/neovim/neovim/pull/40525) `#40525`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted 🤖

**概要:** #40064 を修正。'autocompletedelay' が非ゼロのときにメインループをブロックして Insert モードの autocommand（TextChangedI/TextChangedP/CursorMovedI）が遅延・欠落する問題を、遅延をノンブロッキング化して解消する（CursorHoldI に倣った入力待ちタイムアウト K_COMPLETE_DELAY で遅延）。関連する CTRL-G U / i_CTRL-K の干渉、Insert モード離脱後の状態リーク、マクロ記録時の挙動不整合、不要になったコードの削除などの後続パッチをまとめて取り込む。

**直近のアクティビティ:** justinmk が「これはものすごくハック的に見える :(」とコメント、zeertzjq が「元の実装より泥臭くないと思う :)」と返し、justinmk が「それなら安心だ :)」と応じた。

**変更ファイル:** `src/nvim/edit.c`、`src/nvim/getchar.c`、`src/nvim/insexpand.c`、`src/nvim/keycodes.h`、`src/nvim/os/input.c`、テスト（completion_spec.lua / test_ins_complete.vim）の計 7 ファイル（+246 / -78）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) ['autocompletedelay' adds actual delay to 'TextChangedP' autocmd](https://github.com/neovim/neovim/issues/40064) `#40064`
**ステータス:** Closed | **ラベル:** has:vim-patch, bug-vim, completion, events

**概要:** `autocomplete` を有効にし `autocompletedelay` を 0 より大きい値にすると、`TextChangedP` の autocmd 発火時に実際の遅延が発生し、同時に `CursorMovedI` や `TextChangedI` も発火しなくなる。Insert モードでのカーソル移動/テキスト変更を正しく検出できなくなる破壊的変更だとする（0.13 nightly、Linux）。再現用 `init.lua` を提示。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40525 のマージに伴いクローズ）。

**次のアクション:** 対応不要（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(runtime): dispatch directories via `FileType`](https://github.com/neovim/neovim/pull/40425) `#40425`
**ステータス:** Open | **ラベル:** runtime

**概要:** #40496 / #40489 / #40529 に依存、#31621 をクローズ。ディレクトリバッファが組み込みブラウザによって `filetype=directory` 設定前に開かれるため、他のディレクトリブラウザが `FileType` イベントを先に処理できない問題を解消する。Nvim がローカルのディレクトリバッファを検出した時点で `filetype=directory` を設定する。ドラフト状態。

**直近のアクティビティ:** justinmk が「filetype 割り当てを autocmd から C のコアなディレクトリ編集パスへ移して `filetype=directory` を設定するのはどうか」「`vim.filetype` が filetype を設定すべきでは」とコメント。barrettruth は「そのために #40529 を出した」と応答した。

**変更ファイル:** `runtime/lua/nvim/dir.lua`、`runtime/lua/vim/filetype.lua`、`runtime/plugin/dir.lua`、`src/nvim/buffer.c`、`runtime/doc/plugins.txt`、テスト（filetype_spec.lua / dir_spec.lua）の計 7 ファイル（+78 / -85）。

**次のアクション:** 依存 PR（#40529 など）のマージ後にリベースし、ドラフトを解除する。

---

### ![PR](icons/git-pull-request.svg) [fix(dir): detect directory filetype](https://github.com/neovim/neovim/pull/40529) `#40529`
**ステータス:** Open | **ラベル:** filetype

**概要:** #40425 に関連。ディレクトリバッファはディレクトリブラウザの後段 autocmd から `filetype=directory` を得るため、`FileType directory` がディレクトリハンドラのディスパッチ地点として使うには遅すぎる。justinmk の提案どおり `vim.filetype` で実ディレクトリを検出し、コアのディレクトリ編集パスが後段の buffer-enter フローより前に filetypedetect を実行するようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/filetype.lua`（+7）、`src/nvim/buffer.c`（+12）、テスト（filetype_spec.lua / dir_spec.lua）の計 4 ファイル（+55 / -0）。

**次のアクション:** レビューを待ち、#40425 の前提としてマージする。

---

### ![PR](icons/git-pull-request.svg) [fix(dir): restore loaded plugin guard](https://github.com/neovim/neovim/pull/40528) `#40528`
**ステータス:** Merged | **ラベル:** なし

**概要:** 現状 dir.lua の無効化が autocmd を削除するだけでは済まない問題（#40458 のコメント参照）に対し、当面グローバル変数によるガード（`g:loaded_`）を復活させる。

**直近のアクティビティ:** justinmk が「ありがとう。手間をかけてすまない」とコメント。

**変更ファイル:** `runtime/doc/plugins.txt`、`runtime/plugin/dir.lua`、`test/functional/plugin/dir_spec.lua` の計 3 ファイル（+19 / -3）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [doc(vim_diff): move ":mode" to nvim-changed](https://github.com/neovim/neovim/pull/40527) `#40527`
**ステータス:** Open | **ラベル:** documentation

**概要:** `:mode` を `nvim-removed` から移す。完全には削除されていないコマンド/関数を、`:h vim-diff` から削除済みの ex コマンド・Vim eval 関数を導出する #40473 のために `nvim-changed` へ分類し直す。あわせて「changed と removed の分類基準は何か」を問題提起する（基本的な期待動作が維持され `:h` でアクセスできるなら `nvim-changed` などが適切ではないか）。ドラフト状態。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/vim_diff.txt` のみ（+1 / -1）。

**次のアクション:** changed/removed の分類基準について合意を得て、ドラフトを解除する。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): jobstart() term:// buffer name](https://github.com/neovim/neovim/pull/35951) `#35951`
**ステータス:** Open | **ラベル:** defaults, terminal

**概要:** `jobstart(term=true)` が生成する `term://` バッファ名が完全なコマンドを設定せず壊れている（分かりにくく、`term://` バッファ名を訪れても端末を「復元」＝再起動できない）問題を修正。完全な cmd+args を JSON 配列として URI に付加し、デフォルトの `BufReadCmd term://*` ハンドラを改善（JSON コマンド形式対応、Lua への書き換え）。URI の `#fragment` サポートも追加。#3278 / #14525 / #32932 を修正、#31494 に関連。ドラフト状態。

**直近のアクティビティ:** 過去に justinmk のレビューコメントと github-advanced-security[bot]のコメントがある。直近 24 時間のコメントなし。

**変更ファイル:** ドキュメント多数（cmdline.txt / editing.txt / terminal.txt / options.txt など）、`runtime/lua/vim/_core/defaults.lua`、`src/nvim/os/shell.c`、`src/nvim/eval/funcs.c`、新規テスト `test/functional/terminal/bufname_uri_spec.lua`（+318）など計 30 ファイル（+737 / -272）。

**次のアクション:** URI `#fragment` サポートの完成と、`jobstart` が既存端末バッファを再利用する際のリーク修正（別 PR 想定）。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.fs): dir(), find() error-reporting](https://github.com/neovim/neovim/pull/40114) `#40114`
**ステータス:** Merged | **ラベル:** filesystem

**概要:** #39878 を修正。`vim.fs.dir()` と `vim.fs.find()` が `uv.fs_scandir()` のファイルシステムエラーを捨てており、読めないディレクトリが空ディレクトリと同じ結果に見える問題を解消する。既存の呼び出し元との後方互換を保ちつつ、`dir()` はルートスキャン失敗を第 2 戻り値で、再帰スキャン失敗をイテレータの第 3 戻り値 `err` で返し、`find()` は走査エラーを `{ path, err }` の配列として第 2 戻り値で返す。

**直近のアクティビティ:** barrettruth が「dir.lua も更新してくれてありがとう」とコメント。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/lua/nvim/dir.lua`、`runtime/lua/vim/fs.lua`（+86 / -41）、テスト `test/functional/lua/fs_spec.lua`（+132）など計 5 ファイル（+248 / -57）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [reversible "slug" utility for arbitrary filesystem paths](https://github.com/neovim/neovim/issues/40460) `#40460`
**ステータス:** Open | **ラベル:** plugin, lua, has:plan, core, filesystem, editor-state

**概要:** 複数のサブシステム（'undodir'/swapfiles、今後の端末状態ディレクトリ #40097、`:connect ssh://` の ControlPath ソケット名など）が任意パスから短くファイルシステム安全な識別子を導出する処理を個別に再発明している。共有ユーティリティがなく、挙動や長さ保証が不統一。理想は一方向ハッシュではなく可逆な双方向圧縮で元パスを復元できること、加えて長さ上限をサポートすること。#39378 から派生。

**直近のアクティビティ:** justinmk が具体的な slug のイメージ（swapfile 風の `%` 区切りより `-` 区切りが読みやすい）を例示し、当初検討した「可逆」要件は「短さ」要件と衝突し、swap/undo/端末状態はメタデータにパスを保存済みなので不要と結論。「上限付き一方向 `slug(path, {maxlen=N})`、`<readable-truncated-prefix>-<short-hash>` 形式」「既知プレフィックス（$HOME、cwd、stdpath('data')、プラグインルート）の辞書置換」「既存 `pathshorten()` 拡張も検討」という提案を提示。bew が「よく行うことの統一より『根本的なニーズ』に焦点を当てて API 設計すべきでは」と問い、justinmk がデータベース抽象化（sqlite 統合や 'shada' 拡張、#1180）はスコープ外で、この小さな戦術的改善はそれと衝突しないと説明した。

**次のアクション:** 上限付き一方向 slug ユーティリティの API を設計・実装する。

---

### ![PR](icons/git-pull-request.svg) [feat(ui): externalize handling of window layout and control (wincmds) to UIs](https://github.com/neovim/neovim/pull/38086) `#38086`
**ステータス:** Open | **ラベル:** ui-extensibility

**概要:** #13504（元は #8707）の継続。`ext_windows` UI オプションを追加し、ウィンドウの配置とナビゲーションを UI に委譲する。有効時、Nvim はフレームツリーを保持せず、ウィンドウ操作（`win_split`、`win_move_cursor`、`win_exchange`、`win_rotate`、`win_resize`、`win_resize_equal`、`win_move`）のイベントを送信して UI が独自のウィンドウ管理を実装できるようにする。`ext_multigrid` と `ext_linegrid` を暗黙的に有効化。#2464 を修正。ドラフト状態。

**直近のアクティビティ:** vhakulinen が「ウィンドウレイアウトを GUI に委譲するのではなく、nvim のウィンドウ処理を完全に無効化して GUI に任せてはどうか（グリッド作成/クローズ/リサイズの API を用意し、レイアウト状態を発信しない）」と提案。justinmk は「`nvim_open_win()` の `external` フラグが近いことをするが、全ウィンドウで行うと `c-w` ナビゲーションを UI に委譲する必要があり、UI の役割を有用でない形に変えてしまう気がする」とコメント。作者 akiyosi は #8707 以降のイベントとの重複を精査し、新イベントは状態ブロードキャストではなく「操作の委譲」で重複はないが `win_resize` と `grid_resize` の隣接など整理点があると認め、`win_resize_equal` を `win_resize` のフラグに畳む・`win_move`/`win_exchange`/`win_rotate` を単一「rearrange」イベントに統一する案を提示。

**変更ファイル:** `runtime/doc/api-ui-events.txt`、`src/nvim/window.c`（+360 / -196）、`src/nvim/ui.c`、`src/nvim/move.c`、新規テスト `test/functional/ui/ext_win_spec.lua`（+272）など計 15 ファイル（+856 / -203）。

**次のアクション:** GUI クライアント作者（bfredl 含む）を交えたレビューと、イベント粒度（統合するか細分化するか）の設計判断。

---

### ![PR](icons/git-pull-request.svg) [fix(test): use absolute paths in Zig harness](https://github.com/neovim/neovim/pull/40491) `#40491`
**ステータス:** Closed | **ラベル:** test

**概要:** #40381 をクローズする目的。Zig テストランナーがまだリポジトリルートから実行されており、CMake ランナー（Xtest_xdg の隔離 cwd をコードへのシンボリックリンクで用いる）と異なる問題。リポジトリローカルの zig-out/Xtest_tmpdir を使うテストで通常の `:cd`/`shorten_fnames()` 挙動が発火しバッファ名が `zig-out/...` に短縮されて観測される。Zig ランナーを Xtest_xdg から実行し、runtime/src/test/README.md のシンボリックリンクを張って CMake 環境に合わせる（POSIX のみ）。

**直近のアクティビティ:** barrettruth・justinmk・bfredl が COMMENTED を残した。この PR はクローズされ、後継として bfredl の #40522 に引き継がれた。

**変更ファイル:** `test/run_tests.zig`（+37 / -12）、`test/functional/plugin/dir_spec.lua`、`test/functional/treesitter/inspect_tree_spec.lua` の計 3 ファイル。

**次のアクション:** 対応不要（クローズ済み。#40522 に統合された）。

---

### ![Issue](icons/issue-opened.svg) [Stray sequence and DSR error in Windows](https://github.com/neovim/neovim/issues/40526) `#40526`
**ステータス:** Closed | **ラベル:** なし

**概要:** Windows で Neovim 実行時に DSR 応答が受信されない旨の警告が出て、画面上部に不可解なシーケンス `+q4D73` が表示されるという報告。文字は実際にはバッファに入っておらず、Insert モードで入力すると上書きされる。0.12.2（MSYS2）と最新 nightly の両方、MinTTY と Windows コマンドプロンプトで発生（Windows 10 22H2）。Windows 11 では正常。

**直近のアクティビティ:** justinmk が「この謎は issue 検索で解決する :)」として `q4D73` の検索リンクを提示し、クローズした。

**次のアクション:** 対応不要（既存 issue の重複としてクローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [vim.schedule_idle](https://github.com/neovim/neovim/pull/40419) `#40419`
**ステータス:** Open | **ラベル:** DO NOT MERGE

**概要:** 作者本人が「これはほぼ雑な下書きで、まだ見るものはない。名前も仮」と明記するドラフト。#40380 / #40407 の一部。

**直近のアクティビティ:** 作者 justinmk 自身の COMMENTED が 1 件（2026-06-25）。直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/dev_arch.txt`、`runtime/doc/lua.txt`、`runtime/lua/vim/_core/editor.lua`、`runtime/lua/vim/_core/cmdwin.lua`、`src/nvim/ex_getln.c` など計 9 ファイル（+209 / -55）。

**次のアクション:** 設計と命名を固める（マージ対象外）。

---

### ![PR](icons/git-pull-request.svg) [refactor(path): path_skip_sep()](https://github.com/neovim/neovim/pull/40524) `#40524`
**ステータス:** Merged | **ラベル:** refactor

**概要:** 冗長なコードに対し `path_skip_sep()` を追加して利用する。`MB_PTR_ADV` の削除は安全（ループは `*p` が 1 バイト区切り文字 `/`・`\`・`:` の間だけ進むため。`MB_PTR_ADV` は非 UTF-8（DBCS）内部エンコーディングをサポートしていた旧 Vim で必要だった）。#40447 の後続。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/news.txt`、`src/nvim/os/fs.c`、`src/nvim/path.c` の計 3 ファイル（+33 / -40）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [`set diffopt-=filler` does not remove filler lines](https://github.com/neovim/neovim/issues/37565) `#37565`
**ステータス:** Closed | **ラベル:** closed:question

**概要:** `diffopt` の `filler` オプションを `set diffopt-=filler` で外しても、diff モードで filler 行が実際には消えないという報告（NVIM v0.12.0-dev、macOS）。再現手順を提示し、Vim 9.1.1752 では同じ問題は起きないとする。

**直近のアクティビティ:** zeertzjq が「"linematch:40" も外す必要がある。"linematch:{n}" は暗黙的に "filler" を含む」と `:h diffopt` の該当箇所を引用。加えて「0.12+ では末尾コロン付きの `:set diffopt-=filler,linematch:` で一括、旧バージョンでは `:set diffopt-=filler` と `:set diffopt-=linematch:40` を別々に実行すればよい」と補足しクローズ。

**次のアクション:** 対応不要（質問として解決・クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(path): normalize separators in functions not covered](https://github.com/neovim/neovim/pull/39155) `#39155`
**ステータス:** Open | **ラベル:** なし

**概要:** パス区切りの正規化を継続し、nvim と外部の境界（`fn.xxx`、`api.xxx`、`:xxx`）で行うようにする。`$HOME-windows` の正規化、カレントディレクトリ変更の入口（`nvim_set_current_dir()`、`chdir()`）、名前付きパイプ（`--listen`/`--server`/`:restart`、`serverstart()`/`sockconnect()`/`serverstop()`）の正規化、`expand()`/`fnamemodify()` が再び 'shellslash' を尊重するようにする、`forward_slash` を `TO_SLASH` に置換、廃止された `TMP_PATHSEPSTR` 削除など。#23639 / #32590 をクローズ。

**直近のアクティビティ:** justinmk と作者 ntdiary の間で長期にわたる COMMENTED のやり取りがある（最新 2026-07-01）。直近 24 時間のコメントなし。ntdiary は初回レビューで `fnamemodify()` が常に `/` を返せるか、`do_autocmd_dirchanged` の `slash_adjust` を削除すべきか等の論点を挙げていた。

**変更ファイル:** `src/nvim/eval/fs.c`、`src/nvim/eval/funcs.c`、`src/nvim/option.c`、`src/nvim/os/env.c` ほか多数のコアファイルと大量のテスト（buffer_spec.lua +230、defaults_spec.lua -281 ほか）計 52 ファイル（+845 / -849）。

**次のアクション:** レビューコメントへの対応を継続し、正規化の残り関数を仕上げる。

---

### ![PR](icons/git-pull-request.svg) [feat(path): `fnamemodify(':h')` preserves logical root](https://github.com/neovim/neovim/pull/40447) `#40447`
**ステータス:** Merged | **ラベル:** なし

**概要:** #39841 / #39819 をクローズ。Windows で `fnamemodify('//foo/C$', ':h')` が `C$` を通常のファイル名として誤って除去し `//foo` を返す問題を修正。これは有効な UNC パス（`foo` がサーバー名、`C$` が共有名）であり正しくは `//foo/C$` を返すべき。`FileInfo` と `os_fileinfo2` を拡張して一般的なパス種別とルート長を検出し、`:h` がルート部分を越えて遡らないようにする（例: `//server/share/foo` => `//server/share/`、`C:/foo` => `C:/`）。

**直近のアクティビティ:** justinmk が「この push は良い改善だ、ありがとう」と特定コミットへのリンク付きでコメント。作者 ntdiary との COMMENTED のやり取り多数（最新 2026-07-01）。

**変更ファイル:** `src/nvim/os/fs.c`（+50 / -14）、`src/nvim/os/fs_defs.h`、`src/nvim/eval/fs.c`、`runtime/doc/news.txt`、テスト `fnamemodify_spec.lua` の計 5 ファイル（+141 / -65）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Windows: fnamemodify(':h') does not preserve UNC share roots](https://github.com/neovim/neovim/issues/39819) `#39819`
**ステータス:** Closed | **ラベル:** platform:windows, bug-vim, filesystem

**概要:** Windows で `fnamemodify(..., ':h')` が `//foo/C$` のような UNC 共有ルートを保持しない問題。`C$` は `C:` ドライブのルートに対する Windows の管理共有なので `//foo/C$` を UNC ルートとして扱うべきだが、nightly では通常パスのように分割され `//foo` になる。`//foo/C$`、`//foo/C$/`、`//foo/C$/bar` はいずれも `//foo/C$` を返すべき。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40447 のマージに伴いクローズ）。

**次のアクション:** 対応不要（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [cmdwin: *dark ritual*](https://github.com/neovim/neovim/pull/40463) `#40463`
**ステータス:** Open | **ラベル:** terminal, DO NOT MERGE

**概要:** #40407 のコメントで示した内容を実装。#1496 / PR #10842 / PR #22374 / #12103 / #1716 / #6233 をクローズする狙い。cmdwin と端末実行（run_in_terminal）に関わる大規模な変更を含む。ドラフト状態。

**直近のアクティビティ:** bfredl が画像付きで APPROVED（2026-06-29）。直近 24 時間のコメントなし。

**変更ファイル:** 新規 `runtime/lua/vim/_core/run_in_terminal.lua`（+181）、`runtime/lua/vim/_core/cmdwin.lua`（+126 / -31）、`src/nvim/os/pty_proc_unix.c`（+122 / -5）、`src/nvim/channel.c`、`src/nvim/ex_getln.c`、新規テスト `run_in_terminal_spec.lua`（+278）など計 31 ファイル（+1216 / -71）。

**次のアクション:** ドラフトを詰めてマージ可能な状態にする（現時点はマージ対象外）。

---

### ![Issue](icons/issue-opened.svg) [filewatch: handle OS resource exhaustion (`(libuv) kqueue(): Too many open files`)](https://github.com/neovim/neovim/issues/40238) `#40238`
**ステータス:** Open | **ラベル:** performance, has:plan, events, ux-visibility, filewatch

**概要:** `_watch.lua`（ひいては `autoread.lua`）が OS ハンドル枯渇（macOS の `(libuv) kqueue(): Too many open files`、Linux の inotify 上限 / `EMFILE`）を扱えない。`vim._watch.watch()` は `ENOENT` 以外の `handle:start()` 失敗を黙って捨てるため、呼び出し元は no-op の cancel を受け取りウォッチャが死んでいることを検知できず、機能が静かに劣化する。影響: ウォッチャ駆動の 'autoread' 停止、LSP の `workspace/didChangeWatchedFiles` 通知停止、`M.active.watch` カウンタが誤って健全値を示す。提案として `M.watch`/`M.watchdirs` から `(cancel, err)` を返す、`M.active.watch_failed` カウンタの追加、`:checkhealth` での助言表示（#40516 で対応済み）を挙げる。

**直近のアクティビティ:** justinmk が「『too many open files』問題の半分は swapfile が原因のようだ」とし、緩和策として「'autoread' ウォッチャをファイル単位ではなくディレクトリ単位（非再帰）で定義してファイル名でイベントをフィルタする（多くの場合ウォッチャ総数が半減、ただし `.git/` などノイズ増のトレードオフ）」「全ロード済みバッファではなく可視（または最近可視）のバッファのみ監視する」を検討中とコメント。

**次のアクション:** `M.watch` のエラー返却化とウォッチャ数の削減緩和策を実装する。

---

### ![PR](icons/git-pull-request.svg) [feat(health): report ulimit info](https://github.com/neovim/neovim/pull/40516) `#40516`
**ステータス:** Merged | **ラベル:** ci:skip-news, ux-visibility, filewatch

**概要:** macOS で `(libuv) kqueue(): Too many open files` が表示される状況（#40238 参照）に対し、このケースを検出するヘルスチェックを追加する。

**直近のアクティビティ:** justinmk が「OK、#40114 をマージしたのでこの PR は `err=true` を使うようにした」とコメント。

**変更ファイル:** `runtime/lua/vim/health/health.lua`（+56）、新規 `runtime/lua/vim/_core/proc.lua`（+20）、`runtime/lua/vim/pack/health.lua`、`runtime/doc/options.txt` など計 7 ファイル（+150 / -86）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [nvim-treeconfig: upstream nvim-treesitter](https://github.com/neovim/neovim/issues/39006) `#39006`
**ステータス:** Open | **ラベル:** distribution, project-management, treesitter

**概要:** #22313 の作業計画。nvim-treesitter を nvim-lspconfig と同様の形でアップストリーム化する。既知の課題はクエリがエディタ固有になりうる点、パーサが自前のクエリを維持しない点、サプライチェーン攻撃（悪意あるパーサ）。期待動作として、`vim.pack.add{}` 一発で「トップ 20」のパーサを簡単に導入できること。`neovim/nvim-treeconfig` リポジトリを nvim-lspconfig 的な役割で提供する（「データのみ」でフレームワークなし、indent 非対応、スコープは「トップ N」パーサに限定、クエリ含むパーサ関連は各パーサが所有）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** サブイシューに沿って `nvim-treeconfig` の設計・提供を進める。

---

### ![Issue](icons/issue-opened.svg) [`vim.fs.{dir,find}`: allow callers to observe traversal errors](https://github.com/neovim/neovim/issues/39878) `#39878`
**ステータス:** Closed | **ラベル:** lua, filesystem

**概要:** #18583 / #39723 に関連。`vim.fs.dir()` が `uv.fs_scandir()` のエラーを捨てるため、読めないディレクトリと空ディレクトリが区別できず、`vim.fs.find()` で「該当なし」と「ツリーの一部を探索できなかった」が区別できない。後方互換を保ったまま `vim.fs.dir()` に opt-in の `on_error` コールバック（初回スキャン失敗時と再帰中の子ディレクトリ走査失敗時に呼ばれる）を追加し、`vim.fs.find()` は同オプションを転送する案を提示。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40114 のマージに伴いクローズ）。

**次のアクション:** 対応不要（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [cmdwin freedom](https://github.com/neovim/neovim/issues/40407) `#40407`
**ステータス:** Open | **ラベル:** refactor, cmdline-mode

**概要:** #40325 の後続候補。`CHECK_CMDWIN`（"E11: Invalid in command-line window"）の削除（#40484 で対応済み）、`BUFLOCK_OK`/`LOCK_OK` の統一、`nvim__cmdwin_set` の廃止、expr レジスタ（`<c-r>= c-f`）や `input()` からの cmdwin サポート（#40463）、'scrollbind' への対応検討などをチェックリスト化。一部は実需要（誰かが実際に欲しいという声）待ち。

**直近のアクティビティ:** Gelio が特定コミット（`WIP: ":read :term"`）を試し「基本的な `c-f` 機能は動く。`CTRL-W n` で新規ウィンドウを開けてしまうので無効化すべきでは。`:q!` で通常リネーム体験に戻れるのは良い。ただしそのバッファに自分の全マッピング（nvim-spider / nvim-various-textobjs）が効かない」とフィードバック。justinmk は「`CTRL-W n` の制限は #40484 で意図的に外した。マッピング欠如は子 nvim を `--clean` で起動しているためで、後で見直せる」と応答。

**次のアクション:** 子 nvim を `--clean` で起動する挙動の見直しなど、チェックリストの残項目を検討する。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.1.{1540,1542}](https://github.com/neovim/neovim/pull/40523) `#40523`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** (1) 補完が中断されたときにメニュー状態が画面に反映されない問題（fuzzy ソート後にポップアップメニューが構築されていなかった）を、中断時に `show_pum()` を呼ぶことで修正（Girish Palya、テストなし）。(2) Coverity が未初期化変数を指摘する問題を、変数を初期化して解消。closes vim/vim#17736、vim/vim#17717。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/insexpand.c` のみ（+6 / -2）。

**次のアクション:** 対応不要（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [When using a custom statuscolumn, an increase in the number width doesn't redraw lines properly](https://github.com/neovim/neovim/issues/27709) `#27709`
**ステータス:** Closed | **ラベル:** side-column

**概要:** `numberwidth` が 3 のときにバッファ変更で幅が 4 に増える必要が生じ、かつカスタム `statuscolumn` を設定していると、幅変更が全行を正しく再描画せず、number/status カラムがバッファの 1 列に重なって "line NN" が "ine NN" になる（強制再描画やカーソル移動まで残る）。`:redraw` では直らず `Control+L` で直る（Nvim v0.12.0-dev、Arch Linux）。

**直近のアクティビティ:** Johnkb101313 が「'cursorline' オプションでも同様の問題に遭遇した」として、`cursorline` を使う別の再現用 `repro.lua` と手順（`gg` → `o` で下方の行が "line NN" から "ine NN" に）を提示（NVIM v0.12.3）。

**次のアクション:** number 幅変更時の statuscolumn 再描画不具合（cursorline 経路含む）の原因調査。

---

### ![PR](icons/git-pull-request.svg) [refactor(test): unify XDG path handling (cmake vs zig)](https://github.com/neovim/neovim/pull/40522) `#40522`
**ステータス:** Open | **ラベル:** build, refactor, test

**概要:** #40381 を修正。CMake と Zig のテストランナー間で XDG パス処理を統一するリファクタ（本文の Problem / Solution 欄は未記入）。#40491 の後継。

**直近のアクティビティ:** justinmk が APPROVED（2026-07-01）。

**変更ファイル:** `cmake/RunTests.cmake`（+1 / -13）、`test/runner.lua`（+18 / -4）、`test/run_tests.zig`、`test/functional/lua/fs_spec.lua`、`test/functional/plugin/dir_spec.lua` など計 7 ファイル（+29 / -44）。

**次のアクション:** マージする（承認済み）。

---

### ![Issue](icons/issue-opened.svg) [dir.lua is a reusable UI component](https://github.com/neovim/neovim/issues/40458) `#40458`
**ステータス:** Open | **ラベル:** ui, plugin, runtime, filesystem, remote-ssh

**概要:** netrw の問題の 1 つは多くを詰め込みすぎて再利用/拡張できなかったこと。新しいコアプラグイン `dir.lua` は非常にシンプルなので、(1) ディレクトリ的概念の「一覧」表示、(2) 項目へのナビゲート、といった基本 UI コンポーネントとして再利用可能にする好機とする。ユースケースは github.com のツリー閲覧、ローカル git ツリー、リモート ssh、zip アーカイブなど。`dir.lua` を任意プラグインが非ファイルシステムの「ディレクトリ一覧」UI に再利用できるようにする。

**直近のアクティビティ:** justinmk が「『プラグイン xx を有効化しない』本当の仕組みができるまで `g:loaded_xx` に縛られるかもしれない（#34704）。#40489 が示唆する `after/...` アプローチはあまり良くない」とコメント。SanchayanMaity が「デフォルトのキーマッピングが問題になりうる。`-` に `unique=true` で mini.files を割り当てていると起動時に neovim が文句を言い、マッピングと augroup を削除する必要があった」と具体例を報告。

**次のアクション:** プラグイン無効化の仕組み（`g:loaded_` ガード等）と `dir.lua` の再利用性・拡張ポイントの設計を進める。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): conceal backslash in markdown inline highlights](https://github.com/neovim/neovim/pull/40495) `#40495`
**ステータス:** Merged | **ラベル:** needs:response, treesitter

**概要:** Markdown のインラインのバックスラッシュエスケープが視覚的に表示される問題に対し、`@conceal` クエリでバックスラッシュを隠す。この変更に伴い、バックスラッシュエスケープは `@string.escape` を使わなくなり他のリテラルと区別されなくなる。CommonMark 仕様の「ASCII 句読点はバックスラッシュエスケープ可能、それ以外の文字の前のバックスラッシュはリテラル扱い」に従う。#39993 の再導入。

**直近のアクティビティ:** justinmk が「Markdown 構文サポートが『レンダリング』か『ソース編集』かは曖昧で迷いがある。本来は presentation-mode（#31825）で駆動したいが、ひとまず今回はこれで進め、それが入ったら見直そう」とコメント。clason が「nvim-treesitter をインストールしていると（markdown パーサもそれ経由なら）バンドルクエリを意図的に上書きするため、この変更は見えない」と注意喚起（PSA）。

**変更ファイル:** `runtime/queries/markdown_inline/highlights.scm`（+9 / -4）、`runtime/doc/news.txt`、テスト `test/functional/treesitter/highlight_spec.lua`（+15）の計 3 ファイル。

**次のアクション:** 対応不要（マージ済み）。将来的に presentation-mode で駆動する方針の再検討余地あり。

---

### ![PR](icons/git-pull-request.svg) [fix(:restart): remove `-S [file]` from `v:argv`](https://github.com/neovim/neovim/pull/40521) `#40521`
**ステータス:** Open | **ラベル:** AI assisted 🤖

**概要:** `-S [file]` は `:restart` の文脈では意味をなさず、`:restart!` でも望まれないだろう（#38940 のコメント参照）。あらゆる restart 時に `-S [file]` を `v:argv` から除去する。関連して全 `[files]`（`v:argf`）を落とす要望もあるが、それは #39586 で進行中かもしれない。ドラフト状態。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/gui.txt`（+1 / -1）と `src/nvim/ex_docmd.c`（+10）の計 2 ファイル。

**次のアクション:** レビューを受けてドラフトを解除する。

---

### ![PR](icons/git-pull-request.svg) [feat(:restart)!: no bang restores session and window layout](https://github.com/neovim/neovim/pull/40321) `#40321`
**ステータス:** Merged | **ラベル:** breaking-change

**概要:** 多くのユーザーが `:restart` にセッションとウィンドウレイアウトの復元を望み、カスタムコマンド/マッピングを作っている問題に対応。`:restart` をセッションとウィンドウレイアウトを復元する挙動に変更し、従来の挙動は新設の `:restart!` に移す。`v:startreason` に新しい値を追加し、`ZR` を `:restart!` にマップ。Lua 実装は mini.sessions（MIT ライセンス、Copyright (c) 2021 Evgeni Chasnovski）から移植。#38940 をクローズ。AI 支援: Codex。

**直近のアクティビティ:** superatomic が「（Neovim のバックポート手順に不慣れとのことだが）`master` ではなく `release-0.12` ブランチに PR を出せばよい」とコメント。

**変更ファイル:** `runtime/lua/vim/_core/server.lua`（+61）、`src/nvim/ex_docmd.c`（+31 / -4）、`runtime/doc`（editing.txt / gui.txt / news.txt / vvars.txt）、`src/nvim/vvars.lua`、テスト（tui_spec.lua +113 / -38、server_spec.lua）など計 14 ファイル（+231 / -63）。

**次のアクション:** 対応不要（マージ済み）。必要なら `release-0.12` へのバックポート PR を出す。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新は、バッファのライブプレビューを求める既存の機能要望への追加コメントのみ。

### ![Issue](icons/issue-opened.svg) [[Feature] Buffer 'live' preview](https://github.com/nvim-telescope/telescope.nvim/issues/2481) `#2481`
**ステータス:** Open | **ラベル:** enhancement

**概要:**「Telescope buffers」使用時に、fzf-lua のようにバッファの実際の内容を表示するオプションがあるかという問い合わせ。無ければ機能要望として提案したいという 2023 年からの Issue。

**直近のアクティビティ:** mattcargile が「今は `buffer_previewer_maker` で何かする想定なのか」と質問。捨て置きの SQL クエリなどのスクラッチバッファをプレビューしたいが、`builtin.buffers` のプレビューは保存済みファイルのバッファでしか機能しないと述べている。

**次のアクション:** 未保存バッファに対するライブプレビュー機能の実装可否を検討する。

---

## ghostty-org/ghostty

> libghostty の UTF-8 グラフェム長オーバーフローというメモリ安全性のバグ修正がマージされたほか、tip リリースの CI 改善もマージ済み。非メンテナが作成した Issue／PR は方針により自動クローズされている。

### ![PR](icons/git-pull-request.svg) [libghostty: fix utf-8 grapheme length overflow](https://github.com/ghostty-org/ghostty/pull/13145) `#13145`
**ステータス:** Merged | **ラベル:** libghostty

**概要:** `GRAPHEMES_UTF8` の row-cells ゲッターは必要バイト数を `utf8CodepointSequenceLength`（値を u3 に格納）から推定していたため、7 バイトを超えるマルチスカラークラスタでは容量チェック前にアキュムレータがオーバーフローし、プローブサイズが誤って最適化ビルドで呼び出し側バッファを越えて書き込む恐れがあった。必要バイト数に usize を使い、プローブと容量チェックを後段のエンコードループと一致させる。短い結合クラスタ・8 バイトの旗クラスタ・長い家族絵文字・ちょうどのサイズでの成功・cap == needed - 1 の非書き込み境界をカバーするよう C API テストを拡張。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル変更（+79/-20）。`src/terminal/c/render.zig`。

**次のアクション:** マージ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [How to view the configurable options in a configuration file](https://github.com/ghostty-org/ghostty/issues/13144) `#13144`
**ステータス:** Closed | **ラベル:** なし

**概要:** 公式ドキュメントの `language=de` のような設定例に対し、他に設定可能なオプションをどう確認するのかという質問。

**直近のアクティビティ:** ghostty-vouch[bot]が「非メンテナはこのリポジトリで Issue を作成できない。まず discussion を作成してほしい（理由は #3558 と CONTRIBUTING.md 参照）」と案内し、自動クローズされた。

**次のアクション:** クローズ済み。質問は discussion への誘導で対応。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump docker/build-push-action from 7.2.0 to 7.3.0](https://github.com/ghostty-org/ghostty/pull/13143) `#13143`
**ステータス:** Open | **ラベル:** dependencies, github_actions

**概要:** Dependabot による `docker/build-push-action` の 7.2.0 → 7.3.0 更新。esbuild バンドルの名前保持や各種依存（`@docker/actions-toolkit`、js-yaml、undici、vite など）のバンプを含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル変更（+1/-1）。`.github/workflows/test.yml`。

**次のアクション:** CI 通過後のマージ。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13141) `#13141`
**ステータス:** Merged | **ラベル:** なし

**概要:** ghostty-vouch[bot]による VOUCHED リストの自動更新。discussion コメント（@jcollie 由来）をトリガーに @yak3d を vouch リストへ追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル変更（+1/-0）。`.github/VOUCHED.td`。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [WIP: Sidebar spaces](https://github.com/ghostty-org/ghostty/pull/13138) `#13138`
**ステータス:** Closed | **ラベル:** なし

**概要:** サイドバーの spaces 機能を追加する WIP PR（macOS 向け、+1909 行）。コードの完全レビューと軽微なバグ修正が残タスクとして挙げられていた。

**直近のアクティビティ:** 作者 luka-stanisic が「フォークではなく誤って本家リポジトリに PR を開いてしまった、申し訳ない」とコメント。ghostty-vouch[bot]は「PR 作者が vouch リストにないため自動クローズされる（CONTRIBUTING.md 参照）」と案内した。

**変更ファイル:** 16 ファイル変更（+1909/-37）。`macos/Sources/Features/Terminal/TerminalSidebarView.swift`（+828）や `SpacesModel.swift`（+139）、`TerminalController.swift`、`Config.zig` など、設計/レビュー用ドキュメント（`docs/superpowers/` 配下）も含む。

**次のアクション:** クローズ済み。作者のフォークで vouch 手順を経て再提出する必要がある。

---

### ![PR](icons/git-pull-request.svg) [ci: skip tip release when only non-artifact files change](https://github.com/ghostty-org/ghostty/pull/13130) `#13130`
**ステータス:** Merged | **ラベル:** ci

**概要:** tip リリース時に無視するファイルを追加し、テスト可能にするため release-tip の実行を別スクリプトへ分離する PR。AI 開示として「Claude が実施し、作者がレビューしてローカルでのテスト実行を依頼した」と明記されている。

**直近のアクティビティ:** jcollie が当初 CHANGES_REQUESTED を出し、作者 bo2themax との複数回のレビューコメントのやり取りを経て、最終的に jcollie が「LGTM」で APPROVED した。

**変更ファイル:** 1 ファイル変更（+52/-42）。`.github/workflows/release-tip.yml`。

**次のアクション:** マージ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [undo action does not focus the restored window/surface](https://github.com/ghostty-org/ghostty/issues/13136) `#13136`
**ステータス:** Closed | **ラベル:** なし

**概要:** ウィンドウやタブを閉じて `undo` で復元すると、サーフェスは再表示されるがキーボードフォーカスが直前にアクティブだったウィンドウ/サーフェスのままで、復元されたものへ移らないという報告。閉じる前と同様に復元サーフェスへフォーカスが移るべきとしている。

**直近のアクティビティ:** ghostty-vouch[bot]が非メンテナによる Issue 作成を認めない旨を案内し、自動クローズした。

**次のアクション:** クローズ済み。discussion での再提起が必要。

---

### ![Issue](icons/issue-opened.svg) [macOS paste prefers NSURL over plain text, causing Transmit SFTP paths to paste as sftp:// URLs](https://github.com/ghostty-org/ghostty/issues/13132) `#13132`
**ステータス:** Closed | **ラベル:** なし

**概要:** macOS で Panic Transmit から `Cmd+C` でリモートパスをコピーすると、プレーンテキストのパスと `NSURL`/URL 表現の複数表現がペーストボードに載る。Sublime Text ではプレーンパスが貼られるが、Ghostty では `sftp://user@host:port/...` の URL 表現が貼られてしまう。`NSPasteboard+Extension.swift` の `getOpinionatedStringContents()` が `readObjects(forClasses: [NSURL.self])` を先に試し、非ファイル URL では `absoluteString` を返すため SFTP URL がプレーンテキストに勝つのが原因とみられ、ターミナルのペーストではプレーンテキスト表現を優先すべきと指摘。#12519 と同種の問題の可能性。

**直近のアクティビティ:** ghostty-vouch[bot]が非メンテナによる Issue 作成を認めない旨を案内し、自動クローズした。

**次のアクション:** クローズ済み。discussion での再提起が必要。ペーストボード表現の優先順位（プレーンテキスト優先）の見直しは検討余地あり。

---

## astral-sh/uv

> リゾルバのパフォーマンス改善（投機的メタデータ取得の優先度制御、phase saving、競合ベースの優先順位付け、TOML の SIMD パース）を中心に charliermarsh が多数のドラフト PR を投入し、あわせてインデックス/ビルドキャッシュ関連のバグ修正や Renovate による依存更新も進行中。

### ![PR](icons/git-pull-request.svg) [Use a priority-aware limiter for speculative metadata requests](https://github.com/astral-sh/uv/pull/20084) `#20084`
**ステータス:** Open | **ラベル:** なし

**概要:** バッチプリフェッチは各ディストリビューションをメタデータ要求の前に共有 once-map に登録するため、ダウンロードキューが飽和すると投機的要求がリゾルバの必要とするバージョンを先取りしてしまう。この PR は投機的メタデータをアクティブ要求と区別したまま、スケジューリング方針を共有ダウンロードリミッタへ移す。リゾルバがアイドルの間は投機的要求が全ダウンロード許可を使えるが、アクティブな作業が待機し始めると投機的要求はセマフォキューの外に留まるか取得した許可を譲り、ソルバをブロックする Simple API とホイールメタデータの要求が追い越せるようにする。

**直近のアクティビティ:** astral-sh-bot が、テスト inventory に 3 件のテスト（`active_request_overtakes_speculative_request` など）が追加されたと報告。人的なコメントはなし。

**変更ファイル:** `uv-configuration/src/concurrency.rs`（+187）や `uv-resolver/src/resolver/mod.rs`、`uv-client/src/registry_client.rs` などリミッタとリゾルバ周辺の 11 ファイルを変更。

**次のアクション:** ドラフトのためレビュー・ベンチマーク結果（JupyterLab -5.0%、Semantic Kernel -3.0% など効果は限定的）の確認と完成待ち。

---

### ![PR](icons/git-pull-request.svg) [Prioritize solver-blocking metadata requests over pre-fetch requests](https://github.com/astral-sh/uv/pull/20082) `#20082`
**ステータス:** Open | **ラベル:** performance

**概要:** #20084 と同じ問題に対する別アプローチ。バッチプリフェッチのメタデータを独立した投機的要求に変え、once-map 登録前に許可を取得させたうえで、その並行数を設定済みダウンロード並行数より 1 少なくキャップする。これにより待機中の投機的要求が始まる前にアクティブな作業がバージョンを確保でき、Simple API 要求などソルバをブロックする作業用に 1 スロットを確保する。ダウンロード並行数が 1 のときは投機的メタデータ要求を完全に無効化する。解決結果は変えず、要求の処理順のみを変更。初期計測ではコールドキャッシュの Transformers 解決が 48.8s から 27.0s（-44.6%）へ。

**直近のアクティビティ:** maurycy が、ネットワークの信頼性に応じてスケジューリング戦略を切り替えたり、成功率・信頼性・レイテンシに応じて許可数を TCP の AIMD のように適応させてはどうかと提案。現状 `concurrency.rs` の許可数は起動後に固定である点を指摘。

**変更ファイル:** `uv-resolver/src/resolver/mod.rs`（+112/-63）と `batch_prefetch.rs`、`uv-dispatch/src/lib.rs` など 5 ファイル。

**次のアクション:** ドラフトのため、提案された適応的スケジューリングを取り入れるかの判断とレビュー完了待ち。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/20069) `#20069`
**ステータス:** Open | **ラベル:** なし

**概要:** github-actions bot による Python リリースの自動更新。`download-metadata.json` のみを変更する定型 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-python/download-metadata.json`（+12/-12）のみ。

**次のアクション:** 自動生成のドラフトのため、内容確認後のマージ。

---

### ![PR](icons/git-pull-request.svg) [Hint when a build failure is caused by a requires-python mismatch](https://github.com/astral-sh/uv/pull/19673) `#19673`
**ステータス:** Open | **ラベル:** error messages

**概要:** #7035 のクローズを目的とする。古いパッケージのビルド失敗の多くは、当該ディストリビューションの `requires-python` がアクティブなインタプリタを除外していることが原因だが、バックエンドのエラー（深い SyntaxError や RuntimeError）はそれを直接示さない。この PR は、`uv pip install`／`uv pip sync` 中のビルド失敗時に、キャッシュ済みメタデータの `requires-python` にアクティブな Python バージョンが含まれない場合、根本原因を示すヒントを既存のヒントより前に表示する。例として Python 3.12 で `numba==0.53.1`（推移的に `llvmlite==0.36.0`、`requires-python` は `>=3.6,<3.10`）をインストールしたケースを挙げている。適用範囲は `uv pip install`／`uv pip sync` に限定し、diff を小さく保っている。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に zanieb と作者 SuryanshSS1011 の間で多数のレビューコメントのやり取りがあり、直近では 6/30 に zanieb がコメント）。

**変更ファイル:** `uv-distribution/src/source/mod.rs`（+94）、`uv-distribution/src/error.rs`、`pip_install.rs` のテストなど 5 ファイル。

**次のアクション:** レビュー指摘への対応と、テスト追加形式（insta スナップショット等）の合意。

---

### ![PR](icons/git-pull-request.svg) [Archive requirements structurally with rkyv](https://github.com/astral-sh/uv/pull/20083) `#20083`
**ステータス:** Closed | **ラベル:** なし

**概要:** `Requirement` は現状 PEP 508 要件を文字列化して rkyv 実装しており、キャッシュ読み出し時に再びパーサを呼ぶため型付き表現を捨ててウォームキャッシュ経路にパース処理を追加していた。この PR は要件のフィールドを構造的にアーカイブするよう変更する。マーカーツリーは正準 DNF で保存してマーカーインターナを直接再構築し、直接 URL・Git 参照・パース済み URL などは再構築に必要なパース済みコンポーネントを保持する。Simple キャッシュのバージョンを上げ、ラウンドトリップのテストカバレッジを追加。

**直近のアクティビティ:** 作者 charliermarsh が「今のところ意味のある効果は見られない」とコメントし、その後クローズ。

**変更ファイル:** `uv-pep508/src/marker/tree.rs`（+125）、`uv-redacted/src/lib.rs`（+164）、`uv-pypi-types/src/parsed_url.rs` など 22 ファイル。新規 `rkyv_support.rs` を 2 つ追加。

**次のアクション:** 効果がないためクローズ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Reuse proven-safe resolver decisions](https://github.com/astral-sh/uv/pull/20071) `#20071`
**ステータス:** Open | **ラベル:** performance

**概要:** phase saving（progress saving）はバックトラック探索で重複作業を避ける手法で、非時系列バックトラックが競合と無関係な割り当てを消したとき、その割り当てを記憶し再選択時に優先する。#20020 が「同一の許容範囲で再訪した場合のみ」保存バージョンを再利用する限定的な phase saving を導入したのに対し、この PR は範囲が変化した場合とユニバーサル解決へ拡張する。ただし uv は最大解を出す必要があるため無条件には適用できず、範囲変化時は保存バージョンが依然許容され、ロックファイルの選好やインストール済み配布物が候補順序を変えず、優先される全候補がすでに利用不可・非互換である場合にのみ再利用する。このために `State::range_conflicts_with_partial_solution` クエリを導入。訪問バージョン数は合計 -26.6%（Warehouse -58.3%）、実時間は Transformers -18.5% など。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-resolver/src/resolver/mod.rs`（+280/-76）、`pins.rs`、`lock_scenarios.rs`（+588）と phase-saving 系のシナリオ TOML 6 件を追加。

**次のアクション:** ドラフトかつ上流 pubgrub PR #73 に依存するため、依存 PR のマージとレビュー完了待ち。

---

### ![PR](icons/git-pull-request.svg) [Generate PEP 740 attestations](https://github.com/astral-sh/uv/pull/19489) `#19489`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv publish` に PEP 740 アテステーションの「生成」サポートを初期実装する。これにより `attest-action` などで事前・帯域外にアテステーションを生成する必要がなくなり、`uv publish` が自動で行う。WIP でマージ準備は未完。TODO として上流の依存トリミング（`chrono` から `jiff` への置き換えなど、sigstore-rust や tough への PR）、`--no-attestations` 以外の適切な CLI フラグ設計が残る。#15618 をクローズ予定、#9122 参照。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 新規クレート `uv-pep740`（`types.rs` +259、`sign.rs` +55 など）、`uv-publish/src/lib.rs`（+449/-183）、`Cargo.lock`（+352）など 15 ファイル。

**次のアクション:** ユニット・統合テストの追加、CLI フラグ設計の確定、上流依存トリミングの完了。

---

### ![PR](icons/git-pull-request.svg) [Consider system site packages if activated](https://github.com/astral-sh/uv/pull/11670) `#11670`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** インタプリタの新鮮で順序付けされたランタイム `sys.path` からインストール済み配布物を発見する実験的 PR。ランタイムで有効な配布物と uv が所有・変更しうる配布物を分離し、継承・外部注入された配布物は読み取り専用として扱う（要件は満たせるが exact sync や uninstall では削除しない）。PyPy の同梱 `cffi` や実行可能な `.pth` により先行挿入されたパッケージなど、ランタイムで勝てないと保証できない置換を拒否する。`--system-site-packages`（#2500、#4466）に加え `.pth`／`sitecustomize`／ユーザーサイト／`PYTHONPATH` 由来のインポート順・キャッシュ無効化問題にも対応。ランタイムパスはキャッシュに永続化しない（永続化は不健全なため）。

**直近のアクティビティ:** Hangzhi が「なぜこの PR を諦めたのか、再開は可能か」と質問。HPC 環境で torch などの重い依存の上に軽い依存を重ねる用途で site packages の発見性が必要で、uv を全面採用できていないと述べ、リスクを承知のうえで使えるようオプションでゲートして `uv sync`／`uv pip install` に導入できないかと要望。

**変更ファイル:** `uv-python/src/interpreter.rs`（+280）、`uv-installer/src/installed_packages.rs`（rename +269）、pip 系コマンド多数など 26 ファイル。

**次のアクション:** クローズ済み。再開の是非とオプションによる段階導入の可能性はメンテナ（konstin）の判断待ち。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate が管理する依存更新と検出依存の一覧ダッシュボード。放置とみなされた依存 33 件（`byteorder`、`walkdir`、`serde_yaml` など）、スケジュール待ち更新（`astral-sh/uv` を v0.11.26 へ、`rustls` を v0.23.41 へ、`criterion` を v5 へなど）、編集によりブロックされた更新、ステータスチェック待ち、オープン中の更新（`http` v1.4.2 等）、クローズ済み PR にブロックされた更新（MSRV を v1.94.1 へ、`itertools` 0.15.0、Rust を v1.96.0 へ等）を列挙。cargo 依存 71 件を検出。

**直近のアクティビティ:** 直近 24 時間のコメントなし（bot 管理の自動更新イシュー）。

**次のアクション:** メンテナが各チェックボックスを選択して更新 PR の作成・リベース・再作成を制御。

---

### ![PR](icons/git-pull-request.svg) [Prioritize active metadata requests](https://github.com/astral-sh/uv/pull/20081) `#20081`
**ステータス:** Closed | **ラベル:** なし

**概要:** #20082 とほぼ同内容。バッチプリフェッチのメタデータを独立した投機的要求に変え、once-map 登録前に設定済みダウンロード並行数でキャップした許可を取得させる一方、リゾルバ駆動のメタデータ要求は既存の「登録してからキュー投入」の挙動を維持する。候補選択や解決結果は変えず、どの保留メタデータ要求を先に処理するかを変更。局所計測でコールドキャッシュ Transformers ロックが 48.8s から 27.0s（-44.6%）、80 回の交互ウォームキャッシュ packse 実行では中央値ほぼ不変（118.5ms → 118.0ms）。

**直近のアクティビティ:** 作者 charliermarsh が「astral-sh/uv の head ブランチから main に対して #20082 として再オープンした」とコメントしクローズ。

**変更ファイル:** `uv-resolver/src/resolver/mod.rs`、`batch_prefetch.rs`、`uv-dispatch/src/lib.rs` など 5 ファイル。

**次のアクション:** #20082 に引き継がれたためクローズ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [`uv <command> --help` should prioritise options specific to the command](https://github.com/astral-sh/uv/issues/6856) `#6856`
**ステータス:** Open | **ラベル:** cli

**概要:** `uv <command> --help` を実行した際に、まずコマンド固有のオプションを表示してほしいという要望。VS Code の下部ターミナルで使うと、見たいコマンド固有オプションが上方にスクロールアウトしてしまう。`uv help <command>` は先頭にスクロールするが用途が異なるとする。

**直近のアクティビティ:** aswild が「一部のオプションセクションをデフォルトで隠し、全一覧は `uv help` で見せるべき」というメンテナの案に賛同。`uv foo -h` はコマンド固有のみ、`uv foo --help`／`uv help foo` は全部を表示する形を望み、clap の `Arg::hide_short_help` が使えそうだと指摘。Index/Resolver/Installer などのセクションが約 2 画面分を占め、素早い確認には不要だと述べる。

**次のアクション:** short help と full help の表示方針を決め、`hide_short_help` による実装を検討。

---

### ![Issue](icons/issue-opened.svg) [Cannot add packages from internal index, uv skips all versions. Pip works fine.](https://github.com/astral-sh/uv/issues/19952) `#19952`
**ステータス:** Closed | **ラベル:** question

**概要:** 社内 devpi インスタンスを `UV_DEFAULT_INDEX` に設定したが、uv がパッケージのどのバージョンも見つけられず「no versions of[package-name]」で解決に失敗する（pip は `PIP_INDEX_URL` で正常動作）。`-vv` ログでは全バージョンを「Skipping file」として飛ばしており理由が表示されない、と報告。uv 0.11.23、Rocky Linux 9、Python 3.9.16。

**直近のアクティビティ:** 報告者 austinwitherspoon が「URL に `+simple` を付けたら完璧に動いた。そんなものがあるとは知らなかった」とコメントし解決。

**次のアクション:** 自己解決済みでクローズ。対応不要（devpi の Simple API エンドポイント指定が必要だった）。

---

### ![PR](icons/git-pull-request.svg) [Warn when the build cache is inside the source directory](https://github.com/astral-sh/uv/pull/20056) `#20056`
**ステータス:** Merged | **ラベル:** bug

**概要:** #19991 で導入したエラーを警告に緩和する。ビルドバックエンドがキャッシュディレクトリを除外するよう設定されているか判別できず、GitLab ではキャッシュがプロジェクトローカルにあることが多いため。将来的には生成物を覗いてキャッシュディレクトリの包含を確実に判定でき、`CACHEDIR.TAG` を持つ任意のディレクトリに対して診断を出せるようにすべきと述べる。#20050 をクローズ。

**直近のアクティビティ:** gsemet が「これも破壊的変更に対する別の修正だ。破壊的変更は何としても避けてほしい。次回は失敗させる前に警告してほしい」とコメント。

**変更ファイル:** `crates/uv/src/commands/build_frontend.rs` と `tests/build/cache.rs`。

**次のアクション:** charliermarsh 承認済みでマージ完了。将来的な生成物ベースの診断は別途検討。

---

### ![Issue](icons/issue-opened.svg) [`python3-config` equivalent?](https://github.com/astral-sh/uv/issues/10263) `#10263`
**ステータス:** Open | **ラベル:** question

**概要:** ThunderKittens の Makefile が `python3-config` を要求するが uv 管理の venv には無い、という問題。報告者は回避策として、`pyvenv.cfg` の `home` が指すキャッシュ済み Python の `python3-config` を直接使う、`PATH` を実体の python の場所へ通す、などを挙げている。

**直近のアクティビティ:** agbuckley が、これは組み込み venv／virtualenv でも長年の問題で、`python` と `python-config` が異なる Python バージョンを指すと `Python.h`／`libpython` の場所や API が食い違い致命的だとコメント。キャッシュ済み uv Python から venv の `bin/` へスクリプトを手動シンボリックリンクすれば足りるのか確認しつつ、少なくとも誤った `python-config` がデフォルトで使われないようにするのは実際的な必要性だと述べ、CPython 側 issue（python/cpython#135147）にも言及。

**次のアクション:** venv 作成時に `python-config` 相当を提供する（あるいはシンボリックリンクする）方針の検討。CPython 側の venv パス上書き議論の進展も待ち。

---

### ![PR](icons/git-pull-request.svg) [Update actions/setup-python action to v6.3.0](https://github.com/astral-sh/uv/pull/20073) `#20073`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `actions/setup-python` の v6.2.0 → v6.3.0 更新。RHEL サポートの追加やキャッシュキーへの Linux ディストリ含め、Windows での pip キャッシュのエラー処理修正などが含まれる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/` 配下の ci.yml、test-integration.yml など 8 個のワークフローファイル。

**次のアクション:** マージ完了。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Expose `UV_PROJECT_ENVIRONMENT` as flag to `uv sync`](https://github.com/astral-sh/uv/issues/20060) `#20060`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `UV_PROJECT_ENVIRONMENT` に対応する CLI フラグ（例 `uv sync --venv <my_env>`）が無いため、フラグでの指定を可能にする PR を出してよいか、変更規模はどの程度かという問い合わせ。slurm クラスタで親ジョブが環境を事前ビルドする運用をしており、新しい別アーキテクチャのノードで事前ビルド環境が壊れる。環境変数は継承ジョブに伝播してしまうため CLI フラグで渡せると良い、と背景を説明。

**直近のアクティビティ:** sr-murthy が、uv は作業ディレクトリ外にあるアクティブな仮想環境を検出せず作業ディレクトリの `.venv` に依存する点を指摘。`UV_PROJECT_ENVIRONMENT` の設定は理解しているが `uv venv` に自動検出を追加できないか、PDM の自動検出はうまく機能していると述べる。

**次のアクション:** CLI フラグ導入の可否と設計方針をメンテナが判断し、コントリビュータの PR 受け入れを検討。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Update documentation](https://github.com/astral-sh/uv/pull/19026) `#19026`
**ステータス:** Open | **ラベル:** documentation, breaking

**概要:**「package by default」の安定化に伴うドキュメント更新。アプリケーションとパッケージ化アプリケーションの位置づけを入れ替え、パッケージ化レイアウトを特別扱いとして強調しないようにする。package by default を安定化するリリースの直前にマージする必要がある。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/concepts/projects/config.md`、`docs/guides/package.md`、`uv-cli/src/lib.rs` など 7 ファイル。

**次のアクション:** package by default 安定化リリースの直前までマージを保留。

---

### ![PR](icons/git-pull-request.svg) [Avoid shared temporary directories for Windows Python launchers](https://github.com/astral-sh/uv/pull/20019) `#20019`
**ステータス:** Open | **ラベル:** windows

**概要:** Windows の Python ランチャ構築は現状、共有 `%TEMP%` 下に一時ディレクトリを作り、そこでランチャを書き込み・更新してメモリへ読み戻し、さらにターゲット横に別の一時ファイルを書いてからアトミックに置換していた。この PR はバイト列を返すランチャ API を廃し、ランチャを宛先横の一時ファイルに直接構築する。新規作成経路は上書き禁止の永続化、置換経路は既存のリトライ付きアトミック永続化を使い、完全な PE ランチャ構築後にのみ宛先へ触れることで共有一時ディレクトリと中間バッファを回避する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-trampoline-builder/src/lib.rs`、`uv-python/src/managed.rs`、`uv-fs/src/lib.rs`。

**次のアクション:** レビューと Windows 環境での検証。

---

### ![PR](icons/git-pull-request.svg) [Re-add `pub` APIs used in Pixi](https://github.com/astral-sh/uv/pull/20074) `#20074`
**ステータス:** Merged | **ラベル:** なし

**概要:** 最近のリリースで一部 API が `pub(crate)` にされ、Pixi でのアップグレードがブロックされていたため、パッチなしで使えるよう公開に戻す。Pixi で uv を更新してテスト済みで、uv 自体の挙動は変わらない。

**直近のアクティビティ:** wolfv が「ありがとう」とコメント、charliermarsh が「こちらこそ申し訳ない」と返答。

**変更ファイル:** `uv-resolver/src/resolver/provider.rs`、`python_requirement.rs`、`yanks.rs`、`uv-client/src/lib.rs` など 7 ファイル。

**次のアクション:** charliermarsh 承認済みでマージ完了。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Restart shallow conflict-heavy resolves](https://github.com/astral-sh/uv/pull/20080) `#20080`
**ステータス:** Closed | **ラベル:** なし

**概要:** バックトラックにより PubGrub が浅い競合クラスタを繰り返し探索し続けることがある。SAT ソルバはコールドリスタートで学習した節を保持しつつ古い探索状態をリセットする手法を使う。この PR はフォークあたり 1 回のガード付きコールドリスタートを追加する。128 回の実競合の後、部分解の決定数が 64 以下（決定あたり少なくとも 2 競合の浅い探索）のときにのみ再起動し、学習した非互換性と証明済み保存バージョンを保ちつつ最古の非ルート決定へバックトラックする。動的競合優先度バケットをクリアして別経路を探索させる。深く線形な解決は再起動しない。合計の訪問バージョン数は -1.8%（black -17.7%）。#20077 上にスタック。

**直近のアクティビティ:** codspeed-hq bot が「この PR はパフォーマンスを変えない」と報告（`resolve_warm_airflow` -3.27% 悪化、`resolve_warm_jupyter_universal` +5.36% 改善）。

**変更ファイル:** `uv-resolver/src/pubgrub/priority.rs`、`resolver/mod.rs`、`pins.rs`、Transformers のスナップショットと phase-saving シナリオ TOML など 15 ファイル。

**次のアクション:** クローズ済み。効果が乏しいため取り下げられたとみられ、追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Prioritize packages by recent conflict activity](https://github.com/astral-sh/uv/pull/20077) `#20077`
**ステータス:** Closed | **ラベル:** なし

**概要:** PubGrub のパッケージ順序は解に至るまでの訪問バージョン数に大きく影響する。既存の閾値付き優先度バケットは数回の競合後に反応し、その後は解決終了までその順序を維持する。この PR はパッケージ名ごとに減衰する競合アクティビティを追加する。通常のレジストリパッケージが実競合に関与するとアクティビティを増やし、より最近アクティブなパッケージを無関係なパッケージより優先する。直接 URL とシングルトン要件は既存優先度を維持し、アクティビティは 32 競合ごとに減衰する。合計訪問バージョン数 -1.1%（jupyterlab -10.1%）。#20064 上にスタック。

**直近のアクティビティ:** codspeed-hq bot が「この PR はパフォーマンスを 6.23% 改善する」と報告（`resolve_warm_jupyter_universal` 125.8ms → 118.5ms）。

**変更ファイル:** `uv-resolver/src/pubgrub/priority.rs`、`resolver/mod.rs`、`pins.rs`、スナップショットと phase-saving シナリオ TOML など 15 ファイル。

**次のアクション:** クローズ済み（後続の #20080 等に整理・引き継がれたとみられる）。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [`uv add --index <name>=<url> <package>` can overwrite an index of the wrong format](https://github.com/astral-sh/uv/issues/19759) `#19759`
**ステータス:** Closed | **ラベル:** bug

**概要:** `uv add --index <name>=<url>` は既存インデックスを `<name>` で照合して `<url>` を上書きするが、`format` キーはそのまま残る。`format = "flat"` のインデックスを Simple API の URL に付け替えると `format` が古いまま残り、`uv add` 時にはエラーにならず、後続の `uv lock` で解決に失敗する。再現手順とともに、これは明確にバグだと報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #19818 の修正でクローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Sync index format when `uv add --index` updates an existing index URL](https://github.com/astral-sh/uv/pull/19818) `#19818`
**ステータス:** Merged | **ラベル:** bug

**概要:** #19759 の修正。`PyProjectTomlMut::add_index` で URL が実際に変わるかを捕捉し、変わる場合は `format` を新しいインデックスに合わせて同期する（flat インデックスは `format = "flat"` を書き、Simple インデックスは `format` キーを削除）。URL が変わらない場合は既存 `format` を保持する。ユニットテスト 2 件（`add_index_clears_format_on_url_update`、`add_index_preserves_format_when_url_unchanged`）を追加。プロジェクトの AI ポリシーに従い AI をコーディング補助に使用したと明記。#19759 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-workspace/src/pyproject_mut.rs`、`project/add.rs`、`tests/project/edit.rs`。

**次のアクション:** charliermarsh 承認済みでマージ完了。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Avoid redundant pyproject parsing](https://github.com/astral-sh/uv/pull/20076) `#20076`
**ステータス:** Merged | **ラベル:** performance

**概要:** `PyProjectToml::from_string` は現状、すべての有効な `pyproject.toml` を 2 回パースしていた（`tool.uv.sources` 検証で型付き `SourceError` を保つための sources 限定表現と、完全なプロジェクトのデシリアライズ）。専用パースはデシリアライズ失敗時に型付きソースエラーを回収する場合にのみ必要。この PR は成功経路では `tool.uv.sources` を含む完全なプロジェクトを直接パースし、失敗時のみソースを再パースして元の TOML エラーを返す。これにより有効なプロジェクトは単一の TOML パースで済む。

**直近のアクティビティ:** codspeed-hq bot が「この PR はパフォーマンスを 78.26% 改善する」と報告（`discover_workspace_from_all_members` が Simulation 106ms → 56.6ms など。ただし異なるランタイム環境間の比較である旨の警告あり）。

**変更ファイル:** `uv-workspace/src/pyproject.rs`、`tests/lock/lock.rs`。

**次のアクション:** マージ完了。対応不要。

---

### ![Issue](icons/issue-opened.svg) [How to prefer cached package version?](https://github.com/astral-sh/uv/issues/16108) `#16108`
**ステータス:** Open | **ラベル:** question

**概要:** `uv run --with rich example.py` を、キャッシュ済みの最新安定版があればそれを選ぶ（無ければ通常どおり最新安定版にフォールバックする）形で実行したい、という相談。

**直近のアクティビティ:** NorthLake が実装の見込みを尋ねるコメント。Azure DevOps のプライベートフィードに artifacts-keyring で認証しており動作はするが、フィードへのリクエストごとに約 3 秒の遅延が生じるため、要件に合うキャッシュ版があればその遅延を避けたいと述べる。

**次のアクション:** キャッシュ済みバージョンを優先する解決オプションの検討。

---

### ![PR](icons/git-pull-request.svg) [Enable SIMD-accelerated TOML parsing](https://github.com/astral-sh/uv/pull/20079) `#20079`
**ステータス:** Merged | **ラベル:** performance

**概要:** `toml_parser` には Winnow 経由で SIMD 加速スキャンを有効化する任意の `simd` フィーチャがあるが、uv では現状無効になっている。この PR はそれを有効化する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.toml` のみ。

**次のアクション:** マージ完了。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Preserve direct dependency selection context for locked project tools](https://github.com/astral-sh/uv/pull/20078) `#20078`
**ステータス:** Open | **ラベル:** bug, preview

**概要:** `Lock::dependency_selection` は直接プロジェクト依存に対して正確なロック済みパッケージを選ぶが、選択された extras や依存グループの競合コンテキスト（競合フォーク選択に使用）といった情報を現状破棄している。ruff・ty の現用途では致命的でないが将来の混乱を避けるため修正する。この PR は正確なパッケージ・適用可能な直接エッジからの extras の和集合・production／依存グループコンテキストを含む `SelectedDependency` を返し、`Lock::to_resolution_from_dependency` がそれを既存のロック済みサブグラフ走査へ渡すことで、選択 extras の有効化とプロジェクト競合項目の解決を可能にする。#19982、#19989 のフォローアップ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-resolver/src/lock/mod.rs`、`lock/installable.rs`、`project/check.rs`、`project/toolchain.rs` など 5 ファイル。

**次のアクション:** ドラフトのためレビューと完成待ち。

---

### ![PR](icons/git-pull-request.svg) [Require locks for pep723 scrolls in this workspace](https://github.com/astral-sh/uv/pull/20075) `#20075`
**ステータス:** Open | **ラベル:** internal

**概要:** 本文は空。変更内容から、このワークスペースの PEP 723 スクリプト（スクロール）にロックファイルを必須とし、CI（check-lint／check-lock ワークフロー）に組み込む変更とみられる。多数の `scripts/*.py.lock` ファイルを追加している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/check-lint.yml`、`check-lock.yml` と `scripts/` 配下の各種 `.py.lock` ファイル（`bump-workspace-crate-versions.py.lock` など）。

**次のアクション:** レビューと、追加された各ロックファイルの妥当性確認。

---

### ![PR](icons/git-pull-request.svg) [Check PEP 723 scrolls in CI](https://github.com/astral-sh/uv/pull/20007) `#20007`
**ステータス:** Merged | **ラベル:** testing

**概要:** 既存の `ty` ジョブは `python/uv` と 1 つの PEP 723 スクリプトを独自の呼び出しでチェックするのみで、リポジトリ内の他のインラインメタデータスクリプトは未チェックだった。この PR は一度きりのスクリプトチェックを、`# /// script` ファイルを自動発見して `uv check --script` を実行するパスに置き換える。隣接ロックファイルを持つスクリプトは `--locked` で実行し、空の明示的な `ty` 設定によって各スクリプト環境がルートプロジェクトの Python 3.8 最小要件を継承せず自前の Python バージョンを供給できるようにする。`uvx ty check python/uv` はプロジェクトレベルの `uv check` がリポジトリ全体を走査するため変更しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に zanieb と zsol のレビューコメントあり）。

**変更ファイル:** `.github/workflows/check-lint.yml`。

**次のアクション:** マージ完了。ワークスペースのメンバー単位で `uv check` できるようにする件は別途対応。

---

### ![Issue](icons/issue-opened.svg) [uv add --no-index --find-links fails to resolve platform-specific dependencies that are not required on the current platform](https://github.com/astral-sh/uv/issues/19999) `#19999`
**ステータス:** Closed | **ラベル:** question

**概要:** `uv add` が、現在のプラットフォームに該当しないプラットフォーム固有依存でも解決に失敗する。Linux x86_64 で `opensandbox-server` を `--no-index --find-links` で追加すると、`docker==7.1.0` が要求する `pywin32{sys_platform == 'win32'}>=304`（Windows 専用で、`pip download` が Linux では取得しないためローカルに無い）が原因で解決失敗する。一方 `pip install` と `uv pip install` は同じローカルホイールリポジトリで成功する。lock 生成（`uv add`）のリゾルバの問題であり、エラーメッセージ（`only pywin32<304 is available` と `no versions of pywin32>=304` の不整合）も紛らわしいと指摘。uv 0.11.24、Rocky Linux 9.6、Python 3.12.13。

**直近のアクティビティ:** DHclly が、`uv add` はユニバーサル解決を行うため現在のプラットフォームを特別扱いしない（意図的な挙動）と説明し、`[tool.uv] environments = ["sys_platform == 'linux'"]` で Windows を無視できると案内。あわせてエラーメッセージにドキュメントへのリンク（common-marker-values）を追加すべきと提案。

**次のアクション:** ユニバーサル解決の仕様上クローズ済み。回避策は `environments` 指定。エラーメッセージへのドキュメントリンク追加は改善余地として残る。

---

## microsoft/vscode

> 直近の活動は Agent Host / Copilot チャット・セッション（Agents ウィンドウ）まわりの機能追加とツール管理 UI の改善に集中しており、あわせてターミナル PTY 出力によるメインプロセスの OOM、起動の重さ、SSH 接続不良といった不具合報告や、過去のセッション/検索まわりのコミュニティ PR が多数クローズされている。

### ![Issue](icons/issue-opened.svg) [Disabled the run task tool, but agents window panel chat stil suggests using it](https://github.com/microsoft/vscode/issues/323433) `#323433`
**ステータス:** Open | **ラベル:** bug, chat, chat-tools, ai-customizations, agent-host

**概要:** カスタマイズ画面で run task ツールを無効化したにもかかわらず、Agents ウィンドウのパネルチャットで「run task ツールでビルドタスクを実行して」と依頼すると、チャットが実行タスクを尋ねて実行しようとしてしまう不具合。#323371 のテスト中に発見された。

**直近のアクティビティ:** dmitrivMS は Agent Host セッションでは再現できなかったと報告しつつ、Copilot CLI で本来出ないはずのツールが表示される、ツールを無効化しても `toolSearch` が有効だと検索されてしまう、といった別の問題を確認したとコメント。

**次のアクション:** セッション種別ごとにツールの有効・無効判定と `toolSearch` の挙動を精査し修正する。

---

### ![Issue](icons/issue-opened.svg) [vs code responding very slow](https://github.com/microsoft/vscode/issues/323910) `#323910`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** VS Code 1.127.0（Windows）が非常に遅く、ときに応答しなくなるというパフォーマンス報告。1TB ストレージ環境で動作が重いとしている。

**直近のアクティビティ:** deepak1556 が `Developer: Start Tracing`／`Developer: Stop Tracing` で CPU プロファイルの取得を依頼。ボットが情報不足のため報告ガイドラインの確認を促した。

**次のアクション:** 報告者に CPU プロファイルの提出を待ち、原因を切り分ける。

---

### ![Issue](icons/issue-opened.svg) [Crash, Lag windows response](https://github.com/microsoft/vscode/issues/323983) `#323983`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** MacBook Pro M2 / macOS 26、最新版 1.127.0 で発生するクラッシュ・遅延の報告。旧バージョンでは起きなかったとしている。

**直近のアクティビティ:** deepak1556 が CPU プロファイル取得を依頼。buitranduong は直近 2 バージョンへの更新後に応答が非常に遅くなり、他アプリとウィンドウを切り替えると約 30 秒フリーズしてから復帰すると補足。ボットが情報不足を通知。

**次のアクション:** CPU プロファイルを収集し、更新後に発生したフリーズの原因を調査する。

---

### ![PR](icons/git-pull-request.svg) [Fix OS_ID parsing from /etc/os-release](https://github.com/microsoft/vscode/pull/321293) `#321293`
**ステータス:** Open | **ラベル:** なし

**概要:** #232159 を修正。`check-requirements-linux.sh` が `/etc/os-release` の `OS_ID` を解析する際、`ID="rocky"` のようにクォートが付くと正しく解析できず、Rocky 8.5 などで devcontainer セットアップが失敗していた。POSIX 標準の `.`（source）演算子で読み込むよう変更し、クォートを安全に扱う。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]は「このプルリクエストのファイルをレビューできなかった」とコメント。

**変更ファイル:** `resources/server/bin/helpers/check-requirements-linux.sh`（+1/-1）の 1 ファイルのみ。

**次のアクション:** メンテナによるレビューとマージ判断を待つ。

---

### ![PR](icons/git-pull-request.svg) [Add Tools tree keyboard navigation, fix focus outline cut-off at widget edges](https://github.com/microsoft/vscode/pull/323984) `#323984`
**ステータス:** Open | **ラベル:** accessibility, chat-tools, ai-customizations, agent-host

**概要:** #323427 を修正。AI カスタマイズの「Tools」管理 UI にツリー形式のキーボードナビゲーション（roving tabindex）を追加し、フォーカスアウトラインがウィジェット端で切れないようスタイルを調整してアクセシビリティを改善する。

**直近のアクティビティ:** Copilot レビュアーが変更概要（ツリー式キーボード操作とフォーカス表示の改善）を要約。dmitrivMS がコメント。github-actions[bot]が 16 件のスクリーンショット差分レポートを投稿。

**変更ファイル:** `aiCustomizationManagement.css`（+14/-8）と `toolsListWidget.ts`（+236/-26）。

**次のアクション:** レビュー指摘とスクリーンショット差分を確認のうえマージ判断を進める。

---

### ![Issue](icons/issue-opened.svg) [VS Code editor cannot use Microsoft Chinese input method — unable to input Chinese](https://github.com/microsoft/vscode/issues/323989) `#323989`
**ステータス:** Open | **ラベル:** なし

**概要:** Microsoft の中国語 IME を使ってエディタに中国語を入力できないという不具合報告（本文は主にバグ報告テンプレート）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 再現手順と環境情報を確認し、IME 入力経路の問題を切り分ける。

---

### ![PR](icons/git-pull-request.svg) [Support uninstalling extensions from Tools section of Chat Customizations](https://github.com/microsoft/vscode/pull/323981) `#323981`
**ステータス:** Open | **ラベル:** chat-tools, ai-customizations, agent-host

**概要:** #323509 を修正。チャットカスタマイズの「Tools」セクションから、ツールを提供する拡張機能を直接アンインストールできるようにする。インストール後に拡張機能を削除できなかった問題に対応。

**直近のアクティビティ:** Copilot レビュアーが、インストール済みかつ非ビルトインのツール拡張をアンインストールするコンテキストメニューの追加、という変更点を要約。

**変更ファイル:** `toolsListWidget.ts`（+82/-4）の 1 ファイル。

**次のアクション:** レビューを経てマージ判断を進める。

---

### ![Issue](icons/issue-opened.svg) [designer](https://github.com/microsoft/vscode/issues/323988) `#323988`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容のないテンプレートのみの投稿で、スパムとして扱われた Issue。

**直近のアクティビティ:** vs-code-engineering[bot]がコミュニティガイドラインに合致しないスパムとしてクローズした旨をコメント。

**次のアクション:** 対応不要（スパムとしてクローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [designer](https://github.com/microsoft/vscode/issues/323985) `#323985`
**ステータス:** Closed | **ラベル:** spam

**概要:** 機能要望テンプレートのみで内容のない投稿。スパムと判定された。

**直近のアクティビティ:** vs-code-engineering[bot]がガイドライン不適合のスパムとしてクローズ。

**次のアクション:** 対応不要（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [designer1](https://github.com/microsoft/vscode/issues/323987) `#323987`
**ステータス:** Closed | **ラベル:** spam

**概要:** 同じく内容のないテンプレートのみの投稿で、スパムと判定された。

**直近のアクティビティ:** vs-code-engineering[bot]がスパムとしてクローズ。

**次のアクション:** 対応不要（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Show More Info In HELP/ABOUT](https://github.com/microsoft/vscode/issues/310902) `#310902`
**ステータス:** Open | **ラベル:** feature-request, portable-mode

**概要:** ヘルプ／バージョン情報画面に、実行中の VS Code がどこから起動されたか（起動元パス）を表示してほしいという機能要望。PC と USB ドライブ間でポータブル運用をしたいという背景。

**直近のアクティビティ:** vs-code-engineering[bot]が「過去 60 日でコミュニティの賛成票が 20 票未満だったためクローズした」旨をコメント。

**次のアクション:** 賛成票が集まらなかったため、追加対応の予定はない。

---

### ![PR](icons/git-pull-request.svg) [Filter extension gallery results in Tools view](https://github.com/microsoft/vscode/pull/323986) `#323986`
**ステータス:** Open | **ラベル:** chat-tools, ai-customizations, agent-host

**概要:** #323419 と #323510 を修正。チャットカスタマイズ→Tools のマーケットプレイス閲覧で、実際に言語モデルツールを提供する拡張機能のみを表示し、Agents／Sessions ウィンドウ実行時はそこで動作可能な拡張にさらに絞り込む。

**直近のアクティビティ:** Copilot レビュアーが、LM ツールを提供する拡張のみ表示し実行環境に応じて結果を制限するという変更内容を要約。

**変更ファイル:** `toolsListWidget.ts`（+40/-3）の 1 ファイル。

**次のアクション:** レビューを経てマージ判断を進める。

---

### ![Issue](icons/issue-opened.svg) [BYOK to GHCP throws an error (GPT 5.3 Codex model)](https://github.com/microsoft/vscode/issues/317503) `#317503`
**ステータス:** Open | **ラベル:** なし

**概要:** BYOK（自前キー）で GPT 5.3 Codex を GitHub Copilot に接続すると、応答フィルタリングのエラーが発生するという報告。Agent モードで再現。

**直近のアクティビティ:** dews が「`"thinking": true,` を追加する必要がある。#323110 を参照」とコメント。

**次のアクション:** `thinking` オプションの追加で解消するか確認し、必要なら BYOK 設定周りの改善を検討する。

---

### ![PR](icons/git-pull-request.svg) [Fix main process OOM from unconsumed buffered pty host service events](https://github.com/microsoft/vscode/pull/323980) `#323980`
**ステータス:** Open | **ラベル:** なし

**概要:** #307156 を修正。メインプロセスがターミナル PTY 出力を V8 ヒープに際限なく蓄積し、約 4GB の上限に達して OOM で異常終了する問題。原因は `ProxyChannel.fromService` がすべてのサービスイベントを `Event.buffer(..., flushAfterTimeout=true)` で先行ラップし、リスナーが付くまで配列に溜め込むこと。`LocalPty` チャネルでこの先行バッファリングを回避する。

**直近のアクティビティ:** Copilot と jacobjove がレビュー。deepak1556 が過去の対応 #285419 を挙げ anthonykim1 に cc。jacobjove が CLA に同意。ボットが CODENOTIFY で deepak1556 へ通知。

**変更ファイル:** `ipc.ts`（+16/-3）、`ipc.test.ts`（+39/-0）、`app.ts`（+3/-1）。

**次のアクション:** #285419 の議論（先行実装）を踏まえ、レビュー指摘を反映してマージ判断を進める。

---

### ![PR](icons/git-pull-request.svg) [fix pty output make main process oom](https://github.com/microsoft/vscode/pull/285419) `#285419`
**ステータス:** Open | **ラベル:** なし

**概要:** #234393 を修正する先行 PR（ドラフト）。ローカル PTY プロセスはレンダラーと直接通信するが、メインプロセスも出力をバッファに保存し続け消費者がいないためリークする、という問題。ヒープスナップショットで蓄積を示している。

**直近のアクティビティ:** レビュー多数。Tyriar は一度 lgtm（後に dismissed）、bpasero と joaomoreno は CHANGES_REQUESTED。joaomoreno は「_lazy_ 概念の導入は好ましくない。文字列マッチではなく `@IPCNoBuffer` のようなデコレータや、手書きチャネルを使う案」を提示。theanarkh も複数コメント。

**変更ファイル:** `ipc.ts`（+11/-1）、`ipc.test.ts`（+39/-0）、`app.ts`（+3/-1）。

**次のアクション:** 後継 PR #323980 との整合を取りつつ、lazy 概念への設計上の指摘に対応する。

---

### ![Issue](icons/issue-opened.svg) [Main process OOM: terminal PTY output strings accumulate unboundedly in V8 heap (~37 MB/min)](https://github.com/microsoft/vscode/issues/307156) `#307156`
**ステータス:** Closed | **ラベル:** bug, confirmation-pending, terminal-process

**概要:** メイン（ブラウザ）プロセスが、ターミナル PTY 出力（特に ANSI エスケープシーケンス）を GC されない V8 文字列として蓄積し続け、ヒープが際限なく膨張して最終的に OOM クラッシュする。ヒープスナップショットでは ANSI 文字列が 842MB（ヒープの 97.3%）を占めていた。

**直近のアクティビティ:** jacobjove が 2 台目のマシンでも再現を確認し、保持参照の特定に役立つ可能性のあるリテイナートレースとともに報告。

**次のアクション:** クローズ済み。修正 PR（#323980 / #285419）で対応。

---

### ![Issue](icons/issue-opened.svg) [Expose the VS Code integrated browser DOM to Claude Code chat](https://github.com/microsoft/vscode/issues/322883) `#322883`
**ステータス:** Closed | **ラベル:** feature-request, browser-integration

**概要:** 統合ブラウザの DOM が現状 Copilot CLI からしか使えないため、チャットセッション中に Claude Code からもライブ DOM を参照できるようにしてほしいという要望。Playwright などとの自動化ワークフロー改善が狙い。

**直近のアクティビティ:** jruales が Claude 用ブラウザツールを有効にする設定（`chat.agentHost.enabled` など）を提示し、「動作するようになった」と報告。karanjeetbirdeye が礼を述べた。

**次のアクション:** 対応不要（設定で解決しクローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [I cannot not see worktree function](https://github.com/microsoft/vscode/issues/323982) `#323982`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot サブスクリプションを使っていない場合に Worktree 機能が利用できなくなったのか、という質問。報告者は Copilot CLI モードでしか worktree 機能が見えないとしている。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** worktree 機能の表示条件（サブスクリプション／モード依存）を確認し回答する。

---

### ![PR](icons/git-pull-request.svg) [Enable cache-break hint by default](https://github.com/microsoft/vscode/pull/323959) `#323959`
**ステータス:** Merged | **ラベル:** なし

**概要:** 実験的設定 `chat.cacheBreakHint.enabled` とそのゲートを削除し、モデル／オプションピッカーの cache-break ヒントをデフォルトで表示するようにする。ヒントはユーザーの非表示操作・キャッシュウォーム状態・自動モデル時の抑制は引き続き尊重する。

**直近のアクティビティ:** rzhao271 が APPROVED。

**変更ファイル:** `chat.shared.contribution.ts`（+0/-7）、`chatModelPicker.ts`（+3/-12）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [Improve prioritization of review-ready external PRs and implementation-ready issues](https://github.com/microsoft/vscode/issues/314560) `#314560`
**ステータス:** Open | **ラベル:** feature-request

**概要:** 外部コントリビューターがどの PR や実装可能な Issue を優先すべきか分かりにくいため、既存シグナルを活用したレビュー可能 PR・実装可能 Issue の軽量な優先度表示を追加してほしいという要望。メンテナ判断を置き換えるのではなく意思決定支援層として提案。

**直近のアクティビティ:** ボットが「まだ 20 票の賛成に達していない。残り 10 日」と通知。

**次のアクション:** コミュニティの賛成票が集まるかを見守る。

---

### ![Issue](icons/issue-opened.svg) [Add an integrated release decision dashboard for endgame](https://github.com/microsoft/vscode/issues/314562) `#314562`
**ステータス:** Open | **ラベル:** feature-request

**概要:** エンドゲーム時のリリース判断に必要な情報（Issue、リグレッション、テストプラン、スモークテスト結果、プラットフォーム別失敗、マイルストーン状況など）を統合表示するダッシュボードの追加要望。

**直近のアクティビティ:** ボットが「まだ 20 票に達していない。残り 10 日」と通知。

**次のアクション:** 賛成票の推移を見守る。

---

### ![Issue](icons/issue-opened.svg) [Frequent update popups](https://github.com/microsoft/vscode/issues/320168) `#320168`
**ステータス:** Open | **ラベル:** feature-request, install-update

**概要:** 管理者権限のない業務用 PC で、更新のために 1 日に何度も（管理者）ログイン資格情報を求められるという問題（パフォーマンス Issue として報告）。

**直近のアクティビティ:** ボットが「十分な賛成票を得たためバックログに移動した」と通知。

**次のアクション:** バックログとして、資格情報を繰り返し要求しない更新フローの改善を検討する。

---

### ![PR](icons/git-pull-request.svg) [Clean up ExPs](https://github.com/microsoft/vscode/pull/323979) `#323979`
**ステータス:** Merged | **ラベル:** なし

**概要:** すでに 100%展開済みの実験（ExP）設定を整理する。統合ブラウザまわりの実験フラグと agent-host 固有の実験設定を削除し、ブラウザ関連設定の扱いを簡素化する。

**直近のアクティビティ:** benvillalobos が APPROVED。ボットが CODENOTIFY で kycutler に通知。

**変更ファイル:** `browserViewWorkbenchService.ts`、`browserEditorChatFeatures.ts`、`browserNavigationFeatures.ts`、`browserSearchFeatures.ts`（計 4 ファイル、+4/-30）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [rehaul chat input padding and spacing and icons](https://github.com/microsoft/vscode/pull/323977) `#323977`
**ステータス:** Open | **ラベル:** なし

**概要:** ワークベンチのチャット入力と Agents ウィンドウにわたり、余白・間隔の調整とプロバイダー固有のモデルアイコン導入、狭いレイアウト向けのコンパクト表示を行う UI の刷新（ドラフト）。agent-host のセッション設定ピッカーの配置・挙動も調整。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。github-actions[bot]が 50 件のスクリーンショット差分レポートを投稿。

**変更ファイル:** `SESSIONS.md`、`chatInput.css`、`chatWidget.css`、`modelPicker.ts`、`newChatInput.ts`、`agentHostModePicker.ts`、`agentHostSessionConfigPicker.ts` など計 19 ファイル（+270/-169）。

**次のアクション:** ドラフトを仕上げ、スクリーンショット差分を確認のうえレビューへ進める。

---

### ![PR](icons/git-pull-request.svg) [Remove tasks and problems tools from Agents window](https://github.com/microsoft/vscode/pull/323978) `#323978`
**ステータス:** Merged | **ラベル:** chat-tools, agents-window, agent-host

**概要:** #323543 を修正。ビルトインの「クライアント」ツールセットを更新し、Agents（セッション）ウィンドウで Tasks 関連ツールと Problems ツールを表示しないようにする。これらがマルチセッション環境では有用でないという指摘に対応。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。benvillalobos と justschen が APPROVED。

**変更ファイル:** `clientToolSetsContribution.ts`（+15/-13）の 1 ファイル。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [Problems tool has limited use in the Agents window](https://github.com/microsoft/vscode/issues/323543) `#323543`
**ステータス:** Closed | **ラベル:** bug, unreleased, chat-tools, agents-window, ai-customizations, agent-host

**概要:** Problems ツールは拡張ホストが管理する単一ワークスペースに依存する一方、Agents は複数の作業を扱えるため、Agents ウィンドウでは有用性が低いという指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。#323978 で対応。

---

### ![PR](icons/git-pull-request.svg) [Automations: Management UI (create/edit dialog, list widget, ChatInputPart integration)](https://github.com/microsoft/vscode/pull/323914) `#323914`
**ステータス:** Open | **ラベル:** なし

**概要:** スケジュール実行されるエージェントタスク（Automations）を VS Code に追加する 4 段構成 PR の第 3 弾。作成／編集ダイアログ、リストウィジェット、ChatInputPart 統合など、ユーザーが操作する UI 面を追加する。PR1（基盤）はマージ済み、PR2（ランナー）と PR4（統合）が別途進行中。

**直近のアクティビティ:** Copilot レビュアーが変更概要と複数回のレビュー詳細（低努力レベル、指摘 3〜4 件）を投稿。dmitrivMS が APPROVED（フィードバックはオフラインで送付）。

**変更ファイル:** `automationDialog.ts`（+756/-0）、`automationDialogService.ts`（+197/-0）、`sessionWorkspacePicker.ts`、`dialog.ts` など計 42 ファイル（+4261/-138）。

**次のアクション:** オフラインで送付されたフィードバックを反映し、4 段 PR の残り（ランナー・統合）と歩調を合わせる。

---

### ![Issue](icons/issue-opened.svg) [Agents window not connecting to ssh](https://github.com/microsoft/vscode/issues/315870) `#315870`
**ステータス:** Closed | **ラベル:** agents-window

**概要:** Agents ウィンドウでリモートセッションを開始すると、`unexpected argument '--port' found` というエラーで SSH 接続に失敗するという報告（Insiders 1.120.0）。

**直近のアクティビティ:** powerpbx と JustinGrote が回避策を共有。エディタモードで一度接続し `~/.vscode-server` フォルダを削除・再生成してから、改めて Agents モードで接続すると解決するが、過去のチャットセッションデータを失い拡張機能の再インストールが必要になると注意喚起。

**次のアクション:** クローズ済み。SSH リモートエージェントホストへの `--port` 引数受け渡し不具合の根本対応が別途必要。

---

### ![Issue](icons/issue-opened.svg) [[CopilotCLISession] Unexpected generated prompt structure.](https://github.com/microsoft/vscode/issues/323696) `#323696`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Agent セッションで簡単なプロンプト（"Hello" など）を送っただけで即座に `[CopilotCLISession] Unexpected generated prompt structure.` エラーで失敗する。通常の Copilot Chat は正常で Agent モードのみ失敗する。`.copilot` フォルダのリセットでも解消しない。

**直近のアクティビティ:** Mac / Windows 双方で同症状の報告が相次ぐ。chikaramike は既定ターミナルの Zsh プラグインが一因で bash に変えても直らず、Insiders では動作すると報告。mfinotti は Copilot ポリシーで Copilot CLI が無効化されていたのを有効化して解決したと共有。DonJayamanne が原因プロンプトの共有を依頼し、maxxdevine は空プロジェクト・添付なしでも同エラーと返答。

**次のアクション:** 生成プロンプト構造の不整合の原因を特定し、CLI ポリシー無効時のエラー表示改善も検討する。

---

### ![Issue](icons/issue-opened.svg) [Can't view uninstalled Tools extension pages via keyboard](https://github.com/microsoft/vscode/issues/323507) `#323507`
**ステータス:** Closed | **ラベル:** bug, accessibility, chat, chat-tools, ai-customizations, agent-host

**概要:** #323371 のテスト中に発見。カスタマイズモーダルの Tools ページからマーケットプレイスまではキーボードで到達できるが、マーケットプレイスでは Install ボタンしか操作できず、拡張機能そのものを選択して情報ページを開くことがキーボードではできない（クリック時のみ可能）という問題。

**直近のアクティビティ:** dmitrivMS が「行を選択した状態なら `Enter` と `Space` の両方が機能する」と画像付きでコメント。

**次のアクション:** クローズ済み（キーボード操作で対応可能と確認）。

---

### ![PR](icons/git-pull-request.svg) [agents: remove problems tool sessions window 268e3be7](https://github.com/microsoft/vscode/pull/323033) `#323033`
**ステータス:** Closed | **ラベル:** なし

**概要:** Sessions ウィンドウで agent host に「problems」クライアントツールを提示・使用させないようにし、公開ツール面をその環境でサポートされる範囲に合わせる変更。

**直近のアクティビティ:** sandy081 が APPROVED、Copilot がレビュー。dmitrivMS は CHANGES_REQUESTED で「ツールごとの特殊対応は避けたい。Problems を tasks ツールセットに移してまとめて除外する方がよいのでは」と指摘。最終的に「#323978 に置き換えられた」とコメント。

**変更ファイル:** `agentHostActiveClientService.ts`（+13/-2）、`clientToolSetsContribution.ts`（+1/-1）、`agentHostClientTools.test.ts`（+55/-5）。

**次のアクション:** クローズ済み。#323978 が後継。

---

### ![PR](icons/git-pull-request.svg) [Handle opened and unsaved documents as attachments for Agent Host/Copilot CLI](https://github.com/microsoft/vscode/pull/321591) `#321591`
**ステータス:** Merged | **ラベル:** agent-host

**概要:** #320428 を修正。Agent Host Protocol（AHP）セッションでアクティブなエディタ（暗黙のコンテキスト）を軽量なドキュメント添付として転送し、「このファイル」といった参照を agent-host バックエンドが解決できるようにする。

**直近のアクティビティ:** DonJayamanne が当初 dismissed のレビューで「untitled（未保存）エディタの扱いが必要。ディスク上に存在しないため CLI からアクセスできず、ブロックする仕組みが要る」と指摘し、後に APPROVED。Copilot が変更ファイルごとに複数回レビュー。

**変更ファイル:** `agentHostSessionHandler.ts`（+186/-9）、`agentHostChatContribution.test.ts`（+474/-11）、`copilotAgentSession.ts`、`sessionState.ts` など計 7 ファイル（+725/-35）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [missing active doc context in AHP sessions](https://github.com/microsoft/vscode/issues/320428) `#320428`
**ステータス:** Closed | **ラベル:** bug, unreleased, agent-host

**概要:** 保存済み・未保存いずれかのドキュメントを開き「このファイル」の内容を尋ねると、ローカルチャットは文脈を理解するのに対し AHP エージェントは対象ファイルの確認を求めてしまう。保存済みドキュメントの選択テキストは添付されるが、未保存ドキュメントの選択はローカル・AHP とも添付されない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。#321591 で対応。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/323976) `#323976`
**ステータス:** Open | **ラベル:** info-needed

**概要:** Copilot の補完に関するフィードバック Issue。JavaScript の `forEach` 補完テキストが提示されているが、問題の説明や再現手順は空欄。VS Code 1.126.0。

**直近のアクティビティ:** ボットが情報不足の通知と、旧バージョン利用の可能性があるため最新の 1.127.0 へのアップグレードを促した。

**次のアクション:** 具体的な問題内容と最新版での再現有無の情報を待つ。

---

### ![Issue](icons/issue-opened.svg) [Meta: Sorry, your request failed. Please try again.](https://github.com/microsoft/vscode/issues/253136) `#253136`
**ステータス:** Open | **ラベル:** under-discussion, chat-request-failed

**概要:** チャットリクエストが失敗するさまざまなシナリオを追跡するメタ Issue。ユーザー環境からサービス障害（特にプレビューモデル）まで原因は多岐にわたる。リトライ・モデル変更・新規チャット・ログ確認などの回避策を案内している。

**直近のアクティビティ:** welllog が「同じエラーが繰り返し起きる。エラーメッセージは "Response too long."」と報告し、`qG._provideLanguageModelResponse` の発生箇所（copilot 拡張の extension.js）を添えた。

**次のアクション:** "Response too long." のケースについてログを収集し、原因を切り分ける。

---

### ![Issue](icons/issue-opened.svg) [SSH Host File - Grouping several servers](https://github.com/microsoft/vscode/issues/200524) `#200524`
**ステータス:** Open | **ラベル:** feature-request, ssh

**概要:** SSH config に多数のホストがある場合、それらをグループ化できるようにしてほしいという要望。アプリごとに複数環境（D/UAT/P）のホストを管理しており、config の並び順のまま列挙されて扱いにくい。

**直近のアクティビティ:** infanasotku が「進捗はあるか」と尋ね、config 直書きの代わりに `remote-explorer.categories` のような VS Code 設定でグループ定義する案を提示。

**次のアクション:** SSH ホストのグループ化 UI／設定の実装可否を検討する。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/323309) `#323309`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Copilot 補完のフィードバック Issue。PowerShell 起動コマンドの補完テキストが貼られているが、問題の記述は空欄。

**直近のアクティビティ:** ramialshani250-cpu が本文と同じ PowerShell 設定断片を再掲するコメントを投稿。

**次のアクション:** クローズ済み。具体的な問題記述がないため追加対応なし。

---

### ![Issue](icons/issue-opened.svg) [shellIntegration.ps1 Script digital signatures do not match file hashes](https://github.com/microsoft/vscode/issues/323975) `#323975`
**ステータス:** Open | **ラベル:** new release

**概要:** `shellIntegration.ps1` スクリプトのデジタル署名がファイルのハッシュと一致しないという不具合報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** スクリプトの署名とハッシュの不一致の原因（配布物の生成／署名工程）を調査する。

---

### ![Issue](icons/issue-opened.svg) [Implement Google Chrome-like grouped tabs](https://github.com/microsoft/vscode/issues/100335) `#100335`
**ステータス:** Open | **ラベル:** feature-request, workbench-tabs, keep

**概要:** Google Chrome のグループ化タブのような機能を VS Code に導入してほしいという要望。検索結果や Peek references、同一フォルダ内ファイルの自動グループ化など、タブ乱雑化の抑制に活用できるとする長期の feature-request。

**直近のアクティビティ:** MoradFN が「Come on!（早く実現してほしい）」と一言コメント。

**次のアクション:** `keep` ラベル付きの長期要望として、実装可否・優先度の判断を継続する。

---

### ![PR](icons/git-pull-request.svg) [agentHost: chat-address the operational agent methods (collapse default/peer branches)](https://github.com/microsoft/vscode/pull/323967) `#323967`
**ステータス:** Merged | **ラベル:** なし

**概要:** #323950 のフォローアップ。Copilot・Claude・Codex の 3 ハーネスで、operational な `IAgent` メソッド（`sendMessage`／`abort`／`changeModel`／`changeAgent`）を「デフォルトチャット vs ピアチャット」で分岐させず、チャット URI で統一的にアドレッシングするようリファクタリング。デフォルトチャット URI を一度正規化してセッションを導出し、セッションごとのチャットマップで解決する。

**直近のアクティビティ:** Copilot レビュアーが要約。sandy081 がコメント、dmitrivMS が APPROVED。

**変更ファイル:** `claudeAgent.ts`（+86/-83）、`copilotAgent.ts`（+90/-95）、`agentService.ts`、`MULTI_CHAT_ARCHITECTURE.md` など計 12 ファイル（+397/-440）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [Claude Opus 4.7 fails in Agent Mode with 400 error due to unsupported thinking.type.enabled](https://github.com/microsoft/vscode/issues/312290) `#312290`
**ステータス:** Closed | **ラベル:** なし

**概要:** Agent モードで Claude Opus 4.7 を選び任意のメッセージを送ると、サポートされない `thinking.type.enabled` が原因の 400 エラーで全リクエストが失敗するという報告。

**直近のアクティビティ:** thewolff-engineer が「『Pro+ の課金制限が原因』としてクローズされたが全容ではない。Pro+ にアップグレードしても、サインインし直しても、再認証・VS Code 更新をしても identical な失敗が続き、実際のエラーを突き止めた」とフォローアップ。

**次のアクション:** クローズ済みだが、課金制限とは別の `thinking.type.enabled` 起因のエラーとして再調査の余地がある。

---

### ![Issue](icons/issue-opened.svg) [Extension host crash loop on Stable 1.122.1: built-in Copilot Chat 0.50.1 registers undeclared "claude-code" chatParticipant → TypeError: e is not iterable](https://github.com/microsoft/vscode/issues/319423) `#319423`
**ステータス:** Open | **ラベル:** bug, extension-host, claude-agent

**概要:** Stable 1.122.1 で、ビルトインの GitHub Copilot Chat 0.50.1 が宣言されていない "claude-code" chatParticipant を登録し、`TypeError: e is not iterable` で拡張ホストがループ的にクラッシュする。全拡張を無効化しても発生（ビルトイン拡張が原因のため）。関連 #276860 は「初期化中の無害なログ」として扱われクローズされたが、本件は拡張ホストを終了させる致命的な変種。

**直近のアクティビティ:** nj9194 が最新 Stable 1.127.0（arm64 / macOS）でも依然として存在するが、当初のクラッシュループとは異なるユーザー可視の症状になっていると報告。「0.51 の部分修正 vs 完全修正」の切り分けの一助として投稿。

**次のアクション:** 未宣言の "claude-code" chatParticipant 登録と `TypeError` の根本原因を特定し、部分修正の残課題を潰す。

---

### ![Issue](icons/issue-opened.svg) [The vscode server is not installing on my cluster](https://github.com/microsoft/vscode/issues/323974) `#323974`
**ステータス:** Open | **ラベル:** new release

**概要:** 1.127.0 でクラスターに vscode server がインストールできず、パスフレーズが正しくても SSH がロード・要求を繰り返す。server の削除や VS Code の再インストールでも解決しないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** SSH 接続・server インストール失敗のログを収集し、原因を切り分ける。

---

### ![Issue](icons/issue-opened.svg) [Agent Host: tool responses with long outputs create an extremely wide file](https://github.com/microsoft/vscode/issues/323973) `#323973`
**ステータス:** Open | **ラベル:** なし

**概要:** 長い出力を持つツール応答が、6 行のファイルに保存されるものの 4 行目が 68K 文字の極端に長い行（リテラルの改行文字入り）になり、エージェントが JSON を解析するのに Python を使わざるを得ない。テキスト出力は JSON 内の 1 行リテラルではなく、通常の複数行テキストファイルにすればよいのではという提案。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** テキスト系ツール出力を複数行ファイルとして保存する案を検討する。

---

### ![Issue](icons/issue-opened.svg) [In Paste Confirmation Dialog: "Cancel" Still Pastes, and "Paste" Pastes Twice](https://github.com/microsoft/vscode/issues/323841) `#323841`
**ステータス:** Open | **ラベル:** なし

**概要:** 貼り付け時の確認ダイアログで、「Cancel」を押しても 1 回貼り付けられ、「Paste」を押すと 2 回貼り付けられるという不具合。期待は Cancel で中止・Paste で 1 回のみ。

**直近のアクティビティ:** luo2430 が（中国語で）「1.127.0 で一部コードが差し戻され、無許可で貼り付ける問題はなくなったが、外部エクスプローラーからファイルをコピーして貼り付ける機能が失われた。1.125.1 への差し戻しを推奨」とコメント。liuxuan000 が「その解決策で動作した」と返答。

**次のアクション:** Cancel／Paste ボタンの挙動修正と、外部エクスプローラーからのファイル貼り付け機能の回帰を確認する。

---

### ![PR](icons/git-pull-request.svg) [Preserve advanced sandbox network settings](https://github.com/microsoft/vscode/pull/323963) `#323963`
**ステータス:** Open | **ラベル:** なし

**概要:** #323944 を修正。無制限ネットワークアクセス（`chat.agent.sandbox.allowNetwork`）が有効な場合でも、`chat.agent.sandbox.advanced.runtime` の高度なランタイムネットワークオプションを保持しつつ、生成されるネットワーク設定を正とする。`allowAllUnixSockets` の回帰テストを追加。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。

**変更ファイル:** `terminalSandboxEngine.ts`（+1/-7）、`terminalSandboxEngine.test.ts`（+22/-0）。

**次のアクション:** レビューを経てマージ判断を進める。

---

### ![PR](icons/git-pull-request.svg) [Streamline update mode configuration and handling](https://github.com/microsoft/vscode/pull/323206) `#323206`
**ステータス:** Open | **ラベル:** なし

**概要:** #289144 を修正（ドラフト）。`update.mode: "none"` が自動更新（特に Windows のバックグラウンド更新）を確実に止められない問題に対し、更新モード変更を実行時に反映させ、更新無効時に処理中の作業をキャンセルするようにする。

**直近のアクティビティ:** Copilot レビュアーが変更概要と複数回のレビュー詳細（指摘 0〜5 件、低努力レベル）を投稿。

**変更ファイル:** `abstractUpdateService.ts`（+122/-22）、`updateService.win32.ts`（+68/-3）、`abstractUpdateService.test.ts`（+244/-0）など計 8 ファイル（+458/-48）。

**次のアクション:** ドラフトを仕上げ、レビュー指摘を反映してマージ判断へ進める。

---

### ![Issue](icons/issue-opened.svg) [1.127.0 Endgame](https://github.com/microsoft/vscode/issues/323403) `#323403`
**ステータス:** Closed | **ラベル:** endgame-plan

**概要:** 1.127.0 のエンドゲーム計画 Issue（自動生成テンプレート）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [clicking outside the browser modal makes the browser disappear](https://github.com/microsoft/vscode/issues/314145) `#314145`
**ステータス:** Open | **ラベル:** bug, browser-integration, workbench-modal-editor

**概要:** #313802 のテスト中に発見。ブラウザを開いているとチャットが見えないためチャットをクリックしてフォーカスすると、ブラウザが消えてしまう。実際には閉じておらず前の状態で戻ってくるが、操作する方法が分からないという問題。

**直近のアクティビティ:** jruales が内部バックログ Issue（#7703）との関連をコメント。

**次のアクション:** ブラウザモーダルのフォーカス喪失時の挙動と再表示手段を改善する。

---

### ![PR](icons/git-pull-request.svg) [sessions: quick chats (workspace-less sessions) in the Agents window](https://github.com/microsoft/vscode/pull/323972) `#323972`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents ウィンドウに「クイックチャット」（ワークスペースに紐づかない軽量チャット、agent-host セッションで裏打ち）を追加する。`workspace === undefined` の単一チャットセッションで、コンポーザーから新規作成し、常時表示の「Chats」セクションに一覧表示する。

**直近のアクティビティ:** Copilot レビュアーが要約。sandy081 が 3 件コメント、dmitrivMS が APPROVED。ボットが CODENOTIFY で benibenj に通知。

**変更ファイル:** `agentService.ts`（+43/-4）、`copilotAgent.ts`（+83/-9）、`sessionState.ts`（+42/-0）、`workspacelessScratchDir.ts` など計 59 ファイル（+2106/-167）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Support Agent Host session attachments with Copilot CLI events.jsonl](https://github.com/microsoft/vscode/pull/323773) `#323773`
**ステータス:** Open | **ラベル:** なし

**概要:** #315193 の一部（ドラフト）。Agent Host のセッション参照添付を、ホストローカルの `events.jsonl` に解決できる Copilot CLI セッションに限定する。対応する `sessionReference` 変数を小さな `Simple` 添付（リプレイメタデータ付き）に変換して Sessions チップを表示し、CLI の全トラジェクトリを `events.jsonl` を指す隠し `Resource` 添付として付ける。`Sessions...` ピッカーは添付可能な Copilot CLI セッションのみ表示する。

**直近のアクティビティ:** Copilot レビュアーが要約。anthonykim1 が 4 件コメント。

**変更ファイル:** `agentHostSessionHandler.ts`（+52/-1）、`agentHostSessionReferenceAttachment.ts`（+92/-0）、`chatContext.ts`、`agentHostChatContribution.test.ts` など計 6 ファイル（+444/-7）。

**次のアクション:** ドラフトを仕上げ、anthonykim1 のレビュー指摘を反映する。

---

### ![PR](icons/git-pull-request.svg) [Add VSC model E prompt with keep_compact instructions](https://github.com/microsoft/vscode/pull/323803) `#323803`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Copilot 拡張に「VSC model E」向けのプロンプト（`keep_compact` 指示付き）とモデル能力設定を追加する。

**直近のアクティビティ:** Copilot レビュアーは課金設定の問題でレビューできずエラーを返した。karthiknadig が CHANGES_REQUESTED。

**変更ファイル:** `vscModelPrompts.tsx`（+35/-2）、`chatModelCapabilities.ts`（+9/-0）。

**次のアクション:** karthiknadig の変更要求に対応し、triage を進める。

---

### ![PR](icons/git-pull-request.svg) [Agent-host MCP authentication + accurate/persisted MCP auth state](https://github.com/microsoft/vscode/pull/323968) `#323968`
**ステータス:** Merged | **ラベル:** なし

**概要:** agent-host 経由の MCP 認証を追加し、MCP サーバーの認証状態を正確かつ永続化する。`AuthRequired` の MCP サーバーをチャット内プロンプトで提示し OAuth フロー（クリックで対話、再接続時はサイレント自動許可）を駆動しトークンを agent host へ転送する。記憶する認証を「セッションホスト authority + サーバー名 + リソース URL」という安定 ID にひも付け、リロードごとに付与が失効していた問題を修正。

**直近のアクティビティ:** Copilot レビュアーが要約。dmitrivMS が APPROVED。ボットが CODENOTIFY で TylerLeonhardt に通知。

**変更ファイル:** `copilotAgent.ts`（+93/-11）、`copilotAgentSession.ts`（+116/-2）、`mcpCustomizationController.ts`（+98/-26）など計 31 ファイル（+1256/-240）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [chats are leaking wetween workspaces](https://github.com/microsoft/vscode/issues/323971) `#323971`
**ステータス:** Open | **ラベル:** new release

**概要:** 新しいワークスペースで新規プロジェクトを開くと、別プロジェクトの無関係なチャットセッションが表示されるという不具合報告（1.127.0、Linux）。

**直近のアクティビティ:** qt1 が「Gemini によると github-copilot から cline か cursor に乗り換える必要があるらしい」と皮肉的にコメント。

**次のアクション:** ワークスペース間でチャットセッションが混在する原因（スコープ／ストレージキー）を調査する。

---

### ![Issue](icons/issue-opened.svg) [Track session-reference attachment support across chat session types](https://github.com/microsoft/vscode/issues/323969) `#323969`
**ステータス:** Open | **ラベル:** なし

**概要:** PR #323773 は Copilot CLI の `events.jsonl` に解決できる Agent Host セッション参照に意図的に絞られている。本 Issue は、チャット／セッションプロバイダー全体にわたるセッション参照の添付マトリクス（より広範な対応範囲）を追跡する。#315193 に関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 各プロバイダーでのセッション参照添付の対応方針を整理する。

---

### ![Issue](icons/issue-opened.svg) [Track full session porting for Continue in Agent Host](https://github.com/microsoft/vscode/issues/323970) `#323970`
**ステータス:** Open | **ラベル:** なし

**概要:** PR #323773 が扱うのは「Copilot CLI セッションを Agent Host チャットに添付する」狭いケースだが、本 Issue は「continue in」フローでの完全なセッション移植（旧セッションをアクティブな Agent Host 会話にインポート／変換する）という別問題を追跡する。#315193 に関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 添付と移植の違いを整理し、continue in フローの完全セッション移植の設計を検討する。

---

### ![PR](icons/git-pull-request.svg) [Add client tool completion routing logs](https://github.com/microsoft/vscode/pull/323953) `#323953`
**ステータス:** Merged | **ラベル:** なし

**概要:** agent-host のクライアントツール完了ハンドオフまわりに診断ログを追加する。`AgentSideEffects` がクライアントツール完了を転送する際のログ、転送不能時（エージェント欠落や subagent 親チャット解決不可）の警告、`CopilotAgent.onClientToolCallComplete` でのデフォルト／ピアチャット欠落時のログ／破棄を記録。UI は `chat/toolCallComplete` を観測するのにランタイムが再開しないケースを将来切り分けるための診断専用。

**直近のアクティビティ:** Copilot レビュアーが要約。anthonykim1 が APPROVED。

**変更ファイル:** `agentSideEffects.ts`（+15/-4）、`copilotAgent.ts`（+15/-2）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [GA vision in vs code](https://github.com/microsoft/vscode/pull/323965) `#323965`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot Vision の一般提供（GA）に合わせて、VS Code の画像・ビジョン体験を更新する。実験設定を削除し、画像カルーセルをデフォルト化、通常の画像・ツール結果でのプレビューチェックと文言を整える。

**直近のアクティビティ:** Copilot レビュアーが「preview/experimental のゲートを削除」と要約。jruales が APPROVED。

**変更ファイル:** `package.json`、`image.tsx`、`toolCalling.tsx`、`configurationService.ts`、`chatAttachmentWidgets.ts` など計 13 ファイル（+20/-51）。

**次のアクション:** マージ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [Snippet Choice Traps Focus](https://github.com/microsoft/vscode/issues/236489) `#236489`
**ステータス:** Closed | **ラベル:** bug, verified, snippets, insiders-released

**概要:** スニペットの選択肢（choice）がフォーカスをトラップしてしまう不具合。verified 済みで Insiders にリリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![PR](icons/git-pull-request.svg) [Implement session cache for chat sessions view](https://github.com/microsoft/vscode/pull/278901) `#278901`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッションビューにキャッシュ機構を実装し、ビュー初期化時に既読セッションを即座に表示する（agent sessions view と同じパターン）。`ChatSessionsCache` クラスがワークスペースストレージにプロバイダー種別ごとにシリアライズ／デシリアライズし、`localChatSessionType` のみをキャッシュする。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `sessionsViewPane.ts`（+140/-3）、`chatSessionsCache.test.ts`（+201/-0）、`sessionsTreeRenderer.ts`（+11/-0）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Search results: configurable truncation and size threshold](https://github.com/microsoft/vscode/pull/273450) `#273450`
**ステータス:** Closed | **ラベル:** なし

**概要:** 検索結果プレビューの切り詰め挙動を設定可能にする。全行表示、先頭／末尾／両側からの切り詰め（マッチを中央に維持）を選べる `search.resultsTruncation` と、切り詰め前の文字数 `search.allowedSizeBeforeTruncation` を追加。既定の挙動は据え置き。

**直近のアクティビティ:** osortega が CHANGES_REQUESTED で「この PR が何の問題を解決するのか分からない。検索時は用語の位置に応じて先頭・末尾いずれも切り詰められている」と指摘（`interface` 検索例の画像付き）。

**変更ファイル:** `search.ts`（+63/-38）、`getFileResults.ts`（+39/-4）、`searchEditor.ts` など計 7 ファイル（+217/-79）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Implement AI search success tracking telemetry for user interaction analytics](https://github.com/microsoft/vscode/pull/267874) `#267874`
**ステータス:** Closed | **ラベル:** なし

**概要:** AI 検索の有効性とユーザーエンゲージメントを測定するテレメトリを追加する。`SearchSessionTracker` クラスで検索セッションのライフサイクルと指標（クエリ数・AI 利用・トリガー方法・結果への操作による成功判定）を管理する（ドラフト）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `searchView.ts`（+209/-0）、`searchTelemetry.test.ts`（+213/-0）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add Most Recently Used (MRU) find history option](https://github.com/microsoft/vscode/pull/265941) `#265941`
**ステータス:** Closed | **ラベル:** なし

**概要:** 検索履歴の並び順を選べる `editor.find.historyOrder` 設定を追加する。`"chronological"`（既定・現行動作）と `"mostRecentlyUsed"`（再利用した語を先頭に移動）の 2 値。頻用する検索語が履歴の奥に埋もれる問題に対応（ドラフト）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `findWidgetMRUHistory.ts`（+122/-0）、`replaceWidgetMRUHistory.ts`（+122/-0）、`mruHistory.test.ts`（+140/-0）など計 7 ファイル（+437/-4）。

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Custom LaTeX snippets intermittently trigger multi-cursor mode](https://github.com/microsoft/vscode/issues/279349) `#279349`
**ステータス:** Closed | **ラベル:** bug, verified, snippets, insiders-released

**概要:** カスタム LaTeX スニペットが断続的にマルチカーソルモードを誤起動する不具合。verified 済みで Insiders リリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![PR](icons/git-pull-request.svg) [No implementation - awaiting requirements](https://github.com/microsoft/vscode/pull/290067) `#290067`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot コーディングエージェントが作成した PR。プロンプトが "hello" のみで具体的な要件がないため、コード変更は一切行われていない（作業ツリーはクリーン）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 変更なし（+0/-0、0 ファイル）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Skip failing browser tests in agentSessionViewModel](https://github.com/microsoft/vscode/pull/289278) `#289278`
**ステータス:** Closed | **ラベル:** なし

**概要:** macOS/Browser ワークフローで `agentSessionViewModel.test.ts` の 5 テストが失敗（2 件は `undefined logService.trace`、3 件は発火しないイベント待ちでタイムアウト）していたため、`test.skip()` でスキップする。根本原因はブラウザ環境向けのサービス注入不足。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `agentSessionViewModel.test.ts`（+5/-5）の 1 ファイル。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Remove cyclic dependency on IAgentSessionsService in chat input part](https://github.com/microsoft/vscode/pull/289277) `#289277`
**ステータス:** Closed | **ラベル:** なし

**概要:** `chatInputPart.ts` が `agentSessionsService.model` に依存して `chatInputPart → agentSessionsService.model → chatModel` という循環依存を作っていた問題を解消する。chatInputPart は既に chatModel に属するため、この間接参照は不要かつアーキテクチャ的に誤りとして依存を削除する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `chatInputPart.ts`（+4/-27）の 1 ファイル。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Experimenting welcome and picker](https://github.com/microsoft/vscode/pull/286854) `#286854`
**ステータス:** Closed | **ラベル:** なし

**概要:** 2 つの実験的機能を導入するドラフト PR。項目数の多いチャットセッションオプショングループ（例: リポジトリ選択）向けの検索可能ピッカー UI と、エージェントセッションのウェルカム／ランディングページ（agent sessions start page）。

**直近のアクティビティ:** Copilot レビュアーが検索可能ピッカーと start page の追加を要約。

**変更ファイル:** `agentSessionsStartPage.contribution.ts`（+92/-0）、`extHostChatSessions.ts`（+63/-2）、`agentSessionsStartInput.ts`（+73/-0）など計 17 ファイル（+1537/-40）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Preserve search listeners after replace (fix #282773)](https://github.com/microsoft/vscode/pull/283061) `#283061`
**ステータス:** Closed | **ラベル:** なし

**概要:** #282773 を修正。Replace All 後に `clear()` がファイルマッチとモデルリスナーを破棄していたため、undo しても変更が検知されず Replace All ボタンが無効のままだった問題。`clear()` をやめて `forceUpdateMatches()` で即時マッチ更新し、リスナーを維持したまま undo を検知できるようにする（大規模結果ではやや遅くなるトレードオフ）。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。

**変更ファイル:** `fileMatch.ts`（+6/-0）、`textSearchHeading.ts`（+8/-3）、`searchTreeCommon.ts`（+1/-0）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Adding needsinput state](https://github.com/microsoft/vscode/pull/282558) `#282558`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッションに `needsinput`（入力待ち）状態を追加する変更（`release/1.107` ブランチ対象、ドラフト）。本文は PR テンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `chatModel.ts`（+7/-3）、`mainThreadChatSessions.ts`（+8/-2）、`localAgentSessionsProvider.ts` など計 5 ファイル（+22/-6）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Use getChatSessionType for proper session type handling in multidiff content](https://github.com/microsoft/vscode/pull/281369) `#281369`
**ステータス:** Closed | **ラベル:** なし

**概要:** #281316 のレビュー指摘に対応。ローカルチャットセッションでは `resource.scheme` を直接使うのは不正確なため、`getChatSessionType()`（`vscodeChatEditor` → `localChatSessionType` など scheme を適切に解決）に置き換え、正しいコンテキストキー `ChatContextKeys.agentSessionType.key` を使う。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `chatMultiDiffContentPart.ts`（+12/-14）の 1 ファイル。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Feature/export search results](https://github.com/microsoft/vscode/pull/279665) `#279665`
**ステータス:** Closed | **ラベル:** なし

**概要:** 検索結果を JSON／CSV／プレーンテキストにエクスポートする機能を追加する。分析・共有・アーカイブ目的で結果を保存できる。

**直近のアクティビティ:** Copilot レビュアーが「検索ツールバーに新規エクスポートアクションを登録」と要約。

**変更ファイル:** `searchActionsExport.ts`（+1514/-0）、`searchActionsExport.test.ts`（+1830/-0）、`searchActionsExport.browser.test.ts`（+371/-0）など計 11 ファイル（+3824/-101）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [feat(search): add file and folder name search to search view](https://github.com/microsoft/vscode/pull/279640) `#279640`
**ステータス:** Closed | **ラベル:** なし

**概要:** 検索結果ビューに、検索パターンに名前が一致するファイル・フォルダを表示する折りたたみ可能な「File and Folder Names」セクションを追加する。既存のテキスト内容検索と並列に実行され、結果クリックでファイルを開く。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。

**変更ファイル:** `fileNameSearchHeading.ts`（+179/-0）、`searchModel.ts`（+96/-11）、`searchResultsView.ts`（+91/-2）など計 10 ファイル（+854/-31）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Osortega/multi-diff-core](https://github.com/microsoft/vscode/pull/279507) `#279507`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットサービスにマルチ diff エディタ対応を追加し、チャット編集セッションがアクティブなときにファイル変更を表示できるようにする（ドラフト）。編集セッションの diff データをチャットモデルの応答進捗システムに統合する。

**直近のアクティビティ:** Copilot レビュアーが要約。osortega と connor4312 がコメント。

**変更ファイル:** `chatInputPart.ts`（+194/-1）、`chatWidget.ts`（+23/-0）、`chatReferencesContentPart.ts` など計 7 ファイル（+243/-5）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Rename "History" to "Archived" in Agent Sessions view](https://github.com/microsoft/vscode/pull/279283) `#279283`
**ステータス:** Closed | **ラベル:** なし

**概要:** Agent Sessions ビューのローカルチャットセッションで、グループラベルを「History」から「Archived」へ改名する（議論済み）。仮命名に関する古いコメントも削除。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `sessionsTreeRenderer.ts`（+1/-2）の 1 ファイル。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Restrict chat session actions to local chat sessions only](https://github.com/microsoft/vscode/pull/279260) `#279260`
**ステータス:** Closed | **ラベル:** なし

**概要:** `ChatSessionsMenu` の 3 つのメニューアクション（`DeleteChatSessionAction`、`OpenChatSessionInNewWindowAction`、`OpenChatSessionInNewEditorGroupAction`）が全セッション種別に表示されていたのを、`sessionType.isEqualTo(localChatSessionType)` の `when` 条件を追加してローカルチャットセッションのみに限定する。既存の他アクションと整合させる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `chatSessionActions.ts`（+3/-0）の 1 ファイル。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [ChatSessionService test fix](https://github.com/microsoft/vscode/pull/279107) `#279107`
**ステータス:** Closed | **ラベル:** なし

**概要:** `ChatSessionsService` のテストスイートのスキップを解除し、`extractFileNameFromLink` メソッドを public にしてテスト可能にする修正。

**直近のアクティビティ:** deepak1556 が APPROVED。bpasero は CHANGES_REQUESTED で「これは良くない。そもそもコンパイルエラーにならなかった原因（元のスキップ）だ」と指摘し該当箇所へリンク。

**変更ファイル:** `chatSessions.contribution.ts`（+1/-1）、`chatSessionsService.test.ts`（+1/-1）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Standardize Learnings sections across instruction files](https://github.com/microsoft/vscode/pull/278245) `#278245`
**ステータス:** Closed | **ラベル:** なし

**概要:** `.github/instructions/` 配下のエージェント指示ファイルで、ベストプラクティスや落とし穴を追跡する Learnings セクションの体裁を統一する。`observables`・`disposable`・`telemetry`・`tree-widgets` の各 instructions.md を `learnings.instructions.md` 仕様（カウンター形式 `(N)`）に合わせる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `disposable.instructions.md`、`observables.instructions.md`、`telemetry.instructions.md`、`tree-widgets.instructions.md`（計 4 ファイル、+17/-8）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Fix Search Editor tab labels not displaying query after window reload](https://github.com/microsoft/vscode/pull/277059) `#277059`
**ステータス:** Closed | **ラベル:** なし

**概要:** ウィンドウ再読み込み後、非アクティブな Search Editor タブが「Search: <query>」でなく「Search」とだけ表示される問題を修正する。`getName()` が非同期の `resolveModels()` に依存していたため、モデルファクトリで設定を同期的に保存し、`getConfigSync(resource)` を追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `searchEditorModel.ts`（+7/-1）、`searchEditorInput.ts`（+2/-2）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [WIP](https://github.com/microsoft/vscode/pull/275278) `#275278`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッションビューのビューコンテナ登録を、標準の VS Code アーキテクチャパターンに沿うようリファクタリングする（ドラフト）。登録をワークベンチ contribution クラス内からモジュールレベルコードへ移動する。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。

**変更ファイル:** `chatSessionsView.ts`（+10/-20）、`chat.contribution.ts`（+1/-2）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [[WIP] Add local git change detection feature](https://github.com/microsoft/vscode/pull/272591) `#272591`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot クラウドチャットセッションプロバイダーが、ローカルの git 変更を検知し、リモートコーディングエージェントへ委任する前にユーザーへコミットして継続するかキャンセルするか促す機能を追加する意図の WIP。実装は未着手。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 変更なし（+0/-0、0 ファイル）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [[WIP] Add detection for local git changes before chat session](https://github.com/microsoft/vscode/pull/272590) `#272590`
**ステータス:** Closed | **ラベル:** なし

**概要:** #272591 と同趣旨の WIP。リモート委任前にローカル git 変更を検知してユーザーに確認を促す機能を計画するもので、実装は未着手。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 変更なし（+0/-0、0 ファイル）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Support for chat sessions metadata](https://github.com/microsoft/vscode/pull/268591) `#268591`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッションのメタデータ対応を追加する変更（ドラフト）。本文は PR テンプレートのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `chatSessionActions.ts`（+56/-4）、`mainThreadChatSessions.ts`（+13/-19）、`extHostChatSessions.ts` など計 18 ファイル（+174/-70）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Support for creating new chat with metadata](https://github.com/microsoft/vscode/pull/268236) `#268236`
**ステータス:** Closed | **ラベル:** なし

**概要:** #268219 に関連。メタデータ付きで新規チャットを作成する新コマンド `OpenNewChatSessionEditorWithMetadata` を追加する（ドラフト）。

**直近のアクティビティ:** Copilot レビュアーが変更概要を要約。joshspicer がコメント。

**変更ファイル:** `chatSessionActions.ts`（+32/-0）、`chatSessions.contribution.ts`（+6/-1）、`chat.contribution.ts` など計 4 ファイル（+41/-2）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Claude with worktree](https://github.com/microsoft/vscode/pull/268081) `#268081`
**ステータス:** Closed | **ラベル:** なし

**概要:** git worktree とチャットセッションを連携させる変更（ドラフト）。git 拡張にコマンドを追加し、チャットセッションビューまわりを拡張する。

**直近のアクティビティ:** rebornix がコメント。

**変更ファイル:** `commands.ts`（+94/-0）、`chatSessions.contribution.ts`（+56/-7）、`sessionsViewPane.ts`（+66/-1）など計 7 ファイル（+227/-11）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add dropdown with chevron to chat session "New Editor" buttons](https://github.com/microsoft/vscode/pull/267549) `#267549`
**ステータス:** Closed | **ラベル:** なし

**概要:** チャットセッションビューのタイトルバーの「New Chat Editor」ボタンを、既存の `CopilotTitleBarMenuRendering` に似たプライマリアクション付きドロップダウンパターンにする（ドラフト）。プライマリアクションは従来どおり新規チャットエディタ作成で、シェブロンのドロップダウンから追加オプションを選べる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `chatSessionButtonMenuRendering.ts`（+99/-0）、`chat.contribution.ts`（+2/-0）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Production release setup v2 6829665191136401404](https://github.com/microsoft/vscode/pull/316836) `#316836`
**ステータス:** Closed | **ラベル:** triage-needed

**概要:** CI ワークフローの大幅削除や `app/` 配下への多数のドキュメント・設定追加を含む、79 ファイル・+8835/-1259 の広範な変更（本文は PR テンプレートのみで、リリースセットアップを名乗る大規模変更）。

**直近のアクティビティ:** Copilot レビュアーがエラーでレビューできず、再リクエストを案内。

**変更ファイル:** `build.yml`（+69/-0）、`pr-linux-test.yml`（-416）、`app/DEPLOYMENT.md`（+200/-0）など計 79 ファイル（+8835/-1259）。

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Shell integration fails in PowerShell terminal](https://github.com/microsoft/vscode/issues/303020) `#303020`
**ステータス:** Closed | **ラベル:** bug, install-update, terminal, windows, insiders-released

**概要:** Windows の PowerShell ターミナルでシェル統合が失敗する不具合。Insiders リリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [Disposable leak in ChatListRenderer when tool transitions from thinking part to main template](https://github.com/microsoft/vscode/issues/302687) `#302687`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** ツールが thinking パートからメインテンプレートへ遷移する際、`ChatListRenderer` で Disposable のリークが発生する不具合。Insiders リリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [Timeout Error during chat](https://github.com/microsoft/vscode/issues/301651) `#301651`
**ステータス:** Closed | **ラベル:** なし

**概要:** リモート SSH 利用時にチャットで「network error / HTTP/2 stream timeout after 300000」というタイムアウトエラーが出るという報告（拡張 0.39.0、VS Code 1.111.0）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Command line `--install-extension` & `--list-extensions` options do not work within VSCode integrated terminal](https://github.com/microsoft/vscode/issues/301050) `#301050`
**ステータス:** Closed | **ラベル:** bug, verified, install-update, windows, insiders-released, terminal-shell-pwsh

**概要:** VS Code 統合ターミナル（PowerShell）内で `code --list-extensions` や `code --install-extension` を実行しても、UI は開くがコマンドが何も返さない。ターミナル外では正常。extensions.json から拡張を一括インストールするタスクが機能しない。verified 済み、Insiders リリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [git rebase --interactive in the terminal pops up a new window and reports errors when VS Code is started from a shortcut.](https://github.com/microsoft/vscode/issues/300231) `#300231`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, insiders-released

**概要:** VS Code をショートカットから起動した場合に限り、統合ターミナル（PowerShell Core）での `git commit` や `git rebase --interactive` が既存ウィンドウのタブではなく新しいウィンドウを開いてエラーを出す。`code.cmd` や `Code.exe` から起動した場合は正常。1.110.0 以降で発生。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [After Update: VS Code Opens Multiple Windows on Startup and Claude Latency Makes Models Unusable](https://github.com/microsoft/vscode/issues/299962) `#299962`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, insiders-released

**概要:** 更新後、起動時に余分なウィンドウが多数開いて手動で閉じる必要があり、さらに Claude モデルの応答が遅すぎて使えないという報告（1.110.1、Windows）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [PWSH Terminal Integration startup failure](https://github.com/microsoft/vscode/issues/299703) `#299703`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, regression, insiders-released, terminal-shell-pwsh

**概要:** PowerShell ターミナル統合が起動時に失敗するリグレッション不具合（Windows）。Insiders リリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [Error "Failed to delete the database: Database IO error" mentioning service_worker_storage.cc, while writing a `git commit` message](https://github.com/microsoft/vscode/issues/299598) `#299598`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, insiders-released

**概要:** `git commit` メッセージ作成中に `service_worker_storage.cc` に言及する「Failed to delete the database: Database IO error」エラーが出る不具合（Windows）。Insiders リリース済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。Insiders で修正リリース済み。

---

### ![Issue](icons/issue-opened.svg) [Disabling Git in settings breaks sessions view in Copilot Chat UI](https://github.com/microsoft/vscode/issues/298781) `#298781`
**ステータス:** Closed | **ラベル:** なし

**概要:** 設定で `"git.enabled": false` にすると、Copilot Chat の sessions ビューが壊れる。チャットにメッセージを送っても新規セッションが作られず、「New Session」を押すと過去セッションが失われたように見える。拡張ホストを再起動すると一覧が更新される。git 設定を true に戻すと復旧する。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Integrated Browser: Improvements to link click entrypoint](https://github.com/microsoft/vscode/issues/296769) `#296769`
**ステータス:** Closed | **ラベル:** feature-request, browser-integration

**概要:** ターミナルやチャットのリンククリックから統合ブラウザで開くエントリポイント（現状は `workbench.browser.openLocalhostLinks` 有効時の localhost リンクのみ）の改善を追跡する Issue。重複としてマークされた複数 Issue の提案を集約している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要（改善提案の集約先）。

---

### ![Issue](icons/issue-opened.svg) [compatiblity issue](https://github.com/microsoft/vscode/issues/294783) `#294783`
**ステータス:** Closed | **ラベル:** なし

**概要:** VS Code 1.109.0 が GitHub Copilot との互換性警告を表示するため、最新の安定版に更新してほしいという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要（最新版への更新で解消）。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Git Branch Tree/Folder View (Hierarchical Branch Explorer)](https://github.com/microsoft/vscode/issues/294038) `#294038`
**ステータス:** Closed | **ラベル:** なし

**概要:** VS Code は Git ブランチをフラットなリストで表示するため、`feature/*`・`release/*`・`hotfix/*` のような階層的・プレフィックス命名のリポジトリで扱いにくい。Visual Studio や JetBrains IDE のような階層的ブランチツリー／フォルダ表示を追加してほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add min-width to agents welcome view to prevent text overlap](https://github.com/microsoft/vscode/pull/297070) `#297070`
**ステータス:** Closed | **ラベル:** なし

**概要:** エディタ領域が狭いとき、製品名見出し（"Visual Studio Code - Insiders"）が行の重なりを起こす問題を修正する。`.agentSessionsWelcome-content` に `min-width: 350px` を設定してレイアウトの崩壊を防ぎ、`DomScrollableElement` の水平スクロールを Auto にして極端な狭幅ではクリップせず横スクロールさせる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `agentSessionsWelcome.ts`（+2/-1）、`agentSessionsWelcome.css`（+1/-0）。

**次のアクション:** クローズ済み。対応不要。
## qmk/qmk_firmware

> tap-hold 処理の weak mods クリアというコア修正や macOS Dictation キーコード追加といった新しい動きに加え、長期無活動でステイル化した PR（キーボード追加、コア修正）や unicode タップ遅延の機能要望が動いている。

### ![PR](icons/git-pull-request.svg) [Clear weak mods before processing queued keypresses in tap-hold handling](https://github.com/qmk/qmk_firmware/pull/26214) `#26214`
**ステータス:** Open | **ラベル:** core, stale

**概要:** `action_exec()` には前回のキー押下から残った weak mods をクリアするコードがあるが、tap-hold 処理はイベントをバッファリングして tap-hold 判定確定後に処理するため `action_exec()` を経由せず、バッファ内のキー押下の weak mods が後続の処理に影響してしまう。LT でレイヤーを起動し、タッピングターム内で修飾付きキーコードに続けて非修飾キーコードを素早く打つと発生（例: `!=` を打とうとして `!+` になる）。バッファされたレコードを処理へ渡す前に weak mods をクリアする修正を実装した。

**直近のアクティビティ:** github-actions[bot]が「45 日間活動がないため自動的にステイルとしてマークした。30 日以内にさらなる活動がなければクローズされる」と通知した。

**変更ファイル:** 2 ファイル変更（+75/-3）。`quantum/action_tapping.c`（+16/-3）とテスト `tests/tap_hold_configurations/default_mod_tap/test_tap_hold.cpp`（+59）。

**次のアクション:** drashna が APPROVED 済みのため、ステイルクローズを避けるためのステータス更新とマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Add YMDK YMD75 rev4 ANSI](https://github.com/qmk/qmk_firmware/pull/26204) `#26204`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale

**概要:** YMDK YMD75 rev4 キーボードの ANSI バリアントのサポートを追加する PR。YMDK Discord から入手したソース（ANSI と ISO の両方を含むが、両者はスイッチ／LED マトリクスのみ異なる）に基づく。QMK には既に ISO バリアントがあるが、それは全く異なるプロセッサと USB 識別子を使っており、入手したソースは従来リビジョンと同じ識別子・プロセッサを使う点が意外だと述べる。入手したソースは状態が悪く古い QMK リビジョンと複数ソースの混在だったため最新形式へ変換・修正・クリーンアップした。作者は QMK ソースを触るのが初めてとのこと。

**直近のアクティビティ:** github-actions[bot]が 45 日間の無活動によるステイルマークを通知した。

**変更ファイル:** 10 ファイル変更（+487/-0）。`keyboards/ymdk/ymd75/rev4/ansi/` 配下に `keyboard.json`（+275）、`matrix.c`（+77）、`keymaps/default/keymap.c`、`config.h`、`mcuconf.h` などを新規追加。

**次のアクション:** 過去に drashna が CHANGES_REQUESTED を出しレビューのやり取りがある。ステイルクローズを避けるための対応と再レビュー。

---

### ![PR](icons/git-pull-request.svg) [Chore log useless work](https://github.com/qmk/qmk_firmware/pull/26123) `#26123`
**ステータス:** Closed | **ラベル:** core, keyboard, keymap, CI, dd

**概要:** 本文は説明が未記入のテンプレートのみのドラフト PR。差分が異常に巨大（+30346/-1482106、19609 ファイル変更）で、多数のキーボード定義の削除や `is31fl3763` LED ドライバの追加、NuPhy キーボードのビルドワークフロー追加などを含み、ブランチ運用上の誤りとみられる内容。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 19609 ファイル変更（+30346/-1482106）。`.github/workflows/build_nuphy_keyboards.yml`（新規）、`drivers/led/issi/is31fl3763.c`／`.h`（新規）、多数の `keyboards/` 配下定義の削除など。

**次のアクション:** クローズ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Need a way to configure unicode tap code delay](https://github.com/qmk/qmk_firmware/issues/26304) `#26304`
**ステータス:** Open | **ラベル:** enhancement, help wanted

**概要:** 一部アプリが `tap_code()` で送られたキーを検出しないため `TAP_CODE_DELAY` を 15〜20ms 以上にする必要があるが、その結果 unicode キー（7〜8 キーをタップ）の送信が非常に遅くなり、同じ unicode キーを素早く連打するとキー押下が取りこぼされる。unicode タップに別の遅延を使えるようにしたいという要望。解決案として、(1) `UNICODE_TAP_DELAY` を追加し `unicode_input_start()` などで使う（デフォルトは `TAP_CODE_DELAY`）、(2) `send_char()` を `__attribute__((weak))` にしてユーザーがカスタマイズ可能にする、の 2 案を提示。作者は案 1 を好むが案 2 の方がコア変更が少ないとし、方針合意後に PR を出すと述べる。

**直近のアクティビティ:** drashna が「tap_code キーが認識されない問題があるなら unicode 文字（通常文字を送っているだけ）でも同じ問題が起きるはず。『extra key』キーコードの問題ならそれは別だが、そういう書き方ではなかった。実際に何を使っているのか例はあるか」と質問した。

**次のアクション:** drashna の質問（実際の使用例）への回答と、2 つの解決案のどちらを採るかの方針合意。

---

### ![PR](icons/git-pull-request.svg) [Add KC_DICTATION keycode for macOS Dictation](https://github.com/qmk/qmk_firmware/pull/26305) `#26305`
**ステータス:** Open | **ラベル:** core, documentation, dd

**概要:** macOS の Dictation（音声入力）用に、標準 usage 0x0CF へマップする `KC_DICTATION` キーコードを追加する PR。`EXTRAKEY_ENABLE` が必要。ドキュメントも更新している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル変更（+21/-2）。`data/constants/keycodes/keycodes_0.0.9_basic.hjson`（+12、新規）、`quantum/keycodes.h`、`tmk_core/protocol/report.h`、`docs/keycodes.md`、`docs/keycodes_basic.md`。

**次のアクション:** レビューを待つ。

---

