# GitHub ダイジェスト — 2026-07-21

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Dictation: migrate on-device runtime to Foundry Local streaming ASR](https://github.com/microsoft/vscode/pull/326678)** (microsoft/vscode) — オンデバイス音声入力ランタイムを Foundry Local の Nemotron ストリーミング ASR に全面移行する大規模変更で、以降の dictation 関連 PR 群の起点になっている
2. ![PR](icons/git-pull-request.svg) **[fix(window): avoid UAF in `close_windows`](https://github.com/neovim/neovim/pull/40853)** (neovim/neovim) — `WinClosed` autocmd 起因の use-after-free クラッシュ修正で release-0.12 へバックポート済み、無限ループ懸念のフォローが残る安定性重要修正
3. ![PR](icons/git-pull-request.svg) **[convert tests to unittest](https://github.com/commaai/openpilot/pull/38387)** (commaai/openpilot) — 82 ファイルに及ぶテスト基盤の大規模移行で、詳細なベンチマークを伴い、別方針の自作ランナー案 #38383 との調整が進行中
4. ![PR](icons/git-pull-request.svg) **[terminal: handle page capacity errors in cursorScrollAbove](https://github.com/ghostty-org/ghostty/pull/13394)** (ghostty-org/ghostty) — ページ境界越えの行コピーで発生する状態破壊クラッシュ (#13160) を修正し、関連する `eraseRow` 側の調査も継続中
5. ![Issue](icons/issue-opened.svg) **[Vulnerability : Update quick-xml crate to 0.41.0](https://github.com/astral-sh/uv/issues/20542)** (astral-sh/uv) — high 1 件を含む 3 件の脆弱性報告でセキュリティ影響あり

---

## sunnypilot/sunnypilot

> 直近24時間は upstream openpilot からの同期マージや C3(TICI) 起動処理の revert が完了し、HKG 向けの車両ポート・レーダー機能やロング/ラテラル制御の新機能 PR が多数進行中です。多くはドラフト状態で、Issue はいずれもメンテナから回答が付いてクローズされました。

### ![PR](icons/git-pull-request.svg) [Revert "agnos: split launch for c3 and c3x to support custom agnos"](https://github.com/sunnypilot/sunnypilot/pull/1879) `#1879`
**ステータス:** Merged | **ラベル:** なし

**概要:** commit 54174d1e を revert し、C3/C3X 向けにカスタム agnos をサポートするための起動処理の分割を取り消しています。comma three (TICI) のサポートは `master-tici` ブランチで継続する方針です。5 ファイルで 217 行を削除しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `launch_openpilot.sh` の変更に加え、`openpilot/sunnypilot/system/hardware/c3/` 配下の `README.md`・`agnos.json`・`launch_chffrplus.sh`・`launch_env.sh` を削除しています。

**次のアクション:** マージ済みのため対応不要。TICI 向け作業は `master-tici` で継続。

---

### ![PR](icons/git-pull-request.svg) [mapd: ignore in plannerd health check](https://github.com/sunnypilot/sunnypilot/pull/1880) `#1880`
**ステータス:** Merged | **ラベル:** なし

**概要:** `plannerd` のヘルスチェックにおいて `mapd` を無視するように変更しています。`plannerd.py` の 1 ファイルで 2 行追加・1 行削除の小規模修正です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `openpilot/selfdrive/controls/plannerd.py` を変更。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Sync: `commaai/openpilot:master` → `sunnypilot/sunnypilot:master`](https://github.com/sunnypilot/sunnypilot/pull/1876) `#1876`
**ステータス:** Merged | **ラベル:** なし

**概要:** upstream の `commaai/openpilot:master` を `sunnypilot/sunnypilot:master` へ同期する定例 PR です。1,474 ファイルにわたり 3,883 行追加・3,507 行削除と大規模で、`cereal` 配下の `openpilot/` 以下への移動（リネーム）や CI ワークフローの更新を多数含みます。

**直近のアクティビティ:** github-actions[bot]による UI Preview では mici・big いずれも動画に差分が出ていると報告されています。

**変更ファイル:** `.github/workflows/` 配下の各種ワークフロー、`SConstruct`、`Jenkinsfile`、`cereal` のディレクトリ再配置（`openpilot/cereal/` へのリネーム）など広範囲に及びます。

**次のアクション:** マージ済みのため対応不要。UI 差分は同期に伴う想定内の変化か要確認。

---

### ![PR](icons/git-pull-request.svg) [UI: Show experimental mode indicator that respects DEC](https://github.com/sunnypilot/sunnypilot/pull/1878) `#1878`
**ステータス:** Open | **ラベル:** ui

**概要:** mici の torque bar 右側に experimental mode の有効状態を示すインジケータを追加します。非アクティブ時は experimental_white、アクティブ時は experimental を使用し、DEC 使用時の状態も考慮します。big UI 側でも experimental mode の判定を DEC 対応にするロジック変更を含みます。

**直近のアクティビティ:** github-actions[bot]の UI Preview では mici は動画が一致、big は動画に差分ありと報告。copilot-pull-request-reviewer[bot]が変更概要（アイコンテクスチャの読み込み、torque bar 右側へのアイコン描画、sunnypilot UI + DEC 時は DEC が `blended` 状態のときのみアクティブアイコンを表示）をレビューコメントとして残しています。

**変更ファイル:** `openpilot/selfdrive/ui/mici/onroad/hud_renderer.py`（22 行追加）と `openpilot/selfdrive/ui/onroad/exp_button.py`（7 行追加）を変更。

**次のアクション:** big UI の動画差分が意図通りか確認し、メンテナによるレビュー・マージ判断を待つ。

---

### ![PR](icons/git-pull-request.svg) [HKG: Radar Tracks](https://github.com/sunnypilot/sunnypilot/pull/1723) `#1723`
**ステータス:** Open (draft) | **ラベル:** enhancement, ui, raylib

**概要:** HKG 車両向けにレーダートラックを表示・処理する大規模な機能追加です。tizi/mici でのトグル、HDA2 の異なるバス対応、レーダー種別の自動検出が完了済みで、残タスクとして「sp ext への移動」が残っています。30 ファイルで 6,315 行追加と非常に大きな変更です。

**直近のアクティビティ:** github-actions[bot]の UI Preview では mici・big いずれも動画に差分ありと報告されています。

**変更ファイル:** `selfdrive/controls/radard.py`・`selfdrive/car/card.py` のレーダー処理、`selfdrive/ui/sunnypilot/onroad/radar_tracks.py`（新規 345 行）とそのテスト、mici/big 両方の `model_renderer.py`・`augmented_road_view.py`、`tools/radar/` 配下の調査資料（`RADAR_RESEARCH.md`・`SIGNALS.md`・`PLATFORM_MATRIX.md`）や `ui.py`（新規 2,774 行）などを含みます。

**次のアクション:** ドラフトのため「sp ext への移動」を完了させ、レビュー・テスト整備を進める。

---

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open (draft) | **ラベル:** car

**概要:** HKG の ccNC（non-HDA2）ポートに関するドラフト PR で、本文には opendbc の関連 PR (sunnypilot/opendbc#94) へのリンクのみ記載されています。`opendbc_repo` の 1 行更新のみの変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** サブモジュール参照 `opendbc_repo` を 1 行変更。

**次のアクション:** ドラフトのため、opendbc 側 PR の進捗と合わせてポート作業を継続。

---

### ![PR](icons/git-pull-request.svg) [Lateral: Road Edge Lane Change Controller](https://github.com/sunnypilot/sunnypilot/pull/1085) `#1085`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 車両が路肩（road edge）付近にいるときの車線変更を防ぐ `RoadEdgeLaneChangeController` を追加します。確率しきい値（edge 0.35、lane line 0.2）で路肩を検出し、誤検出回避のため 1 秒の反応時間を設けて、検出した路肩方向への車線変更をブロックします。左右のブロックフラグを新規 `modelDataV2SP` メッセージで配信します。

**直近のアクティビティ:** github-actions[bot]の UI Preview では mici・big とも動画が一致と報告されています。

**変更ファイル:** `openpilot/cereal/custom.capnp`・`common/params_keys.h` のスキーマ／パラメータ、`desire_helper.py`・`selfdrived.py` の車線変更ロジック、新規 `relc.py`（84 行）とテスト `test_relc.py`（99 行）、`modeld.py`、車線変更設定 UI などを変更。

**次のアクション:** メンテナによるレビューを待つ。

---

### ![PR](icons/git-pull-request.svg) [Long Control: Vibe Controller](https://github.com/sunnypilot/sunnypilot/pull/1068) `#1068`
**ステータス:** Closed (未マージ) | **ラベル:** enhancement

**概要:** 加速挙動（AccelPersonality: eco/normal/sport）と制動・追従距離（LongPersonality: relaxed/standard/aggressive）を独立して調整できる `VibePersonalityController` を導入する PR です。プロファイルを Params に保存して実行時に動的更新し、Hermite 補間で加速上限を制御します。ユニットテストも追加しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に sourcery-ai[bot]と devtekve のレビューコメントあり）。

**変更ファイル:** `cereal/custom.capnp`・`common/params_keys.h`、`long_mpc.py`・`longitudinal_planner.py` の制御ロジック、settings 系 UI、新規 `vibe_personality/` パッケージ（`accel_personality.py`・`follow_personality.py`・`vibe_personality.py` とテスト 314 行）などを追加。

**次のアクション:** クローズ済み。再開する場合は既存レビュー指摘の反映が必要。

---

### ![PR](icons/git-pull-request.svg) [car: use vEgoCluster pre-hysteresis for longitudinal planner](https://github.com/sunnypilot/sunnypilot/pull/999) `#999`
**ステータス:** Closed (未マージ) | **ラベル:** bug

**概要:** longitudinal planner が使う自車速を、ヒステリシス適用後の `vEgo` から適用前のクラスター速度 `vEgoCluster` に切り替えるドラフト PR です。`CarStateSP` スキーマに `vEgoCluster` を追加し、`plannerd` で `carStateSP` を購読して `LongitudinalPlanner` が `sm['carStateSP'].vEgoCluster` を参照するようにします。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `cereal/custom.capnp`、`opendbc_repo`、`longitudinal_planner.py`、`plannerd.py` を変更。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Hkg angle steering 2025](https://github.com/sunnypilot/sunnypilot/pull/807) `#807`
**ステータス:** Open (draft) | **ラベル:** なし

**概要:** HKG の angle steering 対応と、pygame ベースのジョイスティック制御・ラテラル制御チューニングを追加するドラフト PR です。#801 と #641 を supersede します。Ioniq 5 PE / GV80 / GV80 coupe のチェック項目が未完で、angle steering 用のスムージング・トルク・オーバーライドなどの新設定を追加します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に作成者 devtekve 自身のレビューコメントあり）。

**変更ファイル:** IDE/実行構成ファイル群、`opendbc_repo`、`openpilot/common/version.py`、新規 Jupyter ノートブック `hkg_canfd_angle_steering.ipynb`（2,196 行）・`hyundai_angle_steering.ipynb`（228 行）などを追加。

**次のアクション:** ドラフトのため、対象車種の検証チェック項目を完了させる。

---

### ![PR](icons/git-pull-request.svg) [Joystick Control: Refactor to use pygame for cross-platform support](https://github.com/sunnypilot/sunnypilot/pull/702) `#702`
**ステータス:** Closed (未マージ) | **ラベル:** なし

**概要:** ジョイスティック制御ツールを `inputs` ライブラリから `pygame` に置き換え、クロスプラットフォーム対応と PlayStation/Xbox コントローラー両対応を実現するドラフト PR です。コントローラー種別を検出してボタンマッピングを切り替え、ノイズ低減のためデッドゾーンを導入します。

**直近のアクティビティ:** sunnyhaibin が「Closing for now.」とコメントしてクローズしました。

**変更ファイル:** `pyproject.toml`・`uv.lock` の依存関係、`process_config.py`、`tools/joystick/joystick_control.py`（119 行追加）・`joystickd.py`・README を変更。

**次のアクション:** クローズ済み。再開時は依存関係更新と動作検証が必要。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/sunnypilot/sunnypilot/pull/1866) `#1866`
**ステータス:** Open | **ラベル:** bot

**概要:** repo-maintenance からの自動 PR で、Python パッケージを更新します。`uv.lock` を中心に 245 行追加・238 行削除し、`.venv` の容量内訳や依存ツリーが本文に記載されています。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `docs/CARS.md` と `uv.lock` を変更。

**次のアクション:** CI 通過を確認のうえマージ判断。

---

### ![Issue](icons/issue-opened.svg) [2021 Subaru Forester Sport - LKAS Error on Engage](https://github.com/sunnypilot/sunnypilot/issues/1661) `#1661`
**ステータス:** Closed | **ラベル:** bug

**概要:** 2021 Subaru Forester Sport で、高速道路の出口ランプへ進入中にステアリングアシストボタンを押した際、Eyesight がフォルト状態になり Comma 4 に LKAS エラーが表示された、という報告です。車両再起動で解消したものの、Subaru の監視アプリにアラートが残ったとしています。Sunnypilot Dev 2026.01.19.3998 で発生。

**直近のアクティビティ:** sunnyhaibin が「まだ発生するか」と確認し、報告者 IIAIronWolf が「ランダムに発生するがクローズして良い」と回答してクローズされました。

**次のアクション:** 報告者の同意によりクローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Auto Lane Change: Delay with Blind Spot - Unavailable](https://github.com/sunnypilot/sunnypilot/issues/1851) `#1851`
**ステータス:** Closed | **ラベル:** bug

**概要:** 2017 Honda CR-V EX-L と 2024 Honda Civic EX で、ブラインドスポットモニタリングを備えているにもかかわらず、c4/mici のリリース版では該当オプションが無効（unavailable）になっているという報告です。2026 年 5 月 6 日以前の staging 版では利用可能だったとしています。

**直近のアクティビティ:** sunnyhaibin が、これは車両側の制約であり BSM 信号が LKAS と同じ CAN に流れないため comma デバイスがネイティブに BSM 信号を検出できない、追加ハードウェアを入れれば LKAS バスで検出できる可能性があると回答し、クローズしました。

**次のアクション:** 車両制約による仕様のためクローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Sunnypilot timing instability when subscribing to messaging bus (SubMaster/SubSocket) from external Python process](https://github.com/sunnypilot/sunnypilot/issues/1785) `#1785`
**ステータス:** Closed | **ラベル:** bug

**概要:** 外部 Python の「bridge」プロセスから messaging bus（ZMQ）を購読して車両データを読み取ると、sunnypilot で `liveParameters`・`liveTorqueParameters` 関連のタイミングエラーが発生し制御が不安定になる、という報告です。openpilot では発生せず、`SubMaster` から `SubSocket + recv_one_or_none`、`conflate=True`、低頻度化など各種対策を試しても再現し、bridge 停止で即解消するとしています。

**直近のアクティビティ:** sunnyhaibin が「最新の `master` で修正済みのはず。まだ問題があれば community forum にバグ報告を」とコメントしてクローズしました。

**次のアクション:** 最新 master で修正済みとしてクローズ。再発時はコミュニティフォーラムへ。

---

### ![Issue](icons/issue-opened.svg) [URML (open robot intent language): a typed operating-design-domain check in front of a maneuver (request for comment)](https://github.com/sunnypilot/sunnypilot/issues/1865) `#1865`
**ステータス:** Closed | **ラベル:** なし

**概要:** URML（urml.dev、Apache-2.0 のロボット意図記述言語）の作者による request for comment です。運転支援システムの ODD（operational design domain）を型付き・宣言的な safety envelope として表現し、意図したマニューバをディスパッチ前に capability manifest と安全境界に照らして検証する、という構想を提案しています。採用や保守の依頼ではなく、(1) 型付き宣言的な ODD チェックが ADAS マニューバの前段として有用か、(2) URML の capability manifest が sunnypilot の車両可否判断に対応するか、という 2 点を問うています。

**直近のアクティビティ:** 直近 24 時間のコメントなし（Issue はクローズ済み）。

**次のアクション:** クローズ済み。関心があれば提案の RFC を確認。

---

## commaai/openpilot

> テスト基盤の刷新（pytest からの脱却、unittest 化、カスタムランナー導入）と CI/Jenkins の信頼性向上に開発リソースが集中している。加えて MADS 機能追加、longitudinal 制御のリファクタ、サウンド追加やモデルロード高速化などの改善が並行して進んでいる。

### ![PR](icons/git-pull-request.svg) [try MADS](https://github.com/commaai/openpilot/pull/38391) `#38391`
**ステータス:** Open | **ラベル:** なし

**概要:** MADS（Modified Assistive Driving System）を試験導入する Draft PR。cereal/log.capnp、params_keys.h、selfdrived の state machine、UI のトグルや HUD renderer など 15 ファイルにわたって変更を加え、専用テスト test_mads.py も追加している。

**直近のアクティビティ:** process replay diff report が 66 セグメント全パス（変更・エラーなし）で通過。人によるレビューコメントはなし。

**変更ファイル:** cereal/log.capnp、selfdrived.py、state.py、UI の toggles.py と hud_renderer.py、テスト（test_mads.py 新規、test_state_machine.py 拡張）など計 15 ファイル（+163/-20）。

**次のアクション:** Draft 状態のためレビューとマージ判断が必要。state machine とイベント周りの挙動確認が求められる。

---

### ![PR](icons/git-pull-request.svg) [fix branch cleanup burning GH API requests](https://github.com/commaai/openpilot/pull/38392) `#38392`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** cleanup_closed_branches ジョブが PR クローズのたびに 17k 件のクローズ済み PR 全てをスキャンし、1 回あたり 1 万件の API リクエストを発行していた問題を修正する。diff report など他の CI ジョブが API 制限に達する原因になっていた。

**直近のアクティビティ:** process replay diff report が全パス（66 件）で通過。人によるレビューコメントはなし。

**変更ファイル:** .github/workflows/repo-maintenance.yaml のみ（+8/-30）。

**次のアクション:** レビューとマージ。CI の API 制限問題の解消につながるため優先度が高い。

---

### ![PR](icons/git-pull-request.svg) [convert tests to unittest](https://github.com/commaai/openpilot/pull/38387) `#38387`
**ステータス:** Open | **ラベル:** なし

**概要:** テストスイートを pytest から unittest スタイルへ移行する大規模 PR。テスト本体と分離を unittest 形式に保ちつつ、パラメータケースを実際の unittest メソッドとして公開し、遅いクラスをスケジュール前に除外、並列実行には pytest と pytest-xdist のみを使う。82 ファイルを変更（+601/-475）。

**直近のアクティビティ:** 作者 adeebshihadeh が詳細なベンチマークを投稿。ローカルの実行時間は元の 27.70s から 21.92s へ 18% 高速化した一方、pure unittest では 120.73s へ回帰したこと、その原因がパラメータグループのスケジューリングと遅いテストのスキップが遅すぎたためであることを説明。diff report は全パス。

**変更ファイル:** conftest.py（-86）、common/test.py 新規（+175）、common/parameterized.py、pyproject.toml、uv.lock、多数のテストファイルと CI 設定（tests.yaml、Jenkinsfile）など 82 ファイル。

**次のアクション:** ベンチマーク結果を踏まえたレビューと、関連する #38383（カスタムランナー案）との方針調整が必要。

---

### ![PR](icons/git-pull-request.svg) [jenkins reliability](https://github.com/commaai/openpilot/pull/38385) `#38385`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** Jenkins のテストを 100 回連続グリーンにすることを目標に、信頼性を反復的に改善する PR。Jenkinsfile、panda.cc、model_replay.py、test_onroad.py、test_power_draw.py、camerad の snapshot、manager の process.py、github_utils.py など 9 ファイルを変更（+116/-42）。

**直近のアクティビティ:** commaci-public が model replay のプロット（velocity、desiredCurvature など）を投稿。diff report は全パス。

**変更ファイル:** Jenkinsfile（+22/-3）を中心に、camerad やプロセス管理、テストの安定化に関わる 9 ファイル。

**次のアクション:** 100/100 連続グリーンの達成状況を確認し、安定したらマージ。

---

### ![PR](icons/git-pull-request.svg) [longitudinal: move cruise speed limit out of the MPC](https://github.com/commaai/openpilot/pull/38367) `#38367`
**ステータス:** Open | **ラベル:** なし

**概要:** クルーズ速度制限を MPC（long_mpc.py）の外に移し、longitudinal_planner.py 側で扱うようにリファクタする PR。long_mpc.py を簡素化（-36）し planner にロジックを移動（+78/-49）している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** long_mpc.py、longitudinal_planner.py、テスト用 plant.py の 3 ファイル（+95/-86）。

**次のアクション:** longitudinal 制御の挙動変化のレビューと検証。

---

### ![PR](icons/git-pull-request.svg) [speedup chunk reading](https://github.com/commaai/openpilot/pull/38389) `#38389`
**ステータス:** Merged | **ラベル:** なし

**概要:** ChunkStream.readinto が 45MB チャンクを f.read() 経由でステージングしてからコピーしていたのを、呼び出し側のバッファへ直接読み込むよう変更。コールドキャッシュで 2 倍高速化し、1.8GB の大型モデルロードで 4〜6 秒短縮する。

**直近のアクティビティ:** diff report が全パスで通過し、マージ済み。

**変更ファイル:** common/file_chunker.py のみ（+9/-8）。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [New sounds](https://github.com/commaai/openpilot/pull/38154) `#38154`
**ステータス:** Merged | **ラベル:** ui

**概要:** 新しいサウンドを導入する PR。soundd の挙動を変更し、保留中のアラートがない場合にループ音を途中で切らず最後まで再生するようにした。critical.wav や warning.wav などを追加し、旧 prompt.wav などを削除している。

**直近のアクティビティ:** 作者 elkoled が test_onroad.py の test_camera_sync 失敗（"Cameras don't start on same frame ID"）は無関係の HIL 失敗だとコメント。diff report は全パス。マージ済み。

**変更ファイル:** assets/sounds 配下の wav 複数（追加・変更・削除）と soundd.py（+20/-7）の計 13 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [soundd: add complete sound](https://github.com/commaai/openpilot/pull/38390) `#38390`
**ステータス:** Merged | **ラベル:** なし

**概要:** 複数の UX フローに完了サウンドを追加する PR。log.capnp、complete.wav 新規追加、soundd.py の 3 ファイルを変更（+6/-0）。

**直近のアクティビティ:** diff report が全パスで通過し、マージ済み。

**変更ファイル:** cereal/log.capnp、assets/sounds/complete.wav（新規）、soundd.py。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [profile usb gpu link stability](https://github.com/commaai/openpilot/pull/38368) `#38368`
**ステータス:** Merged | **ラベル:** なし

**概要:** USB3 リンクが列挙直後に不安定になりうるため、SuperSpeed リンクを使う前に PORTLI レジスタのリンクエラーを 2 秒間・毎秒ポーリングする。PORTLI レジスタアクセスは agnos-kernel-sdm845 の PR#125 で実装されており、古い AGNOS でレジスタアクセス未対応の場合はこのステップをスキップする。

**直近のアクティビティ:** 作者 elkoled が CI 失敗は無関係とコメント。commaci-public が model replay プロットを投稿。マージ済み。

**変更ファイル:** modeld の compile_modeld.py、modeld.py、usbgpu_link.py（新規 +34）の 3 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [webrtc: REMB-based livestream bitrate controller](https://github.com/commaai/openpilot/pull/38189) `#38189`
**ステータス:** Closed | **ラベル:** なし

**概要:** LivestreamBitrateController のパケットロスヒューリスティックを、ブラウザの REMB（Receiver Estimated Maximum Bitrate）で駆動するコントローラに置き換える Draft PR。RTCRtpSender をモンキーパッチして送信者ごとの REMB フィードバックを取得し、推定値が現在値を下回るとエンコーダのビットレートを下げ、安定後に再び上げる。

**直近のアクティビティ:** 作者 stefpi が「stale なのでクローズする」とコメントしクローズ。

**変更ファイル:** system/webrtc/webrtcd.py のみ（+57/-42）。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [bump tinygrad](https://github.com/commaai/openpilot/pull/38388) `#38388`
**ステータス:** Closed | **ラベル:** なし

**概要:** tinygrad を更新し、buffer API の変更に合わせて compile_modeld.py を調整する PR。tinygrad_repo と compile_modeld.py の 2 ファイルを変更（+4/-3）。

**直近のアクティビティ:** diff report が全パスで通過。その後クローズ（未マージ）。

**変更ファイル:** modeld/compile_modeld.py、tinygrad_repo。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/commaai/openpilot/pull/38386) `#38386`
**ステータス:** Open | **ラベル:** bot

**概要:** repo-maintenance からの自動 PR で、Python パッケージ（uv.lock）と opendbc/rednose/teleoprtc/tinygrad のサブモジュール参照を更新する。docs/CARS.md も更新。6 ファイル変更（+207/-212）。

**直近のアクティビティ:** commaci-public が model replay プロットを投稿。adeebshihadeh がレビューコメント（本文なし）を残している。

**変更ファイル:** uv.lock（+201/-206）が中心で、各種サブモジュール参照ファイルと docs/CARS.md。

**次のアクション:** レビューとマージ。model replay 結果の確認が必要。

---

### ![PR](icons/git-pull-request.svg) [CI: bump Build devel timeout, it times out on the free runner tier](https://github.com/commaai/openpilot/pull/38286) `#38286`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** `build release` が全 fork PR で失敗する問題への対応。fork PR がルーティングされる ubuntu-24.04 ランナーで `Build devel` ステップが 1 分のタイムアウトに達する（72s で強制終了を 2 回確認、決定論的）。LFS モデルを含むツリー全体をコピーするため近年のツリー肥大化で 1 分を超えたと分析し、タイムアウトを 3 分に引き上げる。

**直近のアクティビティ:** 作者 JPL11 が「以前のプロファイリングで質問に回答済み。追加計測も可能だが、進行中の jenkins reliability 作業に取り込むなら本 PR をクローズしてよい」とフォローアップコメント。

**変更ファイル:** .github/workflows/tests.yaml、tools/release/build_stripped.sh の 2 ファイル（+4/-1）。

**次のアクション:** メンテナが独立してマージするか、#38385 の jenkins 信頼性作業に統合するかの判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Yaw is zero](https://github.com/commaai/openpilot/pull/37825) `#37825`
**ステータス:** Closed | **ラベル:** stale

**概要:** 絶対ヨー方位は GPS を使う ECEF フレームと違い NED では可観測でなく、共分散が長距離ルートで徐々に増大するだけなので安全に無視できるか、という提起。pose KF で追跡する状態（および共分散）の数を減らし、ログの S/N 比を高める。

**直近のアクティビティ:** 非アクティブのため github-actions[bot]により自動クローズ。

**変更ファイル:** locationd.py、pose_kf.py の 2 ファイル（+21/-13）。

**次のアクション:** クローズ済み。活動再開時に再オープン可能。

---

### ![PR](icons/git-pull-request.svg) [2026.05.20 fix compressed_vipc.py by using pyzmq as zmq client](https://github.com/commaai/openpilot/pull/38072) `#38072`
**ステータス:** Open | **ラベル:** tools, stale

**概要:** tools/compressed_vipc.py の実行時エラーを修正する PR。cereal.messaging が内部で使う msgq の C++ 実装 SubSocket が、ゼロ依存化のため zmq_impl が削除されて動作しなくなっていた。SubSocket 依存を外し pyzmq で直接クライアントを作る形に置き換え、デコードロジックは変更していない。watch3.py で 3 カメラ全ての映像表示を確認済み。

**直近のアクティビティ:** github-actions[bot]が「24 日間活動なし、7 日後に自動クローズ」と通知。

**変更ファイル:** tools/camerastream/compressed_vipc.py のみ（+123/-24）。

**次のアクション:** 自動クローズ前にメンテナのレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [ci: run MetaDrive simulator test in GitHub Actions](https://github.com/commaai/openpilot/pull/38242) `#38242`
**ステータス:** Open | **ラベル:** stale

**概要:** Issue #30693 を解決する PR。無料の ubuntu-24.04 ランナーで確実に動くよう CI 専用のシミュレータ経路を強化し、Xvfb 下で test_metadrive_bridge.py を実行する `simulator driving` ジョブを追加。qlog/rlog/カメラファイルをアーティファクトとしてアップロードし、観測された CI フレーク（out_of_lane、modeld クラッシュ再起動、Athena アップロードのリトライタイミングなど）に個別対処。同一コミットで tests.yaml を 20 回連続成功させたと報告。

**直近のアクティビティ:** github-actions[bot]が「24 日間活動なし、7 日後に自動クローズ」と通知。

**変更ファイル:** tests.yaml、tools/sim 配下の metadrive bridge/process/world、setup_dependencies.sh、test_athenad.py など 14 ファイル（+165/-40）。

**次のアクション:** 自動クローズ前にメンテナのレビューが必要。bounty 対象 Issue #30693 に紐づく。

---

### ![PR](icons/git-pull-request.svg) [replace pytest with tiny custom test runner](https://github.com/commaai/openpilot/pull/38383) `#38383`
**ステータス:** Open | **ラベル:** なし

**概要:** pytest を小さな自作テストランナーに置き換える PR。理由は「xdist が遅く脆い」ため。conftest.py（-95）と pyproject.toml を削り、run_tests.py（新規 +238）を追加し、81 ファイルを変更（+1000/-837）。#38387 とは別方針のテスト刷新案。

**直近のアクティビティ:** diff report が全パス（66 件）で通過。人によるレビューコメントはなし。

**変更ファイル:** run_tests.py 新規、conftest.py 削除、common/parameterized.py、test/helpers.py、多数のテストファイルと CI 設定（tests.yaml、Jenkinsfile）など 81 ファイル。

**次のアクション:** unittest 移行案の #38387 とどちらの方針を採るか、メンテナの判断とレビューが必要。

---

### ![Issue](icons/issue-opened.svg) [Run MetaDrive simulation test in GitHub Actions](https://github.com/commaai/openpilot/issues/30693) `#30693`
**ステータス:** Open | **ラベル:** good first issue, CI / testing, bounty

**概要:** MetaDrive シミュレーションテストを GitHub Actions で動かす bounty 付き Issue。要件はオープンソース向け無料 Actions インスタンスで動くこと、20 回以上連続で確実に通ること、実時間で動く（CI 上の 1 分が openpilot の 1 分の走行に相当）こと、ログ（qlog/rlog/カメラ）をアーティファクトとしてアップロードすること。

**直近のアクティビティ:** riajha-usc が「まだ着手可能か、取り組みたい」とコメント。

**次のアクション:** bounty の空き状況の回答と、対応中の PR #38242 との調整が必要。

---

## commaai/opendbc

> car safety 領域の PR が中心で、safety コードの抽象化・整理や pure python math への移行といったリファクタリングが進行中。実車挙動に関する Tesla のバグ報告も1件挙がっている。

### ![PR](icons/git-pull-request.svg) [try MADS](https://github.com/commaai/opendbc/pull/3570) `#3570`
**ステータス:** Open | **ラベル:** car safety

**概要:** MADS の試験的な実装を行う PR。safety 層（hyundai モードや lateral、safety.h など）とテストに変更が入っている。本文の記載はない。

**直近のアクティビティ:** github-actions[bot]による Car behavior report が投稿され、Hyundai 系 260 セグメントすべてで `carParams.steerLimitAlert` と `carParams.deprecated.steerLimitAlert` の値が入れ替わる差分（260 changed）が検出されている。

**変更ファイル:** `opendbc/safety/__init__.py`、`declarations.h`、`lateral.h`、`modes/hyundai.h`、`safety.h`、`safety.c`、libsafety のバインディング、`tests/test_hyundai.py` を変更（+123/-14）。

**次のアクション:** Car behavior report で検出された steerLimitAlert 差分が意図通りかレビューし、確認する。

---

### ![PR](icons/git-pull-request.svg) [abstract safety watchdog reset](https://github.com/commaai/opendbc/pull/3569) `#3569`
**ステータス:** Open | **ラベル:** car safety

**概要:** safety の watchdog reset を抽象化する PR。`declarations.h` と `safety.h`、テスト共通コードと libsafety バインディングに変更が入る。本文の記載はない。

**直近のアクティビティ:** github-actions[bot]の Car behavior report は「No changes detected（挙動変化なし）」を報告。

**変更ファイル:** `opendbc/safety/declarations.h`、`safety.h`、`tests/common.py`、`tests/libsafety/libsafety_py.py` を変更（+24/-2）。

**次のアクション:** 抽象化の内容をレビューしてマージ判断を行う。

---

### ![PR](icons/git-pull-request.svg) [safety: remove inactive is zero in curvature](https://github.com/commaai/opendbc/pull/3568) `#3568`
**ステータス:** Open | **ラベル:** car safety

**概要:** curvature の inactive-is-zero フラグを削除する PR。このフラグは MEB の carcontroller が解除時・作動時に curvature コマンドを実測 curvature にスナップして EPS フォールトを防いでいたために存在していたが、carcontroller の挙動修正後は不要になったと説明されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `opendbc/safety/declarations.h`、`lateral.h`、`modes/ford.h`、`modes/volkswagen_meb.h`、`tests/test_volkswagen_meb.py` を変更（+5/-9）。

**次のアクション:** フラグ削除が Ford / VW MEB の挙動に影響しないかレビューし確認する。

---

### ![Issue](icons/issue-opened.svg) [Tesla: car does not follow light braking command through DAS_control](https://github.com/commaai/opendbc/issues/3567) `#3567`
**ステータス:** Open | **ラベル:** bug, car

**概要:** Tesla で -0.4 m/s^2 から 0 の間の軽い減速コマンドに車両が追従しない事象が散発的に発生するとの報告。openpilot が -0.4 m/s^2 未満を指示して初めて減速に従うという。報告者は `teslacan.py` の `create_longitudinal_command()` が原因ではないかと推測しており、加速エンベロープと set speed（現在速度から目標減速分を引いた値）を用いるため、小さい減速では set speed が現在速度とほとんど変わらず追従しないと分析している。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 該当ルート（c0fe59cc1ebbd1b3/00000005--052ecce665/380/390）で挙動を再現し、`create_longitudinal_command()` の set speed 計算を検証する。

---

### ![PR](icons/git-pull-request.svg) [stick (mostly) to pure python math](https://github.com/commaai/opendbc/pull/3566) `#3566`
**ステータス:** Open | **ラベル:** car, body, ford, gm, honda, hyundai, nissan, subaru, tesla, toyota, volkswagen, rivian, car safety

**概要:** 数学処理を（ほぼ）pure python math に統一するリファクタリング PR。新規に `opendbc/math.py` を追加し、各メーカーの carcontroller や vehicle_model、pid、simple_kalman、conversions など広範囲に変更が及ぶ。本文の記載はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `opendbc/math.py` を新規追加し、body / ford / gm / honda / hyundai / nissan / rivian / subaru / tesla / toyota / volkswagen の各 carcontroller、`vehicle_model.py`、`lateral.py`、`common/pid.py`、`common/simple_kalman.py`、`common/conversions.py`、関連テストなど計 29 ファイルを変更（+306/-195）。

**次のアクション:** 全メーカーに横断する変更のため、各車種の挙動に影響がないか広範にレビューする。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3565) `#3565`
**ステータス:** Merged | **ラベル:** bot

**概要:** uv.lock の依存関係を週次で更新する bot PR。`uv.lock` のみを変更（+86/-86）。

**直近のアクティビティ:** github-actions[bot]の Car behavior report は「No changes detected（挙動変化なし）」を報告。

**変更ファイル:** `uv.lock` のみ変更。

**次のアクション:** 既にマージ済みのため対応不要。

---

## commaai/panda

> 直近24時間は maintainer を中心に、board ファームウェアの機能追加やリファクタリングを行う小規模な PR が3件オープンされた。いずれもコメント・レビューはまだ付いていない。

### ![PR](icons/git-pull-request.svg) [try MADS](https://github.com/commaai/panda/pull/2416) `#2416`
**ステータス:** Open | **ラベル:** なし

**概要:** MADS を試験的に導入する PR。`board/health.h`、`board/main.c`、`board/main_comms.h`、`python/__init__.py` の 4 ファイルに合計 8 行を追加する小規模な変更。本文の説明はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** board 側の health / main / main_comms と python の `__init__.py` を修正し、いずれも追加のみ(削除なし)。

**次のアクション:** 変更内容が未説明のため、MADS の目的と実装意図の確認、およびレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [abstract safety watchdog reset](https://github.com/commaai/panda/pull/2415) `#2415`
**ステータス:** Open | **ラベル:** なし

**概要:** safety watchdog reset の処理を抽象化するリファクタリング PR。`board/main.c` のみを対象に 1 行追加・9 行削除と、コードを整理する変更。本文の説明はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `board/main.c` のみを修正(+1 / -9)。

**次のアクション:** 抽象化後の挙動が従来と等価であることの確認とレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [add digital temperature sensor to health packet](https://github.com/commaai/panda/pull/2414) `#2414`
**ステータス:** Open | **ラベル:** なし

**概要:** digital temperature sensor (DTS) の情報を health packet に追加する PR。新規ファイル `board/stm32h7/lldts.h`(36 行)を含む 7 ファイルに合計 48 行を追加する、この日で最大規模の機能追加。本文の説明はない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `board/stm32h7/lldts.h` を新規追加し、`board/health.h`、`board/main.c`、`board/main_comms.h`、`board/stm32h7/board.h`、`board/sys/power_saving.h`、`python/__init__.py` を修正。いずれも追加のみ。

**次のアクション:** 新規 lldts ドライバの実装と power_saving への影響のレビュー、および health packet 変更に伴う python 側との整合性確認が必要。

---

## neovim/neovim

> 直近24時間は ui2 のメッセージ表示・フロートウィンドウ描画・LSP capability まわりの修正が活発で、tree-sitter diff パーサーのバンドルとハイライトクエリ導入をめぐる設計議論、ウィンドウ close 時の UAF クラッシュ修正なども進行中。vim-patch の取り込みやテスト基盤・CI の安定化 PR も多数含まれる。

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0812: :argdelete with pattern leads to wrong argidx()](https://github.com/neovim/neovim/pull/40876) `#40876`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** `:argdelete` をパターン付きで実行すると `argidx()` の値が誤る問題を修正する vim-patch の取り込み。`ex_argdelete()` の両分岐で `argidx()` を正しく補正し、あわせて 2 箇所で `ARGCOUNT` マクロを使うようにする。Issue #40564 の修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/arglist.c` の argidx 補正ロジックと、`test/old/testdir/test_arglist.vim` のテストを変更。

**次のアクション:** レビューとマージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Intro logo is misaligned](https://github.com/neovim/neovim/issues/40875) `#40875`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows / WSL の既定等幅フォントである Cascadia Code と Consolas でイントロロゴ（起動画面のロゴ）が崩れて表示されるという報告。フォントサイズ 22 で確認しているが、すべてのフォントサイズで再現するとのこと。`v0.13.0-dev-1061+g01c4d` で確認。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 影響範囲の切り分けとロゴのアライメント調整の検討。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40855) `#40855`
**ステータス:** Open（ドラフト）| **ラベル:** vim-patch

**概要:** `vim-patch:9.2.0807: MS-Windows: ellipsis character is garbled` を含む version.c 更新のドラフト。`src/nvim/version.c` のみを変更。

**直近のアクティビティ:** zeertzjq と janlazo が、メモリリーク修正やポップアップウィンドウ関連など、追加で取り込むべき vim-patch 番号のリストをコメントで列挙している。

**変更ファイル:** `src/nvim/version.c` のみ。

**次のアクション:** 列挙された N/A パッチや関連パッチの整理を経てドラフトを仕上げる。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): support textDocumentContent](https://github.com/neovim/neovim/pull/39592) `#39592`
**ステータス:** Open（ドラフト）| **ラベル:** lsp, AI assisted 🤖

**概要:** LSP サーバーが `workspace/textDocumentContent` で提供する仮想ドキュメント内容を Nvim が広告・処理できるようにする機能追加。対応する URI スキームに対して `BufReadCmd` ハンドラを登録し、該当クライアントに内容を要求、リフレッシュ通知にも対応する。#39574 にブロックされ、#39526 をクローズ予定。

**直近のアクティビティ:** sebastianstudniczek が roslyn での `textDocument/rename` の問題に触れたが、実装を見直した結果 neovim 側の問題ではないと自己解決。mfussenegger・tris203 らからレビューコメントあり。

**変更ファイル:** `runtime/lua/vim/lsp.lua`、新規 `text_document_content.lua`、`_capability.lua`、`client.lua`、`handlers.lua`、`protocol.lua`、ドキュメント `lsp.txt`、テストを追加・変更。

**次のアクション:** ブロッカー #39574 の解消と、レビュー指摘の反映。

---

### ![PR](icons/git-pull-request.svg) [feat(text): support `opts.prefix` in `vim.text.indent()`](https://github.com/neovim/neovim/pull/40874) `#40874`
**ステータス:** Open | **ラベル:** なし

**概要:** `vim.text.indent()` に、Python の `textwrap` に着想を得た各行への文字列前置を行う TODO が残っていたのを実装。`opts.prefix` オプションを追加し、インデント後に各非空行へ指定文字列を前置する。

**直近のアクティビティ:** MariaSolOs が「news に載せるほどの内容ではないと思うが、異論があれば」と @justinmk に確認している。

**変更ファイル:** `runtime/lua/vim/text.lua` に実装、`runtime/doc/lua.txt` のドキュメントと `text_spec.lua` のテストを追加。

**次のアクション:** news 記載要否の判断とレビュー。

---

### ![Issue](icons/issue-opened.svg) [Windows: spawn() is broken with 'shellslash'](https://github.com/neovim/neovim/issues/40872) `#40872`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows + PowerShell 環境で `shellslash` が有効なとき `spawn()` が壊れているという報告。`exepath` が shellslash に従い前方スラッシュのパスを返すが、`vim.system` が Windows で使う `CreateProcess` は前方スラッシュを受け付けず、`vim.system({'cmd.exe', ...})` が構文エラーになる。

**直近のアクティビティ:** jbsolomon が、これは PR #39155 の API 変更が原因だと考えられ、spawn は絶対ネイティブパスを使うべきだと補足している。

**次のアクション:** spawn 時のパスをネイティブ表記へ変換する対応の検討。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): preserve Visual mode messages](https://github.com/neovim/neovim/pull/40828) `#40828`
**ステータス:** Merged | **ラベル:** ui2

**概要:** ビジュアルモードからの cmd ターゲットメッセージが `msg_showmode` の extmark にすぐ覆われてしまう問題を修正。ビジュアルモードで発行されるメッセージを既存の「important」メッセージ遅延で保護する。#40824 をクローズ。

**直近のアクティビティ:** justinmk と barrettruth から計 8 件のレビューコメントが付き、議論を経てマージされた。

**変更ファイル:** `runtime/lua/vim/_core/ui2/messages.lua` と `messages2_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [bug: visual counter v_g_CTRL-g does not display in ui2 messages bar](https://github.com/neovim/neovim/issues/40824) `#40824`
**ステータス:** Closed | **ラベル:** statusline, messages, visual-mode, ui2

**概要:** ビジュアルモードの単語カウント `v_g_CTRL-g` の結果が新しい `ui2` では表示されず、`:messages` にログされるだけで従来 `ui` と挙動が異なるという報告。#36447 の ui/ui2 間パリティ向上の観点から、editing 系の他コマンドも確認する価値があるかもしれないと指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40828 のマージによりクローズ）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(ui): handle combining chunked float titles](https://github.com/neovim/neovim/pull/40873) `#40873`
**ステータス:** Open | **ラベル:** ui

**概要:** #40796 のフォローアップ。フロートタイトルがウィンドウ属性と結合されるのは文字列タイトル（`FloatTitle`/`FloatFooter` ハイライトを含意）の場合のみで、text-hl チャンクの場合には結合されない問題を修正。指定ハイライトグループをウィンドウローカルの Normal ハイライトとも結合する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/grid.c` と `float_spec.lua` を変更。

**次のアクション:** レビューとマージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Window path separator (slash) handling](https://github.com/neovim/neovim/issues/39382) `#39382`
**ステータス:** Open | **ラベル:** platform:windows, environment, system, filesystem

**概要:** Windows のパス処理が一貫せずバグがあるという課題。内部では常に `/`（前方スラッシュ）を使い、Windows では特に見た目上の用途（バッファ名・statusline・quickfix 等）では `/` を既定とし、非見た目用途でユーザーが明示的に `\` を期待する限定的な機能でのみ `\` に変換するという方針を提示。

**直近のアクティビティ:** justinmk が Windows ユーザーからのフィードバックに感謝しつつ、jbsolomon のコメントを別 Issue で追跡すると述べた。jbsolomon は `shellslash` 有効時の `spawn()` 破損（#40872 として切り出し）を報告。

**次のアクション:** 切り出された個別課題（#40872 など）ごとの対応と方針の段階的実装。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): expose row continuation state](https://github.com/neovim/neovim/pull/40859) `#40859`
**ステータス:** Closed（ドラフト）| **ラベル:** terminal

**概要:** ターミナルバッファの行が新しい論理行の開始かソフトラップの継続かを公開する POC 実装。ターミナルコマンドエディタがカーソル以降のコマンド継続行とシェル補完出力を区別できるようにすることを狙う（#23645 関連）。

**直近のアクティビティ:** justinmk と kalomak が設計を議論。継続状態が内部にあるなら `:terminal` 側でソフトラップすればよいのでは、libghostty があればこのメタデータは不要では、といった疑問が justinmk から提起され、PR の意図が不明瞭との指摘。作者はドラフトへの反応に恐縮しつつ設計意図を説明。

**変更ファイル:** `src/nvim/terminal.c`、`src/nvim/api/vim.c`、`api.txt`、`api.gen.lua`、`buffer_spec.lua` を変更。

**次のアクション:** クローズ済み。設計方針の再検討が必要。

---

### ![PR](icons/git-pull-request.svg) [test(harness): migrate away from magic globals](https://github.com/neovim/neovim/pull/40865) `#40865`
**ステータス:** Open | **ラベル:** test

**概要:** テストで使われる `it`・`describe` などのマジックグローバルを廃し、`testutil` から `t.it()`・`t.describe()` の形で明示的に呼び出せるようにする大規模移行。luals/emmylua の混乱、発見性の低下、`getfenv()` ハックの必要性といった DX 上の問題を解消する。

**直近のアクティビティ:** justinmk から 2 件のレビューコメント。ユーザーコメントは直近 24 時間なし。

**変更ファイル:** 902 行追加・268 行削除で、`.emmyrc.json`・`.luacheckrc`・`test/assert.lua` ほか多数の `*_spec.lua`（benchmark、functional/api 等）を横断的に変更。

**次のアクション:** 広範な変更のためレビューと段階的な合意形成。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.net): add response status code and headers in on_response callback](https://github.com/neovim/neovim/pull/39685) `#39685`
**ステータス:** Open | **ラベル:** なし

**概要:** #38946 の継続で、`on_response` コールバックに `status` と `headers` を追加し、HTTP ステータスコード用の `vim.net.status` enum を新設、テストも追加する。

**直近のアクティビティ:** ellisonleao と justinmk の間で計 19 件のレビューコメントによる活発なやり取り。関連する #40870 では、この PR の機能テストが httpbingo.org など外部プロバイダに強く依存する点への懸念が共有されている。

**変更ファイル:** `runtime/lua/vim/net.lua`、`lua.txt`、`news.txt`、`net_spec.lua` を変更。

**次のアクション:** レビュー指摘の反映と、外部プロバイダ依存のテスト耐障害性の検討。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): save state file with `:write`](https://github.com/neovim/neovim/pull/40097) `#40097`
**ステータス:** Open | **ラベル:** terminal, gsoc

**概要:** ターミナルプロセス終了後に失われるターミナルバッファ内容とメタデータを保存する機能。`TermOpen` autocmd でバッファごとの `BufWriteCmd` を登録し、`:write` で `term_getansi()` により ANSI をエクスポートして `stdpath('state')/term/<name>.mpack` 等に保存する。

**直近のアクティビティ:** Willaaaaaaa と justinmk から計 57 件という多数のレビューコメントが付いており、精力的にレビューが進行中。直近 24 時間のユーザーコメントはなし。

**変更ファイル:** 760 行追加・86 行削除。`src/nvim/terminal.c`、新規 `terminal_encode.c/.h`、`bufwrite.c`、`ex_cmds.c`、Lua 側 `_core/terminal.lua`、ドキュメント・テスト多数を変更。

**次のアクション:** GSoC の大規模機能として引き続きレビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix(diagnostic): don't accumulate BufRead autocmds for unloaded buffers](https://github.com/neovim/neovim/pull/40869) `#40869`
**ステータス:** Open | **ラベル:** diagnostic

**概要:** #40802 の修正。`vim.diagnostic.set()` が未ロードバッファに対し呼び出しごとに `once=true` の `BufRead` autocmd を重複登録し診断データを保持し続ける問題を、キャッシュから位置を計算する単一の静的 `BufRead` autocmd に置き換えることで解消し、蓄積そのものをなくす。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/diagnostic/_store.lua` と `diagnostic_spec.lua` を変更。

**次のアクション:** レビューとマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): position message windows without autocmds](https://github.com/neovim/neovim/pull/40867) `#40867`
**ステータス:** Merged | **ラベル:** ui2

**概要:** メッセージウィンドウの位置決めを autocmd を使わずに行うよう変更。#40788、#40780 に関連し、#40788 のレビューコメントを受けた対応。

**直近のアクティビティ:** luukvbaal が LGTM、barrettruth が luukvbaal の確認を待とうと述べ、justinmk が APPROVE してマージされた。

**変更ファイル:** `runtime/lua/vim/_core/ui2/messages.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [unreliable test: vim.net.request fetches a URL into memory (async success)](https://github.com/neovim/neovim/issues/40870) `#40870`
**ステータス:** Open | **ラベル:** test, ci, network

**概要:** `net_spec.lua` の「vim.net.request fetches a URL into memory (async success)」テストが `attempt to index local 'result' (a nil value)` で失敗する不安定テストの報告。

**直近のアクティビティ:** justinmk はプロバイダ障害だと分かるよう `result` が nil のときのエラーメッセージを改善すれば十分と提案。ellisonleao は #39685 の機能テストが httpbingo.org など外部プロバイダに依存する点を挙げ、別プロバイダへのフォールバックなど耐障害層の追加を相談。

**次のアクション:** エラーメッセージ改善と外部プロバイダ依存の緩和策の検討。

---

### ![PR](icons/git-pull-request.svg) [build: raise `preloadFileSize` in `src/.luarc.json` to match root config](https://github.com/neovim/neovim/pull/40868) `#40868`
**ステータス:** Merged | **ラベル:** build

**概要:** `src/.luarc.json` が `workspace.preloadFileSize` を設定しておらず lua_ls の既定 500KB が使われるため、最近 510KB 程度に肥大化した `src/nvim/eval.lua` がプリロードされず `Too large file` 通知が出ていた。`"preloadFileSize": 1000` を追加してルート設定に揃える。

**直近のアクティビティ:** barrettruth がコメントし、barrettruth と justinmk が APPROVE してマージ。

**変更ファイル:** `src/.luarc.json` に 1 行追加。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): clear showcmd after mapping timeout](https://github.com/neovim/neovim/pull/40871) `#40871`
**ステータス:** Open | **ラベル:** ui2

**概要:** #40856 の修正で、マッピングのタイムアウト後に ui2 の `showcmd` 表示を消去する。

**直近のアクティビティ:** barrettruth が @phanen に「これで動くか」と確認している。

**変更ファイル:** `runtime/lua/vim/_core/ui2.lua` と `messages2_spec.lua` を変更。

**次のアクション:** 報告者による動作確認とレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): highlight code inside diff hunks](https://github.com/neovim/neovim/pull/40851) `#40851`
**ステータス:** Open（ドラフト）| **ラベル:** treesitter

**概要:** バンドルされた diff パーサーにランタイムクエリがなく、diff バッファで tree-sitter ハイライトを開始しても構造やハンク内コードがハイライトされない問題に対し、ファイルヘッダからパーサーを解決する diff ハイライトクエリとインジェクションを追加。新ハイライトグループ `DiffLine{Add,Delete}` を追加し、diff バッファでの自動有効化はせず有効化手段のみ文書化する。

**直近のアクティビティ:** clason が「クエリは nvim-treesitter 側で管理したい、そちらをソースオブトゥルースにしたい」と主張し `CHANGES_REQUESTED`（ブロッキング）。この変更（既マージ分含む）は下流への影響が大きく、nvim-treesitter 側の対応も必要でシャドーイングの問題があるため、マージ前に十分検討したいとした。ハイライトグループ変更自体はこのリポジトリが正しいことも確認。ambroisie がインジェクション無効化の文書化を要望。

**変更ファイル:** `runtime/queries/diff/highlights.scm`・`injections.scm`、`src/nvim/highlight_group.c`、`syntax.txt`・`treesitter.txt`・`news.txt`、`diff_spec.lua` を変更。

**次のアクション:** clason のブロッキング懸念（nvim-treesitter との整合、下流影響）を解消してから進める。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): add queries for bundled diff parser](https://github.com/neovim/neovim/pull/40866) `#40866`
**ステータス:** Open | **ラベル:** treesitter

**概要:** バンドルされた diff パーサー向けのクエリ（`folds.scm`・`highlights.scm`）を、#40851 の直近の調整前の「枯れた」バージョンとして追加する提案。作者は「これらは実績があるはず」と述べ、そうでなければクローズしてよいとしている。

**直近のアクティビティ:** barrettruth がフィードバックを受けてコメントインジェクションとテストを（当面）削除。clason から 2 件のレビューコメント。

**変更ファイル:** `runtime/queries/diff/folds.scm`・`highlights.scm`、`treesitter.txt` を追加・変更。

**次のアクション:** #40851 の議論と連動した方針決定とレビュー。

---

### ![PR](icons/git-pull-request.svg) [test: unreliable "CursorHold not triggered after only K_EVENT on startup"](https://github.com/neovim/neovim/pull/40863) `#40863`
**ステータス:** Merged | **ラベル:** test

**概要:** 起動時に `K_EVENT` のみの後に `CursorHold` が発火しない不安定テストを、`sleep(50)` のハードコードではなくリトライ方式に変更して安定化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test/functional/autocmd/cursorhold_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): rework capabilities enable()/is_enabled()](https://github.com/neovim/neovim/pull/40816) `#40816`
**ステータス:** Open | **ラベル:** lsp

**概要:** LS 停止時に codelens（LuaLS の参照数など）がクリアされない問題を発端に、capability の `enable()`/`is_enabled()` を再設計。`is_enabled` がグローバルまたは特定バッファで有効化されていない限り常に false を返す挙動を見直す。

**直近のアクティビティ:** justinmk と sagg0t の間で「enabled」対「allowed」の用語区別や、バッファ/ウィンドウ/LSP クライアントといった「現在のコンテキスト」の扱いをめぐる設計議論が計 4 件。justinmk は関連 Issue #28603、#38293 を挙げ、繊細で要注意な変更だとした。

**変更ファイル:** `runtime/lua/vim/lsp/_capability.lua`、`_folding_range.lua`、`client.lua`、`dev_arch.txt`、複数の LSP テストを変更（474 行追加）。

**次のアクション:** 用語・コンテキスト設計の合意と @jdrouhard の作業との調整。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(lsp): incorrect rendering of markdown codeblock](https://github.com/neovim/neovim/pull/40862) `#40862`
**ステータス:** Merged | **ラベル:** lsp, target:release

**概要:** #40861 を `release-0.12` にバックポートしたもの。LSP hover で 4 スペースインデントのコードブロック直前の空行が誤って削除される問題を修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/lsp/util.lua` と `util_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): vim.iter totable() handles packed multivalues and advances iterator state](https://github.com/neovim/neovim/pull/40134) `#40134`
**ステータス:** Open | **ラベル:** lua, AI assisted 🤖

**概要:** #39705 で報告された、配列に対する `vim.iter()` のパック済み多値の扱いの 2 つの不具合を修正。`IterArray:totable()` が先頭要素しかパックメタテーブルを確認しない問題と、`totable()` が配列イテレータを消費した後に head/tail が進まない問題を修正する。

**直近のアクティビティ:** 作者 mvanhorn が LLM 出力の混入を謝罪し、テストの describe/it 分割や識別子リネームなど指摘を反映。gpanders は APPROVE しつつ、PR に LLM 出力をコピペするのは失礼だと苦言を呈した（barrettruth・gpanders 計 9 件のレビュー）。

**変更ファイル:** `runtime/lua/vim/iter.lua`、`news.txt`、`iter_spec.lua` を変更。

**次のアクション:** 残りのレビューコメント対応後にマージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): incorrect rendering of markdown codeblock](https://github.com/neovim/neovim/pull/40861) `#40861`
**ステータス:** Merged | **ラベル:** lsp, ci:backport release-0.12

**概要:** LSP hover が 4 スペースインデントのコードブロック直前の空行を誤って落とし（不正な Markdown となり）解析・表示が崩れる問題を、`split_lines` を修正して該当空行を落とさないようにする。#40860 の修正。

**直近のアクティビティ:** neovim-backports[bot]が `release-0.12` 向けバックポート PR #40862 の作成成功を報告。

**変更ファイル:** `runtime/lua/vim/lsp/util.lua` と `util_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Hover markdown from LSP interprets [ and ] in indented code blocks as links](https://github.com/neovim/neovim/issues/40860) `#40860`
**ステータス:** Closed | **ラベル:** lsp

**概要:** インデントされたコードブロック内の `[` と `]` を含む Markdown が hover で誤って（リンクとして）レンダリングされる報告。

**直近のアクティビティ:** justinmk が `:InspectTree` では正しく解析されるが、空行を除くと再現すると調査し、hover で空行が削除されるのは自分の過去コミットが原因だと認めた（#40861 でクローズ）。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [logging: nvim_log(), other improvements](https://github.com/neovim/neovim/pull/7062) `#7062`
**ステータス:** Open（ドラフト）| **ラベル:** api, core, logging

**概要:** `nvim_log(level, message)` API 関数の追加をはじめ、ロギング全般の改善を進める長期ドラフト。API クライアントの `nvim_log()` 利用への更新、`stdpath('log')`、`logf()` VimL 関数などのチェックリストを含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に blueyed・BK1603・justinmk のレビューコメントあり）。

**変更ファイル:** `src/nvim/log.c/.h`、`src/nvim/api/vim.c`、`options.lua` ほか多数のコアファイルとドキュメント・テストを変更（446 行追加）。

**次のアクション:** 未完タスクの整理と方針の再確認。

---

### ![PR](icons/git-pull-request.svg) [feat(iter): add `Iter:count()`](https://github.com/neovim/neovim/pull/40831) `#40831`
**ステータス:** Merged | **ラベル:** なし

**概要:** イテレータの要素数を数えるのに一旦テーブル化して長さを取る必要があった非効率を解消するため、`vim.iter():count()` を追加。

**直近のアクティビティ:** cpea2506 が `count()` と `len()` の意味区別や配列バックのイテレータ向け `len()` 実装について議論。justinmk は型定義更新の必要性を指摘し、関連する過去議論 #23233 の確認を求めた（計 8 件のレビュー、superatomic が APPROVE）。

**変更ファイル:** `runtime/lua/vim/iter.lua`、`_meta.lua`、`lua.txt`、`news.txt`、`iter_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [feat(log): buffered, async writter, based on `string.buffer` and `vim.uv`](https://github.com/neovim/neovim/pull/39735) `#39735`
**ステータス:** Open（ドラフト）| **ラベル:** logging

**概要:** `string.buffer` と `vim.uv` を使ったバッファリング付き非同期ログ書き込みの実装（ドラフト）。

**直近のアクティビティ:** 直近 24 時間のコメントなし（過去に justinmk・ofseed のレビューコメントあり）。

**変更ファイル:** `runtime/lua/vim/log.lua`、`lua.txt`、`log_spec.lua`、`lsp_spec.lua`、`testnvim.lua` を変更。

**次のアクション:** ロギング設計（#7062 等）との整合を取りつつドラフトを進める。

---

### ![PR](icons/git-pull-request.svg) [refactor(log)!: extract log sink as an interface](https://github.com/neovim/neovim/pull/39750) `#39750`
**ステータス:** Closed | **ラベル:** refactor, lsp, logging, breaking-change

**概要:** `vim.log` を複数のシンク（ファイル以外も）に対応させるため、`vim.log.Sink` をインターフェースとして抽出し `vim.log.FileSink` を提供するリファクタ（破壊的変更）。

**直近のアクティビティ:** justinmk が「イベント（autocmd）駆動であるべきで、この形は正しくない」と判断してクローズ。ただし `nvim_echo` が適切なインターフェースかは検討中とした。

**変更ファイル:** `runtime/lua/vim/log.lua`、`lsp/log.lua`、`lua.txt`、`log_spec.lua` を変更。

**次のアクション:** クローズ済み。イベント駆動の設計として再検討。

---

### ![Issue](icons/issue-opened.svg) [ci: Windows test job sometimes fails](https://github.com/neovim/neovim/issues/40840) `#40840`
**ステータス:** Open | **ラベル:** test, ci

**概要:** Windows CI がランダムに失敗し、ある時点から全テストが「nvim crashed」を報告し始める。特定のテストに起因せず、任意の地点で発生する。

**直近のアクティビティ:** justinmk が PR #40841 後の最新の失敗ログを共有し、`python3 provider does not truncate error message <1 MB` テストでの新たな失敗事例を提示している。

**次のアクション:** クラッシュの根本原因の切り分け。

---

### ![Issue](icons/issue-opened.svg) [LSP: omnifunc (clangd) unexpectedly deletes "->" when completeopt contains "longest"](https://github.com/neovim/neovim/issues/39001) `#39001`
**ステータス:** Open | **ラベル:** lsp, completion

**概要:** 0.12.0 以降、C++ コードで clangd による LSP omni 補完が特定の文脈で `->` を予期せず削除する。作者は bisect で最初の不良コミットを #37491 の 36db6ff と特定。

**直近のアクティビティ:** singhgaurav1302 が別の `completeopt` 設定でも本問題が起きるが、`editsNearCursor = false` で回避できると報告。

**次のアクション:** #37491 の変更に関連する補完編集ロジックの調査。

---

### ![PR](icons/git-pull-request.svg) [cmdwin: *dark ritual*](https://github.com/neovim/neovim/pull/40463) `#40463`
**ステータス:** Open（ドラフト）| **ラベル:** terminal, multiproc, DO NOT MERGE, async

**概要:** #40407 のコメントの実装で、cmdline-window 関連の長年の Issue/PR（#1496、#10842、#22374、#12103、#1716、#6233）を一挙にクローズする大規模な実験的変更。

**直近のアクティビティ:** bfredl が画像付きで APPROVE。ユーザーコメントは直近 24 時間なし。

**変更ファイル:** 1216 行追加・71 行削除。`src/nvim/ex_getln.c`、`ex_docmd.c`、`channel.c`、`os/pty_proc_unix.c`、Lua 側 `_core/cmdwin.lua`・`run_in_terminal.lua`、多数のドキュメント・テストを横断的に変更。

**次のアクション:** `DO NOT MERGE` ラベル付きの実験段階。設計の詰めが必要。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): avoid `ModeChanged` in message window](https://github.com/neovim/neovim/pull/40788) `#40788`
**ステータス:** Merged | **ラベル:** ui2

**概要:** メッセージウィンドウ内で `ModeChanged` が発火するのを避ける修正。#40780 をクローズ。

**直近のアクティビティ:** luukvbaal からレビューコメントあり。作者が @stg73 に動作確認を依頼。

**変更ファイル:** `runtime/lua/vim/_core/ui2/messages.lua` と `messages2_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(ui): combine float border with window background](https://github.com/neovim/neovim/pull/40796) `#40796`
**ステータス:** Merged | **ラベル:** ui, floatwin

**概要:** フロートの境界ハイライトグループ（`FloatBorder`・`FloatTitle`・`FloatFooter`）が背景色未設定時に Normal 背景ハイライトにフォールバックする問題を、境界色をウィンドウローカルの Normal ハイライトと結合することで修正。#38330 の修正。

**直近のアクティビティ:** echasnovski が「これはリンク先の問題を部分的にしか解決しておらず、タイトルが文字列の場合のみ結合される」と指摘（→フォローアップ #40873 で対応）。justinmk が APPROVE してマージ。

**変更ファイル:** 752 行追加・735 行削除。`src/nvim/drawscreen.c`、`grid.c`、`popupmenu.c`、複数の UI テストを変更。

**次のアクション:** マージ済み。残課題は #40873 で対応中。

---

### ![PR](icons/git-pull-request.svg) [feat(float): 'previewpopup' option](https://github.com/neovim/neovim/pull/26799) `#26799`
**ステータス:** Open | **ラベル:** floatwin

**概要:** Vim の `'previewpopup'` オプションを移植し、`:pedit`・`:ptag`・`:psearch` のプレビューを分割ではなくフロートウィンドウで表示する。値は `height:N,width:N,border:style` 形式で、border は `'winborder'` スタイルに従う。#9920 の修正。

**直近のアクティビティ:** 直近 24 時間のユーザーコメントはなし（justinmk から計 51 件の多数のレビューコメントが蓄積）。

**変更ファイル:** 1043 行追加。`src/nvim/winfloat.c`、`popupmenu.c`、`option_vars.h`、`options.lua`、ドキュメント・テスト多数を変更。

**次のアクション:** 蓄積したレビュー指摘の反映を継続。

---

### ![PR](icons/git-pull-request.svg) [docs(news): mention bundled diff parser](https://github.com/neovim/neovim/pull/40848) `#40848`
**ステータス:** Open | **ラベル:** documentation, typo

**概要:** tsdiff パーサーのバンドルを news エントリに追記する（#40778 のコメント参照）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/news.txt` に 1 行追加。

**次のアクション:** レビューとマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(diagnostic): skip out-of-range lnum in underline handler](https://github.com/neovim/neovim/pull/40839) `#40839`
**ステータス:** Merged | **ラベル:** diagnostic, AI assisted 🤖

**概要:** `vim.diagnostic.handlers.underline.show` が、バッファ末尾を超えた `lnum` を持つ診断で `E565: Index out of bounds` を投げる問題を修正。`bufadd()` で未ロードのバッファに設定された古い診断を後からロード時に描画する際に発生する。範囲外の `lnum` を underline ハンドラでスキップする。

**直近のアクティビティ:** justinmk が、同ファイルの `virtual_text.show()`・`signs.show()` など類似ハンドラも同じ扱いをしている点を挙げつつ LGTM とし、今後は PR で類似箇所を指摘してほしいと述べた。

**変更ファイル:** `runtime/lua/vim/diagnostic/_handlers.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [vim.diagnostic.set() leaks BufRead autocmds (and retained diagnostics) for unloaded buffers](https://github.com/neovim/neovim/issues/40802) `#40802`
**ステータス:** Open | **ラベル:** diagnostic

**概要:** 未ロードバッファへの `vim.diagnostic.set()` 呼び出しごとに `once=true BufRead` autocmd が重複登録され、各クロージャが診断ペイロードを保持するため、バッファが読み込まれなければセッション中蓄積し続ける。rust-analyzer での長時間セッションで数万の pending autocmd と数 GB の RAM 消費に至ると報告。

**直近のアクティビティ:** justinmk・nat-e・faergeek が、バッファごとに 1 つではなく常設のグローバル autocmd を 1 つ置き同ロジックにする方針で合意しつつある。

**次のアクション:** 常設グローバル autocmd 方式での修正 PR（→ #40869）。

---

### ![Issue](icons/issue-opened.svg) [Lua: more vim.iter functions](https://github.com/neovim/neovim/issues/23233) `#23233`
**ステータス:** Open | **ラベル:** lua

**概要:** `vim.iter()` に追加すべき関連関数を追跡するトラッキング Issue。`fold`（reduce）や `count`（#40831）は済み、テーブル管理系（clone、keys、values など）が残る。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 未実装関数の優先度付けと実装。

---

### ![PR](icons/git-pull-request.svg) [refactor(window): extract w_kind for window role](https://github.com/neovim/neovim/pull/40830) `#40830`
**ステータス:** Merged | **ラベル:** refactor, window

**概要:** ウィンドウの役割とフロートレイアウトという直交する性質を混同していた `w_float_is_info` を、役割を表す `w_kind` enum に置き換えるリファクタ。

**直近のアクティビティ:** glepnir が previewpopup PR での用途を説明。justinmk が `win_float_find_preview` が「info window」を取得している点に触れ、「info」に用語を統一しランダムな同義語を避けるべきと指摘。

**変更ファイル:** `src/nvim/buffer_defs.h`、`window.c`、`winfloat.c`、`popupmenu.c`、`mouse.c` を変更。

**次のアクション:** マージ済み。命名統一のフォローが望ましい。

---

### ![Issue](icons/issue-opened.svg) [ui2: `"showcmd"` don't disappear after timeoutlen like legacy ui](https://github.com/neovim/neovim/issues/40856) `#40856`
**ステータス:** Open | **ラベル:** ui, display, messages, ui2

**概要:** ui2 で `<space>` を押したとき `showcmd` が `timeoutlen` 経過後（`timeoutlen=0` なら表示されない）にレガシー ui のように消えない問題。`v0.13.0-dev-1062` で確認。

**直近のアクティビティ:** justinmk が #40747 起因の回帰かと bisect を提案。phanen は回帰ではないようだと回答（→ #40871 で対応）。

**次のアクション:** #40871 の修正での解消確認。

---

### ![Issue](icons/issue-opened.svg) ["modifiable" terminal emulator (i.e., enable normal mode commands in terminal buffers)](https://github.com/neovim/neovim/issues/23645) `#23645`
**ステータス:** Open | **ラベル:** terminal

**概要:** Nvim ターミナルで normal モードコマンドが効く完全な「変更可能」モードを求める提案。Emacs のターミナルのように、シェルプロセスとバッファを「分離」する実装が考えられる。

**直近のアクティビティ:** kalomak が、シェルから行を取得しそれを更新/上書きするアプローチが最も無難であり、ctrl-u アプローチは「素朴な」解だとコメント。

**次のアクション:** 実装アプローチの合意（#40859 の POC 議論と関連）。

---

### ![Issue](icons/issue-opened.svg) [vim.diagnostic.handlers.underline.show throws "Index out of bounds" (E565) for stale diagnostics with lnum past end of buffer](https://github.com/neovim/neovim/issues/40838) `#40838`
**ステータス:** Closed | **ラベル:** なし

**概要:** 未ロードバッファにファイル末尾を超えた `lnum` で診断が設定されていると、後でバッファをロードした際に underline ハンドラがクラッシュする。`nvim_buf_get_lines` を `strict_indexing=true` で呼ぶが `lnum` をクランプしていないのが原因。

**直近のアクティビティ:** 直近 24 時間のコメントなし（#40839 でクローズ）。

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [default mapping to convert floatwin to splitwin](https://github.com/neovim/neovim/issues/36659) `#36659`
**ステータス:** Closed | **ラベル:** complexity:low, has:workaround, lsp

**概要:** signature_help/hover ウィンドウを自動クローズさせず分割ウィンドウ化して常駐させたいという要望。

**直近のアクティビティ:** justinmk が、新しいマッピングは不要で `<c-w>H` でフロートを分割へ変換できると回答。#40770 が一部を修正したとしてクローズ。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(docs): correct multi-level list dedent indentation](https://github.com/neovim/neovim/pull/40850) `#40850`
**ステータス:** Merged | **ラベル:** なし

**概要:** `gen_help_html.lua` のリスト項目インデント処理が、複数レベルを一度にデデントすると左マージンを誤る問題を、各インデントレベルの正確な先頭空白を `opt.indent_ws` スタックで追跡することで修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/gen/gen_help_html.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): highlight code inside diff hunks](https://github.com/neovim/neovim/pull/40225) `#40225`
**ステータス:** Closed（ドラフト）| **ラベル:** treesitter

**概要:** diff ハンク内コードをハイライトする初期実装（`runtime/lua/nvim/diffhl.lua` ベース）。AI コーディング時代に diff 表示の重要性が増していること、コミュニティの関心の高さ、diff 表示ロジックの重複削減を動機とする。

**直近のアクティビティ:** justinmk が「より良いアプローチが見つかった」と関係者に感謝。barrettruth も diff パーサーのバンドルによりこの方式は今後の進め方ではないとしてクローズ（barrettruth・clason・zeertzjq・ribru17 計 12 件のレビュー）。

**変更ファイル:** `runtime/ftplugin/diff.lua`、`runtime/lua/nvim/diffhl.lua`、`plugins.txt`、`news.txt`、`diffhl_spec.lua` を変更。

**次のアクション:** クローズ済み。後継 #40851/#40866 で対応中。

---

### ![PR](icons/git-pull-request.svg) [test(api): normalize context comparisons](https://github.com/neovim/neovim/pull/40854) `#40854`
**ステータス:** Merged | **ラベル:** api, test

**概要:** `version_spec.lua` のコンテキスト比較を正規化して CI エラーを修正。

**直近のアクティビティ:** justinmk が、これらのテストに `nvim_get_context` を使うのはおそらく不適切だったと述べつつ感謝。

**変更ファイル:** `test/functional/api/version_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bundle tree-sitter-diff parser](https://github.com/neovim/neovim/pull/40778) `#40778`
**ステータス:** Merged | **ラベル:** build

**概要:** tree-sitter-diff パーサー（v0.1.1）をバンドルするビルド依存の追加。マージ時点でパーサーの当該バージョンはまだ存在しない見込みの前提で進められた。

**直近のアクティビティ:** clason が「なぜクエリなしでマージしたのか」と疑問を呈し、justinmk はブロッキングコメントを見落とした、CI 失敗は #39675 の重複のようだ、news.txt にも記載すべきと応答。

**変更ファイル:** `build.zig`・`build.zig.zon`、`cmake.deps/deps.txt`、`BuildTreesitterParsers.cmake`、`static_ts_registry.c`、`bump_deps.lua` を変更。

**次のアクション:** マージ済み。クエリ整備（#40851/#40866）と news 記載（#40848）が後続。

---

### ![Issue](icons/issue-opened.svg) [LSP: vim.lsp.codelens.run() operates on the innermost scope](https://github.com/neovim/neovim/issues/31258) `#31258`
**ステータス:** Closed | **ラベル:** lsp

**概要:** 複数行にまたがるコードレンズ（ruby-lsp の `rubyLsp.runTest` 等）で、`vim.lsp.codelens.run()` を呼ぶには先頭行へ移動する必要がある摩擦の解消要望。

**直近のアクティビティ:** komar007 が、LSP 仕様上コードレンズの range は複数行にまたがるべきでない（1 行のみ）と指摘。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(window): avoid UAF in `close_windows`](https://github.com/neovim/neovim/pull/40857) `#40857`
**ステータス:** Merged | **ラベル:** target:release

**概要:** #40853 を `release-0.12` にバックポートし、`close_windows` の use-after-free クラッシュを修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/window.c` と `buffer_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(window): avoid UAF in `close_windows`](https://github.com/neovim/neovim/pull/40853) `#40853`
**ステータス:** Merged | **ラベル:** window, ci:backport release-0.12

**概要:** `win_close_othertab()` 失敗後にキャッシュした次タブページが存在しない可能性があるため、存在確認を行う。autocommand がタブを削除していれば、ダングリングポインタではなく最初の有効なタブから続行する。#40852 をクローズ。

**直近のアクティビティ:** neovim-backports[bot]がバックポート PR #40857 作成を報告。zeertzjq が「無限ループ問題を招かないか。この分岐の `break;` は `goto theend;` にすべき」と指摘。zeertzjq・seandewar が APPROVE。

**変更ファイル:** `src/nvim/window.c` と `buffer_spec.lua` を変更。

**次のアクション:** マージ済み。zeertzjq の無限ループ懸念のフォローが望ましい。

---

### ![Issue](icons/issue-opened.svg) [UAF in close_windows](https://github.com/neovim/neovim/issues/40852) `#40852`
**ステータス:** Closed | **ラベル:** has:backtrace, bug-crash

**概要:** `WinClosed` autocmd 内でウィンドウをクローズし、別バッファを force 削除する再現手順で `close_windows` に heap-use-after-free（AddressSanitizer 検出）が発生するクラッシュ報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし（#40853 でクローズ）。

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Replace legacy archive plugins with a minimal vim.archive Lua API decoupled from UI](https://github.com/neovim/neovim/issues/38760) `#38760`
**ステータス:** Open | **ラベル:** plugin, runtime, netrw

**概要:** レガシーな `zipPlugin.vim`/`tarPlugin.vim` は外部ツール呼び出し・バッファ統合・閲覧 UI という複数の関心を混在させ、Lua から再利用できず `vim.net` 等の新モジュールと統合しにくい。UI から分離した最小の `vim.archive` Lua API への置き換えを提案。

**直近のアクティビティ:** tampueroc が、アーカイブプラグインごとに私的に開発してから統一する段階的な出荷方針を示している。

**次のアクション:** プラグインごとの段階的実装と API 統一の設計。

---

### ![Issue](icons/issue-opened.svg) [port matchit.vim to lua](https://github.com/neovim/neovim/issues/40678) `#40678`
**ステータス:** Open | **ラベル:** なし

**概要:** matchit.vim の Lua への「移植」（単なる書き直しではなく）を提案。元コードはデバッグモード等の奇妙な拡張機能を持ち、長く直感的でなく保守困難。#39683 で追跡。

**直近のアクティビティ:** clason が、大多数の言語は tree-sitter 非対応なのでレガシー syntax（または no syntax）も必ずサポートする必要があると指摘。barrettruth が、両バージョンのプラグインを同時にアクティブにする必要があるという主張の詳細説明を求めた。

**次のアクション:** レガシー言語のフォールバック方針を含む移行設計の議論。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): added native treesitter grammar for ssh config parsing](https://github.com/neovim/neovim/pull/40849) `#40849`
**ステータス:** Closed | **ラベル:** build, treesitter

**概要:** 現行の SSH config パーサーが手動の文字列操作・パターンマッチに依存し複雑/非標準の設定に脆弱なため、tree-sitter-grammars の ssh_config パーサーを `deps.txt` に追加してローカル tree-sitter 解析を導入する提案（#39648 関連）。

**直近のアクティビティ:** superatomic が「#39648 には良かったが、些細な用途に追加パーサー1 つは割に合わない」と同意。clason は「否。パーサーのバンドルはパッケージング上の大きな摩擦を生み、非常に強い理由がある場合のみ行う」としてクローズ方向に。

**変更ファイル:** `cmake.deps/deps.txt`、`BuildTreesitterParsers.cmake`、`runtime/ftplugin/sshconfig.lua`、`ssh_config` 用クエリ群を変更。

**次のアクション:** クローズ済み。LPeg 等の代替検討（#39648）。

---

### ![Issue](icons/issue-opened.svg) [remote-ssh: use formal grammar (LPeg/Tree-sitter) for SSH config parsing](https://github.com/neovim/neovim/issues/39648) `#39648`
**ステータス:** Open | **ラベル:** complexity:low, syntax, treesitter, remote-ssh

**概要:** `runtime/lua/vim/net/_ssh.lua` の SSH config パーサーが手動パターンに依存し複雑な設定に脆弱なため、LPeg または Tree-sitter による形式文法へ置き換える提案。

**直近のアクティビティ:** superatomic が、（別トピックだが）`:flatten()` がリストイテレータのみ対応なのは恣意的な制約に見え、`:peek()` 同様に修正できそうだとコメントし、PR 提出の意向を示した。

**次のアクション:** LPeg ベースのパーサー実装の検討。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): handle delayed flushes during shutdown](https://github.com/neovim/neovim/pull/40845) `#40845`
**ステータス:** Closed | **ラベル:** event-loop, DO NOT MERGE, AI assisted 🤖

**概要:** シャットダウン中に保留 libuv コールバックを処理する際、キューされた redraw イベントが `tui_stop()` 後の `tui_flush()` に到達し、`loop_close()` がキュー保護用ミューテックスを破棄する問題。libuv がループを閉じ終えるまで `Loop.mutex` を生かし、保護対象キューの直前で破棄する。

**直近のアクティビティ:** zeertzjq が、`os_exit()` 中に `ui_client_attached` が true に設定される箇所があること自体が大きな問題だと分析し、「まだマージしないで、再現を試みている」「この解決策では tui_flush() のより後の地点でクラッシュするだけでは」と懸念を表明。barrettruth はガード追加で対応し、これ以上は専門家に委ねるとした。

**変更ファイル:** `src/nvim/event/loop.c` と `src/nvim/tui/tui.c` を変更。

**次のアクション:** クローズ済み。根本原因（`ui_client_attached` の設定箇所）の調査（#40844）。

---

### ![Issue](icons/issue-opened.svg) [crash: inspect_tree: ThreadSanitizer: use of an invalid mutex](https://github.com/neovim/neovim/issues/40844) `#40844`
**ステータス:** Open | **ラベル:** test, ci, bug-crash

**概要:** `vim.treesitter.inspect_tree` のテストで ThreadSanitizer が「無効なミューテックス（未初期化または破棄済み）の使用」を報告するクラッシュ。CI ログで確認。

**直近のアクティビティ:** barrettruth が、原因に関する詳細は #40845 とそのコードフロートレースのコメントを参照するよう案内。

**次のアクション:** #40845 の議論と連動した根本原因の調査。

---

### ![PR](icons/git-pull-request.svg) [feat(options): add 'wrapcolumn' option for column-based soft wrap](https://github.com/neovim/neovim/pull/38167) `#38167`
**ステータス:** Open（ドラフト）| **ラベル:** options

**概要:** 広いターミナルで散文やコミットメッセージを編集する際、`'wrap'` は端で折り返し読みにくく、`'textwidth'` は実改行を挿入してしまう。バッファを変更せず指定カラム（例: 80）で視覚的に折り返すウィンドウローカルの `'wrapcolumn'`（略 `'wcl'`）オプションを追加。

**直近のアクティビティ:** mikejmcguirk が `set wrap=80` のような構文でカラム上限を指定できないか、現行のオプションアーキテクチャと衝突するかと質問。

**変更ファイル:** `src/nvim/drawline.c`、`move.c`、`plines.c`、`options.lua`、`buffer_defs.h` ほかを変更。

**次のアクション:** オプション構文とアーキテクチャの検討、ドラフトの仕上げ。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:8.2.2271: ml_get error when changing hidden buffer in Python](https://github.com/neovim/neovim/pull/40833) `#40833`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** Python で隠しバッファを変更すると `ml_get` エラーが出る問題を、フォールドの更新をブロックすることで修正する vim-patch の取り込み。

**直近のアクティビティ:** janlazo が次の version.c で N/A パッチを提案するので 1 日待ってほしいと述べ、zeertzjq が非 static 関数 2 つが未使用である点、リンク段階でこれを検出できるかを議論。janlazo は #15831（2022 年）以来の未使用でデッドコード検出が無効/不十分ではと応じ、N/A としてマークすると回答。

**変更ファイル:** `src/nvim/eval/buffer.c`、`test/old/testdir/test_python3.vim`、`scripts/vim_na_hunks_c.txt` を変更。

**次のアクション:** マージ済み。未使用関数の N/A 整理が後続。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): preview window converted to normal window is auto-closed](https://github.com/neovim/neovim/pull/40770) `#40770`
**ステータス:** Merged | **ラベル:** lsp

**概要:** フロートのプレビューウィンドウ（hover、signature help）を `CTRL-W_H` や `nvim_win_set_config()` で通常ウィンドウに変換しても `open_floating_preview()` の autocommand に閉じられてしまう問題を修正。フロートでなくなったら自動クローズ autocommand を外し、バッファのフロートプレビューとしての追跡を停止する。#36659 をクローズ。

**直近のアクティビティ:** MariaSolOs・rvaccone から計 5 件のレビューコメント。MariaSolOs が APPROVE してマージ。

**変更ファイル:** `runtime/lua/vim/lsp/util.lua`、`news.txt`、`util_spec.lua` を変更。

**次のアクション:** マージ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [docs: nvim_open_term, terminal](https://github.com/neovim/neovim/pull/40847) `#40847`
**ステータス:** Open（ドラフト）| **ラベル:** なし

**概要:** `nvim_open_term` とターミナル関連のドキュメント整備（ドラフト）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/api.txt`・`lua.txt`・`news.txt`・`terminal.txt`、`src/nvim/api/vim.c`、`_core/editor.lua`、`api.gen.lua` を変更。

**次のアクション:** ドキュメントの仕上げとレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(abbrev): add position-aware cmdline abbreviation](https://github.com/neovim/neovim/pull/40387) `#40387`
**ステータス:** Open（ドラフト）| **ラベル:** なし

**概要:** `:cabbrev` がコマンドライン中どこでも展開され、略語が引数位置に現れると不要な展開を招く問題に対し、LHS に `:` 接頭辞を付けた第 4 のアブリビエーション種別を導入。`:` 接頭辞の略語は語がコマンド位置（行頭または `|` の後）にあるときのみ展開する。#39394 の修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/mapping.c`、`mapping_defs.h`、`abbrev_spec.lua` を変更。

**次のアクション:** レビューとドラフトの仕上げ。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません

---

## ghostty-org/ghostty

> ターミナルコアのページ容量エラー処理やVTシーケンス実装、macOS 27(Golden Gate)関連の表示不具合修正が活発。多くのPRが短時間でmergeされ、非メンテナ作成のIssueは自動クローズされている。

### ![PR](icons/git-pull-request.svg) [terminal: handle page capacity errors in cursorScrollAbove](https://github.com/ghostty-org/ghostty/pull/13394) `#13394`
**ステータス:** Merged | **ラベル:** vt, libghostty

**概要:** #13160 に関連する修正。`cursorScrollAbove` がページ境界をまたいで行を回転させる際、前ページの最終行を次ページへクローンする処理で容量エラーが未処理だった。エラーが行回転後に伝播するとページリストが中途半端に変更された状態となり、後続処理でクラッシュしうる。`insertLines`/`deleteLines` と同様に、容量が不足した次元のページ容量を増やして行コピーを再試行する方式で対処する。

**直近のアクティビティ:** mitchellh が「`eraseRow` 系にも同じバグがあると思う。他に容量を適切に扱っていない箇所を探している」とコメント。

**変更ファイル:** `src/terminal/Screen.zig`（+246 -14）の 1 ファイルを変更。

**次のアクション:** 既に merge 済み。関連する `eraseRow` 側の修正（#13397）が別途対応されている。

---

### ![PR](icons/git-pull-request.svg) [terminal: handle page capacity errors in eraseRow](https://github.com/ghostty-org/ghostty/pull/13397) `#13397`
**ステータス:** Merged | **ラベル:** libghostty

**概要:** #13160 に関連する（同一 Issue ではない）修正。`eraseRow`/`eraseRowBounded` は、`cursorScrollAbove` で以前修正されたのと同じ問題を抱えていた。ページ境界をまたいで行を上にシフトする際、次ページの先頭行を前ページの最終行へクローンするが、行の管理メモリ分の容量が宛先ページに不足するとクローンが失敗しうる。他のページ間コピーと同様に容量を増やして再試行する。共通ロジックがあちこちで繰り返されていたため、PageList と Screen にヘルパーとして抽出した（Screen はカーソルの追加処理があるため若干異なる）。実際の遭遇シナリオは不明だがテストでは容易に再現できた。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/terminal/PageList.zig`（+319 -4）、`src/terminal/Screen.zig`（+80 -60）、`src/terminal/Terminal.zig`（+193 -93）の 3 ファイルを変更。

**次のアクション:** 既に merge 済み。特筆すべき残作業なし。

---

### ![PR](icons/git-pull-request.svg) [kitty images: add support for transient usage hints](https://github.com/ghostty-org/ghostty/pull/13389) `#13389`
**ステータス:** Merged | **ラベル:** なし

**概要:** Kitty 0.48 が画像プロトコルに追加した usage hints、特に画像を「transient」（メモリ逼迫時に優先的に退避すべき）とマークする機能をサポートする。あわせて退避アルゴリズムを、ArrayList ではなく確保済み配列で退避対象画像を整理する方式に変更し、割り当て回数を最小化した（メモリ節約効果自体はない）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/terminal/kitty/graphics_command.zig`（+38）、`graphics_image.zig`（+2）、`graphics_storage.zig`（+77 -19）の 3 ファイルを変更。

**次のアクション:** 既に merge 済み。特筆すべき残作業なし。

---

### ![PR](icons/git-pull-request.svg) [macOS: hide visible NSScrollPocket for hidden title bar](https://github.com/ghostty-org/ghostty/pull/13393) `#13393`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** #13390 を修正する。技術的には `#available(macOS 27, *)` チェックを外しても安全だが、全 OS バージョンで検証していないため残している。AI は使用していない（AI Disclosure 記載）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `macos/Sources/Features/Terminal/Window Styles/HiddenTitlebarTerminalWindow.swift`（+12）の 1 ファイルを変更。

**次のアクション:** 既に merge 済み。対応する Issue #13390 もクローズ済み。

---

### ![PR](icons/git-pull-request.svg) [terminal: implement DECSTR soft reset (CSI ! p)](https://github.com/ghostty-org/ghostty/pull/13333) `#13333`
**ステータス:** Open | **ラベル:** vt

**概要:** `CSI ! p`（DECSTR、ソフトターミナルリセット）はこれまで未実装として無視され、スクロール領域・原点モード・SGR 属性・文字セットなどの設定が残存し、以降の出力が誤った位置・色・文字セットで表示される問題があった。DECSTR は DEC STD 070 に従い、画面内容とカーソル位置を変えずにプログラマブルな状態を電源投入時の既定へ戻す（カーソル可視化、replace モード、絶対原点、autowrap 有効、既定 SGR、ASCII 文字セット、全スクロール領域、保護解除、保存カーソル位置クリア）。xterm/VTE/conhost も実装済みでテストも含む。

**直近のアクティビティ:** mitchellh が CHANGES_REQUESTED でレビュー。「サポートは妥当。広くは使われないがリスクも低い。他のターミナルがどう扱うか検証付きで知りたい（LLM はそのあたりをでっち上げるので）」とコメント。

**変更ファイル:** `src/terminal/Terminal.zig`（+104）、`stream.zig`（+12 -1）、`stream_terminal.zig`（+1）、`src/termio/stream_handler.zig`（+1）の 4 ファイルを変更。

**次のアクション:** 他ターミナルの挙動について検証可能な根拠を示し、CHANGES_REQUESTED に対応する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13401) `#13401`
**ステータス:** Merged | **ラベル:** なし

**概要:** @jcollie による discussion comment（#13400）をトリガーとして bot が自動生成した PR。@Ragnoroct を vouch（保証）リストに追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/VOUCHED.td`（+1）の 1 ファイルを変更。

**次のアクション:** bot 自動運用のため対応不要。既に merge 済み。

---

### ![Issue](icons/issue-opened.svg) [macOS 27 with reduced transparency and `macos-titlebar-style = hidden` hides top lines](https://github.com/ghostty-org/ghostty/issues/13390) `#13390`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** discussion #13387 から起票。macOS 27 beta（golden gate）で「Reduced Transparency」を有効にし、`macos-titlebar-style = hidden` のみの最小構成にすると、上部の数行が隠れてしまう。Reduced Transparency がオフのときは隠れない。#13128 に関連する可能性がある。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 対応 PR #13393 が merge 済みでクローズ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [fcitx5 candidate window doesn't follow cursor after commit in TUI apps, only backspace updates it](https://github.com/ghostty-org/ghostty/issues/13395) `#13395`
**ステータス:** Closed | **ラベル:** なし

**概要:** opencode などの TUI アプリで、fcitx5 の候補ウィンドウが最初の文字位置に固定され、文字をコミットしても移動しない。backspace を押すと正しい位置へジャンプする。同じアプリを alacritty で使うと候補ウィンドウはコミットごとにカーソルへ追従する。環境は Ghostty 1.3.1（Arch Linux）、KDE Plasma 6/KWin Wayland、fcitx5（text-input-v3）。コミットされたテキスト自体は正しい位置に落ちるため、IME ポップアップの位置更新のみが問題。

**直近のアクティビティ:** ghostty-vouch[bot]が「非メンテナはこのリポジトリで Issue を作成できない。まず discussion を作成してほしい」とコメントし、自動クローズされた。

**次のアクション:** メンテナ以外のため discussion での起票が必要。現状は自動クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [GTK: new window opens grey/blank intermittently, even with gtk-single-instance=false (Hyprland direct-exec)](https://github.com/ghostty-org/ghostty/issues/13392) `#13392`
**ステータス:** Closed | **ラベル:** gtk

**概要:** Hyprland のキーバインドで直接 `ghostty` バイナリを実行して新規ウィンドウを開くと、間欠的に空の灰色ウィンドウが開きシェルプロンプトが表示されない。既知の GTK single-instance/D-Bus activation のレース（#11724）を疑い `gtk-single-instance = false` を設定、直接起動では D-Bus/systemd 経路を通らないことを確認したが症状は継続。GTK 4.18+の「ngl」レンダラー回帰（#6872）か、別要因（GPU コンテキスト生成レース等）の可能性。まだ失敗時のログ/スクリーンショットは未取得で、後日 `GHOSTTY_LOG`/journalctl 付きで追記予定。

**直近のアクティビティ:** ghostty-vouch[bot]が「非メンテナは Issue を作成できない。まず discussion を作成してほしい」とコメントし、自動クローズされた。

**次のアクション:** メンテナ以外のため discussion での起票が必要。ライブ発生時のログ取得が待たれる。

---

### ![Issue](icons/issue-opened.svg) [Check whether the `tabs` style works correctly after the first public release of Golden Gate](https://github.com/ghostty-org/ghostty/issues/13070) `#13070`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS 27 beta 2 でタブバーの名称変更が行われており、正式リリース後に `macos-titlebar-style = tabs` のタブバーが正しく動作するか最終確認が必要。Xcode で既存のタブ関連 UI テストを実行して検証できる。Golden Gate 用の tabs サブクラスを動的ディスパッチにすべきか、`hidden` スタイルについても同様（スクロールポケット非表示の回避策あり）を検討項目として挙げている。tip 版で問題がある場合のみコメントするよう注記。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** macOS 27 正式リリース後にタブバー動作の最終確認と、動的ディスパッチ化の要否判断を行う。

---

### ![Issue](icons/issue-opened.svg) [Implement CSI t w/ params 14, 18 (report terminal size)](https://github.com/ghostty-org/ghostty/issues/476) `#476`
**ステータス:** Closed | **ラベル:** documentation

**概要:** notcurses が使用する `CSI t` のパラメータ 14・18（ターミナルサイズ報告）の実装。terminalguide の該当シーケンス仕様（csi_st-14、csi_st-18）へのリンクが記載されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [Feature: Support for tmux's Control Mode](https://github.com/ghostty-org/ghostty/issues/1935) `#1935`
**ステータス:** Open | **ラベル:** vt, gui

**概要:** tmux の Control Mode（クライアント側のターミナルエミュレータが tmux ペインを管理できる機能）のサポート要望。ターミナルエミュレータおよびホスト OS とのネイティブ統合が可能になり、tmux を直接使う際の制約なしにバッファ管理が容易になる利点がある。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 機能要望として継続オープン。実装方針の検討が必要。

---

## astral-sh/uv

> リリース 0.11.30 前後のワークフロー整備、Renovate による依存更新、リゾルバ・キャッシュのパフォーマンス最適化実験、preview 機能への JSON 出力追加が活発です。あわせて依存クレートの脆弱性報告も複数上がっています。

### ![PR](icons/git-pull-request.svg) [Use a scoped token for GitHub releases](https://github.com/astral-sh/uv/pull/20562) `#20562`
**ステータス:** Merged | **ラベル:** internal, automations

**概要:** `main` がワークフロー変更をまたいで進むとリリースタグ作成前に `GITHUB_TOKEN` が `workflows: write` を得られず、GitHub リリース作成が `HTTP 403: Resource not accessible by integration` で失敗する問題への対応です。保護されたリリースジョブの OIDC ID を短命かつリポジトリスコープの App トークン（`contents: write` と `workflows: write`）に交換し、`gh release create` にのみ使用し、組み込みトークンの contents 権限を read-only に下げます。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb による APPROVED あり）。

**変更ファイル:** `.github/workflows/release.yml` の 1 ファイル変更。

**次のアクション:** 対応する release-workflow broker ルールと `release` 環境の `STS_API_URL` 設定が前提。マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Use Namespace runners for macOS](https://github.com/astral-sh/uv/pull/20276) `#20276`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** macOS のテスト・リリースジョブを Depot から Namespace macOS 15 プロファイルへ移行します。`actions/checkout` と `Swatinem/rust-cache` を維持して GitHub のキャッシュ API を使い続け、native-auth テスト用に空パスワードのキーチェーンを初期化します。3 回のサンプルで Namespace はテストジョブで 46%、リリースジョブで約 26%高速でしたが、ハードウェア構成が異なるため厳密な同条件比較ではないと注記しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/build-release-binaries.yml`、`test.yml` の 2 ファイル。

**次のアクション:** Draft 段階。レビューと本採用可否の判断が必要。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate による依存更新と検出済み依存関係を一覧する自動管理 Issue です。リポジトリの renovate 実行時に発生した問題も記載され、ファイル内に隠し Unicode 文字が検出されたという警告（意図的なものか確認を促す内容）が含まれます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Renovate 管理用ダッシュボードのため通常は対応不要。隠し Unicode の警告は確認が望ましい。

---

### ![PR](icons/git-pull-request.svg) [Bump Hawk to 0.1.9 and Rust to 1.97.1](https://github.com/astral-sh/uv/pull/20566) `#20566`
**ステータス:** Merged | **ラベル:** なし

**概要:** CI の Hawk チェックを 0.1.8 から 0.1.9 へ更新します。0.1.9 は Rust 1.97.1 でビルドされているため、Hawk のツールチェーンのインストールと呼び出しを 1.97.1 に更新し、root および `uv-build` のツールチェーン固定と揃えます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/check-lint.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Widen resolver singletons using adjacent versions](https://github.com/astral-sh/uv/pull/20506) `#20506`
**ステータス:** Closed (Draft) | **ラベル:** なし

**概要:** singleton 依存の非互換を広げる際、現状は毎回候補全体をクローン・ソート・重複排除しています。単一インデックス／未インストールの一般的ケースでは、隣接する採用済みバージョンを直接使い、cutoff で除外されるエントリをスキップして distribution を実体化しないよう最適化します。インストール済み・複数インデックス・URL 候補には従来の全体走査パスを維持します。計測では Jupyter で -1.22%、Airflow で -0.92% の CPU 改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-resolver/src/resolver/mod.rs`、`version_map.rs` の 2 ファイル。

**次のアクション:** Closed のため本 PR での対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Skip CLI construction for version-only invocations](https://github.com/astral-sh/uv/pull/20505) `#20505`
**ステータス:** Closed (Draft) | **ラベル:** なし

**概要:** `uv --version` と `uv -V` の version 専用呼び出しで、実行パス解決と Clap コマンド構築全体をスキップします。それ以外の引数は従来のパーサを通り、両綴りともスナップショットで統合テスト済み。計測では `uv --version` の wall time が 9.15ms→7.14ms（-21.8%）、CPU が -23.6% で、`--help` などの対照は中立、出力ハッシュも一致。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv/src/lib.rs`、`crates/uv/tests/it/version.rs` の 2 ファイル。

**次のアクション:** Closed のため本 PR での対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Further compact cached index URLs](https://github.com/astral-sh/uv/pull/20491) `#20491`
**ステータス:** Closed (Draft) | **ラベル:** なし

**概要:** Simple API キャッシュをさらに圧縮する実験です。共通の絶対アーティファクト URL プレフィックスをプロジェクトごとに 1 回保存し、正規の Warehouse パスを 32 バイトのダイジェスト＋末尾へパックします。相対・混在インデックス・クエリ/フラグメント・パーセントエンコード・非正規パスにはロスレスのフォールバックを保持し、アーカイブ表現から直接ファイルを実体化します。レイアウト変更のためキャッシュを `simple-v25` に更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-cache`、`registry_client.rs`、`file.rs`、`version_map.rs`、`uv-small-str`、`pip/latest.rs`、`cache_clean.rs` の 7 ファイル。

**次のアクション:** Closed のため本 PR での対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Experiment with bundled wheel metadata](https://github.com/astral-sh/uv/pull/20488) `#20488`
**ステータス:** Closed (Draft) | **ラベル:** なし

**概要:** バックトラッキングの多い warm 解決では、数千の小さな wheel-METADATA キャッシュファイルを開きます（boto3/botocore だけで 1,903 ファイル・1.42MB、うち約 1.1MB は繰り返しの HTTP キャッシュポリシートレーラ）。パッケージ単位の追加的・オフライン専用メタデータバンドルを試作し、初回リクエストでバンドルを読み込み、以降はバッファとオフセットインデックスを再利用します。キー・URL・デコード結果のミス時は既存の per-wheel キャッシュにフォールバックし、オンラインの鮮度挙動は不変。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock`、`uv-client/Cargo.toml`、`registry_client.rs`、`scripts/benchmark/pack-wheel-metadata-bundle.py` の 4 ファイル。

**次のアクション:** Closed のため本 PR での対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Restore changes to workflows following 0.11.30](https://github.com/astral-sh/uv/pull/20564) `#20564`
**ステータス:** Merged | **ラベル:** なし

**概要:** #20563 で 0.11.30 リリースのため一時的に revert したワークフロー変更を、リリース完了後に元へ戻します。コミット `f8a1fa25` の完全な逆操作で、`.github/workflows` を revert 前の状態に復元し、`main` の他の変更には影響しません。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `bench.yml`、`check-lint.yml`、`promote-pull-request.yml` ほか `.github/workflows` 配下 9 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Revert changes to workflows for 0.11.30](https://github.com/astral-sh/uv/pull/20563) `#20563`
**ステータス:** Merged | **ラベル:** なし

**概要:** 0.11.30 リリースは `4cb3d036` からディスパッチされましたが、タグ作成前に `main` がワークフロー変更をまたいで進み、リリースジョブの `GITHUB_TOKEN` が `workflows: write` を得られず GitHub がタグを 403 で拒否しました。既存リリースを完了させるため `.github/workflows` をリリースコミット時点の状態へ復元する一時対応で、直後にスタックした follow-up (#20564) で元に戻します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows` 配下 9 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update Rust toolchain to 1.97.1](https://github.com/astral-sh/uv/pull/20561) `#20561`
**ステータス:** Merged | **ラベル:** internal

**概要:** Rust 1.97.0 には x86_64 ビルドに影響する最適化コードの誤コンパイルが確認されているため、root と `uv-build` のツールチェーン固定を `1.97.1` に更新します。これにより wheel・sdist・Docker イメージ・ローカルビルドがパッチ済みコンパイラを使用します。Hawk は配布バイナリがそのコンパイラバージョンに紐づくため 1.97.0 に据え置きます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-build/rust-toolchain.toml`、`rust-toolchain.toml` の 2 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Allow configuration of proxy indexes](https://github.com/astral-sh/uv/pull/20218) `#20218`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** proxy index を `pyproject.toml` / `uv.toml` から設定可能にする大規模 PR です。`[[tool.uv.proxy-index]]` と `[[proxy-index]]` を追加し、各宣言は正規の `index` を指定し、物理 Simple API の `url` と、物理アーティファクトプレフィックスから正規 lock プレフィックスへの必須 `artifact-url-map` を与えます。宣言順を保持し、プロジェクト・ユーザー・システム設定をまたいで正規等価な重複を拒否します。本文冒頭に「まだコードを見ておらず、レビュー準備前に必ず変わる」との注意書きあり。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb による COMMENTED レビューあり）。

**変更ファイル:** `uv-cli/options.rs`、`artifact_url.rs`、`index.rs`、`uv-settings` 各所、`tool/install.rs`、テスト群、`uv.schema.json` など 18 ファイル、+2684/-25。

**次のアクション:** Draft・レビュー前。作者による仕上げと本格レビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Remove the dispatcher policy canary](https://github.com/astral-sh/uv/pull/20560) `#20560`
**ステータス:** Merged | **ラベル:** なし

**概要:** compact v2 ポリシーと workflow-dispatch 経路を検証した dispatcher の canary が成功したため、一時的な合成ルートを削除し、変更のない promotion トリガとターゲットのみを compact ポリシーに残します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/automations-dispatch.json` の 1 ファイル（-10 行）。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add a bounded dispatcher policy canary](https://github.com/astral-sh/uv/pull/20559) `#20559`
**ステータス:** Closed | **ラベル:** なし

**概要:** compact v2 スキーマ用に本番の限定的 canary を追加する内容です。既存の promotion ルートを保持し、`uv-dev` 上の `issue-triage.yml`（全ジョブが確定的にスキップ）を狙う合成専用ルートを 1 本追加し、PR の promote/rebase/変更なしにポリシー読み込みと dispatch を検証します。単回配信後に削除予定。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/automations-dispatch.json` の 1 ファイル。

**次のアクション:** Closed のため本 PR での対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Use the compact automations dispatch policy](https://github.com/astral-sh/uv/pull/20558) `#20558`
**ステータス:** Merged | **ラベル:** なし

**概要:** dispatcher が固定の PR セーフガードと正規のワークフロー入力を導出する compact ポリシースキーマに対応したため、既存の promotion ルートをトリガ・ターゲットを変えず移行し、`uv-dev` のスキップ対象 sync ワークフローに対して dispatch を検証する一意にゲートした一時 canary ルートを追加します。canary は本番確認後に削除。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/automations-dispatch.json` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Prepare Rust for test automations](https://github.com/astral-sh/uv/pull/20557) `#20557`
**ステータス:** Merged | **ラベル:** automations

**概要:** ネットワーク無効の rebase サンドボックスは PR チェックアウト後に固定ツールチェーンや `cargo-nextest` を用意できません（失敗した rebase ジョブが read-only な Rustup ディレクトリと依存欠落を示す）。PR をフェッチ／チェックアウトする前にツールチェーン準備、read-only Rust キャッシュ復元、locked な main 依存のフェッチ、`cargo-nextest` インストールを行い、PR 制御の Cargo 設定を信頼済みセットアップの外に保ちます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `pull-request-security-review.yml`、`rebase-conflicted-pull-request.yml` の 2 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Use Depot for conflicted pull request rebases](https://github.com/astral-sh/uv/pull/20556) `#20556`
**ステータス:** Merged | **ラベル:** automations

**概要:** コンフリクトした PR の rebase ジョブが通常の GitHub runner を奪い合うため、既存の Depot Linux runner へ移動し、共有 runner プールを消費しないようにします。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/rebase-conflicted-pull-request.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update jiff-tzdb to 0.1.8](https://github.com/astral-sh/uv/pull/20554) `#20554`
**ステータス:** Merged | **ラベル:** enhancement

**概要:** Jiff 0.2.32 は同梱タイムゾーン DB を `2026c` に更新しましたが、`jiff-tzdb-platform` が lockfile の `jiff-tzdb` 0.1.6 を許容するため Windows ビルドは `2026a` を埋め込み続けていました。`jiff-tzdb` を 0.1.8 に更新して Windows ビルドが `2026c` を同梱するようにします。British Columbia / Alberta の恒久サマータイム化や Morocco・Western Sahara の恒久 UTC 化などが反映されます。

**直近のアクティビティ:** codspeed-hq ボットが「マージによりパフォーマンス 5.2% 改善」（`resolve_warm_jupyter` が改善）と報告。

**変更ファイル:** `Cargo.lock` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Post promotion comments as the automations bot](https://github.com/astral-sh/uv/pull/20555) `#20555`
**ステータス:** Merged | **ラベル:** なし

**概要:** promotion は現状 `GITHUB_TOKEN` で `uv-dev` の PR をクローズするため「Promoted to ...」コメントが `github-actions[bot]` として表示されます。close ステップに `pull_requests: write` を持つ `uv-dev` スコープトークンを要求し、`astral-automations-bot[bot]` として投稿させ、ジョブのトークンは初期チェックに必要な read 権限に縮小します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/promote-pull-request.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate reqsign to v0.20.2](https://github.com/astral-sh/uv/pull/19845) `#19845`
**ステータス:** Open | **ラベル:** internal, do-not-merge, build:skip-release, build:skip-docker

**概要:** Renovate による `reqsign` 0.20.0→0.20.2 のパッチ更新（`Cargo.lock`）です。Azure Storage のクライアントシークレット／ワークロード ID プロバイダ用セッターの追加などが含まれます。ただし `do-not-merge` ラベルが付いています。

**直近のアクティビティ:** zsol が、この更新は PKCS#1 サービスアカウント鍵での GCS 認証を壊すようだと指摘。uv は presigned expiry なしで GCS signer を呼び、サービスアカウント鍵をアクセストークンに交換するが、従来 `jsonwebtoken::EncodingKey::from_rsa_pem` が PKCS#1/PKCS#8 双方を受け付けていたのに新しい JWT ヘルパーが受け付けなくなる、という趣旨。

**変更ファイル:** `Cargo.lock` の 1 ファイル、+58/-68。

**次のアクション:** GCS 認証のリグレッションを解消しない限りマージ不可。上流での対応または回避策の確認が必要。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate open to v5.4.0](https://github.com/astral-sh/uv/pull/20536) `#20536`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `open` クレート 5.3.6→5.4.0（minor）の更新です。`cargo run` が試行したコマンドを表示する新機能や、WSL PowerShell 呼び出しの Windows との整合などのバグ修正を含みます。

**直近のアクティビティ:** codspeed-hq ボットが「パフォーマンスは変化なし」と報告。

**変更ファイル:** `Cargo.lock` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要（関連する脆弱性 Issue #20541 も参照）。

---

### ![PR](icons/git-pull-request.svg) [Use `regex!` for static patterns](https://github.com/astral-sh/uv/pull/20553) `#20553`
**ステータス:** Merged | **ラベル:** internal

**概要:** `regex` 1.13 が追加した `regex!`（リテラルパターンを遅延コンパイルして再利用）を使い、既存の `LazyLock<Regex>` の定型コードと直接の `Regex::new` 呼び出しを置き換えます。毎回パターンを再コンパイルしていた shebang チェックも改善します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb による APPROVED あり）。

**変更ファイル:** `Cargo.toml`、`uv-build-backend`、`uv-extract`、`uv-pep508` ほか 10 ファイル、+64/-123。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.30](https://github.com/astral-sh/uv/pull/20540) `#20540`
**ステータス:** Merged | **ラベル:** releases

**概要:** 0.11.30 リリース用のバージョン更新 PR です。`CHANGELOG.md`・`Cargo.toml`・各クレートのバージョンなど 151 ファイルにわたる定型的な変更です。

**直近のアクティビティ:** codspeed-hq ボットが「マージによりパフォーマンス 6.27% 改善」と報告。zanieb・zsol による多数のレビューコメントと zanieb の APPROVED あり。

**変更ファイル:** `CHANGELOG.md`、`Cargo.lock/toml`、各クレートの `Cargo.toml`/`README.md` など 151 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update fedora:45 Docker digest to 0c1f63e](https://github.com/astral-sh/uv/pull/19231) `#19231`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `fedora:45` コンテナダイジェストの更新（`af8cdc4`→`0c1f63e`）です。テストシステム用ワークフローの digest ピンを更新します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test-system.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update taiki-e/install-action action to v2.83.2](https://github.com/astral-sh/uv/pull/20538) `#20538`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による GitHub Action `taiki-e/install-action` の v2.82.9→v2.83.2（minor）更新です。`mise`・`just`・`cargo-neat` などバンドルツールのバージョン更新を含みます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `bench.yml`、`check-lint.yml`、`test-windows-trampolines.yml`、`test.yml` の 4 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update CodSpeedHQ/action action to v4.18.5](https://github.com/astral-sh/uv/pull/20526) `#20526`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `CodSpeedHQ/action` の v4.18.1→v4.18.5（patch）更新です。ピン留めされた valgrind-codspeed の更新などを含みます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/bench.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update aws-actions/configure-aws-credentials action to v6.2.2](https://github.com/astral-sh/uv/pull/20525) `#20525`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `aws-actions/configure-aws-credentials` の v6.2.1→v6.2.2（patch）更新です。統合テスト用ワークフローで使用されます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test-integration.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate http to v1.4.2](https://github.com/astral-sh/uv/pull/19938) `#19938`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `http` クレート 1.4.1→1.4.2（patch）更新です。HTTP/2 リクエストで scheme と authority が設定されている場合にパスとして `"*"` を許可する修正、`Uri` が `DEL` 文字を適切に拒否する修正を含みます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `Cargo.lock` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Pin the development Rust toolchain to nightly](https://github.com/astral-sh/uv/pull/20551) `#20551`
**ステータス:** Open | **ラベル:** internal

**概要:** `rust-toolchain.toml` を `nightly-2026-07-13` に切り替え、uv 開発全体で一貫して nightly を使うようにします。#20459 の nightly lint 修正も取り込みます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build-dev-binaries.yml`、`test.yml`、`credentials.rs`、`candidate_selector.rs`、`rust-toolchain.toml`、`uv.lock` など 17 ファイル、+103/-128。

**次のアクション:** レビューとマージ判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Update dependency astral-sh/uv to v0.11.29](https://github.com/astral-sh/uv/pull/20527) `#20527`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による、CI ワークフローで使う `astral-sh/uv` アクションのバージョン 0.11.28→0.11.29（patch）更新です。`uv tree` の JSON 出力追加や CUDA 13.2 の PyTorch バックエンド対応などが 0.11.29 に含まれます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `check-docs.yml`、`check-fmt.yml`、`check-lint.yml` ほか 7 ワークフロー。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [docs: Move contributing to references](https://github.com/astral-sh/uv/pull/20552) `#20552`
**ステータス:** Merged | **ラベル:** documentation

**概要:** #20511 でトップレベルの Guides セクション（本来ユーザー向け）に追加された contributing ガイドを Reference セクションへ移動し、ナビゲーション・セクションインデックス・`llms.txt` 設定を更新します。`CONTRIBUTING.md` は引き続きページのソースです。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb による APPROVED あり）。テスト計画として strict モードでドキュメントをビルドし、レンダリング済みガイドの 26 リンクを検証。

**変更ファイル:** `docs/guides/index.md`、`docs/reference/contributing.md`、`docs/reference/index.md`、`mkdocs.yml` の 4 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.29](https://github.com/astral-sh/uv/pull/20345) `#20345`
**ステータス:** Merged | **ラベル:** releases

**概要:** 0.11.29 リリース用のバージョン更新 PR です。151 ファイルにわたる定型的なバージョン更新です。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb 等の多数の COMMENTED と charliermarsh の APPROVED あり）。

**変更ファイル:** `CHANGELOG.md`、各クレートの `Cargo.toml`/`README.md` など 151 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.28](https://github.com/astral-sh/uv/pull/20226) `#20226`
**ステータス:** Merged | **ラベル:** releases

**概要:** 0.11.28 リリース用のバージョン更新 PR です。151 ファイルにわたる定型的な変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `CHANGELOG.md`、各クレートの `Cargo.toml`/`README.md` など 151 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.24](https://github.com/astral-sh/uv/pull/19968) `#19968`
**ステータス:** Merged | **ラベル:** releases

**概要:** 0.11.24 リリース用のバージョン更新 PR です。151 ファイルにわたる定型的な変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb の COMMENTED あり）。

**変更ファイル:** `CHANGELOG.md`、各クレートの `Cargo.toml`/`README.md` など 151 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.21](https://github.com/astral-sh/uv/pull/19810) `#19810`
**ステータス:** Merged | **ラベル:** releases

**概要:** 0.11.21 リリース用のバージョン更新 PR です。151 ファイルにわたる定型的な変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb の APPROVED あり）。

**変更ファイル:** `CHANGELOG.md`、各クレートの `Cargo.toml`/`README.md` など 151 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add contributing section to docs](https://github.com/astral-sh/uv/pull/20511) `#20511`
**ステータス:** Merged | **ラベル:** documentation, ep-2026-sprints

**概要:** MkDocs に contributing セクションを追加し、`good first issue` に関する段落を削除します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（woodruffw の APPROVED あり）。テスト計画は MkDocs をビルドして手動確認。

**変更ファイル:** `CONTRIBUTING.md`、`docs/guides/contributing.md`、`docs/guides/index.md`、`mkdocs.yml` の 4 ファイル。

**次のアクション:** マージ済み。なお配置は後続の #20552 で Reference へ変更済み。

---

### ![PR](icons/git-pull-request.svg) [Use `astral-automations-bot` for `prepare-release`](https://github.com/astral-sh/uv/pull/20543) `#20543`
**ステータス:** Merged | **ラベル:** internal

**概要:** prepare-release ワークフローは現状 `GITHUB_TOKEN` でリリースブランチを push するため CI が `github-actions[bot]` をアクターとして認識し、その bot に write 権限がないため Codex セキュリティレビューが失敗していました（#20540 が該当）。ブローカートークンとコミット ID を使い `astral-automations-bot[bot]` でリリース準備を行うよう切り替えます。

**直近のアクティビティ:** 直近 24 時間のコメントなし（ewdurbin の APPROVED あり）。

**変更ファイル:** `.github/workflows/release-prepare.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Allow Renovate pull requests in security reviews](https://github.com/astral-sh/uv/pull/20544) `#20544`
**ステータス:** Merged | **ラベル:** internal

**概要:** Renovate の PR をセキュリティレビューワークフローで許可する変更です（本文説明なし）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/pull-request-security-review.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Fix JSON schema validation with fastjsonschema](https://github.com/astral-sh/uv/pull/20550) `#20550`
**ステータス:** Merged | **ラベル:** internal

**概要:** #20304 のスキーマ変更が上流 fastjsonschema のコード生成でつまずく問題の修正です。fastjsonschema は `not` サブスキーマの try ブロック内で `data_keys` を初期化し、`required` が先に失敗すると例外が握りつぶされて未初期化の `data_keys` にアクセスし `UnboundLocalError` になります。競合する preview 設定を `$ref` の定義に移すことで `not` サブスキーマ用に別の検証関数を生成させ、一時変数を分離して問題を回避します。Codex の助けを借りて作成。

**直近のアクティビティ:** EliteTK が「素晴らしい」とコメントし、CI を `--tool uv=uv.schema.json` で実行すれば validate-pyproject に対してスキーマをテストでき #20549 をクローズできるのではと提案。astral-sh-bot がテストインベントリ不変を報告。ewdurbin は差分用テストの扱いについて、より丁寧なテスト手法(#20549)が望ましく、当該テストの削除は問題ないとの見解。

**変更ファイル:** `.github/workflows/check-lint.yml`、`crates/uv-settings/src/settings.rs`、`uv.schema.json` の 3 ファイル。

**次のアクション:** マージ済み。より体系的なスキーマテストは #20549 で追跡。

---

### ![PR](icons/git-pull-request.svg) [Remove the stdin metadata from `uv check`](https://github.com/astral-sh/uv/pull/20501) `#20501`
**ステータス:** Merged | **ラベル:** preview

**概要:** `uv check` から stdin メタデータのモードを削除します。ワークスペースのファイルを走査する ty が PEP-723 スクリプトを見つけると結局 uv に呼び戻す必要があり、簡易ワークスペース向けの 2 モード構成は「うまく動かない」と判断されたためです。ty が uv に呼び戻す単一モードに統一し、sync の重複という若干の冗長性は残るものの安価な再 sync で許容します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb の APPROVED あり）。

**変更ファイル:** `project/check.rs`、`project/check/ty.rs`、`workspace/metadata.rs`、`workspace/module_owners.rs`、`tests/project/check.rs` の 5 ファイル、+6/-177。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate test-log to v0.2.21](https://github.com/astral-sh/uv/pull/19721) `#19721`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `test-log` クレート 0.2.20→0.2.21（patch）更新です。生成コードのスパン修正により `rust-analyzer` との連携が改善されます。

**直近のアクティビティ:** codspeed-hq ボットが「パフォーマンスは変化なし」と報告。

**変更ファイル:** `Cargo.lock` の 1 ファイル。

**次のアクション:** 手動マージが必要（automerge は設定で無効）。

---

### ![PR](icons/git-pull-request.svg) [Support `.venv` as a file containing a path fallback mechanism for centralised project environments](https://github.com/astral-sh/uv/pull/20022) `#20022`
**ステータス:** Open | **ラベル:** なし

**概要:** 一部のファイルシステムはシンボリックリンクをサポートせず、Windows でも状況によってはジャンクションを作成できません。「現在の」環境の場所を追跡するため、`.venv` の位置に環境への絶対パスを保持するプレーンテキストファイルを作成するフォールバックを追加します。上流ツールのサポートはまだなく、現時点では uv 専用の対応です。

**直近のアクティビティ:** codspeed-hq ボットが「マージによりパフォーマンス 5.47% 低下」（1 件のベンチマークが退行）と警告し、修正または CodSpeed 上での承認を求めています。

**変更ファイル:** `uv-fs/src/lib.rs`、`project/mod.rs`、`venv.rs`、テスト群、`docs/concepts/projects/layout.md` の 7 ファイル、+344/-90。

**次のアクション:** パフォーマンス退行への対応と、上流ツール非対応の設計トレードオフのレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Add prettier install step to developer docs](https://github.com/astral-sh/uv/pull/20512) `#20512`
**ステータス:** Open | **ラベル:** documentation, internal, ep-2026-sprints

**概要:** contributing.md の Documentation 手順で prettier 未インストール時に `cargo dev generate-all` が失敗するため、Node インストールドキュメントへのリンク付きで手順を追加し npx 経由で prettier を実行できるようにします。あわせて 2 箇所で npx が取得する prettier のバージョンを固定しますが、CI の `check-fmt.yml` が定義するバージョンとの二重管理が必要になります。

**直近のアクティビティ:** 直近 24 時間のコメントなし（woodruffw にレビュー依頼）。

**変更ファイル:** `CONTRIBUTING.md`、`agents/hooks/post-edit-format.py`、`crates/uv-dev/src/generate_json_schema.rs` の 3 ファイル。

**次のアクション:** バージョン二重管理の是非を含めレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Add async tempfile creation](https://github.com/astral-sh/uv/pull/20513) `#20513`
**ステータス:** Open (Draft) | **ラベル:** ep-2026-sprints

**概要:** Issue #19429 に対する最初の試みで、非同期な一時ファイル作成を追加します。async ブロック内の `tempfile::tempfile_in` の呼び出しはまだ変更しておらず（例: `distribution_database.rs:909`）、ローカルでは corporate VPN の影響を除き cargo テストが通っています。

**直近のアクティビティ:** 直近 24 時間のコメントなし（woodruffw にレビュー依頼）。

**変更ファイル:** `crates/uv-fs/src/lib.rs` の 1 ファイル。

**次のアクション:** Draft。残りの同期呼び出しの移行とレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Use pretty json for `uv python list` & add tests](https://github.com/astral-sh/uv/pull/20522) `#20522`
**ステータス:** Open | **ラベル:** ep-2026-sprints

**概要:** `uv tool list` の JSON 出力対応中に `uv python list` にテストが無いことに気づき、非 JSON テストを複製・変更してテストを追加します。あわせて多くの箇所が既に pretty JSON を使っていることから、本コマンドも未整形から pretty JSON に切り替えます。もし変更が望ましくない場合は、テストスナップショット用に JSON を pretty 出力するユーティリティ追加を提案しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zsol の COMMENTED レビューあり）。

**変更ファイル:** `crates/uv/src/commands/python/list.rs`、`crates/uv/tests/python/python_list.rs` の 2 ファイル、+354/-1。

**次のアクション:** pretty JSON への変更方針の合意とレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Add JSON output to `uv tool upgrade`](https://github.com/astral-sh/uv/pull/20518) `#20518`
**ステータス:** Open | **ラベル:** ep-2026-sprints

**概要:** `uv tool upgrade` に `--output-format json` を追加します。JSON 出力は `schema.version` と `upgrades` 配列を持ち、各項目に `name` と `outcome`（`no_op`／`upgrade_tool`／`upgrade_dependencies`／`failed`）や制約・エラー情報を含みます。既存の `UpgradeReport` 型を拡張しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-cli/src/lib.rs`、`tool/upgrade.rs`、`uv/src/lib.rs`、`settings.rs`、`tests/tool/tool_upgrade.rs` の 5 ファイル、+632/-52。

**次のアクション:** レビューとマージ判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Add json output to `uv tool list`](https://github.com/astral-sh/uv/pull/20516) `#20516`
**ステータス:** Open | **ラベル:** ep-2026-sprints

**概要:** `uv tool list` に JSON 出力を追加します。出力は `schema.version` と `tools` 配列を持ち、各ツールの名前・バージョン・`latest_version`・パス・コマンド一覧・extras・バージョン指定子などを含みます。当初テストなしとしていましたが、スナップショットテストで容易だったため追加済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし（ThiefMaster の COMMENTED レビューあり）。

**変更ファイル:** `uv-cli/src/lib.rs`、`tool/list.rs`、`uv/src/lib.rs`、`settings.rs`、`tests/tool/tool_list.rs` の 5 ファイル、+393/-20。

**次のアクション:** レビューとマージ判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Remove "unused code" build warnings (Cargo approach))](https://github.com/astral-sh/uv/pull/20515) `#20515`
**ステータス:** Open | **ラベル:** ep-2026-sprints

**概要:** `apple-native` フィーチャ有効でビルドすると `uv-keyring` で `new_with_target` や `get_credential` が未使用という dead_code 警告が出ます。本 PR は Cargo 設定側のアプローチ（`crates/uv-keyring/Cargo.toml` の変更）でこの警告を解消します。#20514 の代替案です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-keyring/Cargo.toml` の 1 ファイル。

**次のアクション:** #20514 との方針比較とレビューが必要。

---

### ![PR](icons/git-pull-request.svg) [Remove "unused code" build warnings](https://github.com/astral-sh/uv/pull/20514) `#20514`
**ステータス:** Open | **ラベル:** ep-2026-sprints

**概要:** #20515 と同じ `apple-native` ビルド時の `uv-keyring` の dead_code 警告に対する別アプローチで、ソースコード側（`lib.rs`、`macos.rs`）を修正して警告を解消します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-keyring/src/lib.rs`、`crates/uv-keyring/src/macos.rs` の 2 ファイル。

**次のアクション:** #20515 のどちらのアプローチを採用するか判断が必要。

---

### ![PR](icons/git-pull-request.svg) [Add python interpreter info to `uv workspace metadata`](https://github.com/astral-sh/uv/pull/20502) `#20502`
**ステータス:** Merged | **ラベル:** preview

**概要:** `uv workspace metadata` に Python インタプリタ情報を追加します。`uv sync --format json` と同じ形式で、ty がこの情報を必要とする場面があるほか一般的に有用なため追加します。`--sync` を行わない `uv tree` の JSON には意図的に変更を加えていません。

**直近のアクティビティ:** 直近 24 時間のコメントなし（zanieb の APPROVED、Gankra/zanieb の COMMENTED あり）。

**変更ファイル:** `implementation.rs`、`lock/export/metadata.rs`、`project/sync.rs`、`workspace/metadata.rs`、`docs/reference/internals/metadata.md` など 9 ファイル、+85/-45。

**次のアクション:** マージ済みで追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump Hawk to 0.1.8](https://github.com/astral-sh/uv/pull/20548) `#20548`
**ステータス:** Merged | **ラベル:** internal

**概要:** CI の Hawk チェックを 0.1.7 から 0.1.8 へ更新し、新たに必須となった `check` サブコマンドに合わせて呼び出しを更新します。（後続の #20566 でさらに 0.1.9 へ更新。）

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/check-lint.yml` の 1 ファイル。

**次のアクション:** マージ済みで追加対応不要。

---

### ![Issue](icons/issue-opened.svg) [Test `uv.schema.json` against validate-pyproject](https://github.com/astral-sh/uv/issues/20549) `#20549`
**ステータス:** Open | **ラベル:** testing

**概要:** #20547 で遭遇・修正したような問題を避けるため、`uv.schema.json` を validate-pyproject でテストできると有用だという提案です。validate-pyproject がパッチ済みスキーマを受け取れるか調査が必要で、これにより自分たちの `pyproject.toml` の変更が新しいスキーマを要する別の不具合ケースも回避できるとしています。

**直近のアクティビティ:** 直近 24 時間のコメントなし（#20550 の議論から派生）。

**次のアクション:** validate-pyproject でパッチ済みスキーマを扱えるかの技術調査が必要。

---

### ![Issue](icons/issue-opened.svg) [`uv cache clean` reports unexpected result](https://github.com/astral-sh/uv/issues/20546) `#20546`
**ステータス:** Open | **ラベル:** question

**概要:** 再現手順が用意できないため question として起票された報告です。システムディスクは 30GB のみのはずなのに `uv cache clean` が 79.4GB（755204 ファイル）を削除したと報告する、という内容で、`df` の出力が併記されています。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現手順や集計ロジックの確認が必要。報告値の計算方法の調査。

---

### ![Issue](icons/issue-opened.svg) ["uv sync" workspace behavior: opt in flag for shared venv](https://github.com/astral-sh/uv/issues/20545) `#20545`
**ステータス:** Open | **ラベル:** enhancement

**概要:** #16026 で述べられているように、ワークスペースメンバーのディレクトリで `uv sync` を実行すると他メンバーが使うパッケージがアンインストールされる挙動が分かりにくいという指摘です。ドキュメントへの明記を求めるとともに、全メンバーの依存を含む共有／グローバルな `.venv` を保つオプトインフラグの追加を要望しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ドキュメントへの挙動明記の検討と、共有 venv オプトインフラグの機能要望の評価。

---

### ![Issue](icons/issue-opened.svg) [Vulnerability : Update quick-xml crate to 0.41.0](https://github.com/astral-sh/uv/issues/20542) `#20542`
**ステータス:** Open | **ラベル:** bug

**概要:** `quick-xml` クレートが 3 件の脆弱性（high 1 件、low 2 件）の影響を受けているという報告です（AIKIDO-2026-371823 ほか）。すべてを修正するには `quick-xml` を 0.41.0 に更新する必要があるとしています。報告環境は uv 0.11.29 / Python 3.14.6。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** `quick-xml` を 0.41.0 へ更新して脆弱性に対応するかの判断が必要。

---

### ![Issue](icons/issue-opened.svg) [Vulnerability : Update open crate to 5.4.0](https://github.com/astral-sh/uv/issues/20541) `#20541`
**ステータス:** Open | **ラベル:** bug

**概要:** `open` クレートが呼び出し側指定の URL やファイルパスをサニタイズせずプラットフォームランチャに渡す脆弱性（AIKIDO-2026-549490）の報告です。Unix ではダッシュ始まりのパスがオプションとして渡されフラグ注入が可能で、Windows/WSL では PowerShell の `-Command` 文字列に埋め込まれるためメタ文字が任意コマンド実行につながり得ます。5.4.0 への更新で修正されるとしています。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** `open` は #20536 で 5.4.0 に更新済みのため、本 Issue のクローズ可否を確認。

---

### ![Issue](icons/issue-opened.svg) [`uvx` complains about non-existent `--with`](https://github.com/astral-sh/uv/issues/8757) `#8757`
**ステータス:** Open | **ラベル:** help wanted, error messages

**概要:** Discord で報告された不具合で、`uvx --from ./python/ruff-ecosystem ...` のように `--from` のみを渡した場合でも、存在しない `--with` 要件が無効だというエラーメッセージが表示されます。エラーの文脈が実際に指定したフラグと一致していません。

**直近のアクティビティ:** takerukondo が対応を表明。`uvx --from ./missing-path ruff` で再現し、`--from` が明示された場合はエラー文脈を `--from` にする（＋回帰テスト追加）という修正方針を示し、まもなく PR を出すとコメント。

**次のアクション:** takerukondo による修正 PR の提出待ち。

---

## microsoft/vscode

> Agent Host・Copilot チャット・音声入力（dictation/voice mode）・Agents ウィンドウ（sessions）まわりの機能改善とバグ修正が活発で、加えて安定版に取り込まれた古いバグの一括クローズも多く見られます。

### ![Issue](icons/issue-opened.svg) [Failed to import session from cloud](https://github.com/microsoft/vscode/issues/326732) `#326732`
**ステータス:** Open | **ラベル:** new release

**概要:** クラウドからセッションをインポートしようとしてモデルを選び実行すると「Failed to import session from cloud」エラーになるという不具合報告です。VS Code 1.129.1、Windows 環境で発生しています。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 再現条件の切り分けとエラー原因の調査が必要です。

---

### ![Issue](icons/issue-opened.svg) [Using alt to debug a test no longer works](https://github.com/microsoft/vscode/issues/326724) `#326724`
**ステータス:** Open | **ラベル:** bug, testing

**概要:** 以前はガター上の実行ボタンを alt 押下でクリックするとテストをデバッグ実行できたが、アイコンはデバッグ用に変わるものの実際には「条件付きブレークポイントの追加」相当の動作になってしまうという回帰報告です。

**直近のアクティビティ:** bot が旧バージョン利用の可能性を指摘し最新版への更新を促し、報告者は @connor4312 をメンションしました。

**次のアクション:** テストのデバッグ実行ジェスチャの回帰調査が必要です。

---

### ![PR](icons/git-pull-request.svg) [agentHost: fix extraneous github login prompt for enterprise](https://github.com/microsoft/vscode/pull/326731) `#326731`
**ステータス:** Open | **ラベル:** なし

**概要:** GitHub MCP サーバー URL の解決時に、デフォルト URL と解決済み URL の両方をチェックすることで、エンタープライズユーザーへの不要なログインプロンプトを防ぐ修正です。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています。

**変更ファイル:** `copilotAgentSession.ts` 1 ファイルの小規模修正（+5/-3）。

**次のアクション:** レビュー対応とマージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [refactor(claude): make the pipeline the sole owner of the WarmQuery (handoff)](https://github.com/microsoft/vscode/pull/326561) `#326561`
**ステータス:** Open | **ラベル:** なし

**概要:** `WarmQuery` のライフサイクル所有者をパイプラインに一本化するリファクタリングです（#326558 にスタック）。`_installPipeline` が無条件にパイプラインを構築し、abort/dispose ゲートを通すことで、レース時の破棄をパイプライン側の teardown に委ねます。

**直近のアクティビティ:** 直近 24 時間のコメントなし（ドラフト）。

**変更ファイル:** `claudeAgentSession.ts` と e2e スモークテストの 2 ファイル（+71/-39）。

**次のアクション:** レビューとスタック元 PR のマージ後の取り込みが必要です。

---

### ![PR](icons/git-pull-request.svg) [agentHost: expand end-to-end coverage](https://github.com/microsoft/vscode/pull/326720) `#326720`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host の共有 E2E シナリオを 33 件追加し、ホスト入力機能・ファイル補完・ローカルコマンド・セッション/チャット状態・設定・アクティブクライアント・ターミナルライフサイクルをカバーします。Claude/Codex/Copilot 向けの決定論的なゼロモデル replay フィクスチャと AHP スナップショットも追加。行/文カバレッジが 68.64%→69.95% に向上しています。

**直近のアクティビティ:** github-actions[bot]がスクリーンショット差分レポート（1 件変更）を投稿しています。

**変更ファイル:** e2e キャプチャの YAML やハーネスなど 116 ファイルの大規模追加（+1733/-372）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [Bump github/copilot 1.0.72](https://github.com/microsoft/vscode/pull/326704) `#326704`
**ステータス:** Open | **ラベル:** なし

**概要:** github/copilot を 1.0.72 にバージョンアップする依存更新 PR です。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `package.json`/`package-lock.json` 群や CLI SDK アップグレードテストなど 10 ファイル（+118/-129）。

**次のアクション:** ドラフト解除とマージ判断が必要です。

---

### ![Issue](icons/issue-opened.svg) [Cursor jumps to the line above after accepting an inline completion that adds an auto-import](https://github.com/microsoft/vscode/issues/326730) `#326730`
**ステータス:** Open | **ラベル:** なし

**概要:** `editor.quickSuggestions` を inline に設定した状態で、未インポート識別子のインライン補完を Tab で受理し auto-import が追加されると、カーソルが 1 行上にジャンプする不具合報告です。macOS、VS Code 1.129.1 で再現します。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** auto-import 挿入時のカーソル位置調整の調査が必要です。

---

### ![PR](icons/git-pull-request.svg) [sessions: toggle editor pane from secondary sidebar command](https://github.com/microsoft/vscode/pull/326729) `#326729`
**ステータス:** Open | **ラベル:** なし

**概要:** セカンダリサイドバーの共有コマンドをレイアウトサービスの意味的操作に委譲し、クラシックレイアウトでは補助バー、単一ペインの Agents レイアウトではエディタペインへマッピングする変更です。可視性のアナウンスは各レイアウト実装側に残します。

**直近のアクティビティ:** github-actions[bot]のスクリーンショット差分レポートと、vs-code-engineering[bot]の CODENOTIFY（@benibenj 宛）が投稿されています。

**変更ファイル:** `singlePaneWorkbench.ts`・`layout.ts`・`auxiliaryBarActions.ts` など 10 ファイル（+83/-13）。

**次のアクション:** レビュー対応が必要です。

---

### ![Issue](icons/issue-opened.svg) [Test: View and edit Markdown in the Agents window](https://github.com/microsoft/vscode/issues/326540) `#326540`
**ステータス:** Closed | **ラベル:** testplan-item

**概要:** Agents ウィンドウの実験的ハイブリッドエディタで Markdown（見出し・強調・リンク・タスクリスト・表・コードフェンス・数式・Mermaid）を表示/編集するテストプラン項目です。`workbench.editor.markdownDefaultEditorInAgentsWindow` を前提とします。

**直近のアクティビティ:** 直近 24 時間のコメントなし（両 OS のチェック完了済み）。

**次のアクション:** テスト完了済み。派生した不具合 Issue 群の対応に移行します。

---

### ![Issue](icons/issue-opened.svg) [Strange rendering of SKILL.md in MD editor](https://github.com/microsoft/vscode/issues/326728) `#326728`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウのハイブリッド MD エディタで `SKILL.md` を開くと、黄色いタグ `unhandled: settextHeading` が表示されるという描画不具合です（#326540 のテスト中に発見）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** ハイブリッド MD エディタの見出しレンダリング処理の修正が必要です。

---

### ![Issue](icons/issue-opened.svg) [Make horizontal cursor scroll padding configurable](https://github.com/microsoft/vscode/issues/131203) `#131203`
**ステータス:** Open | **ラベル:** feature-request, editor-rendering

**概要:** テキストカーソル移動時の水平・垂直スクロールのパディング量（文字数・行数）を設定可能にしてほしいという要望です。

**直近のアクティビティ:** alexdima が、`editor.scrollBeyondLastColumn` はスクロール可能幅にのみ影響しカーソル reveal のパディングは制御しないと指摘し、設定可能な水平カーソルパディングに論点を絞って再オープンしました。

**次のアクション:** 設定可能な水平カーソルパディングの実装検討が必要です。

---

### ![Issue](icons/issue-opened.svg) [Drag & drop does not work from MD editor](https://github.com/microsoft/vscode/issues/326727) `#326727`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウのハイブリッドモード MD ドキュメントでテキストを選択してチャットへドラッグ&ドロップしようとしても動作しないという報告です（#326540 のテスト中）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** MD エディタからの D&D 対応の調査が必要です。

---

### ![Issue](icons/issue-opened.svg) [Markdown editor context/selection is not added to chat](https://github.com/microsoft/vscode/issues/326726) `#326726`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウのハイブリッド MD エディタでテキストを選択しても、その選択範囲がチャットのコンテキストとして添付されないという報告です（#326540 のテスト中）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** MD エディタ選択範囲のチャットコンテキスト連携の実装が必要です。

---

### ![Issue](icons/issue-opened.svg) [Markdown Editor renders in the tab space instead of document spave](https://github.com/microsoft/vscode/issues/326725) `#326725`
**ステータス:** Open | **ラベル:** なし

**概要:** 未保存の MD ドキュメントを持つセッションを切り替えて戻ると、Markdown エディタがドキュメント領域ではなくタブヘッダー領域に描画され、切り替え後も未保存ドキュメントが開いたままになるという報告です（auto-save オフ時）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** セッション切り替え時のエディタ描画位置の修正が必要です。

---

### ![Issue](icons/issue-opened.svg) [Dirty model is cleared when closing without save](https://github.com/microsoft/vscode/issues/326723) `#326723`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウのハイブリッドエディタで MD を変更し保存せず閉じようとすると、保存確認ダイアログは出るものの既に dirty 状態がクリアされており、Cancel を押してもファイルが dirty でなくなるという報告です。

**直近のアクティビティ:** dmitrivMS が「auto-save が働いているのかもしれない」とコメントしています。

**次のアクション:** 閉じる操作時の dirty 状態管理の調査が必要です。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Add telemetry for applied enterprise policies](https://github.com/microsoft/vscode/pull/326716) `#326716`
**ステータス:** Open | **ラベル:** cherry-pick-artifact

**概要:** エンタープライズポリシー適用状況を把握するための `policy.applied` テレメトリイベントを追加する #326686 のチェリーピックです。ワークベンチ復元後と有効ポリシー状態変化時に発火し、500ms デバウンスと重複抑制を行い、適用ポリシー数や各種ポリシー適用有無を報告します。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のコメントに加え、roblourens と eleanorjboyd が Approve しています。

**変更ファイル:** `policyTelemetry.contribution.ts` とそのテスト、`workbench.common.main.ts` の 3 ファイル（+320/-0）。

**次のアクション:** マージして安定版へ取り込みます。

---

### ![PR](icons/git-pull-request.svg) [Chat dictation: always use Nemotron model, remove model setting](https://github.com/microsoft/vscode/pull/326718) `#326718`
**ステータス:** Merged | **ラベル:** なし

**概要:** オンデバイスのチャット dictation ランタイムは Microsoft Foundry Local のストリーミング ASR エンジンで、対応モデルは NVIDIA Nemotron（`nemotron-speech-streaming-en-0.6b`）のみのため、`chat.speechToText.model` 設定を廃止し Nemotron をハードコードします。旧 Whisper のモデル ID も解決不能になっています。

**直近のアクティビティ:** 直近 24 時間のコメントなし（eleanorjboyd・roblourens が Approve 済み）。

**変更ファイル:** `localTranscription` 関連やチャット speechToText サービスなど 5 ファイル（+15/-50）。

**次のアクション:** マージ済み。設定移行の周知が必要です。

---

### ![Issue](icons/issue-opened.svg) [Assisted approvals changing repo visibility](https://github.com/microsoft/vscode/issues/326693) `#326693`
**ステータス:** Open | **ラベル:** なし

**概要:** アシスト承認がリポジトリの可視性変更を承認なしに試み、失敗すると別の API で再試行し、その後に確認要求が出たという報告です（#326555 のテスト中）。スキップ後も再度確認なしで試行したとされています。

**直近のアクティビティ:** JonahAlexanian が承認外の不可逆操作を問題視するコメント、osortega がスキップ後も再試行された旨をスクリーンショット付きで報告しています。

**次のアクション:** 破壊的操作前の承認ゲートの見直しが必要です。

---

### ![Issue](icons/issue-opened.svg) [broke my code entirely](https://github.com/microsoft/vscode/issues/324762) `#324762`
**ステータス:** Closed | **ラベル:** chat-billing

**概要:** 仕上げ段階だったコードが AI により大きく壊され、追加利用枠まで使い切ってしまったという苦情で、リセットを求めています。

**直近のアクティビティ:** JonahAlexanian が、証拠に基づいて編集をゲートするツールを使っている旨のコメントをしています。

**次のアクション:** クローズ済み。課金/利用枠に関する問い合わせ対応です。

---

### ![Issue](icons/issue-opened.svg) [No way to "uninstall" local plugin folder](https://github.com/microsoft/vscode/issues/326363) `#326363`
**ステータス:** Open | **ラベル:** なし

**概要:** フォルダ経由で追加したプラグインが customizations に残り続け、無効化はできてもアンインストールできないという報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** ローカルプラグインフォルダのアンインストール手段の提供検討が必要です。

---

### ![PR](icons/git-pull-request.svg) [fix: prevent multi-diff phantom headers while scrolling](https://github.com/microsoft/vscode/pull/326722) `#326722`
**ステータス:** Open | **ラベル:** なし

**概要:** マルチ diff の仮想化で画面外の行テンプレートの非表示化が autorun まで遅延し、高速スクロール時に古い DOM が残り重複/細切れのヘッダーが出る問題（#326702）を修正します。`VirtualizedViewItem.hide()` が即座に DOM を隠しテンプレートをオブジェクトプールへ同期返却するようにします。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています。

**変更ファイル:** `diffEditorItemTemplate.ts`・`multiDiffEditorWidgetImpl.ts`・`objectPool.ts` とそのテストの 4 ファイル（+109/-1）。

**次のアクション:** レビューとマージ判断が必要です。

---

### ![Issue](icons/issue-opened.svg) [Clicking on the signed in user menu doesn't expand its sub menu](https://github.com/microsoft/vscode/issues/326714) `#326714`
**ステータス:** Closed | **ラベル:** bug, *duplicate

**概要:** サインイン中のユーザーメニューをクリックしてもサブメニューが展開されず、サインアウトできないという不具合報告です（Insiders 1.130.0）。

**直近のアクティビティ:** bot が重複としてクローズし、gjsjohnmurray が `/duplicate` と #326317 のワークアラウンドを案内しました。

**次のアクション:** クローズ済み（#326317 で対応）。

---

### ![Issue](icons/issue-opened.svg) [Voice mode: pressing voice mode button while a session is in progress does nothing (spins continuously)](https://github.com/microsoft/vscode/issues/326033) `#326033`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, voice-mode

**概要:** セッション進行中に voice mode ボタンを押しても何も起きずボタンが回転し続けるという不具合報告です。進行中の期待動作の明確化を求めています。

**直近のアクティビティ:** dmitrivMS が「現在はメッセージをキューイングするようになった」と画像付きでコメントしています。

**次のアクション:** クローズ済み（キューイング動作で解消）。

---

### ![Issue](icons/issue-opened.svg) [Voice mode: manually typed text is dropped when turning on voice mode in a new chat](https://github.com/microsoft/vscode/issues/326034) `#326034`
**ステータス:** Open | **ラベル:** bug, verification-found, voice-mode

**概要:** 新規チャットでテキストを一部入力し画像を貼り付けた後、voice mode をオンにして音声で追記して送信すると、手入力テキストが失われ音声部分のみが送信されるという不具合です。

**直近のアクティビティ:** dmitrivMS が期待どおり動かない事例を報告し、動作するものの別の問題（#326721）が発生する旨をコメントしています。

**次のアクション:** 手入力テキストと音声入力のマージ処理の修正が必要です。

---

### ![PR](icons/git-pull-request.svg) [agentHost: Handle subagent user messages safely](https://github.com/microsoft/vscode/pull/326711) `#326711`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot SDK が subagent 向けに発する `user.message` イベントを、ルートステアリング・SDK イベント境界追跡・命令テレメトリで無視し、永続化された subagent ユーザーメッセージを子ターンのプロンプトとして復元する変更です。`agentId` 未処理だとルートターン状態を汚染する恐れがありました。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]と roblourens がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `copilotAgentSession.ts`・`mapSessionEvents.ts` とテスト群の 5 ファイル（+89/-26）。

**次のアクション:** レビュー対応と、より広範な監査（#326710）との連携が必要です。

---

### ![Issue](icons/issue-opened.svg) [bad_request: the model cannot be changed after a WebSocket connection is established](https://github.com/microsoft/vscode/issues/326712) `#326712`
**ステータス:** Open | **ラベル:** なし

**概要:** カスタムチャットエージェントが `runSubagent` で subagent を呼ぶ際、親セッションが GPT-5.6 Luna、subagent が GPT-5.6 Terra を宣言していると「the model cannot be changed after a WebSocket connection is established」という CAPI エラーが出るという報告です。

**直近のアクティビティ:** peakyblinders-tm が「自分も同様」とコメントしています（スパム様の文字列を含む）。

**次のアクション:** subagent のモデル切り替えと WebSocket 接続の整合性の調査が必要です。

---

### ![PR](icons/git-pull-request.svg) [https://gmashrsa.com](https://github.com/microsoft/vscode/pull/325898) `#325898`
**ステータス:** Open | **ラベル:** なし

**概要:** README.md のみを変更する PR で、本文には「新規 Issue を開く GitHub Actions ワークフローを追加した」と記載されていますが、実際の変更内容と説明が一致しておらずスパム/不正の疑いがあります。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています。

**変更ファイル:** `README.md` 1 ファイル（+15/-78）。

**次のアクション:** スパム/不正 PR としてクローズ判断が必要です。

---

### ![Issue](icons/issue-opened.svg) [Text from voice is duplicated in chat input](https://github.com/microsoft/vscode/issues/326721) `#326721`
**ステータス:** Closed | **ラベル:** bug, workbench-voice

**概要:** エディタウィンドウの新規チャットで画像を貼り「What is」と入力後、マイクで音声認識させて即 Send すると、認識テキストがチャット入力欄に複数回重複するという不具合です（#326034 のテスト中）。

**直近のアクティビティ:** meganrogge が「これは明日の Insiders で修正される」とコメントしています。

**次のアクション:** クローズ済み（Insiders で修正予定）。

---

### ![Issue](icons/issue-opened.svg) [Surface AH MCP disablement in a better way](https://github.com/microsoft/vscode/issues/326719) `#326719`
**ステータス:** Open | **ラベル:** feature-request, agent-host

**概要:** Agent Host の MCP 無効化状態を UI でより目立つ形で表示してほしいという要望で、GitHub MCP サーバーがサードパーティ扱いになるのは意外だという指摘も含みます（#326073 のコメントから派生）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** MCP 無効化状態の UI 表示改善の設計が必要です。

---

### ![Issue](icons/issue-opened.svg) [MCP servers for custom agents from copilot CLI don't start](https://github.com/microsoft/vscode/issues/318589) `#318589`
**ステータス:** Open | **ラベル:** bug, agent-host-mitigated

**概要:** copilot CLI のカスタムエージェント用 MCP サーバーが起動しないという不具合報告です。

**直近のアクティビティ:** Cosifne が @connor4312 に修正の目処を尋ねています。

**次のアクション:** カスタムエージェントの MCP サーバー起動不具合の対応方針提示が必要です。

---

### ![Issue](icons/issue-opened.svg) [Ctrl+I does not work in Agents window](https://github.com/microsoft/vscode/issues/326694) `#326694`
**ステータス:** Closed | **ラベル:** bug, unreleased, workbench-voice

**概要:** Windows の Agents ウィンドウでチャット入力に Ctrl+I を押しても何も起きない（VS Code チャットエディタでは動作する）という不具合報告です（#326388 のテスト中）。

**直近のアクティビティ:** meganrogge が「これは dictation 用」とコメントしています。

**次のアクション:** クローズ済み（#326697 で修正）。

---

### ![PR](icons/git-pull-request.svg) [Fix Cmd/Ctrl+I dictation in the Agents window composer](https://github.com/microsoft/vscode/pull/326697) `#326697`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents ウィンドウの composer（`NewChatInputWidget`）で Cmd/Ctrl+I が効かなかった問題を修正します。原因は raw editor の `onKeyDown` で chord を処理していたため `editor.action.triggerSuggest` にキーが奪われていたことで、メインウィンドウ同様に keybinding service 経由（`WorkbenchContrib + 1`）で登録します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（meganrogge・dmitrivMS が Approve 済み）。

**変更ファイル:** `newChatInput.ts`・`sessionsChatAccessibilityHelp.ts`・`chatSpeechToTextActions.ts` の 3 ファイル（+128/-73）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [ЕБАНЫЙ ИСКУСТВЕННЫЙ ИНТЕЛЛЕКТ (spam)](https://github.com/microsoft/vscode/issues/326717) `#326717`
**ステータス:** Closed | **ラベル:** spam

**概要:** Copilot 補完に対する罵倒的な内容で、コミュニティガイドラインに適合しないスパムとしてクローズされた投稿です。

**直近のアクティビティ:** bot がスパム/ガイドライン違反としてクローズし、旧バージョン利用の可能性も案内しています。

**次のアクション:** クローズ済み（対応不要）。

---

### ![PR](icons/git-pull-request.svg) [sessions: scope git availability to each session surface](https://github.com/microsoft/vscode/pull/326420) `#326420`
**ステータス:** Merged | **ラベル:** なし

**概要:** リポジトリピッカーの可視性がグローバルなアクティブセッションで決まっていた問題（#326396 のフォローアップ）を修正し、各ツールバーのスコープ付きセッション単位で判定するようにします。`ISession.hasGitRepository` observable を追加し、root と各セッションのコンテキストキーサービスに独立して設定します。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY（@lszomoru 宛）を投稿しています（dmitrivMS が Approve 済み）。

**変更ファイル:** `contextkeys.ts`・`session.ts`・`visibleSessions.ts` など 11 ファイル（+113/-57）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat Sessions list missing Claude Agent SDK sessions after updating to latest release.](https://github.com/microsoft/vscode/issues/326630) `#326630`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.129.0 から 1.129.1 への更新後、Claude Agent SDK の過去セッションがセッション一覧に表示されなくなったという不具合です（新規セッションは表示、ファイルはディスク上に残存）。Remote SSH（Windows→Linux）で `ms-vscode.vscode-claude-sdk` を使用しています。

**直近のアクティビティ:** intelligent-box が 4 つの設定を明示的にピン留めすると一覧が回復するワークアラウンド、A/B 実験フライト情報、`reloadWindow` で新規 SDK セッションも失われる点を報告しています。

**次のアクション:** 更新後のセッション一覧の欠落原因の調査が必要です。

---

### ![Issue](icons/issue-opened.svg) [Dictation text shows up again](https://github.com/microsoft/vscode/issues/326672) `#326672`
**ステータス:** Closed | **ラベル:** bug, unreleased, workbench-voice

**概要:** 録音開始→停止→テキスト削除→再度録音開始すると、古いトランスクリプトが再表示されるという不具合報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（#326673 で修正）。

---

### ![PR](icons/git-pull-request.svg) [Reset dictation transcript state on session teardown](https://github.com/microsoft/vscode/pull/326673) `#326673`
**ステータス:** Merged | **ラベル:** なし

**概要:** オンデバイス dictation のトランスクリプト状態（`_finalizedText`/`_deltaText`）のクリアが session ライフサイクル間で一貫せず、通常の停止後に前回分が残る問題を修正します。全ての session 終了経路が通る `_teardown()` にリセットを集約します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（meganrogge・pwang347・anthonykim1 が Approve 済み）。

**変更ファイル:** `chatSpeechToTextService.ts` 1 ファイル（+3/-6）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [agentHost: make the orchestrator own sessions; providers keep exact chat bindings](https://github.com/microsoft/vscode/pull/326620) `#326620`
**ステータス:** Open | **ラベル:** なし

**概要:** オーケストレータ（`AgentService`）をセッション ID・チャットカタログ/メンバーシップ・ライフサイクル・永続化の権威とし、Claude/Copilot/Codex プロバイダはフラットな `chat URI -> SDK conversation` バインディングのみ保持する構造的リファクタリングです。プロトコル・DB スキーマ・マイグレーションの変更はありません。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `agentService.ts`・`agentPeerChats.ts` や各プロバイダなど 21 ファイルの大規模変更（+2838/-1896）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [Floating chat-input window with multi-session routing](https://github.com/microsoft/vscode/pull/326698) `#326698`
**ステータス:** Open | **ラベル:** なし

**概要:** フローティングかつフレームレス・常時最前面のチャット入力ウィンドウを追加し、dictation・voice mode・入力グローを持つ入力専用 `ChatWidget` をホストします。送信内容は新しい `ISessionRouter` で最適な既存セッションへルーティングされ、コマンド「Toggle Floating Chat Input Window」で切り替えます。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `chatInputWindowService.ts`・`sessionRouterService.ts`・`chatWidget.ts` など 14 ファイル（+1041/-7）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![Issue](icons/issue-opened.svg) [Focus moves to static option list items but no visible focus indicator is present (A11y MCP elicitation)](https://github.com/microsoft/vscode/issues/321134) `#321134`
**ステータス:** Open | **ラベル:** bug, accessibility-sla, chat-ask

**概要:** Copilot チャットの elicitation フォームで、テキストや見出しなどの静的コンテンツが Tab フォーカス対象になり、かつ可視のフォーカスインジケータがないというアクセシビリティ不具合（WCAG 2.4.3 関連）です。

**直近のアクティビティ:** mrleemurray が最新 Insiders の Dark テーマで依然再現する旨を動画付きで報告しています。

**次のアクション:** 静的コンテンツをタブ順から外す/フォーカスインジケータ表示の修正が必要です。

---

### ![PR](icons/git-pull-request.svg) [editTelemetry: unify long-term edit source tracking](https://github.com/microsoft/vscode/pull/326403) `#326403`
**ステータス:** Open | **ラベル:** なし

**概要:** ローカルテキストモデル編集と Agent Host のファイル編集について、長期の `editTelemetry.editSources.details` 追跡を 1 つのワークベンチ所有のリソース単位ストリームに統合します。Agent Host の `ChatToolCallComplete` スナップショットを正規化・重複排除し、ディスク内容と突き合わせて既存の `DocumentEditSourceTracker` に射影します。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `unifiedDocument*`・`editSourceTracking*` など 19 ファイル（+3286/-83）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [Voice: clear routing/pending state on disconnect and remove idle glow](https://github.com/microsoft/vscode/pull/326696) `#326696`
**ステータス:** Merged | **ラベル:** なし

**概要:** `VoiceSessionController.disconnect()` がユーザー選択のルーティング先（`_targetSession`）や保留中確認スナップショット（`_pendingToolConfirmations`）をクリアしていなかったため、別セッションから再接続すると前回のセッションに voice が再ピン留めされる問題を修正します。あわせてアイドル時のグローを除去します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（dmitrivMS が Approve 済み）。

**変更ファイル:** `voiceSessionController.ts`・`voiceGlow.ts`・`chatView.ts` など 12 ファイル（+200/-146）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Dictation: show download progress ring instead of spinning mic](https://github.com/microsoft/vscode/pull/326709) `#326709`
**ステータス:** Merged | **ラベル:** なし

**概要:** 初回 dictation 時のオンデバイス音声モデルのダウンロード中に、回転マイクと通知の代わりに、`cloud-download` アイコンと進捗リングを表示する UX 改善です。バイト総数が不明な間は不確定表示にフォールバックします。

**直近のアクティビティ:** 直近 24 時間のコメントなし（dmitrivMS が Approve 済み）。

**変更ファイル:** `dictationDownloadRing.ts`・`dictationDownloadActionViewItem.ts` など 11 ファイル（+360/-26）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Bump axios from 1.16.0 to 1.18.1](https://github.com/microsoft/vscode/pull/326691) `#326691`
**ステータス:** Closed | **ラベル:** javascript, dependencies

**概要:** dependabot による axios を 1.16.0 から 1.18.1 に上げる依存更新 PR です（Node HTTP アダプタ修正や AxiosError シリアライズの安全化などを含む）。

**直近のアクティビティ:** rzhao271 のコメント後、dependabot[bot]が今後この更新の通知はしない旨を返しています。

**変更ファイル:** `package-lock.json`・`pnpm-lock.yaml` の 2 ファイル（+1293/-342）。

**次のアクション:** クローズ済み（未マージ）。

---

### ![PR](icons/git-pull-request.svg) [Add telemetry for applied enterprise policies](https://github.com/microsoft/vscode/pull/326686) `#326686`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** エンタープライズポリシー適用状況を把握する `policy.applied` テレメトリイベントを追加します。ワークベンチ復元後と状態変化時に発火し、500ms デバウンス・重複抑制のうえ、適用ポリシー総数や各種ポリシーの適用有無・低カーディナリティの値バケットを報告します。

**直近のアクティビティ:** vs-code-engineering[bot]が「マージ時に `release/1.130` へ自動チェリーピックされる」旨をコメントしています（dmitrivMS が Approve 済み）。

**変更ファイル:** `policyTelemetry.contribution.ts` とテスト、`workbench.common.main.ts` の 3 ファイル（+320/-0）。

**次のアクション:** マージ済み（#326716 でチェリーピック）。

---

### ![PR](icons/git-pull-request.svg) [ah: fix client tool call confirmations](https://github.com/microsoft/vscode/pull/326685) `#326685`
**ステータス:** Open | **ラベル:** なし

**概要:** クライアント提供ツールが Allow All / Assisted Permissions モードでも、Agent Host が既に呼び出しを承認済みなのに確認プロンプトを表示してしまう問題を修正します。原因は Agent Host のプロトコル上の承認表現にありました。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `agentHostSessionHandler.ts` とテストの 2 ファイル（+174/-15）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [chore: fix telemetry-extractor versions](https://github.com/microsoft/vscode/pull/326193) `#326193`
**ステータス:** Merged | **ラベル:** なし

**概要:** CI ワークフロー（`.github/workflows/pr.yml`）の telemetry-extractor のバージョンを修正する小さな変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし（lszomoru・dmitrivMS が Approve 済み）。

**変更ファイル:** `.github/workflows/pr.yml` 1 ファイル（+1/-1）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat: Confusing error when BYOK utility model not configured](https://github.com/microsoft/vscode/issues/326715) `#326715`
**ステータス:** Open | **ラベル:** なし

**概要:** BYOK モデルをメインエージェントモデルにして Agent モードに切り替えると「No utility model is configured for 'copilot-utility-small'...」というエラーが出るが、設定方法（`chat.byokUtilityModelDefault`）を案内しないため分かりにくい、という改善要望です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** エラーメッセージへの設定案内追加が必要です。

---

### ![PR](icons/git-pull-request.svg) [JSON - support ${workspaceFolder} in json.schemas fileMatch patterns](https://github.com/microsoft/vscode/pull/326706) `#326706`
**ステータス:** Open | **ラベル:** なし

**概要:** `json.schemas` の `fileMatch` パターン内の先頭 `${workspaceFolder}`（`!` 除外プレフィックスの後も含む）をクライアント側で展開し、サーバー API は変更しない実装です（#44135 の対応）。展開はフォルダが一意に定まるスコープに限定します。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]と soreavis がレビューコメントを付けています。

**変更ファイル:** `jsonClient.ts`・`package.nls.json` の 2 ファイル（+26/-6）。

**次のアクション:** レビュー対応とマージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [Fix timeline rail to render next to the native scrollbar](https://github.com/microsoft/vscode/pull/326707) `#326707`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットセッションのタイムラインレールを、ネイティブスクロールバーを置き換えるのではなく隣に描画するよう修正します。レール用オフセットの CSS 変数を導入し、ネイティブスクロールバーやカスタムサム/スクラブ関連のロジックを削除します。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のコメントに加え、dmitrivMS が Approve しています。

**変更ファイル:** `promptTimelineRail.ts`・`promptTimelineRulerRail.ts` など 4 ファイル（+47/-208）。

**次のアクション:** マージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [Use Copilot SDK APIs to discover customizations](https://github.com/microsoft/vscode/pull/325241) `#325241`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot SDK の API を用いてセッションのカスタマイズを検出する実装です（本文説明なし）。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のコメントと dmitrivMS の Approve があります。

**変更ファイル:** `sessionCustomizationDiscovery.ts`・`copilotAgent.ts`・`copilotRCP.ts` など 8 ファイル（+2044/-479）。

**次のアクション:** マージ判断が必要です。

---

### ![Issue](icons/issue-opened.svg) [Agents window keybinding rendering in release notes](https://github.com/microsoft/vscode/issues/325326) `#325326`
**ステータス:** Closed | **ラベル:** bug, verified, release-notes, insiders-released

**概要:** Agents ウィンドウ固有のキーバインドが Web サイトやエディタウィンドウのリリースノートに描画されないという不具合報告です。

**直近のアクティビティ:** rzhao271 が 1.128 のリリースノートを後のビルドで読み込む必要があると確認し、sbatten が @alexr00 に検証方法を尋ねています。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix voice barge in regression](https://github.com/microsoft/vscode/pull/326611) `#326611`
**ステータス:** Merged | **ラベル:** なし

**概要:** 中断された応答の遅延オーディオが再生を再開してしまう voice の barge-in 競合を修正します。バックエンドのターン ID を保持し、中断ストリームのキュー済み/遅延/遅着オーディオを破棄しつつ、新しいトランスクリプトとチャットコマンドは正常に続行させます。

**直近のアクティビティ:** 直近 24 時間のコメントなし（meganrogge・dmitrivMS が Approve 済み）。

**変更ファイル:** `voiceClientService.ts`・`voiceSessionController.ts` とテストの 5 ファイル（+599/-73）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [NES: split open-tab vs related-file neighbor context](https://github.com/microsoft/vscode/pull/326626) `#326626`
**ステータス:** Open | **ラベル:** なし

**概要:** xtab（NES / Next Edit Suggestions）の近傍ファイルコンテキストについて、開いているタブ由来のスニペットと言語サービスの「related」ファイル由来のスニペットを区別する 2 つの改善です。related ファイル由来のスニペットはプロンプトから行番号を省略し、NES 自身の言語コンテキスト経路と整合させます。

**直近のアクティビティ:** ulugbekna が複数のレビューコメントを付けています。

**変更ファイル:** `neighborFiles.ts`・`similarFiles.ts`・`xtabProvider.ts` など 13 ファイル（+257/-30）。

**次のアクション:** レビュー対応が必要です（ドラフト）。

---

### ![Issue](icons/issue-opened.svg) [VSCode cannot open SSH workspace](https://github.com/microsoft/vscode/issues/326093) `#326093`
**ステータス:** Open | **ラベル:** なし

**概要:** `apt install ./code_1.128.1...deb` で更新後、「Open SSH Connection」の段階で停止し動作しなくなるという Remote SSH の不具合報告です（Linux）。

**直近のアクティビティ:** kycutler が 1.129.1 の類似報告を紹介し、joshspicer が 1.129.1 での再現有無を確認し、影響を受ける人にログの共有を依頼しています。

**次のアクション:** 1.129.1 での再現確認とログ収集による調査が必要です。

---

### ![Issue](icons/issue-opened.svg) [Agents - primary action tooltip contains icon code](https://github.com/microsoft/vscode/issues/325540) `#325540`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, agents-window

**概要:** Agents のプライマリアクションのツールチップにアイコンコードがそのまま表示される不具合報告です。

**直近のアクティビティ:** jruales がスクリーンショットを追加しています。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Allow custom device presets in integrated browser's "Emulate Device..." picker](https://github.com/microsoft/vscode/issues/326713) `#326713`
**ステータス:** Open | **ラベル:** なし

**概要:** 統合ブラウザの `Emulate Device...`（`workbench.action.browser.pickDevicePreset`）がソース直書きの 4 プリセット固定なので、Chrome DevTools のようにユーザー独自の名前付きデバイスプリセットを `settings.json` で追加できるようにしてほしいという要望です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** カスタムデバイスプリセット設定の実装検討が必要です。

---

### ![Issue](icons/issue-opened.svg) [Subtle flickering of UI elements on VS code under Windows 11](https://github.com/microsoft/vscode/issues/286923) `#286923`
**ステータス:** Open | **ラベル:** info-needed, windows, gpu

**概要:** Windows 11 上で VS Code の UI 要素が微妙にちらつくという不具合報告です。

**直近のアクティビティ:** mfmayer が Linux でも同様の現象を独立して確認したとコメントしています。

**次のアクション:** GPU/描画まわりのちらつきの追加情報収集と調査が必要です。

---

### ![Issue](icons/issue-opened.svg) [Updating additional spend doesnt update the UI](https://github.com/microsoft/vscode/issues/322402) `#322402`
**ステータス:** Closed | **ラベル:** bug, upstream, verified, UBB

**概要:** 追加利用枠（additional spend）を設定ページで増やしても VS Code 側の UI が更新されず、進捗が 100% のまま固定されるという不具合報告です。

**直近のアクティビティ:** eleanorjboyd が「@cwebster-99 が先週検証できた」とコメントしています。

**次のアクション:** クローズ済み（upstream 側で対応）。

---

### ![Issue](icons/issue-opened.svg) [Agent Host: GitHub MCP initial-auth token reuse doesn't support GitHub Enterprise (hardcoded MCP server URL)](https://github.com/microsoft/vscode/issues/326073) `#326073`
**ステータス:** Closed | **ラベル:** bug, verification-steps-needed, insiders-released, agent-host

**概要:** #325719 で Agent Host が GitHub MCP サーバーの初回認証に既存トークンを再利用するようになったが、対象判定に `DEFAULT_GITHUB_MCP_SERVER_URL = 'https://api.githubcopilot.com/mcp'` というハードコード定数をマッチさせているため、GitHub Enterprise の MCP サーバー URL では機能しないという不具合です。

**直近のアクティビティ:** lramos15・connor4312・TylerLeonhardt らが 6 件のコメントで議論し、GHE MCP はビルトインで手動インストール不要である点、必要な 2 つの設定、ポリシー設定が原因の可能性、デバッグログの共有依頼などをやり取りしています。

**次のアクション:** クローズ済み。関連の UI 改善要望（#326719）や修正 PR（#326731）へ引き継がれています。

---

### ![PR](icons/git-pull-request.svg) [Change the default max result to 100](https://github.com/microsoft/vscode/pull/326648) `#326648`
**ステータス:** Merged | **ラベル:** なし

**概要:** `findTextInFilesTool` のデフォルト最大結果数を 100 に変更します（#324242 の実験を受けたもの）。

**直近のアクティビティ:** 直近 24 時間のコメントなし（anthonykim1 が Approve 済み）。

**変更ファイル:** `findTextInFilesTool.tsx`・`configurationService.ts`・`package.json`・`package.nls.json` の 4 ファイル（+4/-4）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [[AHP/CLI:COGS] grep_search Different default result size values](https://github.com/microsoft/vscode/issues/324242) `#324242`
**ステータス:** Open | **ラベル:** なし

**概要:** `grep_search` ツールのデフォルト結果サイズが 20 のため、LLM が結果サイズを指定しないと 20 件しか返らず、同じ条件で結果サイズを増やした再検索が頻発し不要な入力/キャッシュトークンを消費するという課題です。20（対照）・50/100/200（処置）でデフォルト値を変える実験を実施しています。

**直近のアクティビティ:** dbaeumer が「100 に更新する PR（#326648）を出した」と報告し、スコアカードやコスト分析のクエリ・画像を複数投稿、bhavyaus もクエリを共有しています。

**次のアクション:** 実験結果に基づくデフォルト値決定と反映の追跡が必要です。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat fails after updating to VS Code Insiders 1.130.0: missing telemetryExperimentProperty API proposal](https://github.com/microsoft/vscode/issues/326581) `#326581`
**ステータス:** Closed | **ラベル:** なし

**概要:** 2026-07-20 の Insiders 1.130.0 更新直後から Copilot Chat の全リクエストが失敗し、ログに「Extension 'GitHub.copilot-chat' CANNOT use API proposal: telemetryExperimentProperty」というエラーが出るという不具合です。

**直近のアクティビティ:** vijayupadya が #326675 で修正済みとコメントし、GongT が同一エラーログを共有、sanket-bhalerao が #326579 を本 Issue に統合してクローズすると述べています。

**次のアクション:** クローズ済み（#326675 で修正）。

---

### ![Issue](icons/issue-opened.svg) [Regression: Panel tabs use wrong capitalization](https://github.com/microsoft/vscode/issues/326495) `#326495`
**ステータス:** Open | **ラベル:** modernization, new release

**概要:** パネルタブの大文字/小文字の扱いが誤っているという回帰報告です。

**直近のアクティビティ:** alex-kinokon が git サイドバータイトルにも当てはまり、`Console De Code Source` は `Console de code source` が正しく、`text-transform` プロパティを削除すると正しく表示されるとコメントしています。

**次のアクション:** `text-transform` による誤った大文字化の修正が必要です。

---

### ![Issue](icons/issue-opened.svg) [Audit Copilot SDK events with agentId for subagent-safe handling](https://github.com/microsoft/vscode/issues/326710) `#326710`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** Copilot SDK のセッションイベントエンベロープが subagent 用に `agentId` を持ち得るため、`user.message` 以外の残る Agent Host コンシューマについてもルート限定の前提を監査し、`system.notification` などの状態変更系イベントをルーティング/無視/明示的にドキュメント化する必要がある、という追跡 Issue です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** `agentId` を持つ各 SDK イベントの subagent 安全性の監査が必要です。

---

### ![Issue](icons/issue-opened.svg) [polish: hover on new checkbox for worktree](https://github.com/microsoft/vscode/issues/325662) `#325662`
**ステータス:** Closed | **ラベル:** bug, verified, polish, insiders-released, agents-window

**概要:** 新規セッション設定ピッカーの New Worktree チェックボックスのホバーツールチップの文言と、グレーアウト理由の表示を改善してほしいという polish 要望です（#325500 のテスト中）。

**直近のアクティビティ:** pwang347 がボタンクリックで UI がちらつく理由を @sandy081 に尋ね、sandy081 が #325752 で修正済みとして検証手順を提示しています。

**次のアクション:** クローズ済み（#325752 で修正）。

---

### ![Issue](icons/issue-opened.svg) [Integrated terminal automatically sends 'Y' when terminating node batch script](https://github.com/microsoft/vscode/issues/142341) `#142341`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, terminal-conpty, unreleased, terminal-input

**概要:** 統合ターミナルで node バッチスクリプトを Ctrl+C 終了する際「Terminate batch job (Y/N)?」に対して 'Y' が自動送信され、最初のスクリプトのみ終了し残りの出力が次行のプロンプトに残るという Windows の不具合です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Git - "Git: Open Changes" and "Git: Open File" do not work for notebooks](https://github.com/microsoft/vscode/issues/176401) `#176401`
**ステータス:** Closed | **ラベル:** bug, help wanted, git, unreleased

**概要:** ノートブックに対して「Git: Open Changes」「Git: Open File」コマンドが機能しないという不具合報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [VSCode freezes when opening a terminal](https://github.com/microsoft/vscode/issues/239838) `#239838`
**ステータス:** Closed | **ラベル:** important, freeze-slow-crash-leak, candidate, terminal, gpu, unreleased

**概要:** ターミナルを開くと VS Code がフリーズするという不具合報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Native context menu hijacks custom one in webview editor](https://github.com/microsoft/vscode/issues/239862) `#239862`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, electron, confirmed, regression, unreleased

**概要:** webview エディタ（Git Graph の項目を右クリック）で、カスタムのコンテキストメニューではなくネイティブメニュー（テキスト操作のみ）が表示されるという回帰不具合です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) ["Open with Code" old context menu option missing from Explorer on version 1.103.0](https://github.com/microsoft/vscode/issues/260389) `#260389`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, regression, unreleased

**概要:** 1.103.0 でインストール時に「Add 'Open with Code' action...」を有効にしても、Windows Explorer のディレクトリコンテキストメニューに「Open with Code」が表示されないという回帰不具合です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Chat session picker: No picker in restored chat editor](https://github.com/microsoft/vscode/issues/275289) `#275289`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased

**概要:** CCA チャットエディタでリロード後にエディタは復元されるものの、エージェントピッカーが表示されないという不具合報告です（#274938 のテスト中）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Telemetry event `inline.done` isn't send for v2 inline chat sessions](https://github.com/microsoft/vscode/issues/277422) `#277422`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased

**概要:** inline chat v1 と v2 の性能比較に使う `inline.done` テレメトリイベントが v2 のインラインチャットで送信されないという不具合で、core と拡張の両方の修正を要します。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [InlineChatController2 creates widget eagerly](https://github.com/microsoft/vscode/issues/283017) `#283017`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, unreleased, inline-chat

**概要:** `InlineChatController2` がウィジェットを先行生成しており、軽微な性能低下に加え、実験的な `inlineChat.notebookAgent` ではウィジェット生成時にモデル未設定で正しい位置情報を渡せないという問題です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Git Source Control Graph tree doesn't expand for some entries](https://github.com/microsoft/vscode/issues/286776) `#286776`
**ステータス:** Closed | **ラベル:** bug, candidate, scm, unreleased

**概要:** Git ソース管理グラフのツリーで一部エントリが展開できないという不具合報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Backport fix for CVE-2026-2441 from chromium](https://github.com/microsoft/vscode/issues/295264) `#295264`
**ステータス:** Closed | **ラベル:** bug, upstream, verified, candidate, chromium, unreleased

**概要:** Chromium の CVE-2026-2441 の修正を Electron 経由でバックポートする対応です（該当 Electron コミットを参照）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（セキュリティ修正のバックポート）。

---

### ![Issue](icons/issue-opened.svg) [autopilot mode should allow curl in terminal](https://github.com/microsoft/vscode/issues/303174) `#303174`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** `chat.tools.global.autoApprove: true`（Autopilot モード）でも `curl` の実行が失敗し、明確なエラーや対処情報がないという不具合で、ターミナルの `curl` がブロックされているのではないかという指摘です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Terminal commands should be auto approved when invoked from `ask_questions` tool](https://github.com/microsoft/vscode/issues/309589) `#309589`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** `ask_questions` ツール経由で実行されるターミナルコマンドは、ユーザーが既に回答を確定しているため、`Enter` 送信を含め確認を求めるべきでないという要望です。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Dictation: show download progress ring instead of spinning mic](https://github.com/microsoft/vscode/pull/326708) `#326708`
**ステータス:** Closed | **ラベル:** なし

**概要:** 初回 dictation のモデルダウンロード中に回転マイクと通知の代わりにダウンロードアイコンと進捗リングを表示する UX 改善の PR ですが、fork ではなく microsoft/vscode 上のブランチから作り直すためクローズされました（#326709 に置き換え）。

**直近のアクティビティ:** meganrogge が「fork ではなく microsoft/vscode のブランチから作り直す」とコメントしています。

**変更ファイル:** `chatSpeechToTextActions.ts`・`dictationDownloadActionViewItem.ts` など 6 ファイル（+245/-10）。

**次のアクション:** クローズ済み（#326709 に置換）。

---

### ![PR](icons/git-pull-request.svg) [Fix ContextView motion CSS creating a containing block that clips submenus](https://github.com/microsoft/vscode/pull/326600) `#326600`
**ステータス:** Open | **ラベル:** なし

**概要:** #325190 でドロップダウンにモーションアニメーションを追加した際、`.monaco-scrollable-element` に付けた `will-change: transform` と `animation-fill-mode: both` が新しい CSS containing block を作り、`position: fixed` のサブメニューフライアウトがビューポートではなくスクロール要素基準になってクリップされる問題（#326593）を修正します。

**直近のアクティビティ:** LuckysHorizon が CLA 同意を促すボットへの返信について言及しています（レビューはまだ）。

**変更ファイル:** `contextview.ts` とそのテストの 2 ファイル（+53/-4）。

**次のアクション:** CLA 対応とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [Fix Themes submenu in Modern UI](https://github.com/microsoft/vscode/pull/326703) `#326703`
**ステータス:** Open | **ラベル:** なし

**概要:** Modern UI のメニューモーションがスクロール可能なメニュー面に transform の containing block を残し、Settings > Themes のような fixed 配置のサブメニューがクリップされる問題を修正します。開くアニメーションの fill mode を `both` から `backwards` に変え、`will-change` を制限します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（ドラフト）。

**変更ファイル:** `contextview.ts` とそのテストの 2 ファイル（+33/-4）。

**次のアクション:** ドラフト完成とレビューが必要です（#326600 と関連）。

---

### ![PR](icons/git-pull-request.svg) [voice: make passive ptt_start the canonical hands-free narration fix](https://github.com/microsoft/vscode/pull/326405) `#326405`
**ステータス:** Merged | **ラベル:** なし

**概要:** ハンズフリーモードでパッシブな barge-in マイクが開いている間、`ptt_start` によりバックエンドが「ユーザー発話中」とラッチし、マイクが `ptt_end` なしで中断されるためラッチが解除されず、その間のナレーション要求が busy で NACK されたまま滞留する問題を修正します。再接続やアンブロック経路でのナレーションの取りこぼし/再送失敗も修正します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（meganrogge・pwang347・benvillalobos が Approve 済み）。

**変更ファイル:** `voiceClientService.ts`・`voiceSessionController.ts`・`micCaptureService.ts` など 7 ファイル（+506/-104）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Dictation: migrate on-device runtime to Foundry Local streaming ASR](https://github.com/microsoft/vscode/pull/326678) `#326678`
**ステータス:** Merged | **ラベル:** なし

**概要:** オンデバイス dictation ランタイムを Microsoft Foundry Local のネイティブストリーミング ASR（onnxruntime + onnxruntime-genai、NVIDIA `nemotron-speech-streaming-en-0.6b` の RNN-T モデル）へ移行し、デコード・VAD・エンドポイント検出をネイティブで行うことでレイテンシと精度を大幅に改善します。従来の transformers.js Whisper + 手製 Nemotron デコード実装を置き換えます。

**直近のアクティビティ:** 直近 24 時間のコメントなし（pwang347・connor4312 が Approve 済み）。

**変更ファイル:** `nemotronTranscriber.ts`・`foundryLocal.ts`・ビルドスクリプトなど 16 ファイル（+702/-1545）。

**次のアクション:** マージ済み。後続の設定廃止（#326718）等へ続きます。

---

### ![PR](icons/git-pull-request.svg) [Resolve execution subagent model by exact CAPI id before family fallback](https://github.com/microsoft/vscode/pull/324859) `#324859`
**ステータス:** Merged | **ラベル:** triage-needed

**概要:** `github.copilot.chat.executionSubagent.model` 設定が CAPI の `capabilities.family` のみで解決され、正確なモデル `id` で解決されなかったため、同一 family に複数 id があると常に先頭モデルが選ばれ、特定 id を指定できず MAI ルーティングが失敗していた問題を修正します。exact id を family フォールバックより先に解決します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（bhavyaus・pwang347・vikramnitin9 が Approve 済み）。

**変更ファイル:** `executionSubagentToolCallingLoop.ts`・`searchSubagentToolCallingLoop.ts` とテストの 4 ファイル（+372/-4）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: preserve native browser shortcuts across keyboard layouts](https://github.com/microsoft/vscode/pull/326383) `#326383`
**ステータス:** Merged | **ラベル:** なし

**概要:** キリル文字などの非ラテンキーボードレイアウトで、統合ブラウザ内の `Ctrl+C/V/X` などのネイティブ編集ショートカットが `event.key` が非ラテン文字になるため無効化される問題を修正します。ラテン文字は `event.key` を使い続け、非ラテン時のみ `event.code` の物理キーにフォールバックします。

**直近のアクティビティ:** 直近 24 時間のコメントなし（pwang347 が Approve 済み）。

**変更ファイル:** `preload-browserView.ts` 1 ファイル（+5/-1）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [remove unnecessary condition](https://github.com/microsoft/vscode/pull/326699) `#326699`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host のリソース操作の統合テストから不要な条件を削除する小さな変更です。

**直近のアクティビティ:** roblourens・pwang347 が Approve し、copilot-pull-request-reviewer[bot]がコメントしています。

**変更ファイル:** `resourceOperations.integrationTest.ts` 1 ファイル（+3/-3）。

**次のアクション:** マージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Revert "chore: update electron@42.6.0 (#325428)"](https://github.com/microsoft/vscode/pull/326231) `#326231`
**ステータス:** Closed | **ラベル:** cherry-pick-artifact

**概要:** `release/1.129` からの #326219 のチェリーピックで、electron@42.6.0 への更新を revert するものですが、`package.json` と `remote/package.json` にマージ競合があり手動解決が必要な状態でした。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています。

**変更ファイル:** `.npmrc`・`.nvmrc`・electron チェックサムや依存リストなど 23 ファイル（+128/-195）。

**次のアクション:** クローズ済み（未マージ）。

---

### ![PR](icons/git-pull-request.svg) [fix: translate notebook range to alt-cell index space in alternativeNotebookTextDocument (fixes #326632)](https://github.com/microsoft/vscode/pull/326637) `#326637`
**ステータス:** Open | **ラベル:** errors-fix, *error-fix-driving

**概要:** Copilot Chat 拡張の `alternativeNotebookTextDocument.ts` で `withNotebookChangesAndEdit` から `TypeError: Cannot read properties of undefined (reading 'startOffset')` が発生する問題を修正します。原因は `event.range` がマークダウンセルを含むノートブックの全セルインデックス空間で表現される一方、alt ドキュメントの `altCells` はそれを除外しているためのズレです。

**直近のアクティビティ:** vs-code-engineering[bot]（errors-fix-driver）が複数回コミットをプッシュし、copilot-pull-request-reviewer[bot]とともに多数のレビューコメントを付けています。

**変更ファイル:** `alternativeNotebookTextDocument.ts` とそのテストの 2 ファイル（+72/-9）。

**次のアクション:** レビュー対応とマージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [Reverse lines: preserve single-line selection](https://github.com/microsoft/vscode/pull/284988) `#284988`
**ステータス:** Open | **ラベル:** なし

**概要:** 行の反転（Reverse lines）操作で、以前は選択範囲がドキュメント全体に拡張されていた挙動を改め、単一行選択を保持するようにします。特殊分岐を除去し行数削減にもつながります（#294190 の修正）。

**直近のアクティビティ:** hickford が @alexdima にレビューを依頼しています。

**変更ファイル:** `linesOperations.ts` とそのテストの 2 ファイル（+38/-24）。

**次のアクション:** レビュー着手が必要です。

---

### ![PR](icons/git-pull-request.svg) [Sort pinned models by group](https://github.com/microsoft/vscode/pull/326683) `#326683`
**ステータス:** Merged | **ラベル:** なし

**概要:** モデルピッカーでピン留めモデルをグループ順にソートする修正です（#326294 の修正）。

**直近のアクティビティ:** 直近 24 時間のコメントなし（eleanorjboyd が Approve 済み）。

**変更ファイル:** `modelPickerItems.ts` とそのテストの 2 ファイル（+30/-14）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Revert "Forward CAPI assignment context to VS Code core telemetry"](https://github.com/microsoft/vscode/pull/326682) `#326682`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** `main` からの #326675 のチェリーピックで、CAPI アサインメントコンテキストを VS Code コアテレメトリへ転送する変更（#326273）を revert します。

**直近のアクティビティ:** 直近 24 時間のコメントなし（vijayupadya・rzhao271 が Approve 済み）。

**変更ファイル:** `telemetryServiceImpl.ts`・`telemetryService.ts`・`extHost.api.impl.ts` など 13 ファイル（+12/-77）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Batch ListView horizontal width measurement](https://github.com/microsoft/vscode/pull/326641) `#326641`
**ステータス:** Open | **ラベル:** なし

**概要:** `ListView` の行水平幅測定を write/read/reset の各フェーズにバッチ化し、可視行挿入・範囲スクロール・実行時の水平スクロール有効化に適用してレイアウトのスラッシングを減らす性能改善です。行単位のパディング計算や幅キャッシュ、遅延スクロール幅更新は保持します。

**直近のアクティビティ:** vs-code-engineering[bot]が CODENOTIFY（@benibenj 宛）を投稿しています（lszomoru が Approve 済み）。

**変更ファイル:** `listView.ts` とそのテストの 2 ファイル（+129/-20）。

**次のアクション:** マージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [Batch action list width measurements](https://github.com/microsoft/vscode/pull/326642) `#326642`
**ステータス:** Open | **ラベル:** なし

**概要:** アクションリストの行幅の書き込み・ジオメトリ読み取り・復元を 3 フェーズにバッチ化する性能改善です。Chromium CDP 計測では、交互 write/read に比べレイアウト回数とスタイル再計算が大幅に削減されています。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]のコメントと lszomoru の Approve があります。

**変更ファイル:** `actionList.ts` とそのテストの 2 ファイル（+55/-18）。

**次のアクション:** マージ判断が必要です。

---

### ![PR](icons/git-pull-request.svg) [chat: support inline attachment references](https://github.com/microsoft/vscode/pull/326666) `#326666`
**ステータス:** Open | **ラベル:** なし

**概要:** 既に添付済みのコンテキスト（ファイル・フォルダ・画像・貼り付けテキストなど）をプロンプト内でインラインに言及できるようにし、重複や削除なしに関連箇所を表現できるようにします。インライン参照状態を軽量に保ちタイピングの遅延を防ぎつつ、送信時には元のペイロードを保持し、# と @ 補完で既存コンテキストを優先表示します。

**直近のアクティビティ:** copilot-pull-request-reviewer[bot]がレビューコメントを付けています（ドラフト）。

**変更ファイル:** `chatVariables.ts`・`chatRequestParser.ts`・`chatInputCompletions.ts` など 14 ファイル（+380/-19）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [sessions: reduce hidden animation work](https://github.com/microsoft/vscode/pull/326668) `#326668`
**ステータス:** Open | **ラベル:** なし

**概要:** セッションタイトルのシマーやピクセルスピナーのアニメーションを、要素がビューポート外またはドキュメントが非表示のときに一時停止し、見えない描画作業を回避する性能改善です。可視性対応の CSS アニメーション処理を集約し、シマーの視覚更新頻度を抑制します。

**直近のアクティビティ:** github-actions[bot]のスクリーンショット差分レポート（レンダリング失敗 1 件、軽微な差分 2 件を省略）が投稿されています。

**変更ファイル:** `animationSync.ts`・`pixelSpinner.ts`・`sessionsList.ts` など 5 ファイル（+144/-62）。

**次のアクション:** ドラフト完成とレビューが必要です。

---

### ![PR](icons/git-pull-request.svg) [Add telemetry for applied managed settings and device policies](https://github.com/microsoft/vscode/pull/325385) `#325385`
**ステータス:** Closed | **ラベル:** なし

**概要:** エンタープライズ管理設定と OS/MDM デバイスポリシーの適用状況を報告する単一の `policy.applied` テレメトリイベントを追加する PR ですが、`main` 上で再構築・簡素化した takeover ブランチに置き換えられクローズされました。

**直近のアクティビティ:** joshspicer が「takeover ブランチに引き継いだためこのドラフトをクローズする」とコメントしています。

**変更ファイル:** `policyTelemetryContribution.ts` など 5 ファイル（+649/-0）。

**次のアクション:** クローズ済み（後継ブランチ・#326686 へ継承）。

---

### ![PR](icons/git-pull-request.svg) [[WIP] Fix failing GitHub Actions job Code OSS / Compile & Hygiene](https://github.com/microsoft/vscode/pull/326677) `#326677`
**ステータス:** Closed | **ラベル:** なし

**概要:** 失敗している GitHub Actions ジョブ「Code OSS / Compile & Hygiene」を修正するためのエージェント駆動の WIP PR で、`agentHostCustomizationService.ts` を 1 行変更しています。

**直近のアクティビティ:** javi0guerrero4-cloud がレビューコメントを付けています。

**変更ファイル:** `agentHostCustomizationService.ts` 1 ファイル（+2/-0）。

**次のアクション:** クローズ済み（未マージ）。

---

### ![PR](icons/git-pull-request.svg) [Revert "Forward CAPI assignment context to VS Code core telemetry"](https://github.com/microsoft/vscode/pull/326675) `#326675`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** CAPI アサインメントコンテキストを VS Code コアテレメトリへ転送する変更（#326273）を revert します。これにより Copilot Chat が `telemetryExperimentProperty` API proposal を使えず失敗する問題（#326581）が解消されます。

**直近のアクティビティ:** 直近 24 時間のコメントなし（DonJayamanne・pwang347 が Approve 済み）。

**変更ファイル:** `telemetryServiceImpl.ts`・`extHost.api.impl.ts` など 13 ファイル（+12/-77）。

**次のアクション:** マージ済み（#326682 でチェリーピック）。

---

### ![PR](icons/git-pull-request.svg) [Prompt to disable redundant VS Code Speech extension when built-in dictation is available](https://github.com/microsoft/vscode/pull/326665) `#326665`
**ステータス:** Merged | **ラベル:** なし

**概要:** VS Code のビルトインオンデバイス dictation が利用可能になり `ms-vscode.vscode-speech` 拡張が冗長になった際に、無効化を促す一度限りの通知を追加します。新しいワークベンチ寄与 `RedundantDictationExtensionNotifier` が、プラットフォームがビルトイン dictation をサポートするなどの条件を全て満たす場合にのみプロンプトを表示します。

**直近のアクティビティ:** cwebster-99 のレビューコメントと dmitrivMS の Approve があります。

**変更ファイル:** `chat.shared.contribution.ts`・`redundantDictationExtensionNotifier.ts` の 2 ファイル（+138/-0）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [checking line number of valid before rendering it](https://github.com/microsoft/vscode/pull/326669) `#326669`
**ステータス:** Merged | **ラベル:** なし

**概要:** sticky scroll ウィジェットで、行番号が有効かを確認してからレンダリングするようにする修正です（#325642 の修正）。

**直近のアクティビティ:** dmitrivMS のコメントと DonJayamanne の Approve があります。

**変更ファイル:** `stickyScrollWidget.ts` 1 ファイル（+5/-1）。

**次のアクション:** マージ済み。

---

## qmk/qmk_firmware

> 直近24時間はキーボード関連のPRが中心で、新規キーボード追加やREADME更新、コア実装への統合が進んでいます。メンテナによるレビュー・承認の動きが活発です。

### ![PR](icons/git-pull-request.svg) [Update Play Keyboard README](https://github.com/qmk/qmk_firmware/pull/26345) `#26345`
**ステータス:** Open | **ラベル:** keyboard

**概要:** Play Keyboard の README を更新する PR です。紹介文の刷新と、書式・可読性の改善を行っています。変更は `keyboards/playkbtw/readme.md` の 1 ファイルのみです。

**直近のアクティビティ:** tzarc は「このファイルは丸ごと削除する方が望ましいと思う」とコメントしました。投稿者 yj7272098 は「フィードバックに感謝。削除が望ましいなら対応する。既存の内容が古かったので更新した」と応じています。また drashna が Approve（承認）しています。

**変更ファイル:** `keyboards/playkbtw/readme.md`（+17 / -3）。

**次のアクション:** ファイルを更新するか削除するか、メンテナ間の方針を確定させる必要があります。

---

### ![PR](icons/git-pull-request.svg) [Use core `spi_init` implementation for gmmk/gmmk2/p96](https://github.com/qmk/qmk_firmware/pull/26344) `#26344`
**ステータス:** Open | **ラベル:** keyboard

**概要:** gmmk/gmmk2/p96 で独自に持っていた `spi_init` 実装を削除し、コア側の実装を利用するようにする最適化 PR です。コアが既に `SPI_SCK_PIN` 向けに WB32 固有の `PAL_WB32_CURRENT_LEVEL3` 設定を扱っているため、キーボード側での対応は不要と説明されています。

**直近のアクティビティ:** 直近 24 時間のコメントなし。drashna が Approve（承認）しています。

**変更ファイル:** `keyboards/gmmk/gmmk2/p96/ansi/ansi.c`（-24）と `keyboards/gmmk/gmmk2/p96/iso/iso.c`（-24）から計 48 行を削除。base_branch は `develop` です。

**次のアクション:** 承認済みのため、`develop` へのマージ対応が想定されます。

---

### ![PR](icons/git-pull-request.svg) [Add SM Boards Macro2040](https://github.com/qmk/qmk_firmware/pull/26247) `#26247`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** RP2040 ベースの 7 キーマクロパッド「SM Boards Macro2040」のサポートを追加します。AS5600 磁気ロータリエンコーダ、WS2812B RGB マトリクス、SSD1306 OLED、スイッチインジケータ LED などを搭載しています。AS5600 は I²C 磁気角度センサのため、GPIO で A/B 直交パルスを合成し物理的にエンコーダ入力へループバックすることで QMK 標準のエンコーダ機構を利用しています。また、AS5600 が I²C0 の `i2c_master` バスを占有するため、SSD1306 は I²C1 上で動く専用ドライバを用意しています。VIA サポートは方針に従い別途提出予定で本 PR には含みません。

**直近のアクティビティ:** 投稿者 sjb-dev が drashna 宛に「自分ではマージできない。さらに必要な変更はあるか」と問い合わせています。過去には zvecr と tzarc から CHANGES_REQUESTED が出ていましたが、7 月 19 日に drashna が Approve（承認）済みです。

**変更ファイル:** `keyboards/sm_boards/macro2040/` 配下に 9 ファイルを新規追加（計+569 行）。`macro2040.c`（+235）、`config.h`（+73）、`keyboard.json`（+112）、`readme.md`（+70）、default キーマップなど。

**次のアクション:** 承認済みのため、メンテナによるマージ実行が求められています。

---

### ![PR](icons/git-pull-request.svg) [Add support for Bowl Pangea](https://github.com/qmk/qmk_firmware/pull/25784) `#25784`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale

**概要:**「Bowl Pangea」キーボードのサポートを追加する PR です。`pngh` と `pngs` の 2 バリアント分の設定ファイル・キーマップ・README を追加しています。

**直近のアクティビティ:** github-actions ボットが「45 日間活動がないため自動的に stale としてマークした。30 日後にさらに活動がなければクローズする」と通知しました。過去に zvecr から CHANGES_REQUESTED、その後 drashna が Approve およびコメントを行っています。

**変更ファイル:** `keyboards/bowl/pangea/` 配下に 13 ファイルを新規追加（計+623 行）。`pngh` と `pngs` それぞれの `keyboard.json`（+159 / +155）、`config.h`、`halconf.h`、`mcuconf.h`、キーマップ、共通の `readme.md` など。

**次のアクション:** stale でクローズされる前に、ステータス更新かレビュー対応を行い、マージ可否を確定させる必要があります。

---
