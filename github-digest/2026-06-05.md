# GitHub ダイジェスト — 2026-06-05

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[feat(terminal): replace `libvterm` with `libghostty-vt`](https://github.com/neovim/neovim/pull/39773)** (neovim/neovim) — 組み込みターミナルの基盤を libvterm から libghostty-vt へ全面移行する大型 PR（+2944/-13085）。各コミットで動作を保ちながら段階移植しており、Neovim と Ghostty 双方のエコシステムに影響する。
2. ![PR](icons/git-pull-request.svg) **[Migrate package manager from npm to pnpm](https://github.com/microsoft/vscode/pull/320104)** (microsoft/vscode) — VS Code モノレポを npm から pnpm へ移す巨大 PR（+49703/-81482）。workspace 構成や standalone ディレクトリ、インストールオーケストレーションを全面的に書き換える。
3. ![PR](icons/git-pull-request.svg) **[chat: stop agent host config update loop across windows](https://github.com/microsoft/vscode/pull/320015)** (microsoft/vscode) — 全ウィンドウ共有の agent host root config により 2 ウィンドウが `root/configChanged` を 25 秒で約 8000 回押し合う無限ループを修正したマージ済み bugfix。
4. ![PR](icons/git-pull-request.svg) **[Avoid following symlinks during cache prune](https://github.com/astral-sh/uv/pull/19543)** (astral-sh/uv) — `uv cache prune` がシンボリックリンクを辿って `UV_CACHE_DIR` 外のディレクトリを削除しうる問題を修正（関連する cache clean 修正 #19682 も同日マージ）。セキュリティ性の高い修正。
5. ![PR](icons/git-pull-request.svg) **[Volkswagen ID.4 longitudinal](https://github.com/commaai/opendbc/pull/3424)** (commaai/opendbc) — VW MEB プラットフォーム初の車種となる ID.4 の縦制御ポート（+1423/-41）。安全モデルや新 DBC を含み、car safety ラベル付きで活発に動いている。

---

## sunnypilot/sunnypilot

> 直近はビルド成果物まわりの CI 調整のみで、大きな機能変更は見られない。

### ![PR](icons/git-pull-request.svg) [ci: exclude ONNX models from publish output](https://github.com/sunnypilot/sunnypilot/pull/1853) `#1853`
**ステータス:** Merged | **ラベル:** ci

**概要:** prebuilt ビルドの publish 成果物から ONNX モデルを除外する CI 調整。`sunnypilot-build-prebuilt.yaml` を 2 行修正する小規模変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/sunnypilot-build-prebuilt.yaml`（+2/-2）。

**次のアクション:** なし（マージ済み）。

---

## commaai/openpilot

> macOS 向け MetaDrive シミュレータの復旧や、より大きな usbgpu モデルへの準備、Hyundai のクルーズボタン挙動を純正に合わせる対応など、シミュレータと車種制御まわりが活発に動いている。

### ![PR](icons/git-pull-request.svg) [sim: fix MetaDrive startup on macOS](https://github.com/commaai/openpilot/pull/38140) `#38140`
**ステータス:** Open

**概要:** macOS で MetaDrive シミュレーションが起動できない問題を修正。`SIMULATION=1` の場合のみ `forkpty()` をスキップし、シミュレータの遅いフレームタイミングが鮮度チェックに引っかからないよう単調増加のカメラタイムスタンプを使う。#33207 を修正。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。macOS 上で該当テストを 3 回繰り返し全パスしたとのこと。

**変更ファイル:** `system/manager/helpers.py`、`tools/sim/bridge/common.py`、`tools/sim/bridge/metadrive/metadrive_bridge.py`、`tools/sim/lib/camerad.py`（+34/-4）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [prereq for bigger usbgpu models](https://github.com/commaai/openpilot/pull/38138) `#38138`
**ステータス:** Open

**概要:** より大きな usbgpu モデルを扱うための前提整備。`file_chunker.py` や modeld 系のコンパイル・実行コードを変更する。

**直近のアクティビティ:** commaci-public がモデルリプレイ用の成果物参照とプロットを提示。Process replay diff bot は 66 件パス・変化なしを報告。

**変更ファイル:** `common/file_chunker.py`、`selfdrive/modeld/compile_modeld.py`、`selfdrive/modeld/dmonitoringmodeld.py`、`selfdrive/modeld/modeld.py`（+107/-45）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [RL1: only lat](https://github.com/commaai/openpilot/pull/38139) `#38139`
**ステータス:** Open | **ラベル:** autonomy

**概要:** 強化学習系モデル（RL1）を横制御のみに限定する更新で、driving 系の ONNX モデルファイルを差し替える。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。Model Review bot がモデルの実験リンク比較を提示。

**変更ファイル:** `driving_off_policy.onnx`、`driving_on_policy.onnx`、`driving_vision.onnx`（+3/-3）。

**次のアクション:** モデル挙動の確認とマージ判断。

---

### ![PR](icons/git-pull-request.svg) [test_onroad.py: test time to first modelV2 on onroad transition](https://github.com/commaai/openpilot/pull/38083) `#38083`
**ステータス:** Open

**概要:** onroad へ遷移してから最初の modelV2 が出るまでの時間を `test_onroad.py` でテストする変更。modeld 系のコンパイル・実行コードも併せて修正する。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。

**変更ファイル:** `test_onroad.py` ほか modeld 関連 4 ファイル（+50/-16）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [test_models: add fuzzy TX safety test](https://github.com/commaai/openpilot/pull/38135) `#38135`
**ステータス:** Closed | **ラベル:** CI / testing, car

**概要:** `TestCarModelBase` に `test_panda_safety_tx_fuzzy` を追加し、panda safety の fuzz テストを RX に加えて TX まで拡張する。Hypothesis ベースで `controls_allowed` や `accel` などをランダムに振り、決定論的テストが見逃すエッジケースを洗い出す。#32425 をクローズ予定。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。

**変更ファイル:** `selfdrive/car/tests/test_models.py`（+72/-0）。

**次のアクション:** クローズ済み（再オープンや別 PR での取り込みは要追跡）。

---

### ![PR](icons/git-pull-request.svg) [esim: comma four profile management UI](https://github.com/commaai/openpilot/pull/37844) `#37844`
**ステータス:** Open | **ラベル:** ui

**概要:** comma four の eSIM プロファイル管理 UI を追加。バックグラウンドスレッドで LPA 操作する CellularManager と、プロファイルカード・切り替え・スライド確認による削除を持つ MICI 設定画面を実装する。#34924 の一部に対応。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。UI Preview bot が mici/big 両方で動画差分の発生を報告。

**変更ファイル:** MICI ネットワーク設定 UI、`cellular_manager.py`、`hardware/tici/lpa.py` ほか（+529/-10）。

**次のアクション:** UI 差分の確認とレビュー。

---

### ![PR](icons/git-pull-request.svg) [ci: re-enable MetaDrive simulator driving test (#30693)](https://github.com/commaai/openpilot/pull/38136) `#38136`
**ステータス:** Open

**概要:** タイムアウトで `if: false` 無効化されていた MetaDrive シミュレータ走行テストを再有効化する。scons だけで 10〜40 分かかるのに総合 2 分のタイムアウトが原因だったため、ビルド・テストのタイムアウトを延長し、`MAX_TIME_PER_STEP` を可変化する。#30693 をクローズ予定。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。

**変更ファイル:** `.github/workflows/tests.yaml`、`tools/sim/launch_openpilot.sh`、`tools/sim/tests/test_sim_bridge.py`（+25/-10）。

**次のアクション:** CI の安定性確認とマージ判断。

---

### ![PR](icons/git-pull-request.svg) [car: match stock Hyundai longitudinal button behavior (#30950)](https://github.com/commaai/openpilot/pull/38137) `#38137`
**ステータス:** Open | **ラベル:** car

**概要:** Hyundai の openpilot-longitudinal（非 PCM）車向けに純正 SCC のボタン挙動を実装。Set 前の Resume を無効化し、CAN FD 車では cancel ボタンを一時停止/再開トグルにする。PCM クルーズ車には影響しない。`CarSpecificEvents` にステートマシンを追加し、14 件のテストを追加する。#30950 をクローズ予定。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。

**変更ファイル:** `selfdrive/car/car_specific.py`、`selfdrive/car/tests/test_hyundai_button_logic.py`（+182/-0）。

**次のアクション:** レビューとマージ判断。

---

## commaai/opendbc

> Honda・GM・VW など複数メーカーの fingerprint 追加や、安全性に関わる VW MEB（ID.4）の縦・横制御ポート、起動時の LKAS/Bosch フォルト対策などが動いている。長期未活動の PR が自動クローズされる動きも見られる。

### ![PR](icons/git-pull-request.svg) [Honda - Add 2024-25 Civic SI fingerprint](https://github.com/commaai/opendbc/pull/3334) `#3334`
**ステータス:** Open | **ラベル:** car, honda, fingerprint

**概要:** 2024・2025 年式 Honda Civic SI の fingerprint を追加する小規模 PR。

**直近のアクティビティ:** Car behavior report bot が Honda 154 セグメントで 0 changed・全パスを報告。

**変更ファイル:** `opendbc/car/honda/fingerprints.py`（+2/-0）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Honda - clear startup Bosch faults](https://github.com/commaai/opendbc/pull/3029) `#3029`
**ステータス:** Open | **ラベル:** car, honda

**概要:** Bosch 車が前回ドライブのフォルトを起動時に保持し、新しいドライブ開始から 1 分以内に解消する挙動への対策。起動 10 秒以内に現れるフォルトでは（可逆的な）クルーズ有効化を抑止し、解消する時間を与える。

**直近のアクティビティ:** Car behavior report bot が Honda 154 セグメントで 1 changed・153 パスを報告。

**変更ファイル:** `opendbc/car/honda/carstate.py`（+16/-1）。

**次のアクション:** 挙動差分の確認とレビュー。

---

### ![Issue](icons/issue-opened.svg) [[$1000 bounty] Kia EV9 lateral port](https://github.com/commaai/opendbc/issues/1175) `#1175`
**ステータス:** Open | **ラベル:** car, hyundai, car port

**概要:** Kia EV9 の横制御ポートに対する 1000 ドルの賞金 Issue。横制御 API の把握、全速度域での最大横加速度の保証、フォルトや不意のアラートが出ないこと、滑らかで安全な操舵ブレンディングを条件とする。

**直近のアクティビティ:** @mkcash が NVIDIA AI を使って取り組むと宣言（[CLAIM]）。

**次のアクション:** 取り組み状況の追跡。

---

### ![PR](icons/git-pull-request.svg) [PSA: add FW query](https://github.com/commaai/opendbc/pull/2936) `#2936`
**ステータス:** Closed | **ラベル:** car, stale

**概要:** PSA の ABS ECU 向けに最小限の FW クエリを追加し、Peugeot 508・DS DS7・Citroen Berlingo を覆う 2 つ目の fingerprint を加える。STEERING_ALT メッセージのバス位置で 208 系と 508 系を区別する。

**直近のアクティビティ:** 非活動のため bot が自動クローズ。@sshane と @elkoled のレビューコメントあり。

**変更ファイル:** PSA の carstate/fingerprints/values、routes.py、torque override（+43/-11）。

**次のアクション:** 活動再開時の再オープン。

---

### ![PR](icons/git-pull-request.svg) [Subaru Angle-Based LKAS port without car-specific platforms](https://github.com/commaai/opendbc/pull/3103) `#3103`
**ステータス:** Closed | **ラベル:** car, subaru, stale, car safety

**概要:** 車種固有プラットフォームを持たない Subaru の角度ベース LKAS ポート。carcontroller/interface/values と安全モデルを変更する。

**直近のアクティビティ:** 非活動のため bot が自動クローズ。@jacobwaller のレビューコメントあり。

**変更ファイル:** Subaru 系コードと `safety/modes/subaru.h`、`test_subaru.py` ほか（+181/-35）。

**次のアクション:** 活動再開時の再オープン。

---

### ![PR](icons/git-pull-request.svg) [Operation GAC (Silverado Tuning)](https://github.com/commaai/opendbc/pull/3115) `#3115`
**ステータス:** Closed | **ラベル:** car, gm, stale

**概要:** #3106 の続きとなる GM Silverado のチューニング。GM interface と torque override を微調整する。

**直近のアクティビティ:** 非活動のため bot が自動クローズ。

**変更ファイル:** `opendbc/car/gm/interface.py`、`torque_data/override.toml`（+2/-2）。

**次のアクション:** 活動再開時の再オープン。

---

### ![PR](icons/git-pull-request.svg) [GMC Yukon Update values.py](https://github.com/commaai/opendbc/pull/3124) `#3124`
**ステータス:** Open | **ラベル:** car, gm

**概要:** 2020 GMC Yukon で約 10 か月・6680 マイル走行して検証した wheelbase と tireStiffnessFactor の値に調整する。

**直近のアクティビティ:** bot レビューが、レビューを早めるためのチェックリスト（ドラフト化やルート添付など）を案内。

**変更ファイル:** `opendbc/car/gm/values.py`（+1/-1）。

**次のアクション:** 検証ルートの提出とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Volkswagen ID.4 longitudinal](https://github.com/commaai/opendbc/pull/3424) `#3424`
**ステータス:** Open | **ラベル:** DBC signals, car, volkswagen, car safety

**概要:** VW MEB プラットフォーム初の車種となる ID.4 の縦制御ポート。AEB 無効化を防ぐためゲートウェイハーネスのみ対応とし、MEB EPS は curvature target を使う。EA エスカレーション検証までは Dashcam モードに設定する。

**直近のアクティビティ:** Car behavior report bot が Ford・VW など 221 セグメントを対象に実行。

**変更ファイル:** VW 系コード一式、`vw_meb.dbc`、`safety/lateral.h`、`safety/modes/volkswagen_meb.h` ほか多数（+1423/-41）。

**次のアクション:** 安全性レビューと検証。

---

### ![PR](icons/git-pull-request.svg) [Honda - bump Accord Hybrid to 2026](https://github.com/commaai/opendbc/pull/3434) `#3434`
**ステータス:** Merged | **ラベル:** car, honda

**概要:** Honda Accord Hybrid の対応年式を 2026 年式まで広げる fingerprint/values 更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `opendbc/car/honda/fingerprints.py`、`values.py`（+2/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [VW: fix rare temporary LKAS fault on startup](https://github.com/commaai/opendbc/pull/3268) `#3268`
**ステータス:** Open | **ラベル:** car, volkswagen, stale

**概要:** 工場 LKAS 非搭載車で EPS が前回ドライブの LKAS を覚えていて自己初期化し、起動時に「LKAS Fault」が出る問題への対策。すべての操舵フォルトを一時フォルト扱いとし、ゲーティングを不要にする。#3228 を置き換える 2 本目の PR。

**直近のアクティビティ:** Car behavior report bot が VW 130 セグメントで実行（130 changed と報告）。

**変更ファイル:** `opendbc/car/volkswagen/carstate.py`（+11/-12）。

**次のアクション:** 挙動差分の確認とレビュー。

---

## commaai/panda

> 直近は CAN ループバックに関する不具合 Issue が継続的に報告されている。

### ![Issue](icons/issue-opened.svg) [panda sometimes doesn't loopback sent messages on bus 128](https://github.com/commaai/panda/issues/2122) `#2122`
**ステータス:** Open

**概要:** panda が bus 128 で送信メッセージをループバックしないことがあり、Bolt の LKAS フォルトを引き起こすという報告。複数の connect ルートが添付されている。

**直近のアクティビティ:** @morrislee が問題が続いているとし、自分以外にも発生していないか別ルートを添えて尋ねている。

**次のアクション:** 再現条件の切り分けと原因調査。

---

## neovim/neovim

> 画像表示（vim.ui.img）やターミナル永続化（GSoC）、libghostty-vt 移行といった大型機能が進行中。加えて winborder のパース修正やセキュリティ関連の vim-patch 取り込みなど、修正・バックポートも活発。

### ![PR](icons/git-pull-request.svg) [feat(img): relative images (editor & buffer) using unicode placeholder](https://github.com/neovim/neovim/pull/39496) `#39496`
**ステータス:** Open | **ラベル:** ci:skip-news

**概要:** `vim.ui.img.Opts` に `relative`（`ui`/`editor`/`buffer`）と `buf` を追加し、画像を端末基準だけでなくエディタ基準・バッファ基準で配置できるようにする。floating window や virtual line + extmark と Unicode プレースホルダで画像を重ねる。#39432 をクローズ予定で #39484 に依存。

**直近のアクティビティ:** @justinmk が、外部クライアントに vim.ui.img の上書きを推奨するのはあくまで短期的措置で長期方針は未確定とコメント。@chipsenkbeil と @justinmk の間で 11 件のレビューが進む。

**変更ファイル:** `runtime/lua/vim/ui/img.lua`、`_kitty.lua`、`_util.lua`、`lua.txt`、`img_spec.lua`（+1094/-67）。

**次のアクション:** 依存 PR #39484 完了後のリベースとレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(shada): prevent case-duplicate oldfiles](https://github.com/neovim/neovim/pull/40013) `#40013`
**ステータス:** Open

**概要:** Windows/macOS で大文字小文字だけ異なる同一ファイルが `oldfiles` に重複する問題を修正。パス対応比較する `Set_path_t` を追加し、Windows ではパス区切りやドライブ違いも吸収する。#25033 をクローズ予定。

**直近のアクティビティ:** @justinmk が性能は後で直せるとし、まず正しい挙動とテストを優先したいとコメント。@ntdiary が 500 パスのベンチを hyperfine で計測し、許容範囲か確認している。

**変更ファイル:** `src/nvim/map.c`、`map_defs.h`、`shada.c`、`shada_spec.lua`（+101/-4）。

**次のアクション:** ベンチ結果の妥当性判断とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [feat(api): expose command preview through nvim__buf_preview_cmd](https://github.com/neovim/neovim/pull/40090) `#40090`
**ステータス:** Open | **ラベル:** api

**概要:** コマンドプレビューを API から使えるよう `nvim__buf_preview_cmd()` を追加。既存の `cmdpreview_may_show` をバッファ引数を受け取る形に更新し、グローバルの `bool cmdpreview` を `buf_T *cmdpreview_curbuf` に置き換える。cmdwin からのプレビュー表示（#30340）を可能にする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/api/vim.c`、`ex_getln.c`、`ex_docmd.c`、`globals.h` ほか（+274/-36）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [AltGr + alternative keyboard layout (hungarian) + kitty protocol](https://github.com/neovim/neovim/issues/40123) `#40123`
**ステータス:** Open | **ラベル:** platform:windows, tui, input

**概要:** Windows Terminal（WSL/ArchLinux）で kitty プロトコルを有効にすると、ハンガリー語配列の `AltGr + キー`（`\`・`|`・`^` など）や `Ctrl+Alt+キー` が Escape を送ってしまう。`AltGr+Q` で `\` が入力できないとの報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** kitty プロトコル入力処理の調査。

---

### ![Issue](icons/issue-opened.svg) [incremental selection: inconsistent initial range for single-char vs multi-char nodes](https://github.com/neovim/neovim/issues/38547) `#38547`
**ステータス:** Closed | **ラベル:** documentation, treesitter

**概要:** インクリメンタル選択で、1 文字ノードと複数文字ノードの初期範囲が一致しないという報告。`a` に対し `van` すると `a = 1` が選択されてしまう。

**直近のアクティビティ:** @altermo が、それは `vin`（select-inner-node）の挙動だと説明し、ドキュメント改善が必要と指摘。報告者 @przepompownia は問題を再定義したうえで再オープンするとコメント。

**次のアクション:** ドキュメント改善と挙動の再整理。

---

### ![PR](icons/git-pull-request.svg) [fix(option): allow empty and blank edges in 'winborder'](https://github.com/neovim/neovim/pull/40112) `#40112`
**ステータス:** Merged | **ラベル:** floatwin, options

**概要:** `'winborder'` のカンマ区切りを literal に `,` で分割し、空フィールドや単一スペースのフィールドを許容するよう修正。これまで `copy_option_part()` がカンマ後の空白を食い、空フィールドを拒否していた。#40111 を修正。

**直近のアクティビティ:** @justinmk のレビューコメントあり。

**変更ファイル:** `options.txt`、`options.gen.lua`、`win_config.c`、`options.lua`、`float_spec.lua`（+38/-26）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [feat: allow winborder to use empty strings or spaces](https://github.com/neovim/neovim/issues/40111) `#40111`
**ステータス:** Closed | **ラベル:** floatwin, options

**概要:** `vim.opt.winborder` に `",,, ,,,, "` のような文字列を設定すると `E474: Invalid argument` になる。左右のみ・背景色のみのボーダー（疑似パディング）が使えないという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（#40112 で対応済みのためクローズ）。

---

### ![PR](icons/git-pull-request.svg) [fix(ui): `gx` should include query string `?` in URL](https://github.com/neovim/neovim/pull/40120) `#40120`
**ステータス:** Open | **ラベル:** ui

**概要:** クエリ文字列（`?`）を含む URL で `gx` が全体を開けず `tag=nvim.txt` のような部分だけ開く問題を修正。`<cfile>` フォールバックの `isfname` に `,?` を含めてテストを追加する。

**直近のアクティビティ:** @wookayin が、ファイル名に `?` は使えないため URL でない場合は `?` で切るべきと指摘。@justinmk はブロッカーか follow-up かを問うている。

**変更ファイル:** `runtime/lua/vim/ui.lua`、`ui_spec.lua`（+43/-1）。

**次のアクション:** URL とファイルパスの判定方針の合意とレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(extmark): virt_lines_overflow "wrap" and "auto"](https://github.com/neovim/neovim/pull/40113) `#40113`
**ステータス:** Open | **ラベル:** marks

**概要:** extmark の `virt_lines_overflow` に "wrap" と "auto" を追加し、横スクロール・切り詰めに加えて折り返しをサポートする。#18282 の提案と #32497 のマージ結果を踏まえる。#18282 をクローズ予定。

**直近のアクティビティ:** @joe-p が、これは #33099 で実現したかったことを満たすとコメント。@zeertzjq が CHANGES_REQUESTED を含む 23 件のレビューを実施。

**変更ファイル:** `api/extmark.c`、`decoration.c`、`drawline.c`、`api.txt`、関連テスト（+485/-13）。

**次のアクション:** 指摘対応とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [refactor(oldtests): always use "nvim -l" test runner](https://github.com/neovim/neovim/pull/40121) `#40121`
**ステータス:** Open | **ラベル:** build, refactor

**概要:** 旧テストを常に `nvim -l` ランナーで実行するようリファクタし、古いランナー関連を大量に削除する。

**直近のアクティビティ:** @justinmk が、`vim_na_files.txt` や `vim-patch.sh` を更新すべきか、Makefile の upstream マージを考えると不適切かを @zeertzjq・@janlazo に問う。

**変更ファイル:** `.github/workflows/test.yml`、`Makefile`、`test/old/` 配下のランナー類（+2/-443）。

**次のアクション:** upstream 追従方針の確認とレビュー。

---

### ![Issue](icons/issue-opened.svg) [feat: vim.mpack.decode_one](https://github.com/neovim/neovim/issues/40115) `#40115`
**ステータス:** Open | **ラベル:** encoding, channels-rpc, lua

**概要:** パイプから受け取るチャンクに複数の msgpack メッセージや末尾の不完全メッセージが含まれるため、単一メッセージ前提の `vim.mpack.decode` では扱えないという課題。位置を返す `decode_one` か、`decode()` への第 2 戻り値追加を求める。

**直近のアクティビティ:** @justinmk が `vim.mpack.decode()` に 2 つ目の戻り値を追加できると提案。@librolibro が次メッセージ開始位置を返す API 案やフラグ案を複数提示し、活発に議論（10 コメント）。

**次のアクション:** API 形状（戻り値追加かフラグか新関数か）の決定。

---

### ![Issue](icons/issue-opened.svg) [project-local `.nvim/` directory which acts like an extension of stdpath()](https://github.com/neovim/neovim/issues/33214) `#33214`
**ステータス:** Open | **ラベル:** workspace-project

**概要:** プロジェクトローカルな `.nvim/` ディレクトリの概念を導入し、`stdpath('data', getcwd())` が `.nvim/data/` を自動検出するなど、stdpath() の拡張として機能させる提案。`:trust` の拡張や undodir などのプロジェクト相対化も含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 設計方針の議論。

---

### ![Issue](icons/issue-opened.svg) [`vim.fs.{dir,find}`: allow callers to observe traversal errors](https://github.com/neovim/neovim/issues/39878) `#39878`
**ステータス:** Open | **ラベル:** lua, filesystem

**概要:** `vim.fs.dir()` が `uv.fs_scandir()` のエラーを握りつぶすため、「一致なし」と「走査不能」が区別できない。後方互換を保つ opt-in の `on_error` コールバック追加を提案する。

**直近のアクティビティ:** @barrettruth がこのアプローチを好まず、このエラー返却パターンは見たことがないとして gpanders のコメントを希望。

**次のアクション:** エラー通知 API の設計合意。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): make --remote wait by default with +async opt-out](https://github.com/neovim/neovim/pull/37951) `#37951`
**ステータス:** Open | **ラベル:** needs:response, remote, AI assisted

**概要:** `--remote` をデフォルトで wait 挙動にし、`+async` で非ブロッキングに opt-out できるようにする。neovim-remote 不要で `GIT_EDITOR` に使えるようにする。quit 系コマンドは `rpcnotify` で送りチャネルクローズ時の競合を避ける。#24788 をクローズ予定。

**直近のアクティビティ:** @giang299 が Linux・Windows 双方で `GIT_EDITOR` 設定が動作したと報告。@justinmk・@rafaelcl292 を中心に 25 件のレビュー。

**変更ファイル:** `remote.txt`、`editor.lua`、`api/events.c`、`main.c`、`remote_spec.lua` ほか（+824/-239）。

**次のアクション:** レビュー指摘への対応（needs:response）。

---

### ![PR](icons/git-pull-request.svg) [docs(lsp): set vim.lsp.ListOpts.pos as optional](https://github.com/neovim/neovim/pull/40119) `#40119`
**ステータス:** Open | **ラベル:** documentation, lsp, typo

**概要:** `vim.lsp.ListOpts.pos` がデフォルトでカーソル位置を取るのに nil 不可の警告が出るため、optional として宣言する小修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/lsp.txt`、`runtime/lua/vim/lsp/buf.lua`（+2/-2）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(test): only test for unibilium if a compilation string exists](https://github.com/neovim/neovim/pull/40118) `#40118`
**ステータス:** Merged | **ラベル:** test, target:release

**概要:** #40116 を `release-0.12` にバックポートする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test/functional/vimscript/has_spec.lua`（+13/-10）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40108) `#40108`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** `version.c` を更新し、GTK4 のポップアップメニュー時のフォーカス形状に関する vim-patch 9.2.0590 を反映する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/version.c`（+23/-18）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Shift modifier on input 0-9 blocked in VSCode terminal when upgrading from Neovim 0.11.7 to 0.12.0](https://github.com/neovim/neovim/issues/38651) `#38651`
**ステータス:** Closed | **ラベル:** status:blocked-external, tui, input, mappings

**概要:** Neovim 0.11.7 → 0.12.0 で、VSCode 統合ターミナルにおいて Shift + 数字（`!@#$%^&*()`）が認識されず `1234567890` になる問題。Windows Terminal では発生しない。

**直近のアクティビティ:** @ghiden が VSCode `1.123` で再び動作するようになったと報告。

**次のアクション:** なし（外部要因のためクローズ）。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0597: [security]: possible code execution with python complete](https://github.com/neovim/neovim/pull/40117) `#40117`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** pythoncomplete/python3complete で生成ソースからデフォルト式や注釈を除去し、コード実行の可能性を塞ぐセキュリティ修正（GHSA-65p9-mwwx-7468）を取り込む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `python3complete.vim`、`pythoncomplete.vim`、`test_plugin_python3complete.vim`（+302/-8）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(test): only test for unibilium if a compilation string exists](https://github.com/neovim/neovim/pull/40116) `#40116`
**ステータス:** Merged | **ラベル:** test, ci:backport release-0.12

**概要:** `-DNDEBUG` ビルドや zig ビルドではコンパイル文字列が無く unibilium テストが失敗するため、両ケースを検出してテストを失敗ではなくスキップするよう修正。

**直近のアクティビティ:** backport bot が `release-0.12` 向けに #40118 を作成。

**変更ファイル:** `test/functional/vimscript/has_spec.lua`（+13/-10）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): replace `libvterm` with `libghostty-vt`](https://github.com/neovim/neovim/pull/39773) `#39773`
**ステータス:** Open | **ラベル:** build, defaults, tui, terminal, ci, AI assisted

**概要:** 組み込みターミナルの基盤ライブラリを libvterm から libghostty-vt へ移行する大型 PR（#33155 で議論）。各コミットで動作を保ちながらフォーカス報告・bracketed paste・入力エンコード・描画などを段階的に移植する。ユーザー可視の挙動変化はほぼない想定。

**直近のアクティビティ:** @clason・@kylesower・@noib3 を中心に 32 件のレビューが進行。

**変更ファイル:** CMake/zig ビルド設定、`runtime/doc/terminal.txt`、ターミナル関連コード多数（+2944/-13085）。

**次のアクション:** レビュー継続と段階的マージ。

---

### ![PR](icons/git-pull-request.svg) [feat(events): add `SessionWritePre` event](https://github.com/neovim/neovim/pull/39688) `#39688`
**ステータス:** Open | **ラベル:** events

**概要:** セッションファイル書き込み直前に発火するイベントがない問題を解決するため `SessionWritePre` を追加。#23000 を置き換え、#39004・#22814 をクローズ予定。

**直近のアクティビティ:** @igorlfs がテストケースを追加し、構成（専用 spec への分離か単一テストか）について意見を求めている。

**変更ファイル:** `autocmd.txt`、`auevents.lua`、`ex_session.c`、`mksession_spec.lua` ほか（+103/-0）。

**次のアクション:** テスト構成の合意とレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): keep synchronized output active across partial flushes](https://github.com/neovim/neovim/pull/40086) `#40086`
**ステータス:** Open | **ラベル:** tui

**概要:** TUI 出力バッファが再描画中に埋まると flush 時に `CSI ? 2026 l` が出て synchronized output が終了し、大きな再描画でちらつく問題を修正。同期出力が有効か追跡し、最終 flush でのみ終了シーケンスを出す。#33205 を直すかもしれないとのこと。

**直近のアクティビティ:** @gpanders が、`flush_buf` 系関数が増えて API が分かりづらいとし、`bool partial` 引数や enum での統合を提案。

**変更ファイル:** `src/nvim/tui/tui.c`、`tui_spec.lua`（+67/-12）。

**次のアクション:** API 整理の検討とレビュー。

---

### ![PR](icons/git-pull-request.svg) [refactor(quickfix): remove duplicate unset of qf_ptr and qf_start](https://github.com/neovim/neovim/pull/40109) `#40109`
**ステータス:** Merged | **ラベル:** refactor, quickfix

**概要:** `qf_free_items` で `qf_ptr` と `qf_start` が二重に NULL 設定されていた重複を削除する小リファクタ。

**直近のアクティビティ:** @zeertzjq が APPROVED。

**変更ファイル:** `src/nvim/quickfix.c`（+0/-2）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(option): Set related options based on detected shell](https://github.com/neovim/neovim/pull/40031) `#40031`
**ステータス:** Open

**概要:** `'shellcmdflag'` がドキュメント通りに `'shell'` の値に応じて設定されない問題を修正。Vim にあった検出ロジックが Nvim の最初のコミットで失われていたため、再導入する。#31528・#28384・#16957 をクローズしようとする。

**直近のアクティビティ:** @ntdiary・@justinmk・@zeertzjq が 7 件レビュー。@ntdiary が OS × シェル種別ごとの動作確認表を提示。

**変更ファイル:** `option.c`、`options.lua`、`path.c`、`startup_spec.lua` ほか（+158/-77）。

**次のアクション:** シェル検出ロジックの妥当性確認とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): save terminal state to file with `:termsave`](https://github.com/neovim/neovim/pull/40097) `#40097`
**ステータス:** Open | **ラベル:** terminal, gsoc

**概要:** ターミナルバッファに安定した 64bit id（`b:term_id`）を付与し、ANSI エスケープ済み状態とメタ data（cwd・cmd・title・timestamp など）を msgpack で `stdpath('state')/term/` に保存する。#40017 のターミナル永続化の一部。

**直近のアクティビティ:** @justinmk のレビューコメントが 6 件。

**変更ファイル:** `terminal.c`、`terminal.h`、`ex_cmds.lua`、`terminal.txt`、関連テスト（+454/-4）。

**次のアクション:** レビュー指摘への対応。

---

### ![Issue](icons/issue-opened.svg) [tracking: terminal buffer persistence](https://github.com/neovim/neovim/issues/40017) `#40017`
**ステータス:** Open | **ラベル:** terminal, gsoc, editor-state

**概要:** GSoC 2026 の `:terminal` バッファ永続化トラッキング Issue。安定した端末 id、状態の export/restore、`:termsave`/`:termrestore` などのコマンド、`:mksession` 連携、ShaDa マーク連携などを項目化する。

**直近のアクティビティ:** @justinmk が、ランダム id は予測しづらく Vim/Nvim らしくないので見直したいとし、原文リストの更新を依頼。

**次のアクション:** id 方式の再検討と各項目の実装。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> Glyph Protocol や glyf アウトラインラスタライザといったフォント・端末描画の基盤実装が進む一方、grapheme 幅やカーソル減算のオーバーフロー修正、a11y 向けの selection 通知など堅牢化も進んでいる。

### ![PR](icons/git-pull-request.svg) [terminal: Glyph Protocol Glossary and request handler implementation](https://github.com/ghostty-org/ghostty/pull/12930) `#12930`
**ステータス:** Open | **ラベル:** vt

**概要:** Glyph Protocol パッケージに glossary とリクエストハンドラのロジックを追加し、仕様準拠のビジネスロジック部分を完成させる。まだターミナル状態には接続しておらず実利用には影響しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/terminal/apc/glyph.zig`、`glyph/Glossary.zig`、`execute.zig`、`request.zig`、`response.zig`（+881/-5）。

**次のアクション:** レビューとターミナル状態への接続。

---

### ![Issue](icons/issue-opened.svg) [macOS: search bar should remember what was previously entered](https://github.com/ghostty-org/ghostty/issues/12516) `#12516`
**ステータス:** Closed | **ラベル:** os/macos, gui

**概要:** macOS アプリのように `cmd-f` 再押下時に前回の検索文字列を覚え、再検索とハイライトを行ってほしいという要望。テキストは選択状態にして上書きしやすくする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [When `key-remap` is set, update configured keybinds](https://github.com/ghostty-org/ghostty/issues/10230) `#10230`
**ステータス:** Open

**概要:** メニューアクセラレータが macOS メニューシステムに横取りされて優先される制限について、`Config.finalize` で key-remap を keybind に適用すれば直せそうという提案。`input.Binding.Set` に `applyRemaps` を追加する案を示す。#10064 の follow up。

**直近のアクティビティ:** @fz-deepeye が ctrl と super の入れ替えを試したがうまくいかず、物理 ctrl に new_split を割り当てたいと具体的な設定例を添えて報告。

**次のアクション:** remap を keybind に適用する実装方針の検討。

---

### ![Issue](icons/issue-opened.svg) [more flexible `copy-on-select` and `middle-click-action`](https://github.com/ghostty-org/ghostty/issues/12600) `#12600`
**ステータス:** Open | **ラベル:** contributor friendly, gui, input

**概要:** `copy-on-select` を `none`/`primary`/`clipboard`/`both` に、`middle-click-action` を `ignore`/`paste-primary`/`paste-clipboard` に拡張する提案。Linux の PRIMARY/CLIPBOARD の慣習に合わせ、両者を独立に設定できるようにする。

**直近のアクティビティ:** @vcastellm が、最新版で `copy-to-clipboard` 関連の設定が効かなくなり tmux + opencode で copy on select が動かないと報告。

**次のアクション:** 設定モデルの設計と実装。

---

### ![Issue](icons/issue-opened.svg) [macOS: Dragging window between displays with different backing scale factors transiently shouldn't change pty dimensions](https://github.com/ghostty-org/ghostty/issues/12925) `#12925`
**ステータス:** Open | **ラベル:** os/macos

**概要:** backing scale factor が異なるディスプレイ間でウィンドウをドラッグすると、pty 寸法が一瞬誤った値に変わり自己修正する。この過渡が TUI アプリの再描画を招き、Claude Code ではスクロールバックが不可逆的に破壊されるという報告。両ディスプレイを同じ倍率にすると再現しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ドラッグ中の pty 寸法変化の抑止。

---

### ![PR](icons/git-pull-request.svg) [terminal: change cell width when wider grapheme detected](https://github.com/ghostty-org/ghostty/pull/10465) `#10465`
**ステータス:** Merged

**概要:** grapheme cluster の合計幅が広い場合（Devanagari の `क्‍ष` など、各コードポイントが幅 1 で合計 2 になるケース）にセル幅を広げる処理を追加。grapheme break 変更と併せて、これらのスクリプトが狭いセルで表示される問題を防ぐ。

**直近のアクティビティ:** @jquast が、foot や Windows Terminal も grapheme を幅 2 に上限化しており、3〜5 セル幅の対応は一般的な制限だと補足。@mitchellh は当初 CHANGES_REQUESTED で、`print` がホットパスのためスループット検証が必須と指摘していた。

**変更ファイル:** `Terminal.zig`、`page.zig`、shaper の coretext/harfbuzz、`unicode/props.zig` ほか（+415/-71）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [libghostty-vt: add options to configure default cursor's style and blink](https://github.com/ghostty-org/ghostty/pull/12900) `#12900`
**ステータス:** Merged | **ラベル:** libghostty

**概要:** `libghostty-vt` に、プログラムが明示指定しない場合や DECSCUSR リセット（`CSI 0 q`）時のデフォルトカーソルのスタイルと点滅を設定する 2 オプションを追加する。

**直近のアクティビティ:** @mitchellh が CI 待ちで承認（APPROVED）。

**変更ファイル:** `include/ghostty/vt/terminal.h`、`c/terminal.zig`、`stream_terminal.zig`（+136/-3）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [font: glyf outline decoder and rasterizer](https://github.com/ghostty-org/ghostty/pull/12893) `#12893`
**ステータス:** Merged | **ラベル:** font

**概要:** glyf テーブルの直接ラスタライズが FreeType/CoreText の API では極めて煩雑（CoreText は不可）なため、独自の glyf アウトラインデコーダ兼ラスタライザを追加する。Glyph Protocol に必要。

**直近のアクティビティ:** @raphamorim のレビューコメントあり。

**変更ファイル:** `font/glyf_rasterize.zig`、`opentype/glyf.zig`、テスト/テストデータ（+1584/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [core: send selection_changed notification](https://github.com/ghostty-org/ghostty/pull/12902) `#12902`
**ステータス:** Merged | **ラベル:** os/macos, a11y

**概要:** 選択状態が変わったことを apprt に伝える signal が無く、スクリーンリーダーが古い選択を読み続ける問題を修正。ペイロードなしの `selection_changed` アクションを追加し、選択変更を `setSelection` 経由に集約する。macOS では debounce 後に accessibility 通知を出す。

**直近のアクティビティ:** @mitchellh が a11y 改善の小さな PR に感謝。@jcollie・@bo2themax・@mitchellh が APPROVED。

**変更ファイル:** `Surface.zig`、`apprt/action.zig`、macOS の `SurfaceView_AppKit.swift` ほか（+93/-17）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [terminal: saturate cursor subtraction in resizeCols](https://github.com/ghostty-org/ghostty/pull/12907) `#12907`
**ステータス:** Merged | **ラベル:** vt

**概要:** 列縮小時の `resizeCols` で、両軸が同時に縮みカーソルが新しい下端以降にあると `self.rows - c.y - 1` がアンダーフローして safety ビルドで panic する問題を修正。saturating 減算を使う。discussion#12905 で報告。

**直近のアクティビティ:** @jcollie が APPROVED。

**変更ファイル:** `src/terminal/PageList.zig`、`c/terminal.zig`（+57/-2）。

**次のアクション:** なし（マージ済み）。

---

## astral-sh/uv

> ワークスペース探索の大幅高速化（#18311）とその一連のベンチマーク整理、resolver のスタックオーバーフロー対策、`uv audit` の SARIF 出力や `uv upgrade` の初期実装など、性能・新機能・堅牢化が同時並行で進む。cache prune/clean のシンボリックリンク逸脱というセキュリティ性の修正も複数マージされた。

### ![Issue](icons/issue-opened.svg) [Support RFC 8628 device auth flow with `uv auth login`](https://github.com/astral-sh/uv/issues/19136) `#19136`
**ステータス:** Open | **ラベル:** enhancement

**概要:** OIDC 対応のパッケージインデックス向けに、`uv auth login` で OAuth2 Device Authorization Grant（RFC 8628、URL とコードを使うブラウザ認証）をサポートする提案。プロトタイプへのリンクを添え、方向性の確認を求める。

**直近のアクティビティ:** @cgravill が内部 Azure Artifacts フィードで動作再現し、PKCE 付き Authorization Code フローを追加した実験も共有。

**次のアクション:** 実装方針の合意と PR 化。

---

### ![PR](icons/git-pull-request.svg) [Add a synthetic large-workspace benchmark](https://github.com/astral-sh/uv/pull/19687) `#19687`
**ステータス:** Open

**概要:** 127 メンバーと大きな生成 TOML を持つ自己完結のワークスペース探索ベンチマークを追加。キャッシュ前後で 1,916ms → 124ms（15.41x）の高速化を示す。

**直近のアクティビティ:** test-inventory bot がテスト 1 件追加を報告。codspeed bot は性能変化なしと報告。

**変更ファイル:** `crates/uv-bench/` 配下の synthetic_workspace 一式（+395/-4）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Add (optional) confirmation before installing with `uvx`](https://github.com/astral-sh/uv/issues/16600) `#16600`
**ステータス:** Open | **ラベル:** enhancement

**概要:** typosquatting 対策として、`uvx` が初回ダウンロード前に確認を促せるようにする提案。`npx` の `--yes`/`--no` を先例とする。

**直近のアクティビティ:** @SahandJ が `uvx tool install` の打ち間違いで何度も困ったとして要望に賛同。

**次のアクション:** 確認プロンプトの設計検討。

---

### ![PR](icons/git-pull-request.svg) [Avoid stack overflows in resolver error reporting](https://github.com/astral-sh/uv/pull/19684) `#19684`
**ステータス:** Open | **ラベル:** bug

**概要:** 多数の却下バージョンを含む解決失敗で PubGrub の導出ツリーが深くなり、正規化・描画・破棄の再帰でスタックがあふれる問題を修正。`stacker` でスタックを拡張し、収集と破棄は反復走査、レポート描画はスタックセーフな実装にする。#19672 をクローズ予定。

**直近のアクティビティ:** @charliermarsh が再帰そのものの回避も検討すると表明。

**変更ファイル:** `uv-resolver` の error.rs・pubgrub・resolver、`pip_compile.rs`（+522/-111）。

**次のアクション:** レビューと再帰回避の検討。

---

### ![PR](icons/git-pull-request.svg) [Centralize index argument handling](https://github.com/astral-sh/uv/pull/19689) `#19689`
**ステータス:** Open | **ラベル:** internal

**概要:** `--index`/`--default-index` の処理を 1 つのヘルパに集約し、これまで重複していた 4 箇所で再利用する小リファクタ。`uv add` の重複処理も簡素化する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-cli/src/options.rs`、`crates/uv/src/settings.rs`（+50/-55）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Airflow workspace benchmark](https://github.com/astral-sh/uv/pull/18409) `#18409`
**ステータス:** Open | **ラベル:** performance, internal

**概要:** Airflow を対象としたウォームキャッシュ・lockfile パースのエンドツーエンドベンチマークを追加する。

**直近のアクティビティ:** codspeed bot が性能変化なしと報告。@konstin・@KingBodo が 5 件のレビュー（APPROVED あり）。

**変更ファイル:** `bench.yml`、`uv-bench/benches/airflow_run.rs` ほか（+220/-54）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Add SARIF output for GitHub code scanning](https://github.com/astral-sh/uv/pull/19685) `#19685`
**ステータス:** Open

**概要:** `uv audit` に `--output-format sarif` を追加し、GitHub code scanning に直接アップロードできる SARIF v2.1.0 を出力する。脆弱性は error、状態は warning として 1 finding 1 result で出す。ruff が pin 済みの `serde_sarif` を再利用。#19660 をクローズ予定。

**直近のアクティビティ:** @lucasew のレビューコメントあり。

**変更ファイル:** `uv-cli/src/lib.rs`、`commands/project/audit.rs`、`tests/it/audit.rs`（+364/-0）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Install with new cache fails sporadically](https://github.com/astral-sh/uv/issues/19674) `#19674`
**ステータス:** Closed | **ラベル:** bug, question

**概要:** コールドキャッシュからのインストールで `Resource temporarily unavailable`（os error 11）が散発的に発生し、10 回中 5 割超が失敗するという報告。

**直近のアクティビティ:** @woodruffw が NFS など networked FS との相互作用を疑い、報告者 @ido123net は個人 PC で再現せず NFS 由来と判断してクローズ。

**次のアクション:** なし（環境固有としてクローズ）。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate による依存更新と検出済み依存を一覧するダッシュボード Issue。長期更新のない abandoned 依存（35 件）も列挙される。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Renovate による継続管理。

---

### ![PR](icons/git-pull-request.svg) [Benchmark synthetic workspace discovery with caching](https://github.com/astral-sh/uv/pull/19688) `#19688`
**ステータス:** Closed

**概要:** `konsti/implement-workspace-caching` 上で 127 メンバーの synthetic ベンチを走らせる PR（main 比 15.41x）。

**直近のアクティビティ:** @konstin が、#18311 が main に入ったためキャッシュ対応ベンチを #19687 に統合したとコメント。

**変更ファイル:** `uv-bench`・`uv-workspace`・`uv-distribution` 多数（+1407/-339）。

**次のアクション:** なし（#19687 に統合しクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Workspace caching 2 bench](https://github.com/astral-sh/uv/pull/19085) `#19085`
**ステータス:** Closed

**概要:** ワークスペースキャッシュのベンチマーク用 PR。

**直近のアクティビティ:** @konstin が、マージ済み #18311 と #18409 のリベース版に置き換えられたとコメント。

**変更ファイル:** `bench.yml`、`uv-bench`・`uv-workspace` ほか（+458/-280）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Cache `pyproject.toml` parsing in workspace discovery](https://github.com/astral-sh/uv/pull/19007) `#19007`
**ステータス:** Closed | **ラベル:** performance

**概要:** ワークスペース探索で `pyproject.toml` のパースをキャッシュする変更。

**直近のアクティビティ:** @konstin が、マージ済み #18311 に置き換えられたとコメント。

**変更ファイル:** `uv-workspace`・`uv-distribution` ほか（+238/-226）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Workspace caching 1 bench](https://github.com/astral-sh/uv/pull/19084) `#19084`
**ステータス:** Closed

**概要:** ワークスペースキャッシュのベンチマーク用 PR（1 本目）。

**直近のアクティビティ:** @konstin が、マージ済み #18311 と #18409 のリベース版に置き換えられたとコメント。

**変更ファイル:** `bench.yml`、`uv-bench`・`uv-workspace` 多数（+1281/-389）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Consider adding documentation on using `uv ...` to use a `.venv` in Jupyter notebooks](https://github.com/astral-sh/uv/issues/6329) `#6329`
**ステータス:** Closed | **ラベル:** documentation

**概要:** Jupyter notebook 内から uv 管理の Python バージョンや venv をどう使うかのドキュメント追加を求める要望。

**直近のアクティビティ:** @Gravifer が、Jupyter に `%uv` ライン magic が追加されたことや `uv venv --seed` などに触れつつ、uv の思想に沿う挙動は判断しかねるとコメント。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Speedup discovery of large workspaces](https://github.com/astral-sh/uv/pull/18311) `#18311`
**ステータス:** Merged | **ラベル:** performance

**概要:** ワークスペースのルートとメンバーの `pyproject.toml` を同じ情報のために何度も読む処理を排除し、キャッシュキー以外の重複読み込みをなくす。airflow で `uv run python -V` が約 1.8x 高速化し、ルート読み込みは 130 回から 2 回に減る。

**直近のアクティビティ:** codspeed bot が性能変化なしと報告。@konstin・@EliteTK を中心に 37 件のレビュー。

**変更ファイル:** `uv-workspace`・`uv-distribution`・各 project コマンド多数（+1108/-330）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add initial hidden `uv upgrade` command](https://github.com/astral-sh/uv/pull/19678) `#19678`
**ステータス:** Open

**概要:** 隠し実装として `uv upgrade <package>` を追加し、依存選択・制約書き換え・resolver 統合を検証する。単一の本番依存について、上限・等値制約を外し下限などを保ちつつ targeted な universal resolution を行う。読み取り専用で `pyproject.toml`/`uv.lock`/`.venv` は変更しない。

**直近のアクティビティ:** test-inventory bot がテスト 25 件追加を報告。

**変更ファイル:** `commands/project/upgrade.rs`、`pyproject_mut.rs`、`tests/it/upgrade.rs` ほか（+1385/-6）。

**次のアクション:** レビューとマニフェスト/lockfile 更新への拡張。

---

### ![Issue](icons/issue-opened.svg) [`uv init` should generate LLM/AI assistant configuration (similar to `bun init`)](https://github.com/astral-sh/uv/issues/19677) `#19677`
**ステータス:** Open | **ラベル:** enhancement

**概要:** AI コーディングアシスタントが uv プロジェクトで raw `python` や `pip` を使い環境を壊す問題を踏まえ、`uv init` が `CLAUDE.md`/`.cursorrules` などの設定を生成する提案。`bun init` を先例とする。

**直近のアクティビティ:** @zhcn000000 は AGENTS.md で十分と指摘。@zsxh1990 は lockfile 破壊の実害があるとして、デフォルト OFF + `--ai-rules` opt-in の最小実装を提案。

**次のアクション:** 生成方式と opt-in 設計の検討。

---

### ![PR](icons/git-pull-request.svg) [Handle invalid tool receipts on uninstall](https://github.com/astral-sh/uv/pull/19686) `#19686`
**ステータス:** Open

**概要:** 名前付きツールのアンインストール時、壊れた `uv-receipt.toml` を receipt 欠落と同様に扱い、読み取りで失敗せず宙ぶらりんの環境を削除する。`uv tool list` の復旧ヒントを実用的にする。#19630 を修正。

**直近のアクティビティ:** test-inventory bot がテスト 1 件追加を報告。

**変更ファイル:** `commands/tool/uninstall.rs`、`tests/it/tool_uninstall.rs`（+51/-18）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Support a list (requirements.txt or reqs.json) in uv lock --upgrade](https://github.com/astral-sh/uv/issues/19675) `#19675`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 特定環境（Microsoft Fabric 1.3）に合わせて多数の依存をピンするため、`uv lock --upgrade` にリスト（requirements.txt や json）を一括で渡せるようにしてほしいという要望。現状はループで `--upgrade-package` を回す必要があり原子性がない。

**直近のアクティビティ:** @woodruffw が、まとめてアップグレードするなら `pyproject.toml` に制約を書くのが idiomatic とし、requirements.txt 対応は形式が大きく難しいとコメント。報告者がユースケースを補足。

**次のアクション:** 一括アップグレードの提供方法の検討。

---

### ![Issue](icons/issue-opened.svg) [Sign published executables for Windows](https://github.com/astral-sh/uv/issues/10336) `#10336`
**ステータス:** Open | **ラベル:** windows, releases

**概要:** ウイルス対策ソフトに誤検知されないよう、Windows リリースに署名したいという要望。物理キー必須は避けたい意向。

**直近のアクティビティ:** @vkbo が、昨日の未署名 exe が問題を起こしているとし、GitHub Actions で署名を扱う事業者の利用を提案。

**次のアクション:** 署名ワークフローの調査。

---

### ![Issue](icons/issue-opened.svg) [Discussion: uv workspaces in a monorepo - thoughts on change-only testing](https://github.com/astral-sh/uv/issues/6356) `#6356`
**ステータス:** Open | **ラベル:** wish, needs-design

**概要:** モノレポでの uv workspaces について、変更コードに影響するテストのみ実行する（bazel/pants 的な）ツーリングが欲しいという議論。単一 lockfile が docker キャッシュを無効化する懸念にも触れる。

**直近のアクティビティ:** @sarimak が、Poetry の複数 lockfile（約 50 サービス）でのリベース競合に苦しんだ経験から、uv の単一 lockfile はむしろ利点だとコメント。

**次のアクション:** モノレポ向けツーリングの設計検討。

---

### ![Issue](icons/issue-opened.svg) [SARIF output on uv audit](https://github.com/astral-sh/uv/issues/19660) `#19660`
**ステータス:** Open | **ラベル:** enhancement

**概要:** govulncheck のように `uv audit` で SARIF を出力するフラグを追加する提案（`uv audit --format sarif`、デフォルトは text）。

**直近のアクティビティ:** @zsxh1990 が、ruff の SARIF emitter 再利用を踏まえた約 150 行の最小 CLI 設計案を提示。

**次のアクション:** PR #19685 での実装レビュー。

---

### ![Issue](icons/issue-opened.svg) [The uv lock -U command is throwing an error.](https://github.com/astral-sh/uv/issues/19670) `#19670`
**ステータス:** Open | **ラベル:** needs-mre

**概要:** ドキュメント記載の `uv lock -U`（および `uv sync`）が実行時に失敗するという報告。

**直近のアクティビティ:** @charliermarsh が `uv sync --upgrade-package <pkg>` の意図か確認し、再現手順を求める。報告者はドキュメント通りの `uv sync -U qwenpaw` が存在しないと言われたと説明。

**次のアクション:** 再現情報（MRE）の提供待ち。

---

### ![Issue](icons/issue-opened.svg) [uv pip install uvicorn==0.41.0 silently drops the supervisors/ subpackage](https://github.com/astral-sh/uv/issues/19683) `#19683`
**ステータス:** Closed | **ラベル:** needs-mre

**概要:** `uv pip install uvicorn==0.41.0` が wheel 展開時に `supervisors/` サブパッケージ（RECORD 記載の 5 ファイル）を黙って取りこぼし、pip では正しく展開されるという報告。

**直近のアクティビティ:** @charliermarsh がクリーンキャッシュでは再現できないとし、報告者 @Pauliehedron がクリーンキャッシュで正常と確認、Hermes bootstrap 由来の stale/corrupt キャッシュが原因としてクローズ。

**次のアクション:** なし（キャッシュ起因としてクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Content-address distributions in the cache by `dirhash`](https://github.com/astral-sh/uv/pull/19665) `#19665`
**ステータス:** Open

**概要:** 展開済み `.whl` を、抽出後のファイルツリーの BLAKE3 ダイジェスト下に格納し、ZIP メタデータや順序が異なっても同じ正規パス・内容に収束させる。streaming/seekable 両方で同一ダイジェストを計算し、安全でない親ディレクトリ entry の除外や重複出力パスの拒否も行う。#1061・#13995・#16786 をクローズ予定。

**直近のアクティビティ:** codspeed bot がフレームグラフ生成失敗を報告した後、性能が 6.69% 劣化する（2 件 regress）と報告。

**変更ファイル:** `uv-cache/src/archive.rs`、`uv-extract/src/dirhash/` ほか（+1933/-256）。

**次のアクション:** 性能劣化への対応とレビュー。

---

### ![Issue](icons/issue-opened.svg) [uv-resolver stack overflow](https://github.com/astral-sh/uv/issues/19672) `#19672`
**ステータス:** Open | **ラベル:** bug

**概要:** 解決不能な制約（boto3<1.36 と botocore>=1.43.14）で `uv pip compile`/`uv lock` が `stack overflow` でクラッシュするという報告。両パッケージのバージョン数が多いことが関係しそうとのこと。

**直近のアクティビティ:** @charliermarsh が、エラーレポート中にスタックがあふれているとコメント（#19684 で対応中）。

**次のアクション:** #19684 のマージ。

---

### ![PR](icons/git-pull-request.svg) [Split uv integration tests into smaller test harnesses](https://github.com/astral-sh/uv/pull/19603) `#19603`
**ステータス:** Open | **ラベル:** performance

**概要:** 統合テストを専用 `uv-integration` クレートへ移し、本番変更が統合テストハーネスを再リンクせず `uv` 実行体だけ再ビルドできるようにする。さらに 12 のコマンド領域別ハーネスに分割し、テスト変更時の再リンクを局所化する。

**直近のアクティビティ:** @charliermarsh のレビューコメントあり。

**変更ファイル:** `nextest.toml`、`uv-integration/Cargo.toml`、各 it テスト多数（+668/-254）。

**次のアクション:** トレードオフの合意とレビュー。

---

### ![Issue](icons/issue-opened.svg) [`uv tool install -e .` does not interact correctly with `dynamic` field of `pyproject.toml`](https://github.com/astral-sh/uv/issues/19541) `#19541`
**ステータス:** Closed | **ラベル:** bug

**概要:** `uv tool install -e .` が古いバージョンをインストールし、dynamic な scripts/version の最新実行ファイルを取りこぼす（キャッシュや git 履歴を読んでいる疑い）という報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（#19591 で対応しクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Rebuild explicit local directory tool installs](https://github.com/astral-sh/uv/pull/19591) `#19591`
**ステータス:** Merged | **ラベル:** bug

**概要:** ローカルプロジェクトが dynamic メタデータを使う場合、`uv tool install -e .` の再インストールで古い editable ビルドが再利用され古いメタデータに戻る問題を修正。明示的なローカルディレクトリのツールインストールを毎回リフレッシュ・再インストールする。#19541 を修正。

**直近のアクティビティ:** @charliermarsh が APPROVED。

**変更ファイル:** `commands/tool/install.rs`、`tests/it/tool_install.rs`（+406/-8）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Avoid following external symlinks during cache clean](https://github.com/astral-sh/uv/pull/19682) `#19682`
**ステータス:** Merged | **ラベル:** bug

**概要:** `uv cache clean <package>` が解決済み参照をすべて archive entry とみなし、Unix で予期せぬシンボリックリンクがあるとキャッシュ外のディレクトリを削除しうる問題を修正。クリーンアップ後の GC を `archive-v0` 配下の正規ターゲットに限定する。#19543 の follow up。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-cache/src/lib.rs`、`tests/it/cache_clean.rs`（+48/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate nix to v0.31.3](https://github.com/astral-sh/uv/pull/19552) `#19552`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による `nix` クレートの 0.31.2 → 0.31.3 パッチ更新。

**直近のアクティビティ:** test-inventory bot がテスト変動（追加 2・削除 115）を報告。

**変更ファイル:** `Cargo.lock`（+9/-9）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [`uv sync --frozen` ignores all index/mirror configuration and uses hardcoded URLs from `uv.lock`](https://github.com/astral-sh/uv/issues/19625) `#19625`
**ステータス:** Closed

**概要:** `uv sync --frozen` が CLI フラグ・環境変数・設定ファイル・pyproject のインデックス/ミラー設定をすべて無視し、`uv.lock` のハードコード URL から取得するという報告。中国などミラー必須環境で問題になるとする。

**直近のアクティビティ:** @charliermarsh が、これは意図的で、`--frozen` は lockfile（ロック済み URL を含む）からインストールするものだと回答。

**次のアクション:** なし（仕様としてクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Move `UV_CUDA_DRIVER_VERSION` and `UV_AMD_GPU_ARCHITECTURE` parsing to `EnvironmentOptions`](https://github.com/astral-sh/uv/pull/18846) `#18846`
**ステータス:** Merged | **ラベル:** internal

**概要:** torch 関連の 2 つの環境変数のパースを、呼び出しスタック深部の直接 `std::env::var()` から集中管理する `EnvironmentOptions` に移す。#14720 の一部。

**直近のアクティビティ:** @zanieb のレビューコメントと @charliermarsh の APPROVED。

**変更ファイル:** `uv-settings/src/lib.rs`、`uv-torch/src/accelerator.rs`・`backend.rs` ほか（+273/-95）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [`uv tool uninstall` fails on corrupt receipts / `uv tool list` recovery hint is un-actionable](https://github.com/astral-sh/uv/issues/19630) `#19630`
**ステータス:** Open | **ラベル:** bug

**概要:** receipt が壊れていると `uv tool list` が `uv tool uninstall <name>` を勧めるのにそれが失敗する。宙ぶらりんの環境と同様、警告付きで削除すべきという指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #19686 での対応。

---

### ![Issue](icons/issue-opened.svg) [`uv cache prune` can delete directories outside `UV_CACHE_DIR` if a symlink is present in a cache bucket](https://github.com/astral-sh/uv/issues/19542) `#19542`
**ステータス:** Closed

**概要:** `uv cache prune` が削除前に `canonicalize()` するため、キャッシュバケット内にシンボリックリンクがあると `UV_CACHE_DIR` 外のディレクトリを削除しうるという報告。再現スクリプトつき。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（#19543 で対応しクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Avoid following symlinks during cache prune](https://github.com/astral-sh/uv/pull/19543) `#19543`
**ステータス:** Merged

**概要:** `uv cache prune` で `rm_rf` に canonicalize 済みパスを渡すのをやめ、archive の canonicalize は参照比較のみに限定する。シンボリックリンクの外部ターゲットではなくリンク自体を削除するよう修正。#19542 をクローズ。

**直近のアクティビティ:** @charliermarsh が APPROVED。

**変更ファイル:** `crates/uv-cache/src/lib.rs`（+83/-4）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Allow configuring preview features in `uv.toml` and `pyproject.toml`](https://github.com/astral-sh/uv/pull/18437) `#18437`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv.toml`/`pyproject.toml` に `preview-features`（真偽値か機能リスト）を追加し、既存の `preview` を置き換える。両者の競合警告や `deny_unknown_fields` との両立など細部を扱う。#15767・#16452・#17202 をクローズ予定。

**直近のアクティビティ:** test-inventory bot がテスト変動（追加 5・削除 1）を報告。

**変更ファイル:** `uv-preview`・`uv-settings` ほか、`docs/concepts/preview.md`（+1218/-100）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Respect extra- and group-scoped sources for production dependencies](https://github.com/astral-sh/uv/pull/19652) `#19652`
**ステータス:** Open

**概要:** extra・group スコープの `[tool.uv.sources]` を本番依存でも尊重するための resolver/lock の大規模変更。多数のスナップショットテストを更新する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-resolver` の lock/pubgrub・`uv-distribution` の metadata 多数（+9887/-532）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [docs: add a small section for malware checks](https://github.com/astral-sh/uv/pull/19680) `#19680`
**ステータス:** Merged | **ラベル:** documentation

**概要:** #18936 で入ったマルウェアチェックのドキュメントを追加する。#19519 をクローズ。

**直近のアクティビティ:** @charliermarsh が APPROVED。

**変更ファイル:** `docs/concepts/projects/sync.md`、`uv.lock`（+15/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Document malware check behavior](https://github.com/astral-sh/uv/issues/19519) `#19519`
**ステータス:** Closed | **ラベル:** documentation

**概要:** OSV ベースのマルウェアチェック（`MAL` advisory、ロック済み解決に対して実行されることなど）の挙動を公式ドキュメントに記載すべきという Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（#19680 で対応しクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Document `--find-links` caching behavior](https://github.com/astral-sh/uv/pull/19585) `#19585`
**ステータス:** Merged | **ラベル:** documentation

**概要:** `--find-links` のキャッシュ挙動をドキュメント化する AI 生成 PR。#12425 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/concepts/cache.md`（+4/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [`uv pip install` with `--find-links` and `--extra-index-url`](https://github.com/astral-sh/uv/issues/12425) `#12425`
**ステータス:** Closed | **ラベル:** question, cache

**概要:** 同一バージョンの wheel が `--find-links` のローカルと `--extra-index-url` の私設 PyPI 双方にある場合、uv が後者を優先するのは意図的か、ローカルを優先する方法はあるかという質問。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（#19585 のドキュメント化でクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Preserve `--locked` error message when `--quiet` is set](https://github.com/astral-sh/uv/pull/19333) `#19333`
**ステータス:** Closed

**概要:** `uv sync --locked --quiet` などが lockfile 古い場合に終了コード 1 でもエラーを表示しない問題を、`stderr_important()` 経由にして修正する。#19326 をクローズ予定だった。

**直近のアクティビティ:** @woodruffw が CHANGES_REQUESTED で、important 表示の一貫した方針が未定のため、この種の変更前に方針決定が必要とコメント。

**変更ファイル:** `commands/project/lock.rs`・`sync.rs`、`printer.rs` ほか（+14/-5）。

**次のアクション:** なし（クローズ済み。方針未定）。

---

### ![Issue](icons/issue-opened.svg) [`available_versions` doesn't account for `--exclude-newer`](https://github.com/astral-sh/uv/issues/12340) `#12340`
**ステータス:** Closed | **ラベル:** bug, testing

**概要:** resolver エラー時の「利用可能バージョン」表示が `--exclude-newer` で除外されたバージョンを数えてしまう問題。

**直近のアクティビティ:** @sharifhsn が #13884 でクローズされたとし、再現例とともに修正前後の出力を提示。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Generate PEP 740 attestations](https://github.com/astral-sh/uv/pull/19489) `#19489`
**ステータス:** Open

**概要:** `uv publish` に PEP 740 attestation の生成サポートを追加し、attest-action などで事前生成せずとも uv 側で生成する。WIP で依存トリミングや CLI フラグ整備が残る。#15618 をクローズ予定。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-pep740` 新設、`uv-publish/src/lib.rs`、`commands/publish.rs` ほか（+1209/-219）。

**次のアクション:** 依存整理とテスト追加。

---

### ![Issue](icons/issue-opened.svg) [uv publish: create attestations](https://github.com/astral-sh/uv/issues/15618) `#15618`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv publish` が PyPI アップロード時に PEP 740 attestation を作成できると便利という要望。

**直近のアクティビティ:** @woodruffw が PR #19489 で着手したと報告。

**次のアクション:** PR #19489 の完成。

---

### ![Issue](icons/issue-opened.svg) [Build backend: reject license classifiers when structured license metadata is present](https://github.com/astral-sh/uv/issues/18421) `#18421`
**ステータス:** Open | **ラベル:** breaking, build-backend

**概要:** PEP 639 に従い、license classifier と構造化 license メタデータ（`project.license`/`license-files`）が両方あるビルドを最終的に拒否すべきという Issue。まず警告、将来の breaking リリースでハードエラー化する 2 段構えを想定。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 警告追加とハードエラー化の段階実施。

---

### ![Issue](icons/issue-opened.svg) [Doc and install: avoid security issue on Windows](https://github.com/astral-sh/uv/issues/10428) `#10428`
**ステータス:** Open | **ラベル:** releases

**概要:** Windows のインストールコマンド（PowerShell 経由）がセキュリティソフトにブロックされるため、インストール手順の改善を求める Issue。

**直近のアクティビティ:** @sweetlilmre が、Netskope が x64 ビルドをマルウェア誤検知し、リリースごとにハッシュを bypass リストへ追加していると報告。

**次のアクション:** 署名・インストール手順の改善検討。

---

### ![Issue](icons/issue-opened.svg) [ruff Checks Files That are Not Included When Copying uv From Distroless Image for GitHub Workflow](https://github.com/astral-sh/uv/issues/19681) `#19681`
**ステータス:** Open | **ラベル:** bug

**概要:** distroless イメージから uv をコピーし、キャッシュをワークスペース配下（`UV_CACHE_DIR`）に置くと、`uv run ruff check` がキャッシュ内の pandas などのファイルまで検査してしまうという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** キャッシュ配置と ruff スキャン範囲の切り分け。

---

### ![Issue](icons/issue-opened.svg) [Dependency resolution failure in workspaces with identical dependency lists that work in standalone packages](https://github.com/astral-sh/uv/issues/12272) `#12272`
**ステータス:** Closed | **ラベル:** question

**概要:** 同じ依存リストでも単体パッケージでは解決でき、ワークスペースの一部だと解決失敗するという報告。

**直近のアクティビティ:** @sharifhsn が #14876 でクローズされたとし、独自インデックスを定義する path 依存の再現例を提示。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Running unit tests with pytest in scripts](https://github.com/astral-sh/uv/issues/9061) `#9061`
**ステータス:** Closed

**概要:** インライン依存を持つ uv スクリプトに pytest でユニットテストを実行したいという要望。`uv run pytest my_script.py` のような形を望む。

**直近のアクティビティ:** @klmr が、これは pytest に限らずスクリプトのコードと対話する全ツール（LSP など）に関わるとし、`uv run nvim script.py` で venv コンテキストを使いたいとコメント。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Remove bz/lzma/xz support from ZIP handling](https://github.com/astral-sh/uv/pull/18927) `#18927`
**ステータス:** Open | **ラベル:** breaking

**概要:** Python パッケージングの標準でない bzip2/lzma/xz 圧縮を ZIP 依存から外す breaking change。tarball 側は今回対象外。0.12 まではマージしない想定。#16911 が背景。

**直近のアクティビティ:** test-inventory bot がテスト変動（追加 6・削除 3）を報告。@woodruffw・@konstin が 6 件のレビュー（APPROVED あり）。

**変更ファイル:** `uv-extract`・`uv-distribution-filename` ほか多数（+533/-353）。

**次のアクション:** 0.12 リリースに合わせたマージ。

---

### ![Issue](icons/issue-opened.svg) [Prune stale `exclude-newer-package` entries on `uv lock`](https://github.com/astral-sh/uv/issues/18792) `#18792`
**ステータス:** Open | **ラベル:** enhancement

**概要:** セキュリティ対応で追加した `exclude-newer-package` が、cooldown 経過後も `pyproject.toml` に死んだ設定として残る。`uv lock` 時に不要になったエントリを検出・削除する提案。

**直近のアクティビティ:** @isbldr が、pnpm の `minimumReleaseAgeExclude` のように特定バージョンの bypass を許す別解を提案。

**次のアクション:** 自動 prune または bypass 方式の検討。

---

### ![Issue](icons/issue-opened.svg) [exclude-newer-package config getting undone by other dependency actions](https://github.com/astral-sh/uv/issues/17999) `#17999`
**ステータス:** Open | **ラベル:** question

**概要:** 相対 `exclude-newer` 環境で `--exclude-newer-package` を使って個別更新すると、別パッケージの更新時に先に更新したパッケージがダウングレードされてしまうという報告。

**直近のアクティビティ:** @isbldr が pnpm の先例に触れつつ、推移依存にも効くため、より許容的な方が勝つ `exclude-newer-behavior = "prefer-more-recent"` のような案を提示。

**次のアクション:** exclude-newer の優先規則の見直し。

---

### ![PR](icons/git-pull-request.svg) [Continue uninstalling tools after encountering a tool environment with a missing receipt](https://github.com/astral-sh/uv/pull/19319) `#19319`
**ステータス:** Closed

**概要:** 複数ツールのアンインストールで先頭の receipt が壊れていると、以降のツールがスキップされる問題を `return Ok(())` から `continue` への変更で修正する。#19219 を修正。

**直近のアクティビティ:** @EliteTK が、#19623 がマージされたためクローズとコメント。

**変更ファイル:** `commands/tool/uninstall.rs`、`tests/it/tool_uninstall.rs`（+42/-1）。

**次のアクション:** なし（#19623 に置き換えクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Validate egg top-level entries as identifiers](https://github.com/astral-sh/uv/pull/19679) `#19679`
**ステータス:** Merged | **ラベル:** bug

**概要:** egg の `top_level.txt` エントリを、区切り文字なしでも `C:target` のようなドライブ相対パスを弾くよう、Python 識別子として検証してからモジュール削除に使う。Windows 統合テストつき。

**直近のアクティビティ:** @woodruffw のレビューと APPROVED。

**変更ファイル:** `uv-install-wheel/src/uninstall.rs`、`tests/it/pip_uninstall.rs`（+76/-14）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Integrate Hook Support into the `uv` Command](https://github.com/astral-sh/uv/issues/11826) `#11826`
**ステータス:** Open | **ラベル:** enhancement

**概要:** npm の husky/lint-staged のように、`uv` 実行時に特定のフックを自動起動する機能の要望。pre-commit では強制インストールできない点を課題とする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** フック機構の設計検討。

---

### ![Issue](icons/issue-opened.svg) [Failure to download packages from Azure Artifacts starting version 0.11.8](https://github.com/astral-sh/uv/issues/19200) `#19200`
**ステータス:** Closed | **ラベル:** bug, needs-mre

**概要:** uv 0.11.8 以降、Azure Artifacts からのパッケージ取得が 401 Unauthorized で失敗し、0.11.7 に戻すと直るという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [`uv pip tree` machine readable output](https://github.com/astral-sh/uv/issues/4711) `#4711`
**ステータス:** Closed | **ラベル:** help wanted, cli

**概要:** `uv pip tree` の機械可読出力（`-j` での JSON など）を求める Issue。

**直近のアクティビティ:** @zsol が、`uv export` が存在し関心も低いため取り組まない方がよいとコメント。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [mention "correct" thing to put in #! for a script in documentation?](https://github.com/astral-sh/uv/issues/12153) `#12153`
**ステータス:** Closed | **ラベル:** documentation

**概要:** スクリプトの shebang に `#!/usr/bin/env -S uv run -s` を書くのが正しいと思うが、ドキュメントに明記してほしいという要望。

**直近のアクティビティ:** @sharifhsn が #11553 でドキュメント化されクローズと報告。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Creating `Package Application` with different target subfolder](https://github.com/astral-sh/uv/issues/12078) `#12078`
**ステータス:** Closed | **ラベル:** question

**概要:** パッケージ名と異なるターゲットサブフォルダ（`example-pkg` に対し `ep`）を使うと `ModuleNotFoundError` になるという質問。

**直近のアクティビティ:** @sharifhsn が、uv build backend の `module-name` で対応できる（uv 0.7.19 以降）と回答。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Create complementary dependencies for extra-only sources](https://github.com/astral-sh/uv/pull/18272) `#18272`
**ステータス:** Open | **ラベル:** bug

**概要:** `[tool.uv.sources]` の `extra = "foo"` をその extra が有効なときだけ適用し、`extra != "foo"` 用にソース未適用の補完的依存を生成する。例では foo 有効時は example.com、無効時は PyPI からインストールする。#17967・#18229 をクローズ予定。

**直近のアクティビティ:** codspeed bot が性能変化なしと報告。test-inventory bot がテスト 106 件追加を報告。@konstin がロジックを追いづらいとして質問形式のコメント。

**変更ファイル:** `uv-resolver` の pubgrub/resolver、`uv-distribution` の metadata 多数（+13496/-3300）。

**次のアクション:** ロジックの説明とレビュー継続。

---

### ![Issue](icons/issue-opened.svg) [Add `--torch-backend=auto` Flag to `uv add torch`](https://github.com/astral-sh/uv/issues/12994) `#12994`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 現状 `uv pip` で使える `--torch-backend=auto`（互換 CUDA 版 PyTorch の自動選択）を `uv add torch` でも使えるようにする要望。

**直近のアクティビティ:** @kipavy が `uv sync` でも欲しいとコメント。

**次のアクション:** add/sync への対応検討。

---

### ![Issue](icons/issue-opened.svg) [`uv add` hangs on `setuptools.build_meta:__legacy__.get_requires_for_build_wheel`](https://github.com/astral-sh/uv/issues/12061) `#12061`
**ステータス:** Closed | **ラベル:** external

**概要:** `uv add apache-beam` 後の `uv add datasets` がビルド中にハングするという報告。

**直近のアクティビティ:** @sharifhsn が、上流の multiprocess 0.70.19 で修正済みで pip でも 0.70.9 は失敗する uv 非依存の問題と説明（version 検出に float 演算を使っていたのが原因）。

**次のアクション:** なし（上流修正でクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Fix uv init layout for stubs packages](https://github.com/astral-sh/uv/pull/19671) `#19671`
**ステータス:** Open

**概要:** `uv init --package foo-stubs` が `uv_build` の期待する stub 専用レイアウト（`src/foo-stubs/__init__.pyi`、console script なし）を生成するよう修正。#19663 を修正。

**直近のアクティビティ:** @zanieb のレビューコメントあり。

**変更ファイル:** `commands/project/init.rs`、`tests/it/init.rs`（+78/-3）。

**次のアクション:** レビューとマージ判断。

---

## microsoft/vscode

> Agents window（sessions レイヤー）と Copilot/CLI 統合まわりの開発が突出して活発で、セッション復元・モデル選択・agent host のリソース/権限統合・スモークテスト整備が多数進む。並行して 2026 テーマの色やコーナー半径・スペーシングのトークン化、npm から pnpm への大規模移行 PoC、各種 GPU 描画・リモート SSH 関連のバグ報告が集まっている。

### ![Issue](icons/issue-opened.svg) [Support for 1M context window selection in Claude harness](https://github.com/microsoft/vscode/issues/319039) `#319039`
**ステータス:** Closed | **ラベル:** UBB

**概要:** 各モデルで最大コンテキストをデフォルトにすべきという提案。Claude harness はもともと最大コンテキストを使うため、ユーザーはトークン上限の恩恵のみ受ける。

**直近のアクティビティ:** @cwebster-99 が hover と context widget に 1M 表示を確認。@lramos15 が、Claude harness のプロンプトは管理しておらず SDK がエンド to エンドを制御するため変更不要と判断しクローズ。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix issue reporter wizard dropping preset extension `data`](https://github.com/microsoft/vscode/pull/320103) `#320103`
**ステータス:** Open

**概要:** 新しい `IssueReporterOverlay` ウィザードが、`extensionId` 同時指定時に `workbench.action.openIssueReporter` の `data` ペイロードを取りこぼし、Copilot の NES フィードバックの STest/記録コンテキストが失われる問題を修正。#320101 を修正。

**直近のアクティビティ:** @ulugbekna がより深いバグ（空リストで `extensionId` が undefined に上書きされる）を見つけて force-push し、ガードを `!this.includeExtensionData` に切り替えた経緯を説明。

**変更ファイル:** `issueReporterOverlay.ts`、`inlineEditDebugComponent.ts`（+42/-4）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Update hover background colors and titlebar widget styling for 2026 themes](https://github.com/microsoft/vscode/pull/320099) `#320099`
**ステータス:** Merged

**概要:** 2026 ダーク/ライトテーマの hover 背景色を調整し、titlebar widget のスタイルを整える。

**直近のアクティビティ:** Copilot reviewer がツールバー系 UI の一貫性向上とコメント。@lramos15 が APPROVED。

**変更ファイル:** `2026-dark.json`、`2026-light.json`、`openInVSCode.css`（+11/-9）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [NES: more detailed speculation prediction for patch-based prompts](https://github.com/microsoft/vscode/pull/320107) `#320107`
**ステータス:** Open

**概要:** patch ベースの xtab モデルに送る「予測出力」speculation を実験フラグで変える仕組みを追加し、より豊かなヒントが first-token レイテンシや精度を改善するか測れるようにする。`PatchModelPrediction` enum と 4 バリアントを導入。

**直近のアクティビティ:** Copilot reviewer が概要をコメント。

**変更ファイル:** `xtab/node/xtabProvider.ts`・`responseFormatHandlers.ts` ほか（+190/-39）。

**次のアクション:** レビューと実験計測。

---

### ![PR](icons/git-pull-request.svg) [sessions: retain selected session type across window reload](https://github.com/microsoft/vscode/pull/320084) `#320084`
**ステータス:** Open

**概要:** ウィンドウ再読み込み時に Agents window の選択済みセッションタイプが保持されず常に Copilot Chat の `copilotcli` に戻る問題を修正。picker が手動選択時しか永続化しない点と、`NewChatWidget` が早すぎるタイミングで保存値を検証して無効化する点の 2 つが原因。

**直近のアクティビティ:** Copilot reviewer が非同期登録でも復元できるようにする旨をコメント。@lramos15 が APPROVED。

**変更ファイル:** `newChatWidget.ts`、`sessionTypePicker.ts`、SKILL/SESSIONS ドキュメント（+56/-62）。

**次のアクション:** マージ。

---

### ![Issue](icons/issue-opened.svg) [Toggle "truncation" in responses API request body](https://github.com/microsoft/vscode/issues/320096) `#320096`
**ステータス:** Open

**概要:** `useResponsesApiTruncation` 設定にかかわらず Responses API のリクエスト body に `truncation` フィールドが常に含まれ、対応しないモデルで `unknown field "truncation"` エラーになる。フィールド有無を制御するトグル追加の要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トグル追加の検討。

---

### ![Issue](icons/issue-opened.svg) [Delete Claude session types in chat](https://github.com/microsoft/vscode/issues/319989) `#319989`
**ステータス:** Open

**概要:** delegate session で Claude code（BYOM）を使えるが、チャットの Claude セッションを削除する手段が UI にないため削除機能の追加を求める要望。

**直近のアクティビティ:** @kalam-87 が #317216 と同じ問題と指摘。@kalam-ul が `~/.claude/projects/<path>/*.jsonl` などを手動削除する回避策を表で提示。

**次のアクション:** 削除 UI の実装検討。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Distinct syntax highlighting for the 'step' argument in python slicing](https://github.com/microsoft/vscode/issues/319205) `#319205`
**ステータス:** Closed

**概要:** Python のスライス `text[0:10:2]` で range（start:stop）と step を別色でハイライトしたいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [[Indonesia] Kursor Mouse Hilang saat hover di area kode](https://github.com/microsoft/vscode/issues/319186) `#319186`
**ステータス:** Closed | **ラベル:** new release

**概要:** コード領域外をクリック後にコード領域へホバーするとマウスカーソルが消えるという報告（Windows、再インストールでも再発、最新版更新後から発生）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Auto-completion disabled for languages with ID >= 256 due to LanguageID bits overlapping with TokenType bits](https://github.com/microsoft/vscode/issues/319118) `#319118`
**ステータス:** Open | **ラベル:** info-needed

**概要:** LineTokens メタ生成の LanguageID 8bit 上限により、ID が 256 以上の言語で自動補完が無効化されるという報告。

**直近のアクティビティ:** @alexdima が、実際に 256 を超える言語 ID を登録している環境なのか理論上の問題かを確認。

**次のアクション:** 報告者からの情報待ち。

---

### ![PR](icons/git-pull-request.svg) [Migrate package manager from npm to pnpm](https://github.com/microsoft/vscode/pull/320104) `#320104`
**ステータス:** Open

**概要:** VS Code モノレポを npm から pnpm（`pnpm@10.32.1` via corepack）へ移行する大規模 PR。明示的メンバーの `pnpm-workspace.yaml` 追加、`build/`/`remote/` の standalone 化、`node-linker=hoisted`、インストールオーケストレーションの書き換えなどを含む。

**直近のアクティビティ:** Copilot reviewer が 2 万行超で自動レビュー不可と報告。

**変更ファイル:** 多数の workflow・`.npmrc`・`build/npm/` スクリプト（+49703/-81482）。

**次のアクション:** レビューと再検証。

---

### ![Issue](icons/issue-opened.svg) [Frequent missing text in editors and terminal](https://github.com/microsoft/vscode/issues/318950) `#318950`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** エディタやターミナルでテキストが見えなくなる（カーソルや空白マーカーは見える）という再現手順不明のバグ。ウィンドウ再読み込みで一時的に解消。

**直近のアクティビティ:** @alexdima が GPU メモリの問題に見えるとし、`--disable-gpu` での再現可否を確認。

**次のアクション:** `--disable-gpu` での切り分け。

---

### ![Issue](icons/issue-opened.svg) [High CPU from VS Code `rg` when Java workspace detection scans pnpm `node_modules`](https://github.com/microsoft/vscode/issues/318897) `#318897`
**ステータス:** Open | **ラベル:** info-needed

**概要:** フロント/バックエンド混在の multi-root ワークスペースで、Java ワークスペース検出が pnpm の `node_modules` を `rg` でスキャンし高 CPU になるという報告。

**直近のアクティビティ:** @alexdima が、Java 拡張を `*` でアクティベートするよう `package.json` を編集して再現するか確認を依頼。

**次のアクション:** 報告者の確認待ち。

---

### ![Issue](icons/issue-opened.svg) [Agent Feedback Feature for AHP for Agents Window](https://github.com/microsoft/vscode/issues/320067) `#320067`
**ステータス:** Open | **ラベル:** feature-request, agents-window

**概要:** agent host protocol と統合したコメント機能を Agents window に実装する提案。ユーザーがコード片を選んでコメントし、レビューエージェントがコメントを作成・応答する。状態はサーバー側で管理。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 設計の具体化。

---

### ![Issue](icons/issue-opened.svg) [New Agents windows does not use my profiles and use Default instead](https://github.com/microsoft/vscode/issues/319918) `#319918`
**ステータス:** Open | **ラベル:** new release

**概要:** 新しい Agents window が現在のプロファイルを尊重せず Default で開くという報告。

**直近のアクティビティ:** @sandy081 が、Agents window はあえてプロファイルのカスタマイズを適用しない設計だと説明し、ユースケースを尋ねている。

**次のアクション:** ユースケース確認後の対応判断。

---

### ![Issue](icons/issue-opened.svg) [UI Polish: Replace the send up arrow with a return button](https://github.com/microsoft/vscode/issues/314829) `#314829`
**ステータス:** Open

**概要:** 送信ボタンの上矢印を return ボタンに置き換える UI 改善の提案。

**直近のアクティビティ:** @mrleemurray が、vscode ウィンドウ側も更新するか確認。

**次のアクション:** 適用範囲の決定。

---

### ![Issue](icons/issue-opened.svg) [Minimap #region section headers not rendering in embedded languages (.vue, .svelte, .astro)](https://github.com/microsoft/vscode/issues/318644) `#318644`
**ステータス:** Open | **ラベル:** feature-request, editor-minimap

**概要:** 埋め込み言語（.vue/.svelte/.astro）で `#region` のセクションヘッダーが minimap に表示されないという報告。

**直近のアクティビティ:** bot がバックログ候補として 60 日以内に 20 upvote が必要と案内。

**次のアクション:** コミュニティ投票。

---

### ![Issue](icons/issue-opened.svg) [Terminal missing letters and black blocks replacing content](https://github.com/microsoft/vscode/issues/319943) `#319943`
**ステータス:** Open | **ラベル:** new release

**概要:** ターミナルで多色/装飾テキスト（atuin、Maven、Angular CLI など）の文字が欠けたり黒ブロックに置き換わる。`terminal.integrated.gpuAcceleration = off` で回避可。1.122.1→1.123.0 で発生。

**直近のアクティビティ:** @jerch が Chrome/ANGLE 側の問題と見られ xterm.js では回避済み（PR #5987）と説明。@regunakyle も同症状を報告し GPU アクセラ無効で解消と確認。

**次のアクション:** xterm.js 側回避の取り込み検討。

---

### ![Issue](icons/issue-opened.svg) [Enable use of local agent in Agents Window](https://github.com/microsoft/vscode/issues/318358) `#318358`
**ステータス:** Open | **ラベル:** feature-request, agents-window, local-harness

**概要:** Agents window が GitHub CLI に限定され、より高速な local オプションが使えない理由を問い、解放を求める要望。

**直近のアクティビティ:** bot がバックログ候補として投票を案内。

**次のアクション:** コミュニティ投票。

---

### ![Issue](icons/issue-opened.svg) [Agents Window - Import Session](https://github.com/microsoft/vscode/issues/318632) `#318632`
**ステータス:** Open | **ラベル:** feature-request, agents-window, local-harness

**概要:** 既存のエージェントチャットセッションを Agents window へインポートできるようにしたいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** インポート機能の検討。

---

### ![Issue](icons/issue-opened.svg) [foo](https://github.com/microsoft/vscode/issues/320105) `#320105`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容のないスパム Issue。

**直近のアクティビティ:** bot がコミュニティガイドライン非適合/スパムとしてクローズ。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [sdofa](https://github.com/microsoft/vscode/issues/320106) `#320106`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容のないスパム Issue。

**直近のアクティビティ:** bot がスパムとしてクローズ。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Fork not working in Agents window](https://github.com/microsoft/vscode/issues/318556) `#318556`
**ステータス:** Open | **ラベル:** agents-window, copilot-chat, local-harness

**概要:** Agents window でセッションを fork しようとしても機能しないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現調査。

---

### ![Issue](icons/issue-opened.svg) [Pantalla lenta](https://github.com/microsoft/vscode/issues/318328) `#318328`
**ステータス:** Open | **ラベル:** info-needed, ai-translated

**概要:** テキストを多く選択してカーソルを動かすと下線が遅れて追従し、削除やスクロールが 0.75 倍速のように遅いというパフォーマンス報告。

**直近のアクティビティ:** @alexdima がパフォーマンス問題のガイド手順とプロファイル添付を依頼。

**次のアクション:** プロファイル提供待ち。

---

### ![Issue](icons/issue-opened.svg) [[1.121] Remote extension host dies repeatedly on Windows hosts](https://github.com/microsoft/vscode/issues/318300) `#318300`
**ステータス:** Open | **ラベル:** info-needed

**概要:** リモート Windows ホスト接続時に 60 秒以内に extension host が繰り返しクラッシュ（code 1）するという報告。

**直近のアクティビティ:** @alexdima がログから原因が読み取れないとし、SSH/WSL/tunnel のどれを使っているか確認。

**次のアクション:** 接続方式などの情報待ち。

---

### ![PR](icons/git-pull-request.svg) [use nonce when comparing customizations changes](https://github.com/microsoft/vscode/pull/320102) `#320102`
**ステータス:** Open

**概要:** `BaseAgentHostSessionsProvider` のセッションカスタマイズ変更検出を改善し、関連入力が実際に変わったときだけ `onDidChangeCustomizations` などを発火させて不要な picker 再計算を減らす。

**直近のアクティビティ:** Copilot reviewer が概要をコメント。

**変更ファイル:** `baseAgentHostSessionsProvider.ts`（+23/-3）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Allow custom endpoint models for VS Code Copilot CLI sessions](https://github.com/microsoft/vscode/issues/320056) `#320056`
**ステータス:** Open

**概要:** GitHub Copilot Local 用に設定したカスタムエンドポイントを、VS Code 同梱の Copilot CLI でも使えるようにする要望。`chatLanguageModels.json` のモデルが CLI のドロップダウンに出ない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** カスタムエンドポイント対応の検討。

---

### ![Issue](icons/issue-opened.svg) [The editor goes blank after some time](https://github.com/microsoft/vscode/issues/318277) `#318277`
**ステータス:** Open | **ラベル:** info-needed

**概要:** 5〜10 分使用するとエディタが毎回ブランクになるという報告（Ubuntu 20.04）。

**直近のアクティビティ:** @alexdima が `--disable-gpu` での再現可否を確認。

**次のアクション:** GPU 無効での切り分け。

---

### ![Issue](icons/issue-opened.svg) [Getting Copilot actions when Chat features are OFF](https://github.com/microsoft/vscode/issues/319060) `#319060`
**ステータス:** Open | **ラベル:** info-needed

**概要:** チャット機能を全部無効にしているのに UI に AI/Chat アクションが出るという苦情。

**直近のアクティビティ:** @alexdima が、それは拡張が提供する code actions に見えるとし、使用言語を確認。

**次のアクション:** 言語情報の確認。

---

### ![PR](icons/git-pull-request.svg) [[PoC] Migrate package manager from npm to pnpm](https://github.com/microsoft/vscode/pull/320100) `#320100`
**ステータス:** Closed

**概要:** npm から pnpm への移行 PoC（#320104 の前身）。

**直近のアクティビティ:** @ulugbekna が、fork ではなく upstream にドラフトとして push し直した #320104 に置き換えたとコメント。

**変更ファイル:** workflow・`.npmrc`・`build/npm/` ほか（+561/-81451）。

**次のアクション:** なし（#320104 に置き換えクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Add local agent host test](https://github.com/microsoft/vscode/pull/319590) `#319590`
**ステータス:** Open

**概要:** ローカル Agent Host を介した Agents window のスモークテストスイートを追加し、非同期で埋まるセッションタイプ選択を安定させる自動化更新を加える。

**直近のアクティビティ:** Copilot reviewer が概要をコメント。

**変更ファイル:** `agentHostStateManager.ts`、smoke/automation テスト一式（+256/-23）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [ChatModes: throttle update requests, only fire on change](https://github.com/microsoft/vscode/pull/320088) `#320088`
**ステータス:** Merged

**概要:** チャットモードの更新リクエストをスロットルし、計算済みモードデータが実際に変わったときだけ変更イベントを発火させる。複数のカスタマイズ系ペイロードの構造的等価ヘルパも追加。

**直近のアクティビティ:** @alexr00 が APPROVED。

**変更ファイル:** `chatModes.ts`、`pluginParsers.ts`、`promptsService.ts` ほか（+309/-39）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Issue reporter wizard drops preset extension "data" when opened with extensionId](https://github.com/microsoft/vscode/issues/320101) `#320101`
**ステータス:** Open

**概要:** `openIssueReporter` を `extensionId` と `data` 付きで起動すると、新しいウィザード型レポーターが `data` を取りこぼし、Copilot の NES「Report feedback」の添付コンテキストが本文に入らないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #320103 での対応。

---

### ![Issue](icons/issue-opened.svg) [[accessibility] screen reader cannot announce results while navigating between results in search box](https://github.com/microsoft/vscode/issues/320098) `#320098`
**ステータス:** Open | **ラベル:** new release

**概要:** 検索ボックスの結果ツリーで矢印キー移動時に NVDA が結果を読み上げられないアクセシビリティ回帰。1.123.0 で発生した可能性。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 回帰の調査。

---

### ![PR](icons/git-pull-request.svg) [Skip for now CLI smoke tests](https://github.com/microsoft/vscode/pull/320097) `#320097`
**ステータス:** Merged

**概要:** Copilot CLI 関連のスモークテスト（Chat と Agents Window）を一時的にスキップする。

**直近のアクティビティ:** @alexr00 が APPROVED（一度 DISMISSED ののち）。

**変更ファイル:** `chatSessions.test.ts`、`copilotCli.test.ts`（+2/-2）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [https://github.com/microsoft/vscode/issues/320062#issue-4595458815](https://github.com/microsoft/vscode/issues/320094) `#320094`
**ステータス:** Closed | **ラベル:** spam

**概要:** 他 Issue（#320062）のコメントを転記しただけの重複/スパム Issue。

**直近のアクティビティ:** bot がスパムとしてクローズ。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix forwarded ports status bar item to toggle the ports view](https://github.com/microsoft/vscode/pull/320090) `#320090`
**ステータス:** Open

**概要:** 転送ポートのステータスバー項目クリックがビューにフォーカスするだけだったのを、表示済みなら閉じる本来のトグル動作に置き換える（Problems と同様）。定数を common/browser に切り出す。#320086 を修正。

**直近のアクティビティ:** Copilot reviewer がトグル用アクション追加を概要コメント。@thernstig のレビューコメント。

**変更ファイル:** `remote/browser/messages.ts`・`remote.contribution.ts`・`remoteExplorer.ts` ほか（+47/-1）。

**次のアクション:** レビュー（類似 #320092 と重複あり）。

---

### ![PR](icons/git-pull-request.svg) [bump distro](https://github.com/microsoft/vscode/pull/319955) `#319955`
**ステータス:** Open

**概要:** `package.json` の `distro` ポインタを新しいコミット SHA に更新する。

**直近のアクティビティ:** @rzhao271・@benvillalobos・@ulugbekna が APPROVED。

**変更ファイル:** `package.json`（+1/-1）。

**次のアクション:** マージ。

---

### ![Issue](icons/issue-opened.svg) [New issue reporter prev/next page buttons placement](https://github.com/microsoft/vscode/issues/320095) `#320095`
**ステータス:** Open

**概要:** 新しい issue reporter の prev/next ページボタンを他のボタンの近く（左下から左上へ）に配置すべきという UI 指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ボタン配置の調整。

---

### ![PR](icons/git-pull-request.svg) [Make Forwarded Ports status bar entry toggle the Ports view](https://github.com/microsoft/vscode/pull/320092) `#320092`
**ステータス:** Open

**概要:** 転送ポートのステータスバー項目を、`<TUNNEL_VIEW_ID>.toggle` コマンドでビューの開閉トグルにする（#320090 と同趣旨の別実装）。

**直近のアクティビティ:** @alexr00 が APPROVED。Copilot reviewer も概要をコメント。

**変更ファイル:** `remoteExplorer.ts`（+14/-1）。

**次のアクション:** #320090 とどちらを採用するか整理。

---

### ![Issue](icons/issue-opened.svg) [Vscode crashing constantly](https://github.com/microsoft/vscode/issues/318951) `#318951`
**ステータス:** Open | **ラベル:** new release

**概要:** 何もしなくても頻繁にクラッシュし、`rg` プロセスが CPU 50% を消費するという報告。V8 のヒープ枯渇（~4GB）ログあり。

**直近のアクティビティ:** @flogrows が同様のクラッシュを報告し、リモートワークスペースで renderer が ~4.5GB まで増えて error 132 でクラッシュ、`chat.disableAIFeatures` などで回避と説明。

**次のアクション:** メモリリークの調査。

---

### ![PR](icons/git-pull-request.svg) [Remove unused import and update high contrast button colors](https://github.com/microsoft/vscode/pull/320085) `#320085`
**ステータス:** Merged

**概要:** 拡張アクションボタンの未使用 import を削除し、ハイコントラストのボタン枠色を標準セカンダリ枠トークンに合わせる。

**直近のアクティビティ:** @alexr00 が APPROVED。

**変更ファイル:** `extensionsActions.ts`（+3/-3）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [sessions: inherit active session workspace for new session view](https://github.com/microsoft/vscode/pull/320091) `#320091`
**ステータス:** Merged

**概要:** Agents window で Ctrl/Cmd+N などによる新規セッションが、最後に作った新規セッションのワークスペースではなく現在開いているセッションのワークスペースを継承するようにする。`openNewSessionView` に `inheritWorkspaceFromActiveSession` フラグを追加。

**直近のアクティビティ:** CODENOTIFY bot が通知。@alexr00 が APPROVED。

**変更ファイル:** `sessionsView.ts`、`sessionsManagementService.ts` ほか（+189/-9）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Remote session/task persistence: Detach, preserve, and reattach all active tasks, terminals, and extension processes](https://github.com/microsoft/vscode/issues/320044) `#320044`
**ステータス:** Open | **ラベル:** feature-request, remote

**概要:** VS Code Remote で、Copilot agent だけでなくターミナル・タスク・拡張プロセス・デバッグセッションまで含めた全リモートセッションを detach/reattach できるようにする要望。

**直近のアクティビティ:** bot がバックログ候補として投票を案内。

**次のアクション:** コミュニティ投票。

---

### ![Issue](icons/issue-opened.svg) [Not showing code in proper block and indent](https://github.com/microsoft/vscode/issues/309007) `#309007`
**ステータス:** Closed | **ラベル:** agent-behavior

**概要:** Codex（model 5.3）でコード出力がコードブロックに包まれず、インデントが崩れるという報告。

**直近のアクティビティ:** @Ahwar が GPT-5.3 系でのみ起きると補足。bot が、追跡しづらい AI 品質の問題として他モデル利用を推奨しクローズ。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [nes-datagen: stream multi-GB inputs/outputs and switch output to JSON Lines](https://github.com/microsoft/vscode/pull/320089) `#320089`
**ステータス:** Merged

**概要:** `nes-datagen` が複数 GB 入力で `File size > 2 GiB` や V8 の ~512MiB 文字列上限により失敗する問題を、ストリーミング処理と JSON Lines 出力で解決する。バックプレッシャ対応の書き込みヘルパも追加。

**直近のアクティビティ:** @alexr00 が APPROVED。

**変更ファイル:** `test/pipeline/` のストリーミング/パース/書き込み一式（+675/-94）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Initialising Git and extensions in WSL in VS Code Insiders takes a long time](https://github.com/microsoft/vscode/issues/318730) `#318730`
**ステータス:** Open

**概要:** WSL で Git や拡張の初期化に時間がかかり、一部拡張アイコンが正しく表示されないという報告。

**直近のアクティビティ:** @jamiescottie1 が、WSL2 で git source control の初期化に ~80 秒かかるが `chat.disableAIFeatures: true` で正常化すると報告。

**次のアクション:** AI 機能と初期化遅延の関連調査。

---

### ![Issue](icons/issue-opened.svg) [Remote SSH: Connection times out in VS Code while terminal SSH works](https://github.com/microsoft/vscode/issues/318914) `#318914`
**ステータス:** Open

**概要:** macOS Tahoe + Insiders 1.122.0 で、ターミナルからの SSH は成功するのに VS Code Remote SSH が必ずタイムアウトするという報告。PATH に npm エラー出力が混入する観察あり。

**直近のアクティビティ:** @balupton も VS Code の SSH が壊れたとし Helix に移行したとコメント。

**次のアクション:** PATH 汚染と接続失敗の調査。

---

### ![Issue](icons/issue-opened.svg) [MCP Apps: inline iframe (mcpAppPart) not mounted despite tool def carrying _meta.ui.resourceUri](https://github.com/microsoft/vscode/issues/320093) `#320093`
**ステータス:** Open | **ラベル:** new release

**概要:** ツール定義が `_meta.ui.resourceUri` を持ち `chat.mcp.apps.enabled` も有効なのに、インライン iframe（mcpAppPart）がマウントされないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** MCP Apps の iframe マウント調査。

---

### ![PR](icons/git-pull-request.svg) [extensions: make auto-update minimum release age configurable](https://github.com/microsoft/vscode/pull/320046) `#320046`
**ステータス:** Open

**概要:** 拡張の自動更新の bake time を `extensions.autoUpdateMinimumReleaseAge`（分単位、デフォルト 120）で設定可能にする。自動更新のみ対象で、手動更新やマーケットプレイス検索などは不変。#316867 の一部。

**直近のアクティビティ:** @hand-dot が、既存の 2 時間挙動に直接対応するため分を採用したと説明し CLA に同意。@karlhorky のレビューコメント。

**変更ファイル:** `extensions.contribution.ts`・`extensionsWorkbenchService.ts` ほか（+69/-11）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Add a second turn to the chat smoke tests](https://github.com/microsoft/vscode/pull/319942) `#319942`
**ステータス:** Open

**概要:** チャットのスモークテストに、セッションタイプ（Copilot CLI/Claude/Local）ごとの 2 ターン目を追加し、応答待ちの自動化を堅牢にする。

**直近のアクティビティ:** Copilot reviewer が概要をコメント。

**変更ファイル:** `agentsWindow.test.ts`、`chatSessions.test.ts`、automation 一式（+323/-269）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [在使用windows端Romote Explorer链接Linux端时左侧菜单栏图标显示方框](https://github.com/microsoft/vscode/issues/320078) `#320078`
**ステータス:** Closed | **ラベル:** ai-translated, new release

**概要:** Windows から Remote Explorer で Linux に接続すると、左サイドバーのメニューアイコンが四角（豆腐）で表示されるという報告。

**直近のアクティビティ:** bot が AI 翻訳を付与。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [isDate is not a function (when downloading files with sftp extension by Natizyskunk)](https://github.com/microsoft/vscode/issues/319963) `#319963`
**ステータス:** Closed | **ラベル:** *caused-by-extension, new release

**概要:** 1.123.0 で `util.isDate` が削除され、sftp 拡張が `TypeError: isDate is not a function` で壊れる（ダウンロードファイルが空になる）という報告。

**直近のアクティビティ:** @fibanez7 がメンテ済み fork を公開。@deepak1556 が、`util.isDate` は node v23 で削除済みで依存ライブラリ ssh2 は修正済み、拡張側が依存更新すべきと説明。

**次のアクション:** なし（拡張側対応のためクローズ）。

---

### ![Issue](icons/issue-opened.svg) [git operations very slow after upgraded to 1.122.0](https://github.com/microsoft/vscode/issues/318882) `#318882`
**ステータス:** Open

**概要:** 1.122.0 へ更新後、git 操作が（全拡張無効でも）非常に遅くなったという報告（WSL2/Ubuntu 24.04）。

**直近のアクティビティ:** @jtremesay が、ネットワーク切断時の git/python 拡張の無限ローディングに言及し 1.123.0 でも同様と報告。@PierTrailslight も同症状。

**次のアクション:** 遅延原因の調査。

---

### ![Issue](icons/issue-opened.svg) [Make status bar `Forwarded Ports` toggleable](https://github.com/microsoft/vscode/issues/320086) `#320086`
**ステータス:** Open | **ラベル:** feature-request

**概要:** 転送ポートのステータスバー項目を 2 回目クリックで閉じられるトグルにしてほしいという要望。

**直近のアクティビティ:** bot がバックログ候補として投票を案内。

**次のアクション:** PR #320090/#320092 での対応。

---

### ![Issue](icons/issue-opened.svg) [Error: VSCode Server fails, if update fails to download](https://github.com/microsoft/vscode/issues/320045) `#320045`
**ステータス:** Open | **ラベル:** new release

**概要:** 更新が CLI 経由でダウンロードされ失敗すると、サーバー接続が永久に失われるという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 更新失敗時のフォールバック対応。

---

### ![Issue](icons/issue-opened.svg) [Clicking on "Plugins" in Agents window opens generic customizations view instead of Plugins view](https://github.com/microsoft/vscode/issues/319702) `#319702`
**ステータス:** Open

**概要:** Agents window の「Plugins」をクリックすると Customizations UI が開くが Plugins セクションが選択されないという報告。

**直近のアクティビティ:** @aeschli が、`sessions.customizations.sidebarMode` 設定の存在に触れ、Plugins セクションを表示すべきとしてデフォルト変更を提案。

**次のアクション:** デフォルト挙動の変更検討。

---

### ![Issue](icons/issue-opened.svg) [VS Code crashes (code 5) with Remote SSH + Copilot Chat codebase indexing on M1](https://github.com/microsoft/vscode/issues/312925) `#312925`
**ステータス:** Open

**概要:** Apple Silicon で Remote SSH 接続後に Copilot Chat のコードベースインデックス開始から約 40 秒で VS Code が SIGSEGV（code 5）でクラッシュするという報告。

**直近のアクティビティ:** @sukosmos が、サーバーの codex 拡張が原因で、削除するとクラッシュ頻度が下がったと共有。

**次のアクション:** インデックス処理のクラッシュ調査。

---

### ![Issue](icons/issue-opened.svg) [Remove extra icons from the terminal area?](https://github.com/microsoft/vscode/issues/317695) `#317695`
**ステータス:** Open

**概要:** ペインを別の場所から表示/非表示するときの「X」と区切りが不要では、という UI 整理の問い。

**直近のアクティビティ:** @mrleemurray が UI を整理する意義に賛同。

**次のアクション:** UI 整理の判断。

---

### ![PR](icons/git-pull-request.svg) [Update checkbox border colors for 2026 themes](https://github.com/microsoft/vscode/pull/320082) `#320082`
**ステータス:** Merged

**概要:** 2026 ダーク/ライトテーマのチェックボックス枠色をアクセシビリティ向上のため調整する。#319909 に対応。

**直近のアクティビティ:** @sandy081 が APPROVED。

**変更ファイル:** `2026-dark.json`、`2026-light.json`（+2/-2）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [scm.autoReveal does not reveal/select staged (index) files in the Source Control view](https://github.com/microsoft/vscode/issues/320087) `#320087`
**ステータス:** Open

**概要:** `scm.autoReveal` が working-tree の変更では機能するが、staged（index）ファイルでは何もしないという報告。

**直近のアクティビティ:** bot が最新版（1.123.0）での再確認を案内。

**次のアクション:** staged ファイルの reveal 対応。

---

### ![Issue](icons/issue-opened.svg) [Agents window not connecting to ssh](https://github.com/microsoft/vscode/issues/315870) `#315870`
**ステータス:** Closed | **ラベル:** info-needed, agents-window

**概要:** Agents window でリモートセッションを開始すると `unexpected argument '--port'` で SSH 接続に失敗するという報告。

**直近のアクティビティ:** bot が info-needed のまま追加情報が得られず 7 日経過しクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [New Agents Window won't connect to SSH tunnel](https://github.com/microsoft/vscode/issues/316418) `#316418`
**ステータス:** Closed | **ラベル:** info-needed, agents-window

**概要:** Agents window で remote > ssh を選ぶと「Connecting to SSH」のまま進まないという報告。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [Agents window: chat input silently dropped in "SSH in Workspace" mode](https://github.com/microsoft/vscode/issues/317297) `#317297`
**ステータス:** Closed | **ラベル:** info-needed, agents-window, agent-host

**概要:**「SSH in Workspace」モードでチャット入力が黙って捨てられ、リモート Copilot CLI が stdio トンネル越しにリクエストを受け取らないという報告。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [claude 4.7 is not working and other agent is keep crashing](https://github.com/microsoft/vscode/issues/318046) `#318046`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Claude 4.7 が動かず他のエージェントもクラッシュし続けるという苦情。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [VR-Code zeigt mir Provider-Probleme.](https://github.com/microsoft/vscode/issues/318728) `#318728`
**ステータス:** Closed | **ラベル:** info-needed, ai-translated

**概要:** プロバイダーに無効なリクエストが送られた（`Invalid request sent to provider`）という報告。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [Worker terminated](https://github.com/microsoft/vscode/issues/318763) `#318763`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `Worker terminated due to reaching memory limit: JS heap out of memory` という報告。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [MAPEPIRE version clash between local and remote](https://github.com/microsoft/vscode/issues/318806) `#318806`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** local と remote の mapepire のバージョン（2.3.5）が一致しないのはなぜか、という報告。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![Issue](icons/issue-opened.svg) [highlighting the code incorrect even though the code is correct](https://github.com/microsoft/vscode/issues/318824) `#318824`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 正しいコードなのに（.sql や Jupyter notebook で）赤い下線が引かれるという報告。

**直近のアクティビティ:** bot が info-needed のままクローズ。

**次のアクション:** なし（情報不足でクローズ）。

---

### ![PR](icons/git-pull-request.svg) [Add `.wixproj` to xml language extension list](https://github.com/microsoft/vscode/pull/317565) `#317565`
**ステータス:** Closed

**概要:** WiX MSBuild プロジェクトファイル `.wixproj` を XML 言語として認識させる。

**直近のアクティビティ:** @aeschli が、セキュリティ上 external committer は `package.json` を変更できないとコメント。@aeschli・@Yoyokrazy は APPROVED。

**変更ファイル:** `extensions/xml/package.json`（+1/-0）。

**次のアクション:** なし（クローズ済み。内部での取り込みが必要）。

---

### ![PR](icons/git-pull-request.svg) [Fix suggest model triggering on disposed inline completions model](https://github.com/microsoft/vscode/pull/320077) `#320077`
**ステータス:** Merged

**概要:** `SuggestModel._waitForInlineCompletionsAndTrigger` が、dispose・再生成された `InlineCompletionsModel` の derived observable を購読し続け、`readOnly` トグルなどで dead model に対して `stop`/`trigger` を呼ぶ問題を修正。

**直近のアクティビティ:** @lszomoru が APPROVED。

**変更ファイル:** `suggest/browser/suggestModel.ts`、`suggestModel.test.ts`（+97/-49）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix: support token auth for CLI SDK mock server to enable auto-model in smoke tests](https://github.com/microsoft/vscode/pull/320072) `#320072`
**ステータス:** Merged

**概要:** `chat.cli.autoModel.enabled` が true のとき Copilot CLI スモークテストが失敗する問題を修正。HMAC 認証が auto-mode をブロックする点、mock の `/models/session` に `selected_model` が無い点、Responses API ハンドラが無い点などを直す。`overrideAuthType` 設定も追加。

**直近のアクティビティ:** @lszomoru が APPROVED。

**変更ファイル:** `copilotcli/node/copilotCli.ts`、`mock-llm-server.js` ほか（+345/-118）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Improve border visibility for key bindings in light/dark themes](https://github.com/microsoft/vscode/pull/320076) `#320076`
**ステータス:** Merged

**概要:** Quick Input のキーバインド「pill」枠が透明背景で見えない問題を、前景色から枠色を導出して改善する（ハイコントラストは不変）。

**直近のアクティビティ:** CODENOTIFY bot が通知。@sandy081 が APPROVED。

**変更ファイル:** `quickinput/browser/media/quickInput.css`（+8/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat: implement GitHub pull request operations in agent host](https://github.com/microsoft/vscode/pull/318256) `#318256`
**ステータス:** Merged

**概要:** agent host から GitHub PR 関連の changeset 操作を呼べるようにし、最小の GitHub REST クライアント（AgentHostOctoKitService）と PR 作成/管理のハンドラ・テストを追加する。#317113 を修正。

**直近のアクティビティ:** @connor4312 のコメント・DISMISSED を経て @lszomoru が APPROVED。

**変更ファイル:** `agentHost/node/agentHostPullRequestOperationHandler.ts` ほか（+1013/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [adding prompt completions](https://github.com/microsoft/vscode/pull/319939) `#319939`
**ステータス:** Merged

**概要:** チャット入力エディタに Monaco の inline-completions コントローラを配線し、「prompt completions」を有効にする。

**直近のアクティビティ:** @hediet が APPROVED。

**変更ファイル:** `chatInputPart.ts`、completions-core の `config.ts` ほか（+15/-2）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [sessions: respect hidden secondary side bar on session switch](https://github.com/microsoft/vscode/pull/320075) `#320075`
**ステータス:** Merged

**概要:** Agents window で、あるセッション用に隠した二次サイドバー（auxiliary bar）がセッション切り替え後も隠れたままになるよう修正。working-set 復元時の editor reveal が「editor 表示なら aux bar 表示」invariant を誤発火していた点を抑制する。

**直近のアクティビティ:** CODENOTIFY bot が通知。@lszomoru が APPROVED。

**変更ファイル:** `sessionLayoutController.ts` ほか（+277/-7）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add spacing size variables to theme sizes](https://github.com/microsoft/vscode/pull/320073) `#320073`
**ステータス:** Merged

**概要:** デザインシステムに標準スペーシングトークン（`spacing.size20` など）を追加し、padding/margin/gap で一貫して使えるようにする。

**直近のアクティビティ:** @alexdima が APPROVED。

**変更ファイル:** `theme/common/sizes/baseSizes.ts`、`vscode-known-variables.json`（+76/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Refactor CSS to use `--vscode-shadow-lg`](https://github.com/microsoft/vscode/pull/320071) `#320071`
**ステータス:** Merged

**概要:** hover/peek/rename ウィジェットの box-shadow を `--vscode-shadow-lg` に統一し、未使用の `--vscode-shadow-hover` を削除する。

**直近のアクティビティ:** screenshot-diff bot が 6 件の差分を報告。@alexdima が APPROVED。

**変更ファイル:** `hover.css`・`peekViewWidget.css`・`renameWidget.css` ほか（+4/-6）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix #236814: Auto-escape text when pasting inside JSON string literals](https://github.com/microsoft/vscode/pull/319460) `#319460`
**ステータス:** Closed

**概要:** JSON/JSONC の文字列リテラル内へのペースト時に、引用符・バックスラッシュ・改行・タブを自動エスケープするプロバイダを追加する（`json.editor.autoEscapePaste` 設定対応）。#236814 を修正。

**直近のアクティビティ:** Copilot reviewer が概要をコメント。

**変更ファイル:** `json-language-features` の `jsonStringParser.ts`・`jsonClient.ts` ほか（+1056/-20）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Refactor border-radius styles for consistency](https://github.com/microsoft/vscode/pull/320065) `#320065`
**ステータス:** Merged

**概要:** Agents window 配下の CSS の `border-radius` ハードコード値を `--vscode-cornerRadius-*` CSS 変数に置き換え、テーマ性と一貫性を高める。

**直近のアクティビティ:** CODENOTIFY bot が通知。@chrmarti が APPROVED。

**変更ファイル:** `src/vs/sessions/` 配下の多数の CSS（+109/-96）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Improve split editor menu items for agents window and compact window editor](https://github.com/microsoft/vscode/pull/319736) `#319736`
**ステータス:** Open

**概要:** Agents window がアクティブなとき「Split Editor」「Collapse/Expand All Diffs」をプライマリツールバーからオーバーフロー（…）メニューに移し、ツールバーを整理する。

**直近のアクティビティ:** CODENOTIFY bot が通知。

**変更ファイル:** `editor.contribution.ts`、`browserTabManagementFeatures.ts`、`multiDiffEditor/browser/actions.ts`（+86/-15）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Refactor button styles for consistency](https://github.com/microsoft/vscode/pull/316004) `#316004`
**ステータス:** Merged

**概要:** セカンダリボタンの枠スタイルを `button.secondaryBorder` トークン導入で統一し、テーマ間の見た目の一貫性を高める。

**直近のアクティビティ:** @alexr00 が APPROVED。

**変更ファイル:** `2026-dark.json`・`2026-light.json`、`button.css`、`inputColors.ts` ほか（+23/-10）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix arrow key navigation not reaching filter button in notebook find widget](https://github.com/microsoft/vscode/pull/308429) `#308429`
**ステータス:** Open

**概要:** notebook find widget で矢印キーが標準 3 トグルしか巡回せず filter（じょうご）ボタンに届かない問題を、`getOptionNavigationIndexes()` を protected 化して subclass で拡張できるようにして修正。#207765 を修正。

**直近のアクティビティ:** @yogeshwaran-c が、ヘッドレス環境のため DOM レベルの自動テストを追加したと説明。

**変更ファイル:** `findInput.ts`、`notebookFindReplaceWidget.ts`、テスト（+238/-6）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix(search): only show Notebook Find Filters toggle for visible notebooks](https://github.com/microsoft/vscode/pull/309137) `#309137`
**ステータス:** Open

**概要:** notebook が開かれているだけで（非表示でも）検索ビューに Notebook Find Filters トグルが出る問題を、`visibleEditors` ベースに切り替えて可視 notebook のときだけ出すよう修正。#224043 を修正。

**直近のアクティビティ:** @osortega・@yogeshwaran-c のレビューコメント。

**変更ファイル:** `searchWidget.ts`（+3/-7）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Sanitize non-finite range coordinates in Diagnostic.from](https://github.com/microsoft/vscode/pull/318520) `#318520`
**ステータス:** Open

**概要:** 非有限（NaN/Infinity）の range 座標を持つ Diagnostic が RPC 越しに `null` 化され LSP 仕様違反となる問題を、`Diagnostic.from` で各座標を独立に sanitize して防ぐ。#177094 を修正。

**直近のアクティビティ:** @yogeshwaran-c が、各座標を独立 sanitize して end が start を反転しないよう修正したと説明し再レビューを依頼。@dbaeumer が CHANGES_REQUESTED。

**変更ファイル:** `extHostTypeConverters.ts`、テスト（+99/-3）。

**次のアクション:** 指摘対応とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Browser Search](https://github.com/microsoft/vscode/pull/317408) `#317408`
**ステータス:** Open

**概要:** 統合ブラウザのアドレスバー入力を URL かクエリかで分類し、設定済み検索エンジンの検索 URL を組み立てる「アドレスバーからの検索」の初版を追加する。

**直近のアクティビティ:** @jruales・@kycutler のレビューコメント。

**変更ファイル:** `browserView/common/browserSearch.ts`、`browserUrlBarWidget.ts` ほか（+867/-34）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix: persist SCM repository visibility state across restarts](https://github.com/microsoft/vscode/pull/301029) `#301029`
**ステータス:** Open

**概要:** `isHidden` リポジトリ（Copilot worktree など）が再起動時の可視状態復元を壊す問題を修正し、新規リポジトリは既存の可視状態を崩さず可視として追加する。#301028 を修正。

**直近のアクティビティ:** @aruizdesamaniego-sh のレビューコメント。Copilot reviewer が複数回レビュー。

**変更ファイル:** `scm/browser/scmViewService.ts`、`menus.ts` ほか（+397/-24）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Update GetErrorsTool to handle Windows drive letter casing](https://github.com/microsoft/vscode/pull/320053) `#320053`
**ステータス:** Open

**概要:** `GetErrorsTool` の Windows でのドライブレター大小（`C:` vs `c:`）の違いを吸収し、`extUriBiasedIgnorePathCase` で大文字小文字を無視したパス比較にする。#319858 を修正。

**直近のアクティビティ:** @Wangmz-1211 が CLA に同意。Copilot reviewer が概要をコメント。

**変更ファイル:** `tools/node/getErrorsTool.tsx`、テスト（+44/-6）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Send repoInfo telemetry for each repo in multi-repo workspaces](https://github.com/microsoft/vscode/pull/320054) `#320054`
**ステータス:** Open

**概要:** これまでアクティブリポジトリのみだった `repoInfo` テレメトリを、multi-repo ワークスペースで上限付きにリポジトリごとに 1 イベント発火するよう拡張する。

**直近のアクティビティ:** @vijayupadya のレビューコメント。

**変更ファイル:** `prompt/node/repoInfoTelemetry.ts`、テスト（+358/-37）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [agentHost: support attaching virtual resources (untitled, notebook cells)](https://github.com/microsoft/vscode/pull/320040) `#320040`
**ステータス:** Merged

**概要:** agent host の権限サービスと仮想リソースプロバイダを単一の `IAgentHostResourceService` に統合し、untitled ドキュメントや notebook セルを `ITextModelService` フォールバックで読み書きできるようにする。local/remote の経路を薄いワイヤアダプタに縮小する。

**直近のアクティビティ:** @DonJayamanne の DISMISSED を経て @lszomoru が APPROVED。

**変更ファイル:** `agentHostResourceService.ts`、`remoteAgentHostProtocolClient.ts` ほか（+898/-562）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat: add Github issue creation tool for Copilot Chat](https://github.com/microsoft/vscode/pull/319731) `#319731`
**ステータス:** Open

**概要:** Copilot Chat に、投稿前確認付きで GitHub Issue を作成する `GithubCreateIssueTool` を追加し、`IOctoKitService` に `createIssue` を実装する。#319555 を修正。

**直近のアクティビティ:** @yash3605 が、前進するのかバグがあるのかを尋ねている。

**変更ファイル:** `tools/node/createGithubIssueTool.tsx`、`octoKitServiceImpl.ts` ほか（+166/-71）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [chat: stop agent host config update loop across windows](https://github.com/microsoft/vscode/pull/320015) `#320015`
**ステータス:** Merged

**概要:** local agent host の root config が全ウィンドウ共有のため、`AgentHostTerminalContribution` が他ウィンドウの値変更にも再 push して 2 ウィンドウが `root/configChanged` を無限に押し合う（25 秒で約 8000 回）問題を修正。再 push をスキーマ hydrate 時の初回再試行のみに限定する。#320011 を修正。

**直近のアクティビティ:** @vijayupadya が APPROVED。

**変更ファイル:** `agentHostTerminalContribution.ts`、`copilotAgent.ts` ほか（+244/-89）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Browser: Match ExP value for setting `workbench.browser.enableChatTools`](https://github.com/microsoft/vscode/pull/319465) `#319465`
**ステータス:** Merged

**概要:** ExP が 100% に達したため、`workbench.browser.enableChatTools` をコード上もデフォルト有効にする。

**直近のアクティビティ:** @vijayupadya が APPROVED。

**変更ファイル:** `policyData.jsonc`、`browserEditorChatFeatures.ts` ほか（+2/-23）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Agents: Align session list action icon buttons with top bar search icon button](https://github.com/microsoft/vscode/pull/320050) `#320050`
**ステータス:** Open

**概要:** Agents のセッションリストのセクションヘッダーアクションアイコンを、トップバーの検索アイコンと視覚的に揃える CSS 調整。#319372 を修正。

**直近のアクティビティ:** @vijayupadya が APPROVED。

**変更ファイル:** `sessions/browser/media/sessionsList.css`（+1/-3）。

**次のアクション:** マージ。

---

### ![PR](icons/git-pull-request.svg) [fix: hide redundant Disable option when extension is already disabled globally](https://github.com/microsoft/vscode/pull/320048) `#320048`
**ステータス:** Open

**概要:** 拡張がグローバル無効かつワークスペース有効のとき、Disable ボタンに「Disable」と「Disable (Workspace)」が両方出る問題を、`isDisabledGlobally` チェック追加で修正。#244138 を修正。

**直近のアクティビティ:** Copilot reviewer が概要をコメント。

**変更ファイル:** `extensions/browser/extensionsActions.ts`（+1/-0）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Add better guards for Specialized Subagents and BG Todo Agent](https://github.com/microsoft/vscode/pull/319978) `#319978`
**ステータス:** Merged

**概要:** バックグラウンド Todo Agent を、サインイン済み有料プランかつ CAPI 経由のときのみ有効にする。specialized subagent の `ownsAuthorization` 誤用を除去し main agent のエンドポイントが CAPI 経由かを確認する。

**直近のアクティビティ:** @dmitrivMS・@bhavyaus が APPROVED。

**変更ファイル:** `intents/node/agentIntent.ts`、`extChatEndpoint.ts` ほか（+168/-42）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [extensions: delay auto-update of new versions by 2 hours](https://github.com/microsoft/vscode/pull/319340) `#319340`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** 拡張の新バージョン公開から自動更新を 2 時間遅らせる（手動更新は即時）。insiders に出ていた `delayed` 値を戻し、ユーザー設定なしで自動的に bake time を与える。

**直近のアクティビティ:** @DanielRuf が 2 時間では不十分とし、設定可能化を求めつつ #320046 に言及。@benibenj が APPROVED。

**変更ファイル:** `pluginAutoUpdate.ts`、`extensionsWorkbenchService.ts` ほか（+126/-188）。

**次のアクション:** なし（マージ済み。設定可能化は #320046 で継続）。

---

### ![PR](icons/git-pull-request.svg) [Update customizations modal titles](https://github.com/microsoft/vscode/pull/320043) `#320043`
**ステータス:** Open

**概要:** カスタマイズモーダルを、どの agent harness を編集中かが分かるよう「Agent Customizations for {harness}」のタイトルに変更し、サイドバー先頭を「Home」に固定する小リデザイン。#317696・#319633 を修正。

**直近のアクティビティ:** screenshot-diff bot が 52 件の差分を報告。

**変更ファイル:** `aiCustomizationManagementEditor.ts`、`aiCustomizationWelcomePage.ts` ほか（+67/-21）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Fix model selection issues when switching session types](https://github.com/microsoft/vscode/pull/320036) `#320036`
**ステータス:** Open | **ラベル:** chat, model-picker, copilot-cli-agent, claude-agent, agent-host

**概要:** セッションタイプ切り替え時（local と agent-host など）にモデル選択が auto などに勝手に戻る問題を修正し、無効/古いモデル ID の永続化や転送を防ぐ。#319583 など多数の Issue を修正。

**直近のアクティビティ:** @dmitrivMS が main window 向けで複数の安定再現を直すと説明。@DonJayamanne が、別の weird な問題を招かないか懸念し、insiders で 1 週間検証するため月曜まで待ちたいとコメント。

**変更ファイル:** `agentHostSessionHandler.ts`、`chatModelSelectionLogic.ts` ほか（+192/-24）。

**次のアクション:** insiders での検証後にマージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix: surface missing Git LFS git errors](https://github.com/microsoft/vscode/pull/319973) `#319973`
**ステータス:** Open

**概要:** Git LFS 欠落で `git worktree add` が失敗したとき、agent host の Git エラー整形が最終行の汎用 fatal だけを出していたのを、`git-lfs` 欠落行があればそれを優先表示するよう修正。#319824 を修正。

**直近のアクティビティ:** @KirtiRamchandani が hygiene 警告（外部文字列はダブルクオート）を修正。@dmitrivMS が CI エラー対応を依頼。

**変更ファイル:** `agentHostGitService.ts`、テスト（+19/-2）。

**次のアクション:** CI 対応とレビュー。

---

## qmk/qmk_firmware

> 新規キーボード（Tiwaz・Polygon）の追加 PR がレビュー中で、Azoteq ポインティングのボタン保持バグ修正や GPL コンプライアンスに関する質問 Issue が動いている。

### ![PR](icons/git-pull-request.svg) [New Keyboard: Tiwaz](https://github.com/qmk/qmk_firmware/pull/26221) `#26221`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** ジョイスティック付きの分割キーボード Tiwaz の追加。スタンドアロンのマクロパッド/ゲーミングキーボードとしても使える。1 年以上かけて製作したとのこと。

**直近のアクティビティ:** @tstrube・@drashna が 8 件のレビューコメント。

**変更ファイル:** `keyboards/tsc/tiwaz/` 配下一式（+773/-0）。

**次のアクション:** レビュー指摘への対応。

---

### ![PR](icons/git-pull-request.svg) [Add keyboard Polygon](https://github.com/qmk/qmk_firmware/pull/26119) `#26119`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** キーボード Polygon の追加。ANSI と ISO は別 PCB のため別キーボードとして登録し、それぞれ info.json をサポートする。

**直近のアクティビティ:** @Rotwitt・@drashna・@sigprof が 8 件のレビュー（@drashna が CHANGES_REQUESTED）。

**変更ファイル:** `keyboards/polygon/ansi/`・`iso/` 配下一式（+844/-0）。

**次のアクション:** 指摘対応とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Preserve Azoteq mouse buttons](https://github.com/qmk/qmk_firmware/pull/26248) `#26248`
**ステータス:** Open | **ラベル:** core

**概要:** Azoteq IQS5xx ドライバがレポートを組み立てる際に、入力されたマウスボタンのビットを保持するよう修正し、既存ボタンとの組み合わせテストを追加する。#26168 を修正。

**直近のアクティビティ:** @drashna が APPROVED。

**変更ファイル:** `drivers/sensors/azoteq_iqs5xx.c`、Azoteq ポインティングテスト一式（+116/-1）。

**次のアクション:** マージ。

---

### ![PR](icons/git-pull-request.svg) [Add support for geigeigeist/totem](https://github.com/qmk/qmk_firmware/pull/25877) `#25877`
**ステータス:** Closed | **ラベル:** keyboard, keymap, awaiting review

**概要:** Seeed XIAO RP2040 を使う 38 キーのカラムスタッガード分割キーボード TOTEM のサポート追加。キーマップは miryoku をベースに基本機能を網羅。

**直近のアクティビティ:** @waffle87 が CHANGES_REQUESTED、@drashna が APPROVED、@zvecr がコメント。

**変更ファイル:** `keyboards/geigeigeist/totem/` 配下一式（+160/-0）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Request for Guidance on GPL Compliance and Source Code Release – YUNZII Tri-Mode Boards](https://github.com/qmk/qmk_firmware/issues/25737) `#25737`
**ステータス:** Open | **ラベル:** question, help wanted

**概要:** メーカー YUNZII が、License Violations に挙げられた tri-mode キーボードについて GPL コンプライアンスの指針を求める Issue。プロプライエタリな無線ドライバがあるため、公開すべき範囲や部分公開の可否、正式な対応手順を問う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** QMK チームからのコンプライアンス指針の提示。

---
