# GitHub ダイジェスト — 2026-05-01

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[font/shaper: add RTL shaping for Arabic and Hebrew](https://github.com/ghostty-org/ghostty/pull/11079)** (ghostty-org/ghostty) — アラビア語・ヘブライ語の双方向レイアウトを Unicode 双方向アルゴリズム (UAX #9) ベースで導入する大型 PR で、純 Zig 実装 itijah を新規依存に追加する基盤的変更。
2. ![PR](icons/git-pull-request.svg) **[agentHost/claude: add CAPI-backed local Anthropic proxy service](https://github.com/microsoft/vscode/pull/313677)** (microsoft/vscode) — Claude Agent SDK を CAPI 経由で動かすためのローカル Anthropic プロキシ (`ClaudeProxyService`) をマージ。+4573/-17 行の大規模追加で、今後の Claude Agent 統合の足場。
3. ![PR](icons/git-pull-request.svg) **[Reject locked malware installations](https://github.com/astral-sh/uv/pull/18936)** (astral-sh/uv) — `uv sync` 等の同期処理に OSV ベースのマルウェア検出を組み込み、ロックファイルにマルウェアが含まれている場合インストールを拒否する preview 機能。
4. ![Issue](icons/issue-opened.svg) **[gx seems to be broken on Windows on the latest nightlies](https://github.com/neovim/neovim/issues/39524)** (neovim/neovim) — nvim 0.13 nightly の Windows でのリグレッション。`gx` が `shellslash` 状況下でパスのスラッシュ正規化に失敗する問題で、活発に原因究明中。
5. ![PR](icons/git-pull-request.svg) **[modem.py](https://github.com/commaai/openpilot/pull/37811)** (commaai/openpilot) — モデム管理を NetworkManager から openpilot 内 modem.py へ完全移行する PR。3 デバイスでフルブート→ping<60秒の目標達成。

---

## commaai/openpilot

> 自動運転スタックのアップデート。モデム管理刷新やビルド依存関係 (acados, xvfb) のベンダリング、agnos 18.2 対応など、システム基盤側の整備が中心。

### ![PR](icons/git-pull-request.svg) [modem.py](https://github.com/commaai/openpilot/pull/37811) `#37811`
**ステータス:** Open | **ラベル:** なし

**概要:** モデムの起動・停止管理を新規 `modem.py` に集約し、NetworkManager からモデム関連の制御責務を取り除く。issue #37277 の解決を目指す大規模リファクタ。

**直近のアクティビティ:** 投稿者の greatgitsby が 3 デバイス (comma three EG25、comma four EG916 ×2、Verizon/AT&T SIM) で再ストレステストを実行し、pppd 強制終了の連発、APN/ローミング切替、USB 再列挙、5 分アイドル放置などを全パス。「フル再起動から ping 到達まで <60 秒」のボーナス目標も全 8 試行で達成 (平均 50.5 秒)。

**変更ファイル:** `system/hardware/tici/modem.py`、`lpa.py`、`hardwared.py`、`process_config.py`、`restart_modem.sh` など 8 ファイル。+558/-308 行。

**次のアクション:** メンテナーレビューを受けてのマージ判断待ち。

---

### ![PR](icons/git-pull-request.svg) [vendor acados via commaai/dependencies](https://github.com/commaai/openpilot/pull/37932) `#37932`
**ステータス:** Open | **ラベル:** なし

**概要:** これまでリポジトリに同梱されていた acados (MPC ソルバ) を `commaai/dependencies` 経由でベンダリングに切り替える。差分は +44/-59338 行と巨大。

**直近のアクティビティ:** プロセスリプレイ差分は 0 changed・66 passed でリグレッションなし。

**変更ファイル:** `third_party/acados/` 配下の同梱バイナリ削除、`SConstruct`、`pyproject.toml`、`lateral_mpc_lib`/`longitudinal_mpc_lib` のビルド設定修正。

**次のアクション:** 関連 PR #37933 (vendored acados パッケージ追加、マージ済み) と整合させた最終レビュー。

---

### ![PR](icons/git-pull-request.svg) [agnos 18.2](https://github.com/commaai/openpilot/pull/37912) `#37912`
**ステータス:** Open | **ラベル:** なし

**概要:** ベース OS (agnos) を 18.2 に上げる更新。+33/-10890 行で多数のカメラ関連バイナリ・センサ実装の削除を含む。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `system/camerad/cameras/` (camera_qcom2、spectra)、`system/camerad/sensors/os04c10`、`ox03c10` などカメラスタック中心の 39 ファイル。

**次のアクション:** バイナリ更新を含むためレビューと CI を慎重に通す段階。

---

### ![PR](icons/git-pull-request.svg) [use vendored xvfb](https://github.com/commaai/openpilot/pull/37935) `#37935`
**ステータス:** Merged | **ラベル:** なし

**概要:** UI テストで使う xvfb をベンダリング済みパッケージに置き換える。

**直近のアクティビティ:** プロセスリプレイ差分は 0 changed/66 passed。

**変更ファイル:** `pyproject.toml`、`setup_xvfb.sh`、`uv.lock` 等 4 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [add vendored acados package](https://github.com/commaai/openpilot/pull/37933) `#37933`
**ステータス:** Merged | **ラベル:** なし

**概要:** acados のベンダリング切替に必要な `pyproject.toml` 側のパッケージ追加。

**直近のアクティビティ:** プロセスリプレイ差分は問題なし。

**変更ファイル:** `pyproject.toml`、`uv.lock` の 2 ファイル。

**次のアクション:** マージ済み。#37932 の前提として機能。

---

### ![PR](icons/git-pull-request.svg) [lpa: stop loading BPP once eUICC returns ProfileInstallResult](https://github.com/commaai/openpilot/pull/37890) `#37890`
**ステータス:** Open | **ラベル:** なし

**概要:** eSIM プロファイルのインストール中に eUICC が `ProfileInstallResult` (BF37) を返した時点で残りの A3 子チャンクの送信を打ち切る。これまでは閉じたインストールセッションに送り続けて、本来のエラーを覆い隠す `SW=6A88` が発生していた。

**直近のアクティビティ:** リプレイ差分テストは異常なし。

**変更ファイル:** `system/hardware/tici/lpa.py` の 1 ファイル、+3/-2 行。

**次のアクション:** 小粒な改善でレビュー後のマージを想定。

---

### ![PR](icons/git-pull-request.svg) [ui diff: Chunk frame diffs and group in report with thumbnails](https://github.com/commaai/openpilot/pull/37328) `#37328`
**ステータス:** Open | **ラベル:** なし

**概要:** UI リプレイ動画のフレーム差分を `difflib` で本格的に diff し、変更・追加・削除のグループ化と HTML レポートでのサムネイル表示に対応。バックグラウンドでの並列ハッシュ化やチャンク抽出も導入。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `selfdrive/ui/tests/diff/diff.py`、`diff_template.html`、UI プレビュー workflow の 3 ファイル。+397/-110 行。

**次のアクション:** メンテナーによるレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [ci: reliable MetaDrive test via relaxed tolerances and CPU yielding (#30693)](https://github.com/commaai/openpilot/pull/37900) `#37900`
**ステータス:** Open | **ラベル:** なし

**概要:** 4 コア無料ランナーで MetaDrive CI が CPU スターベーションや teardown ハングするのを解消する策。`launch_openpilot.sh` の `BLOCK` を解除し、`SIMULATION=1` と `CI=1` で `commIssue`/`modeldLagging` の閾値を緩める、`os.killpg` でクリーンアップ、ブリッジループのスリープ化を組み合わせる。

**直近のアクティビティ:** 投稿者 FuZoe が adeebshihadeh 宛にフォローアップを再依頼。`os.killpg` 採用がゾンビプロセス問題を最も確実に防ぐと自分の追加分析で確認したと述べる。

**変更ファイル:** `selfdrived.py`、`tools/sim/bridge/common.py`、`metadrive_process.py`、`launch_openpilot.sh`、`test_sim_bridge.py` の 5 ファイル。

**次のアクション:** メンテナーの方針判断待ち。

---

### ![PR](icons/git-pull-request.svg) [bump tinygrad](https://github.com/commaai/openpilot/pull/37926) `#37926`
**ステータス:** Open | **ラベル:** なし

**概要:** tinygrad サブモジュールのバージョンを更新する 1 ファイル PR。

**直近のアクティビティ:** Armandpl が `model_replay.py` のタイミングが master より約 0.9ms 遅いと指摘。sirhcm はモデルランタイム自体は同等で、Python 側 enqueue が遅いだけと分析し、簡易な修正があるか調査予定と返信。

**変更ファイル:** `tinygrad_repo` サブモジュール 1 ファイル。

**次のアクション:** Python オーバーヘッドの追加プロファイリングと対処。

---

### ![PR](icons/git-pull-request.svg) [log ui frame time](https://github.com/commaai/openpilot/pull/37927) `#37927`
**ステータス:** Open | **ラベル:** なし

**概要:** `uiDebug.drawTimeMillis` が CPU 時間しか含んでいないので、UI フレームの実時間をロギングするよう拡張。

**直近のアクティビティ:** UI プレビューが「Videos differ」と警告。リプレイ差分は問題なし。

**変更ファイル:** `cereal/log.capnp`、UI まわりの 7 ファイル。

**次のアクション:** UI 差分の許容判断とレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix frame under text](https://github.com/commaai/openpilot/pull/37936) `#37936`
**ステータス:** Open (Draft) | **ラベル:** ui

**概要:** `augmented_road_view.py` のフレーム描画順を修正する小さな UI 修正。

**直近のアクティビティ:** UI プレビューが「Videos differ」、リプレイ差分は 0 changed。

**変更ファイル:** `selfdrive/ui/mici/onroad/augmented_road_view.py` 1 ファイル。

**次のアクション:** Draft 解除待ち。

---

### ![PR](icons/git-pull-request.svg) [Action grid](https://github.com/commaai/openpilot/pull/37772) `#37772`
**ステータス:** Open | **ラベル:** stale

**概要:** drive_helpers と modeld 周辺、ONNX モデル群を更新する PR。

**直近のアクティビティ:** 24 日間活動なしのため自動 stale 通知。あと 7 日で自動クローズ予定。

**変更ファイル:** `drive_helpers.py`、`modeld.py`、3 種類の ONNX モデルなど 7 ファイル。

**次のアクション:** クローズを避けたい場合は更新が必要。

---

### ![PR](icons/git-pull-request.svg) [usbgpu](https://github.com/commaai/openpilot/pull/37906) `#37906`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** modeld を USB GPU 経由で動かす実験ブランチ。コンパイル時間計測、フラグでのゲート、big/smol 両モデルのビルドなどが TODO に並ぶ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `modeld/SConscript`、`compile_modeld.py`、`dmonitoringmodeld.py`、`big_driving_policy.onnx` などの 9 ファイル。

**次のアクション:** TODO 消化と benchmark 共有。

---

### ![PR](icons/git-pull-request.svg) [use vendored xvfb](https://github.com/commaai/openpilot/pull/37934) `#37934`
**ステータス:** Merged | **ラベル:** なし

**概要:** #37935 と同内容の vendored xvfb 切替。マージ済み。

**直近のアクティビティ:** リプレイ差分問題なし。

**変更ファイル:** `pyproject.toml`、`setup_xvfb.sh`、`tools/setup_dependencies.sh`、`uv.lock` の 4 ファイル。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Detect bad power supplies](https://github.com/commaai/openpilot/issues/37723) `#37723`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** 不良電源を検知してユーザーに通知する機能要望 (画像のみの報告)。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Useradmin cannot read current logs - screen is blank](https://github.com/commaai/openpilot/issues/37925) `#37925`
**ステータス:** Closed | **ラベル:** bug

**概要:** nightly-dev で useradmin の特定ルートのログが空になるバグ報告。

**直近のアクティビティ:** fmonpelat が「自分の環境でも同じだったが、rlog をアップロードし直したら表示されるようになった」と報告。

**次のアクション:** 既に解消したとして Close。

---

## commaai/opendbc

> 新規プラットフォーム移植 (BYD Atto 3、GWM Haval H6、VW ID.4) と、テストコレクションの最適化を含むパフォーマンス改善が並ぶ。

### ![PR](icons/git-pull-request.svg) [VW ID.4](https://github.com/commaai/opendbc/pull/3366) `#3366`
**ステータス:** Open (Draft) | **ラベル:** car, volkswagen, car safety

**概要:** Volkswagen ID.4 (MEB プラットフォーム) の opendbc 対応を追加する WIP。MEB CAN フォーマットや MEB 用 safety mode を新規追加し、VW ファミリ既存コードを拡張。

**直近のアクティビティ:** car behavior report は 130 セグメント全パスでマスタからの変化なし。VW 各車種の挙動への影響なし。

**変更ファイル:** `volkswagen/carcontroller.py`、`carstate.py`、`mebcan.py`、`safety/modes/volkswagen_meb.h`、`tests/test_volkswagen_meb.py` など 15 ファイル。+894/-10 行。

**次のアクション:** Draft 解除と Carport/Harness クレジット記載者によるテスト。

---

### ![PR](icons/git-pull-request.svg) [perf: optimize get_interface_attr collection overhead](https://github.com/commaai/opendbc/pull/3367) `#3367`
**ステータス:** Open | **ラベル:** car

**概要:** `opendbc/car/interfaces.py` の `get_interface_attr()` を最適化し、pytest コレクション時間を削減。`BASEDIR` を歩く代わりに正準 `BRANDS` を反復し、ブランドディレクトリ探索とインターフェース属性ルックアップをキャッシュする。pytest 収集が 1.13 秒 → 0.74 秒に短縮。

**直近のアクティビティ:** brianescutia が、`tests / test models (1)` の失敗は Rivian ルート利用不可によるもので本 PR とは無関係と説明。car behavior report は変化なし。

**変更ファイル:** `opendbc/car/interfaces.py` の 1 ファイル、+32/-25 行。

**次のアクション:** メンテナーレビュー。

---

### ![PR](icons/git-pull-request.svg) [Tesla: smooth transition after gas pedal override](https://github.com/commaai/opendbc/pull/3249) `#3249`
**ステータス:** Closed | **ラベル:** car, tesla

**概要:** Tesla でガスペダルオーバーライド後に jerkMin/jerkMax を 0 から通常値へランプ復帰させ、DI レートリミッタの急変を抑える。

**直近のアクティビティ:** haraschax が、これは stock Tesla の挙動で、設定速度を超えたとき jerk/accel リミットが無視されるため適切な緩和は不可能と判断、クローズ。Tesla 側で `set_speed = speed_limit + 30` にすれば回避可能と指摘。

**変更ファイル:** `tesla/carcontroller.py`、`teslacan.py`、`values.py` の 3 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [GWM Haval H6: Initial Platform Support](https://github.com/commaai/opendbc/pull/3263) `#3263`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car, car safety

**概要:** GWM Haval H6 (Mk3) 初期サポートを追加。複数ユーザーのドンドル ID とルート、ハーネス図、コネクタ型番 (1376352-1、1565804-1)、Jolion でのテスト結果も共有。

**直近のアクティビティ:** car behavior report は変化なし。

**変更ファイル:** `gwm/` 配下の `carcontroller.py`、`carstate.py`、`gwmcan.py`、`safety/modes/gwm.h`、新規 DBC、tests など 20 ファイル。+1028/-42 行。

**次のアクション:** メンテナーレビュー。

---

### ![PR](icons/git-pull-request.svg) [Update fingerprints.py](https://github.com/commaai/opendbc/pull/3331) `#3331`
**ステータス:** Open | **ラベル:** car, subaru, fingerprint

**概要:** Subaru の fingerprints.py に新しい指紋を追加するだけの小修正。Dongle ID とルートが提示されている。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `subaru/fingerprints.py` 1 ファイル、+7/-0 行。

**次のアクション:** メンテナー確認。

---

### ![PR](icons/git-pull-request.svg) [BYD: Atto 3 / Initial Platform Support](https://github.com/commaai/opendbc/pull/3337) `#3337`
**ステータス:** Open (Draft) | **ラベル:** CI / testing, DBC signals, can, car, car safety

**概要:** BYD Atto 3 の初期プラットフォーム対応 WIP。PR #2429 で残っていたステアリングメッセージの未知ビットがアングル制御のレートリミットだろうと推測しており、bounty が確定したらリモート/ローカル双方でテストを進める計画。

**直近のアクティビティ:** car behavior report は変化なし。

**変更ファイル:** `byd/` 配下の `carcontroller.py`、`carstate.py`、`bydcan.py`、`byd_atto3.dbc`、`safety/modes/byd.h`、tests など 19 ファイル。+791/-2 行。

**次のアクション:** Bounty 確定とテスト続行。

---

## commaai/panda

> Volkswagen MEB の CAN ベース ignition を panda 側で扱うかの議論。

### ![PR](icons/git-pull-request.svg) [VW MEB can ignition](https://github.com/commaai/panda/pull/2395) `#2395`
**ステータス:** Open | **ラベル:** なし

**概要:** VW MEB の CAN ベース ignition 検出を `can_common.h` に追加。TODO はカウンタ追加の実現性確認。

**直近のアクティビティ:** adeebshihadeh が「ついでに CAN ignition を opendbc に移してしまわないか?」と提案。アーキテクチャ移管の議論に発展中。

**変更ファイル:** `board/drivers/can_common.h` の 1 ファイル、+6/-0 行。

**次のアクション:** opendbc への移管を含めた方針確定。

---

## neovim/neovim

> 0.13 nightly のリグレッション (Windows の `gx`、TUI カーソル復元、環境変数大文字化) と、`vim.ui.select`/`vim.F`/`vim.net` を中心とする Lua API のリファクタが活発。tree-sitter `get_node_text` の長期バグも複数 PR が並走中。

### ![PR](icons/git-pull-request.svg) [fix(diagnostic): show diagnostic signs according to config](https://github.com/neovim/neovim/pull/39525) `#39525`
**ステータス:** Open | **ラベル:** diagnostic

**概要:** `vim.diagnostic.config()` の `signs.text` 設定が `status.format` の影響で無視されていた問題を、ユーザーが `status.format` でシンボルを与えていればそちらを優先し、未指定なら `signs.text` を使うように修正。

**直近のアクティビティ:** justinmk が sagg0t と antonk52 にレビュー依頼。

**変更ファイル:** `runtime/lua/vim/diagnostic.lua` 1 ファイル、+11/-5 行。

**次のアクション:** 関係者レビュー。

---

### ![PR](icons/git-pull-request.svg) [build(zig): add formatc and lintc steps](https://github.com/neovim/neovim/pull/38546) `#38546`
**ステータス:** Open | **ラベル:** ci:skip-news

**概要:** Zig ビルドに `formatc`/`lintc` ステップを追加。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `build.zig`、`build.zig.zon`、`.clang-tidy`、`.clangd`、`MAINTAIN.md` など 11 ファイル。+492/-15 行。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0418: wildcards in expanded env vars reinterpreted by glob](https://github.com/neovim/neovim/pull/39517) `#39517`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** 環境変数を展開した結果に含まれるワイルドカードがその後のグロブで再解釈される問題を修正する Vim パッチ移植。`expand_env_esc()` の 4 引数目を bool ではなくエスケープ対象文字列に変更し、`PATH_ESC_WILDCARDS` を渡す。Issue #31334 を解決。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `cmdexpand.c`、`ex_docmd.c`、`file_search.c`、`os/env.c`、`path.c`/`path.h` など 10 ファイル。

**次のアクション:** Vim パッチレビュー手続き。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): reset cursor style on exit](https://github.com/neovim/neovim/pull/39522) `#39522`
**ステータス:** Closed | **ラベル:** tui

**概要:** Windows Terminal で nvim 終了後にカーソルスタイルが元に戻らない問題を、`DECSCUSR 0` で端末既定にリセットするように変更。

**直近のアクティビティ:** kylesower が、Cygwin (mintty) + Win10 では関連 issue #25619 の画面消去問題は解決していなかったと報告し、`message.c` の修正の必要性に疑問を提示。justinmk が luukvbaal にレビュー依頼。

**変更ファイル:** `src/nvim/tui/tui.c` 1 ファイル、+6/-0 行。

**次のアクション:** Closed されたが、関連 issue の解決手段は要再検討。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): don't strip debuginfo in precompile module](https://github.com/neovim/neovim/pull/39191) `#39191`
**ステータス:** Merged | **ラベル:** build, lua, ci:backport release-0.12

**概要:** プリコンパイル済みバイトコードでも `debug.getinfo` が詳細を返すよう、`luac`/`luajit -b` の代わりに `loadstring`+`string.dump(…, false)` を使う実装に切替。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `CMakeLists.txt`、`src/gen/gen_char_blob.lua`、`src/gen/gen_steps.zig`、`src/nvim/lua/executor.c` など 5 ファイル。

**次のアクション:** マージ済み (release-0.12 へバックポート対象)。

---

### ![PR](icons/git-pull-request.svg) [feat(lua)!: deprecate or move functions in `vim.F`](https://github.com/neovim/neovim/pull/39495) `#39495`
**ステータス:** Open | **ラベル:** lua, breaking-change

**概要:** `vim.F` の整理を進める破壊的変更。`vim.F.if_nil` を `vim.nonnil` に、`npcall` を `vim.npcall` に移し、`ok_or_nil`/`nil_wrap` は削除。`vim.isnil` を新規追加。`{un,}pack_len` は据え置き。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `runtime/doc/deprecated.txt`、`lua.txt`、`news.txt`、`runtime/lua/vim/F.lua`、`vim/_core/shared.lua`、`vim/_core/util.lua`、`vim/diagnostic.lua` など 22 ファイル。+197/-58 行。

**次のアクション:** API 設計の合意形成と移行ドキュメント整備。

---

### ![PR](icons/git-pull-request.svg) [feat(highlights): new Dimmed hl group for de-emphasized text](https://github.com/neovim/neovim/pull/39505) `#39505`
**ステータス:** Open | **ラベル:** なし

**概要:** カラースキームが `Comment` を二次的テキスト用に流用していた慣習を整理し、専用の `Dimmed` 高度グループを新設。デフォルトでは `Comment` にリンクして互換性維持。

**直近のアクティビティ:** justinmk が「すでに適用できそうな箇所はある?」と質問。

**変更ファイル:** `news.txt`、`syntax.txt`、`highlight_group.c`、parser_spec の 4 ファイル。

**次のアクション:** 適用箇所の例示と議論。

---

### ![PR](icons/git-pull-request.svg) [feat(ui): use vim.ui.select for :oldfiles, :recover](https://github.com/neovim/neovim/pull/39488) `#39488`
**ステータス:** Merged | **ラベル:** ui, spell

**概要:** `:oldfiles` と `:recover` (スワップファイル復旧) を `vim.ui.select` 経由に統一。既知の `recover_names` の `<dir>//<dir>/<file>` パス生成バグも修正。さらに 39506 (z=/tselect が select_blocking で動かない) を `vim.wait` を捨てて async 化することで修正。

**直近のアクティビティ:** justinmk のコメント "The work continues." と画像のみ。

**変更ファイル:** `news.txt`、`vim/_core/ex_cmd.lua`、`spell.lua`、`swapfile.lua`、`tag.lua`、`ex_cmds.c`、`ex_docmd.c` など 20 ファイル。+497/-284 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(env): force uppercase environ() keys on Windows](https://github.com/neovim/neovim/pull/39523) `#39523`
**ステータス:** Merged | **ラベル:** なし

**概要:** Windows で `vim.fn.environ()` のキーが大文字化されなくなったリグレッション (#39443) を修正。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `vimfn.lua`、`eval/funcs.c`、`strings.c`、`environ_spec.lua` の 4 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(vim.system): use REAL PIPES for vim.system, similar to jobs](https://github.com/neovim/neovim/pull/39469) `#39469`
**ステータス:** Merged | **ラベル:** なし

**概要:** `vim.system` で `uv pipe` (実体はソケット) ではなく `pipe(3p)` 互換の本物のパイプを使うようにする。`vim.system()` 版の #35991 ベースの修正。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_core/system.lua`、`test/functional/lua/system_spec.lua` の 2 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [refactor(tty): tty.request()](https://github.com/neovim/neovim/pull/39489) `#39489`
**ステータス:** Merged | **ラベル:** defaults, refactor

**概要:** 散在していた `TermRequest` ハンドラと `XTGETTCAP DCS` 限定の `tty.query` をまとめ、汎用の `tty.request()` を提供。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `vim/_core/defaults.lua`、`vim/_init_packages.lua`、`vim/_meta.lua`、`vim/tty.lua`、`vim/ui/clipboard/osc52.lua`、`vim/ui/img/_kitty.lua` など 11 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): send didClose and didOpen when languageId changes](https://github.com/neovim/neovim/pull/39499) `#39499`
**ステータス:** Merged | **ラベル:** lsp, AI assisted

**概要:** modeline などでバッファのファイルタイプが変わってもクライアントがアタッチを継続する場合に、`textDocument/didClose` + `didOpen` を送り直して新しい `languageId` をサーバへ伝える修正。Gemini 3.1 Pro の AI 支援。

**直近のアクティビティ:** neovim-backports[bot] が release-0.12 へのバックポート cherry-pick が衝突したと報告し、手動対応を依頼。

**変更ファイル:** `lsp.txt`、`news.txt`、`vim/lsp.lua`、`vim/lsp/client.lua`、`lsp_spec.lua` の 5 ファイル。

**次のアクション:** リリースブランチへの手動バックポート。

---

### ![PR](icons/git-pull-request.svg) [refactor(json)!: drop "eval" impl of JSON de/serialization](https://github.com/neovim/neovim/pull/39181) `#39181`
**ステータス:** Open (Draft) | **ラベル:** refactor, breaking-change

**概要:** `eval/decode.c`/`eval/encode.c` の Vimscript JSON 実装を削除する探索 PR。Lua から Vimscript への JSON ラウンドトリップ機能の利用者がいるか不明なため、まず実験的に。

**変更ファイル:** `vim/_core/vimfn.lua`、`channel.c`、`eval.lua`、`eval/decode.c`、`eval/encode.c`、`eval/funcs.c`、`json_spec.lua` など 12 ファイル。+234/-1700 行。

**次のアクション:** ユーザー影響調査と方針決定。

---

### ![PR](icons/git-pull-request.svg) [feat(img): support images relative to the editor or buffer by using unicode placeholders](https://github.com/neovim/neovim/pull/39496) `#39496`
**ステータス:** Open (Draft) | **ラベル:** ci:skip-news

**概要:** `vim.ui.img.Opts` に `relative` (`ui`/`editor`/`buffer`) と `buf` フィールドを追加。Unicode placeholder と仮想行・extmark を活用してエディタや特定バッファ相対の画像表示を可能にする実装。

**変更ファイル:** `runtime/lua/vim/ui/img.lua`、`_kitty.lua`、`_placeholders.lua`、`img_spec.lua` の 4 ファイル。+980/-45 行。

**次のアクション:** 依存 PR #39484 マージ後の rebase とレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.net): accept multiple http methods on request()](https://github.com/neovim/neovim/pull/39406) `#39406`
**ステータス:** Open | **ラベル:** なし

**概要:** `vim.net.request` を GET 限定から HTTP メソッド全般 (POST/PUT/PATCH/DELETE/HEAD/OPTIONS) に拡張する第一弾。レスポンスの `status` 系拡充や `body` の table 自動 JSON 化は次段以降。

**変更ファイル:** `lua.txt`、`news.txt`、`vim/net.lua`、`net_spec.lua` の 4 ファイル。

**次のアクション:** 関連 issue #38946 の API 設計合意。

---

### ![PR](icons/git-pull-request.svg) [refactor(globals): group search state into SearchState struct](https://github.com/neovim/neovim/pull/39485) `#39485`
**ステータス:** Open | **ラベル:** refactor, needs:response

**概要:** `globals.h` の 283 個の `EXTERN` シンボルを subsystem 単位の構造体へ集約する移行の第一スライス。検索系 8 個のグローバルを `SearchState` に集約。

**変更ファイル:** `change.c`、`cmdexpand.c`、`drawline.c`、`drawscreen.c`、`eval/vars.c` など 17 ファイル。

**次のアクション:** メンテナーからの方針応答待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): get_node_text() drops trailing newline for buffer source](https://github.com/neovim/neovim/pull/39409) `#39409`
**ステータス:** Open | **ラベル:** treesitter, ci:backport release-0.12

**概要:** ノード範囲の `end_col == 0` (ノードが改行で終わる) 時に、buffer ソースで末尾 `\n` が落ちるバグを `buf_range_get_text()` で改行を補い、`#trim!` の暫定対応も削除。Issue #28677 を解決。

**直近のアクティビティ:** dbalatero が「CI が完了するのを待ってからレビュー依頼するつもりだった」と説明、justinmk が好意的にレビュー予定。

**変更ファイル:** `treesitter.lua`、`languagetree.lua`、`query.lua`、tests 5 ファイル。

**次のアクション:** CI 修正とレビュー反映。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0416: filename completion splits at space for single-file Ex commands](https://github.com/neovim/neovim/pull/39520) `#39520`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** Unix 上で単一ファイル引数の Ex コマンドのファイル名補完が空白で分割される問題を、`SPACE_IN_FILENAME` の ifdef を撤去して修正。

**変更ファイル:** `cmdexpand.c`、`test_cmdline.vim`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0415: Wrong behavior when executing register that ends in Insert mode](https://github.com/neovim/neovim/pull/39518) `#39518`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** Insert モードで終わるレジスタを実行した際の挙動を `:startinsert` 等で復元するように修正する Vim パッチ移植。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0417: completion: no support for "noinsert" with 'wildmode'](https://github.com/neovim/neovim/pull/39516) `#39516`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** `'wildmode'` に `noinsert` を追加し、`completeopt` の `noinsert` と同等の挙動をコマンドライン補完にも提供。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build(lint): allow "bufnr" as positional param](https://github.com/neovim/neovim/pull/39515) `#39515`
**ステータス:** Merged | **ラベル:** refactor, treesitter, diagnostic, filetype, snippet

**概要:** lint 規約で `bufnr` を位置引数名として許可 (フィールド名や関数名としてはこれまで通り誤りを指摘)。多数のサブシステムにわたる小さな改名。

**変更ファイル:** `diagnostic.txt`、`lua.txt`、`treesitter.txt`、`editorconfig.lua` など 17 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:7da90de: runtime(cabal): add missing haskell language editions](https://github.com/neovim/neovim/pull/39533) `#39533`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** `runtime/syntax/cabal.vim` に欠落していた Haskell 言語 edition を追加。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: misc](https://github.com/neovim/neovim/pull/39511) `#39511`
**ステータス:** Merged | **ラベル:** ci, lsp

**概要:** ドキュメント類のまとめ更新。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: Update instructions for debugging LSP](https://github.com/neovim/neovim/pull/39527) `#39527`
**ステータス:** Merged | **ラベル:** lsp, typo, ci:backport release-0.12

**概要:** `vim.lsp.log.set_format_func(vim.inspect)` を勧めていた古い指示を撤回し、デフォルトのフォーマッタに任せるよう更新。

**直近のアクティビティ:** backports[bot] がバックポート PR #39530 を自動作成。

**次のアクション:** マージ済み (release-0.12 にバックポート済み #39530)。

---

### ![PR](icons/git-pull-request.svg) [backport: docs: Update instructions for debugging LSP](https://github.com/neovim/neovim/pull/39530) `#39530`
**ステータス:** Merged | **ラベル:** lsp, target:release

**概要:** #39527 の release-0.12 へのバックポート。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [backports](https://github.com/neovim/neovim/pull/39519) `#39519`
**ステータス:** Merged | **ラベル:** lsp, target:release, AI assisted

**概要:** #39499 と #39515 のリリースブランチバックポートをまとめた PR。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39466) `#39466`
**ステータス:** Open (Draft) | **ラベル:** vim-patch

**概要:** `src/nvim/version.c` の更新ドラフト。

**次のアクション:** Draft 解除待ち。

---

### ![PR](icons/git-pull-request.svg) [refactor(excmd): pass fargs to Lua for builtin cmds](https://github.com/neovim/neovim/pull/39528) `#39528`
**ステータス:** Open | **ラベル:** refactor

**概要:** `nlua_do_ucmd` だけが行っていた `eap->arg` の空白分割トークナイズを `nlua_push_eap` に移し、ビルトインコマンド経由 (`nlua_call_excmd`) からも `eap.fargs` が利用できるようにする。`M.ex_lsp` の再パースを削除。

**変更ファイル:** `vim/_core/ex_cmd.lua`、`swapfile.lua`、`tag.lua`、`executor.c`、`lsp_spec.lua` の 5 ファイル。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(windows): "Assertion failed: process_title"](https://github.com/neovim/neovim/pull/39023) `#39023`
**ステータス:** Closed (Draft) | **ラベル:** ci

**概要:** Windows oldtest CI が `process_title` のアサーションで落ちる問題の探索。libuv 1.52.1 で症状緩和されているが、根本原因不明のまま Close。

**変更ファイル:** `test_windows.yml`、`api.txt`、API 関連 13 ファイル。+39/-397 行。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: vim._with silents all cmdline-errors if emsg_silent and not silent](https://github.com/neovim/neovim/pull/34301) `#34301`
**ステータス:** Open | **ラベル:** lua

**概要:** `vim._with` が `emsg_silent` を立てたまま cmdline エラーを抑制してしまう問題の修正 (#37850)。

**直近のアクティビティ:** altermo が justinmk に追加コミットの目的を確認。

**変更ファイル:** `src/nvim/lua/stdlib.c` 1 ファイル。

**次のアクション:** コミット意図の説明とレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(style): disable clang-format](https://github.com/neovim/neovim/pull/39514) `#39514`
**ステータス:** Open | **ラベル:** なし

**概要:** clangd が保存時に clang-format をかけてしまう問題に対し、リポジトリで clang-format を無効化する。CI は uncrustify。

**直近のアクティビティ:** zeertzjq が「`==` は formatexpr を使わないから問題ない」と確認。

**変更ファイル:** `.clang-format`、`CONTRIBUTING.md`、`version.c`。

**次のアクション:** 実運用への影響評価。

---

### ![PR](icons/git-pull-request.svg) [feat(eval): add v:lastactivity and expose in serverlist() info](https://github.com/neovim/neovim/pull/39423) `#39423`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** UI ピッカー (`:connect` 等) からリモートサーバの活動状態を判別可能にするため、`vgetc()` ごとに 1 秒粒度で更新される `v:lastactivity` を追加し、`serverlist({info=true})` で `last_activity` を露出。

**変更ファイル:** `vimfn.txt`、`vvars.txt`、`vim/_core/server.lua`、`vim/_meta/vimfn.gen.lua` など 12 ファイル。

**次のアクション:** 依存 PR #39392 のマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [build(zig): run oldtests in zig builds (RETROMANIA HYPERDRIVE)](https://github.com/neovim/neovim/pull/39074) `#39074`
**ステータス:** Open (Draft) | **ラベル:** build, ci

**概要:** Makefile + シェル + vim ラッパースクリプトで起動していた oldtests を、シンプルな nvim Lua ラッパー一本に置き換える試み。`--headless` を `:terminal` で囲む現状を整理。

**変更ファイル:** `test.yml`、`treesitter.lua`、`language_spec.lua`、`old/runner.lua`、`run_tests.zig` など 6 ファイル。

**次のアクション:** Draft 解除待ち。

---

### ![PR](icons/git-pull-request.svg) [ci: bump the github-actions group across 2 directories with 5 updates](https://github.com/neovim/neovim/pull/39470) `#39470`
**ステータス:** Open | **ラベル:** ci, dependencies, github_actions

**概要:** dependabot による korthout/backport-action、github/codeql-action、msys2/setup-msys2、zizmorcore/zizmor-action、actions/cache の更新。

**変更ファイル:** workflow 5 ファイル。

**次のアクション:** CI 通過後のマージ。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): get_node_text returns trailing newline for buffer source](https://github.com/neovim/neovim/pull/39455) `#39455`
**ステータス:** Closed | **ラベル:** treesitter

**概要:** #28677 の修正案その 1。`buf_range_get_text` で `end_col == 0` フラグを記録し、結合後に `\n` を補う。

**直近のアクティビティ:** justinmk が #39409 を採用するため Close。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): `get_node_text()` newline handling consistency](https://github.com/neovim/neovim/pull/28841) `#28841`
**ステータス:** Closed | **ラベル:** treesitter

**概要:** 同じ #28677 を別解で修正していた古い PR。

**直近のアクティビティ:** justinmk が #39455 と #39409 のほうで対応するためクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`:h i_CTRL-U-default` cannot find any help page](https://github.com/neovim/neovim/issues/39535) `#39535`
**ステータス:** Open | **ラベル:** なし

**概要:** `:h i_CTRL-U-default` (大文字) では見つからないが `:h i_ctrl-u-default` だと開ける、というヘルプ参照のケース不整合。`i_CTRL-W-default`、`CTRL-L-default` でも同様だが `CTRL-W_d-default` は正常。

**直近のアクティビティ:** zeertzjq が yochem に対応依頼。

**次のアクション:** 担当者の対応待ち。

---

### ![Issue](icons/issue-opened.svg) [treesitter: attempt to call method 'range' (a nil value) in languagetree.lua:215](https://github.com/neovim/neovim/issues/39032) `#39032`
**ステータス:** Closed | **ラベル:** treesitter

**概要:** Markdown の fenced code block で `(#set! conceal_lines "")` ディレクティブが `range()` を nil で呼んで落ちるという 0.12.1 バグ報告。

**直近のアクティビティ:** stewdotorg と IshanMohanBishnoi が「nvim-treesitter のメンテナがアーカイブした件」と関連 reddit を共有。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [LazyVim having problems after automatic install and running :LazyHealth](https://github.com/neovim/neovim/issues/39531) `#39531`
**ステータス:** Closed | **ラベル:** closed:invalid

**概要:** LazyVim 自動インストール後の問題報告。

**直近のアクティビティ:** superatomic が「対象は LazyVim リポジトリで詳細を提示して」と案内。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [gx seems to be broken on Windows on the latest nightlies](https://github.com/neovim/neovim/issues/39524) `#39524`
**ステータス:** Open | **ラベル:** platform:windows

**概要:** nvim 0.13 nightly の Windows で `gx` が URL を開けなくなったリグレッション。0.12.2 では動作。

**直近のアクティビティ:** ntdiary がスラッシュ正規化が原因と特定し、`vim/_core/system.lua` の C ではなく Lua 側でも `cmd.exe` パスを `\` に戻す必要があると指摘。`exepath()` が常に `\` を返す方針 (#39382 参照) も議論。一時回避は `cmd = cmd1:gsub("/", "\\")`。

**次のアクション:** Lua 側スラッシュ正規化と `exepath` 仕様変更の合意。

---

### ![Issue](icons/issue-opened.svg) [LSP: client/registerCapability crashes on unknown feature names](https://github.com/neovim/neovim/issues/39491) `#39491`
**ステータス:** Open | **ラベル:** lsp

**概要:** 未知の LSP メソッド名 (例 `tabby/chat`) で `client/registerCapability` を受けると `client.lua:969: table index is nil` で落ちる。PR #37161 のリグレッション。

**直近のアクティビティ:** MariaSolOs が「LSP 仕様上、独自メソッドの動的登録は許容され得る」と指摘。tris203 が「仕様的に許されるか?」と疑問。justinmk は「最低でも意味のあるエラー文を出すべき」と合意。

**次のアクション:** エラー処理改善と仕様解釈の合意。

---

### ![Issue](icons/issue-opened.svg) [ui: select_blocking prevents execution of vim.ui.select](https://github.com/neovim/neovim/issues/39506) `#39506`
**ステータス:** Closed | **ラベル:** ui

**概要:** `vim.ui.select` の async 実装 (artio.nvim) が `vim.wait` で停止させられ、`z=` や `tselect` が動かなくなる問題。

**直近のアクティビティ:** comfysage が「artio でも動くようになった、ありがとう」、mikejmcguirk も動作確認。#39488 で解決。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [LSP: support for `workspace/textDocumentContent`](https://github.com/neovim/neovim/issues/39526) `#39526`
**ステータス:** Open | **ラベル:** lsp

**概要:** dotnet/roslyn の source generation など、サーバ生成の仮想ドキュメントを扱う `workspace/textDocumentContent`/`refresh` の LSP 3.18 サポート要望。`server_capabilities.workspace.textDocumentContent.schemes` ごとに `BufReadCmd` autocmd を登録するアイデア。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 設計議論。

---

### ![Issue](icons/issue-opened.svg) [gx, vim.ui.open: treat `foo.bar` as a URL if file not found](https://github.com/neovim/neovim/issues/39521) `#39521`
**ステータス:** Open | **ラベル:** platform:windows, needs:discussion, filesystem, uri

**概要:** Windows 11 で `www.google.com` に `gx` を打つと `cmd.exe /c start "" www.google.com` が失敗する。文字列が URL かローカルファイルかの判定方針が問題。

**直近のアクティビティ:** wookayin、justinmk が「`x.y.z` を URL と仮定してよいかは決めるのが難しい」と議論。ファイルが見つからない場合は URL と仮定する案も検討中。

**次のアクション:** 仕様決定と実装。

---

### ![Issue](icons/issue-opened.svg) [Refactor: migrate `globals.h` to per-subsystem context structs](https://github.com/neovim/neovim/issues/39481) `#39481`
**ステータス:** Open | **ラベル:** refactor

**概要:** `globals.h` の 283 個の `EXTERN` を subsystem 単位の構造体へ段階移行する大規模リファクタ提案。スレッド化や C → Lua 移行の準備。

**直近のアクティビティ:** mikejmcguirk が「FFI で参照されているグローバル (例 `KeyStuffed`) には新しい公開インターフェースが要るかも」と指摘。

**次のアクション:** 段階的 PR (例 #39485) で進行中。

---

### ![Issue](icons/issue-opened.svg) [`:browse edit` (OS file browser) doesn't work](https://github.com/neovim/neovim/issues/26472) `#26472`
**ステータス:** Open | **ラベル:** ui-extensibility

**概要:** ヘルプ通りに `:browse edit` を実行してもファイルブラウザが開かないという長期 issue。

**直近のアクティビティ:** justinmk が「`:browse` を `vim.ui.select` に委ねるのが妥当、`vim.ui.browse` は OS ダイアログがイマイチなので不要」と方向性を提示。

**次のアクション:** `vim.ui.select` 委譲化の実装。

---

### ![Issue](icons/issue-opened.svg) [vim.net as a full fledged http client](https://github.com/neovim/neovim/issues/38946) `#38946`
**ステータス:** Open | **ラベル:** lua, network

**概要:** `vim.net.request` を Python `requests` 風の API (`vim.net.get/post/put/...`) に拡張する設計案。`Opts` に `body`/`json` を追加し、レスポンスに status/headers を含める。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR #39406 で第一弾実装中。

---

### ![Issue](icons/issue-opened.svg) [`vim.fn.environ()` letter casing of keys on Windows](https://github.com/neovim/neovim/issues/39443) `#39443`
**ステータス:** Closed | **ラベル:** platform:windows, vimscript, bug-regression, has:bisected

**概要:** PR #39176 以降、Windows で `vim.fn.environ()` のキーが大文字化されなくなったリグレッション報告。

**直近のアクティビティ:** PR #39523 で解決し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`get_node_text()` behaves differently between buffer and string sources on EOL char](https://github.com/neovim/neovim/issues/28677) `#28677`
**ステータス:** Open | **ラベル:** complexity:low, treesitter

**概要:** `get_node_text()` で `end_col == 0` のノードに対し buffer ソースと string ソースで結果が違うバグ。0.9.5 から指摘されている長期 issue。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR #39409 で対応進行中。

---

### ![Issue](icons/issue-opened.svg) [Window path separator (slash) handling](https://github.com/neovim/neovim/issues/39382) `#39382`
**ステータス:** Open | **ラベル:** platform:windows, environment, system, filesystem

**概要:** Windows のパス区切り扱いを「内部は常に `/`、必要箇所のみ `\` に変換、`shellslash` 尊重」に整える戦略 issue。`exepath`、`expand`、`fnamemodify` などの仕様も整理。

**次のアクション:** 段階的に PR を出す予定。

---

### ![Issue](icons/issue-opened.svg) [package index (plugin "registry")](https://github.com/neovim/neovim/issues/24221) `#24221`
**ステータス:** Open | **ラベル:** plugin, distribution

**概要:** Vim Awesome 相当のプラグインレジストリ (packages.neovim.io) を構築する古い提案。

**次のアクション:** 直近の動きはなし。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> Zig 0.16 移行を見据えた基盤整備が進む一方、macOS 周辺 (IME、フォーカス、メニュー、フルスクリーン) のバグ修正と新機能 (RTL シェイピング、recent ペイン移動) が同時並行。

### ![PR](icons/git-pull-request.svg) [font/shaper: add RTL shaping for Arabic and Hebrew](https://github.com/ghostty-org/ghostty/pull/11079) `#11079`
**ステータス:** Open | **ラベル:** font

**概要:** アラビア文字・ヘブライ文字を強制 LTR で扱っていたため壊れていた表示を、UAX #9 ベースの双方向アルゴリズム実装 itijah (純 Zig、MIT、著者は本 PR 作成者) を導入することで修正。HarfBuzz と CoreText の双方に RTL 方向と cluster anchor 追跡を組み込む。

**直近のアクティビティ:** DiaaEddin が ramysami 向けに mac arm 用ビルドを共有。

**変更ファイル:** `build.zig.zon`、`SharedDeps.zig`、`GhosttyZig.zig`、`uucode_config.zig`、`bidi_helpers.zig`、`coretext.zig`、`harfbuzz.zig`、`run.zig` 等 10 ファイル。+1776/-289 行。

**次のアクション:** itijah を `deps.files.ghostty.org` にミラーするかの判断と、選択・カーソル位置などフォローアップ対応。

---

### ![PR](icons/git-pull-request.svg) [macOS: restore some menu shortcuts when terminal is not focused](https://github.com/ghostty-org/ghostty/pull/12522) `#12522`
**ステータス:** Open | **ラベル:** os/macos

**概要:** ターミナル非フォーカス時にネイティブビューが macOS メニューバーのキーイクィヴァレント (copy/paste/select all/undo/redo) を扱えるよう、ファーストレスポンダー変更時にメニューを再同期。`cut` メニューも復活してフォーカス時に無効化。Issue #10749 を解決。

**直近のアクティビティ:** bo2themax が「再同期の最適化と config リロードへの影響確認」を TODO に挙げる。

**変更ファイル:** `AppDelegate.swift`、`MainMenu.xib`、`Ghostty.Config.swift`、`Ghostty.MenuShortcutManager.swift`、`SurfaceView_AppKit.swift`。+110/-17 行。

**次のアクション:** TODO 消化とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add `recent` direction to `goto_split` action](https://github.com/ghostty-org/ghostty/pull/9723) `#9723`
**ステータス:** Open | **ラベル:** なし

**概要:** `goto_split` アクションに `recent` 方向を追加し、最近フォーカスしていたペインへ移動可能に。Codex の支援で macOS から GTK へコード移植。Issue #9716 を実装。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `include/ghostty.h`、`BaseTerminalController.swift`、`Ghostty.App.swift`、`apprt/action.zig`、`apprt/gtk/class/application.zig`、`split_tree.zig`、`Binding.zig` など 9 ファイル。+166/-25 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: distinguish debug AppIntent title](https://github.com/ghostty-org/ghostty/pull/12026) `#12026`
**ステータス:** Open | **ラベル:** os/macos

**概要:** Debug ビルドの `AppIntent` タイトルを区別し、開発時のデバッグを容易にする小修正。

**変更ファイル:** AppIntent 群 8 ファイル。+48/-0 行。

**次のアクション:** 軽量レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: fix surface focus/render state after dragging in to to another window/tab](https://github.com/ghostty-org/ghostty/pull/12338) `#12338`
**ステータス:** Open | **ラベル:** os/macos

**概要:** 別ウィンドウ/タブへサーフェスを素早くドラッグした際のキー入力受信不能と描画停止の 2 バグを修正。後者は drop 後にオクルージョン状態が visible に更新されないのが原因。

**変更ファイル:** `BaseTerminalController.swift`、`SurfaceView_AppKit.swift` の 2 ファイル。+19/-3 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: refine new-window behaviors](https://github.com/ghostty-org/ghostty/pull/11161) `#11161`
**ステータス:** Open | **ラベル:** os/macos

**概要:** `lastCascadePoint` を移動時にも更新し、画面に既存ウィンドウがない場合はカスケード位置ではなく前回の frame を復元する。`LastWindowPosition` を screen ID 単位で frame を保存・復元する形に変更。フルスクリーンウィンドウは保存対象外。Issue #9310 を解決。

**変更ファイル:** `AppDelegate.swift`、AppleScript 統合、`TerminalController.swift`、`NSWindow+Extension.swift`、UI テストなど 10 ファイル。+232/-106 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: fix focus state when toggling command palette from inline title editor](https://github.com/ghostty-org/ghostty/pull/12524) `#12524`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** インラインタイトルエディタからコマンドパレットを開いたときに、サーフェスのファーストレスポンダー状態がパレット表示後に true へ戻ってしまい貼り付けが奪われる問題を修正。

**変更ファイル:** `BaseTerminalController.swift` 1 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chore(macOS): remove Ghostty.xctestplan in project tree](https://github.com/ghostty-org/ghostty/pull/12520) `#12520`
**ステータス:** Merged | **ラベル:** なし

**概要:** Xcode が生成するテストプランをプロジェクトツリーから外す。

**変更ファイル:** `Ghostty.xcodeproj/project.pbxproj`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macOS: enable copy only when there's actual selected text](https://github.com/ghostty-org/ghostty/pull/12521) `#12521`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** Copy アクションを実選択がある時のみ有効化し、macOS の標準的なテキスト編集挙動に揃える。

**変更ファイル:** `SurfaceView_AppKit.swift` 1 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macos: suppress control-char input while composing](https://github.com/ghostty-org/ghostty/pull/12518) `#12518`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** AppKit が IME コンポジション中に C0 制御文字を渡してきた場合、それをコンポジションが消費したものとして端末に送らない。日本語 IME 中の `ctrl+h/j/m/n` が端末に漏れる問題 (#10460 ほか) を解消。

**直近のアクティビティ:** OpenAI Codex 支援、AI 開示あり。

**変更ファイル:** `SurfaceView_AppKit.swift`、`SurfaceViewAppKitTests.swift`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fall back to Zig-bundled Darwin headers when an SDK can't be found](https://github.com/ghostty-org/ghostty/pull/12534) `#12534`
**ステータス:** Merged | **ラベル:** libghostty

**概要:** Darwin 上のビルドで Xcode SDK が見つからない時に Zig 同梱の Darwin ヘッダにフォールバック。

**直近のアクティビティ:** mitchellh が「CI 失敗は無関係」「これは libghostty で長らく問題だったので、CI も nix build を回す形に拡張したい」とコメント。bo2themax が具体的なエラー内容を質問。

**変更ファイル:** `pkg/apple-sdk/build.zig`。

**次のアクション:** マージ済み。CI 拡張は別 PR で。

---

### ![Issue](icons/issue-opened.svg) [macOS: Korean IME — first Hangul-composing key after IME switch is committed as decomposed jamo (regression in 1.3.x)](https://github.com/ghostty-org/ghostty/issues/12541) `#12541`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** macOS で韓国語 IME に切り替えた直後、最初の Hangul キーがコンポジションを経ず分解 jamo (NFD) として確定されるリグレッション報告。100% 再現で iTerm2/Terminal.app は問題なし。

**直近のアクティビティ:** ghostty-vouch[bot] により「Issue ではなく Discussion から始めて」というルールに従い自動クローズ。

**次のアクション:** Discussion での再投稿が必要。

---

### ![Issue](icons/issue-opened.svg) [Line selection with Shift + End or Shift + Home not working on micro or nano #4491](https://github.com/ghostty-org/ghostty/issues/12539) `#12539`
**ステータス:** Closed | **ラベル:** なし

**概要:** Ghostty で nano/micro 等のエディタ内で `Shift+End`/`Shift+Home` による行選択が効かない (kitty/alacritty/gnome-terminal では動作する) という報告。

**直近のアクティビティ:** ghostty-vouch[bot] による自動クローズ。

**次のアクション:** Discussion 経由で。

---

### ![Issue](icons/issue-opened.svg) [kitty keyboard protocol: AltGr+plus should produce a tilde, but produces a plus instead](https://github.com/ghostty-org/ghostty/issues/2981) `#2981`
**ステータス:** Open | **ラベル:** os/linux, input

**概要:** Linux + DE レイアウトで AltGr 修飾の解釈が kitty と Ghostty で食い違う長期 issue。

**直近のアクティビティ:** 00-kat が「OP は #8899 とは別の問題で、`GTK_IM_MODULE=simple` 回避策が効くようになる前の話」と整理。neurocyte が「OP の問題は今も残っている」と確認。

**次のアクション:** 詳細調査と修正。

---

### ![Issue](icons/issue-opened.svg) [Zig 0.16 migration](https://github.com/ghostty-org/ghostty/issues/12228) `#12228`
**ステータス:** Open | **ラベル:** なし

**概要:** Zig 0.16 (2026-04-13 リリース) への移行トラッキング。`std.Io` 新インターフェース、`std.posix` 大幅削減、`@cImport` 削除を見据えて libxev など主要依存はおおむね 0.16 対応済み。残るは `zf` のみ。

**直近のアクティビティ:** dribic と ReillyBrogan が「0.17 が LLVM 更新だけで済むのか」を議論。0.16 から分岐して LLVM サポートだけ cherry-pick したらどうかという提案。

**次のアクション:** `zf` 対応待ちと PR #12388 進行。

---

## astral-sh/uv

> Wine/Android などプラットフォーム互換性、Windows 管理 Python の `pythonw` 起動修正、TOML 1.1 自動検出、Astral ミラー URL、ロック時マルウェア検査など preview/拡張機能が並ぶ。Renovate による依存更新も多数。

### ![PR](icons/git-pull-request.svg) [Fix pythonw launcher mode for managed Windows venv links](https://github.com/astral-sh/uv/pull/19235) `#19235`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows の管理 Python マイナーバージョンリンクで `pythonw.exe` がコンソール launcher のままになり、`.pyw` 実行時にコンソールが開いてしまうバグを修正。`is_windowed_python_launcher` で出力リンク名から windowed モードを判定。Issue #19226 を解決。

**変更ファイル:** `crates/uv-python/src/managed.rs` 1 ファイル、+17/-4 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix `uv tool uninstall` silently aborting after first corrupt tool](https://github.com/astral-sh/uv/pull/19234) `#19234`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv tool uninstall ruff black` で `ruff` の receipt が壊れていると `return Ok(())` でループを抜けてしまい後続の `black` がアンインストールされない問題を、`continue` に変更して修正。Issue #19219 を解決。

**変更ファイル:** `crates/uv/src/commands/tool/uninstall.rs` の 1 行修正。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Reject locked malware installations](https://github.com/astral-sh/uv/pull/18936) `#18936`
**ステータス:** Open | **ラベル:** enhancement, preview

**概要:** `uv sync` 等のプロジェクト同期処理に OSV ベースのマルウェアチェックを追加。`uv audit` の機構を `MAL-` 報告にフィルタして適用し、ヒットした場合インストールを拒否。`UV_MALWARE_CHECK=0` で無効化、`UV_MALWARE_CHECK_URL` で OSV 互換サービスを差し替え可能。プライベートパッケージは情報漏洩リスクを避けるため事前にフィルタ。

**直近のアクティビティ:** codspeed-hq[bot] のベンチマーク報告でパフォーマンスへの影響なし。

**変更ファイル:** `uv-audit`、`uv-cache`、`uv-client`、`uv-distribution-types`、`uv-preview`、`uv-resolver` 等 25 ファイル。+993/-102 行。

**次のアクション:** プライベート判定の実装方法やソース判定の詳細レビュー。

---

### ![PR](icons/git-pull-request.svg) [Detect TOML 1.1 and automatically enable the toml backwards compatibility preview feature](https://github.com/astral-sh/uv/pull/19227) `#19227`
**ステータス:** Open | **ラベル:** なし

**概要:** TOML 1.1 機能を検出して TOML 後方互換 preview を自動有効化する `uv-toml` クレートを新設。PyPI のトップ 3512 個の `pyproject.toml` でテスト済み。

**変更ファイル:** `Cargo.lock`、`crates/uv-build-backend/`、`crates/uv-toml/` 新設など 9 ファイル。+468/-3 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add Astral mirror URL override](https://github.com/astral-sh/uv/pull/19206) `#19206`
**ステータス:** Open | **ラベル:** なし

**概要:** Astral リリースメタデータ・成果物 (Ruff、CPython ダウンロード、self-update) のミラー指定 `UV_ASTRAL_MIRROR_URL` を追加。設定時は GitHub フォールバックを行わない。Issue #9134、#16519、#17255、#15970、#18525 を解決。

**直近のアクティビティ:** zsol が `self update` 連番イシューを解決する根拠を説明 (manifest ndjson に切替済みで、企業環境のミラーが容易に)。zanieb は型統合 (`AstralMirror` 型) を提案。

**変更ファイル:** `uv-bin-install/src/lib.rs`、`uv-python/src/downloads.rs`、`uv-static/src/env_vars.rs`、`uv/src/commands/self_update.rs` など 7 ファイル。+582/-93 行。

**次のアクション:** API 構造の合意。

---

### ![PR](icons/git-pull-request.svg) [fetch available CPython releases from the network](https://github.com/astral-sh/uv/pull/19148) `#19148`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** PBS バージョンメタデータを `releases.astral.sh` の ndjson から取得 (失敗時は GitHub フォールバック)。組み込みメタデータをフォールバックに、行単位ストリーミングで既存キャッシュへ前付け。

**変更ファイル:** `uv-cli`、`uv-platform`、`uv-python/src/downloads.rs`、`uv-python/src/installation.rs`、`uv-settings`、`uv-static`、`uv-test`、`uv/src/commands/python/find.rs` など 18 ファイル。+2636/-184 行。

**次のアクション:** Draft 解除と ndjson API 仕様確定。

---

### ![PR](icons/git-pull-request.svg) [Add module owners to workspace metadata](https://github.com/astral-sh/uv/pull/19122) `#19122`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `uv workspace metadata` に `--module-owners` フラグを追加し、モジュール → パッケージのマッピングを集めて出力する。`sync --imprecise` を全 extras/dependency-groups で実行。

**変更ファイル:** `uv-cli`、`uv-configuration`、`uv-distribution-types/src/installed_modules.rs`、`uv-pypi-types/src/module_name.rs`、`uv-resolver/src/lock/export/metadata.rs` など 15 ファイル。+1005/-22 行。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Readjust the trampoline CI check](https://github.com/astral-sh/uv/pull/19225) `#19225`
**ステータス:** Open (Draft) | **ラベル:** internal, build:windows-trampoline

**概要:** Windows trampoline バイナリを sources やラベル `build:windows-trampoline` の変更時のみ要求するように緩和し、毎バージョンの再ビルドを不要にする。

**直近のアクティビティ:** zanieb と konstin が「悪意ある変更が忍び込まないか」を議論。zanieb は -1 で「クレートバージョンを書き換えてビルドを決定論的にしたい」と主張。konstin は「チェックがソース変更時に必ず走るので、書き換え攻撃と差はない」と反論。

**変更ファイル:** `.github/workflows/ci.yml`、`test-windows-trampolines.yml` の 2 ファイル。

**次のアクション:** ビルド決定論性に関する設計議論。

---

### ![PR](icons/git-pull-request.svg) [Refactor query parameter redaction](https://github.com/astral-sh/uv/pull/19218) `#19218`
**ステータス:** Merged | **ラベル:** internal

**概要:** クエリパラメータ redaction の実装を整理する小規模リファクタ (#19146 の続き)。

**変更ファイル:** `crates/uv-redacted/src/lib.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Disable tests that depend on quarantined lightning package](https://github.com/astral-sh/uv/pull/19224) `#19224`
**ステータス:** Merged | **ラベル:** なし

**概要:** PyPI で `lightning` パッケージがプロジェクト級隔離になり、`exclude-newer` でも応答から消えるため依存テストを無効化。

**変更ファイル:** `crates/uv/tests/it/lock_conflict.rs` 1 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Handle incorrect unlock error in `LockedFile::drop` on Wine](https://github.com/astral-sh/uv/pull/19229) `#19229`
**ステータス:** Merged | **ラベル:** bug

**概要:** Wine の不正 unlock エラー (winehq bug 59711) を `LockedFile::drop` で吸収。

**変更ファイル:** `crates/uv-fs/src/locked_file.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Use symlinks instead of junctions on Wine](https://github.com/astral-sh/uv/pull/19213) `#19213`
**ステータス:** Merged | **ラベル:** bug

**概要:** Wine 環境では NTFS ジャンクションが使えないため、Wine 検出時にシンボリックリンクへ切り替え。Issue #19187 を解決。

**変更ファイル:** `uv-fs`、`uv-python/src/managed.rs`、`uv-windows/src/wine.rs`、`uv/src/commands/python/uninstall.rs` など 8 ファイル。+169/-35 行。

**次のアクション:** マージ済み。統合テストは #19214 で別途追加。

---

### ![PR](icons/git-pull-request.svg) [Add an integration test for Wine](https://github.com/astral-sh/uv/pull/19214) `#19214`
**ステータス:** Merged | **ラベル:** testing

**概要:** Wine 用統合テストを CI に追加し、#19213 の効果を検証。

**直近のアクティビティ:** zanieb と woodruffw が「`Z:\…\.lock` の lock violation など silent failure が他にも残っているのでは」と議論。

**変更ファイル:** `.github/workflows/test-integration.yml`。

**次のアクション:** マージ済み。残るサイレント不具合の追跡。

---

### ![PR](icons/git-pull-request.svg) [Fix file locks on Android](https://github.com/astral-sh/uv/pull/18323) `#18323`
**ステータス:** Merged | **ラベル:** なし

**概要:** Android (termux) でのファイルロック実装を修正し、termux パッチ (00002-revert-to-shared-lock.patch) を不要にする。

**変更ファイル:** `crates/uv-fs/src/locked_file.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add an integration test for Android](https://github.com/astral-sh/uv/pull/18302) `#18302`
**ステータス:** Open (Draft) | **ラベル:** testing, test:extended

**概要:** Android 用統合テストを CI に追加するドラフト。`test/integration/android-python-launcher.c`、`android.sh` を追加。

**変更ファイル:** workflow + テスト 4 ファイル。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Add riscv64 Docker images](https://github.com/astral-sh/uv/pull/17775) `#17775`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** RISC-V 64bit 用 Docker イメージを追加。Issue #17764 を解決予定。

**変更ファイル:** `.github/workflows/build-docker.yml`、`Dockerfile`。

**次のアクション:** Draft 解除待ち。

---

### ![PR](icons/git-pull-request.svg) [Add riscv64 Docker images](https://github.com/astral-sh/uv/pull/19228) `#19228`
**ステータス:** Closed (Draft) | **ラベル:** なし

**概要:** 上記 #17775 の重複として Close。

**直近のアクティビティ:** zanieb が「#17775 の重複?」とコメント。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Update conda-incubator/setup-miniconda action to v4](https://github.com/astral-sh/uv/pull/19233) `#19233`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による setup-miniconda v3.3.0 → v4.0.0 メジャー更新。Node 24 ランナー要求と ESM 化が破壊的変更。

**変更ファイル:** `test-integration.yml`、`test-system.yml`。

**次のアクション:** Node 24 ランナー前提を確認。

---

### ![PR](icons/git-pull-request.svg) [Update debian Docker tag to trixie-20260421](https://github.com/astral-sh/uv/pull/19232) / [Update fedora:45 Docker digest to 8b838b3](https://github.com/astral-sh/uv/pull/19231) / [Update cgr.dev/chainguard/python:latest-dev Docker digest to 507e6a9](https://github.com/astral-sh/uv/pull/19230)
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による Docker イメージダイジェスト/タグの月次更新 3 件。

**次のアクション:** 自動マージ予定 (現状は手動マージ待ち)。

---

### ![PR](icons/git-pull-request.svg) [Update taiki-e/install-action action to v2.75.20](https://github.com/astral-sh/uv/pull/19172) `#19172`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による taiki-e/install-action パッチ更新 (`prek`/`cargo-xwin`/`wasmtime`/`tombi`/`mise` などのバージョン同期)。

**次のアクション:** マージ。

---

### ![PR](icons/git-pull-request.svg) [Report multiple exclude-newer package changes](https://github.com/astral-sh/uv/pull/19210) `#19210`
**ステータス:** Closed | **ラベル:** なし

**概要:** `uv lock --exclude-newer-package` 複数指定時のロック再解決メッセージで全変更を列挙するよう修正。Issue #19207 を解決。

**変更ファイル:** `crates/uv-resolver/src/exclude_newer.rs`、`crates/uv/tests/it/lock.rs`。

**次のアクション:** クローズされた (別途マージあり?)。

---

### ![PR](icons/git-pull-request.svg) [fix: ensure temporary caches remain until process completion](https://github.com/astral-sh/uv/pull/15977) `#15977`
**ステータス:** Closed | **ラベル:** なし

**概要:** `uv tool run` の一時キャッシュがプロセス終了前に消える問題の修正案 (#15967)。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [refactor `ExcludeNewer` to avoid accidentally exposing "local date" input](https://github.com/astral-sh/uv/issues/6820) `#6820`
**ステータス:** Open | **ラベル:** internal

**概要:** `ExcludeNewer` が `Deserialize` と `FromStr` で異なる実装を持ち、`[tool.uv]` セクションで「ローカル日付」が許容されないようにしている設計を改善する内部 issue。

**直近のアクティビティ:** Viicos が「曖昧な日時を禁止する方向に賛成」と表明し、`exclude-newer-package` で `pytest = "2026-06-01"` のようなローカル日時がロックファイルで問題を引き起こした事例を共有。

**次のアクション:** API 強制方針の合意。

---

### ![Issue](icons/issue-opened.svg) [uv venv creates the same python.exe and pythonw.exe](https://github.com/astral-sh/uv/issues/19226) `#19226`
**ステータス:** Open | **ラベル:** bug

**概要:** Windows + uv 0.11.8 で `uv venv` を 3.14.4 で作ると `python.exe` と `pythonw.exe` が同一バイナリ (同 SHA256) になり、`.pyw` 実行時にコンソールが開いてしまう報告。

**直近のアクティビティ:** nooscraft が原因 (両 launcher が non-windowed トランポリンを通っている) を解説。PR #19235 で修正中。

**次のアクション:** PR マージ。

---

### ![Issue](icons/issue-opened.svg) [uv --locked looks in the wrong place for lockfile when symlinked](https://github.com/astral-sh/uv/issues/19222) `#19222`
**ステータス:** Open | **ラベル:** bug

**概要:** スクリプトをシンボリックリンク経由で実行すると、`uv run --locked --script` がリンク名に対応するロックファイルを探してしまい元のスクリプトの `.lock` を見つけられない。

**直近のアクティビティ:** solace-rcampbell と zsol が「リンク先解決すべきか、書き込み権限がない場合どうするか」を議論。回避策はロックファイルにもシンボリックリンクを張ること。

**次のアクション:** 解決方針の合意。

---

### ![Issue](icons/issue-opened.svg) [Failure to download packages from Azure Artifacts starting version 0.11.8](https://github.com/astral-sh/uv/issues/19200) `#19200`
**ステータス:** Open | **ラベル:** bug, needs-mre

**概要:** uv 0.11.8 から Azure Artifacts のプライベートインデックスへの認証が 401 で失敗するリグレッション。0.11.7 では動作していた。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** MRE 提出待ち。

---

### ![Issue](icons/issue-opened.svg) [Downgrade torch when install sam2](https://github.com/astral-sh/uv/issues/19223) `#19223`
**ステータス:** Open | **ラベル:** question, resolver

**概要:** `torch==2.11.0+cu130` がインストール済み環境で `uv pip install git+sam2` すると `setuptools<82` 制約により torch がダウングレードされてしまうレゾルバの挙動。

**直近のアクティビティ:** woodruffw が「主観的な最適解はないので、setuptools 範囲が torch に影響する場合は別途指定するか、解決順序の制約を加えると良い」と回答。

**次のアクション:** ユーザー側でのワークアラウンド。

---

### ![Issue](icons/issue-opened.svg) [Is there a way to add local packages from `requirements.txt` without any pyproject.toml file present?](https://github.com/astral-sh/uv/issues/19186) `#19186`
**ステータス:** Open | **ラベル:** question

**概要:** Docker ビルドで `requirements.txt` だけからローカルパッケージ依存を解決したいが `uv add -r` がローカル依存の不在を訴える質問。

**直近のアクティビティ:** NicholasPini が「workspace 化できない (依存が互換でないため)」と背景を説明。

**次のアクション:** 設計上の対応有無の議論。

---

### ![Issue](icons/issue-opened.svg) [keyring support not working on MacOS](https://github.com/astral-sh/uv/issues/12591) `#12591`
**ステータス:** Open | **ラベル:** bug, external

**概要:** macOS の keyring 認証がうまく動かない長期 issue。

**直近のアクティビティ:** drycode が代替記事への謝意とフォローアップを提供。

**次のアクション:** keyring 上流の解決待ち。

---

### ![Issue](icons/issue-opened.svg) [Secure authentication method needed](https://github.com/astral-sh/uv/issues/8810) `#8810`
**ステータス:** Open | **ラベル:** question

**概要:** プライベートインデックスでの安全な認証手段が乏しいという質問。`pyproject.toml` での credential 共有を避けたい。

**直近のアクティビティ:** sbor23 が `[[index]]` テーブルとユーザー名を含めた URL でうまくいくと共有 (#18031 参照)。

**次のアクション:** ドキュメント整備。

---

### ![Issue](icons/issue-opened.svg) [Upgrade dependencies in `pyproject.toml` (`uv upgrade`)](https://github.com/astral-sh/uv/issues/6794) `#6794`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv upgrade` (deno や npm-check-updates 相当) で `pyproject.toml` のバージョン制約を一括更新したい要望。

**直近のアクティビティ:** vdboor は `uvx uppd && uv sync --dev --upgrade` を回避策として共有。hoechenberger は「uppd は最終リリースが 2024 年 11 月で実質メンテされていないのでパス」と返信。

**次のアクション:** uv 公式で機能化するかの判断。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate の Dependency Dashboard。abandoned / detected dependencies のリスト管理。

**次のアクション:** 自動更新。

---

### ![Issue](icons/issue-opened.svg) [fix regression in error message for incompatible dependencies in some cases](https://github.com/astral-sh/uv/issues/7009) `#7009`
**ステータス:** Open | **ラベル:** bug, error messages

**概要:** PR #6268 以降、互換性のない依存関係のエラーメッセージで根本的な不一致 (datasets<2.19 と datasets>=2.19) ではなくマーカー条件の方を報告するようになり情報が薄くなったリグレッション。

**直近のアクティビティ:** sharifhsn が「PR #15200 で解決済み、bisect で確認」とコメント。

**次のアクション:** クローズ予定。

---

### ![Issue](icons/issue-opened.svg) [Global Python pin should be ignored when incompatible with project](https://github.com/astral-sh/uv/issues/14916) `#14916`
**ステータス:** Open | **ラベル:** bug

**概要:** グローバル `python pin` がプロジェクトの `requires-python` と非互換な時にエラーになる挙動を、無視に変更すべきでは、という提案。

**直近のアクティビティ:** sharifhsn が「PR #15473 で解決済み」と報告。

**次のアクション:** クローズ予定。

---

### ![Issue](icons/issue-opened.svg) [`--project <path>` does not fail if `<path>` does not exist](https://github.com/astral-sh/uv/issues/17087) `#17087`
**ステータス:** Open | **ラベル:** bug, breaking

**概要:** `uv run --project /tmp/does-not-exist python` が黙って動作してしまう。

**直近のアクティビティ:** sharifhsn が「PR #17714 で警告版がマージ、エラー版は preview 中」と報告。

**次のアクション:** クローズ予定。

---

### ![Issue](icons/issue-opened.svg) [[minor improvement] show Python interpreter version in `uv tool list`](https://github.com/astral-sh/uv/issues/10427) `#10427`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv tool list` で各 tool のインストール時 Python バージョンが見えると嬉しい、という改善要望。

**直近のアクティビティ:** sharifhsn が「PR #15814 で解決」と報告。

**次のアクション:** クローズ予定。

---

### ![Issue](icons/issue-opened.svg) [Support for --no-sources-package when resolving](https://github.com/astral-sh/uv/issues/14694) `#14694`
**ステータス:** Closed | **ラベル:** question

**概要:** `--upgrade-package` のように `--no-sources-package` を package 単位で指定できる API の要望。

**直近のアクティビティ:** sharifhsn が「PR #14910 で解決」と報告。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`uv lock --exclude-newer-package` only reports the first changed package in the lockfile-invalidation message](https://github.com/astral-sh/uv/issues/19207) `#19207`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** `--exclude-newer-package` 複数指定時、ロックファイル無効化メッセージで最初の 1 件しか表示されない問題。

**直近のアクティビティ:** PR #19210 で対応 (Closed)。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [How to set proxy for `uv self update`](https://github.com/astral-sh/uv/issues/15970) `#15970`
**ステータス:** Closed | **ラベル:** question

**概要:** GitHub に直接アクセスできない環境で `uv self update` のプロキシ設定方法。

**直近のアクティビティ:** PR #19206 で対応 (`UV_ASTRAL_MIRROR_URL`) し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Is there a way to resolve the version compatibility issue with setuptools using `uvx tensorboard`?](https://github.com/astral-sh/uv/issues/19221) `#19221`
**ステータス:** Closed | **ラベル:** question

**概要:** `uvx tensorboard` で `pkg_resources` がなく `ModuleNotFoundError` になる問題。

**直近のアクティビティ:** Aditya-PS-05 が「tensorboard が `pkg_resources` を使うのに setuptools を依存宣言していなかったのが原因。fix は tensorboard 7057 で修正済みだが PyPI 未リリース、回避策は古い setuptools をピン」と説明。shomitarai が動作確認。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [can't install python in wine](https://github.com/astral-sh/uv/issues/19187) `#19187`
**ステータス:** Closed | **ラベル:** bug

**概要:** Wine 環境で `error: Failed to create Python minor version link directory` (`os error 50`) によりインストールできない問題。

**直近のアクティビティ:** PR #19213 と #19214 で対応し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [UV `11.0` flash-attn incompatibility](https://github.com/astral-sh/uv/issues/18690) `#18690`
**ステータス:** Closed | **ラベル:** bug

**概要:** uv 0.11.0 で `extra-build-dependencies` の `match-runtime = true` を flash-attn に適用できなくなった問題。

**直近のアクティビティ:** zsol が「0.11.1 で解決済み。残る大文字小文字問題は #18667 で追跡」と報告。

**次のアクション:** クローズ済み。

---

## microsoft/vscode

> Insiders 1.119 のリリースサイクルで、エージェント (Claude/Copilot CLI/Agents app) 周りの基盤拡張が大量、加えて Insiders で発生したターミナル起動不能のリグレッションを Keychain 共有機能ごと revert する緊急対応が入った。Markdown プレビュー、webview の localResourceRoots、SCM count badge など既存機能のバグ修正も多数。

### ![PR](icons/git-pull-request.svg) [agentHost/claude: add CAPI-backed local Anthropic proxy service](https://github.com/microsoft/vscode/pull/313677) `#313677`
**ステータス:** Merged | **ラベル:** なし

**概要:** Anthropic Messages API を喋るローカル HTTP プロキシ `ClaudeProxyService` を追加。SDK の Claude Agent サブプロセスから `ANTHROPIC_BASE_URL` 経由で受け、CAPI (`ICopilotApiService`) に転送する。`/v1/models`、`/v1/messages` (非ストリーミング/SSE)、`/v1/messages/count_tokens` を実装。モデル ID は SDK ↔ CAPI で双方向変換。Phase 4 の `ClaudeAgent` 実装の足場で、まだ呼び出し元はない。

**変更ファイル:** `agentHost/node/claude/` 配下に `claudeProxyService.ts`、`claudeModelId.ts`、`claudeProxyAuth.ts`、`anthropicErrors.ts`、`anthropicBetas.ts`、フェーズプランなど 15 ファイル。+4573/-17 行。

**次のアクション:** マージ済み。Phase 4 (`ClaudeAgent` 統合) 待ち。

---

### ![PR](icons/git-pull-request.svg) [Copilot CLI with Agent Host should respect user setting when running shell commands](https://github.com/microsoft/vscode/pull/313679) `#313679`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agent Host のターミナルでも `terminal.integrated.defaultProfile.*` などのユーザー設定を尊重し、`agentHost.defaultShell` 相当の専用設定があればそちらを優先。Issue #313160 を解決。

**変更ファイル:** `agentHost/common/agentHostCustomizationConfig.ts`、`agentHostTerminalManager.ts`、`copilot/copilotShellTools.ts`、テストなど 12 ファイル。+279/-31 行。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [fix(byok): populate Context Window usage for local models (ollama, cu…)](https://github.com/microsoft/vscode/pull/313667) `#313667`
**ステータス:** Open | **ラベル:** なし

**概要:** ローカルモデルプロバイダ (Ollama、LM Studio、OpenAI 互換プロキシ) で Context Window 使用量ウィジェットが常に 0 / max になる問題を、`isApiUsage()` の `total_tokens` 必須要件を緩めて解決。

**変更ファイル:** `extensions/copilot/src/platform/networking/common/openai.ts`、`networking/node/stream.ts` の 2 ファイル。+7/-2 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Revert "Share secrets between Code and Agents app via macOS Keychain"](https://github.com/microsoft/vscode/pull/313635) `#313635`
**ステータス:** Merged | **ラベル:** なし

**概要:** Insiders で provisioned アプリ化したことで非 MDM 端末の更新が壊れ、副作用としてターミナルプロセスがスポーンできなくなる重大問題が出たため、Keychain 共有機能を revert。Issue #313580 と #313570 を解決。

**変更ファイル:** `app-entitlements.plist`、`product-build-darwin-compile.yml`、`build/darwin/sign.ts`、`product.json`、シークレット関連 18 ファイル。+268/-485 行。

**次のアクション:** マージ済み。Keychain 共有は別アプローチで再設計。

---

### ![PR](icons/git-pull-request.svg) [webview: respect default localResourceRoots for custom editors](https://github.com/microsoft/vscode/pull/312492) `#312492`
**ステータス:** Merged | **ラベル:** なし

**概要:** カスタムエディタの webview で `localResourceRoots` を未指定にした場合のデフォルト (workspace folders + 拡張インストールディレクトリ) を尊重するよう拡張ホスト側で補完。これまでは 401 になっていた。Issue #282495 を解決。

**変更ファイル:** `extHostCustomEditors.ts`、`extHostWebview.ts`、テスト 3 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Make sure webviews don't intercept modal dragging](https://github.com/microsoft/vscode/pull/313658) `#313658`
**ステータス:** Merged | **ラベル:** なし

**概要:** モーダル (例えば Agents app の Markdown プレビュー) のドラッグ中、マウスが webview 上に乗るとドラッグが止まる問題を修正。Issue #313657 を解決。

**変更ファイル:** `modalEditorPart.css`、`modalEditorPart.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: skip setYieldRequested for terminal-originated steering messages](https://github.com/microsoft/vscode/pull/313581) `#313581`
**ステータス:** Closed | **ラベル:** なし

**概要:** ターミナル監視によるステアリングメッセージで `setYieldRequested(true)` を無条件に呼ぶと現ターンが破壊され `run_in_terminal` のツール結果を処理できないバグの解消案。

**直近のアクティビティ:** 複数の eval-assessment ベンチマーク結果 (terminalbench2 89 件中、claude-opus 60 通過/29 失敗、gpt-5.4 57 通過/32 失敗) 後、meganrogge が「アプローチが機能しない、ステアリングは実際に処理されるべき」として Close。別案で再設計予定。

**変更ファイル:** `chat/common/chatService/chatServiceImpl.ts`、テスト 2 ファイル。

**次のアクション:** クローズ。別アプローチ模索中。

---

### ![PR](icons/git-pull-request.svg) [Reuse VS Code's update widget/tooltip in the Agents app](https://github.com/microsoft/vscode/pull/313450) `#313450`
**ステータス:** Open | **ラベル:** install-update, agents-app

**概要:** VS Code 本体のアップデートウィジェット/ツールチップを Agents アプリでも再利用。

**直近のアクティビティ:** dmitrivMS が無関係な失敗で再ビルドキック。

**変更ファイル:** `accountMenu/browser/`、`updateHoverWidget.ts`、`updateTitleBarEntry.ts` など 6 ファイル。+53/-608 行 (大幅削減)。

**次のアクション:** ビルド成功とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Disable issue reporter submit button while the issue is being submitted](https://github.com/microsoft/vscode/pull/313678) `#313678`
**ステータス:** Open | **ラベル:** なし

**概要:** Issue Reporter のサブミットボタンを送信中無効化し、二重投稿を抑止。Issue #313661 を解決。

**変更ファイル:** `baseIssueReporterService.ts`、`issueReporter.css`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [github: resolve SSH host aliases when classifying remotes](https://github.com/microsoft/vscode/pull/312494) `#312494`
**ステータス:** Open | **ラベル:** なし

**概要:** `~/.ssh/config` のホストエイリアス (`git@gh-personal:owner/repo.git` 等) を解決して `github.com` を判定する。これまでは GitHub 機能 (Open on GitHub、Copilot indexed バッジ等) が静かに無効化されていた。Issue #283245 を解決。

**変更ファイル:** `extensions/github/src/sshConfig.ts` 新設、`util.ts`、`extension.ts`、テスト 4 ファイル。+274/-2 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add Cache Explorer view to chat debug panel](https://github.com/microsoft/vscode/pull/313620) `#313620`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent デバッグパネルに「Cache Explorer」を追加し、現リクエストと前回リクエストの差分を見ながらプロンプトキャッシュミス原因を診断可能に。

**変更ファイル:** `chatDebugFileLoggerService.ts`、`chatMLFetcher.ts`、`otelSpanToChatDebugEvent.ts`、`mainThreadChatDebug.ts` など 20 ファイル。+2465/-17 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [chat: refactor input model handling to include session resource for state syncing](https://github.com/microsoft/vscode/pull/313644) `#313644`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents で選択したモデルが新セッションごとに Auto に戻る (#313642) 問題を、入力モデルハンドリングをセッションリソース込みでリファクタすることで解決。

**変更ファイル:** `copilotChatSessionsProvider.ts`、`chatWidget.ts`、`chatInputPart.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix Copilot CLI /remote command](https://github.com/microsoft/vscode/pull/313377) `#313377`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot CLI の `/remote` 引数正規化と、ローカル `/remote` 出力のセッションリフレッシュ抑制を整備。

**変更ファイル:** `copilotcliSession.ts`、`nodePtyShim.ts`、`copilotCLIChatSessions.ts`、テスト 8 ファイル。+868/-69 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: Chronicle agent_name backfill, summary JSON filtering, schema description](https://github.com/microsoft/vscode/pull/313630) `#313630`
**ステータス:** Merged | **ラベル:** なし

**概要:** Chronicle ローカル schema のドリフト修正 (#312292)。`agent_name` がスパン属性 → スパン名 → unknown とフォールバックするよう拡張、`summary` の JSON コンテンツを human-readable に整形、`_bufferSessionUpsert` のマージリストに `agent_name` を含める。

**変更ファイル:** `sessionStoreTracking.ts`、`sessionReindexer.ts`、`sessionStoreTracker.ts`、`chronicleIntent.ts` 等 5 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix divide-by-zero panic in CLI unzip for small archives](https://github.com/microsoft/vscode/pull/313389) `#313389`
**ステータス:** Merged | **ラベル:** なし

**概要:** ダウンロードした更新アーカイブが 20 件未満の時 `archive.len() / 20 == 0` で `i % 0` panic していたのを、最低 1 にクランプして修正。`code-insiders update` で再現。Issue #313390 を解決。

**変更ファイル:** `cli/src/util/zipper.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Execution Subagent: Use main agent endpoint for Powershell](https://github.com/microsoft/vscode/pull/313521) `#313521`
**ステータス:** Merged | **ラベル:** なし

**概要:** Execution subagent モデルが PowerShell 用に学習されていないため、ターミナル種別を見て PowerShell ならメインエージェントエンドポイントにフォールバック。

**変更ファイル:** `executionSubagentToolCallingLoop.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Eliminate untitled URIs in agenthost](https://github.com/microsoft/vscode/pull/313622) `#313622`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agent Host のセッション URI 生成をクライアント側に集約し、untitled スキームを廃止。Copilot 共著。

**変更ファイル:** `remoteAgentHostProtocolClient.ts`、`baseAgentHostSessionsProvider.ts`、`agentHostSessionHandler.ts` など 12 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Skip registering a few more commands in agents](https://github.com/microsoft/vscode/pull/313649) `#313649`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents モードで不要なコマンドの登録をスキップ。

**変更ファイル:** `layoutActions.ts`、`workspaceActions.ts`、`fileActions.contribution.ts`、`fileActions.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Increase buffer size for hygiene check](https://github.com/microsoft/vscode/pull/313637) `#313637`
**ステータス:** Merged | **ラベル:** なし

**概要:** precommit hygiene チェックがバッファサイズで失敗するのを防ぐ 1 行修正。

**変更ファイル:** `build/hygiene.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Rename search_subagent to explore_subagent if explore is disabled](https://github.com/microsoft/vscode/pull/312175) `#312175`
**ステータス:** Merged | **ラベル:** なし

**概要:** 既存の `exploreAgent` が無効の場合、`search_subagent` を `explore_subagent` にリネームして Anthropic 系モデル (`explore` ツールに親和) で活用させる。

**変更ファイル:** `agentIntent.ts`、`anthropicPrompts.tsx`、`defaultAgentInstructions.tsx`、`geminiPrompts.tsx`、`gpt51CodexPrompt.tsx` など 19 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: support multiline single-dollar math highlighting in markdown](https://github.com/microsoft/vscode/pull/304800) `#304800`
**ステータス:** Open | **ラベル:** なし

**概要:** Markdown の `$...$` で複数行にまたがる数式ハイライトに対応するため、TextMate grammar の `math_inline_block` を `\${1,2}` 開始 + 後方参照 `\1` 終端に変更。Issue #223545 を解決。

**直近のアクティビティ:** RedCMD が「テストが多数壊れている」と指摘。

**変更ファイル:** `md-math-inline.tmLanguage.json`、`md-math.md`。

**次のアクション:** テスト修正。

---

### ![PR](icons/git-pull-request.svg) [fix(chat): respect todo tool frontmatter enablement in subagents](https://github.com/microsoft/vscode/pull/313631) `#313631`
**ステータス:** Open | **ラベル:** なし

**概要:** `runSubagentTool.ts` がエージェントの `tools:` frontmatter を読み取った直後に `manage_todo_list` を強制無効化していた問題を修正し、明示有効化されている時は尊重する。Issue #313520 を解決。

**変更ファイル:** `runSubagentTool.ts`、テスト。+148/-2 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Use proper functions to get chat session types](https://github.com/microsoft/vscode/pull/313634) `#313634`
**ステータス:** Open | **ラベル:** なし

**概要:** スキーム文字列を直接見ていた箇所を専用関数経由に揃える小規模リファクタ。

**変更ファイル:** `mainThreadChatSessions.ts`、`extHostChatSessions.ts`、`chatForkActions.ts` など 14 ファイル。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [see if timeout=0 for sync performs well](https://github.com/microsoft/vscode/pull/313626) `#313626`
**ステータス:** Open | **ラベル:** なし

**概要:** `runInTerminalTool.ts` の同期タイムアウトを 0 にする実験 PR。eval ベンチマークでパフォーマンスを評価。

**変更ファイル:** `runInTerminalTool.ts`。+10/-11 行。

**次のアクション:** ベンチマーク結果確認。

---

### ![PR](icons/git-pull-request.svg) [Disable emmet commands when there is no active editor](https://github.com/microsoft/vscode/pull/313646) `#313646`
**ステータス:** Open | **ラベル:** なし

**概要:** アクティブエディタがない時の Emmet コマンド露出を抑制 (#313645)。

**変更ファイル:** `extensions/emmet/package.json`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [docs: fix typo in README contributing section](https://github.com/microsoft/vscode/pull/313572) `#313572`
**ステータス:** Open | **ラベル:** なし

**概要:** README の "from typos to additional and new content" の冗長な "additional and" を削除。

**直近のアクティビティ:** Jah-yee が重複箇条書きを修正したと報告。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix minor grammatical issues in README](https://github.com/microsoft/vscode/pull/312480) `#312480`
**ステータス:** Merged | **ラベル:** なし

**概要:** README の "not only do we...we also" を "but we also" に、"Cores" を "cores" に修正。

**変更ファイル:** `README.md`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [change readme](https://github.com/microsoft/vscode/pull/313293) `#313293`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** README に 1 行変更を加える PR。

**直近のアクティビティ:** vs-code-engineering[bot] が複数回 eval-assessment ベンチを実行 (terminalbench2 89 件中 51〜59 通過)。CI 配信パイプラインの検証が中心。

**変更ファイル:** `README.md`。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Fix stale onCommandFinished filter to compare by marker identity](https://github.com/microsoft/vscode/pull/313579) `#313579`
**ステータス:** Closed | **ラベル:** なし

**概要:** PR #313414 で導入された exit-130 カスケードフィルタの参照等価比較が、`promoteToFullCommand()` が新オブジェクトを生成するため常に通過してしまうバグの修正案。`commandStartMarker` (xterm IMarker) で比較する形に変更。

**直近のアクティビティ:** eval-assessment ベンチマーク結果でフィルタが効いていないことを確認。Close 済み。

**次のアクション:** クローズ済み。別アプローチへ。

---

### ![PR](icons/git-pull-request.svg) [cli plan widget providing feedback improvements](https://github.com/microsoft/vscode/pull/313604) `#313604`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Plan widget で「edit or provide feedback」をクリックすると編集モードに入り、Agents ではモーダルでフィードバックを入力できる新フローを導入。

**変更ファイル:** `planReviewFeedbackEditorActions.ts`、`planReviewFeedbackEditorContribution.ts`、`chatPlanReviewPart.ts`、`chatInputPart.ts` など 8 ファイル。+751/-304 行。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Get model and multiplier show up for Copilot CLI controller API route](https://github.com/microsoft/vscode/pull/313079) `#313079`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot CLI コントローラ API ルートで model + token info がリロードまで表示されないバグを修正。新設定 `github.copilot.chat.cli.modelDetails.enabled` でゲート。

**変更ファイル:** `extensions/copilot/src/extension/chatSessions/copilotcli/` 配下 14 ファイル。+361/-56 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Evict idle restored agent host sessions](https://github.com/microsoft/vscode/pull/313275) `#313275`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agent Host のリストア済みセッションのうち誰も見ていないものを参照カウントベースで evict する `AgentService` 拡張。Copilot 作。

**変更ファイル:** `agentService.ts`、`agentHostStateManager`、`protocolServerHandler.ts`、`baseAgentHostSessionsProvider.ts` 等 8 ファイル。+450/-82 行。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Enable the toggle markdown preview command for agent diff views too](https://github.com/microsoft/vscode/pull/313655) `#313655`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent diff ビューでも Markdown プレビューのトグルコマンドを有効化。トグル後にサイドバイサイド diff へ自動復帰しない既知の不完全さあり。

**変更ファイル:** `markdown-language-features/package.json`、`editorCommands.ts`。

**次のアクション:** フォローアップ修正。

---

### ![PR](icons/git-pull-request.svg) [Support data URIs in image preview](https://github.com/microsoft/vscode/pull/313660) `#313660`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** media-preview 拡張が `data:` URI を受け付け、メモリ上の画像を image preview で開けるようにする。

**変更ファイル:** `imagePreview/index.ts`、`mediaPreview.ts`。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Bump @anthropic-ai/sdk from 0.82.0 to 0.91.1 in /extensions/copilot/chat-lib](https://github.com/microsoft/vscode/pull/313636) `#313636`
**ステータス:** Open | **ラベル:** javascript, dependencies

**概要:** Copilot chat-lib の `@anthropic-ai/sdk` を 0.82.0 → 0.91.1 へ。CMA Memory パブリックベータなど多数の機能追加。

**変更ファイル:** `chat-lib/package.json`、`package-lock.json`。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Few chronicle updates](https://github.com/microsoft/vscode/pull/312284) `#312284`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Chronicle 関連の小修正をまとめたドラフト。

**変更ファイル:** policyData、Copilot chronicle 関連 29 ファイル。+2130/-72 行。

**次のアクション:** Draft 解除。

---

### ![PR](icons/git-pull-request.svg) [Agents - scaffold Changesets model](https://github.com/microsoft/vscode/pull/313577) `#313577`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents の Changesets モデルを足場として導入。

**変更ファイル:** `baseAgentHostSessionsProvider.ts`、Agent Host 系テスト多数。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fixes #192055 avoids view refresh race condition](https://github.com/microsoft/vscode/pull/250239) `#250239`
**ステータス:** Open | **ラベル:** なし

**概要:** Tree view の "Element with id is already registered" 競合を `extHostTreeViews` でリトライベースで回避。

**直近のアクティビティ:** eamodio が rebase 済みと alexr00 にレビュー依頼。

**変更ファイル:** `extHostTreeViews.ts`、テスト。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) その他 PR (closed/draft 多数)
- [Inline summarization: pass conversationId/turnId for prompt caching](https://github.com/microsoft/vscode/pull/312497) — closed
- [Add `chat.agent.autoAccept` setting](https://github.com/microsoft/vscode/pull/307685) — closed (draft)
- [feat: add Insert Link to Header in File command for markdown](https://github.com/microsoft/vscode/pull/306561) — closed
- [Support validating all markdown files in a workspace](https://github.com/microsoft/vscode/pull/300688) — closed (draft)
- [Fix markdown.styles absolute paths failing to load in preview webview](https://github.com/microsoft/vscode/pull/299552) — closed
- [Fix punctuation in CONTRIBUTING.md](https://github.com/microsoft/vscode/pull/306155) — closed
- [Bring back Copilot CLI node-pty runtime shim](https://github.com/microsoft/vscode/pull/313610) / [Bring back Copilot CLI node-pty runtime shim](https://github.com/microsoft/vscode/pull/313600) — closed
- [Fix Copilot CLI /remote command in release](https://github.com/microsoft/vscode/pull/313375) — closed
- [Add /usage chat slash command for AI usage stats](https://github.com/microsoft/vscode/pull/313295) — closed (DO NOT MERGE)
- [test: trivial README wording tweak to validate eval-assessment flow](https://github.com/microsoft/vscode/pull/312807) — closed (テスト用)
- [fix: resolve NoChangeError tool name interpolation and typo](https://github.com/microsoft/vscode/pull/309709) — open

---

### ![Issue](icons/issue-opened.svg) [activating mcp extensions... skip](https://github.com/microsoft/vscode/issues/313684) `#313684`
**ステータス:** Open | **ラベル:** new release

**概要:** Code 1.118.1 で Copilot Chat が使えないという報告。詳細はシステム情報。

**次のアクション:** トリアージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Updater fails](https://github.com/microsoft/vscode/issues/313683) `#313683`
**ステータス:** Open | **ラベル:** なし

**概要:** Insiders 1.119 で `old_code-tunnel-insiders.exe` のファイルハンドルがアップデート時に閉じられず更新が失敗する Windows の問題。

**次のアクション:** Code-tunnel プロセスのクリーンアップ要対応。

---

### ![Issue](icons/issue-opened.svg) [SCM: Count Badge not working anymore](https://github.com/microsoft/vscode/issues/313682) `#313682`
**ステータス:** Open | **ラベル:** new release

**概要:** Code 1.118.1 にアップグレード後、SCM カウントバッジが全リポジトリ合算ではなくフォーカス中のみ表示するようになったというリグレッション。

**次のアクション:** 担当チームのトリアージ。

---

### ![Issue](icons/issue-opened.svg) [workspaceStorage bucket hash computed from raw path string — OneDrive Folder Backup (KFM) creates permanent duplicate storage buckets on macOS](https://github.com/microsoft/vscode/issues/313681) `#313681`
**ステータス:** Open | **ラベル:** なし

**概要:** macOS で OneDrive の Known Folder Move によりシンボリックリンクが入った状態で同一物理フォルダを開くと `workspaceStorage` のバケット ID が分かれてしまう問題。エンタープライズ MDM デプロイで頻発。

**次のアクション:** バケット ID をパス解決後の inode 等で算出する方針要検討。

---

### ![Issue](icons/issue-opened.svg) [VSCodeの不具合　VS Code bug](https://github.com/microsoft/vscode/issues/313481) `#313481`
**ステータス:** Open | **ラベル:** なし

**概要:** バージョン 1.118 でカラーコード色プレビューが途中から表示されなくなり、メニューバーが英語になる Windows 10 Pro の不具合報告。

**直近のアクティビティ:** RedCMD がスクリーンショット要求、penguinlab がメニュー上位だけ英語化されると共有。表示言語切替やパック再インストールでは解消せず。

**次のアクション:** 再現条件の特定。

---

### ![Issue](icons/issue-opened.svg) [Issue reporter makes it too easy to double submit issues](https://github.com/microsoft/vscode/issues/313661) `#313661`
**ステータス:** Open | **ラベル:** bug

**概要:** Issue Reporter UI で送信ボタンの連打により重複登録が起こる問題。

**次のアクション:** PR #313678 で対応中。

---

### ![Issue](icons/issue-opened.svg) [Cannot select or copy text from Elicitation dialog output](https://github.com/microsoft/vscode/issues/307977) `#307977`
**ステータス:** Open | **ラベル:** bug, polish, good first issue

**概要:** Elicitation ダイアログの出力でテキスト選択・コピーができなくなった。`user-select` あたりが効いていない疑い。

**直近のアクティビティ:** rio767 が Win11 + Insiders で再現確認。

**次のアクション:** good first issue として対応募集。

---

### ![Issue](icons/issue-opened.svg) [[FOCUS] VS Code is turning into a greasy web dashboard, not an editor](https://github.com/microsoft/vscode/issues/313650) `#313650`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot 強制押し出し、半透明 UI、ブロッキング・モーダルなど近年の方針への苦言。

**次のアクション:** トリアージ判断。

---

### ![Issue](icons/issue-opened.svg) [Modal dragging stops if mouse moves over a webview](https://github.com/microsoft/vscode/issues/313657) `#313657`
**ステータス:** Closed | **ラベル:** bug, unreleased

**概要:** モーダルドラッグ中にマウスが webview 上に乗ると停止するバグ。

**直近のアクティビティ:** PR #313658 で解決し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Cannot open terminal by pressing ctrl+shift+` or by clicking on new terminal on "Terminal" manu](https://github.com/microsoft/vscode/issues/313580) `#313580`
**ステータス:** Closed | **ラベル:** unreleased

**概要:** Insiders 1.119.0 (macOS) でターミナルが起動できないリグレッション。

**直近のアクティビティ:** ksylvan が ptyhost.log/terminal.log の解析で zsh 起動直後に PTY fd が EBADF になることを確認。fishcharlie が「duplicate of #313570」と指摘。

**次のアクション:** PR #313635 (Keychain 機能 revert) で解消済み。

---

### ![Issue](icons/issue-opened.svg) [Terminal window cannot be opened](https://github.com/microsoft/vscode/issues/313570) `#313570`
**ステータス:** Closed | **ラベル:** bug, info-needed, important, terminal, unreleased

**概要:** macOS Insiders でターミナル起動不能の本筋 issue。多くの再現報告。

**直近のアクティビティ:** anthonykim1 が「ようやく再現を得てビルドを凍結した、各位アップデートを」と報告。deepak1556 が「`/Applications` 直下か AppTranslocation か」を問い、PID 67974 のプロセス状態を追跡。

**次のアクション:** PR #313635 でクローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Model reverts to Auto in Agents](https://github.com/microsoft/vscode/issues/313642) `#313642`
**ステータス:** Closed | **ラベル:** bug, unreleased, agents-app, copilot-chat

**概要:** Agents パネルでモデル選択が Auto に戻る。drewnoakes が「reasoning level 不一致のエラーで時間が無駄になる」と頻発を訴える。

**直近のアクティビティ:** PR #313644 で解決し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[Agents] Model selection resets to "Auto" on every new session](https://github.com/microsoft/vscode/issues/313627) `#313627`
**ステータス:** Closed | **ラベル:** なし

**概要:** 上記 #313642 と重複。markbrockettrobson が先に立てた issue で drewnoakes が dupe としてクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Custom editor webview's localResourceRoots contract is not respected](https://github.com/microsoft/vscode/issues/282495) `#282495`
**ステータス:** Closed | **ラベル:** bug, custom-editors, unreleased, team-low-hanging

**概要:** カスタムエディタの webview で `localResourceRoots` を未指定のときデフォルトが効かず 401 になる長期 issue。

**直近のアクティビティ:** PR #312492 で解決し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Missing tag for v1.118.1](https://github.com/microsoft/vscode/issues/313499) `#313499`
**ステータス:** Closed | **ラベル:** なし

**概要:** v1.118.1 のビルドはアップデートページにあるが GitHub にタグが切られておらずダウンストリームのパッケージングに支障があるという指摘。

**直近のアクティビティ:** dmitrivMS がリリースを作成、再発防止のためリリース手順を更新。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [All agent operations encounter "Permission denied and could not request permission from user" after update](https://github.com/microsoft/vscode/issues/313165) `#313165`
**ステータス:** Closed | **ラベル:** なし

**概要:** Insiders 1.119 で agent ツール群が全て denied になる報告。

**直近のアクティビティ:** drewnoakes が一過性の問題と判断し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Chronicle: populate agent_name on sessions from invoke_agent spans](https://github.com/microsoft/vscode/issues/312296) `#312296`
**ステータス:** Closed | **ラベル:** unreleased, copilot

**概要:** Chronicle ローカルストアの `sessions.agent_name` が常に空になる問題。`invoke_agent` スパンには `gen_ai.agent.name` が乗っていないのが原因。

**直近のアクティビティ:** PR #313630 で解決し Close。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Update failed deleting folder](https://github.com/microsoft/vscode/issues/312098) `#312098`
**ステータス:** Open | **ラベル:** bug, install-update, windows

**概要:** Windows で更新時に `10c8e557c8` ディレクトリの削除が拒否されるエラー。

**直近のアクティビティ:** dtlhlbs が「毎週の更新で発生する」と再現報告。

**次のアクション:** インストーラ側のクリーンアップ強化要検討。

---

### ![Issue](icons/issue-opened.svg) [Claude Opus 4.7 fails with "effort high not supported" error in Copilot Agent](https://github.com/microsoft/vscode/issues/313652) / [#313653](https://github.com/microsoft/vscode/issues/313653) / [#313654](https://github.com/microsoft/vscode/issues/313654)
**ステータス:** Open | **ラベル:** ai-translated

**概要:** Claude Opus 4.7 を effort: high で使うと `output_config.effort "high" is not supported by model claude-opus-4.7; supported values: [medium]` で 400 になる、という報告が 3 件 (重複)。

**次のアクション:** UI 側で reasoning レベルをフィルタするか、サポートする上流対応。

---

### ![Issue](icons/issue-opened.svg) [Agents app keeps signing out when launching EDH for my VS Code extension](https://github.com/microsoft/vscode/issues/313651) `#313651`
**ステータス:** Open | **ラベル:** なし

**概要:** Insiders 1.119 で VS Code 拡張デバッグホスト (EDH) を Agents アプリから起動するとサインアウト状態に戻る。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Chat Constantly Frozen](https://github.com/microsoft/vscode/issues/296519) `#296519`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Chat が頻繁にフリーズし、エージェント要求が 15〜17 分以上かかる、Code Search の diff 失敗、最終的に Node ワーカが OOM クラッシュする、という詳細報告。

**次のアクション:** トリアージ・調査。

---

### ![Issue](icons/issue-opened.svg) [Chat Sessions panel: allow user-defined groups or folders](https://github.com/microsoft/vscode/issues/312094) `#312094`
**ステータス:** Open | **ラベル:** triage-needed, stale

**概要:** Chat Sessions のドラッグでユーザー定義グループ/タグを作れるようにする要望。`groupByDate`/`groupByRepository`/`groupByCapped` の延長。

**次のアクション:** Stale。

---

### ![Issue](icons/issue-opened.svg) [Remote connection locks other windows](https://github.com/microsoft/vscode/issues/312303) `#312303`
**ステータス:** Open | **ラベル:** triage-needed, stale

**概要:** 複数 Remote-SSH ウィンドウで再接続ダイアログが互いに排他的にロックする UX バグ。

**次のアクション:** Stale。

---

### ![Issue](icons/issue-opened.svg) [Chat Bug](https://github.com/microsoft/vscode/issues/313666) `#313666`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** テンプレートだけで詳細のない報告。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [won't disable](https://github.com/microsoft/vscode/issues/313680) `#313680`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** 不要な拡張を無効化できないという報告 (Code 1.118.1 macOS)。

**直近のアクティビティ:** vs-code-engineering[bot] が情報不足を案内。

**次のアクション:** 情報待ち。

---

### ![Issue](icons/issue-opened.svg) [update error](https://github.com/microsoft/vscode/issues/311152) `#311152`
**ステータス:** Open | **ラベル:** info-needed, triage-needed

**概要:** 更新時にエラーが出る Windows の報告。

**直近のアクティビティ:** shlomoa が同症状と report。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Python Code run error and poweshell and python issue](https://github.com/microsoft/vscode/issues/313659) `#313659`
**ステータス:** Open | **ラベル:** new release

**概要:** Ctrl+F5 で Python が動かない、PowerShell に Python Debugger / Python のメッセージが出るユーザー報告。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Floating explorer menu](https://github.com/microsoft/vscode/issues/206152) `#206152`
**ステータス:** Closed | **ラベル:** feature-request, file-explorer

**概要:** エクスプローラメニューをフローティング化したい要望。

**直近のアクティビティ:** ragavan-r-r が再オープン要望。

**次のアクション:** クローズのまま。

---

### ![Issue](icons/issue-opened.svg) ['Add Models...' button in GitHub Copilot Chat is disabled ("greyed out")](https://github.com/microsoft/vscode/issues/308135) `#308135`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Chat の Manage Language Models で Add Models が無効化されている。

**直近のアクティビティ:** turbolego が「最新 stable では使えるが Insiders では使えない」と更新。

**次のアクション:** Insiders ビルド側の対応待ち。

---

### ![Issue](icons/issue-opened.svg) [Respect the user's preferred shell in agent host terminals](https://github.com/microsoft/vscode/issues/313160) `#313160`
**ステータス:** Open | **ラベル:** feature-request, terminal, agent-host

**概要:** Agent Host のターミナルがユーザー設定の preferred shell を尊重しないという要望。`terminal.integrated.defaultProfile.*` の継承や `agentHost.defaultShell` の新設案を提示。

**次のアクション:** PR #313679 で対応中。

---

### ![Issue](icons/issue-opened.svg) Portable mode 系 Issue 群
- [Make Portable mode easier to migrate to](https://github.com/microsoft/vscode/issues/172197) — feature-request
- [Error using "Open with", portable instalation](https://github.com/microsoft/vscode/issues/261656) — bug
- [Issue with "Recent Items" in the taskbar context menu](https://github.com/microsoft/vscode/issues/307789) — feature-request
- [Show More Info In HELP/ABOUT](https://github.com/microsoft/vscode/issues/310902) — feature-request

**概要:** ポータブルモード関連の機能要望・バグの 4 件。

**次のアクション:** バックログ。

---

### ![Issue](icons/issue-opened.svg) スパムテスト issue 群 (16 件)
`#313672`、`#313662`、`#313676`、`#313673`、`#313674`、`#313675`、`#313663`、`#313671`、`#313670`、`#313669`、`#313668`、`#313665`、`#313664`、`#313656` ほか。「test test test」等の自動投稿で、いずれも spam ラベル付与または自動クローズ済み。issue reporter からの誤投稿の連発が目立ち、PR #313678 (送信ボタンの二重投稿抑止) の動機にもなっている。

---

## qmk/qmk_firmware

> 新規キーボード追加 (YMDK CV11、Idle Builds Dinkey 32/30 と 34、Paws60 改訂、s245_streamdeck) と、`config.h` bool 解析・mingw 弱関数のバグ修正、長期 WIP の XAP プロトコル PR の継続。

### ![PR](icons/git-pull-request.svg) [Add YMDK CV11 keyboard](https://github.com/qmk/qmk_firmware/pull/26182) `#26182`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** YMDK CV11 キーボードのファームウェアサポートを新規追加。default と midi の 2 keymap を含む 12 ファイル、+305 行。

**次のアクション:** メンテナーレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add Idle Builds Dinkey 32|30 keyboard](https://github.com/qmk/qmk_firmware/pull/26175) `#26175`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** Idle Builds Dinkey 32|30 (32 キー split、コンボで 30 キーレイアウト、ATmega32U4、split_3x5_2) のサポート追加。

**変更ファイル:** `keyboards/idlebuilds/dinkey32_30/` 配下 5 ファイル、+153 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add Idle Builds Dinkey 34 keyboard](https://github.com/qmk/qmk_firmware/pull/26176) `#26176`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** 同シリーズの 34 キー版 Dinkey 34 を追加。

**変更ファイル:** 4 ファイル、+157 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open (Draft) | **ラベル:** needs doc, in progress, core, keyboard, help wanted, breaking_change ほか

**概要:** QMK ホスト⇔ファームウェア間の汎用プロトコル XAP の実装トラッキング PR (Issue #11567)。

**変更ファイル:** 96 ファイル、+7548/-25 行。

**次のアクション:** 長期 WIP。継続実装。

---

### ![PR](icons/git-pull-request.svg) [Add sector245/s245_streamdeck keyboard](https://github.com/qmk/qmk_firmware/pull/26013) `#26013`
**ステータス:** Merged | **ラベル:** keyboard, keymap, awaiting review

**概要:** S245 Streamdeck (3x3 + ロータリーエンコーダ macropad、RP2040、VIA 対応) の追加。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Added support new revision for Paws60](https://github.com/qmk/qmk_firmware/pull/26081) `#26081`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** horrortroll/paws60 に新リビジョン (rev1/rev2) サポートを追加。

**変更ファイル:** 10 ファイル、+839/-58 行。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix `config.h` bool parsing](https://github.com/qmk/qmk_firmware/pull/26166) `#26166`
**ステータス:** Merged | **ラベル:** bug, python, cli

**概要:** `qmk info -f json -kb …` で `#define NKRO_DEFAULT_ON true` が `nkro: false` と誤って報告されていたバグを、`info.py` の bool 判定リストに `"true"` を追加することで修正。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add workaround for undefined reference to weak function under mingw](https://github.com/qmk/qmk_firmware/pull/26167) `#26167`
**ステータス:** Merged | **ラベル:** bug, core

**概要:** MSYS の mingw で `make clean test:audio` 時に `register_weak_mods` などへの未定義参照が出る問題に対する `platforms/test/platform.mk` でのワークアラウンド。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add Hands Down Gold keymap for ErgoDone keyboard](https://github.com/qmk/qmk_firmware/pull/26181) `#26181`
**ステータス:** Closed | **ラベル:** keymap

**概要:** ErgoDone 用 Hands Down Gold keymap 追加。

**直近のアクティビティ:** zvecr が #22724 へのリンクを共有し、レビュー基準を理由に Close。

**次のアクション:** クローズ済み。
