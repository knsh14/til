# GitHub ダイジェスト — 2026-04-28

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Add support for usage-based billing](https://github.com/microsoft/vscode/pull/312892)** (microsoft/vscode) — Copilot Chat の従量課金 (usage-based billing) を VS Code workbench、Sessions ウィンドウ、組み込み Copilot 拡張に組み込む +1833 / −1088 の大型 PR。価格メタデータ・クォータ UX を導入してマージ済み。
2. ![PR](icons/git-pull-request.svg) **[uv audit: adverse status reporting](https://github.com/astral-sh/uv/pull/19128)** (astral-sh/uv) — `uv audit` 機能に PEP 792 のプロジェクトステータス（quarantined 等）レポート機能を追加する +704 / −28 の PR。`#18506` `#15254` の対応で、レジストリへの追加リクエストとキャッシュ戦略の見直しを伴う。
3. ![PR](icons/git-pull-request.svg) **[feat(ui): implement vim.ui.img api](https://github.com/neovim/neovim/pull/37914)** (neovim/neovim) — kitty graphics protocol を裏で使う同期画像 API `vim.ui.img.set/get/del` をマージ。後続として healthcheck (#39449)、UI プロトコルイベント (#39434)、画像クリア (#39439)、インライン配置 (#39432) の議論が活発化し画像 API 周りが集中改修中。
4. ![Issue](icons/issue-opened.svg) **[Make ARMv7 FP detection infallible](https://github.com/astral-sh/uv/issues/19190)** (astral-sh/uv) — procfs ベースの ARMv7 FP 検出を `getauxval(AT_HWCAP) & HWCAP_VFP` に置き換えて infallible 化する提案。`rustix::param::linux_hwcap` 採用案や arm32-on-aarch64 のユーザー空間 ABI 整合確認まで踏み込んだ議論。関連 PR `#19157` がマージ済み。
5. ![PR](icons/git-pull-request.svg) **[tesla: tune legacy Model 3/Y steer ratio to 10.3](https://github.com/commaai/opendbc/pull/3365)** (commaai/opendbc) — リフレッシュ前 Tesla Model 3/Y の `steer_ratio` を 12.0 から 10.3 に調整しつつ FSD14 識別車種は 12.0 を維持する PR。`opendbc/safety/modes/tesla.h` の安全テスト境界も整合させる。Tesla 20 セグメントで挙動差分発生中。

---

## commaai/openpilot

> modeld 周辺の synthax 整理と panda submodule 廃止後の細かな調整、UI の DM オレンジ遷移修正など軽量 PR が中心の一日。`#37924` のような巨大ドラフトも投入。

### ![PR](icons/git-pull-request.svg) [modeld: remove deprecated tg flags](https://github.com/commaai/openpilot/pull/37903) `#37903`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `selfdrive/modeld/SConscript` から非推奨化された tinygrad 用フラグを削除する +5 / −9 の小修正。

**直近のアクティビティ:** model replay の再生プロットと process replay diff report が両方とも 0 changed / 66 passed / 0 errors を返却。

**変更ファイル:** `selfdrive/modeld/SConscript` のみ。

**次のアクション:** ドラフト解除前の最終レビュー。

---

### ![PR](icons/git-pull-request.svg) [dmonitoringmodeld: misc synthax](https://github.com/commaai/openpilot/pull/37901) `#37901`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `selfdrive/modeld/dmonitoringmodeld.py` の文法整理 +2 / −2。

**直近のアクティビティ:** model replay 添付済み、process replay diff 0 changed / 66 passed / 0 errors。

**変更ファイル:** `selfdrive/modeld/dmonitoringmodeld.py`。

**次のアクション:** ドラフト解除待ち。

---

### ![PR](icons/git-pull-request.svg) [modeld: remove uop tg patch](https://github.com/commaai/openpilot/pull/37849) `#37849`
**ステータス:** Closed (Draft) | **ラベル:** (なし)

**概要:** `compile_modeld.py` と `tinygrad_repo` 参照から uop パッチを除去する小 PR。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** クローズ済み。後続 PR で別アプローチ予定。

---

### ![PR](icons/git-pull-request.svg) [fix: cruise faults should not disable silently](https://github.com/commaai/openpilot/pull/37557) `#37557`
**ステータス:** Open | **ラベル:** (なし)

**概要:** ACC fault 発生時に `USER_DISABLE` (`wrongCarMode`) と `IMMEDIATE_DISABLE` (`accFaulted`) が同時発火すると critical な「TAKE CONTROL IMMEDIATELY」アラートがソフト disengage チャイムで上書きされる不具合を修正。`IMMEDIATE_DISABLE` を先に判定する +4 / −4。`opendbc#2874` で議論。

**直近のアクティビティ:** `jyoung8607` が「ぱっと見 LGTM だが詳細レビュー要」とコメントし stalebot 回避のため rebase。

**変更ファイル:** `selfdrive/selfdrived/state.py` のみ。

**次のアクション:** メンテナの詳細レビュー。

---

### ![Issue](icons/issue-opened.svg) [Useradmin cannot read current logs - screen is blank](https://github.com/commaai/openpilot/issues/37925) `#37925`
**ステータス:** Open | **ラベル:** bug

**概要:** `useradmin.comma.ai/logs` へ nightly-dev ビルドのルート（`1b56e7310030864a/00000027--2b3c259139`）からアクセスすると画面が真っ白になるバグ報告。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メンテナによる再現確認とフロント側修正。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/commaai/openpilot/pull/37923) `#37923`
**ステータス:** Closed | **ラベル:** bot

**概要:** `repo-maintenance` 由来の自動 PR。`uv pip` 配下の Python パッケージを一括更新。`.venv` 1.3GB 相当でも casadi/panda3d/ffmpeg などサイズ上位は維持。+143 / −168。

**直近のアクティビティ:** process replay diff report は 0 changed / 66 passed / 0 errors。

**変更ファイル:** `Jenkinsfile`、`opendbc_repo`、`tinygrad_repo`、`uv.lock`。

**次のアクション:** クローズ済み（bot 周期更新なので新規 PR が再投入される想定）。

---

### ![PR](icons/git-pull-request.svg) [ui: fix DM orange transitions](https://github.com/commaai/openpilot/pull/37922) `#37922`
**ステータス:** Merged | **ラベル:** ui

**概要:** ドライバーモニタリング画面で「unfull 状態で disengage→再 engage」するとオレンジが点滅する不具合 (`#37826` 関連) の修正 +5 / −7。

**直近のアクティビティ:** `ZwX1616` が引用付きで「huh」と短く反応しつつマージ。

**変更ファイル:** `selfdrive/ui/mici/onroad/driver_state.py`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Car qa taylor](https://github.com/commaai/openpilot/pull/37924) `#37924`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Software QA 用フィクスチャの大規模追加・移設 +1514 / −3（13 ファイル）。`.github/workflows/softwareQA.yml`、`release/test_pack.py`、`selfdrive/car/softwareQA/` 配下に car/cruise/card テストを置く実験的構成。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** クローズ済み。後続 PR で再構成予定とみられる。

---

### ![PR](icons/git-pull-request.svg) [Do not build extras when running --minimal SCons](https://github.com/commaai/openpilot/pull/37892) `#37892`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `--minimal` 指定時に `tools/replay`・`tools/cabana` を含む `extras` をビルドしないよう SCons を修正。`#37712`（imgui 準備）以降に `GetOption('extras')` ガードが消えていた regression を是正する +2 / −2。

**直近のアクティビティ:** process replay diff 0 changed / 66 passed / 0 errors でマージ。

**変更ファイル:** `SConstruct` のみ。

**次のアクション:** マージ済み。

---

## commaai/opendbc

> Tesla の steer ratio チューニングと Model 3 fingerprint 追加が中心。週次 uv.lock 更新と過去 issue のクローズも。

### ![PR](icons/git-pull-request.svg) [tesla: tune legacy Model 3/Y steer ratio to 10.3](https://github.com/commaai/opendbc/pull/3365) `#3365`
**ステータス:** Open (Draft) | **ラベル:** car, tesla, car safety

**概要:** リフレッシュ前 Tesla Model 3/Y の `steer_ratio` を 12.0 → 10.3 に調整。FSD14 識別バリアントには現行 12.0 を残し、`opendbc/safety/modes/tesla.h` の安全テスト境界も同期して整合性を保つ。

**直近のアクティビティ:** `dkneeland` が CI の `test models (1)` 失敗は本 PR 無関係（`CAR.RIVIAN_R1` のルートデータ欠落）と指摘。Car behavior report は 20 セグメント全件で挙動変化を検出（TESLA_MODEL_3 / TESLA_MODEL_Y）。

**変更ファイル:** `opendbc/car/tesla/interface.py`、`opendbc/car/tesla/values.py`、`opendbc/safety/tests/test_tesla.py` の +13 / −3。

**次のアクション:** Car behavior 差分の妥当性確認とドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [Tesla: Fingerprint 2019 Model 3 AWD Longrange (2026.8.6)](https://github.com/commaai/opendbc/pull/3323) `#3323`
**ステータス:** Merged | **ラベル:** car, tesla, fingerprint

**概要:** 2019 Tesla Model 3 AWD Longrange のソフトウェア 2026.8.6 fingerprint を `fingerprints.py` に追加（+1 / −0）。検証ルート: `424b2a6c1b69a3bc/00000005--200c5f1bec/140/1190`。

**直近のアクティビティ:** `dzid26` が同じ `TeM3_E014p10_0.0.0 (24),E014.20.2` 環境を共有しつつ、現行 LKAS タイプは 15mph 以上で override 可能だがオーバーシュートと安定性に課題があると追記。

**変更ファイル:** `opendbc/car/tesla/fingerprints.py`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [tesla: match stock ACC envelope convention for DAS_control](https://github.com/commaai/opendbc/pull/3341) `#3341`
**ステータス:** Closed (Draft) | **ラベル:** car, tesla

**概要:** Tesla 純正 ACC エンベロープ規約に合わせて `DAS_control` の符号を統一する +6 / −5 のドラフト。

**直近のアクティビティ:** `sshane` がレビューコメント。

**変更ファイル:** `opendbc/car/tesla/carcontroller.py`、`opendbc/car/tesla/teslacan.py`。

**次のアクション:** クローズ済み。代替アプローチで再投入予定。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3364) `#3364`
**ステータス:** Open | **ラベル:** bot

**概要:** 週次 uv.lock 更新 +141 / −136（1 ファイル）。

**直近のアクティビティ:** Car behavior report は変化なし。

**次のアクション:** マージ予定。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3363) `#3363`
**ステータス:** Closed | **ラベル:** bot

**概要:** 1 つ前の週次 uv.lock 更新 PR。`#3364` に置き換えられクローズ。

**直近のアクティビティ:** Car behavior report は変化なし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Regression on Tesla Model 3 Highland (HW4) After Tesla OTA 2026.2.9.1 — Comma 3X Falls Back to "Car Unrecognized / Dashcam Mode"](https://github.com/commaai/opendbc/issues/3295) `#3295`
**ステータス:** Closed | **ラベル:** bug, car

**概要:** Tesla 純正 OTA `2026.2.9.1` 適用後、これまで動作していた Comma 3X が車両を認識できなくなる regression。openpilot 側の更新は無く Tesla 側の OTA 互換性問題と判明。

**直近のアクティビティ:** `molan` が「master ブランチで動作した、修正に感謝」とコメントしクローズ。

**次のアクション:** クローズ済み。

---

## commaai/panda

> 直近24時間の更新はありません。

---

## neovim/neovim

> `vim.ui.img` 画像 API 周辺の追加機能（healthcheck, UI イベント, インライン配置, クリア API）と `vim.pos`/`vim.range` のリファクタが集中。`vim-patch` シリーズや LSP/treesitter の細かな改善も多数進行中。

### ![PR](icons/git-pull-request.svg) [feat(ui): implement vim.ui.img api](https://github.com/neovim/neovim/pull/37914) `#37914`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** kitty graphics protocol を裏で使う同期画像 API。`vim.ui.img.set(id|data, opts) -> id`、`vim.ui.img.get(id) -> opts|nil`、`vim.ui.img.del(id) -> bool` の 3 関数を実装。`#31399` の整理版で `#30889` を解決。+685 / −1。

**直近のアクティビティ:** `justinmk` のレビューコメント複数を反映してマージ。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/doc/news.txt`、`runtime/lua/vim/ui.lua`、`runtime/lua/vim/ui/img.lua`、`runtime/lua/vim/ui/img/_kitty.lua`、`runtime/lua/vim/ui/img/health.lua`。

**次のアクション:** マージ済み。後続 issue 群で healthcheck・UI イベント・インライン配置等を整備中。

---

### ![PR](icons/git-pull-request.svg) [feat(health): add vim.ui.img healthcheck for detecting graphics support](https://github.com/neovim/neovim/pull/39449) `#39449`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.ui.img.supported()` を追加し kitty APC で graphics protocol サポートを検出。health check を該 API ベースに更新し、tmux チェックも単純な warning に簡略化。`#39431` を解決。

**直近のアクティビティ:** `chipsenkbeil` と `gpanders` が macOS Terminal で APC コードが画面上部に残る件を議論。`vim.cmd.mode()` を入れて副作用を吸収する形に落ち着き、`_supported()` をプライベート扱いに。

**変更ファイル:** `runtime/lua/vim/ui/img.lua`、`runtime/lua/vim/ui/img/_kitty.lua`、`runtime/lua/vim/ui/img/health.lua` ほか +94 / −39。

**次のアクション:** レビュー反映後マージ予定。

---

### ![Issue](icons/issue-opened.svg) [image API: UI protocol events](https://github.com/neovim/neovim/issues/39434) `#39434`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.ui.img` が termcode を直接送出するため Neovide 等の GUI で動かない問題。UI イベントとして画像表示を formal 化する必要あり。巨大画像のストリーミング送信も検討対象。

**直近のアクティビティ:** `justinmk` が「ファイル名渡しはリモートケース不可・provider 固有・主経路の妨げ」と反対。`y9san9` がディスク経由案を提示するも「初手はイベント内データ、しきい値超でストリーミング検討」が暫定方針。

**次のアクション:** UI イベントスキーマ設計案を引き続き募集。

---

### ![Issue](icons/issue-opened.svg) [image API: user can clear all images](https://github.com/neovim/neovim/issues/39439) `#39439`
**ステータス:** Open | **ラベル:** (なし)

**概要:** バグった plugin が画像を残した場合のフォールバック手段が必要。`vim.ui.img.del(nil)` で一括削除、namespace/extmark を活用したイテレーション可能化を検討。

**直近のアクティビティ:** `chipsenkbeil` がプロトタイプ PR を準備中。`drowning-cat` が `vim.ui.img.clear({})` 命名と `nil` 引数の暗黙削除を懸念。`justinmk` は「初手は nuke-it-all で十分」と方向性を示唆。

**次のアクション:** API 命名と引数設計を確定後 PR 化。

---

### ![Issue](icons/issue-opened.svg) [image API: inline positioning / placement](https://github.com/neovim/neovim/issues/39432) `#39432`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.ui.img` は absolute 配置のみで、テキスト行間への inline 配置ができない。extmark の機能拡張または region パラメータ追加で対応する案。

**直近のアクティビティ:** `chipsenkbeil` が「絶対配置をフローティングウィンドウ＋Unicode に切り替えるブランチ」を試作中と共有。`justinmk` は「現行 master では既に absolute 配置可能、本 issue は buffer 内配置の話」と整理。

**次のアクション:** extmark API の region 拡張案を検討。

---

### ![Issue](icons/issue-opened.svg) [image API: healthcheck](https://github.com/neovim/neovim/issues/39431) `#39431`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.ui.img` 用 healthcheck を追加し、host terminal の kitty graphics サポートを `TermResponse` で検出してエラー表示する。

**直近のアクティビティ:** `chipsenkbeil` が APC 周りの誤認を訂正し PR `#39449` を試作。`justinmk` が DA1/OSC/DCS/APC 系のサポート追加箇所を提示。

**次のアクション:** PR `#39449` 経由で実装中。

---

### ![PR](icons/git-pull-request.svg) [refactor(pos,range): use numeric index internally](https://github.com/neovim/neovim/pull/39447) `#39447`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `vim.pos` / `vim.range` 内部で性能向上のため数値インデックスを使う変更。`#38561` の派生作業 +37 / −51。

**直近のアクティビティ:** `MariaSolOs` は「読みやすさを優先し文字列キーを残したい」と反対意見を残したが、`justinmk` の「`Range4`/`Range6` も `vim.range` で置換可能となる」見解と性能データ（メタテーブル影響無視可能）でマージ。`#39451` で release-0.12 にバックポート。

**変更ファイル:** `runtime/lua/vim/pos.lua`、`runtime/lua/vim/range.lua`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0400: sandbox callbacks selected through 'complete'](https://github.com/neovim/neovim/pull/39452) `#39452`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** modeline 由来の `'complete'` 値が sandbox 外で補完コールバックを呼べる問題への vim 9.2.0400 patch 取込。両 phase で sandbox 進入と regression test 追加。

**直近のアクティビティ:** `justinmk` は「modeline のセキュリティはコールグラフ各所のチェックに依存しており抜本的に `:trust` 化すべき」と長期方針を示唆。`zeertzjq` が APPROVED。

**変更ファイル:** `src/nvim/insexpand.c`、`test/old/testdir/test_modeline.vim`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(path): refine params passing for path normalization](https://github.com/neovim/neovim/pull/39426) `#39426`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim_getenv` の path 系変数取得に続けて `TO_SLASH` を呼ぶようリファインする +40 / −11。`#39382` の Windows 経路区切り整理の続編。

**直近のアクティビティ:** `ntdiary` が `windows_spec.lua` のローカル成功 / CI 失敗を報告。`justinmk` が flaky テストとして CI 再実行。

**変更ファイル:** `src/nvim/os/env.c`、`src/nvim/os/pty_proc_win.c`、`src/nvim/runtime.c`、`test/functional/vimscript/environ_spec.lua`。

**次のアクション:** CI 安定化と追加レビュー。

---

### ![Issue](icons/issue-opened.svg) [Window path separator (slash) handling](https://github.com/neovim/neovim/issues/39382) `#39382`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Windows の path 区切り処理を「内部は常に `/`、cosmetic 用途は `/`、必要箇所のみ `\` 変換」のポリシーに統一する戦略 issue。

**直近のアクティビティ:** `ntdiary` が「`expand_env` での `TO_SLASH` は backslash がエスケープ文字として使われるためトリッキー」と注意点を共有。

**次のアクション:** 段階的に PR を投入中（`#39426` など）。

---

### ![PR](icons/git-pull-request.svg) [feat(lua): add standalone vim.wait](https://github.com/neovim/neovim/pull/39116) `#39116`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `nvim -ll` スタンドアロンスクリプト向けに luv ベースの `vim.wait()` を提供。`-ll` セマンティクスを文書化、timer 駆動の成功 / timeout カバレッジ追加。+220 / −136。AI-assisted: Codex。

**直近のアクティビティ:** `lewis6991`・`justinmk` が「長期的には `nvim -l` を `lua` cli 同等にして本対応を不要化したい」と方向性を確認。`justinmk` が APPROVED で短期マージ容認。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/lua/vim/_core/editor.lua`、`runtime/lua/vim/_core/wait.lua`、`runtime/lua/vim/_init_packages.lua`、`src/gen/gen_vimdoc.lua`、`test/functional/core/main_spec.lua`。

**次のアクション:** マージ予定。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.net): accept multiple http methods on request()](https://github.com/neovim/neovim/pull/39406) `#39406`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.net.request` に複数 HTTP メソッド対応を追加する第一弾 PR。`#38946` 対応。レスポンスパース、HTTP ステータス enum、`body` の table 自動 JSON 化が後続課題。+107 / −15。

**直近のアクティビティ:** `justinmk` のレビューコメント複数。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/doc/news.txt`、`runtime/lua/vim/net.lua`、`test/functional/lua/net_spec.lua`。

**次のアクション:** API 設計決定後の段階的実装。

---

### ![PR](icons/git-pull-request.svg) [perf(treesitter): reuse edited tree ranges for callbacks](https://github.com/neovim/neovim/pull/39448) `#39448`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `LanguageTree:_edit()` で生成済みの編集後 byte offset を後続 ranged parse の `set_included_regions()` で再利用する最適化 +56 / −6。多数 injection を含むファイルでの小編集ロスを削減。

**直近のアクティビティ:** `clason` がレビューコメント。AI 補助で発見した最適化と作者が明示。

**変更ファイル:** `runtime/lua/vim/treesitter/languagetree.lua`、`test/functional/treesitter/parser_spec.lua`。

**次のアクション:** メンテナの妥当性確認。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): error on invalid completion response](https://github.com/neovim/neovim/pull/39445) `#39445`
**ステータス:** Open | **ラベル:** (なし)

**概要:** server が `null` を返した際 `#vim.NIL` でクラッシュする問題を修正。spec 許容の `result=null` は無視、`items=null` のみサーバ名付きでエラー化。`#39400` 修正。

**直近のアクティビティ:** `justinmk`・`glepnir` のレビューコメント。

**変更ファイル:** `runtime/lua/vim/lsp/completion.lua`、`test/functional/plugin/lsp/completion_spec.lua`。

**次のアクション:** レビュー反映後マージ予定。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): support passing arbitrary positions to methods in `vim.lsp.buf`](https://github.com/neovim/neovim/pull/39446) `#39446`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `vim.lsp.buf` に任意位置でリクエストを送れる引数を追加する +30 / −9。`#33090` クローズ目的。

**直近のアクティビティ:** `ofseed` が「カーソル位置制御ではなくバッファ内位置を渡す目的なので `vim.pos` 採用が妥当」と整理し、`vim.pos` 安定化までドラフト保留に変更。

**次のアクション:** `vim.pos` の experimental 解除待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(messages): message kind for :recover and swapfile attention](https://github.com/neovim/neovim/pull/39444) `#39444`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `:recover` と非プロンプト swapfile attention メッセージに `list_cmd` / `wmsg` kind を割り当てる +117 / −36。`#39327` 修正。

**直近のアクティビティ:** `justinmk` が APPROVED し「Windows CI 失敗は関連していそう」とコメント。

**変更ファイル:** `runtime/doc/api-ui-events.txt`、`src/nvim/memline.c`、`test/functional/ex_cmds/swapfile_preserve_recover_spec.lua`。

**次のアクション:** Windows CI 修正後マージ。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): get_node_text returns trailing newline for buffer source](https://github.com/neovim/neovim/pull/39455) `#39455`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `get_node_text` が buffer ソースで末尾改行を返してしまう件の修正 +37 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** treesitter ノードテキスト取得関連。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [feat(log): `vim.log` for easily creating loggers](https://github.com/neovim/neovim/pull/38906) `#38906`
**ステータス:** Open | **ラベル:** (なし)

**概要:** ロガーを簡単に作れる `vim.log` API +592 / −155。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メンテナレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [feat(ex_cmd): `:mktab` to avoid context reset](https://github.com/neovim/neovim/pull/38373) `#38373`
**ステータス:** Open | **ラベル:** (なし)

**概要:** GSoC 提案の第一弾。`sessionoptions` から `tabpages` を外して使う運用で復元時に既存タブを上書きしない `:mktab` を追加する +563 / −63。

**直近のアクティビティ:** `tayheau` が「`#24507` の tabpage layout 保存・復元待ちのほうが適切では」と提示。

**次のアクション:** 上流リファクタの方向性確認後設計。

---

### ![PR](icons/git-pull-request.svg) [feat(ui)!: set mode from mouse state and populate mshape and used_for in mode_info](https://github.com/neovim/neovim/pull/37324) `#37324`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** マウス状態からモードを反映し、`mode_info` に `mshape` と `used_for` を埋める破壊的変更 +283 / −47。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** ドラフト解除前の設計詰め。

---

### ![Issue](icons/issue-opened.svg) [Support `file://` without netrw](https://github.com/neovim/neovim/issues/39278) `#39278`
**ステータス:** Open | **ラベル:** (なし)

**概要:** netrw を無効化すると `file://` URI 経由でのファイルオープンが効かなくなる問題。コア機能として組込みたい。

**直近のアクティビティ:** `superatomic`・`ofseed` が「まずは `BufReadCmd` ベースで実装し既知バグとして差分を許容する」「最終的には Lua 実装が望ましいが新 API 必要」と段階導入策を議論。

**次のアクション:** プロトタイプ実装と新 API 設計。

---

### ![Issue](icons/issue-opened.svg) [Binary mode: carriage return gets stripped when followed by newline](https://github.com/neovim/neovim/issues/39424) `#39424`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `nvim -b file` でパイプから読む際 `\r\n` の `\r` が剥がれる回帰。Vim 9.2.0388 では再現せず。

**直近のアクティビティ:** `616b2f` が調査開始を表明。

**次のアクション:** 担当者の修正 PR 待ち。

---

### ![Issue](icons/issue-opened.svg) [vim.pos, vim.range followup work](https://github.com/neovim/neovim/issues/38561) `#38561`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.pos` / `vim.range` の followup タスク。`to_inclusive_pos` のテスト強化、`intersect` テスト、`Range.has` セマンティクス変更、`Range.to_extmark/to_cursor` の一時オブジェクト等。

**直近のアクティビティ:** 数値インデックス採用議論を踏まえ `#39454` に集約。`justinmk` が「同議論の周回を避けるため数値インデックス継続」と決着。

**次のアクション:** チェックリスト消化 PR が継続投入中。

---

### ![Issue](icons/issue-opened.svg) [`vim.fn.environ()` letter casing of keys on Windows](https://github.com/neovim/neovim/issues/39443) `#39443`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `#39176` 以降 Windows で `vim.fn.environ()` のキーが大文字化されなくなり、`PATH` 取得など既存コードを破る。

**直近のアクティビティ:** `justinmk` が `#39176` レビュー時点で気付いていた旨を共有。

**次のアクション:** 大文字化挙動を再導入する修正方針検討。

---

### ![Issue](icons/issue-opened.svg) [Improve the LSP method based on `decoration_provider`, consider exposing APIs](https://github.com/neovim/neovim/issues/39438) `#39438`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `decoration_provider` ベースで LSP method を改善し、API 公開を検討する設計 issue。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 設計案コメント募集中。

---

### ![Issue](icons/issue-opened.svg) [perf: use vim.range in vim.treesitter](https://github.com/neovim/neovim/issues/39454) `#39454`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.treesitter` 内で `vim.range` を使い `Range4`/`Range6` 廃止を視野にいれるタスク集約。

**直近のアクティビティ:** 直近24時間のコメントなし（`#39447` 議論で並行参照）。

**次のアクション:** PR 化予定。

---

### ![Issue](icons/issue-opened.svg) [messages: `nvim_exec2` shifts column at message start](https://github.com/neovim/neovim/issues/39450) `#39450`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `nvim_exec2` がメッセージ開始時のカラムをずらす不具合。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 修正 PR 待ち。

---

### ![Issue](icons/issue-opened.svg) [vim.pos.rel() gets position relative to mouse/cursor/etc](https://github.com/neovim/neovim/issues/39457) `#39457`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.pos.rel()` でマウス・カーソル等を起点とする相対位置を取得する API 提案。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 設計コメント募集。

---

### ![Issue](icons/issue-opened.svg) [`vim.pos` position delta and distances between positions](https://github.com/neovim/neovim/issues/36331) `#36331`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `vim.pos` 同士の delta / 距離計算 API 設計。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 仕様検討。

---

### ![Issue](icons/issue-opened.svg) [Lua: Position / Range abstraction (vim.pos.Pos)](https://github.com/neovim/neovim/issues/25509) `#25509`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Lua で位置・範囲抽象 `vim.pos.Pos` を提供する初期 issue。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 派生 PR で順次実装中。

---

### ![Issue](icons/issue-opened.svg) [ui2: "Found a swap file" message is ui-messages kind "" (unknown)](https://github.com/neovim/neovim/issues/39327) `#39327`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `ui2` で swap file 通知メッセージの kind が空文字になる問題。`#39444` で対応中。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR `#39444` のマージで解決見込み。

---

### ![Issue](icons/issue-opened.svg) [ui2: entering the pager fails if `<ESC>` is remapped to `:fclose`](https://github.com/neovim/neovim/issues/39463) `#39463`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `<ESC>` を `:fclose` に remap した状態で pager 進入に失敗する問題。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR `#39462` で対応中。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): entering the pager fails if `<ESC>` is remapped to `:fclose`](https://github.com/neovim/neovim/pull/39462) `#39462`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 上記 issue の修正 +2 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39466) `#39466`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** `version.c` のドキュメント更新ドラフト +26 / −46。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** ドラフト整備中。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0405: when jumping to tags, will open URLs](https://github.com/neovim/neovim/pull/39461) `#39461`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** tag ジャンプ時に URL を開いてしまう問題への vim-patch 取込 +33 / −4。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(marks): don't use spell decorations from other lines](https://github.com/neovim/neovim/pull/39460) `#39460`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `#39441` の release-0.12 へのバックポート +123 / −7。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:96be273: runtime(doc): fix :z command description again](https://github.com/neovim/neovim/pull/39459) `#39459`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `:z` コマンドのヘルプ修正 vim-patch +2 / −2。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs(lua): place `vim.ui.img` section after `vim.ui` section](https://github.com/neovim/neovim/pull/39456) `#39456`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Lua docs の `vim.ui.img` セクション順序を `vim.ui` の後に並び替える +61 / −61。

**直近のアクティビティ:** メンテナレビューを反映してマージ。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(marks): don't use spell decorations from other lines](https://github.com/neovim/neovim/pull/39441) `#39441`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** spell decoration が他行から漏れる不具合の修正 +123 / −7。`#39433` のマークダウン inlineMisspelledCode regression を解決。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み（`#39460` で 0.12 にバックポート）。

---

### ![PR](icons/git-pull-request.svg) [fix(excmd): use realtime for v:starttime and :uptime](https://github.com/neovim/neovim/pull/39425) `#39425`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `v:starttime` と `:uptime` で monotonic ではなく realtime を使うよう修正 +30 / −9。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [backport: refactor(pos,range): use numeric index internally](https://github.com/neovim/neovim/pull/39451) `#39451`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `#39447` の release-0.12 バックポート +37 / −51。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [refactor(lua): reimplement vim.wait in Lua](https://github.com/neovim/neovim/pull/38116) `#38116`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `vim.wait` の Lua 再実装 +232 / −225。`#39116` の前提作業。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(events)!: support `Optionset modified`, drop `BufModifiedSet`](https://github.com/neovim/neovim/pull/35610) `#35610`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `BufModifiedSet` を廃止し `Optionset modified` をサポートする破壊的変更 +120 / −68。`#37317`・`#32817` を解決。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39257) `#39257`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `version.c` ドキュメント更新 +91 / −97。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(path): normalize separator to slash on Windows](https://github.com/neovim/neovim/pull/37729) `#37729`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Windows で path 区切りを `/` に正規化する +201 / −214。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(termdebug): fix evaluate display with gdb pretty print](https://github.com/neovim/neovim/pull/39453) `#39453`
**ステータス:** Open | **ラベル:** (なし)

**概要:** termdebug の evaluate 表示が gdb pretty print と噛み合わない問題の修正 +1 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump tree-sitter to fb348c349](https://github.com/neovim/neovim/pull/39442) `#39442`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** tree-sitter サブモジュールを `fb348c349` に更新 +4 / −4。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ci: bump the github-actions group across 2 directories with 4 updates](https://github.com/neovim/neovim/pull/39367) `#39367`
**ステータス:** Open | **ラベル:** (なし)

**概要:** GitHub Actions の group 更新 +5 / −5。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ予定。

---

### ![Issue](icons/issue-opened.svg) [Macros in registries printing string literals not escape characters whilst in input mode](https://github.com/neovim/neovim/issues/39465) `#39465`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** input mode で registry のマクロが escape 文字でなく文字列リテラルとして表示される問題。

**直近のアクティビティ:** 投稿者解決と判断しクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Problem with nvim in macos](https://github.com/neovim/neovim/issues/39464) `#39464`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** macOS で `nvim` が動作しない報告。

**直近のアクティビティ:** 詳細不足のためクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [regression: `[s` does not work when markdown has `inlineMisspelledCode`](https://github.com/neovim/neovim/issues/39433) `#39433`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Markdown で `inlineMisspelledCode` を含むと `[s` が動作しない regression。

**直近のアクティビティ:** PR `#39441` でクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Opening/closing vim windows with :set shellslash causes nvim to exit with return code 127 on MSYS UCRT64](https://github.com/neovim/neovim/issues/38474) `#38474`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** MSYS UCRT64 で `:set shellslash` 利用時に return 127 で異常終了する問題。

**直近のアクティビティ:** Path normalization 系の修正で解決しクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [build fails: `gen_char_blob.lua: "bad argument to format"` if clone directory contains `%` chars](https://github.com/neovim/neovim/issues/39271) `#39271`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** clone ディレクトリ名に `%` が含まれると build に失敗する問題。

**直近のアクティビティ:** クローズ済み。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`BufModifiedSet` does not get triggered when 'modified' is manually set.](https://github.com/neovim/neovim/issues/37317) `#37317`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** `BufModifiedSet` が手動 set 時に発火しない問題。`#35610` の `Optionset modified` 移行で解消。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`BufModifiedSet` does not trigger if the buffer is not current buffer](https://github.com/neovim/neovim/issues/32817) `#32817`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** カレント以外のバッファで `BufModifiedSet` が発火しない問題。`#35610` で解消。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [LSP: disable wrapping in hover window](https://github.com/neovim/neovim/issues/30831) `#30831`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** LSP hover window の wrap を無効化したい要望。

**直近のアクティビティ:** 仕様化で解決しクローズ。

**次のアクション:** クローズ済み。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> macOS のキーバインド・ウィンドウ操作系修正と、IME / preedit 周りの cursor バグ修正、middle-click アクション設定追加など UX 系の改善が並行マージ。

### ![PR](icons/git-pull-request.svg) [macOS: fix keybindings for end_search not working correctly](https://github.com/ghostty-org/ghostty/pull/12492) `#12492`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** macOS で `navigate_search:next` / `previous` を補完する形で `end_search` キーバインドを修正し、メニューバー経由の検索終了後に surface がフォーカスを失う問題も解消。`#11410` を完全クローズ。+20 / −11。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `macos/Sources/App/macOS/AppDelegate.swift` ほか 5 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macos: add `move_tab_to_split` keybinding action](https://github.com/ghostty-org/ghostty/pull/12498) `#12498`
**ステータス:** Closed (Draft) | **ラベル:** os/macos

**概要:** フォーカス中の surface を別タブへ split として移動するアクション `move_tab_to_split:direction` を追加する +153 / −7。direction は `right/left/up/down/auto`。

**直近のアクティビティ:** `ghostty-vouch[bot]` が「投稿者は vouched ユーザーではない」として自動クローズ。

**次のアクション:** vouch 取得後に再投入。

---

### ![PR](icons/git-pull-request.svg) [feat: add middle-click action configuration](https://github.com/ghostty-org/ghostty/pull/12478) `#12478`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `middle-click-action` 設定を追加。`primary-paste`（GTK の primary paste 同様、`copy-on-select` 連動）と `ignore` を提供する +41 / −15。`#12108` discussion を解決。

**直近のアクティビティ:** `mitchellh` が APPROVED「CI 通り次第」。

**変更ファイル:** `src/Surface.zig`、`src/config.zig`、`src/config/Config.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix speedy high-resolution scrolling on Linux](https://github.com/ghostty-org/ghostty/pull/12483) `#12483`
**ステータス:** Merged | **ラベル:** input

**概要:** Linux のハイレゾホイールでスクロール最低 1 強制が高速化を招く件を、最低値強制を macOS 限定に切り替えて修正 +16 / −12。`mouse-scroll-multiplier` では区別不能と説明。

**直近のアクティビティ:** `mitchellh` が「`zig fmt` 整形」「PR レビューはモバイルから」とフィードバック、`rightaditya` が rebase 対応。

**変更ファイル:** `src/Surface.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [update zon2nix to 0.5.0](https://github.com/ghostty-org/ghostty/pull/12488) `#12488`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** flatpak のハッシュ出力修正のため zon2nix を 0.5.0 に更新 +19 / −24。

**直近のアクティビティ:** `mitchellh` が CI 失敗の関連性確認、`jcollie` が「nixpkgs 更新由来かもしれない」と返信。

**変更ファイル:** `flake.lock`、`flatpak/zig-packages.json`、`nix/build-support/check-zig-cache.sh`、`nix/devShell.nix`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macOS: update tests and add test plan](https://github.com/ghostty-org/ghostty/pull/12473) `#12473`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** macOS UI テストと `Ghostty.xctestplan` を追加し、ホストアプリでテスト用設定を切り替えられるように +67 / −24。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `macos/Ghostty.xcodeproj/*` ほか xctest スイート 5 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [input: remove translated in capi](https://github.com/ghostty-org/ghostty/pull/12490) `#12490`
**ステータス:** Merged | **ラベル:** input

**概要:** `a3462dd2` のフォローアップで C API の translated を削除 +2 / −4。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `include/ghostty.h`、`src/input/Binding.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [renderer: fix preedit range width](https://github.com/ghostty-org/ghostty/pull/12479) `#12479`
**ステータス:** Merged | **ラベル:** renderer

**概要:** `Preedit.range()` が inclusive range なのに `start + w` で計算し wide preedit で 1 セル余分にカバーする不具合を修正。Korean IME（`가나다` 編集中）で `index out of bounds: index 2, len 2` panic を再現。+39 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `src/renderer/State.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macos: expose tab color via AppleScript](https://github.com/ghostty-org/ghostty/pull/12468) `#12468`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS の AppleScript で tab `color` プロパティを read/write 可能にする +58 / −0。Discussions `#11590`・`#12314` の要望対応。`Claude code` で初期 `.sdef` 生成、人手で保守性とコンパイル時保証を強化。

**直近のアクティビティ:** `bo2themax`・`mitchellh` が CHANGES_REQUESTED で「RGBA を使う方向で同意」。

**変更ファイル:** `macos/Ghostty.sdef`、`macos/Sources/Features/AppleScript/ScriptTab.swift`、`macos/Sources/Features/Terminal/TerminalTabColor.swift`。

**次のアクション:** RGBA 対応への書き換え。

---

### ![PR](icons/git-pull-request.svg) [gtk: fix quick terminal breaking when manually toggled off while auto-hide is enabled](https://github.com/ghostty-org/ghostty/pull/12471) `#12471`
**ステータス:** Merged | **ラベル:** gtk

**概要:** `quick-terminal-autohide` 有効時に手動非表示で `Window.propIsActive` 経由で再 `toggleVisibility` が呼ばれ壊れる挙動の修正 +4 / −1。`#11679` 修正。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `src/apprt/gtk/class/window.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: update Se terminfo entry to reset cursor to configured default](https://github.com/ghostty-org/ghostty/pull/12487) `#12487`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** terminfo の `Se`（cursor reset）を `\E[2 q`（steady block 固定）から `\E[0 q`（user default）へ修正 +2 / −2。`#12482` および neovim `#38987` 関連を解消。

**直近のアクティビティ:** `jcollie` が APPROVED「LGTM pending CI」。

**変更ファイル:** `src/terminfo/ghostty.zig`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Mouse cursor stuck as crosshair after Option+**key**](https://github.com/ghostty-org/ghostty/issues/12265) `#12265`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS の Option+key（dead key）後にカーソル形状が crosshair のまま戻らない問題。`flagsChanged` での状態遷移取りこぼしと推定。

**直近のアクティビティ:** `aaronhooper` が AppKit 側の `flagsChanged` 経路を Codex で調査。`MagicDuck` が 1.3.1 でも再現、Shift で復帰可能だがアプリ切替で再発と報告。

**次のアクション:** AppKit 修飾キー処理見直し。

---

### ![Issue](icons/issue-opened.svg) [macOS: Global keybind stops in the app BUT not outside the app](https://github.com/ghostty-org/ghostty/issues/12294) `#12294`
**ステータス:** Closed | **ラベル:** os/macos, input

**概要:** `toggle_visiblity` のグローバルキーバインドがアプリ外からは動かなくなる問題。`#11883` と類似。

**直近のアクティビティ:** `taniadaniela` が macOS Ventura 13.7.8 / Ghostty 13.7.8 でも再現と追記しつつクローズ。

**次のアクション:** クローズ済み（duplicate / 関連 issue で追跡）。

---

### ![Issue](icons/issue-opened.svg) [Include scrollback history in state restoration](https://github.com/ghostty-org/ghostty/issues/1847) `#1847`
**ステータス:** Open | **ラベル:** os/macos

**概要:** アプリ更新やクラッシュ時に scrollback 履歴を保持したい要望。

**直近のアクティビティ:** `gregorskii` が iTerm2 から移行する際の最大の痛点と指摘。`0xBigBoss` がセッション単位の append-only バイナリフォーマット試作（メタデータ / scrollback / screen / screen.alt）を共有。

**次のアクション:** プロトタイプ PR 化を検討。

---

### ![Issue](icons/issue-opened.svg) [Inconsistent terminfo entry for resetting cursor style](https://github.com/ghostty-org/ghostty/issues/12482) `#12482`
**ステータス:** Closed | **ラベル:** contributor friendly

**概要:** `Se`（cursor style reset）が `\E[2 q` で steady block 固定になりリセットの挙動が不自然な問題。

**直近のアクティビティ:** PR `#12487` でクローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Reloading config overrides OSC 10/11/12](https://github.com/ghostty-org/ghostty/issues/2795) `#2795`
**ステータス:** Closed | **ラベル:** renderer, contributor friendly

**概要:** config 再読込で OSC 10/11 で設定した foreground/background が theme 色にリセットされる問題（OSC 4 では発生せず）。

**直近のアクティビティ:** 直近24時間のコメントなし（issue 解決によりクローズ）。

**次のアクション:** クローズ済み。

---

## astral-sh/uv

> 0.11.8 リリースとそれに伴う Renovate 由来の依存アップデート群が大量にマージ。`uv audit` のステータスレポート、ARMv7 FP 検出、xz サポート削除、Package by default 安定化など機能設計議論も多数進行。

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.8](https://github.com/astral-sh/uv/pull/19184) `#19184`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** uv 0.11.8 リリースバンプ +389 / −352。

**直近のアクティビティ:** `konstin` が APPROVED、`zanieb` のレビュー指摘は `#19188` で対応予定。

**変更ファイル:** `CHANGELOG.md`、`Cargo.lock`、`Cargo.toml` ほか。

**次のアクション:** リリース済み。

---

### ![PR](icons/git-pull-request.svg) [Tweak changelog entries for 0.11.8](https://github.com/astral-sh/uv/pull/19188) `#19188`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** 0.11.8 changelog の表現微調整 +6 / −3。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [uv audit: adverse status reporting](https://github.com/astral-sh/uv/pull/19128) `#19128`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `uv audit` に PEP 792 のプロジェクトステータス（`hackme is quarantined` 等）を表示する機能を追加 +704 / −28。`#18506` の一部、`#15254` クローズ目的。OSV 以外にレジストリへの追加リクエストが必要。

**直近のアクティビティ:** CodSpeed は性能影響なしと報告。`woodruffw`・`konstin` のレビューコメント進行中。

**変更ファイル:** `crates/uv-audit/Cargo.toml`、`crates/uv-audit/src/service/{mod.rs,osv.rs,project_status.rs}` ほか。

**次のアクション:** レビュー反映後マージ予定。

---

### ![PR](icons/git-pull-request.svg) [Catch missing uv build backend compatible version bumps](https://github.com/astral-sh/uv/pull/18821) `#18821`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** uv build backend の `COMPATIBLE_VERSIONS` リスト更新忘れを CI で検知する仕組み +27 / −0。minor バンプ時にリストへ追記または clear が必要。

**直近のアクティビティ:** `zanieb` が APPROVED、panic メッセージで両選択肢の説明を要求。`konstin` が手順解説を追記。

**変更ファイル:** `crates/uv-build-backend/Cargo.toml`、`crates/uv-build-backend/src/metadata.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Add in preview](https://github.com/astral-sh/uv/pull/17841) `#17841`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `uv init` のデフォルトを src-package（エントリポイント付きパッケージ）に変更する preview。layout は「entrypoint 付き package」「pure lib」「flat main.py」の 3 通り +231 / −39。

**直近のアクティビティ:** CodSpeed は性能影響なし、`konstin` がレビュー継続中。

**変更ファイル:** `crates/uv-cli/src/lib.rs`、`crates/uv-preview/src/lib.rs`、`crates/uv/src/commands/project/init.rs` ほか。

**次のアクション:** preview 安定化に向けて議論継続。`#19026` でドキュメント、`#19197` で stabilize 着手。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Stabilize preview feature](https://github.com/astral-sh/uv/pull/19197) `#19197`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** Package by default を preview から stable に昇格する +201 / −321 のドラフト。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** `#17841` の決着後 stabilize 進行。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Update documentation](https://github.com/astral-sh/uv/pull/19026) `#19026`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** Package by default 関連ドキュメント更新ドラフト +76 / −67。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** stabilize と並行更新。

---

### ![PR](icons/git-pull-request.svg) [Add module owners to workspace metadata](https://github.com/astral-sh/uv/pull/19122) `#19122`
**ステータス:** Open | **ラベル:** (なし)

**概要:** ワークスペース metadata にモジュールオーナー情報を追加する +596 / −44。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Fix PowerShell activator UNC path handling](https://github.com/astral-sh/uv/pull/19159) `#19159`
**ステータス:** Open | **ラベル:** (なし)

**概要:** PowerShell activator スクリプトの UNC path 取り扱い修正 +1 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ予定。

---

### ![PR](icons/git-pull-request.svg) [trampolines: Don't set PYTHONHOME, set __PYVENV_LAUNCHER__ only in venvs](https://github.com/astral-sh/uv/pull/19199) `#19199`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `PYTHONHOME` を設定しない方針に変更し子プロセスへの leak で他 Python ビルドを壊す問題 (`#19080`) を修正。Python 3.10 以前の getpath 実装対策として `__PYVENV_LAUNCHER__` を venv 内のみ設定 +11 / −44。

**直近のアクティビティ:** `geofft` が「AWS Server 2022 のトランポリン EXE をバイナリ編集して手動検証、`X` flag のテストはまだ」と詳細レポート。

**変更ファイル:** `crates/uv-static/src/env_vars.rs`、`crates/uv-trampoline-builder/trampolines/uv-trampoline-*.exe`。

**次のアクション:** Windows 多バージョンでのテスト。

---

### ![PR](icons/git-pull-request.svg) [fix(trampoline): don't set PYTHONHOME for venv trampolines](https://github.com/astral-sh/uv/pull/19123) `#19123`
**ステータス:** Closed (Draft) | **ラベル:** (なし)

**概要:** `#19199` の前段ドラフト +12 / −1。

**直近のアクティビティ:** `#19199` に置き換えクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [filter `[exclude-newer-package]` lockfile entries to resolved packages](https://github.com/astral-sh/uv/pull/19198) `#19198`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `~/.config/uv/uv.toml` のグローバル `[exclude-newer-package]` がプロジェクトの `uv.lock` に丸ごと書き出され `uv sync --locked` が CI で破綻する問題 (`#19196`) の修正。`self.packages` に存在する名前のみフィルタする +120 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** `crates/uv-resolver/src/lock/mod.rs`、`crates/uv/tests/it/lock.rs`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Remove xz support entirely](https://github.com/astral-sh/uv/pull/19143) `#19143`
**ステータス:** Open | **ラベル:** (なし)

**概要:** tarball 経路を含む xz 解凍サポートを完全削除する +471 / −215。`#18927` の上に積む。

**直近のアクティビティ:** `woodruffw` が「`SourceDistFilename` を `Pep625(...)` / `Legacy(...)` の enum で型レベル表現する将来案」をコメント。`konstin`・`woodruffw` の review 進行。

**変更ファイル:** Cargo.lock、`crates/uv-bin-install/src/lib.rs` ほか多数。

**次のアクション:** レビュー反映後マージ。

---

### ![PR](icons/git-pull-request.svg) [Retry failed partial downloads with HTTP Range header when supported](https://github.com/astral-sh/uv/pull/19075) `#19075`
**ステータス:** Open | **ラベル:** (なし)

**概要:** PyPI の頻繁な接続リセットでリトライ枯渇する問題を、`pip` 同様 HTTP Range で部分再取得して回避する +380 / −34。

**直近のアクティビティ:** `apaz-cli` が「PR レビューしてください」と懇願 GIF 付きで催促。`konstin` がレビューコメント。

**変更ファイル:** `crates/uv-distribution/src/distribution_database.rs`、`crates/uv/tests/it/network.rs`。

**次のアクション:** レビュー反映後マージ。

---

### ![PR](icons/git-pull-request.svg) [Generalize detected FP environment handling on ARMv7](https://github.com/astral-sh/uv/pull/19157) `#19157`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** ARMv7 で検出した FP 環境のハンドリング一般化 +64 / −9。`#19139` の `eabihf` サフィックス重複問題対処の流れ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix duplicated `eabihf` suffix in ARMv7 platform triples](https://github.com/astral-sh/uv/pull/19139) `#19139`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** ARMv7 platform triple で `eabihf` が重複する問題への先行修正 +61 / −8。

**直近のアクティビティ:** `#19157` に置き換えクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Move parsing `UV_GITHUB_FAST_PATH_URL` to `EnvironmentOptions`](https://github.com/astral-sh/uv/pull/19193) `#19193`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 毎回 `std::env::var()` を呼ぶ実装を `EnvironmentOptions` に移し `GitResolver` まで builder で配線する +113 / −25。`#14720` 関連。

**直近のアクティビティ:** `konstin` が「自動生成 PR は禁止」（AI_POLICY.md）としてクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Move the Windows exception handler to `uv-windows`](https://github.com/astral-sh/uv/pull/19191) `#19191`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Windows 例外ハンドラを `uv-windows` クレートへ移動 +10 / −10。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: warn when frozen add ignores bounds](https://github.com/astral-sh/uv/pull/19180) `#19180`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** `uv add --bounds <kind> --frozen` が bounds を黙って無視する case で warn を出す +83 / −0。`#19090` 修正狙い。

**直近のアクティビティ:** `konstin` が「自動生成 PR は禁止」としてクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Catch all bot-generated dependency bumps (Renovate)](https://github.com/astral-sh/uv/pull/19135) `#19135`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Depot macOS runner を 15 にバンプする +3 / −3 の試行。

**直近のアクティビティ:** `#19194` に置き換えクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Upgrade Depot runners from macOS 14 to 15](https://github.com/astral-sh/uv/pull/19194) `#19194`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Depot CI runner を macOS 14 → 15 へ +3 / −3。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Bump astral-tokio-tar to 0.6.1](https://github.com/astral-sh/uv/pull/19195) `#19195`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `astral-tokio-tar` を 0.6.1 にバンプ +3 / −3。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update Rust to v1.95.0](https://github.com/astral-sh/uv/pull/19179) `#19179`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Rust toolchain を 1.95.0 へ更新 +82 / −104。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Bump mimalloc crate to 0.1.50 while keeping v2](https://github.com/astral-sh/uv/pull/19192) `#19192`
**ステータス:** Open | **ラベル:** (なし)

**概要:** v3 で発生する Windows aarch64 アクセス違反 (`#19189`) を回避するため v2 を保ったまま mimalloc を 0.1.50 へ +5 / −6。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** Windows aarch64 動作確認後マージ。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate mimalloc to v0.1.49](https://github.com/astral-sh/uv/pull/19167) `#19167`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** mimalloc 0.1.48 → 0.1.49（pending 0.1.50） +4 / −5。

**直近のアクティビティ:** `zanieb` が「`#19189` でブロック」とコメント。

**次のアクション:** `#19192` で代替対応中。

---

### ![PR](icons/git-pull-request.svg) [Use the newly stabilized `<[T]>::as_array::<N>()`](https://github.com/astral-sh/uv/pull/19098) `#19098`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Rust 1.95 安定化された `<[T]>::as_array::<N>()` を採用 +3 / −2。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Use sentinel timestamp for relative exclude-newer-package spans](https://github.com/astral-sh/uv/pull/19101) `#19101`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** 相対 `exclude-newer-package` スパンに sentinel timestamp を使う +74 / −13。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Use an Ubuntu snapshot for apt installs in CI for reproducibility](https://github.com/astral-sh/uv/pull/19032) `#19032`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** CI の `apt` インストールを Ubuntu snapshot 化 +7 / −5。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Trim trailing whitespace in help output](https://github.com/astral-sh/uv/pull/18182) `#18182`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `--help` 出力末尾の空白を除去する +86 / −85。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix inclusion of extra specified in dependency group when listed in a conflict](https://github.com/astral-sh/uv/pull/19109) `#19109`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** dependency group に列挙された extra が conflict listing と組み合わさった際の包含修正 +297 / −5。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** ドラフト整備中。

---

### ![PR](icons/git-pull-request.svg) [docs: Clarify that `uv init` creates git files/folders in projects guide](https://github.com/astral-sh/uv/pull/19183) `#19183`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** docs で `uv init` が git 関連ファイル / フォルダを生成する旨を明記 +9 / −6。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Docs: Add details of `--seed` to pip environments docs](https://github.com/astral-sh/uv/pull/9689) `#9689`
**ステータス:** Open | **ラベル:** (なし)

**概要:** pip environments docs に `--seed` の詳細を追加 +7 / −0。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [dependencies: add section on how to upgrade all versions](https://github.com/astral-sh/uv/pull/17951) `#17951`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** docs に「全バージョンを upgrade する方法」セクションを追加する試み +18 / −0。

**直近のアクティビティ:** 直近24時間のコメントなし（クローズ）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [packaging documentation: add notes on how to install created dist wheel](https://github.com/astral-sh/uv/pull/12001) `#12001`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** dist wheel を作成・インストールする手順 docs +17 / −0。

**直近のアクティビティ:** 直近24時間のコメントなし（クローズ）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix hydration of external-source plugins after reload (#308504)](https://github.com/astral-sh/uv/pull/19124) `#19124`

> （該当なし。スキップ）

---

### Renovate 由来の依存アップデート（バッチ要約）

以下はいずれも Renovate / Dependabot 系の自動 PR で、本日マージ完了したもの。詳細議論はなく diff のみ。

- ![PR](icons/git-pull-request.svg) [Update Rust crate tokio to v1.52.1](https://github.com/astral-sh/uv/pull/19178) `#19178` — Merged。+17 / −17。
- ![PR](icons/git-pull-request.svg) [Update Rust crate rayon to v1.12.0](https://github.com/astral-sh/uv/pull/19177) `#19177` — Merged。+2 / −2。
- ![PR](icons/git-pull-request.svg) [Update Rust crate criterion to v4.5.0](https://github.com/astral-sh/uv/pull/19175) `#19175` — Merged。+21 / −21。
- ![PR](icons/git-pull-request.svg) [Update Rust crate open to v5.3.4](https://github.com/astral-sh/uv/pull/19168) `#19168` — Merged。+2 / −2。
- ![PR](icons/git-pull-request.svg) [Update Rust crate bitflags to v2.11.1](https://github.com/astral-sh/uv/pull/19164) `#19164` — Merged。+22 / −22。
- ![PR](icons/git-pull-request.svg) [Update Rust crate cargo-util to v0.2.28](https://github.com/astral-sh/uv/pull/19165) `#19165` — Merged。+17 / −17。
- ![PR](icons/git-pull-request.svg) [Update Rust crate clap to v4.6.1](https://github.com/astral-sh/uv/pull/19166) `#19166` — Merged。+19 / −19。
- ![PR](icons/git-pull-request.svg) [Update Rust crate assert_cmd to v2.2.1](https://github.com/astral-sh/uv/pull/19163) `#19163` — Merged。+2 / −2。
- ![PR](icons/git-pull-request.svg) [Update Rust crate webpki-root-certs to v1.0.7](https://github.com/astral-sh/uv/pull/19171) `#19171` — Merged。+2 / −2。
- ![PR](icons/git-pull-request.svg) [Update Rust crate uuid to v1.23.1](https://github.com/astral-sh/uv/pull/19170) `#19170` — Open。+3 / −3。
- ![PR](icons/git-pull-request.svg) [Update Rust crate nanoid to 0.5.0](https://github.com/astral-sh/uv/pull/19176) `#19176` — Open。+19 / −20、`apaz-cli` がコメント。
- ![PR](icons/git-pull-request.svg) [Update dependency python to 3.14](https://github.com/astral-sh/uv/pull/19174) `#19174` — Merged。+2 / −2。
- ![PR](icons/git-pull-request.svg) [Update taiki-e/install-action action to v2.75.18](https://github.com/astral-sh/uv/pull/19172) `#19172` — Open。+8 / −8。
- ![PR](icons/git-pull-request.svg) [Update EmbarkStudios/cargo-deny-action action to v2.0.17](https://github.com/astral-sh/uv/pull/19162) `#19162` — Merged。+1 / −1。
- ![PR](icons/git-pull-request.svg) [Update crate-ci/typos action to v1.45.1](https://github.com/astral-sh/uv/pull/19161) `#19161` — Merged。+1 / −1。
- ![PR](icons/git-pull-request.svg) [Update zizmorcore/zizmor-action action to v0.5.3](https://github.com/astral-sh/uv/pull/19173) `#19173` — Merged。+1 / −1。

---

### ![Issue](icons/issue-opened.svg) [Make ARMv7 FP detection infallible](https://github.com/astral-sh/uv/issues/19190) `#19190`
**ステータス:** Open | **ラベル:** (なし)

**概要:** procfs 由来の CPU feature 検出を `getauxval(AT_HWCAP) & HWCAP_VFP` に置換し infallible 化する設計提案。

**直近のアクティビティ:** `nooscraft` が「`rustix::param::linux_hwcap` が候補だが arm32-on-aarch64 ケースでは ABI 整合確認が必要」と指摘。`woodruffw` も同 docs を共有しつつ「ホスト/ユーザー空間 CPU 整合の調整がやや厄介」と確認。

**次のアクション:** 設計案実装に向けた継続調査。

---

### ![Issue](icons/issue-opened.svg) [Selecting the main dependency group with uv lock --upgrade-group ...](https://github.com/astral-sh/uv/issues/19160) `#19160`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `uv lock --upgrade-group` で main 依存グループを選択する方法に関する質問。

**直近のアクティビティ:** メンテナの返答 1 件。

**次のアクション:** ドキュメント拡充検討。

---

### ![Issue](icons/issue-opened.svg) [uv's HEAD-based range detection for wheel metadata is inconsistent with RFC 9110 and misclassifies GAR wheel URLs](https://github.com/astral-sh/uv/issues/18998) `#18998`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `HEAD` プリフライトに依存した range support 検出が RFC 9110 不整合で Google Artifact Registry の wheel URL を誤分類する問題。

**直近のアクティビティ:** `illera88` が「修正にボランティアするのでアプローチ指示が欲しい」と再依頼。`konstin` は「メンテナを連投で ping しないで欲しい、Google 側に Range request 実装を働きかけるほうが良い」と返答。

**次のアクション:** Google Artifact Registry 側の対応待ち、もしくは慎重な uv HTTP 改修。

---

### ![Issue](icons/issue-opened.svg) [Configuration merging produces unusable uv.lock files](https://github.com/astral-sh/uv/issues/19196) `#19196`
**ステータス:** Open | **ラベル:** (なし)

**概要:** プロジェクトの `pyproject.toml` cooldown とグローバル `~/.config/uv/uv.toml` の `[exclude-newer-package]` をマージすると lockfile に解決対象外パッケージが書き出されて CI で `uv sync --locked` が破綻する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR `#19198` で対応中。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate 由来の依存ダッシュボード（自動更新）。

**直近のアクティビティ:** 直近24時間のコメントなし（自動更新のみ）。

**次のアクション:** Renovate が自動更新。

---

### ![Issue](icons/issue-opened.svg) [Make error caused by `--exclude-newer` clearer](https://github.com/astral-sh/uv/issues/5878) `#5878`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `--exclude-newer` 由来のエラーメッセージを分かりやすくしたい要望。

**直近のアクティビティ:** メンテナのレスポンス 1 件。

**次のアクション:** メッセージ改善検討。

---

### ![Issue](icons/issue-opened.svg) [Upgrade dependencies in `pyproject.toml` (`uv upgrade`)](https://github.com/astral-sh/uv/issues/6794) `#6794`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `uv upgrade` で `pyproject.toml` のバージョンピンを一括更新する機能要望（poetry / npm-check-updates 相当）。

**直近のアクティビティ:** `AbdelRahmanRahal` が「まだ実装されてないんですか…」とフォローアップ。

**次のアクション:** 設計議論継続。

---

### ![Issue](icons/issue-opened.svg) [Add a project type that uses `[dependency-groups]` only and doesn't define a `[project]` table?](https://github.com/astral-sh/uv/issues/8582) `#8582`
**ステータス:** Open | **ラベル:** (なし)

**概要:** PEP-735 dependency-groups のみで `[project]` テーブルを持たない project type の提案。データサイエンス用途等を想定。

**直近のアクティビティ:** `CarrotManMatt` が「`requires-python` を `[project]` 無しで表現する方法が未解決」と追記。`#19087` で `[project]`-less プロジェクトの `uv lock` 対応が進行。

**次のアクション:** `requires-python` 等の周辺仕様検討。

---

### ![Issue](icons/issue-opened.svg) [Specify a path and index for package source](https://github.com/astral-sh/uv/issues/16087) `#16087`
**ステータス:** Open | **ラベル:** (なし)

**概要:** package source に path と index を併記する設定要望。

**直近のアクティビティ:** メンテナコメント 2 件。

**次のアクション:** 設計検討。

---

### ![Issue](icons/issue-opened.svg) [multiple sources for same package: local path dependency and regular dependency](https://github.com/astral-sh/uv/issues/15129) `#15129`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 同一パッケージで local path と通常依存を切り替える要望。

**直近のアクティビティ:** メンテナコメント 1 件。

**次のアクション:** 仕様検討中。

---

### ![Issue](icons/issue-opened.svg) [Is there a way to add local packages from `requirements.txt` without any pyproject.toml file present?](https://github.com/astral-sh/uv/issues/19186) `#19186`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Docker イメージ内で `pyproject.toml` を置かず `requirements.txt` だけで local package を入れたいユースケース。

**直近のアクティビティ:** `nooscraft` が「self-contained 不要なら multistage で intermediate に local install」案を提示、`zanieb` が `uv-docker-example/multistage.Dockerfile` と公式 docs の intermediate layer ガイドを案内。

**次のアクション:** docs 改善で対応見込み。

---

### ![Issue](icons/issue-opened.svg) [Caching with dynamically linked binary extensions build from source can lead to mismatches between host and container](https://github.com/astral-sh/uv/issues/19185) `#19185`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Docker で uv キャッシュをホスト共有しつつ動的リンクするソースビルド（例: `libvirt-python`）を扱うとホスト / コンテナ間で不整合が出るバグ。

**直近のアクティビティ:** `bn-jbischko` が暫定対応の限界を説明。`konstin` は「auditwheel 等で vendoring すべき。ワークアラウンドは `uv cache clean <package>`」と返答。

**次のアクション:** パッケージ側のリンク戦略改善が本筋。

---

### ![Issue](icons/issue-opened.svg) [Windows aarch64 smoke test fails with `EXCEPTION_ACCESS_VIOLATION` on mimalloc v3](https://github.com/astral-sh/uv/issues/19189) `#19189`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `#19167` の mimalloc v3 PR で Windows aarch64 smoke test がアクセス違反で落ちる。`purpleprotocol/mimalloc_rust#159`、`microsoft/mimalloc#1277` を参照。

**直近のアクティビティ:** `zanieb` が上流 issue リンクを追記。

**次のアクション:** 上流修正待ち。`#19192` で v2 維持の暫定対応。

---

### ![Issue](icons/issue-opened.svg) [can't install python in wine](https://github.com/astral-sh/uv/issues/19187) `#19187`
**ステータス:** Open | **ラベル:** (なし)

**概要:** wine 上で Python インストールに失敗する報告。

**直近のアクティビティ:** メンテナコメント 1 件。

**次のアクション:** 詳細確認。

---

### ![Issue](icons/issue-opened.svg) [Installing on Windows 2016 fails because of TLS versions](https://github.com/astral-sh/uv/issues/2085) `#2085`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Windows 2016 で TLS バージョン不一致によりインストール失敗する古い issue。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Add platform dependence to `exclude-dependencies`](https://github.com/astral-sh/uv/issues/17642) `#17642`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** `exclude-dependencies` に platform 依存を追加する要望。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Unable to use `uv self update` on armv7 host after 0.11.0](https://github.com/astral-sh/uv/issues/19137) `#19137`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 0.11.0 以降 ARMv7 ホストで `uv self update` が動作しなくなった regression。

**直近のアクティビティ:** PR `#19139` / `#19157` 関連修正でクローズ。

**次のアクション:** クローズ済み。

---

## microsoft/vscode

> 1.118 リリースに向けた Copilot CLI / inline chat / billing 関連の cherry-pick とマージが集中。Insiders ビルド失敗、macOS Tahoe での crash loop、各種 chat UX バグなどユーザ報告も多数。

### ![PR](icons/git-pull-request.svg) [Add support for usage-based billing](https://github.com/microsoft/vscode/pull/312892) `#312892`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Copilot のトークンベース課金とクォータ UX を VS Code workbench、Sessions ウィンドウ、組み込み Copilot 拡張に広範に追加。価格メタデータ、`chatQuota.contribution.ts`、`chatInputNotification.contribution.ts` などを導入する +1833 / −1088 の大型 PR。

**直近のアクティビティ:** `pwang347` が `/api-proposal-change-required` を発行。Copilot reviewer による自動分析、`dmitrivMS` が APPROVED。スクリーンショット 4 件の差分も併走。

**変更ファイル:** `extensions/copilot/CONTRIBUTING.md`、`extensions/copilot/package.json`、`extensions/copilot/src/extension/chat/vscode-node/chatQuota.contribution.ts` ほか。

**次のアクション:** 1.118 マイルストーンでマージ済み。

---

### ![PR](icons/git-pull-request.svg) [Cherry-pick: Fix Copilot CLI /remote session prompt handling (#312874)](https://github.com/microsoft/vscode/pull/312959) `#312959`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Copilot CLI `/remote` セッションプロンプト処理の修正を release/1.118 に cherry-pick +1390 / −62。

**直近のアクティビティ:** `rebornix` が DISMISSED、`dmitrivMS` が APPROVED でマージ。

**変更ファイル:** `extensions/copilot/src/extension/chatSessions/copilotcli/*` ほか。

**次のアクション:** マージ済み。後続 `#312968` cherry-pick で追加対応中。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Cherry-pick: Fix Copilot CLI /remote session prompt handling (#312874)](https://github.com/microsoft/vscode/pull/312968) `#312968`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `#312959` の追加 cherry-pick。`copilotcliSession.ts` でマージ衝突発生中 +527 / −11。

**直近のアクティビティ:** スクリーンショット差分通知のみ。

**次のアクション:** 衝突解消後マージ。

---

### ![PR](icons/git-pull-request.svg) [[Backport release/1.118] Fix Copilot CLI /remote session prompt handling (#312874)](https://github.com/microsoft/vscode/pull/312953) `#312953`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 旧バックポート PR、上記 cherry-pick 系列に置き換えクローズ +1795 / −1510。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Cherry-pick: Fix Copilot CLI remote session prompt handling](https://github.com/microsoft/vscode/pull/312966) `#312966`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 同上 cherry-pick の試行 +869 / −1489。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [profiling: replace v8-inspect-profiler with chrome-remote-interface](https://github.com/microsoft/vscode/pull/312974) `#312974`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `src/vs/base/node/profiling.ts` の `v8-inspect-profiler` 依存を完全 TypeScript 化し `chrome-remote-interface` 直接利用へ +184 / −23。

**直近のアクティビティ:** Copilot reviewer が「型付けされた in-repo profiling モジュール」と評価。`vs-code-engineering[bot]` の CODENOTIFY で `deepak1556` が通知対象。

**変更ファイル:** `package.json`、`package-lock.json`、`src/vs/base/node/profiling.ts`、`src/vs/code/node/cli.ts` ほか。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix: restrict entitlements used for helper.app](https://github.com/microsoft/vscode/pull/312734) `#312734`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** macOS Tahoe (26) での Electron `Helper.app` ネットワークプロセス crash loop (`#312720`) 対策。codesign 時の entitlement を限定する +40 / −7。

**直近のアクティビティ:** `deepak1556` が「リリース後ユーザー報告で初めて検出された理由を理解したい」と原因分析の必要性をコメント。

**変更ファイル:** `build/azure-pipelines/darwin/helper-entitlements.plist`、`build/azure-pipelines/darwin/steps/product-build-darwin-compile.yml`、`build/darwin/sign.ts`。

**次のアクション:** 検証フロー強化と本修正のマージ。

---

### ![PR](icons/git-pull-request.svg) [fix placeholder not showing up + inline chat progress border](https://github.com/microsoft/vscode/pull/312979) `#312979`
**ステータス:** Open | **ラベル:** (なし)

**概要:** inline chat のプレースホルダ非表示とプログレスボーダー描画の修正 +14 / −4。

**直近のアクティビティ:** `vs-code-engineering[bot]` が「マージ時 release/1.118 に自動 cherry-pick」と通知。

**変更ファイル:** `src/vs/workbench/contrib/chat/browser/widget/chatWidget.ts`、`media/chat.css`。

**次のアクション:** レビュー後マージ。

---

### ![PR](icons/git-pull-request.svg) [inlineChat: make status placeholder registration a singleton](https://github.com/microsoft/vscode/pull/312975) `#312975`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 並べた `InlineChatZoneWidget` インスタンス間で `actionViewItemService.register()` 重複登録によるクラッシュを防ぐため StatusPlaceholder の登録を singleton 化する +67 / −18。`#312792` 修正。

**直近のアクティビティ:** Copilot reviewer の overview 詳細、`lszomoru` が APPROVED。

**変更ファイル:** `src/vs/workbench/contrib/inlineChat/browser/inlineChatZoneWidget.ts`。

**次のアクション:** マージ予定。

---

### ![PR](icons/git-pull-request.svg) [Agents - more GitHub REST API cleanup](https://github.com/microsoft/vscode/pull/312978) `#312978`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Agents ウィンドウの `GitHubApiClient` 系 fetcher の REST 呼び出し面を統一メソッドに集約 +35 / −95。

**直近のアクティビティ:** Copilot reviewer の自動分析。

**変更ファイル:** `src/vs/sessions/contrib/github/browser/fetchers/github*Fetcher.ts`、`githubApiClient.ts`。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [customEditor: evict cache entry when model resolver rejects](https://github.com/microsoft/vscode/pull/312583) `#312583`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `CustomEditorModelManager.add()` が rejected promise をキャッシュに残し続ける問題を修正 +152 / −1。`#268301` 修正。

**直近のアクティビティ:** `mohanrajvenkatesan2304-hash` が `@microsoft-github-policy-service agree` で CLA 同意。

**変更ファイル:** `src/vs/workbench/contrib/customEditor/common/customEditorModelManager.ts`、対応 spec。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Clean up some parameters and stuff in Claude](https://github.com/microsoft/vscode/pull/312946) `#312946`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Claude モデルまわりのパラメータ整理 +120 / −174。

**直近のアクティビティ:** マージ済み。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [enable progress border in insiders + reduce motion](https://github.com/microsoft/vscode/pull/312878) `#312878`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** Insiders で chat progress border を有効化、reduce motion 対応 +22 / −8。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [On agent mode change, ignore stateful marker](https://github.com/microsoft/vscode/pull/312951) `#312951`
**ステータス:** Open | **ラベル:** (なし)

**概要:** agent モード切替時に stateful marker を無視する +103 / −3。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix hydration of external-source plugins after reload (#308504)](https://github.com/microsoft/vscode/pull/310499) `#310499`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 外部ソース plugin の reload 後 hydration を修正 +246 / −1。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [microsoft-authentication: fix 'retreive' -> 'retrieve' typo in doc comment](https://github.com/microsoft/vscode/pull/310380) `#310380`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** doc コメントのスペル修正 +1 / −1。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add setting for entry point to Chat from Activity Bar](https://github.com/microsoft/vscode/pull/307321) `#307321`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Activity Bar から Chat へのエントリポイント設定追加試行 +93 / −7。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add find all button](https://github.com/microsoft/vscode/pull/7559) `#7559`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 「Find All」ボタン追加要望の長期 PR +25 / −0。

**直近のアクティビティ:** コメント 1 件（リバイバル系）。

**次のアクション:** メンテナ判断待ち。

---

### ![PR](icons/git-pull-request.svg) [合并分支](https://github.com/microsoft/vscode/pull/312957) `#312957`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 中国語タイトル「合并分支」（ブランチマージ）の偶発 PR。`.devcontainer` 周辺多数を含む +10128 / −65。

**直近のアクティビティ:** CODENOTIFY のみ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [readme change](https://github.com/microsoft/vscode/pull/312903) `#312903`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** README の表現変更 +2 / −2。

**直近のアクティビティ:** ドラフト議論コメント 8 件。

**次のアクション:** ドラフト整備中。

---

### Cherry-pick / バックポート / 自動更新（バッチ要約）

- ![PR](icons/git-pull-request.svg) [[cherry-pick] OSS tool: update third-party notices (v1.118.0)](https://github.com/microsoft/vscode/pull/312952) `#312952` — Merged。+10 / −6。
- ![PR](icons/git-pull-request.svg) [OSS tool: update third-party notices (v1.118.0)](https://github.com/microsoft/vscode/pull/312941) `#312941` — Merged。+224 / −9。`benvillalobos` APPROVED。
- ![PR](icons/git-pull-request.svg) [[cherry-pick] Update GitHub Copilot dependency to version 1.0.38](https://github.com/microsoft/vscode/pull/312954) `#312954` — Merged。+29 / −29。
- ![PR](icons/git-pull-request.svg) [Update GitHub Copilot dependency to version 1.0.38](https://github.com/microsoft/vscode/pull/312947) `#312947` — Merged。+29 / −29。
- ![PR](icons/git-pull-request.svg) [Update distro commit (release/1.118)](https://github.com/microsoft/vscode/pull/312944) `#312944` — Merged。+1 / −1。
- ![PR](icons/git-pull-request.svg) [[cherry-pick] Update distro commit (release/1.118)](https://github.com/microsoft/vscode/pull/312949) `#312949` — Closed。+5 / −0。
- ![PR](icons/git-pull-request.svg) [[cherry-pick] Update CLI settings to disable default options for exit mode, autonomy](https://github.com/microsoft/vscode/pull/312943) `#312943` — Closed。+32 / −18。
- ![PR](icons/git-pull-request.svg) [Add GDPR tag to capi.assignmentcontext](https://github.com/microsoft/vscode/pull/312933) `#312933` — Merged。+1 / −0。
- ![PR](icons/git-pull-request.svg) [otel: support `gen_ai.response.time_to_first_chunk` in sqlite store](https://github.com/microsoft/vscode/pull/312938) `#312938` — Merged。+37 / −2。OTel GenAI セマンティック規約に合わせ vendor 属性とのデュアル読み取りを実装。`dmitrivMS` APPROVED。

---

### ![Issue](icons/issue-opened.svg) [Support marketplace specification in extensions.json](https://github.com/microsoft/vscode/issues/312980) `#312980`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `extensions.json` で marketplace 指定をサポートしてほしい要望。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メンテナ判断待ち。

---

### ![Issue](icons/issue-opened.svg) [VS Code on M5 Pro Macbook Pro crashes with error: The window terminated unexpectedly](https://github.com/microsoft/vscode/issues/312883) `#312883`
**ステータス:** Open | **ラベル:** (なし)

**概要:** M5 Pro の MacBook Pro で VS Code がウィンドウ終了エラーで落ちる報告。

**直近のアクティビティ:** ユーザー追記 2 件。

**次のアクション:** 再現条件詰め。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat fails in Remote‑SSH behind HTTP proxy (TypeError: noProxy.map is not a function)](https://github.com/microsoft/vscode/issues/307456) `#307456`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Remote-SSH 越しの HTTP proxy で `noProxy.map` が関数でないエラーが出る問題。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [just hangs and does nothing!!!!](https://github.com/microsoft/vscode/issues/312680) `#312680`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Insiders ビルドが応答しない報告。

**直近のアクティビティ:** ユーザー追記 4 件。

**次のアクション:** 再現条件確認。

---

### ![Issue](icons/issue-opened.svg) [PendingMigrationError: navigator is now a global in nodejs thrown during module load](https://github.com/microsoft/vscode/issues/312110) `#312110`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Node 側のグローバル `navigator` 化に伴うモジュールロード時エラー。

**直近のアクティビティ:** ユーザーコメント 1 件。

**次のアクション:** 関連 extension の対応待ち。

---

### ![Issue](icons/issue-opened.svg) [There is bug for read jsons in test insiders information](https://github.com/microsoft/vscode/issues/312977) `#312977`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Insiders テスト情報の JSON 読込バグ報告。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[macOS 26.3.1] VS Code Insiders 1.118.0 crashes on startup — network process crash](https://github.com/microsoft/vscode/issues/312720) `#312720`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** macOS 26.3.1 (Tahoe) で Insiders 1.118.0 起動時に network process が crash する。`#312734` の修正対象。

**直近のアクティビティ:** PR `#312734` でクローズ進行。

**次のアクション:** クローズ済み（PR 待ち）。

---

### ![Issue](icons/issue-opened.svg) [[json] include copies of the `package.json`, `jsconfig.json`, `eslint.json` schemas](https://github.com/microsoft/vscode/issues/206792) `#206792`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** `package.json` 等のスキーマを同梱してオフライン補完させたい長期 issue。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot stuck evaluating/analyzing/planning](https://github.com/microsoft/vscode/issues/312526) `#312526`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Copilot が evaluating/analyzing/planning 状態で停止する複数報告。

**直近のアクティビティ:** ユーザーコメント 3 件。

**次のアクション:** 再現条件絞り込み。

---

### ![Issue](icons/issue-opened.svg) [Copilot CLI with worktree mode made local changes](https://github.com/microsoft/vscode/issues/312976) `#312976`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `#312644` テスト中、worktree mode 有効時に worktree が作成されずローカルに変更が入る不具合。

**直近のアクティビティ:** `alexr00` が「再試行も同結果」と確認。

**次のアクション:** 再現ログ詳細確認中。

---

### ![Issue](icons/issue-opened.svg) [Whitelist Fabric DE extension for fileSearchProvider and textSearchProvider support](https://github.com/microsoft/vscode/issues/298654) `#298654`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Fabric DE 拡張を fileSearchProvider / textSearchProvider 対応にホワイトリスト追加要望。

**直近のアクティビティ:** ユーザーコメント 1 件。

**次のアクション:** メンテナ判断待ち。

---

### ![Issue](icons/issue-opened.svg) [copilot --resume uses the old name of the session](https://github.com/microsoft/vscode/issues/312696) `#312696`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `copilot --resume` 後にセッション名が古い名前で残るバグ。

**直近のアクティビティ:** 関連 `#312697`（rename 効果なし）と並走。

**次のアクション:** Copilot CLI 側の rename 反映ロジック修正。

---

### ![Issue](icons/issue-opened.svg) [Renaming sessions started from copilot cli has no effect](https://github.com/microsoft/vscode/issues/312697) `#312697`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Copilot CLI 起動セッションのリネームが反映されない。

**直近のアクティビティ:** ユーザーコメント 2 件。

**次のアクション:** 上記 #312696 と合わせて対応。

---

### ![Issue](icons/issue-opened.svg) [Default option for "Add Triggered Breakpoint" dialog](https://github.com/microsoft/vscode/issues/312509) `#312509`
**ステータス:** Open | **ラベル:** (なし)

**概要:** 「Add Triggered Breakpoint」ダイアログのデフォルトを「None」ではなく「直前に追加した breakpoint」にしてほしい要望。

**直近のアクティビティ:** `harishn1408` が AI 生成「Autonomous / Sovereign Solution Proposal」を 4 件連投。

**次のアクション:** メンテナ確認。

---

### ![Issue](icons/issue-opened.svg) [Review AsyncIterableObject usage: potential memory leaks and migration to AsyncIterable](https://github.com/microsoft/vscode/issues/256854) `#256854`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `AsyncIterableObject` のメモリリーク懸念と AsyncIterable への移行調査タスク。

**直近のアクティビティ:** ユーザーコメント 4 件。

**次のアクション:** 担当者割当待ち。

---

### ![Issue](icons/issue-opened.svg) [Disabled and enabled (workspace) extension Disable button dropdown contains both options](https://github.com/microsoft/vscode/issues/244138) `#244138`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Extension の Disable ボタンドロップダウンに有効/無効両方が出るバグ。

**直近のアクティビティ:** ユーザーコメント 4 件。

**次のアクション:** 修正方針決定待ち。

---

### ![Issue](icons/issue-opened.svg) [Go to symbol in workspace shortcut in Codespaces (browser) is CTRL-T](https://github.com/microsoft/vscode/issues/232381) `#232381`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Codespaces ブラウザ版で `Go to symbol in workspace` のショートカットが CTRL-T になる問題。

**直近のアクティビティ:** ユーザーコメント 4 件。

**次のアクション:** ブラウザ shortcut マップ調整。

---

### ![Issue](icons/issue-opened.svg) [Devcontainer setup ad-hoc parsing fails on Rocky 8.5](https://github.com/microsoft/vscode/issues/232159) `#232159`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Rocky 8.5 で devcontainer 設定の ad-hoc パースが失敗する報告。

**直近のアクティビティ:** ユーザーコメント 4 件。

**次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [insider install failed](https://github.com/microsoft/vscode/issues/312956) `#312956`
**ステータス:** Open | **ラベル:** (なし)

**概要:** macOS 15.7.4 / Apple M3 Pro で Insiders dmg のインストールが失敗する報告。

**直近のアクティビティ:** `peckz` も「昨日から Insiders ビルド壊れている、最新版に更新後 restart したらグレースクリーンしか見えない、フレッシュビルドでも同様」と再現を確認。

**次のアクション:** リリースエンジニアリング側調査。

---

### ![Issue](icons/issue-opened.svg) [unhandlederror-A provider for the command inlineChatWidget.statusPlaceholder](https://github.com/microsoft/vscode/issues/312792) `#312792`
**ステータス:** Open | **ラベル:** (なし)

**概要:** `inlineChatWidget.statusPlaceholder` の provider 重複エラー。`#312975` で修正中。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR `#312975` のマージで解消。

---

### ![Issue](icons/issue-opened.svg) [Contact your Admin for models keeps showing - but it is enabled](https://github.com/microsoft/vscode/issues/312415) `#312415`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Admin 連絡を促すメッセージが有効化済みでも表示され続ける。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メンテナ確認。

---

### ![Issue](icons/issue-opened.svg) [Change websockets to be ON by default](https://github.com/microsoft/vscode/issues/312325) `#312325`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** websockets をデフォルト ON にする提案。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Broken trust system on windows Dev Drive over ssh](https://github.com/microsoft/vscode/issues/312834) `#312834`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Windows Dev Drive を SSH 越しに使うと trust system が壊れる問題。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [Copilot chat crashes VS Code every 2 minutes with code 135 error](https://github.com/microsoft/vscode/issues/312100) `#312100`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Copilot chat が 2 分毎に code 135 で VS Code をクラッシュさせる。

**直近のアクティビティ:** ユーザーコメント 1 件。

**次のアクション:** ログ収集要望。

---

### ![Issue](icons/issue-opened.svg) [k suwr dunjg dudwojc](https://github.com/microsoft/vscode/issues/312971) `#312971`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** スパム / 誤投稿。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [New Agents app: Light mode](https://github.com/microsoft/vscode/issues/312582) `#312582`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** New Agents アプリの Light mode 対応。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Retried with Autopilot breaks the chat](https://github.com/microsoft/vscode/issues/309801) `#309801`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Autopilot を再試行すると chat が壊れる。

**直近のアクティビティ:** ユーザーコメント 1 件。

**次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [3 in 1: Can't set timeout, chat stuck with sending an image, no toolbar button](https://github.com/microsoft/vscode/issues/311626) `#311626`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 複数の chat 関連バグまとめ報告。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Global YOLO mode does not set new chats to 'Bypass Approvals'](https://github.com/microsoft/vscode/issues/311177) `#311177`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Global YOLO mode が新規 chat に Bypass Approvals を設定しない。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Make Copilot Chat input box collapsible to improve conversation readability](https://github.com/microsoft/vscode/issues/309514) `#309514`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat 入力欄を collapsible にして会話の可読性を上げる要望。

**直近のアクティビティ:** ユーザーコメント 1 件。

**次のアクション:** UX 検討。

---

### ![Issue](icons/issue-opened.svg) [Github Copilot issue with Claude model](https://github.com/microsoft/vscode/issues/311826) `#311826`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** GitHub Copilot で Claude モデル選択時の不具合。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Feedback: Misleading "Archive" Button in VSCode Copilot Chat](https://github.com/microsoft/vscode/issues/311492) `#311492`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Archive ボタンの挙動が誤解を招くフィードバック。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** UX 検討。

---

### ![Issue](icons/issue-opened.svg) [Microsoft.CodeAnalysis.LanguageServer.exe using 80% of cpu](https://github.com/microsoft/vscode/issues/312972) `#312972`
**ステータス:** Open | **ラベル:** (なし)

**概要:** C# 言語サーバが 80% CPU を消費する報告。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 拡張機能側調査。

---

### ![Issue](icons/issue-opened.svg) [Cmd+Backspace no longer deletes the line left of the cursor in the text editor](https://github.com/microsoft/vscode/issues/312221) `#312221`
**ステータス:** Open | **ラベル:** (なし)

**概要:** macOS の Cmd+Backspace でカーソル左の行削除が効かなくなった regression。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** keybinding 修正調査。

---

### ![Issue](icons/issue-opened.svg) [Batched confirmations should include "Allow all for this session"](https://github.com/microsoft/vscode/issues/308070) `#308070`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** バッチ確認に「セッション中は全許可」を追加する要望。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[Agents] Tool result output is truncated and expand toggle does not resize](https://github.com/microsoft/vscode/issues/308439) `#308439`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Agents の tool result が truncate され expand toggle が機能しない。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Batched confirmations could show a count of how many approvals there are for a given step](https://github.com/microsoft/vscode/issues/308067) `#308067`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** バッチ確認に承認数カウント表示を追加する要望。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Flaky test in MCP land](https://github.com/microsoft/vscode/issues/312973) `#312973`
**ステータス:** Open | **ラベル:** (なし)

**概要:** MCP 関連の flaky test 検出。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** テスト安定化。

---

### ![Issue](icons/issue-opened.svg) [Bug: Mode selector mid-conversation changes UI state but not session state](https://github.com/microsoft/vscode/issues/309275) `#309275`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 会話途中の mode selector が UI のみ変化し session state は変わらない。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Agent runs while sometimes stop buttons disappears](https://github.com/microsoft/vscode/issues/309204) `#309204`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Agent 実行中に Stop ボタンが消える。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Invalid string length issue reappeared](https://github.com/microsoft/vscode/issues/307616) `#307616`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 文字列長 invalid エラーの再発報告。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Carousel for tools could be grouped better](https://github.com/microsoft/vscode/issues/307964) `#307964`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** Tools の carousel UI のグルーピング改善要望。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Can't attach image](https://github.com/microsoft/vscode/issues/307083) `#307083`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 画像添付ができない不具合。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Enable extensions nag](https://github.com/microsoft/vscode/issues/308033) `#308033`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 拡張機能を有効化するための nag UI。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [unrecoverable endless loop trying to access vision](https://github.com/microsoft/vscode/issues/308063) `#308063`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** vision 関連の無限ループ。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [No Auto-Scroll When Selecting Text in Chat Window](https://github.com/microsoft/vscode/issues/307939) `#307939`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat ウィンドウでテキスト選択時の auto-scroll 不在。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** UX 検討。

---

### ![Issue](icons/issue-opened.svg) [chat flow stuck](https://github.com/microsoft/vscode/issues/307293) `#307293`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Chat flow が停止する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [Duplicate confirmation prompts](https://github.com/microsoft/vscode/issues/306511) `#306511`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 確認プロンプト重複表示。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [command snippets are shown and immediately hidden/removed](https://github.com/microsoft/vscode/issues/306415) `#306415`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** コマンド snippets が一瞬だけ表示されて消える。

**直近のアクティビティ:** クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Input/Ouput UI for GitHub Copilot tools collapses when clicked but it can't be expanded](https://github.com/microsoft/vscode/issues/306478) `#306478`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Copilot tools の I/O UI が collapse 後に展開できないバグ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** UX 修正検討。

---

## qmk/qmk_firmware

> 直近24時間は新規キーボード追加 PR の連投が中心。複数 PR が QMK メンテナの review changes_requested で停滞しており、stalebot で自動クローズも複数発生。

### ![PR](icons/git-pull-request.svg) [Add sector245/s245_streamdeck keyboard](https://github.com/qmk/qmk_firmware/pull/26013) `#26013`
**ステータス:** Open | **ラベル:** (なし)

**概要:** RP2040 ベースの 3x3 + 1 rotary encoder マクロパッド `S245 Streamdeck` 追加 +93 / −0。VIA・encoder map 対応。

**直近のアクティビティ:** `drashna` と `zvecr` が CHANGES_REQUESTED。`zvecr` は「ハードウェア確認用画像が欲しい」「`keyboards/handwired/sector245/s245/` に置き直し README コンパイル例も更新を」とリクエスト。

**次のアクション:** ファイル配置と画像追加。

---

### ![PR](icons/git-pull-request.svg) [Add szr35 keyboard](https://github.com/qmk/qmk_firmware/pull/25662) `#25662`
**ステータス:** Open | **ラベル:** (なし)

**概要:** SZRKBD `SZR35` キーボードサポート追加 +425 / −0。

**直近のアクティビティ:** `waffle87` と `zvecr` が CHANGES_REQUESTED、`drashna` は APPROVED。

**次のアクション:** 残レビュー反映。

---

### ![PR](icons/git-pull-request.svg) [Add support for Lemokey X1 keyboard](https://github.com/qmk/qmk_firmware/pull/26063) `#26063`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Keychron 系列 Lemokey X1 のサポート追加 +896 / −0。Keychron fork からの整理移植。

**直近のアクティビティ:** `github-actions[bot]` が「45 日無活動で stale 扱い、追加 30 日で close」と通知。

**次のアクション:** 投稿者の反応待ち。

---

### ![PR](icons/git-pull-request.svg) [Adds support for mode M75V2](https://github.com/qmk/qmk_firmware/pull/26059) `#26059`
**ステータス:** Open | **ラベル:** (なし)

**概要:** mode `M75V2` キーボードのサポート追加 +315 / −0。

**直近のアクティビティ:** `zvecr` CHANGES_REQUESTED、`drashna`・`Gondolindrim` のレビューコメント。

**次のアクション:** 修正反映待ち。

---

### ![PR](icons/git-pull-request.svg) [[Keyboard] Add Krado Industries Promenade RP24HS](https://github.com/qmk/qmk_firmware/pull/26172) `#26172`
**ステータス:** Open | **ラベル:** (なし)

**概要:** RP2040 化された Promenade RP24HS（hotswap 対応）追加 +254 / −0。

**直近のアクティビティ:** `drashna` CHANGES_REQUESTED、投稿者 `Feags` の応答コメント 2 件。

**次のアクション:** 修正反映。

---

### ![PR](icons/git-pull-request.svg) [Add Idle Builds Dinkey 32|30 keyboard](https://github.com/qmk/qmk_firmware/pull/26175) `#26175`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Idle Builds 製 Dinkey 32|30（split、ATmega32U4）追加 +155 / −0。

**直近のアクティビティ:** `drashna` のレビューコメント、投稿者 `IdleBuilds` の応答 2 件。

**次のアクション:** 修正反映。

---

### ![PR](icons/git-pull-request.svg) [Hybrid scroll for Ploopy Trackball](https://github.com/qmk/qmk_firmware/pull/26171) `#26171`
**ステータス:** Open (Draft) | **ラベル:** (なし)

**概要:** Ploopy Trackball のスクロールボタンをトグル / ホールド両対応にするドラフト +14 / −0。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** ドラフト整備中。

---

### ![PR](icons/git-pull-request.svg) [Feature/owlab link65](https://github.com/qmk/qmk_firmware/pull/26163) `#26163`
**ステータス:** Open | **ラベル:** (なし)

**概要:** Owlab LINK65 hotswap PCB 対応 +263 / −0。QMK 0.14.19 ベースのコードを 0.32 に整備。

**直近のアクティビティ:** `zvecr` CHANGES_REQUESTED 2 件。

**次のアクション:** 修正反映。

---

### ![PR](icons/git-pull-request.svg) [chore: merge from base](https://github.com/qmk/qmk_firmware/pull/26178) `#26178`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** ベースからの merge chore +118 / −3。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add a new RGB matrix effect which colors keys according to the type of the assigned keycode in the current layer](https://github.com/qmk/qmk_firmware/pull/26002) `#26002`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** keycode 種別ごとに色分けする RGB matrix effect 追加提案 +306 / −1。`RM_*` keycode で制御可能。

**直近のアクティビティ:** stalebot が 30 日無活動で自動クローズ。

**次のアクション:** クローズ済み。投稿者の re-open 待ち。

---

### ![PR](icons/git-pull-request.svg) [Add silly_group/meowy_pad](https://github.com/qmk/qmk_firmware/pull/25991) `#25991`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** 3x3 マクロパッド + SSD1306 OLED 構成 `MeowyPad` 追加 +165 / −0。

**直近のアクティビティ:** `drashna` CHANGES_REQUESTED の後、stalebot で自動クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Framework 16 ANSI White Keyboard Module addition](https://github.com/qmk/qmk_firmware/pull/25791) `#25791`
**ステータス:** Closed | **ラベル:** (なし)

**概要:** `#25241`・`#21722` のリバイバルとして Framework 16 ANSI White モジュール追加 +554 / −0。

**直近のアクティビティ:** `waffle87`・`zvecr` CHANGES_REQUESTED、`adophoxia` コメント、stalebot で自動クローズ。

**次のアクション:** クローズ済み。次回 revival 待ち。

---
