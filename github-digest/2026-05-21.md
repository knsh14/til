# GitHub ダイジェスト — 2026-05-21

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[fix(vim.secure): read() command injection vulnerability](https://github.com/neovim/neovim/pull/39918)** (neovim/neovim) — `vim.secure.read()` の View 経路で悪意のあるファイル名から任意の Ex コマンドを実行できるセキュリティ修正。`release-0.12` への backport #39922 も同日にマージされた。
2. ![Issue](icons/issue-opened.svg) **[uv audit fails with error decoding response body when OSV returns empty range events](https://github.com/astral-sh/uv/issues/19492)** (astral-sh/uv) — OSV が空の `{}` を返す異常レコードで `uv audit` がデコード失敗する問題。複数 PR（#19493 / #19496 / #19500）が並走し、astral 側で寛容パースを入れるか、OSV 側で修正させるかを巡る議論が活発。
3. ![PR](icons/git-pull-request.svg) **[Enable BYOK in air-gapped scenarios/without GitHub auth](https://github.com/microsoft/vscode/pull/317428)** (microsoft/vscode) — GitHub 認証なし環境でも BYOK チャットを動かせるようにする大型変更。23 ファイル、レビュー 9 件、関連 Issue 4 件を一括クローズする。
4. ![PR](icons/git-pull-request.svg) **[build_release: check file sizes in CI](https://github.com/commaai/openpilot/pull/38066)** (commaai/openpilot) — リリースビルドの肥大化を Jenkins 直前の GH CI で検知する仕組み。`CI=1` 環境変数とビルド対象解像度の関係を巡る、メンテナと寄稿者の応酬が続いている。
5. ![PR](icons/git-pull-request.svg) **[Rewrite the localization teams docs to improve legibility, and explicitly permit members who know each other](https://github.com/ghostty-org/ghostty/pull/12720)** (ghostty-org/ghostty) — ローカライズチーム参加ポリシーの全面書き直し。11 レビューが付き、`po/README_TRANSLATORS.md` の表現を巡って活発な議論が行われている。

---

## sunnypilot/sunnypilot

> mici タッチ UI の Cruise 設定追加や upstream 同期 PR が中心。UI レイアウト議論と upstream マージの大規模差分が並行している。

### ![Issue](icons/issue-opened.svg) [Sunnylink: Custom ACC Increment allow 0 value](https://github.com/sunnypilot/sunnypilot/issues/1845) `#1845`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 2023 Kia Niro EV のクルーズコントロールは短押しが単位刻み、長押しが端数丸めとして機能する。現在の sunnypilot は短押し増分後にカスタム値を加算する仕様のため、増分を 0 に設定して長押し丸めだけを残せるようにしてほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナによるトリアージとカスタム ACC 増分の許容値域見直しが必要。

---

### ![PR](icons/git-pull-request.svg) [ui/mici: add cruise settings layout](https://github.com/sunnypilot/sunnypilot/pull/1737) `#1737`
**ステータス:** Open | **ラベル:** ui

**概要:** sunnypilot mici タッチ UI に Cruise 設定レイアウトを追加。ICBM、DEC、SCC、カスタム ACC 増分、Speed Limit Assist のサブパネルを含み、上流の `NavScroller`、`BigParamControl`、PR #1734 の SP ウィジェットを再利用する。

**直近のアクティビティ:** holux-design から dev ブランチへの取り込み時期の問い合わせコメント 1 件。Copilot レビューが概要を整理し、その他 10 件のレビューコメントで実装詳細について議論中。

**変更ファイル:** `selfdrive/ui/sunnypilot/mici/layouts/cruise.py` の 1 ファイルで +201 行。

**次のアクション:** レビュアーからの追加フィードバック待ち。

---

### ![PR](icons/git-pull-request.svg) [Sync: `commaai/openpilot:master` → `sunnypilot/sunnypilot:master`](https://github.com/sunnypilot/sunnypilot/pull/1843) `#1843`
**ステータス:** Open | **ラベル:** なし

**概要:** 上流 `commaai/openpilot:master` を sunnypilot にマージするための定例同期 PR（本文なし）。100 ファイルにわたる大規模差分（+4535/-90901）。

**直近のアクティビティ:** `github-actions[bot]` の UI Preview コメントが mici/big とも差分ありを報告。

**変更ファイル:** `.github/`、`Jenkinsfile`、`.vscode/settings.json` など 100 ファイル。

**次のアクション:** UI 差分レビューと CI 通過確認。

---

### ![PR](icons/git-pull-request.svg) [Sync: `commaai/openpilot:master` → `sunnypilot/sunnypilot:master`](https://github.com/sunnypilot/sunnypilot/pull/1819) `#1819`
**ステータス:** Closed | **ラベル:** なし

**概要:** 旧版の定例同期 PR（本文なし）。

**直近のアクティビティ:** 作者 sunnyhaibin が PR #1843 に置き換えられた旨をコメントしクローズ。

**変更ファイル:** `SConstruct`、`cereal/log.capnp` ほか 92 ファイル。

**次のアクション:** 後継 PR #1843 を追跡。

---

## commaai/openpilot

> モデル切替（big/small）、panda パッケージ化、TX safety fuzzing、リリースサイズチェックなど機構の地盤整備が進行中。

### ![PR](icons/git-pull-request.svg) [Op model16 deep](https://github.com/commaai/openpilot/pull/38073) `#38073`
**ステータス:** Open | **ラベル:** なし

**概要:** modeld 周りの再構成と新規 `driving_off_policy.onnx`/`driving_vision.onnx` モデル投入を伴う作業 PR（タイトルどおり「深い」変更）。

**直近のアクティビティ:** プロセスリプレイ差分は 0 件、モデルレビューレポートが Bot により生成済み。

**変更ファイル:** `selfdrive/modeld/` 配下（policy/vision の ONNX、`compile_modeld.py`、`fill_model_msg.py` ほか 13 ファイル）。

**次のアクション:** モデル品質比較レポートのレビュー。

---

### ![PR](icons/git-pull-request.svg) [Use pandacan package for panda](https://github.com/commaai/openpilot/pull/38074) `#38074`
**ステータス:** Open | **ラベル:** なし

**概要:** panda を submodule から外し、PyPI 配布の `pandacan` パッケージとして取り込むための検証 PR。`SConstruct` を更新してインストール済み panda パッケージから `SConscript` を読む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.gitmodules`、`SConstruct`、`pyproject.toml`、`uv.lock`、`panda` gitlink。

**次のアクション:** 対応する `commaai/panda#2403` のマージ後に検証。

---

### ![PR](icons/git-pull-request.svg) [2026.05.20 fix compressed_vipc.py by using pyzmq for zmq client](https://github.com/commaai/openpilot/pull/38072) `#38072`
**ステータス:** Open | **ラベル:** tools

**概要:** `tools/camerastream/compressed_vipc.py` が `cereal.messaging` 経由で消える `msgq` ZMQ 実装に依存しているため、`pyzmq` を直接使うように書き換えるバグ修正。

**直近のアクティビティ:** プロセスリプレイ差分 0 件のレポートのみ。

**変更ファイル:** `tools/camerastream/compressed_vipc.py`。

**次のアクション:** メンテナレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [selfdrive: add fuzzy TX safety coverage for control-state transitions](https://github.com/commaai/openpilot/pull/38070) `#38070`
**ステータス:** Open | **ラベル:** CI / testing, car

**概要:** Issue #32425 で要望されていた、openpilot 側 TX と panda safety_tx_hook のミスマッチを Hypothesis ベースのファズで検出するテスト `test_panda_safety_tx_fuzzy` を追加。

**直近のアクティビティ:** プロセスリプレイ差分 0 件のみ。

**変更ファイル:** `selfdrive/car/tests/test_models.py`。

**次のアクション:** メンテナレビュー、TX 経路網羅性の評価。

---

### ![PR](icons/git-pull-request.svg) [should be Error not Exception](https://github.com/commaai/openpilot/pull/38071) `#38071`
**ステータス:** Open | **ラベル:** なし

**概要:** `system/hardware/tici/hardware.py` の例外型を `Exception` から `Error` に修正する 1 行 PR（本文なし）。

**直近のアクティビティ:** プロセスリプレイ差分 0 件のみ。

**変更ファイル:** `system/hardware/tici/hardware.py`。

**次のアクション:** 軽微な差分のためマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [initial usbgpu support](https://github.com/commaai/openpilot/pull/37906) `#37906`
**ステータス:** Merged | **ラベル:** なし

**概要:** USB GPU 接続時に big モデルを起動時にコンパイルし、走行開始時に big モデルへ切り替える初期サポート。コンパイル状態と利用状況を UI に表示する。

**直近のアクティビティ:** CI Bot がモデルリプレイのプロット、UI Preview の同一性レポート、プロセスリプレイ差分 0 件を報告。

**変更ファイル:** `common/file_chunker.py`、`release/build_release.sh`、`selfdrive/modeld/compile_modeld.py`、`selfdrive/assets/icons_mici/egpu.png` ほか 17 ファイル。

**次のアクション:** マージ済み、後続最適化（カメラ解像度ごとのビルド最適化）が #38008 で追跡。

---

### ![PR](icons/git-pull-request.svg) [build_release: check file sizes in CI](https://github.com/commaai/openpilot/pull/38066) `#38066`
**ステータス:** Open | **ラベル:** なし

**概要:** Jenkins に渡る前の GH CI 段階で `build_release.sh` のファイルサイズチェックを行う追加ステップ。

**直近のアクティビティ:** Armandpl と adeebshihadeh が `CI=1` の有無による big/small モデル両方ビルドのコスト差を議論。「`CI=1` を消すと片方しかビルドされない」「両方ビルドする必要があるのは何故か」のやり取り。

**変更ファイル:** `.github/workflows/tests.yaml`、`release/check-file-sizes.sh`。

**次のアクション:** `CI=1` の意味合いを整理した上で実装方針を確定。

---

### ![Issue](icons/issue-opened.svg) [test_models: add a test that fuzzes the tx messages](https://github.com/commaai/openpilot/issues/32425) `#32425`
**ステータス:** Open | **ラベル:** enhancement, bounty

**概要:** 既存の RX ファズに加え、TX 側 CAN メッセージもファズして panda と openpilot 間の `controlsAllowed` 不整合を検出するテストを追加するバウンティ。

**直近のアクティビティ:** liangtovi-debug が `/claim` と `/attempt` を宣言し、PR #38070 を提出。RX ファズの構造を踏襲し、状態遷移ジェネレータを TX 用に拡張する計画を提示。

**次のアクション:** PR #38070 のレビュー。

---

### ![PR](icons/git-pull-request.svg) [Op model16](https://github.com/commaai/openpilot/pull/38069) `#38069`
**ステータス:** Open | **ラベル:** なし

**概要:** model16 系統のもう 1 つの PR。`compile_modeld.py` と policy/vision モデルの更新を含む（テンプレートのみで本文の実体記述なし）。

**直近のアクティビティ:** モデルリプレイ差分、プロセスリプレイ差分、モデルレビューレポートを Bot が投稿。

**変更ファイル:** `selfdrive/modeld/` 配下 13 ファイル（policy/vision ONNX を含む）。

**次のアクション:** モデル比較結果のレビュー。

---

## commaai/opendbc

> Hyundai/Kia 車種ポートのバウンティ、車両安全性関連のリファクタ、Tesla DBC 仕様変更への追従が進む。

### ![Issue](icons/issue-opened.svg) [[$1000 bounty] Kia EV9 lateral port](https://github.com/commaai/opendbc/issues/1175) `#1175`
**ステータス:** Open | **ラベル:** car, hyundai, car port

**概要:** Kia EV9 のラテラル制御（操舵）ポートに対する $1000 バウンティ。最大ラテラル加速度の保証、警告/フォルトの不発生、滑らかなドライバ介入が条件。

**直近のアクティビティ:** zhaog100 が claim を宣言。

**次のアクション:** zhaog100 の作業進捗を追跡。

---

### ![PR](icons/git-pull-request.svg) [curvature steering safety](https://github.com/commaai/opendbc/pull/3399) `#3399`
**ステータス:** Open | **ラベル:** car, car safety

**概要:** ステアリング曲率ベースの safety チェックを追加（PR テンプレートのみで具体的な記述はテンプレートそのまま）。

**直近のアクティビティ:** `github-actions[bot]` の Car behavior report で master との挙動差分が検出されない旨を報告。

**変更ファイル:** `opendbc/car/lateral.py`、`opendbc/safety/lateral.h`、`opendbc/safety/safety.h`、`opendbc/safety/tests/common.py` ほか 7 ファイル（+304/-1）。

**次のアクション:** Dongle/Route 情報の提示と、実装の説明文追記。

---

### ![PR](icons/git-pull-request.svg) [[codex] move CAN ignition hook](https://github.com/commaai/opendbc/pull/3360) `#3360`
**ステータス:** Closed | **ラベル:** car safety

**概要:** CAN イグニッションフックの移動を試みる codex 自動生成 PR（本文なし）。

**直近のアクティビティ:** elkoled が後継 PR #3370 で対応した旨をコメント。

**変更ファイル:** `opendbc/safety/ignition.h`。

**次のアクション:** PR #3370 を追跡。

---

### ![PR](icons/git-pull-request.svg) [Tesla: Fix DAS_steeringControlType on newer firmware](https://github.com/commaai/opendbc/pull/3389) `#3389`
**ステータス:** Open | **ラベル:** DBC signals, car, tesla, car safety

**概要:** Tesla の新ファームウェアで `DAS_steeringControlType` フィールドが 2 ビットから 3 ビットに拡張された（値 4 = FSD 追加のため）件への対応。フリート全体で観測されている。

**直近のアクティビティ:** Car behavior report が TESLA_MODEL_Y/Model 3 の 20 セグメントテストで 2 件の差分を検出。

**変更ファイル:** `opendbc/car/tesla/carstate.py`、`opendbc/dbc/tesla_model3_party.dbc`、`opendbc/dbc/tesla_model3_party_legacy.dbc` ほか 10 ファイル（+572/-118）。

**次のアクション:** 検出された挙動差分の妥当性確認。

---

## commaai/panda

> pandacan PyPI パッケージとして配布できるよう、ファームウェアやビルド成果物を同梱する作業が進行中。

### ![PR](icons/git-pull-request.svg) [Include firmware and build files in pandacan package](https://github.com/commaai/panda/pull/2403) `#2403`
**ステータス:** Open | **ラベル:** なし

**概要:** `pandacan` ホイールに署名済みファームウェア、bootstub、packet ヘッダ、ボードソース/ビルドファイル、証明書、libpanda ビルドファイルを同梱し、Linux SPI トランスポート依存を `pandacan` 側に移す。下流からの依存を `panda` 内部に直接張らずに済むようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `pyproject.toml`、`python/__init__.py`。

**次のアクション:** 関連する `commaai/openpilot#38074` と連動してマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Include firmware in pandacan package](https://github.com/commaai/panda/pull/2402) `#2402`
**ステータス:** Closed | **ラベル:** なし

**概要:** ファームウェア成果物の同梱と SPI トランスポート依存格上げの先行 PR（#2403 に置き換え）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `pyproject.toml`、`python/__init__.py`。

**次のアクション:** 後継 PR #2403 を追跡。

---

## neovim/neovim

> セキュリティ修正（`vim.secure.read`）、UI2 メッセージ系の改善、`vim.pack` 周り、treesitter デフォルト化、API オプション拡張など多岐にわたる活発な更新。0.12 系への backport も多数。

### ![PR](icons/git-pull-request.svg) [refactor: introduce nvim_on internally](https://github.com/neovim/neovim/pull/39883) `#39883`
**ステータス:** Merged | **ラベル:** defaults, refactor, lsp, treesitter, diagnostic, snippet, plugin-editorconfig, ux-visibility, image, ui2

**概要:** `nvim_create_autocmd` の冗長さとコールバックのネストの深さを軽減するため、内部向け API `nvim_on` を導入し、内部で先行して利用感を確かめる。将来的に公開 API 化を視野に入れる。

**直近のアクティビティ:** justinmk・ggandor・echasnovski の間で `group`/`pattern`/`callback` の引数順序、`group` の存在意義（ソース二重読み防止のためのボイラープレート化問題）、`opts` の典型的なサイズ感などを議論。

**変更ファイル:** `runtime/lua/editorconfig.lua`、`runtime/lua/vim/_core/defaults.lua`、`runtime/lua/vim/diagnostic.lua` など 32 ファイル（+835/-1033）。

**次のアクション:** 内部利用フィードバックを集めつつ公開 API 化を検討。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): unable to stop display of very long message](https://github.com/neovim/neovim/pull/39823) `#39823`
**ステータス:** Open | **ラベル:** ui2

**概要:** 非常に長いメッセージの処理に時間がかかった際、視覚的フィードバックがなく中断もできない問題への対処。100ms 超のメッセージ書き込みでは最初の一部を表示しつつ CTRL-C を監視し、`'wrap'` 込みのテキスト高さを正確に計算する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_core/ui2/messages.lua`。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Doc: `vim.diagnostic.GetOpts` doesn't match `vim.diagnostic.Set`](https://github.com/neovim/neovim/issues/39921) `#39921`
**ステータス:** Open | **ラベル:** documentation

**概要:** `vim.diagnostic.GetOpts` の `lnum` は 0-indexed なのにドキュメント上は明示されておらず、`Set` 側の説明と整合しない問題。`nvim_win_get_cursor` が返す 1-indexed 行と混乱する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39908 で `docs: misc` として対応中。

---

### ![PR](icons/git-pull-request.svg) [docs: misc](https://github.com/neovim/neovim/pull/39908) `#39908`
**ステータス:** Open | **ラベル:** documentation

**概要:** Issue #39921 を含むドキュメント類の微修正をまとめた PR（本文 1 行）。

**直近のアクティビティ:** レビュー 1 件のみ、コメントなし。

**変更ファイル:** `runtime/doc/api.txt`、`runtime/doc/diagnostic.txt`、`runtime/doc/options.txt`、`runtime/lua/vim/diagnostic.lua`、`runtime/lua/vim/pos.lua`、`runtime/lua/vim/range.lua` ほか 9 ファイル。

**次のアクション:** レビュー完了後マージ。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(vim.secure): read() command injection vulnerability](https://github.com/neovim/neovim/pull/39922) `#39922`
**ステータス:** Merged | **ラベル:** target:release

**概要:** PR #39918 を `release-0.12` にバックポート。`vim.secure.read()` の任意コマンド実行脆弱性を修正する。

**直近のアクティビティ:** Bot による自動 backport、コメントなし。

**変更ファイル:** `runtime/lua/vim/secure.lua`、`test/functional/lua/secure_spec.lua`。

**次のアクション:** リリースに含めて配布。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add operation field to nvim_set_option_value](https://github.com/neovim/neovim/pull/39849) `#39849`
**ステータス:** Open | **ラベル:** api

**概要:** `nvim_set_option_value` に `operation` フィールド（`set`/`append`/`prepend`/`remove`）を追加し、`:set opt+=`、`:set opt-=` などのコマンド相当の操作を API で行えるようにする。

**直近のアクティビティ:** レビュー 10 件、コメントなし。

**変更ファイル:** `runtime/doc/api.txt`、`runtime/lua/vim/_core/options.lua`、`src/nvim/api/options.c` ほか 13 ファイル（+433/-250）。

**次のアクション:** レビュー収束待ち。

---

### ![Issue](icons/issue-opened.svg) [`nvim_buf_delete` is confusing, no lua equivalent of `:bdelete`](https://github.com/neovim/neovim/issues/33314) `#33314`
**ステータス:** Open | **ラベル:** api, complexity:low

**概要:** `nvim_buf_delete` がデフォルトで `:bwipeout` 相当の挙動をしてしまい、shada のマークなどを失う問題。`:bdelete` 相当の API がないという指摘。

**直近のアクティビティ:** Skillmon が `{ unload = true }` で `bdelete` と同等と判明したことを補足。wsdjeg が `unload` を消して `wipe`/`force` だけにする案を提示。

**次のアクション:** API シグネチャの再設計議論。

---

### ![PR](icons/git-pull-request.svg) [feat(defaults): enable treesitter for all included parsers](https://github.com/neovim/neovim/pull/32965) `#32965`
**ステータス:** Closed | **ラベル:** defaults

**概要:** 同梱パーサ（c/vimscript）でも treesitter をデフォルト有効化する PR。markdown は #37907 で別途有効化済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/news.txt`、`runtime/ftplugin/c.lua`、`runtime/ftplugin/markdown.lua`、`runtime/ftplugin/vim.lua`。

**次のアクション:** クローズ済み。後継アプローチを別 PR で検討。

---

### ![PR](icons/git-pull-request.svg) [fix(vim.secure): read() command injection vulnerability](https://github.com/neovim/neovim/pull/39918) `#39918`
**ステータス:** Merged | **ラベル:** ci:backport release-0.12

**概要:** `vim.secure.read()` の View 経路で `:sview` Ex コマンドに未エスケープのパスを連結していたため、悪意あるファイル名から追加 Ex コマンドを注入できた問題を `fnameescape()` で修正。Issue #39914 をクローズ。

**直近のアクティビティ:** Bot が `release-0.12` への backport PR #39922 作成を報告。

**変更ファイル:** `runtime/lua/vim/secure.lua`、`test/functional/lua/secure_spec.lua`。

**次のアクション:** 0.12 リリースへ反映済み。

---

### ![Issue](icons/issue-opened.svg) [`vim.secure.read()` View branch command injection vulnerability](https://github.com/neovim/neovim/issues/39914) `#39914`
**ステータス:** Closed | **ラベル:** security

**概要:** `vim.secure.read()` の View 分岐がパスを `sview` コマンドに連結する際にエスケープしないため、悪意あるファイル名で任意 Ex コマンドが実行可能と報告。ローカル動的検証で再現確認済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39918 と backport #39922 によりクローズ。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): use even safer and even better error() message conversion](https://github.com/neovim/neovim/pull/39920) `#39920`
**ステータス:** Open | **ラベル:** lua

**概要:** `setmetatable({}, {__tostring = error})` を `error()` に渡すと nvim がクラッシュする問題への耐性追加。

**直近のアクティビティ:** レビュー 1 件、コメントなし。

**変更ファイル:** `src/nvim/lua/executor.c`、`test/functional/lua/luaeval_spec.lua` ほか 6 ファイル。

**次のアクション:** レビューフィードバック待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(fs): use uv.fs_lstat fallback when uv.fs_scandir_next returns no type in M.dir](https://github.com/neovim/neovim/pull/39749) `#39749`
**ステータス:** Open | **ラベル:** filesystem

**概要:** Linux 系で Btrfs/ext2-4 以外のファイルシステムだと `uv.fs_scandir_next` が `dirent` の type を返さない場合がある。その際に `uv.fs_lstat` へフォールバックし、`fs_lstat` も失敗したら `"unknown"` を返すように修正。

**直近のアクティビティ:** abdulahmoda がモック方針を `n.exec_lua` での monkey patch ベースに切り替える提案。レビュー 10 件で議論継続中。

**変更ファイル:** `runtime/lua/vim/fs.lua`。

**次のアクション:** モック方針確定とテスト追加。

---

### ![PR](icons/git-pull-request.svg) [fix(channel): avoid GCC warnings in channel info](https://github.com/neovim/neovim/pull/39897) `#39897`
**ステータス:** Open | **ラベル:** needs:discussion, DO NOT MERGE

**概要:** GCC 15.2.0 + `-Werror` で `channel.c` の `freopen` 戻り値無視と `info_lines` の未使用パラメータ警告がエラー化する問題への対処。

**直近のアクティビティ:** barrettruth が nix での GCC 13/14/15 各バージョンでの再現状況を共有。bfredl は同じ警告が出ないことを不思議がり、ビルドコマンド全文の提示を依頼。

**変更ファイル:** `src/nvim/channel.c`。

**次のアクション:** ビルド環境差異の特定と判断。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): defer exit from fast callbacks](https://github.com/neovim/neovim/pull/39895) `#39895`
**ステータス:** Open | **ラベル:** lua

**概要:** fast event コールバックの最中での exit 処理を後段に遅延する Issue #39783 のクローズ。

**直近のアクティビティ:** barrettruth は学習目的の側面が強く修正が複雑だと自認。bfredl は fast callback 中での exit が必要なユースケースが見えないと指摘し、`event_teardown()` をスキップする簡易対応で良いのではと提案。

**変更ファイル:** `src/nvim/lua/executor.c`、`test/functional/core/startup_spec.lua`。

**次のアクション:** ユースケース整理後、簡易対応か本実装かを判断。

---

### ![PR](icons/git-pull-request.svg) [fix(fold): confusing fill](https://github.com/neovim/neovim/pull/39917) `#39917`
**ステータス:** Open | **ラベル:** なし

**概要:** Issue #39916 の `foldcolumn` 表示問題への修正（本文ほぼ空）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/drawline.c`、`test/functional/ui/fold_spec.lua`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(net): `:edit <uri>` should set buftype=nofile](https://github.com/neovim/neovim/pull/39919) `#39919`
**ステータス:** Merged | **ラベル:** target:release

**概要:** PR #39915 の `release-0.12` への backport。

**直近のアクティビティ:** Bot による自動マージ、コメントなし。

**変更ファイル:** `runtime/plugin/net.lua`。

**次のアクション:** リリースに含めて配布。

---

### ![Issue](icons/issue-opened.svg) [rename `al` text-object and add a better pair for `il`](https://github.com/neovim/neovim/issues/39893) `#39893`
**ステータス:** Open | **ラベル:** defaults, normal-mode, editor

**概要:** PR #39845 で追加された `al`（全行）と `il`（前後空白除く行内）のテキストオブジェクトについて、対称的な対が成り立っていないため `al` を別マッピングに移し、より良い対を導入したいという提案。

**直近のアクティビティ:** aidancz、mikejmcguirk、echasnovski、drowning-cat、superatomic が、線指向の `.` や `sailf`（mini.surround）など各種マッピング案、`vil`+`an`（incremental selection）の併用などを議論。

**次のアクション:** バッファ全体テキストオブジェクトの需要を見極めた上でデフォルト変更を検討。

---

### ![PR](icons/git-pull-request.svg) [fix(net): `:edit <uri>` should set buftype=nofile](https://github.com/neovim/neovim/pull/39915) `#39915`
**ステータス:** Merged | **ラベル:** ci:backport release-0.12

**概要:** `:edit <uri>` で開いたバッファの `buftype` を `nofile` に設定する。

**直近のアクティビティ:** justinmk と yochem が `ftp://` のように書き込み可能な net プロトコルへの対応について議論。`buftype=acwrite` での opt-in 可能性を確認しつつ、必要になれば容易に戻せるとして簡易対応で合意。

**変更ファイル:** `runtime/plugin/net.lua`。

**次のアクション:** マージ済み。書き込み対応はフォローアップで検討。

---

### ![Issue](icons/issue-opened.svg) [`:nospecial cmd [args...]` invokes cmd with literal args](https://github.com/neovim/neovim/issues/37862) `#37862`
**ステータス:** Open | **ラベル:** security, needs:discussion, cmdline-mode, editor

**概要:** `|` などのコマンドライン特殊文字を扱う際、`fnameescape()` ではコマンドごとの個別エスケープに頼らざるを得ない。`:nospecial cmd arg` のように生の引数で `:cmd` を呼ぶ仕組みを提案。

**直近のアクティビティ:** justinmk が Issue #39914 の脆弱性とも関連付け、`nvim_edit` の API 化は重いかもとコメント。

**次のアクション:** 設計議論を継続。

---

### ![Issue](icons/issue-opened.svg) [fold: confusing `foldcolumn` behavior](https://github.com/neovim/neovim/issues/39916) `#39916`
**ステータス:** Open | **ラベル:** なし

**概要:** `1 < foldcolumn < foldlevel()` の状況で、畳まれた fold 数の表示が複数桁で出てしまい、桁数の意味付けが理解しにくいという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ドキュメント整理と仕様明確化を伴う対応。PR #39917 で着手中。

---

### ![PR](icons/git-pull-request.svg) [fix(api): fail fast if `has_lua_imp` retval looks wrong](https://github.com/neovim/neovim/pull/39866) `#39866`
**ステータス:** Merged | **ラベル:** api

**概要:** `has_lua_imp` の戻り値が想定外な場合に早期失敗させる。PR #39837 関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/gen/gen_api_dispatch.lua`、`src/nvim/lua/executor.c`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(pack): show "Removed plugin" messages on one line](https://github.com/neovim/neovim/pull/39840) `#39840`
**ステータス:** Merged | **ラベル:** messages, packages

**概要:** `vim.pack` で複数プラグインを削除しても 1 つしか表示されない Issue #39835 を、全プラグイン名を 1 行に結合して表示する形で修正。`E5810`（`:packdel`）の場合は別 PR で対応予定。

**直近のアクティビティ:** echasnovski がマージ。

**変更ファイル:** `runtime/lua/vim/pack.lua`、`test/functional/plugin/pack_spec.lua`。

**次のアクション:** `:packupdate`/`:packdel` の `:silent` 対応を別議論で。

---

### ![PR](icons/git-pull-request.svg) [fix(float): keep the calling float focused on buf_delete](https://github.com/neovim/neovim/pull/39858) `#39858`
**ステータス:** Open | **ラベル:** なし

**概要:** フロートウィンドウから別バッファを削除したときにフォーカスがホルダーウィンドウに移ってしまう問題への対応。`switch_win_noblock()` を `buf_jump_open_win()` の代わりに使用する。Issue #39827 を修正。

**直近のアクティビティ:** justinmk が AI 生成の代替パッチを提示し、`close_windows` から情報を返す形の方が読みやすいと提案。seandewar は退避中につき軽くコメント。

**変更ファイル:** `src/nvim/buffer.c`、`test/functional/ui/float_spec.lua`。

**次のアクション:** 代替実装案を比較検討。

---

### ![Issue](icons/issue-opened.svg) [`vim.fs.{dir,find}`: allow callers to observe traversal errors](https://github.com/neovim/neovim/issues/39878) `#39878`
**ステータス:** Open | **ラベル:** なし

**概要:** `vim.fs.dir()` が `uv.fs_scandir()` のエラーを握り潰すため、`vim.fs.find()` で「マッチなし」と「読み取り不能」の区別がつかない。エラー観測 API を提供したいという要望。

**直近のアクティビティ:** KineticTactic が作業意向を表明。基底ディレクトリ自体が読めない場合の API 設計の悩みを共有。

**次のアクション:** API 設計案の固定。

---

### ![PR](icons/git-pull-request.svg) [fix(buffer-updates): ensure undo emits correct on_bytes arguments](https://github.com/neovim/neovim/pull/39911) `#39911`
**ステータス:** Closed | **ラベル:** なし

**概要:** `u_undoredo` のエクストマーク復元時に `kExtmarkNoUndo` フラグでも `buf_updates_send_splice` が発火し、ゴーストイベントが発生する問題への対応案。

**直近のアクティビティ:** ManojPanda3 が当初のガードが正常な編集の `on_bytes` を抑制してしまい CI 失敗したことを謝罪。justinmk は紐づく Issue にテストの手がかりがあるか確認。

**変更ファイル:** `src/nvim/extmark.c`。

**次のアクション:** 別アプローチでクローズ済み。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(messages): avoid recursive rtp build due to msg_show](https://github.com/neovim/neovim/pull/39913) `#39913`
**ステータス:** Merged | **ラベル:** target:release

**概要:** PR #39888 の `release-0.12` への backport。

**直近のアクティビティ:** Bot による自動 backport、コメントなし。

**変更ファイル:** `src/nvim/runtime.c`、`test/functional/core/startup_spec.lua`。

**次のアクション:** リリースに含めて配布。

---

### ![PR](icons/git-pull-request.svg) [fix(test): support multiple --filter-out](https://github.com/neovim/neovim/pull/39885) `#39885`
**ステータス:** Merged | **ラベル:** test

**概要:** `TEST_ARGS` に複数の `--filter-out` を指定できなくなった回帰修正。Lua パターンは `"A|B|C"` のような交替を解釈しないため、複数値を別経路で扱う。

**直近のアクティビティ:** justinmk が lgtm、lewis6991 に cc。

**変更ファイル:** `test/functional/harness/harness_spec.lua`、`test/harness.lua`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [plugin monitoring, active health check](https://github.com/neovim/neovim/issues/36403) `#36403`
**ステータス:** Open | **ラベル:** performance, diagnostic, ux-visibility, namespaces-scopes

**概要:** Neovim の healthcheck はパッシブで、プラグインがプロセスを多数起動して遅くなった場合や LSP の誤設定で言語サーバが乱立する事象を能動的に検知できない。低コストで高価値な能動監視の必要性を提起。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 関連の checkhealth 改善 PR #39876 と歩調を合わせて設計を進める。

---

### ![PR](icons/git-pull-request.svg) [feat(health): checkhealth improvements](https://github.com/neovim/neovim/pull/39876) `#39876`
**ステータス:** Merged | **ラベル:** lsp, ux-visibility

**概要:** filewatcher 情報を `:checkhealth` で表示、LSP healthcheck のクリーンアップ、現在バッファのハイライト、Nvim 標準チェックの上部表示、「Create bug report」ボタン追加など複数の checkhealth 改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_watch.lua`、`runtime/lua/vim/health.lua`、`runtime/lua/vim/lsp/health.lua` ほか 5 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(fold): virtual lines duplicate foldopen](https://github.com/neovim/neovim/pull/39912) `#39912`
**ステータス:** Merged | **ラベル:** target:release

**概要:** PR #39891 の `release-0.12` への backport。

**直近のアクティビティ:** Bot による自動 backport、コメントなし。

**変更ファイル:** `src/nvim/drawline.c`、`test/functional/ui/fold_spec.lua`。

**次のアクション:** リリース配布。

---

### ![PR](icons/git-pull-request.svg) [fix(messages): avoid recursive rtp build due to msg_show](https://github.com/neovim/neovim/pull/39888) `#39888`
**ステータス:** Merged | **ラベル:** runtime, ui-extensibility, messages, ci:backport release-0.12

**概要:** ランタイム検索パス構築開始時に保留メッセージがあると `msg_show` コールバックが再帰的に `runtime_search_path_validate()` を呼ぶ問題。メッセージを先にフラッシュして回避。Issue #39815 を修正。

**直近のアクティビティ:** Bot が backport PR #39913 作成を報告。

**変更ファイル:** `src/nvim/runtime.c`、`test/functional/core/startup_spec.lua`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [crash in `:runtime` with ui2 + invalid rtp + syntax](https://github.com/neovim/neovim/issues/39815) `#39815`
**ステータス:** Closed | **ラベル:** has:backtrace, bug-crash, ui2

**概要:** v0.12 への設定移行中、最小再現で `:runtime` 周りのクラッシュを発見。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39888 でクローズ。

---

### ![Issue](icons/issue-opened.svg) [fold: virtual lines duplicate `foldopen` in `foldcolumn`](https://github.com/neovim/neovim/issues/39892) `#39892`
**ステータス:** Closed | **ラベル:** folds, column

**概要:** fold 開始行の上の virt_lines 領域に `foldopen` 記号が `foldcolumn` で表示されてしまう問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39891 でクローズ。

---

### ![PR](icons/git-pull-request.svg) [refactor(pos,range): drop `to_cursor`, extract `vim.pos._util`](https://github.com/neovim/neovim/pull/39889) `#39889`
**ステータス:** Merged | **ラベル:** refactor, lsp, treesitter

**概要:** `vim.Pos`/`vim.Range` の大幅リファクタ。`vim.Range` 生成時の中間 `vim.Pos` 二重生成を避ける、`to_cursor` を削除、共通処理を `vim.pos._util` に切り出すなど。

**直近のアクティビティ:** clason が `vim.pos` のドキュメントに 0/1-indexed、byte/char、inclusive/exclusive、EOL 含む含まないなどの差異を一覧化したセクション追加を要望。ofseed が `:h api-indexing` の改訂で十分か、別タグで節を新設すべきかを議論。

**変更ファイル:** `runtime/lua/vim/lsp/inline_completion.lua`、`runtime/lua/vim/pos.lua`、`runtime/lua/vim/pos/_util.lua`、`runtime/lua/vim/range.lua`、`runtime/lua/vim/treesitter/_range.lua` ほか 10 ファイル（+566/-444）。

**次のアクション:** フォローアップでドキュメント整理。

---

### ![PR](icons/git-pull-request.svg) [fix(fold): virtual lines duplicate foldopen](https://github.com/neovim/neovim/pull/39891) `#39891`
**ステータス:** Merged | **ラベル:** folds, column, ci:backport release-0.12

**概要:** virt_lines が常に下の行の `foldcolumn` を継承していた問題を、fold の開始行であれば virt_lines には `foldcolumn` を表示しない形で修正。Issue #39892 を修正。

**直近のアクティビティ:** Bot が backport PR #39912 作成を報告。

**変更ファイル:** `src/nvim/drawline.c`、`test/functional/ui/fold_spec.lua`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Markdown rendering functionality.](https://github.com/neovim/neovim/issues/39910) `#39910`
**ステータス:** Open | **ラベル:** runtime, display, filetype

**概要:** Markdown 表示が VS Code のように装飾されないため、VS Code 風のプレビュー機能を組み込んでほしいという要望。

**直近のアクティビティ:** justinmk が Issue #31825 と実質重複、Issue #32539 のコメントが関連と回答。

**次のアクション:** 重複として既存議論に集約。

---

### ![PR](icons/git-pull-request.svg) [fix(warnings): annotate or ignore all uses of printf formatting](https://github.com/neovim/neovim/pull/39887) `#39887`
**ステータス:** Merged | **ラベル:** なし

**概要:** clang 22 の `format(printf, ...)` 属性追加示唆警告をすべて解消。`dialog_msg()` の NULL ハンドリングラッパは不要として削除、バッファサイズ仮定の代わりに `sizeof buff` を使用するなど整理。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/ex_cmds.c`、`src/nvim/ex_docmd.c`、`src/nvim/macros_defs.h`、`src/nvim/optionstr.c` ほか 6 ファイル。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [overlapping keymaps](https://github.com/neovim/neovim/issues/39909) `#39909`
**ステータス:** Closed | **ラベル:** closed:invalid

**概要:** 説明欄が空のテンプレートのみの Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** invalid としてクローズ済み。

---

### ![Issue](icons/issue-opened.svg) [vim.pack: removing multiple plugins shows no indication that more than one plugin was removed](https://github.com/neovim/neovim/issues/39835) `#39835`
**ステータス:** Closed | **ラベル:** messages, packages

**概要:** `vim.pack.del` で複数プラグイン削除しても 1 件しか表示されない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39840 でクローズ。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add ev.win to autocmd callback args](https://github.com/neovim/neovim/pull/39826) `#39826`
**ステータス:** Open | **ラベル:** api

**概要:** autocmd コールバックが実行時には `curwin` が既に変わっているケースがあるため、autocmd コンテキストでウィンドウを追跡し `ev.win` として公開する。WinNew/WinClosed/WinResized/WinScrolled で特に有用。Issue #25844 を修正。

**直近のアクティビティ:** レビュー 7 件、コメントなし。

**変更ファイル:** `runtime/doc/api.txt`、`src/nvim/api/autocmd.c`、`src/nvim/api/vim.c`、`src/nvim/api/win_config.c` ほか 48 ファイル（+310/-177）。

**次のアクション:** レビュー収束。

---

### ![PR](icons/git-pull-request.svg) [fix(api): avoid maybe-uninitialized warning in win_config](https://github.com/neovim/neovim/pull/39896) `#39896`
**ステータス:** Open | **ラベル:** api, needs:discussion, DO NOT MERGE

**概要:** GCC 15.2.0 + `-Werror` で `nvim_win_get_config` 内の `pos` が未初期化扱いされる警告のエラー化対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/api/win_config.c`。

**次のアクション:** GCC バージョン依存の評価。

---

### ![PR](icons/git-pull-request.svg) [fix(proc): avoid maybe-uninitialized spawn status](https://github.com/neovim/neovim/pull/39898) `#39898`
**ステータス:** Open | **ラベル:** needs:discussion, DO NOT MERGE

**概要:** `proc_spawn` のプロセス型 switch 後に `status` 未初期化扱いされる警告対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/event/proc.c`。

**次のアクション:** GCC バージョン依存の評価。

---

### ![PR](icons/git-pull-request.svg) [fix(ex_cmds): improve E77 glob expansion message](https://github.com/neovim/neovim/pull/39843) `#39843`
**ステータス:** Open | **ラベル:** AI assisted

**概要:** E77「Too many file names」エラーメッセージを、単一ファイルコマンド文脈で glob 展開が非サポートである旨を明示する形に改善。`:edit` の glob テストを追加。Issue #12139 をクローズ。

**直近のアクティビティ:** Copilot レビューが概要を整理。

**変更ファイル:** `runtime/doc/message.txt`、`src/nvim/errors.h`、`test/functional/ex_cmds/excmd_spec.lua`。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [vim.pos, vim.range followup 2](https://github.com/neovim/neovim/issues/39867) `#39867`
**ステータス:** Open | **ラベル:** lua

**概要:** `vim.pos`/`vim.range` 実装後の残作業トラッキング。仮想列/仮想行（vcol/vrow）取得 API、`row_offset`/`col_offset` の要否などを整理。

**直近のアクティビティ:** justinmk が `getregionpos()` と `nvim_win_text_height()` が関連すると追記。

**次のアクション:** 仮想位置取得 API の設計議論。

---

### ![Issue](icons/issue-opened.svg) [Add LSP client flag to pull diagnostics on save](https://github.com/neovim/neovim/issues/29792) `#29792`
**ステータス:** Open | **ラベル:** lsp

**概要:** 現在 `didChange` のたびに pull diagnostics を要求するが、eslint のように save 時のみ実行したいケースに対応するため、LSP クライアントに save 時 pull のフラグを追加する要望。

**直近のアクティビティ:** liskin が休暇中で PR 着手が遅れている旨を共有。MariaSolOs が PR #39894 で着手。justinmk が patch シンプルなので試して問題あれば差し戻す方針を提案。

**次のアクション:** PR #39894 のレビューと方針合意。

---

### ![Issue](icons/issue-opened.svg) [difftool: no split view for help files](https://github.com/neovim/neovim/issues/39905) `#39905`
**ステータス:** Closed | **ラベル:** closed:question, runtime, diff, filetype

**概要:** `git difftool` で help ファイルを比較すると単一ファイルしか開かれず、垂直 diff split が出ない問題。

**直近のアクティビティ:** deathbeam が `-d` フラグや特定コマンドで動作する例を提示。gx089 が解決を確認。

**次のアクション:** question としてクローズ。

---

### ![PR](icons/git-pull-request.svg) [docs: misc, custom text-object](https://github.com/neovim/neovim/pull/39877) `#39877`
**ステータス:** Merged | **ラベル:** documentation

**概要:** ドキュメント類の微修正をまとめた PR（本文なし）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/api.txt`、`runtime/doc/motion.txt`、`runtime/doc/news.txt`、`runtime/doc/remote.txt`、`runtime/doc/vim_diff.txt` ほか 8 ファイル。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): make --remote wait by default with +async opt-out](https://github.com/neovim/neovim/pull/37951) `#37951`
**ステータス:** Open | **ラベル:** needs:response, remote, AI assisted

**概要:** `--remote` を既定でブロッキング動作（対象サーバでのバッファ unload まで待機）にし、`+async` で非ブロッキングを明示する。`neovim-remote` 外部ツール不要を目指す。Issue #24788 をクローズ。

**直近のアクティビティ:** レビュー 20 件、コメントなし。

**変更ファイル:** `runtime/doc/cmdline.txt`、`runtime/doc/remote.txt`、`runtime/lua/vim/_core/editor.lua`、`src/nvim/api/events.c` ほか 14 ファイル（+818/-263）。

**次のアクション:** レビュー収束待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): flush change notifications on save](https://github.com/neovim/neovim/pull/39894) `#39894`
**ステータス:** Open | **ラベル:** lsp

**概要:** `_changetracking.lua` でサーバが `textDocumentSync.save` capability を出していなくても、保存時に保留中の debounced `didChange` をフラッシュするバグ修正（ruff など）。

**直近のアクティビティ:** justinmk と liskin が、本 PR が Issue #29792（save 時のみ pull diagnostics）を本当にクローズするのかを確認。実際は別問題のため #29792 のクローズは取り下げる方向。

**変更ファイル:** `runtime/lua/vim/lsp/_changetracking.lua`。

**次のアクション:** ラベリング修正後マージ。

---

### ![Issue](icons/issue-opened.svg) [LSP: Infinite Hang + 100% CPU on typing in Insert Mode](https://github.com/neovim/neovim/issues/36257) `#36257`
**ステータス:** Open | **ラベル:** lsp

**概要:** terraform のような大きな補完リストで Insert モード入力時にハング/100% CPU になる症状。LSP 無効化で再現しないため、LSP 周りの問題と推定される。

**直近のアクティビティ:** mattgallagher92 が別 LSP の最小再現リポジトリを提示。

**次のアクション:** 再現リポジトリでの調査。

---

### ![PR](icons/git-pull-request.svg) [feat(pack): code action for updating all plugins](https://github.com/neovim/neovim/pull/39848) `#39848`
**ステータス:** Closed | **ラベル:** packages

**概要:** `vim.pack` 用に「全プラグイン更新」コードアクション（ソースファイルスコープ）を追加する提案。

**直近のアクティビティ:** echasnovski と justinmk が、`g?` のような汎用パターンでヘルプを呼ぶ案や、フローティング/仮想テキストで一時的な UI ヒントを出すパターンの是非、`# Update` セクションへの「`:write` で確定」案内追加について長文で議論。

**変更ファイル:** `runtime/lua/vim/pack.lua`、`runtime/lua/vim/pack/_lsp.lua`、`test/functional/plugin/pack_spec.lua` ほか 5 ファイル。

**次のアクション:** クローズ済み、UI パターン議論を別アクションに引き継ぐ。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(mouse): mouse=n should not adjust visual selection](https://github.com/neovim/neovim/pull/39907) `#39907`
**ステータス:** Merged | **ラベル:** target:release

**概要:** PR #39429 の `release-0.12` への backport。

**直近のアクティビティ:** Bot による自動 backport、コメントなし。

**変更ファイル:** `src/nvim/mouse.c`、`src/nvim/ui.c`、`test/functional/ui/mouse_spec.lua`。

**次のアクション:** リリース配布。

---

### ![PR](icons/git-pull-request.svg) [backport: `opts.scope` in `vim.ui.input`](https://github.com/neovim/neovim/pull/39906) `#39906`
**ステータス:** Merged | **ラベル:** documentation, target:release

**概要:** PR #39570 の backport。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/lua/vim/lsp/buf.lua`、`runtime/lua/vim/ui.lua` ほか 5 ファイル。

**次のアクション:** リリース配布。

---

### ![PR](icons/git-pull-request.svg) [feat(autoread): use file watchers for real-time change detection](https://github.com/neovim/neovim/pull/37971) `#37971`
**ステータス:** Open | **ラベル:** なし

**概要:** `'autoread'` を file watchers ベースのリアルタイム検出にする長年の要望（#1380）への新実装。`'autoread'` を素直に動かす方針。

**直近のアクティビティ:** レビュー 15 件、コメントなし。

**変更ファイル:** `runtime/doc/options.txt`、`runtime/lua/nvim/autoread.lua`、`runtime/plugin/autoread.lua`、`src/nvim/options.lua`、`test/functional/options/autoread_spec.lua` ほか 8 ファイル（+422/-3）。

**次のアクション:** レビュー収束。

---

### ![PR](icons/git-pull-request.svg) [fix(mouse): mouse=n should not adjust visual selection](https://github.com/neovim/neovim/pull/39429) `#39429`
**ステータス:** Merged | **ラベル:** mouse, ci:backport release-0.12

**概要:** `mouse=n` でドラッグやダブル/トリプル/クアッドクリックが visual mode に影響していた問題を、MOUSE_VISUAL を明示チェックして抑止。

**直近のアクティビティ:** Bot が backport PR #39907 作成を報告。

**変更ファイル:** `src/nvim/mouse.c`、`src/nvim/ui.c`、`test/functional/ui/mouse_spec.lua`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Regression: dragging mouse with mouse=n goes to visual mode](https://github.com/neovim/neovim/issues/15007) `#15007`
**ステータス:** Closed | **ラベル:** bug-regression, mouse, has:bisected

**概要:** `mouse=n` でドラッグが visual mode に入る回帰報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39429 でクローズ。

---

### ![Issue](icons/issue-opened.svg) [nvim_exec_autocmds should execute in the context of the passed "buffer" arg](https://github.com/neovim/neovim/issues/35393) `#35393`
**ステータス:** Closed | **ラベル:** events

**概要:** `nvim_exec_autocmds({ buf=… })` が暗黙にそのバッファコンテキストで実行されるべきという改善要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39061 でクローズ。

---

### ![PR](icons/git-pull-request.svg) [fix(api): make `nvim_exec_autocmds({ buf = ... })` use target buffer context](https://github.com/neovim/neovim/pull/39061) `#39061`
**ステータス:** Merged | **ラベル:** api

**概要:** `nvim_exec_autocmds` が `buf` を `<abuf>` 等には使うものの、その「中」では実行していなかった問題への対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/api/autocmd.c`、`src/nvim/autocmd.c`、`src/nvim/message.c`、`src/nvim/terminal.c` ほか 9 ファイル（+206/-31）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Option for the `confirm` option to always confirm Neovim exit, regardless of buffer status](https://github.com/neovim/neovim/issues/17256) `#17256`
**ステータス:** Open | **ラベル:** options

**概要:** バッファの保存状態に関係なく終了確認を出すオプションの要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 要望の優先度評価。

---

### ![Issue](icons/issue-opened.svg) [provide VertSplitNC for splits not adjacent to the current window](https://github.com/neovim/neovim/issues/12608) `#12608`
**ステータス:** Open | **ラベル:** has:vim-patch, highlight

**概要:** 現在ウィンドウに隣接しない垂直 split の境界線にも独自ハイライト（`VertSplitNC`）を割り当てたいという要望。

**直近のアクティビティ:** zeertzjq が関連する Vim 側のコミット群（c72196・2252d2c・25e9fc44 ほか）を共有。

**次のアクション:** Vim 側パッチの取り込み判断。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(lua): make fields of `nvim.spellfile.Opts` optional](https://github.com/neovim/neovim/pull/39904) `#39904`
**ステータス:** Merged | **ラベル:** target:release

**概要:** PR #39902 の `release-0.12` への backport。

**直近のアクティビティ:** Bot による自動 backport、コメントなし。

**変更ファイル:** `runtime/doc/plugins.txt`、`runtime/lua/nvim/spellfile.lua`。

**次のアクション:** リリース配布。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): make fields of `nvim.spellfile.Opts` optional](https://github.com/neovim/neovim/pull/39902) `#39902`
**ステータス:** Merged | **ラベル:** lua, spell, ci:backport release-0.12

**概要:** `nvim.spellfile.Opts` の型注釈が全フィールド必須になっていたが、実コードでは省略可能であった不整合を修正。

**直近のアクティビティ:** Bot が backport PR #39904 作成を報告。

**変更ファイル:** `runtime/doc/plugins.txt`、`runtime/lua/nvim/spellfile.lua`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [LSP: purescript-language-server "Sort/reformat imports" code action crashes](https://github.com/neovim/neovim/issues/39884) `#39884`
**ステータス:** Closed | **ラベル:** ux, lsp

**概要:** purescript-language-server の「Sort/reformat imports」コードアクションが `workspace_edit`（userdata）への index アクセスで失敗する。

**直近のアクティビティ:** justinmk がエラーメッセージ改善の余地に言及。MariaSolOs はサーバ側が LSP 仕様に従っていないとしてクローズ。

**次のアクション:** より良いエラー表示は別途検討。

---

### ![PR](icons/git-pull-request.svg) [ci: bump the github-actions group across 1 directory with 2 updates](https://github.com/neovim/neovim/pull/39901) `#39901`
**ステータス:** Merged | **ラベル:** ci, dependencies, github_actions

**概要:** `actions/create-github-app-token` を 3.1.1→3.2.0、`korthout/backport-action` を 4.5.1→4.5.2 に更新する Dependabot PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/backport.yml`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(runtime): conceal paths in help and man table of contents loclist](https://github.com/neovim/neovim/pull/33764) `#33764`
**ステータス:** Merged | **ラベル:** runtime, treesitter

**概要:** TOC タイトル変更で qf シンタックスの path conceal が効かなくなっていた問題。`qf_toc` キー設定後にシンタックスファイルを再ロードするように修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/man.lua`、`runtime/lua/vim/treesitter/_headings.lua`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): shell cmd stdout in Visual mode](https://github.com/neovim/neovim/pull/39786) `#39786`
**ステータス:** Open | **ラベル:** ui2

**概要:** Visual mode で実行したシェルコマンドの stdout が msg ウィンドウへ即時表示されない、`Press any key to continue` プロンプトが残る問題への対処。Issue #38273 を修正。

**直近のアクティビティ:** przepompownia が luukvbaal に対し、テストでの再現が困難なので必要なら PR を引き取って欲しい旨をコメント。

**変更ファイル:** `src/nvim/ex_cmds.c`、`src/nvim/os/shell.c`、`test/functional/ui/messages2_spec.lua`。

**次のアクション:** メンテナの引き継ぎ判断。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39776) `#39776`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** `src/nvim/version.c` の Vim パッチ追跡更新（Bot による自動 PR、本文なし）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/version.c`。

**次のアクション:** 通常通りメンテナ確認。

---

### ![PR](icons/git-pull-request.svg) [feat(runtime): add directory buffer for editing directories](https://github.com/neovim/neovim/pull/39723) `#39723`
**ステータス:** Open | **ラベル:** runtime

**概要:** netrw 依存だった `:edit <dir>`/`nvim <dir>` のローカルディレクトリ表示を、`filetype=directory` の組み込み Lua ディレクトリリスティングに置き換える。netrw は `:Explore` やリモート/アーカイブ用に残す。

**直近のアクティビティ:** superatomic が `bufhidden=unload` だと再訪時にシンタックスハイライトが壊れる点を指摘し、`hide` への切替を提案。barrettruth は UI 変更が未対応、`OptionSet` autocmd の `buftype` で発火しない問題などを共有。

**変更ファイル:** `runtime/lua/nvim/directory.lua`、`runtime/pack/dist/opt/netrw/doc/netrw.txt`、`runtime/plugin/directory.lua`、`runtime/syntax/directory.vim` ほか 9 ファイル（+755/-105）。

**次のアクション:** UI 変更とイベント発火問題の解決。

---

### ![Issue](icons/issue-opened.svg) [Shift modifier on input 0-9 blocked in VSCode terminal when upgrading from Neovim 0.11.7 to 0.12.0](https://github.com/neovim/neovim/issues/38651) `#38651`
**ステータス:** Open | **ラベル:** status:blocked-external, tui, input, mappings

**概要:** Neovim 0.12.0 アップグレード後、VS Code 統合ターミナルで Shift+数字（!@#$%^&*()）が押下できなくなる回帰。Windows Terminal では再現しない。

**直近のアクティビティ:** zeertzjq が 0.11.7 が「Report event types」を使っていなかったことを指摘。dbarnett は xterm.js の修正 PR `xtermjs/xterm.js#5791` がマージされた場合の再リリース手順を確認。

**次のアクション:** xterm.js/VS Code 側の対応待ち。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0502: runtime(netrw): bookmark handling can be improved](https://github.com/neovim/neovim/pull/39900) `#39900`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** netrw の bookmark 機能改善。count を伴わない `gb`/`mB` の挙動などを整理する Vim パッチの取り込み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/pack/dist/opt/netrw/autoload/netrw.vim`、`runtime/pack/dist/opt/netrw/doc/netrw.txt`、`test/old/testdir/test_plugin_netrw.vim`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:d004cc4: runtime(htmldjango): Remove unnecessary code.](https://github.com/neovim/neovim/pull/39899) `#39899`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** htmldjango シンタックスで django.vim/html.vim から継承される `djangoOperators` の再宣言を削除。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/syntax/htmldjango.vim`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ci: bump korthout/backport-action from 4.5.1 to 4.5.2 in the github-actions group across 1 directory](https://github.com/neovim/neovim/pull/39875) `#39875`
**ステータス:** Closed | **ラベル:** ci, dependencies, github_actions

**概要:** `korthout/backport-action` を 4.5.1→4.5.2 に更新する Dependabot PR。

**直近のアクティビティ:** Dependabot が #39901 にスーパーシードされた旨を通知。

**変更ファイル:** `.github/workflows/backport.yml`。

**次のアクション:** クローズ。

---

### ![Issue](icons/issue-opened.svg) [vim.fn.filereadable returns 0 or 1 despite docs saying it returns true or false.](https://github.com/neovim/neovim/issues/39890) `#39890`
**ステータス:** Closed | **ラベル:** なし

**概要:** `vim.fn.filereadable` が boolean ではなく `0`/`1` を返すという指摘。

**直近のアクティビティ:** zeertzjq がドキュメントには `TRUE`/`FALSE`（大文字 = 0/1）と書かれており `true`/`false`（lua boolean）ではないと回答。

**次のアクション:** クローズ済み。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> ローカライズ（po）追加、snap パッケージング修正、設定編集 UX 改善、CI/vouch まわりの自動化が並行して進行。

### ![PR](icons/git-pull-request.svg) [vouch: enable auto-locking closed issues](https://github.com/ghostty-org/ghostty/pull/12752) `#12752`
**ステータス:** Merged | **ラベル:** ci

**概要:** vouch-check-issue ワークフローで、クローズ済み Issue の自動ロックを有効化する CI 設定追加。

**直近のアクティビティ:** mitchellh がタグカットを示唆しつつ approve。

**変更ファイル:** `.github/workflows/vouch-check-issue.yml`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Config option for default button in close-surface confirmation dialog](https://github.com/ghostty-org/ghostty/issues/12751) `#12751`
**ステータス:** Closed | **ラベル:** なし

**概要:** `confirm-close-surface` ダイアログで Cancel/Close のうちどちらをデフォルトフォーカスにするかを設定可能にしてほしいという要望。

**直近のアクティビティ:** `ghostty-vouch[bot]` がメンテナ以外からの Issue は受け付けない方針に基づき自動クローズ、Discussion での議論を案内。

**次のアクション:** Discussion で再度提案。

---

### ![PR](icons/git-pull-request.svg) [snap: bundle gstreamer plugins so audio bell works](https://github.com/ghostty-org/ghostty/pull/12676) `#12676`
**ステータス:** Open | **ラベル:** packaging/snap

**概要:** snap ビルドで `bell-features = audio` を使ってもサウンドが鳴らない問題。snap が `libgstcoreelements.so`/`libgstcoretracers.so` しか同梱しておらず、再生・デコード・音声シンクのプラグインが揃っていないため、GStreamer プラグインをバンドルする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `snap/local/launcher`、`snap/snapcraft.yaml`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [snap: export TERMINFO_DIRS so child shells find xterm-ghostty](https://github.com/ghostty-org/ghostty/pull/12662) `#12662`
**ステータス:** Merged | **ラベル:** packaging/snap

**概要:** snap 版 Ghostty で `clear` 等が `terminals database is inaccessible` で失敗する問題。launcher で `TERMINFO_DIRS` を明示エクスポートし、snap 同梱の terminfo を ncurses が見つけられるようにする。自動クローズされた #12303 と同じ修正を再提出。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `snap/local/launcher`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build(highway): require `apple_sdk` for darwin builds](https://github.com/ghostty-org/ghostty/pull/12725) `#12725`
**ステータス:** Merged | **ラベル:** なし

**概要:** darwin ビルドで `apple_sdk` を必須化（特に iOS シミュレータの x86 ビルドで必要）。

**直近のアクティビティ:** mitchellh が CI が緑ならよしとコメント。bo2themax が APPROVED。

**変更ファイル:** `pkg/highway/build.zig`、`pkg/highway/build.zig.zon`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [i18n: add Galician Translation](https://github.com/ghostty-org/ghostty/pull/12674) `#12674`
**ステータス:** Open | **ラベル:** translations

**概要:** Galician 言語（gl）の翻訳ファイルを追加（本文なし）。

**直近のアクティビティ:** 00-kat が、ビルドシステムと CODEOWNERS にロケールを追加する必要があると `README_TRANSLATORS.md` を案内。

**変更ファイル:** `po/gl.po`。

**次のアクション:** ビルドシステム/CODEOWNERS 追加。

---

### ![PR](icons/git-pull-request.svg) [i18n: add Vietnamese translation](https://github.com/ghostty-org/ghostty/pull/8912) `#8912`
**ステータス:** Merged | **ラベル:** translations

**概要:** ベトナム語の翻訳追加。

**直近のアクティビティ:** 00-kat が `vi_VN` ローカライズチームへの招待手続きを案内し、tuananh が招待見逃しのため再送依頼。dungdm93 がネイティブ視点で LGTM レビュー。

**変更ファイル:** `CODEOWNERS`、`po/vi.po`、`src/os/i18n_locales.zig`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Rewrite the localization teams docs to improve legibility, and explicitly permit members who know each other](https://github.com/ghostty-org/ghostty/pull/12720) `#12720`
**ステータス:** Open | **ラベル:** documentation, translations

**概要:** ローカライズチーム参加ポリシーのドキュメントを全面書き直し。少数言語話者寄りの表現を緩めつつ、互いに知り合いのメンバ参加を明示的に許可する。

**直近のアクティビティ:** pluiedev が「感嘆符は確かに多すぎたが…」とコメントしつつ CHANGES_REQUESTED。他 10 件のレビューが続いている。

**変更ファイル:** `po/README_TRANSLATORS.md`。

**次のアクション:** レビュー収束。

---

### ![PR](icons/git-pull-request.svg) [core/gtk: allow editing Ghostty config in a Ghostty window](https://github.com/ghostty-org/ghostty/pull/11905) `#11905`
**ステータス:** Open | **ラベル:** gtk

**概要:** `open_config` キーバインドを拡張し、`$EDITOR`/`$VISUAL` で指定したエディタを Ghostty 内の新規ウィンドウで起動して設定を編集できるようにする。

**直近のアクティビティ:** 00-kat が翻訳ファイル変更のマージブロック回避のため APPROVED（コード未確認）。bo2themax が macOS で `keybind = cmd+/=open_config:new_window` の動作確認を APPROVED。

**変更ファイル:** `include/ghostty.h`、各言語の `.po` ファイル、`po/com.mitchellh.ghostty.pot` ほか 43 ファイル（+1123/-591）。

**次のアクション:** メンテナのコードレビュー。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/12746) `#12746`
**ステータス:** Merged | **ラベル:** なし

**概要:** `ghostty-vouch[bot]` による VOUCHED リスト更新。Discussion #12745 のコメントで @jcollie が @mjbommar を vouch。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/VOUCHED.td`。

**次のアクション:** マージ済み。

---

## astral-sh/uv

> `uv audit` の OSV 異常レコード対応、`uv-build` のエントリポイント検証強化、Hint ハンドリング統一など全方位での改善が進行。

### ![Issue](icons/issue-opened.svg) [add pypi vulnerability service as option to uv audit](https://github.com/astral-sh/uv/issues/19505) `#19505`
**ステータス:** Open | **ラベル:** enhancement

**概要:** OSV 障害時に備えて、`uv audit` のバックエンドとして PyPI 自体の脆弱性サービスも選択できるようにしてほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** プロバイダ抽象化の検討。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate Bot による依存関係ダッシュボードの定期更新 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 自動運用に従う。

---

### ![PR](icons/git-pull-request.svg) [Ensure that incompatible wheel hints do not leak secrets](https://github.com/astral-sh/uv/pull/19504) `#19504`
**ステータス:** Merged | **ラベル:** bug

**概要:** 互換性のない wheel に関するヒントメッセージから秘密情報を漏らさないようにする修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-installer/src/plan.rs`、`crates/uv/tests/it/pip_sync.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Test `create_junction` changes from #19402](https://github.com/astral-sh/uv/pull/19487) `#19487`
**ステータス:** Open | **ラベル:** internal

**概要:** `ALT_FS` をループバック SMB として用意し、`create_junction` をテストするインフラ追加。`ALT_FS` に依存する他テストの網羅性も副次的に高まる。

**直近のアクティビティ:** astral-sh-bot がテストインベントリ差分（+4/-0）を報告。

**変更ファイル:** `.github/workflows/test.yml`、`crates/uv-fs/src/lib.rs`。

**次のアクション:** Windows CI での挙動確認。

---

### ![PR](icons/git-pull-request.svg) [Add first-party line wrapping](https://github.com/astral-sh/uv/pull/17535) `#17535`
**ステータス:** Open | **ラベル:** なし

**概要:** PR #17110 で miette が行いっていた行ラップ処理がなくなる回帰を防ぐため、uv 内で一級の行ラッピング機構を導入。

**直近のアクティビティ:** Bot がテストインベントリ差分（+9/-1）を報告。

**変更ファイル:** `crates/uv-errors/src/lib.rs`、`crates/uv-publish/src/lib.rs` ほか 29 ファイル（+750/-318）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Vendor packse scenarios and serve them from an in-memory index](https://github.com/astral-sh/uv/pull/18084) `#18084`
**ステータス:** Open | **ラベル:** testing, build:skip-release, build:skip-docker

**概要:** packse シナリオを uv 内に取り込み、in-memory index から供給する大規模変更（+7762/-3455、100 ファイル）。

**直近のアクティビティ:** Bot がテストインベントリ差分（+1/-0）を報告。

**変更ファイル:** `crates/uv-dev/src/generate_scenarios.rs`、`crates/uv-test/src/find_links.rs`、`crates/uv-test/src/http_server.rs` ほか。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Allow environment variables that take a list to be empty](https://github.com/astral-sh/uv/pull/19503) `#19503`
**ステータス:** Open | **ラベル:** bug

**概要:** リストを受ける環境変数が空文字列のとき検証エラーで落ちる Issue #19499 への修正。

**直近のアクティビティ:** Bot がテストインベントリ差分（+4/-0）を報告。

**変更ファイル:** `crates/uv-cli/src/lib.rs`、`crates/uv/src/settings.rs`、`crates/uv/tests/it/sync.rs` ほか 8 ファイル。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add support for `uv audit --fix`](https://github.com/astral-sh/uv/pull/19497) `#19497`
**ステータス:** Open | **ラベル:** なし

**概要:** Issue #19428 を受けて、minor/patch 互換の修正バージョンを `uv.lock` に反映する `uv audit --fix` の MVP 実装。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-audit/src/fix.rs`、`crates/uv-audit/src/types.rs`、`crates/uv/src/commands/project/audit.rs`、`crates/uv/tests/it/audit.rs` ほか 7 ファイル。

**次のアクション:** 複数脆弱性のあるパッケージ対応と `pyproject.toml` 更新方針の議論。

---

### ![PR](icons/git-pull-request.svg) [Add a benchmark interface to the `uv` crate](https://github.com/astral-sh/uv/pull/19486) `#19486`
**ステータス:** Open | **ラベル:** performance, testing

**概要:** `uv` クレートにベンチマーク用 IF を追加（本文なし）。

**直近のアクティビティ:** Bot がテストインベントリに変更なしと報告。

**変更ファイル:** `crates/uv-bench/Cargo.toml`、`crates/uv-bench/benches/invocation.rs`、`crates/uv/src/lib.rs` ほか 5 ファイル。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Allow passing `--runs` to hyperfine in the benchmarking harness](https://github.com/astral-sh/uv/pull/19485) `#19485`
**ステータス:** Merged | **ラベル:** bug, internal

**概要:** hyperfine の `--min-runs` と `--runs` 同時指定エラーを避けるため、`--min-runs` を条件付きデフォルトに。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `scripts/benchmark/src/benchmark/resolver.py`、`scripts/benchmark/src/benchmark/tools.py`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fall back to keyring.exe when WSL interop is available](https://github.com/astral-sh/uv/pull/18580) `#18580`
**ステータス:** Open | **ラベル:** なし

**概要:** WSL で `keyring` が見つからず `WSL_INTEROP` 環境変数があるとき、`keyring.exe` にフォールバックする。

**直近のアクティビティ:** cataggar が zanieb/charliermarsh に再レビュー要請。codspeed-hq Bot がパフォーマンス無変化と報告。

**変更ファイル:** `crates/uv-auth/src/keyring.rs`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [docs: Clarify that `uv run python` opens an interactive REPL](https://github.com/astral-sh/uv/pull/19501) `#19501`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv run python` が引数なしでインタラクティブ REPL に入る挙動の補足ドキュメント追加。Issue #6548 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/concepts/projects/run.md`、`docs/guides/projects.md`。

**次のアクション:** マージ。

---

### ![Issue](icons/issue-opened.svg) [ghr install astral-sh/uv](https://github.com/astral-sh/uv/issues/19502) `#19502`
**ステータス:** Open | **ラベル:** show-and-tell

**概要:** cataggar 作の GitHub Releases 用ツール `ghr` を紹介する Show-and-tell。チェックサム検証、minisig/sigstore 対応の uv インストール手段。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** コミュニティ紹介として参照。

---

### ![PR](icons/git-pull-request.svg) [Refactor hint handling](https://github.com/astral-sh/uv/pull/18090) `#18090`
**ステータス:** Merged | **ラベル:** error messages

**概要:** ヒントを一級概念として扱う抽象化を追加。`Hints` 構造体と `Hint` trait を導入し、エラー型から遅延構築できるように移行。

**直近のアクティビティ:** Bot がテストインベントリ差分（+10/-0）、codspeed-hq がパフォーマンス無変化を報告。

**変更ファイル:** `crates/uv-build-backend/src/lib.rs`、`crates/uv-build-frontend/src/error.rs`、`crates/uv-dispatch/src/lib.rs` ほか 79 ファイル（+2237/-1134）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [uv audit fails with error decoding response body when OSV returns empty range events](https://github.com/astral-sh/uv/issues/19492) `#19492`
**ステータス:** Open | **ラベル:** bug, uv audit

**概要:** OSV の脆弱性レコード（PYSEC-2026-89）で `affected.ranges.events` に空の `{}` が含まれており、`uv audit` のデシリアライズが失敗する。

**直近のアクティビティ:** angryfoxx が ack 方針を確認。woodruffw が OSV 側の regression なので uv 側で寛容化はしない方針を表明。milesflo-bd が PR #19500 を予告、woodruffw が PR #19496 を提示。

**次のアクション:** OSV 側修正と uv 側の最小限ワークアラウンド両輪での対応。

---

### ![Issue](icons/issue-opened.svg) [Test coverage for multi-index scenarios for `uv.lock`](https://github.com/astral-sh/uv/issues/5882) `#5882`
**ステータス:** Closed | **ラベル:** なし

**概要:** 複数 index 設定でのロックファイル振る舞いのテスト不足の指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Reject unsafe entry points in `uv-build`](https://github.com/astral-sh/uv/pull/19495) `#19495`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv-build` で wheel ビルド時に PyPI が「不正」とみなす危険なエントリポイント（予約名等）を拒否する。

**直近のアクティビティ:** konstin と zanieb・zsol が、過去（#7781→#14867）に厳しすぎる検証が緩和された経緯と、現在再強化する範囲（script entry point に限定するか、より広く matching するか）を議論。

**変更ファイル:** `crates/uv-build-backend/src/metadata.rs`、`crates/uv/tests/it/build.rs`。

**次のアクション:** 厳格化の範囲確定後マージ。

---

### ![Issue](icons/issue-opened.svg) [Validation on empty environment variables](https://github.com/astral-sh/uv/issues/19499) `#19499`
**ステータス:** Open | **ラベル:** bug

**概要:** 0.11.15 以降、`--no-binary-package ''` のように空文字を渡すと「Not a valid package or extra name」で落ちる回帰の報告。

**直近のアクティビティ:** zanieb が「想定動作」とコメント。

**次のアクション:** PR #19503 で空リスト許可方針へ修正中。

---

### ![PR](icons/git-pull-request.svg) [fix: show lock mismatch error with --quiet/--check](https://github.com/astral-sh/uv/pull/19371) `#19371`
**ステータス:** Closed | **ラベル:** なし

**概要:** `uv lock --check --quiet`、`uv sync --locked --quiet` で lock mismatch エラーが silent に飲み込まれ exit code 1 のみになる問題を `stderr_important()` で常に出力するように修正。

**直近のアクティビティ:** EliteTK が「合意待ち」とコメント。

**変更ファイル:** `crates/uv/src/commands/project/lock.rs`、`crates/uv/src/commands/project/sync.rs` ほか 5 ファイル。

**次のアクション:** 合意形成後に再開。

---

### ![PR](icons/git-pull-request.svg) [docs(settings): disambiguate the duplicate `index` heading](https://github.com/astral-sh/uv/pull/19498) `#19498`
**ステータス:** Open | **ラベル:** なし

**概要:** 設定リファレンスページで `index` 見出しが 2 つあり deep link が衝突するため、2 つめをリネームしてアンカーを区別する。Issue #18789 をクローズ。

**直近のアクティビティ:** Bot がテストインベントリ差分（+3/-0）を報告。

**変更ファイル:** `crates/uv-dev/src/generate_options_reference.rs`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(audit): skip and warn on malformed OSV records](https://github.com/astral-sh/uv/pull/19500) `#19500`
**ステータス:** Closed | **ラベル:** なし

**概要:** OSV の `affected.ranges.events` 空オブジェクトを起因とする `uv audit` クラッシュを、不正レコードを skip + warn する形で回避。PYSEC-2025-183/2026-89 を再現テストでカバー。

**直近のアクティビティ:** milesflo-bd が ack。woodruffw が PR #19496 で同等以下の対応に集約するため、本 PR は skip しない方針でクローズ。

**変更ファイル:** `crates/uv-audit/src/service/osv.rs`、`crates/uv/tests/it/audit.rs` ほか 4 ファイル。

**次のアクション:** PR #19496 を追跡。

---

### ![PR](icons/git-pull-request.svg) [uv-netrc: fix multi-word no-space comment lines causing parse errors](https://github.com/astral-sh/uv/pull/19494) `#19494`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv-netrc` パーサで `#word1 word2 word3` 形式のコメントを誤って解釈する問題の修正（1 行修正）。

**直近のアクティビティ:** Dev-X25874 が tests 同梱しなかったことを謝罪。zanieb は前回 PR からの方針として test なし PR は受け付けないと再警告。Bot はテストインベントリ差分を報告。

**変更ファイル:** `crates/uv-netrc/src/netrc.rs`。

**次のアクション:** テストケース追加。

---

### ![Issue](icons/issue-opened.svg) [Upgrade dependencies in `pyproject.toml` (`uv upgrade`)](https://github.com/astral-sh/uv/issues/6794) `#6794`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `pyproject.toml` 内のバージョンピンを一括更新する `uv upgrade` の要望。deno/poetry/npm との比較を提示。

**直近のアクティビティ:** wummel が自作の `python-update-checker` を共有。

**次のアクション:** 設計議論を継続。

---

### ![PR](icons/git-pull-request.svg) [Skip and warn on malformed OSV records](https://github.com/astral-sh/uv/pull/19496) `#19496`
**ステータス:** Closed | **ラベル:** preview

**概要:** woodruffw 自身による OSV 異常レコードの skip+warn 案。PR #19492 をクローズ予定だった。

**直近のアクティビティ:** Bot がテストインベントリ差分を報告。

**変更ファイル:** `crates/uv-audit/src/service/osv.rs`、`crates/uv/tests/it/audit.rs` ほか 4 ファイル。

**次のアクション:** クローズ。OSV 側修正待ち方針へ。

---

### ![Issue](icons/issue-opened.svg) [Feature request: Add option to ignore missing/unavailable indexes](https://github.com/astral-sh/uv/issues/16301) `#16301`
**ステータス:** Open | **ラベル:** enhancement

**概要:** ローカル wheelhouse など index が使えない環境で、エラーではなく警告として継続できるオプションの要望。

**直近のアクティビティ:** alexchandel が「`ignore-error-codes = [404]` で OS エラーもマップする案」を提示。sveinse が社内 pypi 不在時に発生する `Request failed after 3 retries` の事例を共有。

**次のアクション:** 設計判断。

---

### ![Issue](icons/issue-opened.svg) [Support a `--fix` type flag for `audit` to allow automatic fixing of vulnerabilities according to semantic versioning](https://github.com/astral-sh/uv/issues/19428) `#19428`
**ステータス:** Open | **ラベル:** enhancement, uv audit

**概要:** `uv audit --fix` で semantic versioning に従う脆弱性修正を自動適用したいという要望。

**直近のアクティビティ:** woodruffw、drkspace、crispyricepc、shivakharbanda の間で、`constraint-dependencies` と直接依存どちらを更新するか、lockfile のみで足りるか、`pyproject.toml` の更新ポリシーをどう決めるかを議論。MVP は lockfile 更新のみで合意。

**次のアクション:** PR #19497 のレビューで MVP 着地。

---

### ![Issue](icons/issue-opened.svg) [Configuring multiple indexes with `uv publish`](https://github.com/astral-sh/uv/issues/7963) `#7963`
**ステータス:** Closed | **ラベル:** documentation

**概要:** Test PyPI と本番 PyPI で別 username/token を `uv publish` で扱いたいという要望。

**直近のアクティビティ:** sharifhsn が PR #9694 でクローズと報告。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [python builds are missing AF_VSOCK](https://github.com/astral-sh/uv/issues/19491) `#19491`
**ステータス:** Open | **ラベル:** bug

**概要:** uv 配布の Python ビルドで `socket.AF_VSOCK` が利用できない。`mkosi` や libvirt python bindings が動かない。

**直近のアクティビティ:** woodruffw が python-build-standalone Issue #1050 を参照、zanieb が jjhelmus に cc。

**次のアクション:** python-build-standalone 側で対応。

---

### ![PR](icons/git-pull-request.svg) [fix(audit): skip empty branch in malformed OSV range events](https://github.com/astral-sh/uv/pull/19493) `#19493`
**ステータス:** Closed | **ラベル:** なし

**概要:** Issue #19492 に対する別アプローチ修正案。

**直近のアクティビティ:** woodruffw が OSV 側 bug を握り潰すべきでないとしてマージ拒否。Bot がテストインベントリ差分を報告。

**変更ファイル:** `crates/uv-audit/src/service/osv.rs`、`crates/uv/tests/it/audit.rs`。

**次のアクション:** クローズ。

---

### ![PR](icons/git-pull-request.svg) [Allow disabling reading the system config with `UV_NO_SYSTEM_CONFIG`](https://github.com/astral-sh/uv/pull/19476) `#19476`
**ステータス:** Merged | **ラベル:** configuration

**概要:** system 配置の `uv.toml` を `UV_NO_SYSTEM_CONFIG` 環境変数で無効化できるようにする。

**直近のアクティビティ:** zanieb が「user 側 `uv.toml` で全キーをデフォルト再設定するのは負担なので環境変数経路が要る」とコメント。Bot がテストインベントリ差分を報告。

**変更ファイル:** `crates/uv-settings/src/lib.rs`、`crates/uv-static/src/env_vars.rs`、`crates/uv/tests/it/show_settings.rs`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add support for locking build dependencies](https://github.com/astral-sh/uv/pull/17854) `#17854`
**ステータス:** Open | **ラベル:** build:skip-release

**概要:** ビルド依存の lock サポートを追加する大規模 PR（+8419/-316、42 ファイル、レビュー 47 件）。本人レビュー用と明記。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-build-frontend/src/lib.rs`、`crates/uv-distribution/src/distribution_database.rs`、`crates/uv-resolver/src/lock/mod.rs` ほか。

**次のアクション:** 反復改善継続。

---

### ![PR](icons/git-pull-request.svg) [Infer the version on release workflow instead of requiring user input](https://github.com/astral-sh/uv/pull/19477) `#19477`
**ステータス:** Open | **ラベル:** internal

**概要:** リリースワークフローで version を手入力する代わりに推測する変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/release.yml`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add `exclude-newer` to `[[tool.uv.index]]`](https://github.com/astral-sh/uv/pull/18839) `#18839`
**ステータス:** Merged | **ラベル:** configuration, preview

**概要:** `[[tool.uv.index]]` ごとに `exclude-newer` を設定できるようにする。優先順位はグローバル `exclude-newer-package` > index `exclude-newer` > グローバル `exclude-newer`。Issue #16813 をクローズ。

**直近のアクティビティ:** AllanLeanderRostockHansen が `uv audit --upgrade` との相互作用を質問。

**変更ファイル:** `crates/uv-distribution-types/src/index.rs`、`crates/uv-resolver/src/exclude_newer.rs`、`crates/uv-resolver/src/resolver/mod.rs` ほか 22 ファイル。

**次のアクション:** マージ済み。`uv audit --upgrade` 連携を別途確認。

---

### ![PR](icons/git-pull-request.svg) [fix(install-wheel): parse wheel version components as integers before comparison](https://github.com/astral-sh/uv/pull/19479) `#19479`
**ステータス:** Closed | **ラベル:** なし

**概要:** WHEEL メタデータの `Wheel-Version` を文字列のまま比較していたため、`"10" > "9"` が false 判定される問題（lexicographic）の修正案。

**直近のアクティビティ:** codspeed-hq がパフォーマンス無変化を報告。

**変更ファイル:** `crates/uv-install-wheel/src/wheel.rs`。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [uv-shell/shlex: quote POSIX paths with any shell-unsafe characters, not just spaces](https://github.com/astral-sh/uv/pull/19490) `#19490`
**ステータス:** Closed | **ラベル:** なし

**概要:** `shlex_posix` が空白入りパスしかクオートしておらず、Python の `shlex.quote` 相当の安全文字集合とずれていた問題への修正案。`/home/user/my$project/.venv` のような venv のソース活性化スクリプトが正しくクオートされない事例を提示。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-shell/src/shlex.rs`、`crates/uv/tests/it/venv.rs`。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Drop the `ctrlc` dependency](https://github.com/astral-sh/uv/pull/18162) `#18162`
**ステータス:** Open | **ラベル:** internal

**概要:** `ctrlc` クレート依存を撤廃し、Unix/Windows 別の独自実装に置き換える内部リファクタ（本文なし）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-sys/src/ctrl_c.rs`、`crates/uv-trampoline/src/bounce.rs`、`crates/uv-unix/src/ctrl_c.rs` ほか 15 ファイル（+932/-335）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [BUG/UX: confusing warning in `uvx` running `<tool>@latest` with `--resolution=lowest-direct`](https://github.com/astral-sh/uv/issues/7164) `#7164`
**ステータス:** Closed | **ラベル:** bug, error messages

**概要:** `uvx` の `@latest` 指定と `--resolution=lowest-direct` が両立しないことを示す警告がわかりにくいというバグ報告。

**直近のアクティビティ:** neutrinoceros がトリアージへ感謝、解決済を確認。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Generate PEP 740 attestations](https://github.com/astral-sh/uv/pull/19489) `#19489`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv publish` 内で PEP 740 attestation を生成できるようにする初期実装。WIP、依存削減（`chrono`→`jiff` 等）と sigstore-rust 上流調整を伴う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-pep740/src/lib.rs`、`crates/uv-pep740/src/sign.rs`、`crates/uv-configuration/src/attest.rs` ほか 15 ファイル（+1268/-218）。

**次のアクション:** クリーンアップ、依存整理。

---

### ![Issue](icons/issue-opened.svg) [Document strategies for sharing the cache](https://github.com/astral-sh/uv/issues/5611) `#5611`
**ステータス:** Open | **ラベル:** documentation, cache

**概要:** システムワイドな uv インストールにおけるキャッシュ共有戦略のドキュメント化要望。

**直近のアクティビティ:** axellpadilla がセルフホストランナーでの公式手順を質問。

**次のアクション:** ドキュメント追加。

---

### ![Issue](icons/issue-opened.svg) [Allow override dependencies in workspace members](https://github.com/astral-sh/uv/issues/13069) `#13069`
**ステータス:** Open | **ラベル:** enhancement

**概要:** ワークスペースメンバ単独で sync する場合に、メンバの `override-dependencies` を尊重したいという要望。

**直近のアクティビティ:** Tremeschin が、ワークスペースルート限定の制約がドキュメントから欠落しており嵌った旨を共有。

**次のアクション:** ドキュメント補強と要望検討。

---

### ![PR](icons/git-pull-request.svg) [docs: remove '7 days' as possible value for 'exclude-newer'](https://github.com/astral-sh/uv/pull/19488) `#19488`
**ステータス:** Closed | **ラベル:** question

**概要:** ドキュメントの `exclude-newer = "7 days"` 表記を「相対時間は非サポート」と誤解して削除しようとした PR。

**直近のアクティビティ:** zanieb が相対時間も実際は許容されている旨を回答し、PR は不要と判明。

**変更ファイル:** `docs/concepts/resolution.md`。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Is it possible to upgrade just a single package if you change exclude-newer?](https://github.com/astral-sh/uv/issues/14394) `#14394`
**ステータス:** Closed | **ラベル:** question

**概要:** `--exclude-newer` 変更時、`--upgrade-package foo` だけでも全パッケージが上がってしまう問題への質問。

**直近のアクティビティ:** Ark-kun が lockfile を `exclude-newer` 変更で invalidate しない設計案、`--no-upgrade` の不在などの懸念を述べる。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [exclude-newer-package config getting undone by other dependency actions](https://github.com/astral-sh/uv/issues/17999) `#17999`
**ステータス:** Open | **ラベル:** question

**概要:** 相対 `exclude-newer` 設定下で `exclude-newer-package` を使うと、後段の操作で打ち消される現象の質問。

**直近のアクティビティ:** cscanlin が `exclude-newer = '7 days'` + `--upgrade-package idna==3.15 --exclude-newer-package idna=2026-05-19` で `uv sync --locked` が失敗する事例を共有。

**次のアクション:** 仕様整理。

---

### ![PR](icons/git-pull-request.svg) [Document and test relative exclude-newer support for uv pip](https://github.com/astral-sh/uv/pull/19475) `#19475`
**ステータス:** Merged | **ラベル:** documentation

**概要:** `uv pip` 配下の `exclude-newer` で相対値（ISO 8601 duration や friendly な文字列）が使えることを明示するドキュメント整備と統合テスト追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/`、`crates/uv/tests/it/pip_exclude_newer_relative.rs` ほか 4 ファイル。

**次のアクション:** マージ済み。

---

## microsoft/vscode

> Copilot Chat / Agent Host 周辺の機能整備、BYOK モデル拡充、AI 編集の co-author 帰属、セッション管理 UX 改善が圧倒的な比重を占める。セキュリティ系の Bug 報告（Shell argument injection、snippet エスケープ等）と Copilot CLI 関連の不満報告も目立つ。

### ![Issue](icons/issue-opened.svg) [Add reasoning effort support for google byok models](https://github.com/microsoft/vscode/issues/317642) `#317642`
**ステータス:** Open | **ラベル:** model-byok

**概要:** Google BYOK モデルに reasoning effort 設定を追加してほしいという要望（本文なし）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Die choked on shit for cutting out interactive rebase bitch.](https://github.com/microsoft/vscode/issues/317641) `#317641`
**ステータス:** Closed | **ラベル:** spam

**概要:** スパムとして自動クローズ。

**直近のアクティビティ:** `vs-code-engineering[bot]` がコミュニティガイドライン違反として閉鎖。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Respect Folder for Agent Host Copilot CLI](https://github.com/microsoft/vscode/pull/317581) `#317581`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウで `folder` 選択時に worktree とそのターミナルを強制しないように修正。Issue #317567 を解決。

**直近のアクティビティ:** Copilot レビュー Bot が変更概要を整理。

**変更ファイル:** `src/vs/sessions/contrib/providers/agentHost/browser/baseAgentHostSessionsProvider.ts`、`src/vs/sessions/contrib/providers/copilotChatSessions/browser/copilotChatSessionsProvider.ts` ほか 4 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Agents: Clicking a customization status entry should open the customizations editor on that category's tab](https://github.com/microsoft/vscode/issues/317639) `#317639`
**ステータス:** Open | **ラベル:** agents-window

**概要:** Agents ウィンドウのカスタマイズステータスエリアをクリックしたら、対応するカテゴリ（MCP Servers/Instructions/Prompts/Agents）タブが選択された状態でエディタを開いてほしい。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UI 改善対応。

---

### ![PR](icons/git-pull-request.svg) [Sanity test: tighten agent-mode prompt so the model can't skip the tool call](https://github.com/microsoft/vscode/pull/317426) `#317426`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot E2E Production agent mode のサニティテストで `get_errors` ツール呼び出しがスキップされて flaky になる問題への対処。プロンプトを書き換えて、ツール呼び出しを必須化する。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/copilot/src/extension/test/vscode-node/sanity.sanity-test.ts`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(chronicle): emit assistant.usage events for VS Code Chat sessions](https://github.com/microsoft/vscode/pull/317640) `#317640`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code Chat（ネイティブハーネス、約 85% のセッション）の token 消費が `assistant.usage` イベントとして cloud session store に出ていないため、`eventTranslator.ts` の `translateSpan()` で `GenAiOperationName.CHAT` を扱うようにし、コスト最適化スキル等から可視化できるようにする。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/copilot/src/extension/chronicle/common/eventTranslator.ts`、`extensions/copilot/src/extension/chronicle/vscode-node/remoteSessionExporter.ts` ほか 3 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Move terminal telemetry into a terminalContrib](https://github.com/microsoft/vscode/pull/317605) `#317605`
**ステータス:** Open | **ラベル:** なし

**概要:** ターミナルテレメトリを `terminalContrib/telemetry` 配下の専用 contribution に分離し、コアターミナル contribution をスリム化。

**直近のアクティビティ:** Tyriar が `company="Cursor"` で CLA 同意を 2 度コメント。`microsoft-github-policy-service[bot]` が CLA 同意フォーマットを案内。

**変更ファイル:** `src/vs/workbench/contrib/terminal/terminal.all.ts`、`src/vs/workbench/contrib/terminalContrib/telemetry/browser/terminal.telemetry.contribution.ts` ほか 4 ファイル。

**次のアクション:** CLA 確認後マージ。

---

### ![PR](icons/git-pull-request.svg) [agent-host: stop redundant customization sync events](https://github.com/microsoft/vscode/pull/317611) `#317611`
**ステータス:** Open | **ラベル:** なし

**概要:** カスタマイズ同期を、進捗コールバックでフルリストを毎回送る方式から、SessionAction 経由で状態管理マネージャ越しに publish する方式へ変更。冗長な `activeClientChanged` のディスパッチも抑制し、Issue #316538 を解決。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `src/vs/platform/agentHost/common/agentService.ts`、`src/vs/platform/agentHost/node/copilot/copilotAgent.ts` ほか 14 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add minimal harness for experimental model family](https://github.com/microsoft/vscode/pull/317613) `#317613`
**ステータス:** Open | **ラベル:** なし

**概要:** `chat.modelCapabilityOverrides` で `family: experimental` を指定したモデル向けに、最小プロンプト + 制限ツールセットの opt-in 用ハーネスを追加。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/copilot/src/extension/prompts/node/agent/defaultMinimalPrompt.tsx`、`extensions/copilot/src/extension/tools/node/searchSubagentTool.ts` ほか 6 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Bump Agent host Copilot CLI to 1.0.48](https://github.com/microsoft/vscode/pull/317241) `#317241`
**ステータス:** Open | **ラベル:** なし

**概要:** `@github/copilot` を 1.0.48 に、`@github/copilot-sdk` を 1.0.0-beta.4 系に更新。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `package.json`、`remote/package.json`、`build/lib/copilot.ts` ほか 8 ファイル。

**次のアクション:** サニティテスト後マージ。

---

### ![PR](icons/git-pull-request.svg) [test(mcp): silence sinon FakeTimers native-timer warning in sampling log tests](https://github.com/microsoft/vscode/pull/317631) `#317631`
**ステータス:** Open | **ラベル:** なし

**概要:** MCP Sampling Log テストで sinon FakeTimers が teardown 時に `console.warn` を出し、Electron テストハーネスの「予期しないコンソール出力なし」アサートを破る問題への対処。`shouldClearNativeTimers: true` で抑止。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `src/vs/workbench/contrib/mcp/test/common/mcpSamplingLog.test.ts`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Enable BYOK in air-gapped scenarios/without GitHub auth](https://github.com/microsoft/vscode/pull/317428) `#317428`
**ステータス:** Open | **ラベル:** model-byok

**概要:** GitHub 認証なし環境（air-gapped 等）でも BYOK モデルでチャットを動かせるようにする。サインイン UI、welcome メッセージ、`registerSearchProvider` のガード等を BYOK only モードに対応させる。Issue #246551/275591/313461/272843 をクローズ。

**直近のアクティビティ:** `github-actions[bot]` が 150 件のスクリーンショット差分レポートを生成。Copilot レビュー Bot が概要整理、`registerSearchProvider()` で `_searchProviderRegistered = true` 設定後に early return する箇所など 2 件の low-confidence な指摘あり。

**変更ファイル:** `extensions/copilot/package.json`、`extensions/copilot/src/extension/authentication/vscode-node/authentication.contribution.ts`、`extensions/copilot/src/extension/chatSessions/copilotcli/node/copilotCli.ts` ほか 23 ファイル（+290/-77）。

**次のアクション:** Copilot 指摘の解決とメンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add granular provider group management actions to Manage Models](https://github.com/microsoft/vscode/pull/317419) `#317419`
**ステータス:** Merged | **ラベル:** なし

**概要:** Manage Models 内で provider グループに対し、API キー更新・モデル追加・config file ジャンプ・rename・delete のきめ細かいアクションを追加し、単一の「Configure…」を置き換え。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理、2 件のコメント生成。

**変更ファイル:** `src/vs/workbench/contrib/chat/browser/chatManagement/chatModelsWidget.ts`、`src/vs/workbench/contrib/chat/common/languageModels.ts` ほか 8 ファイル（+531/-60）。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [[chat agent sandbox] .NET telemetry `deviceid` and other `~`-relative files written into workspace root on macOS](https://github.com/microsoft/vscode/issues/317636) `#317636`
**ステータス:** Open | **ラベル:** なし

**概要:** `chat.agent.sandbox.enabled: on` の macOS で、サンドボックス内の `~` 展開や `Application Support` 取得結果がワークスペースルートに書き込まれてしまう。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** サンドボックス分離強化。

---

### ![Issue](icons/issue-opened.svg) [unable to ssh connect to server's workspace after the most recent update](https://github.com/microsoft/vscode/issues/317635) `#317635`
**ステータス:** Open | **ラベル:** なし

**概要:** 直近アップデート後、サーバのワークスペースに SSH 接続できないというバグ報告（テンプレ未記入）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現情報の収集。

---

### ![PR](icons/git-pull-request.svg) [[Search Subagent] Handle context window limit exceeded error](https://github.com/microsoft/vscode/pull/316529) `#316529`
**ステータス:** Open | **ラベル:** なし

**概要:** Search subagent が ToolCall 履歴で context window を超えるとバックエンドが 400 を返す事象に耐性を持たせる。`buildPrompt` での予防的バジェッティングと、context overflow 検出時の縮小再試行を導入。

**直近のアクティビティ:** 24anisha が頻度データのスクリーンショットを共有。bhavyaus は「主エージェントループの仕組みに乗せた方が良いのでは」「予防バジェットだけで足りないか」と提起。CHANGES_REQUESTED あり。

**変更ファイル:** `extensions/copilot/src/extension/prompt/node/searchSubagentToolCallingLoop.ts` ほか 6 ファイル。

**次のアクション:** autopilot モード互換性確認と方針調整。

---

### ![PR](icons/git-pull-request.svg) [fix: enable logging condition in state model change logging](https://github.com/microsoft/vscode/pull/317626) `#317626`
**ステータス:** Merged | **ラベル:** なし

**概要:** チャット入力 state model 変更ログの early-return 条件が反転していたバグ修正。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `src/vs/workbench/contrib/chat/common/model/chatModel.ts`。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Allow tracking cost by tag/billing code](https://github.com/microsoft/vscode/issues/315596) `#315596`
**ステータス:** Open | **ラベル:** upstream, UBB

**概要:** Copilot のトークンコストをタグ・プロジェクト・クライアントごとに按分したいという企業向け要望。

**直近のアクティビティ:** cwebster-99 が billing platform 側への転送を案内。Halling69 が、Org レベルの日次集計しか取れない現状の API 限界と、ユーザごと/日次の breakdown が欲しい旨を詳述。

**次のアクション:** 上流（billing チーム）での対応。

---

### ![PR](icons/git-pull-request.svg) [Fix git extension not working when repo root is "/" (chroot)](https://github.com/microsoft/vscode/pull/317637) `#317637`
**ステータス:** Open | **ラベル:** なし

**概要:** chroot 環境で repo root が `/` のとき、`normalizePath()` が末尾 `/` を機械的に剥がして空文字列→`"."` になり、`isDescendant`/`pathEquals` が壊れる問題への修正。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/git/src/test/git.test.ts`、`extensions/git/src/util.ts`。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Agents -- sub session picker styling](https://github.com/microsoft/vscode/issues/317638) `#317638`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents ウィンドウのサブセッションピッカーのスタイル改善要望（動画添付のみ、本文ほぼ空）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン担当のトリアージ。

---

### ![PR](icons/git-pull-request.svg) [Experimental support for pasting rich content into markdown input box](https://github.com/microsoft/vscode/pull/317604) `#317604`
**ステータス:** Open | **ラベル:** なし

**概要:** チャット markdown 入力欄に `text/html` を貼り付けたとき、簡易変換ユーティリティで Markdown 化する実験的サポート。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `src/vs/base/common/htmlToMarkdown.ts`、`src/vs/base/test/common/htmlToMarkdown.test.ts`、`src/vs/workbench/contrib/chat/browser/widget/input/editor/chatPasteProviders.ts`。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Track AI co-author attribution for hidden and agent-host edits](https://github.com/microsoft/vscode/pull/316691) `#316691`
**ステータス:** Open | **ラベル:** なし

**概要:** AI 編集の co-author 帰属追跡を、隠れている open document や agent-host 編集セッションにも広げる。Git smoke カバレッジで commit lifecycle 全体での trailer 挿入/クリーンアップを検証。

**直近のアクティビティ:** PenguinDOOM が自身のリポジトリでローカル動作確認を共有。Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/git/src/test/smoke.test.ts`、`src/vs/workbench/contrib/chat/browser/agentSessions/agentHost/agentHostEditingSession.ts`、`src/vs/workbench/contrib/editTelemetry/browser/aiContributionFeature.ts` ほか 9 ファイル（+643/-170）。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Update SECURITY.md](https://github.com/microsoft/vscode/pull/317624) `#317624`
**ステータス:** Closed | **ラベル:** なし

**概要:** タイトルは SECURITY.md 更新だが、実体は 100 ファイル超の不適切な差分（+2204040/-184778）を含む PR。

**直近のアクティビティ:** `vs-code-engineering[bot]` の CODENOTIFY、Copilot は変更ファイル数が上限超過のためレビュー不可と報告。

**変更ファイル:** `.devcontainer/devcontainer.json`、`build/azure-pipelines/common/sanity-tests.yml` ほか 100 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [sessions: use per-agent icon for agent host sessions](https://github.com/microsoft/vscode/pull/317620) `#317620`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agent Host セッションがプロバイダ汎用アイコン（`vm`/`remote`）を使っていたところ、セッション型ごとの個別アイコン（`copilot`/`claude`/`openai`）に揃えるよう `iconForAgentProvider` を経由させる。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `src/vs/sessions/contrib/providers/agentHost/browser/baseAgentHostSessionsProvider.ts`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix git extension not working when repo root is "/" (chroot)](https://github.com/microsoft/vscode/pull/317625) `#317625`
**ステータス:** Closed | **ラベル:** なし

**概要:** PR #317637 と同内容の chroot 対応 PR（初版）。

**直近のアクティビティ:** vanklompf が CLA 同意。

**変更ファイル:** `extensions/git/src/test/git.test.ts`、`extensions/git/src/util.ts`。

**次のアクション:** 後継 PR #317637 で対応中。

---

### ![Issue](icons/issue-opened.svg) [Feature Request / Inquiry: Implementing a native setting to disable "Install from VSIX" in corporate environments](https://github.com/microsoft/vscode/issues/317630) `#317630`
**ステータス:** Open | **ラベル:** なし

**概要:** 企業向けに「Install from VSIX」をネイティブ設定で無効化する仕組みの要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Enterprise 向け対応の検討。

---

### ![PR](icons/git-pull-request.svg) [Default reasoning effort to first advertised level for unspecified families](https://github.com/microsoft/vscode/pull/317622) `#317622`
**ステータス:** Open | **ラベル:** なし

**概要:** モデルファミリで明示的なデフォルト reasoning effort がない場合、最初に告知された effort レベルを採用し、ピッカーで `undefined` を表示しないようにする。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/copilot/src/extension/conversation/common/languageModelAccess.ts` ほか 4 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [chore: bump Octokit](https://github.com/microsoft/vscode/pull/317632) `#317632`
**ステータス:** Open | **ラベル:** なし

**概要:** 組み込みの GitHub 連携拡張で `@octokit/*` を新バージョンに揃え、`tunnel` ベースの proxy 処理を `undici`/`ProxyAgent` 経由に置き換える。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**変更ファイル:** `extensions/configuration-editing/`、`extensions/github/` 配下 10 ファイル（+264/-272）。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Experiment where worktree option lead to agents app suggestion](https://github.com/microsoft/vscode/issues/317634) `#317634`
**ステータス:** Open | **ラベル:** なし

**概要:** worktree オプションが Agents アプリ提案に繋がる実験のトラッキング。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 実験計画追跡。

---

### ![Issue](icons/issue-opened.svg) [Agents: Add search/filter bar to workspace picker](https://github.com/microsoft/vscode/issues/317633) `#317633`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents のワークスペースピッカーに検索/フィルタバーを追加してほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UI 改善。

---

### ![PR](icons/git-pull-request.svg) [Add new VS Code theme for mermaid diagrams](https://github.com/microsoft/vscode/pull/317617) `#317617`
**ステータス:** Open | **ラベル:** なし

**概要:** Mermaid 図用の VS Code テーマを追加。

**直近のアクティビティ:** Copilot レビュー Bot が概要整理。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [agents sessions list pixel spinner + abstraction for spinner](https://github.com/microsoft/vscode/pull/317629) `#317629`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents セッションリストのピクセルスピナー追加とスピナー抽象化。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Update README.md](https://github.com/microsoft/vscode/pull/317627) `#317627`
**ステータス:** Open | **ラベル:** なし

**概要:** README.md の軽微な更新（本文未記入）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Make CI artifacts reproducible](https://github.com/microsoft/vscode/pull/317601) `#317601`
**ステータス:** Open | **ラベル:** なし

**概要:** CI 成果物の再現性向上のための変更。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Agents: Add smoke test for Local Agent Host Provider harness](https://github.com/microsoft/vscode/issues/317628) `#317628`
**ステータス:** Open | **ラベル:** なし

**概要:** Local Agent Host Provider のスモークテスト追加 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317545 で部分着手済み。

---

### ![PR](icons/git-pull-request.svg) [Create Outfront](https://github.com/microsoft/vscode/pull/317621) `#317621`
**ステータス:** Open | **ラベル:** なし

**概要:** タイトルのみで本文・コンテキスト不明な PR。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナ確認。

---

### ![PR](icons/git-pull-request.svg) [Update SECURITY.md](https://github.com/microsoft/vscode/pull/317623) `#317623`
**ステータス:** Open | **ラベル:** なし

**概要:** SECURITY.md の更新 PR（テンプレートのみ）。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [customizatio. UI: tage agent host customizations as is](https://github.com/microsoft/vscode/pull/317619) `#317619`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host のカスタマイズを「そのまま」タグ付けする UI 変更。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Terminal penceresinde metinler genişliğine uyum sağlamıyor](https://github.com/microsoft/vscode/issues/317488) `#317488`
**ステータス:** Open | **ラベル:** なし

**概要:** トルコ語で報告された、ターミナルウィンドウのテキスト幅追従不具合。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現情報整理。

---

### ![PR](icons/git-pull-request.svg) [Fix typo in README contributing section](https://github.com/microsoft/vscode/pull/317252) `#317252`
**ステータス:** Merged | **ラベル:** なし

**概要:** README の Contributing セクションの typo 修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Not all changes are shown in diff view in new Agents Window](https://github.com/microsoft/vscode/issues/317616) `#317616`
**ステータス:** Open | **ラベル:** なし

**概要:** 新 Agents ウィンドウの diff view で一部変更が表示されないバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査。

---

### ![PR](icons/git-pull-request.svg) [Fix 'undefined' reasoning effort default for non-claude/gpt models](https://github.com/microsoft/vscode/pull/317618) `#317618`
**ステータス:** Closed | **ラベル:** なし

**概要:** Claude/GPT 以外で reasoning effort のデフォルトが `undefined` になる問題の修正案。PR #317622 に置換。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(arrays): clean up duplicate logic and improve array helpers](https://github.com/microsoft/vscode/pull/317600) `#317600`
**ステータス:** Open | **ラベル:** なし

**概要:** 配列ヘルパの重複ロジック整理と改善。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [sessions: don't reorder session list when find widget opens](https://github.com/microsoft/vscode/pull/317615) `#317615`
**ステータス:** Open | **ラベル:** なし

**概要:** find widget を開いた際にセッションリストが並び替わってしまう問題の修正。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [6](https://github.com/microsoft/vscode/issues/317614) `#317614`
**ステータス:** Closed | **ラベル:** なし

**概要:** タイトル「6」だけのスパム/誤投稿としてクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Gate built-in import commands when useTsgo is active](https://github.com/microsoft/vscode/pull/317425) `#317425`
**ステータス:** Open | **ラベル:** なし

**概要:** `useTsgo` 有効時、ビルトインの import コマンドを抑止する。

**直近のアクティビティ:** Copilot レビュー Bot がコメント。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Enterprise support for Windows: Enable possibility to set managed settings via Intune (Setting Catalogs & OMA-URI)](https://github.com/microsoft/vscode/issues/242922) `#242922`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows の Intune（Setting Catalogs & OMA-URI）で VS Code 設定を管理したいというエンタープライズ要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Enterprise 対応の議論。

---

### ![Issue](icons/issue-opened.svg) [Custom Azure model is not available or incorrectly selected when using Copilot Chat over Remote SSH](https://github.com/microsoft/vscode/issues/314678) `#314678`
**ステータス:** Closed | **ラベル:** なし

**概要:** Remote SSH 経由 Copilot Chat で Azure カスタムモデルが選択できない/誤選択されるバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Agents App: Unable to resolve workspace folder (Can't see files since latest update)](https://github.com/microsoft/vscode/issues/317603) `#317603`
**ステータス:** Open | **ラベル:** なし

**概要:** 直近アップデート後、Agents アプリでワークスペースフォルダ解決ができずファイルが表示されない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現情報収集。

---

### ![Issue](icons/issue-opened.svg) [7](https://github.com/microsoft/vscode/issues/317612) `#317612`
**ステータス:** Closed | **ラベル:** なし

**概要:** タイトル「7」だけのスパム/誤投稿としてクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [agents: add smoke test for Agents Window with mocked LLM server](https://github.com/microsoft/vscode/pull/317545) `#317545`
**ステータス:** Merged | **ラベル:** なし

**概要:** モック LLM サーバを使った Agents Window のスモークテスト追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [[Regression] [Error] [GitHub.copilot-chat] unhandlederror-Va.lm.registerToolDefinition is not a function](https://github.com/microsoft/vscode/issues/317574) `#317574`
**ステータス:** Closed | **ラベル:** なし

**概要:** 古い VS Code バージョンで Copilot Chat 拡張が `Va.lm.registerToolDefinition is not a function` を投げる回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317579 でクローズ。

---

### ![PR](icons/git-pull-request.svg) [fix: guard registerToolDefinition call for older VS Code versions (fixes #317574)](https://github.com/microsoft/vscode/pull/317579) `#317579`
**ステータス:** Closed | **ラベル:** なし

**概要:** 古い VS Code バージョンで `registerToolDefinition` 呼び出しをガードする修正案。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) ["Open Browser Settings" simplified to just "Browser Settings"](https://github.com/microsoft/vscode/pull/317217) `#317217`
**ステータス:** Merged | **ラベル:** なし

**概要:** ブラウザ設定メニュー項目を簡略化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Agents app opens a new lower chat instead of processing the first prompt](https://github.com/microsoft/vscode/issues/317588) `#317588`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents アプリが最初のプロンプトを処理せず、新しい下位チャットを開いてしまうバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査。

---

### ![PR](icons/git-pull-request.svg) [sessions: fix worktree-created task dispatch](https://github.com/microsoft/vscode/pull/317598) `#317598`
**ステータス:** Merged | **ラベル:** なし

**概要:** worktree 作成時のタスクディスパッチ修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Warn users if their system instruction usage is too high as a percentage of total context](https://github.com/microsoft/vscode/issues/300588) `#300588`
**ステータス:** Closed | **ラベル:** なし

**概要:** システムインストラクションが context のうちの大きな割合を占める場合に警告を出すという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Diagrams don't respond to theme changes](https://github.com/microsoft/vscode/issues/316968) `#316968`
**ステータス:** Open | **ラベル:** なし

**概要:** Mermaid 等の図表がテーマ変更に追従しないバグ。PR #317617 と関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317617 を追跡。

---

### ![PR](icons/git-pull-request.svg) [Remove copilot models from default list](https://github.com/microsoft/vscode/pull/317595) `#317595`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot モデルをデフォルトリストから除外する変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [[Bug] Incomplete Sanitization in conversationFeature.ts: Double-Escaping Bug Allows Shell Argument Injection in Git Commit Message](https://github.com/microsoft/vscode/issues/316996) `#316996`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot の `conversationFeature.ts` で git commit メッセージのサニタイズが不完全で、二重エスケープによって shell 引数注入が可能になるセキュリティバグ報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** セキュリティ評価と修正。

---

### ![Issue](icons/issue-opened.svg) [SSH to windows host: VS Code Server failed to start](https://github.com/microsoft/vscode/issues/317348) `#317348`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows ホストへの SSH で VS Code Server が起動しない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ログ収集と再現確認。

---

### ![PR](icons/git-pull-request.svg) [don't show fun messages if we are replacing them for thinking phrases](https://github.com/microsoft/vscode/pull/317589) `#317589`
**ステータス:** Merged | **ラベル:** なし

**概要:** thinking phrase 表示で fun message を置換する際の二重表示を抑止。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Feature request: add setting to disable sensitive input detection in terminal agent tools](https://github.com/microsoft/vscode/issues/317490) `#317490`
**ステータス:** Closed | **ラベル:** なし

**概要:** ターミナル agent ツールの sensitive input 検出を無効化する設定要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317594 でクローズ。

---

### ![PR](icons/git-pull-request.svg) [Fix sudo -S sensitive input detection](https://github.com/microsoft/vscode/pull/317594) `#317594`
**ステータス:** Merged | **ラベル:** なし

**概要:** `sudo -S` パスワード入力検出のロジック修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [uncaught error](https://github.com/microsoft/vscode/issues/317583) `#317583`
**ステータス:** Open | **ラベル:** なし

**概要:** タイトルのみの uncaught error 報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 詳細待ち。

---

### ![Issue](icons/issue-opened.svg) [update popup flashes in-out during % change](https://github.com/microsoft/vscode/issues/317610) `#317610`
**ステータス:** Open | **ラベル:** なし

**概要:** 進捗 % 変化のたびに update popup が点滅するバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UI 修正。

---

### ![Issue](icons/issue-opened.svg) [Security: minimumReleaseAge setting for mitigating supply chain attacks on extensions](https://github.com/microsoft/vscode/issues/316867) `#316867`
**ステータス:** Open | **ラベル:** なし

**概要:** 拡張機能のサプライチェーン攻撃緩和のため、最低リリース経過日数のような設定を導入する要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** セキュリティ機能検討。

---

### ![Issue](icons/issue-opened.svg) [New Copilot CLI Update is Terrible](https://github.com/microsoft/vscode/issues/317609) `#317609`
**ステータス:** Open | **ラベル:** なし

**概要:** 新しい Copilot CLI アップデートに対する不満の報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Language model unavailable](https://github.com/microsoft/vscode/issues/317606) `#317606`
**ステータス:** Closed | **ラベル:** なし

**概要:** 言語モデルが利用不可になる事象。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Allow local models in Agents mode](https://github.com/microsoft/vscode/issues/317591) `#317591`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents モードでローカルモデルの利用許可を求める要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 機能検討。

---

### ![Issue](icons/issue-opened.svg) [Copilot overwriting newer files when switching between nested workspaces](https://github.com/microsoft/vscode/issues/317607) `#317607`
**ステータス:** Open | **ラベル:** なし

**概要:** ネストされたワークスペースを切り替える際、Copilot が新しいファイルを上書きする問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** データロス調査。

---

### ![Issue](icons/issue-opened.svg) [Copilot makes PHP syntax suggestions very slow](https://github.com/microsoft/vscode/issues/311596) `#311596`
**ステータス:** Closed | **ラベル:** なし

**概要:** Copilot が PHP のシンタックス補完を著しく遅延させる問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Breaks Inline Completion (e.g. Snippets)](https://github.com/microsoft/vscode/issues/314220) `#314220`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot が組み込みのインライン補完（snippet 含む）を壊す問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 調査。

---

### ![Issue](icons/issue-opened.svg) [[Copilot Chat Bug] HTTP 400 "thinking blocks cannot be modified" after conversation compaction — paid tokens wasted, session unrecoverable](https://github.com/microsoft/vscode/issues/317608) `#317608`
**ステータス:** Open | **ラベル:** なし

**概要:** 会話圧縮後に `thinking blocks cannot be modified` の HTTP 400 が発生し、有償トークンが消費されたままセッションが復旧不能になる重大バグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Compaction ロジックの確認。

---

### ![Issue](icons/issue-opened.svg) [[VS Code 1.120+] onDidChangeLanguageModelChatInformation EventEmitter presence causes all LM providers to collapse into Azure](https://github.com/microsoft/vscode/issues/317414) `#317414`
**ステータス:** Open | **ラベル:** なし

**概要:** `onDidChangeLanguageModelChatInformation` の EventEmitter が存在すると、全 LM プロバイダが Azure に統合される回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** API 仕様確認。

---

### ![Issue](icons/issue-opened.svg) [Remote-SSH Extension Host Keeps Crashing after latest release](https://github.com/microsoft/vscode/issues/317596) `#317596`
**ステータス:** Open | **ラベル:** なし

**概要:** Remote-SSH 拡張ホストが直近リリース後にクラッシュし続ける。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クラッシュログ収集。

---

### ![Issue](icons/issue-opened.svg) [Uncommitted changes widget breaks flow and hard to understand for new users](https://github.com/microsoft/vscode/issues/317272) `#317272`
**ステータス:** Open | **ラベル:** なし

**概要:** uncommitted changes ウィジェットが新規ユーザにとって理解しにくく、フローを阻害するという指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UX 検討。

---

### ![PR](icons/git-pull-request.svg) [fix: restore word wrap after diff editor inline to split-view transition](https://github.com/microsoft/vscode/pull/314903) `#314903`
**ステータス:** Open | **ラベル:** なし

**概要:** diff editor の inline → split-view 切替後に word wrap が消える問題の修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [test feature](https://github.com/microsoft/vscode/issues/317602) `#317602`
**ステータス:** Closed | **ラベル:** なし

**概要:** 空タイトルのテスト投稿としてクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Bizzare terminal behaviour from remote ssh terminal](https://github.com/microsoft/vscode/issues/317461) `#317461`
**ステータス:** Closed | **ラベル:** なし

**概要:** Remote SSH ターミナルの奇妙な挙動報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [idk](https://github.com/microsoft/vscode/issues/317442) `#317442`
**ステータス:** Closed | **ラベル:** なし

**概要:** 内容不明のためクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[Error] unhandlederror-Session file is corrupted](https://github.com/microsoft/vscode/issues/317566) `#317566`
**ステータス:** Closed | **ラベル:** なし

**概要:** セッションファイル破損 `ephemeral: Invalid literal value` のクラッシュ報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317576 でクローズ。

---

### ![Issue](icons/issue-opened.svg) [Agents: Act on feedback is broken (with data loss) when used in a CCA session](https://github.com/microsoft/vscode/issues/317379) `#317379`
**ステータス:** Open | **ラベル:** なし

**概要:** CCA セッションで Agents の Act on feedback がデータ損失を伴って壊れるバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 緊急度評価。

---

### ![PR](icons/git-pull-request.svg) [add ChatSessionCustomizationItem.source, clean up AICustomizationPromptsStorage](https://github.com/microsoft/vscode/pull/317555) `#317555`
**ステータス:** Merged | **ラベル:** なし

**概要:** ChatSession カスタマイズアイテムに `source` を追加し、AI カスタマイズプロンプトストレージを整理。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Reference to agent-framework sequential_agents sample](https://github.com/microsoft/vscode/issues/317599) `#317599`
**ステータス:** Closed | **ラベル:** なし

**概要:** 外部リポジトリのリンクのみのトラッキング Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: handle corrupted session files gracefully in getChatHistoryImpl (fixes #317566)](https://github.com/microsoft/vscode/pull/317576) `#317576`
**ステータス:** Merged | **ラベル:** なし

**概要:** 破損したセッションファイルを `getChatHistoryImpl` で graceful に処理する修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [[Bug] Incomplete Sanitization in snippetString.ts (Copilot): Unescaped Backslash Corrupts VS Code Snippet Grammar](https://github.com/microsoft/vscode/issues/316999) `#316999`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot の `snippetString.ts` でバックスラッシュが未エスケープのため、VS Code snippet 文法が破壊されるバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** セキュリティ評価。

---

### ![PR](icons/git-pull-request.svg) [chat: persist installed plugin identity](https://github.com/microsoft/vscode/pull/317585) `#317585`
**ステータス:** Merged | **ラベル:** なし

**概要:** インストール済みプラグインの identity を永続化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Sugestão SQL incorreta](https://github.com/microsoft/vscode/issues/317597) `#317597`
**ステータス:** Open | **ラベル:** なし

**概要:** ポルトガル語の SQL 提案精度に関する不具合報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現情報待ち。

---

### ![Issue](icons/issue-opened.svg) [[Bug] Improper Encoding in Copilot Git Commit Message Sanitizer — Double-Escaping Allows Shell Argument Injection](https://github.com/microsoft/vscode/issues/316995) `#316995`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot の Git commit メッセージサニタイザの二重エスケープにより shell 引数注入を許す問題（#316996 と類似）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** セキュリティ修正。

---

### ![Issue](icons/issue-opened.svg) [菜单右键 `用vs code 打开` 版本更新后会丢失](https://github.com/microsoft/vscode/issues/308133) `#308133`
**ステータス:** Closed | **ラベル:** なし

**概要:** Windows メニューの「用 VS Code 打开」がバージョン更新で消える問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Agents Insider always shows "Update Available"](https://github.com/microsoft/vscode/issues/310947) `#310947`
**ステータス:** Closed | **ラベル:** なし

**概要:** Agents Insider が常に「Update Available」と表示する不具合。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [cannot change the model](https://github.com/microsoft/vscode/issues/314647) `#314647`
**ステータス:** Closed | **ラベル:** なし

**概要:** モデル変更ができない不具合報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add agent (grok build, cursor) and agy (antigravity) CLIs to ignoredC…](https://github.com/microsoft/vscode/pull/317532) `#317532`
**ステータス:** Merged | **ラベル:** なし

**概要:** `agent` および `agy` CLI を ignored CLI リストに追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Empty sessions part with grid](https://github.com/microsoft/vscode/pull/317309) `#317309`
**ステータス:** Open | **ラベル:** なし

**概要:** 空のセッション部分をグリッド表示にする変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Address GHE onboarding issues](https://github.com/microsoft/vscode/pull/317205) `#317205`
**ステータス:** Merged | **ラベル:** なし

**概要:** GitHub Enterprise オンボーディング関連の問題への対処。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [sessions: workspace picker emits folder URI, session type picker spans providers](https://github.com/microsoft/vscode/pull/317525) `#317525`
**ステータス:** Merged | **ラベル:** なし

**概要:** ワークスペースピッカーが folder URI を emit し、セッション型ピッカーがプロバイダをまたぐように改修。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update Copilot CLI terminal hint experiment to default](https://github.com/microsoft/vscode/pull/317366) `#317366`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot CLI のターミナルヒント実験をデフォルト化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Issue reporter wizard](https://github.com/microsoft/vscode/pull/317577) `#317577`
**ステータス:** Merged | **ラベル:** なし

**概要:** Issue reporter にウィザード形式の入力フローを追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chore: bump Octokit APIs](https://github.com/microsoft/vscode/pull/317403) `#317403`
**ステータス:** Closed | **ラベル:** なし

**概要:** Octokit API 更新の試行（#317632 に置換）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

## qmk/qmk_firmware

> 直近24時間は LED マトリクス絡みのバグ報告 1 件のみ。

### ![Issue](icons/issue-opened.svg) [[Bug] Idobao ID75 V3 (Drop) Issue with RGB Matrix](https://github.com/qmk/qmk_firmware/issues/23619) `#23619`
**ステータス:** Open | **ラベル:** bug, help wanted

**概要:** Drop 版 Idobao ID75 v3 キーボードで、id75/v2 用 QMK ファームウェアを使うと RGB マトリクスのアニメーション（特に上段付近のリアクティブ効果）でレインボーの偽点灯やオーバーフローが発生する。

**直近のアクティビティ:** DutchMex が筐体を分解し、Drop V3 では底面に 10 個の LED が確認できたこと、Drop V3 が AtMega ベースのため `.uf2` ファームが使えないこと、v2 ビルドは天面 75 + 底面 10 = 85 LED 想定に見えるが実機と矛盾する点をコメント。

**次のアクション:** Drop V3 専用ファームの整理。
