# GitHub ダイジェスト — 2026-05-27

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Claude agent — Phase 11: customizations / plugins](https://github.com/microsoft/vscode/pull/318113)** (microsoft/vscode) — VS Code 内蔵 Agent Host の大型フェーズ 11。Claude エージェントに customizations / plugins 拡張機構を追加する重要な節目で、Agent Host 周辺の議論が今日も活発。
2. ![Issue](icons/issue-opened.svg) **[`uv` tried to use wheels which don't exist](https://github.com/astral-sh/uv/issues/16841)** (astral-sh/uv) — uv キャッシュの整合性破壊問題。ext4 上で fsync/rename の挙動を巡る根本原因解析が続いており、ビルドツールの信頼性に直結する論点。
3. ![PR](icons/git-pull-request.svg) **[Renault: Initial platform support and Renault 5 E-Tech port](https://github.com/commaai/opendbc/pull/3361)** (commaai/opendbc) — Renault プラットフォームと Renault 5 E-Tech の openpilot 初期サポート。新車種対応の追加。
4. ![PR](icons/git-pull-request.svg) **[\[WIP\] XAP — eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733)** (qmk/qmk_firmware) — QMK の長期 WIP である XAP プロトコル。ホスト側ツールとファームウェアを接続する次世代プロトコルで、進展があると周辺ツールへの影響も大きい。
5. ![PR](icons/git-pull-request.svg) **[Add a full "lockfile" to tool receipts](https://github.com/astral-sh/uv/pull/18937)** (astral-sh/uv) — `uv tool` のレシートに完全なロックファイルを埋め込む拡張。`uv tool install` の再現性向上に直結する設計変更。

---

## sunnypilot/sunnypilot

> 直近24時間の更新はありません。

---

## commaai/openpilot

> webrtcd のフレームタイミング修正やドキュメント更新といった小粒な PR が複数マージされ、自動化された依存更新 PR もマージされている。一方で Ubuntu MATE 24.04 でのビルド失敗 Issue がクローズされており、コア開発と環境保守の両面で動きがある。

### ![PR](icons/git-pull-request.svg) [fix frame timing](https://github.com/commaai/openpilot/pull/38090) `#38090`
**ステータス:** Merged | **ラベル:** なし

**概要:** マージ作業中に誤って混入した `video_tracks` への参照ミスを修正する PR。webrtcd のフレームタイミング処理におけるリグレッションを解消する小さな修正で、追加 2 行・削除 3 行のみの軽微な変更となっている。

**直近のアクティビティ:** adeebshihadeh から「ログを読んだときに変更内容がわかりやすいよう、コミットメッセージを `webrtcd: fix frame timing` のように少し詳しくしてほしい」というレビューコメントが入った。CI の Process replay diff report は 66 セグメントすべて pass し、差分は 0 件で問題なしと報告されている。

**変更ファイル:** `system/webrtc/webrtcd.py` 1 ファイルのみの修正。

**次のアクション:** マージ済みのため追加対応は不要だが、レビュー指摘を踏まえ今後コミットメッセージにモジュール名のプレフィックスを付ける運用を徹底するとよい。

---

### ![Issue](icons/issue-opened.svg) [Cannot build on Ubuntu (MATE) 24.04.3 with supplied setup script](https://github.com/commaai/openpilot/issues/36414) `#36414`
**ステータス:** Closed | **ラベル:** bug

**概要:** Ubuntu MATE 24.04.3 上で公式セットアップスクリプトを使った openpilot のビルドに失敗するという Issue。tinygrad の ONNX ランナーから dmonitoring_model.onnx をコンパイルする過程で、ELF ローダーが未定義シンボル `__extendhfsf2` の再配置に失敗し `RuntimeError` で scons が停止する症状が報告されている。報告者の主目的は Cabana のビルドであり、プリビルド版があれば代替で構わないとのこと。

**直近のアクティビティ:** 直近 24 時間のコメントなし。長期間オープンだったが今回クローズされた。

**次のアクション:** クローズ済みのため対応不要。同様の clang/compiler-rt まわりの未定義シンボル問題が他環境でも再発しないか、tinygrad の CPU バックエンド側の互換性を継続監視する。

---

### ![PR](icons/git-pull-request.svg) [docs: update DM description](https://github.com/commaai/openpilot/pull/38089) `#38089`
**ステータス:** Merged | **ラベル:** なし

**概要:** ドライバーモニタリング（DM）に関する記述を `docs/SAFETY.md` で更新するドキュメント修正 PR。本文の説明はなく、追加 2 行・削除 2 行の軽微な変更でマージされた。

**直近のアクティビティ:** 直近のコメントは CI bot による Process replay diff report のみで、66 セグメント pass・差分 0 件で問題なし。人間のレビューコメントは付いていない。

**変更ファイル:** `docs/SAFETY.md` 1 ファイルのみの修正。

**次のアクション:** マージ済みのため追加対応は不要。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update Python packages](https://github.com/commaai/openpilot/pull/38003) `#38003`
**ステータス:** Merged | **ラベル:** bot

**概要:** repo-maintenance bot による自動的な Python パッケージ更新 PR。`uv.lock` を中心に opendbc / panda / tinygrad などのサブモジュール参照や `docs/CARS.md` の自動生成内容を含む 7 ファイル・283 追加 / 282 削除の大規模な依存更新となっている。`.venv` のサイズや上位パッケージ一覧、`uv pip tree` 出力も本文に添付されている。

**直近のアクティビティ:** adeebshihadeh と sirhcm がそれぞれ COMMENTED 状態でレビューを実施したが、いずれもレビュー本文は空。最終的にマージされた。

**変更ファイル:** `uv.lock`（165/166 行）と `docs/CARS.md`（113/111 行）が中心で、`cereal/messaging/__init__.py`、`opendbc_repo`、`panda`、`tinygrad_repo`、`selfdrive/selfdrived/tests/test_alerts.py` の各サブモジュール参照やテストも更新。

**次のアクション:** マージ済みのため追加対応は不要だが、tinygrad / opendbc / panda のサブモジュール更新によるリグレッション有無を、後続 PR の Process replay 結果で継続確認するとよい。

---

### ![PR](icons/git-pull-request.svg) [split warp release](https://github.com/commaai/openpilot/pull/38088) `#38088`
**ステータス:** Closed | **ラベル:** なし

**概要:** modeld のビルド・コンパイル処理を見直し、warp（モデルの空間変換）部分のリリース処理を分離するドラフト PR。`SConstruct`、`selfdrive/modeld/SConscript`、`selfdrive/modeld/compile_modeld.py`、`selfdrive/modeld/modeld.py` の 4 ファイルにわたり 70 追加 / 73 削除を行うリファクタ的な変更で、本文はテンプレートのコメントのみ記入なしの状態でクローズされた。

**直近のアクティビティ:** commaci-public bot がモデルリプレイ成果物（velocity.x、desiredCurvature、laneLines.y、driver monitoring 各種確率など複数のプロット画像）へのリンクを投稿。人間のレビューコメントは付かないままクローズされた。

**変更ファイル:** modeld のビルドスクリプト（`SConstruct`、`SConscript`）とコンパイル・ランタイム（`compile_modeld.py`、`modeld.py`）の 4 ファイル。

**次のアクション:** ドラフトのままクローズされたため、warp 切り出しのアプローチを再検討するか、別 PR として再提出する必要があるか作成者と方針を確認する。

---
## commaai/opendbc

> 直近24時間は新規車種プラットフォーム対応(Renault、MG/SAIC、GWM Haval H6)とFord CAN FD横方向制御リファクタなど、車両ポート関連のPRが活発に動いている。

### ![PR](icons/git-pull-request.svg) [Renault: Initial platform support and Renault 5 E-Tech port](https://github.com/commaai/opendbc/pull/3361) `#3361`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car

**概要:** Renault 5 E-Tech を opendbc へ取り込むための初期プラットフォーム定義、DBC、CarState パース、ダッシュカム専用のスタブ CarController を追加する PR。レガシーCAN の fingerprinting と Renault 固有パーサーのスモークテストも試みている。著者は comma 実機を持たず、red panda を用いた開発ハーネスでデータ取得中。

**直近のアクティビティ:** 著者 rok がレビュアーからの「openpilot/loggerd でルートを取得し rlog.zst をアップロードすればよい」という助言に対し、「rlog.zst はどこに提供すればよいか」と返信し、データ提出先を確認している。

**変更ファイル:** opendbc/car/renault/配下に__init__.py、carcontroller.py、carstate.py、fingerprints.py、interface.py、values.py を新規追加。opendbc/dbc/renault_5_etech.dbc を新規追加(+190 行)。docs/CARS.md、routes.py、test_fw_fingerprint.py、torque_data/override.toml、values.py、labeler.yaml も更新(全 13 ファイル、+395/-3)。

**次のアクション:** 著者が rlog.zst の提出方法をメンテナに確認中。提出ルートでのバリデーション完了後、ダッシュカムモードでのマージ可否が判断される見込み。

---

### ![PR](icons/git-pull-request.svg) [MG/SAIC Motor: Initial Platform Support](https://github.com/commaai/opendbc/pull/3270) `#3270`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car, car safety

**概要:** MG 5 EV (2021)と MG ZS EV (2022)を opendbc 初の中国メーカー車種としてサポートする初期実装。両車種とも VW A ハーネスのコネクタが利用可能。現状は実車での(injection を含む)テスト環境が無いためダッシュカム専用。

**直近のアクティビティ:** レビュアーadeebshihadeh が「シンプルで良いポート」と評価した上で、longitudinal 制御対応の見通しを尋ねるコメントを投稿。

**変更ファイル:** opendbc/car/mg/配下に carcontroller.py、carstate.py、fingerprints.py、interface.py、mgcan.py、values.py を新規追加。opendbc/dbc/mg.dbc(+327 行)、opendbc/safety/modes/mg.h(+113 行)、opendbc/safety/tests/test_mg.py を新規追加。car.capnp、routes.py、values.py 等も更新(全 18 ファイル、+796/-2)。

**次のアクション:** 著者が longitudinal 対応の意向や実車テスト(レンタル含む)の可否について応答する必要がある。

---

### ![PR](icons/git-pull-request.svg) [Ford: Use Path Angle and Offset for CAN FD Lateral Control](https://github.com/commaai/opendbc/pull/3315) `#3315`
**ステータス:** Open (Draft) | **ラベル:** car, ford, car safety

**概要:** Ford CAN FD 車両の PSCM(Power Steering Control Module)に対し、従来の curvature(c2)単独送信から、モデルが出力する予測軌跡の heading(c1=path_angle)と lateral offset(c0=path_offset)を送る方式へ切り替える大規模リファクタ。c2 を送り続けると PSCM 内部状態が累積してドリフトが発生する問題を回避し、90 度ターンや低速時の応答性、緩やかな S 字カーブなど従来不可能だった操舵を実現する。c1 は 95%、c0 は 90%でクリップし、mode=2(PathFollowingExtended)と ramp_type=3(Immediate)を使用。非 CAN FD 車両は既存パイプラインを維持。

**直近のアクティビティ:** 直近 24 時間のコメントなし(車挙動レポート bot による 91 セグメントの自動リプレイは全 PASS 済み)。

**変更ファイル:** opendbc/car/ford/carcontroller.py(+79/-23)、fordcan.py(+5/-4)を修正。opendbc/car/ford/lateral_bal.py を新規追加(+298 行)。opendbc/safety/modes/ford.h(+31/-9)、opendbc/safety/tests/test_ford.py(+48/-10)も更新(全 5 ファイル、+461/-46)。

**次のアクション:** Q3 Fords での動作検証者の確保とドラフト解除に向けたレビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [Honda - electric brake booster refactor](https://github.com/commaai/opendbc/pull/2919) `#2919`
**ステータス:** Open | **ラベル:** car, honda, car safety

**概要:** Honda の electric brake booster 関連処理のリファクタ。シグナル名を alt_brake から electric_brake_booster へ改名し、fingerprint 内のメッセージ存在と対応付ける。手動フラグ管理から自動検出への移行により、新規車種マージの容易化を狙う。test routes および huggingface 上のデータで 0x1BE メッセージの取りこぼしがないことは検証済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** opendbc/car/honda 配下の carstate.py、interface.py、values.py を修正。opendbc/safety/modes/honda.h(+21/-21)と tests/test_honda.py(+21/-21)、libsafety_py.py、safety.c も更新(全 7 ファイル、+55/-58)。

**次のアクション:** メンテナからのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [GWM Haval H6: Initial Platform Support](https://github.com/commaai/opendbc/pull/3263) `#3263`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car, car safety

**概要:** GWM Haval H6 の opendbc 初期プラットフォーム対応 PR。著者および複数の協力者(Conrado、otaviobps、facundomon)の comma three/threex/four でのテストルートを用意。Haval Jolion については別ポートが必要(EPS からカメラへの RX が異なる)。専用ハーネスや回路図、カメラカバー外しの手順も Discord リンクで共有。

**直近のアクティビティ:** レビュアーadeebshihadeh が「かなり完成度の高いポート」と評価しつつ、「コード整理に LLM(codex など)の活用を推奨」と提案。著者 AlexandreSato が連続して 5 件のレビューコメントで応答中。

**変更ファイル:** opendbc/car/gwm/配下に carcontroller.py、carstate.py、fingerprints.py、gwmcan.py(+181 行)、interface.py、values.py を新規追加。opendbc/dbc/generator/gwm/gwm_haval_h6_mk3.dbc を rename(+91/-40)。opendbc/safety/modes/gwm.h(+211 行)、tests/test_gwm.py(+137 行)も新規追加(全 20 ファイル、+1019/-43)。

**次のアクション:** レビュアー指摘に基づくクリーンアップを継続し、再レビュー依頼へ。

---

### ![PR](icons/git-pull-request.svg) [GWM: haval h6](https://github.com/commaai/opendbc/pull/3117) `#3117`
**ステータス:** Open (Draft) | **ラベル:** DBC signals, car, car safety

**概要:** GWM Haval H6 の初期 POC 実装 PR。1 ルートのみで検証されたドラフト段階のもの。後発の PR #3263 で上位互換実装が進行している。

**直近のアクティビティ:** メンテナ adeebshihadeh が「#3263 に superseded された認識でよいか」と確認し、著者 AlexandreSato が「yes!」と肯定。クローズが見込まれる流れ。

**変更ファイル:** opendbc/car/gwm/配下に carcontroller.py、carstate.py、fingerprints.py、gwmcan.py、interface.py、values.py を新規追加。opendbc/dbc/generator/gwm/gwm_haval_h6_mk3.dbc(+303 行)、opendbc/safety/modes/gwm.h(+211 行)、tests/test_gwm.py を新規追加。car.capnp、routes.py、values.py 等も更新(全 19 ファイル、+1171/-3)。

**次のアクション:** PR #3263 への一本化に伴い、本 PR はクローズ予定。

---
## commaai/panda

> 直近24時間の更新はありません。

---

## neovim/neovim

> LSP・診断機能・statusline・fold・ui2 など UI 周辺のバグ修正が活発に進む一方、WASM/Emscripten ビルド対応や Zig ベースのテスト基盤刷新といった大規模な基盤強化、Windows のパス処理見直しなど中長期テーマの議論も並行して進んでいる。

### ![Issue](icons/issue-opened.svg) [vim.diagnostic.open_float uses wrong max width for `relative = 'editor'`](https://github.com/neovim/neovim/issues/39958) `#39958`
**ステータス:** Closed | **ラベル:** diagnostic

**概要:** `vim.diagnostic.open_float` で `relative = 'editor'` を指定した場合でも、`vim/lsp/util.lua` 内のロジックが分割ウィンドウの幅を基準にしてしまい、フロートウィンドウの最大幅が画面全幅ではなくスポーンしたウィンドウ幅にクリップされる不具合。`:vsplit` 後に診断を表示すると本来全幅で開くはずのフロートが半幅になる。`v0.12.2` で再現。

**直近のアクティビティ:** 報告者 Diablo-D3 が、@glepnir の提案に基づく @acehinnnqru の修正 PR がマージされて問題が解消したことを確認するコメントを残しクローズ。

**次のアクション:** 修正済み。`#39977` のリリースに含まれるため追加対応は不要。

---

### ![Issue](icons/issue-opened.svg) [`vim.pack` options](https://github.com/neovim/neovim/issues/34772) `#34772`
**ステータス:** Open | **ラベル:** lua, options, packages

**概要:** `vim.pack` のグローバル設定機能を提供し、`job.threads`（並列スレッド数）や `job.timeout`、`vim.pack.add()` のデフォルト `opts` などをユーザが設定可能にする提案。前方互換性のある設計が議論されている。

**直近のアクティビティ:** folliehiyuki が submodule の取得制御など `vim.pack.Spec` に Git オプションを追加したいと提案。これに対して echasnovski は「本 Issue のスコープはグローバル設定であり、プラグイン仕様に Git フェッチ用フィールドを追加することには強く反対」と返答。

**次のアクション:** グローバル設定とプラグイン仕様の役割分担についてメンテナ側の方針整理が必要。submodule 制御は別 Issue として切り出すか検討。

---

### ![PR](icons/git-pull-request.svg) [fix(ex_cmds): improve E77 glob expansion message](https://github.com/neovim/neovim/pull/39843) `#39843`
**ステータス:** Open | **ラベル:** AI assisted

**概要:** E77「Too many file names」エラーメッセージを、glob 展開がサポートされない単一ファイル用コマンドであることが分かるよう改善。`runtime/doc/message.txt` の説明文も更新し、複数ファイルを開きたいユーザを `:next` に誘導。`:edit` への glob 引数に対する機能テストも追加。`#12139` をクローズする目的。

**直近のアクティビティ:** Copilot レビューアがファイル概要をコメント。justinmk が複数回レビューしフィードバック。投稿者 puneetdixit200 は Windows の functional テスト失敗を確認したが、失敗箇所は変更対象外の `plugin`/`treesitter`/`ui`/`vimscript`/`terminal` グループで Nvim プロセスが予期せず終了する別問題と報告。再実行も GitHub に拒否されている。

**変更ファイル:** `src/nvim/errors.h`（E77 メッセージ修正）、`runtime/doc/message.txt`（ヘルプ更新）、`test/functional/ex_cmds/edit_spec.lua`（テスト追加）の 3 ファイル、+26/-4 行。

**次のアクション:** Windows CI の不安定要因が別問題であることを示した上でレビュアー再確認待ち。再実行可能なタイミングで CI 通過を確認したい。

---

### ![PR](icons/git-pull-request.svg) [fix(statusline): support %= inside fixed-width groups](https://github.com/neovim/neovim/pull/39985) `#39985`
**ステータス:** Open | **ラベル:** statusline, AI assisted

**概要:** `%20(foo%=bar%)` のように固定幅グループ内に `%=` を書いた場合、本来は左右分割の区切りとして機能すべき `%=` が無視されていた不具合を修正。`rulerformat` を statusline 内にレンダリングする際の先頭固定幅グループの保持も合わせて改善し、固定幅グループ内で `%=` をローカルに処理するよう変更。

**直近のアクティビティ:** glepnir と投稿者 NeddM が 5/25〜5/26 にかけて複数回のレビューラリーを実施中。具体的な実装方針について議論が継続。

**変更ファイル:** `src/nvim/statusline.c`（+74/-14）、`test/functional/ui/statusline_spec.lua`（+18）の 2 ファイル。

**次のアクション:** glepnir からのフィードバックを反映したうえで再レビューと統合判断が必要。

---

### ![Issue](icons/issue-opened.svg) [Override [m, ]m, [M, ]M builtin mappings when treesitter is enabled](https://github.com/neovim/neovim/issues/39978) `#39978`
**ステータス:** Open | **ラベル:** treesitter

**概要:** ビルトインの `[m`/`]m`/`[M`/`]M` モーション（メソッドの先頭/末尾移動）は Java など限定言語向けで現代的なシナリオではほぼ機能しない。treesitter が有効な場合に標準ハイライトクエリ（`@function` / `@function.method`）を活用して、より精度の高いメソッド移動を提供する案。

**直近のアクティビティ:** clason はメンテナンス負荷の観点から「追加の treesitter クエリ群はメンテしない方針。やるなら標準ハイライトクエリに依拠する形でのみ」と立場表明。andreihh は「100% 正確でなくても現状より改善するなら歓迎」と前向きに応答。代替として `nvim-treesitter-textobjects` を採用する案にも触れる。

**次のアクション:** 標準ハイライトクエリのみを使う実装方針で合意するか、サードパーティ拡張に委ねるか方向性を確定する。

---

### ![PR](icons/git-pull-request.svg) [fix(shada): prevents case-duplicate oldfiles](https://github.com/neovim/neovim/pull/40013) `#40013`
**ステータス:** Open | **ラベル:**（なし）

**概要:** Windows と macOS では `oldfiles` 内で大文字小文字のみ異なる重複エントリが同じファイルを指し得るため、重複判定前にパスを小文字化することで対処。`Set_path_t` / `hash_path_t` / `equal_path_t` での lowercase 正規化方式も検討。`#25033` をクローズ目的。Draft 状態。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/map.c`（+32）、`src/nvim/map_defs.h`、`src/nvim/shada.c`、`test/functional/shada/shada_spec.lua` の 4 ファイル、+62/-5 行。

**次のアクション:** Draft 解除前に 2 つのアプローチ（明示的 lowercase 変換 vs. `Set_path_t` 導入）から方針確定。レビューと CI 通過確認。

---

### ![Issue](icons/issue-opened.svg) [build(wasm): Add WebAssembly/Emscripten build target [Tracking issue]](https://github.com/neovim/neovim/issues/39987) `#39987`
**ステータス:** Open | **ラベル:** wasm-webassembly, platform:web

**概要:** GSoC 2026 として WASM/Emscripten を Neovim 公式サポートビルドターゲットに追加する追跡 Issue。`nvim.wasm` をブラウザ上で完全な編集機能（シンタックスハイライト・Lua スクリプト・プラグイン・ヘルプ・xterm.js 経由のターミナル UI）付きで動作させることが目標。`#35567` への対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 月別マイルストーン（Week 1: `build.zig` のリンクエラー解消など）に沿って `#40008` などの実装 PR の進捗を追跡。

---

### ![PR](icons/git-pull-request.svg) [fix: set correct '[ mark when pasting chunks](https://github.com/neovim/neovim/pull/33025) `#33025`
**ステータス:** Merged | **ラベル:**（なし）

**概要:** `nvim_paste` 系でチャンク単位の貼り付け時に `'[` マーク（直前に変更/挿入されたテキストの開始位置）が正しくセットされていなかった問題を修正。`#32960` の解決。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_editor.lua`（+7/-1）、`test/functional/api/vim_spec.lua`（+33）の 2 ファイル。

**次のアクション:** マージ済み。追加対応不要。

---

### ![PR](icons/git-pull-request.svg) [feat: introduce nvim_buf_del and deprecate nvim_buf_delete](https://github.com/neovim/neovim/pull/33345) `#33345`
**ステータス:** Closed | **ラベル:** api

**概要:** `nvim_buf_delete` を非推奨化し、新しい `nvim_buf_del` を導入する提案 PR。詳細は `#33314` にあり。代替アプローチとしての `#33370` に発展したためクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/api.txt`、`runtime/lua/vim/_meta/api.lua`、`src/nvim/api/buffer.c`、複数の `test/functional/...` など 13 ファイル、+73/-53 行。

**次のアクション:** 後続 PR（`#33370` など）での議論継続。本 PR 自体は追加作業不要。

---

### ![PR](icons/git-pull-request.svg) [feat: introduce new api.nvim_buf_del](https://github.com/neovim/neovim/pull/33370) `#33370`
**ステータス:** Closed | **ラベル:** api

**概要:** `#33345` / `#33314` のコメントを取り込んだ `nvim_buf_del` 設計の Draft 実装。`type`（"delete"/"wipeout"/"unload"）、`force` オプション、新規 `preserve_layout` オプション（バッファ削除後もウィンドウレイアウトを保持）をサポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/api/buffer.c`、`src/nvim/api/keysets_defs.h`、`runtime/doc/api.txt`、`test/functional/api/buffer_spec.lua` など 6 ファイル、+235/-0 行。

**次のアクション:** echasnovski から設計とテスト面のコメントあり。C 部分のレビュアー確保が必要だがクローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): considering '\n' when get_content_length](https://github.com/neovim/neovim/pull/33413) `#33413`
**ステータス:** Closed | **ラベル:** lsp

**概要:** LSP の `rpc.lua` でヘッダ解析を `vim.gsplit` から `string:gmatch` に変更したコミット（`42657e7`）以降、`"\r\n"` の改行処理が変わり content-length が誤って取得される回帰を修正する変更。`#33408` の対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/lsp/rpc.lua` 1 ファイル、+1/-1 行。

**次のアクション:** クローズ済み。別アプローチで対応されたか確認が必要。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): jump to the target win when opts.reuse_win is true](https://github.com/neovim/neovim/pull/33476) `#33476`
**ステータス:** Merged | **ラベル:** lsp

**概要:** LSP の jump 系操作で `opts.reuse_win = true` のとき、対象シンボルを既に表示しているウィンドウへ実際にフォーカスを切り替えるよう修正。`#33458` の対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/lsp/buf.lua`（+7/-1）、`test/functional/plugin/lsp_spec.lua`（+13/-2）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(spell): save spell files to stdpath('data')/site/spell](https://github.com/neovim/neovim/pull/33528) `#33528`
**ステータス:** Merged | **ラベル:** spell

**概要:** スペル辞書ファイルの保存先を `stdpath('data')/site/spell` に変更する修正。`#33483` の対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/news.txt`、`runtime/doc/options.txt`、`runtime/doc/vim_diff.txt`、`runtime/lua/vim/_meta/options.lua`、`src/nvim/options.lua`、`src/nvim/spellfile.c`、`test/functional/core/spellfile_spec.lua` の 7 ファイル、+9/-9 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(titlestring): add ':p' to default titlestring option](https://github.com/neovim/neovim/pull/33550) `#33550`
**ステータス:** Closed | **ラベル:**（なし）

**概要:** デフォルト `'titlestring'` の `expand` 引数に `:p` を追加してフルパスを取得するよう修正する Draft PR。`#32411` のクローズが目的。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/options.txt`、`runtime/doc/vim_diff.txt`、`runtime/lua/vim/_meta/options.lua`、`src/nvim/buffer.c`、`src/nvim/options.lua` の 5 ファイル、+5/-5 行。

**次のアクション:** クローズ済み。別方針での対応が必要かを確認。

---

### ![PR](icons/git-pull-request.svg) [fix(api): nvim_parse_cmd don't put key 'range' when ea.addr_count == 0](https://github.com/neovim/neovim/pull/33536) `#33536`
**ステータス:** Merged | **ラベル:** api

**概要:** `nvim_parse_cmd` の戻り値で、`ea.addr_count == 0` のときに従来は空の `{}` を `range` キーとして含めていたものを、キー自体を出力しないよう修正。`#33394` の対応。テストも追従。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/api/command.c`（+4/-6）、`test/functional/api/vim_spec.lua`（+2/-6）。

**次のアクション:** マージ済み。挙動変更を伴うので利用側プラグインのリリースノート確認推奨。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): calc correct screen_width when opts.relative == 'editor'](https://github.com/neovim/neovim/pull/39977) `#39977`
**ステータス:** Merged | **ラベル:** lsp

**概要:** `#39958` の修正 PR。`opts.relative == 'editor'` のときは `screen_width` を `vim.o.columns` に合わせるよう調整し、ホバー等のフロートウィンドウがエディタ幅全体を使えるようにする。@glepnir 提案を採用。

**直近のアクティビティ:** MariaSolOs が APPROVED してマージ済み。

**変更ファイル:** `runtime/lua/vim/lsp/util.lua` 1 ファイル、+1/-1 行。

**次のアクション:** マージ済み。`#39958` クローズに反映済み。

---

### ![Issue](icons/issue-opened.svg) [ui2: pressing `<CR>` with active `getchar()` can switch focus to command line](https://github.com/neovim/neovim/issues/40010) `#40010`
**ステータス:** Open | **ラベル:** ui2

**概要:** ui2 を有効にし `cmdheight=0` の状態で `vim.fn.getchar()` 実行中に `<CR>` を押すと、フォーカスがコマンドラインに切り替わってしまう問題。再現用 init.lua 付き。

**直近のアクティビティ:** luukvbaal が「ui2 は `vim.on_key()` ハンドラで `<CR>` をページャ起動キーとして奪っている。アクティブな `getchar()` がそのキーを消費すべきか検出する手段は無さそう」と説明。報告者 echasnovski も「メッセージ操作には明示的に `g<` がある以上、`<CR>` 必須かどうか検討の余地あり」と返答。

**次のアクション:** ui2 における `<CR>` のセマンティクス（ページャ起動 vs. 通常入力）を整理し、`getchar()` アクティブ時はキーを消費しない方針が取れるか検討する。

---

### ![Issue](icons/issue-opened.svg) [ui2: dialog window with shell output overrides input](https://github.com/neovim/neovim/issues/40011) `#40011`
**ステータス:** Closed | **ラベル:** ui2

**概要:** ui2 のメッセージ転送先（`msg.targets`）に `dialog` を設定すると、`:!ls` などの shell 出力ダイアログが `:q` で閉じられない・移動キーが奪われるなどの挙動を示す問題。

**直近のアクティビティ:** luukvbaal が「`dialog` をデフォルトメッセージターゲットに使う構成は未ドキュメントでサポート外」と回答。報告者 comfysage は「了解、ui2 への作業に感謝。0.13 で安定するのが楽しみ」と返答してクローズ。

**次のアクション:** 仕様外利用のためクローズ。サポートしたい場合は別途要望として再整理。

---

### ![Issue](icons/issue-opened.svg) [Cam's bugs](https://github.com/neovim/neovim/issues/39990) `#39990`
**ステータス:** Closed | **ラベル:**（なし）

**概要:** Issue テンプレートのプレースホルダのみが残された未記入のバグ報告。問題内容や再現手順・期待動作のいずれも書かれていない。

**直近のアクティビティ:** 報告者 CamBFunny が「バグレポートが自動生成されると勘違いしていた、申し訳ない」とコメントしクローズ済み。

**次のアクション:** なし。

---

### ![Issue](icons/issue-opened.svg) [setline() CR mapping autoindent fix (9.2.0510) breaks insert-mode bracket expansion for plugins using map_cr](https://github.com/neovim/neovim/issues/39997) `#39997`
**ステータス:** Open | **ラベル:** bug-vim, bug-regression

**概要:** vim-patch:9.2.0510（コミット `c8dbb04`）以降、`setline()` を使う `<CR>` 挿入モードマッピング（典型例は `nvim-autopairs` の `map_cr = true`）でブラケット展開が壊れ、カーソルが閉じ括弧の後ろに置かれてしまう回帰。autoindent 剥がしロジックの条件変更が原因。

**直近のアクティビティ:** glepnir が上流の修正 PR https://github.com/vim/vim/pull/20332 を共有。

**次のアクション:** 上流修正のマージ完了を待ち、vim-patch として取り込み。

---

### ![Issue](icons/issue-opened.svg) [Update global mark position on BufLeave, VimLeavePre](https://github.com/neovim/neovim/issues/36358) `#36358`
**ステータス:** Closed | **ラベル:** marks, editor

**概要:** Harpoon 系プラグインが解決している課題、つまり「グローバルマークがファイル離脱時にカーソル位置を更新しない」点を、`BufLeave`/`VimLeavePre` の autocommand で更新するよう要望する Issue。サンプルコードあり。

**直近のアクティビティ:** 報告者 yilisharcs が「対応完了。詳細は Discussion 36329 のコメント参照」と書き残してクローズ。

**次のアクション:** クローズ済み。Discussion 側で結論を確認。

---

### ![PR](icons/git-pull-request.svg) [build(zig): run oldtests in zig builds (RETROMANIA HYPERDRIVE)](https://github.com/neovim/neovim/pull/39074) `#39074`
**ステータス:** Open | **ラベル:** build, ci

**概要:** Zig ビルドで oldtests を走らせるための変更。実態は Zig 固有というよりも、Makefile + シェルスクリプト + Vimscript ラッパー（被テスト Vim 上で動く）の組み合わせを、シンプルな nvim-Lua ラッパーに置き換えるリファクタ。`--headless` nvim を `:terminal` 内で動かす従来の二重構造を見直し、`oldesttests` の残骸も削除。Draft。

**直近のアクティビティ:** 直近 24 時間のコメントなし（直近の活動は 5/26 のメタ更新）。justinmk と clason が以前にコメント。

**変更ファイル:** `.github/workflows/test.yml`、`runtime/lua/vim/treesitter.lua`、`test/functional/treesitter/language_spec.lua`、新規 `test/old/runner.lua`（+164）、`test/run_tests.zig` の 5 ファイル、+193/-9 行。

**次のアクション:** Draft 解除と CI 通過確認。bfredl のリファクタ意図についてレビューを継続。

---

### ![PR](icons/git-pull-request.svg) [build: add wasm/emscripten build support](https://github.com/neovim/neovim/pull/40008) `#40008`
**ステータス:** Open | **ラベル:** build, gsoc

**概要:** `wasm32-emscripten` ターゲットビルドの初期サポートを `build.zig` に追加する GSoC 成果物。Emscripten 固有コンパイラ設定・WASM 互換ヘッダを参照する正しい sysroot/libc セットアップ・ネイティブ前提の依存関係の調整を含む。LuaJIT が WASM 非対応のため、ビルドは標準 Lua 5.1 にフォールバック。`#39987` の最初のマイルストーン実装。

**直近のアクティビティ:** bfredl が早速レビューコメントを投稿（5/26）。

**変更ファイル:** `build.zig` 1 ファイル、+132/-22 行。

**次のアクション:** bfredl の指摘反映と、追加の Emscripten ビルドステップ・CI 統合の方針整理。

---

### ![Issue](icons/issue-opened.svg) [Windows: Output at cursor position from batch script ran from jobstart()](https://github.com/neovim/neovim/issues/40009) `#40009`
**ステータス:** Open | **ラベル:** platform:windows, job-control, bug-regression

**概要:** Windows で `jobstart()` 経由のバッチスクリプトが `CON` に書き出した出力が、なぜか現在のバッファのカーソル位置に直接挿入されてしまう回帰。スクリーンショット付き。

**直近のアクティビティ:** zeertzjq が「v0.12.2 で再現するが v0.12.1 では発生しない」と切り分け結果を共有。

**次のアクション:** v0.12.1 → v0.12.2 間の Windows job-control 関連変更を二分探索し、コンソール出力ハンドリングの回帰原因を特定。

---

### ![PR](icons/git-pull-request.svg) [fix(folds): foldcolumn is interrupted for virtual line above nested fold](https://github.com/neovim/neovim/pull/39999) `#39999`
**ステータス:** Open | **ラベル:** folds, column, ci:backport release-0.12

**概要:** `#39998` の対応。virtual line 上で `fill_foldcolumn` のロジックを再利用し、外側 fold レベルを計算して foldcolumn に表示するよう修正。条件分岐を増やさず保守性を保つため、末尾の `else if` を `inline` 関数として切り出す方針を採用。

**直近のアクティビティ:** zeertzjq の指摘を受け matystroia が「`foldcolumn > 1` の別エッジケースを修正しテストを追加」と更新報告。さらに zeertzjq が追加コメント。

**変更ファイル:** `src/nvim/drawline.c`（+35/-18）、`src/nvim/statusline.c`（+3/-1）、`test/functional/ui/fold_spec.lua`（+109）の 3 ファイル。

**次のアクション:** zeertzjq の追加レビューを反映し、0.12 への backport ラベル通り安定版へも取り込む。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): conceal backslash in markdown inline](https://github.com/neovim/neovim/pull/39993) `#39993`
**ステータス:** Open | **ラベル:** treesitter

**概要:** Markdown インライン中のバックスラッシュエスケープ（例: `\-`）が見た目に残ってしまう問題に対し、`@conceal` クエリでバックスラッシュを非表示化する変更。あわせて、バックスラッシュエスケープに対する `@string.escape` の付与を取りやめ、他のリテラルと見た目を統一。

**直近のアクティビティ:** igorlfs が「これは上流（tree-sitter-markdown）で直すべき可能性があり、Discussion 37118 と Cloudflare の類似事例を参照」と指摘。投稿者 frarees はホバーで Markdown レンダリング目的だと補足し、テスト用 keymap サンプルを提示。

**変更ファイル:** `runtime/doc/news.txt`（+3/-1）、`runtime/queries/markdown_inline/highlights.scm`（+9/-4）、`test/functional/treesitter/highlight_spec.lua`（+26）の 3 ファイル。

**次のアクション:** 上流対応 vs. Neovim 同梱クエリでの対応の方針を確定。レビュアーの判断待ち。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0538: Cannot keep leading whitespace in %{} statusline expr](https://github.com/neovim/neovim/pull/40007) `#40007`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** `%{}` statusline 式の結果先頭スペースが時折剥がされる・全桁数字結果が数値変換されてしまう問題に対し、評価結果をそのまま挿入する新アトム `%0{}` を追加する vim パッチ移植（glepnir 提案）。`#28918` をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/news.txt`、`runtime/doc/options.txt`、`runtime/lua/vim/_meta/options.gen.lua`、`src/nvim/options.lua`、`src/nvim/statusline.c`、`test/old/testdir/test_statusline.vim` の 6 ファイル、+26/-5 行。

**次のアクション:** マージ済み。ニュース反映済み。

---

### ![PR](icons/git-pull-request.svg) [fix(marks): read from the correct variable in conceal_lines mark collection](https://github.com/neovim/neovim/pull/39991) `#39991`
**ステータス:** Merged | **ラベル:** marks, ci:backport release-0.12

**概要:** `decor_conceal_lines` のマーク収集処理で、重なり判定（1 段目イテレーション）の結果変数を 2 段目イテレーションで誤って使い続けていた不具合を修正。`nvim__set_ns` のウィンドウ別 namespace 割当が `conceal_lines` に効かない事象や、1 段目で重なりが無い場合の未初期化値参照の原因。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/decoration.c` 1 ファイル、+1/-1 行。

**次のアクション:** マージ済み。0.12 への backport ラベル付きで安定版にも反映予定。

---

### ![Issue](icons/issue-opened.svg) [`%!` does not work with `nushell`](https://github.com/neovim/neovim/issues/37371) `#37371`
**ステータス:** Open | **ラベル:** environment

**概要:** Nushell が `<` と `>` のリダイレクト構文を理解しないため、`%!` などのシェル経由フィルタが Nushell 環境では動作しない問題。Vim でも同様で、`E485: Can't read file ...` などのエラーになる。

**直近のアクティビティ:** stg73 が「`shelltemp = false`（Nvim 0.12+ ではデフォルト）にして `shellcmdflag = "--stdin --no-newline --commands"` などを設定すればパイプ経由で動く」と回避策の Lua 設定例を共有。

**次のアクション:** 推奨設定をドキュメント化するか、Nushell 検出時のデフォルト調整可否を検討。

---

### ![Issue](icons/issue-opened.svg) [`'statusline'` `%{…}` expressions will sometimes lose a leading space](https://github.com/neovim/neovim/issues/28918) `#28918`
**ステータス:** Closed | **ラベル:** bug-vim, statusline

**概要:** statusline の `%{...}` 式評価結果について、特定条件で先頭スペース 1 文字が消える不明瞭な挙動。再現手順では `%{expand('%%')}%{&modified?'\ M':''}` で `M` の前のスペースが落ちる。

**直近のアクティビティ:** 直近 24 時間のコメントなし（`#40007` のマージに伴いクローズ）。

**次のアクション:** `%0{}` アトム導入により対処済み。クローズ。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0535: tests: matchit plugin is not tested](https://github.com/neovim/neovim/pull/40006) `#40006`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** matchit プラグインのテスト追加と、html ファイルタイププラグインの `b:match_words` を改善する vim パッチ移植。タグ先頭文字だけでなくタグ全体でマッチさせ、タグ名後のスペースや属性があっても対応タグを見つけられるようにする（Andrey Starodubtsev）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/ftplugin/html.vim`（+2/-1）、新規 `test/old/testdir/test_plugin_matchit.vim`（+58）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(path): normalize separators in functions not covered](https://github.com/neovim/neovim/pull/39155) `#39155`
**ステータス:** Open | **ラベル:**（なし）

**概要:** Windows のパス区切り文字正規化を nvim と外部（`fn.xxx`、`api.xxx`、`:xxx` など）の境界で一貫させる継続作業。`$HOME-windows` の正規化、`nvim_set_current_dir()`/`chdir()` 等の作業ディレクトリ変更エントリーポイント、`--listen`/`--server`/`:restart` および `serverstart()`/`sockconnect()` などの名前付きパイプ周りを対象に大規模な変更。

**直近のアクティビティ:** 投稿者 ntdiary がチェックリスト形式で未解決項目（`fnamemodify()` の `shellslash` 遵守、`do_autocmd_dirchanged` の `slash_adjust` 削除可否など）について自己レビュー継続。

**変更ファイル:** 54 ファイル、+978/-830 行。`src/nvim/{arglist,autocmd,buffer,cmdexpand,ex_docmd,fileio,main,option}.c` ほか、`runtime/doc/options.txt`・`runtime/doc/vimfn.txt` などドキュメント、`src/nvim/msgpack_rpc/server.c`・`src/nvim/event/socket.c` などサーバ／ソケット系を含む広範な変更。

**次のアクション:** 大規模変更のため `#39844` のトラッキング Issue とも連携してメンテナレビューを進める。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0533: '\[ mark moved to end of inserted text after CTRL-R CTRL-P paste](https://github.com/neovim/neovim/pull/40004) `#40004`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted

**概要:** Insert モードで `CTRL-R CTRL-P`/`CTRL-R CTRL-O` によるレジスタ貼り付け後にバックスペース等で編集すると、`stop_arrow()` が `Insstart` を貼り付け後のカーソル位置に書き換えてしまい、`'[` マークが挿入テキストの先頭ではなく末尾を指す問題を修正。`Insstart` の引き戻しは「カーソルが前の `Insstart` より上に移動したとき」に限定する変更（行頭バックスペースは引き続き動作）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/edit.c`（+9/-5）、`test/old/testdir/test_edit.vim`（+14）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: runtime file updates](https://github.com/neovim/neovim/pull/40005) `#40005`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** Zig 上流リポジトリ参照更新（`runtime/compiler/zig*.vim`、`runtime/ftplugin/zig.vim` 等）、`vim` シンタックスの `:def` 戻り型区切り `:` を lookbehind で固定する修正、`spec` ftplugin の整理など、上流ランタイムファイルの同期 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/compiler/zig*.vim`（4 ファイル）、`runtime/ftplugin/spec.vim`（+2/-29）、`runtime/ftplugin/zig.vim`、`runtime/indent/zig.vim`、`runtime/syntax/{vim,zig,zir}.vim` の計 10 ファイル、+14/-41 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.{0531,0536}](https://github.com/neovim/neovim/pull/40003) `#40003`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** `Test_cd_completion()` が MS-Windows で `E344` でも失敗するため `E472` に加えてキャッチするテスト修正（Ken Takata）と、GTK4 環境で xterm_clipboard なしの場合に `Test_invalid_args()` が落ちる問題への対応をまとめた vim パッチ取り込み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test/old/testdir/test_cd.vim`、`test/old/testdir/test_startup.vim` の 2 ファイル、+2/-2 行。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ci: bump zizmorcore/zizmor-action from 0.5.5 to 0.5.6 in the github-actions group](https://github.com/neovim/neovim/pull/40000) `#40000`
**ステータス:** Merged | **ラベル:** ci, dependencies, github_actions

**概要:** Dependabot による `zizmorcore/zizmor-action` の 0.5.5 → 0.5.6 自動更新 PR。同アクションのデフォルト zizmor バージョンが 1.25.2 に上がる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/zizmor.yml` 1 ファイル、+1/-1 行。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [folds: virtual line above nested fold does not display fold level](https://github.com/neovim/neovim/issues/39998) `#39998`
**ステータス:** Open | **ラベル:** folds, column

**概要:** `#39891` ではネスト深さ 1 の単純ケースのみ修正されたが、ネストした fold の場合に virtual line 横の `foldcolumn` が描画されない問題。`virt_lines` と `virt_lines_above` を使った再現スクリプト付き。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正 PR `#39999` のマージ・backport を進める。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): unwanted redraw for buffer not in window](https://github.com/neovim/neovim/pull/40002) `#40002`
**ステータス:** Open | **ラベル:** lsp, diagnostic

**概要:** ロードされたバッファが現在のタブページ内のどのウィンドウにも表示されていない場合に、不要な再描画が走っていた問題を修正。再描画前に「通常ウィンドウで表示されているか」をチェック。`#39983` 関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/diagnostic.lua`（+1/-1）、`runtime/lua/vim/lsp/semantic_tokens.lua`（+3/-1）、`test/functional/lua/diagnostic_spec.lua`（+14）の 3 ファイル。

**次のアクション:** レビューと `#39983` との重複・統合方針の確認。

---

### ![PR](icons/git-pull-request.svg) [fix(api): `nvim__redraw` draws statusline in temporary autocmd windows](https://github.com/neovim/neovim/pull/39983) `#39983`
**ステータス:** Open | **ラベル:** ui

**概要:** `DiagnosticChanged` autocmd が一時的な autocmd ウィンドウ内で `ev.buf` をアクティブにして実行され、その中で `nvim__redraw` が呼ばれることで一時ウィンドウに対する statusline が描画される問題を修正。

**直近のアクティビティ:** luukvbaal が「ウィンドウに無いバッファに対して、statusline 再描画目的の autocommand が `nvim__redraw()` を呼ぶこと自体を避けるのが良さそう」とコメントし、別アプローチ（`#40002`）に発展。

**変更ファイル:** `src/nvim/api/vim.c`（+1/-1）、`test/functional/api/vim_spec.lua`（+21）。

**次のアクション:** `#40002` での diagnostic 側修正と本 PR の API 側ガードのどちらを採用するか、または両方併用するかをメンテナと調整。

---

### ![Issue](icons/issue-opened.svg) [Windows: track UNC and device path root handling](https://github.com/neovim/neovim/issues/39844) `#39844`
**ステータス:** Open | **ラベル:** platform:windows, filesystem

**概要:** Windows のパスルート・UNC 関連のエッジケース（`fnamemodify()`、`vim.fs.normalize()` など複数経路に散在）を 1 つずつパッチしてきたが、`//?C:/` や `//?/BootPartition` など未対応ケースが多数残ることが調査で判明。個別対応を続けると保守性が悪化するため、トラッキング Issue として整理する。

**直近のアクティビティ:** 報告者 barrettruth が「このトラッカ全体を vim/vim 側に上流化すべきと考えるが、関連 Issue でこのトラッカを参照し続けて変更を見失わないようにしたい」とコメント。

**次のアクション:** 上流化方針を決定し、関連 Issue/PR（`#39805`、`#39841`、`#39155` など）との連動でルート・UNC ハンドリングをまとめて改善。

---
## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## ghostty-org/ghostty

> CLI まわりの改修 (+ssh-cache の刷新、+new-tab 追加) と macOS GUI の細かな不具合修正、GTK の GStreamer スレッドリーク対策が同時並行で進行している。RTL 対応や split UI など長期トラッキング Issue にも継続的にコメントが寄せられている。

### ![PR](icons/git-pull-request.svg) [cli: rework +ssh-cache internals and user interface](https://github.com/ghostty-org/ghostty/pull/12814) `#12814`
**ステータス:** Open | **ラベル:** cli

**概要:** `+ssh-cache` のユーザーインターフェイスを刷新し、位置引数のサポートと ISO タイムスタンプ+相対経過時間を含む一貫したリスト出力を追加。実装されていなかった `--expire-days` フラグを Duration ベースの `--prune` に置き換え、エラーコード (0=成功, 1=失敗, 2=利用法) も統一。内部のタイムスタンプ処理やエラーハンドリングも整理し、テストカバレッジを大幅に向上させた。

**直近のアクティビティ:** tristan957 からサブコマンド方式 (`ghostty +ssh add xyz`) を採用する前例があるか質問があり、jparise は既存の `+ssh-cache` インターフェイスとの整合性を理由に現状の long option 方式を維持し、サブコマンド化はフォローアップで検討と回答。tristan957 は納得した。

**変更ファイル:** src/cli/ssh-cache/DiskCache.zig、src/cli/ssh-cache/Entry.zig、src/cli/ssh.zig、src/cli/ssh_cache.zig (4 ファイル、+543/-263)

**次のアクション:** メンテナのレビュー待ち。サブコマンド化は別 PR でフォローアップ検討。

---

### ![Issue](icons/issue-opened.svg) [Add `split-divider-color` configuration](https://github.com/ghostty-org/ghostty/issues/4326) `#4326`
**ステータス:** Closed | **ラベル:** gui

**概要:** GTK と macOS の両方で split divider の色をユーザーが完全にカスタマイズできるよう `split-divider-color` 設定を追加するための Issue。デフォルトは従来の自動色決定ロジックを維持し、自動ロジック自体の改善も将来的に検討する方針。

**直近のアクティビティ:** FuhuXia から、実際の設定キー名が `split-separator-color` であるため過去のコメントの記述を訂正してほしいという指摘があった。

**次のアクション:** 既に Close 済み。コメント内の設定名の記述を更新するか案内する程度。

---

### ![PR](icons/git-pull-request.svg) [macOS: clear stale OSC 11 background cache on config change](https://github.com/ghostty-org/ghostty/pull/12822) `#12822`
**ステータス:** Open | **ラベル:** os/macos

**概要:** `SurfaceView` が OSC 11 で設定された背景色をキャッシュしてしまい、light/dark テーマ自動切替後に古い色がウィンドウクロームに残る問題を修正。`ghosttyConfigDidChange` 内で新しい設定由来の背景色とキャッシュ値が一致しない場合にキャッシュを破棄するガードを追加し、その後の `ghosttyColorDidChange` で再構築される設計。

**直近のアクティビティ:** 作者 b0uks が修正前後の挙動を示す動画を添付した。

**変更ファイル:** macos/Sources/Ghostty/Surface View/SurfaceView_AppKit.swift (1 ファイル、+13/-1)

**次のアクション:** macOS メンテナによるレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [gtk: add +new-tab action](https://github.com/ghostty-org/ghostty/pull/11762) `#11762`
**ステータス:** Open | **ラベル:** cli, gtk

**概要:** GTK 上での自動化に有用な `+new-tab` CLI アクションを追加する PR。`+new-window` 用に整備された仕組みを再利用しつつ、IPC でサーフェスを識別するための一意な surface ID を導入し、通知のコールバックで使われていた raw pointer を排除している。

**直近のアクティビティ:** gitzone83 から、Ghostty の機能性を理由に他端末から乗り換えたユーザーとして、起動時のタブ自動保存/復元のためにもこの機能が必要だとコメントがあり、早期のレビュー・マージを希望する声が改めて寄せられた。

**変更ファイル:** include/ghostty.h、src/apprt/action.zig、src/apprt/embedded.zig、src/apprt/gtk/App.zig、src/apprt/gtk/class/application.zig、src/apprt/gtk/class/window.zig、src/apprt/gtk/ipc/new_tab.zig (新規)、src/apprt/ipc.zig、src/cli/ghostty.zig、src/cli/new_tab.zig (新規) (10 ファイル、+612/-80)

**次のアクション:** メンテナによるレビュー待ち。コミュニティから注目されているため優先度を上げて検討すべき。

---

### ![PR](icons/git-pull-request.svg) [Add sidebar and status indicators](https://github.com/ghostty-org/ghostty/pull/12821) `#12821`
**ステータス:** Closed | **ラベル:** なし

**概要:** macOS 向けに Codex / Claude CLI 用のサイドバーとステータスインジケータを追加する初期実装の PR。64 ファイル・+4443/-152 と大規模で、`TerminalSidebar.swift`、`TerminalAgentActivity.swift`、`agent_hook` 配下などの新規ファイルを多数含む。

**直近のアクティビティ:** ghostty-vouch[bot]が、作者 scottmcpherson が vouched 済みコントリビュータ一覧にいないため自動クローズ。作者本人もフォークの main に向ける予定の PR を誤って upstream に出してしまったと謝罪した。

**変更ファイル:** macos/Sources/Features/Terminal/TerminalSidebar.swift (新規, +728)、macos/Sources/Features/Terminal/TerminalAgentActivity.swift (新規, +331)、macos/Sources/Features/Terminal/Window Styles/TerminalWindow.swift (+585/-24)、src/agent_hook/main.zig (新規, +892)、src/agent_hook/claude (新規, +171) ほか (計 64 ファイル、+4443/-152)

**次のアクション:** 既に自動クローズ済み。作者が vouch を取得した上で、自身のフォーク main 向けに改めて PR を出し直す必要あり。

---

### ![PR](icons/git-pull-request.svg) [apprt/gtk: fix audio-bell GStreamer thread leak (reuse one MediaFile per surface)](https://github.com/ghostty-org/ghostty/pull/12815) `#12815`
**ステータス:** Merged | **ラベル:** gtk

**概要:** オーディオベルを鳴らすたびに `gtk.MediaFile.newForFilename` で GStreamer パイプラインが生成され、GL シンク由来のスレッドが解放されない問題 (約 4 時間で 705 スレッド蓄積) を修正。サーフェスごとに `MediaFile` を 1 つキャッシュ (`priv.bell_media`) し、`bell-audio-path` 変更時のみ再構築、ベル発生時は `seek(0)` + `play()` で同一パイプラインを再利用する設計。回帰テストと headless CI ジョブ (`test/bell-leak.sh`、Xvfb + ソフトウェア GL で 120 回ベルを鳴らしスレッド増を検知) も追加。

**直近のアクティビティ:** jcollie から「`unref` で GStreamer スレッドは止まらないなら、サーフェスごとに依然リークするのでは」というレビューコメント (Changes Requested) があり、pluiedev は LLM 生成コメントの冗長性を指摘しつつロジック自体は問題なしと評価。最終的に jcollie が「リークを完全には解消しないが状況を確実に改善する」として承認し、マージされた。

**変更ファイル:** nix/tests.nix (+109)、src/apprt/gtk/class/surface.zig (+20/-2)、src/apprt/gtk/media.zig (+73/-18) (3 ファイル、+202/-20)

**次のアクション:** マージ済み。残るサーフェス単位のリーク (1000 サーフェス開閉時のスレッド残留懸念) について別途調査・修正が必要。

---

### ![Issue](icons/issue-opened.svg) [RTL Support on macOS](https://github.com/ghostty-org/ghostty/issues/12183) `#12183`
**ステータス:** Open | **ラベル:** os/macos, gui

**概要:** macOS における右から左 (RTL) 言語のレイアウト方向対応をトラッキングする親 Issue。アラビア語・ヘブライ語・ペルシャ語などで Terminal.app や Safari と同様に UI クロームを自動的にミラーリングすべきだが Ghostty は未対応。Xcode の Right-to-Left Pseudolanguage を使った再現手順と Apple HIG/`NSUserInterfaceLayoutDirection` などのリファレンスを含む。

**直近のアクティビティ:** orenmizrahi から「読みづらいので今すぐパッチを当てる方法はないか」という質問があり、00-kat が「この Issue は macOS GUI 側の RTL 課題 (例: 新規タブボタンの右配置) を扱うものであり、ターミナル表面の RTL は #1442」と整理した。

**次のアクション:** サブ Issue の整理と macOS GUI の RTL レイアウト対応実装。質問者には #1442 への誘導が必要。

---

### ![Issue](icons/issue-opened.svg) [Show terminal split path and handy controls within splits](https://github.com/ghostty-org/ghostty/issues/1541) `#1541`
**ステータス:** Open | **ラベル:** os/macos, gui

**概要:** split 上にマウスホバーするとタイトル・閉じるボタン・追加 split 作成 (Option で縦/横切替)・ズームを操作できるツールバーを表示し、ズーム状態ではタイトルバー/タブにも解除ボタンを出すという、peteschaffner 提案の UI を Issue 化したもの。Discord スレッドからの移植で、スクリーンショット付き。

**直近のアクティビティ:** XiminLin がこの機能を実装したと報告し、vouch リクエスト (discussions/12801) を提出済み。macOS で `SurfaceTitleBar` オーバーレイを追加し、各ペインがデフォルトで cwd を表示、ダブルクリックでリネーム可能とのこと。vouch 取得後にブランチを共有予定。

**次のアクション:** XiminLin の vouch 申請の処理。実装内容を PR 化してもらいレビュー。

---
## astral-sh/uv

> 直近24時間は週次の Renovate による依存パッチ PR が一斉にマージされ、加えて PEP 723 重複ブロック拒否やキャッシュ破損対策、ツールロックファイル化など、品質改善とプレビュー機能拡張が同時並行で進んでいる。注目バグはキャッシュ破損問題 (#16841) で、根本原因の議論が活発化している。

### ![PR](icons/git-pull-request.svg) [Use relative path when creating symlinks in cache.](https://github.com/astral-sh/uv/pull/19033) `#19033`
**ステータス:** Open | **ラベル:** なし

**概要:** 非 Windows プラットフォームでキャッシュ内アーカイブを参照する symlink が絶対パスになっていたため、キャッシュディレクトリの再配置や複数コンテナ間での共有ができなかった。本 PR は同一キャッシュ内を相対パスで参照するよう変更し、キャッシュの可搬性を高める。#2908 の関連改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-cache/src/lib.rs` (1 ファイル, +4/-3)

**次のアクション:** メンテナのレビュー待ち。Windows 方式への統一を採用するかどうかも検討事項として挙がっている。

---

### ![PR](icons/git-pull-request.svg) [Add a diagnostic for `uv add` with standard library modules](https://github.com/astral-sh/uv/pull/19572) `#19572`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `uv add pickle` のように標準ライブラリモジュールを追加しようとした際に出る不親切なエラーを改善するためのドラフト PR。既知の標準ライブラリ一覧を生成するスクリプトを追加し、resolver 失敗時に専用の hint を表示する。#18464 を closes する内容。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-static/src/known_stdlib.rs` 新規 (+433)、`scripts/generate-known-stdlib.py` 新規、`crates/uv/src/commands/project/add.rs` および診断系の修正 (計 6 ファイル, +576/-16)

**次のアクション:** Draft からの昇格と、生成スクリプトのレビュー。

---

### ![Issue](icons/issue-opened.svg) [Improve error message when trying to add a builtin module](https://github.com/astral-sh/uv/issues/18464) `#18464`
**ステータス:** Open | **ラベル:** enhancement, error messages

**概要:** Linux 上で `uv add pickle` を実行すると、解決失敗エラーに win32 マーカーへの言及など混乱を招く内容が含まれる。標準ライブラリモジュールであることを示す hint を追加してほしいという改善要望。

**直近のアクティビティ:** qaijuang が再現確認の上、「`pickle` は標準ライブラリであり、現在のインタプリタに含まれているため依存として追加すべきでない」旨の hint を提案。これを受けた PR #19572 が作成されている。

**次のアクション:** PR #19572 のレビュー進行を見守る。

---

### ![PR](icons/git-pull-request.svg) [Avoid preserving Windows virtualenv launcher timestamps](https://github.com/astral-sh/uv/pull/19570) `#19570`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Windows でランチャーをコピーする際に mtime が保持されてしまい、uv のインタプリタキャッシュでハッシュ衝突が発生する問題への対処。`std::fs::copy` ではなくバイト列を読み書きする手動コピーに切り替える。

**直近のアクティビティ:** 著者 zanieb 自身が自己レビューコメントを残しているのみ。

**変更ファイル:** `crates/uv-python/src/interpreter.rs`、`crates/uv/src/commands/project/mod.rs`、テスト (3 ファイル, +58/-1)

**次のアクション:** CI デバッグ後にレビュー依頼へ。

---

### ![PR](icons/git-pull-request.svg) [Add a full "lockfile" to tool receipts](https://github.com/astral-sh/uv/pull/18937) `#18937`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv tool` のレシートに `uv.lock` 形式の完全な解決結果をインライン化する大規模な機能追加。既存のレシートフィールドと一部重複するが、後の整理を見越して許容している。

**直近のアクティビティ:** woodruffw が新たにレビューコメントを追加 (詳細インライン)。著者 charliermarsh も以前にコメントを返している。

**変更ファイル:** ツールインストール系を中心に 15 ファイル変更 (+4013/-301)。`tool_install.rs` のテストが大半を占める。

**次のアクション:** 大規模差分のレビューと、レシートとロックファイルの重複整理方針の検討。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate が自動生成・更新する依存関係ダッシュボード。放置されたままの依存や検出済み依存をリスト化する内部用 issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし (Bot による定期更新のみ)。

**次のアクション:** Renovate の自動更新により最新状態が維持されるため、人的対応は不要。

---

### ![PR](icons/git-pull-request.svg) [Run CI for removed workspace code](https://github.com/astral-sh/uv/pull/19565) `#19565`
**ステータス:** Open (Draft) | **ラベル:** build:release, build:docker

**概要:** 不要に `pub` 指定されていたために Cargo の dead code 解析をすり抜けていたコードを大量に整理する PR。`pub` 修飾を絞り、未使用 API を削除する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** workspace 全体のクレートに対する dead code 整理 (132 ファイル, +583/-1265) — `uv-cli`、`uv-distribution-types`、`uv-python` などほぼ全クレートに渡る縮小。

**次のアクション:** CI で確実に死蔵コードが落ちることを確認後、Draft 昇格。

---

### ![PR](icons/git-pull-request.svg) [Allow future `exclude-newer` values during `uv lock --check`](https://github.com/astral-sh/uv/pull/19571) `#19571`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `uv lock --check` 実行時に、ロックファイルに記録された `exclude-newer` の値が現在の指定より新しい場合でも許容するようにする変更。`--refresh` や `--upgrade` を併用した場合は従来通り更新される。

**直近のアクティビティ:** astral-sh-bot による test inventory 自動コメント (新規テスト 2 件追加) のみ。

**変更ファイル:** `crates/uv-resolver/src/exclude_newer.rs`、`crates/uv/tests/it/lock.rs` (2 ファイル, +156/-1)

**次のアクション:** Draft からの昇格とレビュー依頼。

---

### ![Issue](icons/issue-opened.svg) [`uv` tried to use wheels which don't exist](https://github.com/astral-sh/uv/issues/16841) `#16841`
**ステータス:** Open | **ラベル:** needs-mre

**概要:** `uv tool install` 等の最中に電源断/クラッシュが発生すると、`archive-v0/` キャッシュ内のファイルが 0 バイトのまま残り、再実行時に「Metadata field Name not found」という不明瞭なエラーで失敗する問題。

**直近のアクティビティ:** lmello が powerloss による再現と詳細な根本原因分析を投稿。`Cache::persist()` は `rename` で原子性を確保しているがファイル本体や親ディレクトリへの `fsync` を行っておらず、ext4 の遅延割り当てによりリネーム後もデータがディスクに到達せずに 0 バイトファイルが残ること (Ted Ts'o の "zero-length file problem") を OSDI '14 や fsync(2) man page を引用して説明。 konstin から AI ポリシー遵守の依頼が出ている。

**次のアクション:** fsync 戦略の見直し、もしくはキャッシュ整合性チェック追加の方針決定。

---

### ![PR](icons/git-pull-request.svg) [Follow-up from #16542](https://github.com/astral-sh/uv/pull/16749) `#16749`
**ステータス:** Open | **ラベル:** なし

**概要:** `--python-downloads-json-url` のテストを `network.rs` に追加し、`CachedClient` を利用するよう変更する #16542 のフォローアップ。

**直近のアクティビティ:** konstin が新規レビューコメントを追加 (インライン)。

**変更ファイル:** `uv-python` 配下と `uv` のサブコマンド (`find`/`install`/`list`/`pin`)、ネットワークテスト (8 ファイル, +228/-33)

**次のアクション:** レビューコメントへの対応とマージ。

---

### ![PR](icons/git-pull-request.svg) [Readjust the trampoline CI check](https://github.com/astral-sh/uv/pull/19225) `#19225`
**ステータス:** Merged | **ラベル:** internal, build:windows-trampoline

**概要:** Windows トランポリンの再現ビルドを Docker 経由で行う仕組みに切り替え、`build:windows-trampoline` ラベルや trampoline ソース変更時のみコミット済みバイナリの更新を必須とすることで、バージョンアップごとの再ビルド負担を軽減する。

**直近のアクティビティ:** konstin が「copy-and-edit ソリューションを実装した」とコメント、zanieb が APPROVED を出してマージ。

**変更ファイル:** CI ワークフロー、トランポリンバイナリ群、ビルドスクリプト (10 ファイル, +70/-33)

**次のアクション:** マージ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [UV_TORCH_BACKEND and index interaction](https://github.com/astral-sh/uv/issues/19569) `#19569`
**ステータス:** Open | **ラベル:** enhancement

**概要:** ローカルの JFrog ミラー利用時に `--index` を併用すれば torchvision がインストールできるが、環境ごとに切り替えたいため `UV_TORCH_BACKEND=cu128` を使いたい。しかし `UV_TORCH_BACKEND` を設定するとミラー設定を無視して `download.pytorch.org` を直接取りに行くため、インターネット接続のないクラスタで失敗する。他のインデックス指定と協調動作してほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし (起票直後)。

**次のアクション:** メンテナによる仕様判断待ち。

---

### ![Issue](icons/issue-opened.svg) [Selecting Python version that is Universal2 on macOS](https://github.com/astral-sh/uv/issues/19568) `#19568`
**ステータス:** Open | **ラベル:** enhancement

**概要:** macOS で Universal2 ビルドのみを `--python` で選択したいが、`uv build --python=cpython-3.14-universal2` のような指定では単一アーキ版が選ばれる。絶対パス指定でしか Universal2 を選択できない。

**直近のアクティビティ:** 直近 24 時間のコメントなし (起票直後)。

**次のアクション:** Python 選択 UI の方針決定。

---

### ![Issue](icons/issue-opened.svg) [`uv tool upgrade` Not Functioning as Expected](https://github.com/astral-sh/uv/issues/8276) `#8276`
**ステータス:** Open | **ラベル:** question, uv tool

**概要:** バージョン指定でインストールしたツールに対し `uv tool upgrade` を実行しても "Nothing to upgrade" と表示されるだけで意図通りに更新されない、という古い issue。

**直近のアクティビティ:** 起票者が「現在は `<package>@latest` 構文で運用している」とコメントし、`upgrade` 実行時に `[required: ==0.6.6]` のようにピンされていることを警告表示してほしいと提案。

**次のアクション:** 警告メッセージ追加の要否を検討。

---

### ![PR](icons/git-pull-request.svg) [Vendor packse scenarios and serve them from an in-memory index](https://github.com/astral-sh/uv/pull/18084) `#18084`
**ステータス:** Open | **ラベル:** testing, build:skip-release, build:skip-docker

**概要:** resolver のシナリオパッケージ生成を Python から Rust 実装に書き直し、wiremock ベースのインメモリインデックスから配信するように再構築する大規模リファクタ。packse および packse インデックスへの依存を完全に排除することを目的とする。

**直近のアクティビティ:** astral-sh-bot が test inventory 更新 (新規テスト 3 件) を自動投稿。著者 zanieb による多数のインライン自己レビュー (16 件) が継続中。

**変更ファイル:** `crates/uv-dev/src/generate_scenarios.rs` 新規、`crates/uv-test/src/packse/` 配下に scenario/server/wheel を新規実装、`scripts/scenarios/` を削除し `test/scenarios/` に大量の TOML を新規配置 (177 ファイル, +7850/-3456)

**次のアクション:** 自己レビュー消化後にメンテナレビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [Add module owners to workspace metadata](https://github.com/astral-sh/uv/pull/19122) `#19122`
**ステータス:** Open | **ラベル:** なし

**概要:** 依存リンティング用途のため、`uv workspace metadata` に `--sync` オプションを追加し、Python のインポート可能モジュール名から解決パッケージノードへの `module_owners` マッピングを出力する機能を追加する。`ModuleName` 型と `InstalledDist::read_modules` を導入し、RECORD エントリからモジュール名を導出する。`.pth` のみの editable インストールは現状未対応。

**直近のアクティビティ:** astral-sh-bot による test inventory 自動コメント (新規 13 件)、CodSpeed による性能無変化レポート。インラインレビューは zsol/zanieb/Gankra/konstin の間で計 11 件継続中。

**変更ファイル:** `crates/uv-distribution-types/src/installed_modules.rs` 新規、`crates/uv-pypi-types/src/module_name.rs` 新規、workspace metadata と module_owners コマンドの実装 (15 ファイル, +1077/-23)

**次のアクション:** Editable インストール対応など指摘事項の解消とレビュー継続。

---

### ![Issue](icons/issue-opened.svg) [Documentation feedback](https://github.com/astral-sh/uv/issues/5605) `#5605`
**ステータス:** Open | **ラベル:** documentation, tracking

**概要:** `docs.astral.sh/uv/` 新ドキュメントへのフィードバックを集約するためのトラッキング issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 各種ドキュメントフィードバック PR/Issue の参照ハブとして継続利用。

---

### ![Issue](icons/issue-opened.svg) [`uv run` ignores active venv and `.python-version`, recreates venv with system Python](https://github.com/astral-sh/uv/issues/19563) `#19563`
**ステータス:** Open | **ラベル:** question

**概要:** WSL2 Ubuntu 上で `.python-version` を 3.12 にピン留めし `.venv` を 3.12 で作成・activate しているにもかかわらず、`uv run` がアクティブ venv を無視してシステム Python 3.13 で venv を再作成してしまうという報告。

**直近のアクティビティ:** zanieb が verbose ログを要求 → 起票者がログ投稿 → zanieb が `UV_PYTHON` 設定の可能性を指摘 → 起票者が確かに `UV_PYTHON=/usr/bin/python3` が親プロセスから継承されていたことを確認。設定元は引き続き調査中。

**次のアクション:** 起票者側で `UV_PYTHON` の設定元特定が進行中。仕様としてはおおむね解決の方向。

---

### ![PR](icons/git-pull-request.svg) [Retry failed partial downloads with HTTP Range header when supported](https://github.com/astral-sh/uv/pull/19075) `#19075`
**ステータス:** Open | **ラベル:** なし

**概要:** PyPI からのダウンロード中に接続リセットが多発して uv のリトライ回数を使い切ってしまう問題を解決するため、`pip` 同様に HTTP Range ヘッダを使った部分再開ダウンロードを実装する。#16934、#13359、#18642、#18538 を fix する。

**直近のアクティビティ:** woodruffw がレビューを引き継ぎ、状態遷移マシンの整理を含む詳細レビューを 5 件投稿。リトライスタックと range ループの分岐ロジックを図式化して説明している。

**変更ファイル:** `crates/uv-distribution/src/distribution_database.rs` (+127/-21)、`crates/uv/tests/it/network.rs` (+242)、ほか依存追加 (5 ファイル, +372/-21)

**次のアクション:** woodruffw のレビューを反映してマージへ。

---

### ![PR](icons/git-pull-request.svg) [Improve the 403 forbidden hint to suggest `ignore-error-codes` when applicable](https://github.com/astral-sh/uv/pull/19521) `#19521`
**ステータス:** Merged | **ラベル:** error messages

**概要:** インデックスで 403 Forbidden が返ったが他のインデックスにそのパッケージが存在する場合に、`ignore-error-codes` の使用を勧める hint を出すよう pubgrub レポートを改善した。

**直近のアクティビティ:** Gankra が事前に APPROVED 済み、charliermarsh と zanieb の間で当日インラインレビューが進みマージ。

**変更ファイル:** `crates/uv-resolver/src/pubgrub/report.rs`、`crates/uv/tests/it/edit.rs` (2 ファイル, +107/-10)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Avoid modifying process with `--env-file` in `uv run`](https://github.com/astral-sh/uv/pull/19567) `#19567`
**ステータス:** Merged | **ラベル:** bug

**概要:** `uv tool run` では #19343 で修正済みだった「`--env-file` がプロセス自身の環境変数を書き換える」問題を、`uv run` 側にも適用するバグ修正。子プロセス専用に環境を渡すよう変更。

**直近のアクティビティ:** woodruffw が「`std::env::set_var` 等を clippy.toml で恒久禁止しては」という提案付きで APPROVED、zanieb も APPROVED してマージ。

**変更ファイル:** `clippy.toml`、`crates/uv/src/commands/mod.rs`、`project/run.rs`、`tool/run.rs`、テスト (5 ファイル, +140/-134)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update EmbarkStudios/cargo-deny-action action to v2.0.18](https://github.com/astral-sh/uv/pull/19548) `#19548`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`cargo-deny-action` の v2.0.17 → v2.0.18 パッチ更新で、segfault 修正を取り込む。

**直近のアクティビティ:** 直近 24 時間のコメントなし (マージ済み)。

**変更ファイル:** `.github/workflows/check-lint.yml` (1 ファイル, +1/-1)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update crate-ci/typos action to v1.46.2](https://github.com/astral-sh/uv/pull/19546) `#19546`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`typos` action の v1.46.1 → v1.46.2 パッチ更新で、`criterias` および `replaceables` を誤検出しない修正を取り込む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/check-lint.yml` (1 ファイル, +1/-1)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update taiki-e/install-action action to v2.79.2](https://github.com/astral-sh/uv/pull/19557) `#19557`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`taiki-e/install-action` を v2.77.5 → v2.79.2 へマイナー更新。`cargo-mutants` 等のサポート追加や複数ツールバージョン更新を含む。

**直近のアクティビティ:** CodSpeed による性能無変化レポートのみ。

**変更ファイル:** `bench.yml`、`check-lint.yml`、`test-windows-trampolines.yml`、`test.yml` (4 ファイル, +8/-8)

**次のアクション:** マージ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [exclude-newer should have an extra flag to allow it to skip over packages that don't have publish date information.](https://github.com/astral-sh/uv/issues/12449) `#12449`
**ステータス:** Open | **ラベル:** enhancement, configuration

**概要:** リリース日時メタデータを持たないパッケージがあると `exclude-newer` がリゾルブを失敗させてしまうため、そうしたパッケージをスキップするフラグが欲しいという要望。

**直近のアクティビティ:** jankatins が同様の困りごとを補足。社内で Azure Artifacts を使っており release timestamp が提供されないため、`exclude-newer = false` を全レジストリに書く必要があり、ロックファイルにデフォルトの timestamp が書き込まれて diff の往復が起こるとコメント。`UV_EXCLUDE_NEWER=false` のような env で無効化できると嬉しいと提案。

**次のアクション:** 設定 UX の方針決定。

---

### ![PR](icons/git-pull-request.svg) [Reject duplicate PEP 723 script blocks](https://github.com/astral-sh/uv/pull/19544) `#19544`
**ステータス:** Merged | **ラベル:** なし

**概要:** PEP 723 のスクリプトメタデータブロックは 1 つだけと定められているが、これまで uv は 2 つ目の完全な `# /// script` ブロックを postlude として黙って受理していた。本 PR で複数のブロックを持つスクリプトを拒否する。#18617 を closes する。

**直近のアクティビティ:** qaijuang が「Windows の CI 失敗理由が分からない」と相談、konstin がインラインレビューと「Thanks!」付きの APPROVED でマージ。

**変更ファイル:** `crates/uv-scripts/src/lib.rs` (+91/-6)、`crates/uv/tests/it/run.rs` (+24) (2 ファイル, +115/-6)

**次のアクション:** マージ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [PEP 600: uv lock fails to resolve correct manylinux tag](https://github.com/astral-sh/uv/issues/19566) `#19566`
**ステータス:** Open | **ラベル:** bug

**概要:** Debian 11 (glibc 2.31, manylinux_2_31) 環境で、0.3.1 が manylinux_2_31_x86_64、0.4.0 が manylinux_2_34_x86_64 でビルドされている `sxgeo` パッケージに対し、`uv pip install` や `pip lock` は正しく 0.3.1 を選ぶのに対し `uv lock` は 0.4.0 を選んでしまい `uv sync` で失敗する、という報告。

**直近のアクティビティ:** charliermarsh が「`uv lock` はグローバルロックを行い意図的に現在のマシン固有ではないため、これは正しい挙動。`required-environments` のドキュメントを参照してほしい」と返答。

**次のアクション:** ドキュメント案内で解決の方向。必要なら required-environments の明示。

---

### ![PR](icons/git-pull-request.svg) [Ban graalpy and pypy as script/gui entry point names](https://github.com/astral-sh/uv/pull/19536) `#19536`
**ステータス:** Merged | **ラベル:** なし

**概要:** #19535 のフォローアップ。スクリプト/GUI エントリポイント名として予約する範囲を拡張し、`graalpy` および `uv venv` がサポートする `pypy` 系の名前 (ランチャー衝突防止) を含める。

**直近のアクティビティ:** EliteTK が「テストが多すぎる気がする (unit テスト + integration 1 件くらいで十分では) が、本 PR ではこのままで良い」とコメントしつつ APPROVED。

**変更ファイル:** `crates/uv-install-wheel/src/wheel.rs`、`crates/uv/tests/it/pip_install.rs` (2 ファイル, +46/-2)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [fix(distribution): validate cached wheel archives against corruption](https://github.com/astral-sh/uv/pull/19562) `#19562`
**ステータス:** Closed | **ラベル:** なし

**概要:** 電源断などで `archive-v0/` キャッシュへの wheel 展開が中断された場合に、0 バイトの METADATA ファイルが残り「Metadata field Name not found」エラーが出る #16841 を修正する提案。`Archive::exists()` で `.dist-info/METADATA` が非空であることを軽量チェックし、破損していれば false を返して再ダウンロードに落とす方式。

**直近のアクティビティ:** konstin が「METADATA の空チェックはキャッシュ無効化問題の解決にはならず、uv は atomic write を使っているのでこれが起きる理由を先に説明する必要がある。さらに記述が LLM 製であり AI ポリシー違反」とコメントしクローズ。著者は再オープン可否を尋ねている。

**変更ファイル:** `crates/uv-distribution/src/archive.rs` (1 ファイル, +93/-2)

**次のアクション:** AI ポリシーに沿った形での再提出、もしくは根本的な fsync 改修方針に合わせた別アプローチの検討。

---

### ![Issue](icons/issue-opened.svg) [Script metadata parser silently accepts duplicate script blocks](https://github.com/astral-sh/uv/issues/18617) `#18617`
**ステータス:** Closed | **ラベル:** error messages, compatibility

**概要:** `ScriptTag::parse` が最初の `# /// script` ブロックのみを返し、2 つ目以降を黙って無視していた問題。PEP 723 では複数ブロックがあった場合にツールはエラーを出すべきと定められている。

**直近のアクティビティ:** konstin がエラー文言として「The script contains multiple PEP 723 metadata blocks」を提案、qaijuang が反映完了と返答し、対応 PR #19544 のマージとともに本 issue もクローズ。

**次のアクション:** クローズ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Fix schemars propagation for `uv-distribution-types`](https://github.com/astral-sh/uv/pull/19564) `#19564`
**ステータス:** Merged | **ラベル:** internal

**概要:** `uv-distribution-types` クレートに対して `schemars` フィーチャーが正しく伝搬していなかった問題の修正。`uv-workspace` 側からも明示的に依存を追加する。

**直近のアクティビティ:** Gankra が「出力に変化はあるべきか?新規テストは必要か?」と尋ねつつ APPROVED、charliermarsh が「ほぼ単独で実行されることはない」と回答してマージ。

**変更ファイル:** `crates/uv-distribution-types/Cargo.toml` (+8/-1)、`crates/uv-workspace/Cargo.toml` (+1) (2 ファイル, +9/-1)

**次のアクション:** マージ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [`XDG_BIN_HOME` common default not used](https://github.com/astral-sh/uv/issues/6937) `#6937`
**ステータス:** Closed | **ラベル:** configuration, releases

**概要:** Linux で `XDG_BIN_HOME` が未設定の場合に `~/.cargo/bin/` にインストールされるが、`$HOME/.local/bin` の慣習に合わせてほしいという要望。

**直近のアクティビティ:** HaleTom が「`$XDG_BIN_HOME` は freedesktop 仕様には存在しないが、仕様自体は `$HOME/.local/bin` を「ユーザー固有実行ファイルの場所」として推奨している」と引用付きで補足。起票者の thernstig が「正式仕様外なのは事実だが `~/.local/bin` は事実上のデファクトとして広く使われている」と返答。

**次のアクション:** クローズ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [Suggestion: `uv bundle`, `uv build --release` or similar to create a contained executable a la pyinstaller, py2exe](https://github.com/astral-sh/uv/issues/5802) `#5802`
**ステータス:** Open | **ラベル:** wish

**概要:** pyinstaller / py2exe のように依存とインタプリタを同梱した実行可能ファイルを作る `uv bundle` 相当のコマンドが欲しい、という古くからの要望。

**直近のアクティビティ:** charliermarsh が、別途運用している実験的な Cloudflare Worker (uv 経由でパッケージをインストールするシェルスクリプトを返す、Mistral の vibe 同様) について「自分が実験として作って Astral がドメインを所有しているが、宣伝も保証もしていない」と補足。

**次のアクション:** 直接の対応方針は未定。

---

### ![PR](icons/git-pull-request.svg) [Infer the version on release workflow instead of requiring user input](https://github.com/astral-sh/uv/pull/19477) `#19477`
**ステータス:** Open | **ラベル:** internal

**概要:** リリースワークフローで手動入力していたバージョンを自動推論するように変更し、入力ミスを防ぐ。既に既定の dist ワークフローから乖離している点を踏まえての判断。

**直近のアクティビティ:** zsol が「タグ名手入力には『リリース内容との同期確認』という意図もあった気がするが、バージョン番号入力は良い実装ではなかった。ワークフロー承認フローで担保するのが妥当か」とコメントし APPROVED。

**変更ファイル:** `.github/workflows/release.yml` (1 ファイル, +31/-33)

**次のアクション:** マージ可否の最終判断。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate assert_cmd to v2.2.2](https://github.com/astral-sh/uv/pull/19550) `#19550`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`assert_cmd` を 2.2.1 → 2.2.2 へパッチ更新 (`#[track_caller]` 改善)。

**直近のアクティビティ:** EliteTK が APPROVED してマージ。

**変更ファイル:** `Cargo.lock` (1 ファイル, +2/-2)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate filetime to v0.2.29](https://github.com/astral-sh/uv/pull/19551) `#19551`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`filetime` を 0.2.28 → 0.2.29 へパッチ更新。

**直近のアクティビティ:** EliteTK が APPROVED してマージ。

**変更ファイル:** `Cargo.lock` (1 ファイル, +2/-2)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [docs: fix layout reflow from benchmark images on homepage](https://github.com/astral-sh/uv/pull/19545) `#19545`
**ステータス:** Closed | **ラベル:** なし

**概要:** ドキュメントトップのベンチマーク画像が `width`/`height` 指定を持たないため、読み込み完了時にレイアウトシフト (CLS) を引き起こす #6264 への対処として、`max-width: 720px` と `aspect-ratio: 16/9`、`object-fit: contain` を指定する CSS を追加する提案。

**直近のアクティビティ:** konstin が AI ポリシー違反 (自動生成 PR) を指摘してクローズ。

**変更ファイル:** `docs/stylesheets/extra.css` (1 ファイル, +10)

**次のアクション:** クローズ済み。再提出する場合は AI ポリシー遵守が前提。

---

### ![PR](icons/git-pull-request.svg) [Update dependency astral-sh/uv to v0.11.16](https://github.com/astral-sh/uv/pull/19547) `#19547`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。CI 内で利用している `astral-sh/uv` action を 0.11.14 → 0.11.16 へパッチ更新。0.11.15/0.11.16 のセキュリティ修正 (TAR 部分差分、エントリポイントエスケープ防止) や Azure 署名対応などを取り込む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/check-docs.yml`、`check-fmt.yml`、`check-lint.yml`、`check-lock.yml`、`publish-docs.yml`、`test.yml` (6 ファイル, +10/-10)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate open to v5.3.5](https://github.com/astral-sh/uv/pull/19553) `#19553`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`open` クレートを 5.3.4 → 5.3.5 へパッチ更新 (Wine 環境下で `winebrowser` に委譲する修正を含む)。

**直近のアクティビティ:** EliteTK が APPROVED してマージ。

**変更ファイル:** `Cargo.lock` (1 ファイル, +2/-2)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate rcgen to v0.14.8](https://github.com/astral-sh/uv/pull/19554) `#19554`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`rcgen` を 0.14.7 → 0.14.8 へパッチ更新 (ML-DSA 識別子修正、yasna 0.6 への更新、CSR 関連改善ほか)。

**直近のアクティビティ:** EliteTK が APPROVED してマージ。

**変更ファイル:** `Cargo.lock` (1 ファイル, +14/-4)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate dashmap to v6.2.1](https://github.com/astral-sh/uv/pull/19556) `#19556`
**ステータス:** Merged | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** Renovate による自動 PR。`dashmap` を 6.1.0 → 6.2.1 へマイナー更新 (v7 リリース前の中間メンテリリース、MSRV 1.85 へ)。

**直近のアクティビティ:** EliteTK が APPROVED してマージ。

**変更ファイル:** `Cargo.lock` (1 ファイル, +2/-2)

**次のアクション:** マージ済み。完了。

---

### ![PR](icons/git-pull-request.svg) [Centralised environment storage](https://github.com/astral-sh/uv/pull/18214) `#18214`
**ステータス:** Open (Draft) | **ラベル:** enhancement, preview

**概要:** `centralized-envs` プレビュー機能として、プロジェクトの venv をローカル `.venv` ではなく uv キャッシュ (`~/.cache/uv/environments-v2/`) に集中保管する大型機能。`.venv` には IDE/ツール検出用の symlink/junction/ファイルを残し、プロジェクトパス + Python メタデータから一意なキーを導出して保存場所を決める。Linux/Windows の両方で mypy/pyright/ty/VSCode の挙動を網羅的に検証済み。#1495 と #7642 を closes する。

**直近のアクティビティ:** MayeulC が長期の議論スレッドに新たなコメントを追加。EliteTK と zanieb 間で 3 月以降 72 件の詳細なインラインレビューが継続中。

**変更ファイル:** `uv-workspace`、`uv-python`、`uv` の project コマンド群に加え 1020 行の新規テストファイル `centralized_envs.rs` を追加 (19 ファイル, +1733/-180)

**次のアクション:** プレビュー機能名の確定と、`uv sync --package` 時のキー設計などの最終調整。

---

### ![Issue](icons/issue-opened.svg) [Using uv on HPC Clusters](https://github.com/astral-sh/uv/issues/7642) `#7642`
**ステータス:** Open | **ラベル:** なし

**概要:** HPC クラスタにおける uv 利用上の課題。`$WORK` の inode クォータを `.venv` が圧迫すること、`XDG_CACHE_HOME` をファイルシステム横断で設定したい場合の symlink 制約、複数 MPI アーキ間で wheel キャッシュが共有されてしまう点などを共有し、`UV_USE_VENV_DEPOT` のような環境変数による自動振り分けを要望。

**直近のアクティビティ:** MayeulC が、共有 HPC キャッシュで `g+s` パーミッションと `umask 002` を使っている運用知見を共有。git の `--shared=group` のような明示設定を期待しつつ、`UV_PYTHON_SEARCH_PATH` がクラスタ上で非常に有用とのコメントも。

**次のアクション:** #18214 (centralized envs) との関連で扱われる見込み。

---

### ![Issue](icons/issue-opened.svg) [When creating a venv pay attention to preferred Python version set via PY_PYTHON](https://github.com/astral-sh/uv/issues/6175) `#6175`
**ステータス:** Closed | **ラベル:** compatibility

**概要:** Windows で `PY_PYTHON=3.12` を設定しているのに `uv venv` が 3.13rc1 を選んでしまうため、`PY_PYTHON` を尊重してほしいという要望。

**直近のアクティビティ:** sharifhsn が「#12115 (=`uv python pin --global` 対応) によって "デフォルト Python バージョンをグローバルに指定したい" という要望は満たされた」として、uv 0.6.6 と 0.6.7 のコマンド比較を添えてクローズを記録。`PY_PYTHON` 自体は依然未対応。

**次のアクション:** クローズ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [Document generate-shell-completion subcommand](https://github.com/astral-sh/uv/issues/2191) `#2191`
**ステータス:** Closed | **ラベル:** documentation

**概要:** `generate-shell-completion` サブコマンドが `uv -h` にも表示されずドキュメントもないという指摘。

**直近のアクティビティ:** zanieb が「help の折り返しを増やしてしまうため意図的にヘルプ表示から省いている」とコメントしてクローズ。

**次のアクション:** クローズ済み。完了。

---

### ![Issue](icons/issue-opened.svg) [uv python list doesn't show all possible combinations of python installation](https://github.com/astral-sh/uv/issues/4988) `#4988`
**ステータス:** Open | **ラベル:** help wanted, cli

**概要:** `uv python list` が、システム上で実際に install 可能な (例えば glibc 版の) 組み合わせをすべて表示しないという報告。

**直近のアクティビティ:** sharifhsn が「#5036 (glibc 関係) で大筋クローズ、musl 側は #12121、`--all-arches` は #9782 で追加されている」と、各 uv バージョンでの `uv python list` 出力比較を添えて整理。

**次のアクション:** issue としてはオープンのままだが、関連 PR で大部分は解消済み。クローズ判断を待つ状態。

---

### ![Issue](icons/issue-opened.svg) [Even with `uv sync --no-dev`, development dependencies are still installed when workflow executes.](https://github.com/astral-sh/uv/issues/12558) `#12558`
**ステータス:** Closed | **ラベル:** question

**概要:** GitHub Actions 上で `uv sync --no-dev` を実行しても、後続の `uv run` 時に dev 依存 (ruff) が結局インストールされてしまうという報告。

**直近のアクティビティ:** oPromessa が「`dependency-groups.dev` に `osxphotos` も追加することで解決した」と感謝コメント。`uv run` がデフォルトで dev グループを同期する挙動に起因するもので、既に解説済み。

**次のアクション:** クローズ済み。完了。

---
## microsoft/vscode

> Agent Host / Cloud Sessions / BYOK / UBB（使用量課金）周辺の作業が集中しており、リリースブランチ `release/1.122` への cherry-pick とテストプラン項目、Dependabot 依存更新が並行して進行している。

### ![PR](icons/git-pull-request.svg) [Agents web: surface offline tunnels in host picker + add discovery telemetry](https://github.com/microsoft/vscode/pull/318429) `#318429`
**ステータス:** Merged | **ラベル:** なし

**概要:** vscode.dev/agents のホストピッカーで、過去に作成済みだが現在ホストが接続されていないトンネルが初回ディスカバリ時にサイレントに捨てられていた問題を修正する。`hostConnectionCount > 0` のキャッシュゲートを撤廃し、オフライントンネルをグレーアウト表示するとともに、新規テレメトリ `vscodeAgents.tunnelDiscovery/result` と詳細ログを追加した。

**直近のアクティビティ:** Copilot レビューボットがロギングや変数名（`onlineTunnels` が誤解を招く）に関する低信頼度コメントを残し、osortega が応答、dmitrivMS が承認しマージされた。

**変更ファイル:** `src/vs/sessions/common/sessionsTelemetry.ts`、`tunnelAgentHost.contribution.ts`、`webTunnelAgentHostService.ts`、`tunnelAgentHostServiceImpl.ts`

**次のアクション:** マージ済み。Copilot レビューで挙がった命名改善 (`onlineTunnels` → `discoveredTunnels`) を別 PR で取り込むか判断する。

---

### ![PR](icons/git-pull-request.svg) [Claude agent — Phase 11: customizations / plugins](https://github.com/microsoft/vscode/pull/318113) `#318113`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agent Host の Claude agent provider の Phase 11 として、ワークベンチがプッシュするカスタマイズと SDK が発見するカスタマイズ（コマンド/エージェント/MCP サーバ）を `IAgentPluginManager` 経由で SDK の `Options.plugins` に流し込む。`Query.reloadPlugins()` がパラメータレスのため、変更検出時はリマテリアライザ経路で再構築する設計。

**直近のアクティビティ:** Copilot レビューボットが Phase 11 の実装内容（クライアントプッシュ + SDK 発見の双方向統合）について overview を生成した。Draft 状態で議論継続中。

**変更ファイル:** `claudeAgent.ts`、`claudeAgentSession.ts`、`claudeSdkOptions.ts`、`claudeSdkPipeline.ts`、`claudeSessionMetadataStore.ts` ほか計 17 ファイル

**次のアクション:** Draft 解除と人間レビュアーによるレビュー依頼。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: Better experience for out of date agent hosts](https://github.com/microsoft/vscode/issues/316554) `#316554`
**ステータス:** Closed | **ラベル:** feature-request, unreleased, agent-host

**概要:** 古いバージョンの Agent Host に接続しようとした際の通知が不明瞭で、ユーザーが接続をブロックされてしまう問題。メッセージ改善と「強制終了して更新」ボタンの追加が求められていた。

**直近のアクティビティ:** connor4312 が `protocolServerHandler.ts` の修正方針コードを提示し、vs-code-engineering ボットが検証手順を要請、osortega が再現手順について議論した。

**次のアクション:** クローズ済み。関連 PR #318428 で incompatible ステータスの sticky 問題が解消された。

---

### ![PR](icons/git-pull-request.svg) [agent host: clear sticky incompatible status after upgrade reconnect](https://github.com/microsoft/vscode/pull/318428) `#318428`
**ステータス:** Merged | **ラベル:** なし

**概要:** プロバイダの `incompatible` 状態が reconcile ループ内で固定化されてしまい、アップグレード後の `connecting`/`connected` への正当な遷移までブロックしていた問題を修正。サービスにエントリが存在しない SSH/tunnel catch パスのみで sticky 動作を保持し、エントリがある場合はサービスの状態を正とする。Fixes #316554。

**直近のアクティビティ:** Copilot レビューが overview コメントを残し、dmitrivMS が承認してマージされた。

**変更ファイル:** `remoteAgentHost.contribution.ts`、`tunnelAgentHost.contribution.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![Issue](icons/issue-opened.svg) [Renaming a session requires reload](https://github.com/microsoft/vscode/issues/318442) `#318442`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code エディタウィンドウ側でチャットセッションをリネームしても新しい名前が反映されず、リロードが必要になるバグ。Agents ウィンドウでは発生しない。

**直近のアクティビティ:** 報告者 bamurtaugh が、リネーム済みセッションを fork すると新しい名前が失われて元の名前に戻るという追加事例を報告。

**次のアクション:** トリアージとセッション管理サービス（`sessionsManagementService.ts`）周辺の調査。

---

### ![Issue](icons/issue-opened.svg) [Some chat sessions are emptied out, but remain in history and in workspaceStorage](https://github.com/microsoft/vscode/issues/313236) `#313236`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットセッションが時折空になりながら履歴と workspaceStorage には残り続ける現象。再現手順不明だが、VS Code アップデート時のチャット内容消失として頻発しており、ユーザー業務に深刻な影響を与えている。

**直近のアクティビティ:** 報告者 gh-andre が「私のチャットの 3/4 が空のまま残っている。誰か対応しているのか」と再度問い合わせ。

**次のアクション:** 開発側のオーナー割当てと、永続化フロー（ChatSessionStore）の調査。

---

### ![PR](icons/git-pull-request.svg) [Enable offline BYOK by default](https://github.com/microsoft/vscode/pull/318440) `#318440`
**ステータス:** Open | **ラベル:** model-byok

**概要:** チャット BYOK の "offline" ゲートを既定で有効化し、ランタイムのゲーティングロジックを簡素化する。`release/1.122` ブランチ向けの変更。

**直近のアクティビティ:** Copilot レビューが overview コメントを生成、dmitrivMS が自身でコメントを追加。

**変更ファイル:** `chat.shared.contribution.ts`、`hasByokModelsContribution.ts`、`hasByokModelsContribution.test.ts`

**次のアクション:** 追加レビューとマージ待ち。

---

### ![Issue](icons/issue-opened.svg) [Show model cost info for Cloud session target](https://github.com/microsoft/vscode/issues/318441) `#318441`
**ステータス:** Open | **ラベル:** feature-request, UBB

**概要:** Cloud セッション用のモデルピッカーにも UBB（使用量課金）のコスト情報を表示すべきという機能要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージと UBB 関連の他課題との優先度すり合わせ。

---

### ![PR](icons/git-pull-request.svg) [Improve session management functionality](https://github.com/microsoft/vscode/pull/318435) `#318435`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents ウィンドウのセッション管理 API を拡張し、`insertAt` にアクティベートのオプションを追加。タブ操作とチャットタイトルのホバー機能を改善する UI 更新を伴う。

**直近のアクティビティ:** CODENOTIFY ボットが @sandy081 ら関係者へ通知、Copilot レビューが overview を生成し、sandy081 が承認してマージ。

**変更ファイル:** `chatCompositeBar.ts`、`chatCompositeBar.css`、`sessionsManagementService.ts`、`sessionsTelemetryReporter.ts`、`visibleSessions.ts` ほか計 8 ファイル

**次のアクション:** マージ済み、対応完了。

---

### ![Issue](icons/issue-opened.svg) [Integrated Browser inside DevContainer should use the host port](https://github.com/microsoft/vscode/issues/311133) `#311133`
**ステータス:** Open | **ラベル:** feature-request, browser-integration

**概要:** DevContainer 利用時、ローカルポートがランダムなホストポートにマッピングされるため、統合ブラウザがコンテナ内のローカルポートを使ってもアクセスできない。DevContainer であることを認識し、マッピングされたホストポートを使うべきという要望。

**直近のアクティビティ:** pl4nty がポートフォワーディング設定について「自分の環境では同一ポートに転送される」と質問。

**次のアクション:** DevContainer のポート転送仕様を確認した上で統合ブラウザ側で対応するか判断。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: most workspace skills are duplicated](https://github.com/microsoft/vscode/issues/318439) `#318439`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host でほとんどのワークスペーススキルが二重に表示されるバグ。スクリーンショットで確認。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** スキル列挙ロジックの重複排除を調査。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: Can't open image file pills](https://github.com/microsoft/vscode/issues/318438) `#318438`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** Agent Host でテキストファイルの pill は開けるが、画像ファイルの pill は開けない不具合。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 画像 pill のハンドラ実装確認。

---

### ![Issue](icons/issue-opened.svg) [Test: Browser Emulation](https://github.com/microsoft/vscode/issues/318138) `#318138`
**ステータス:** Closed | **ラベル:** testplan-item, browser-integration

**概要:** 統合ブラウザの新しいデバイスエミュレーションツールバーに関するテストプラン項目。macOS / Linux / Windows のすべてでテスト実施が予定されていた。

**直近のアクティビティ:** TylerLeonhardt が「とてもよく動く！素晴らしい仕事」と評価しテスト完了をマーク。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Quota Warning/Notification not shown when exceeding 50/75% usage](https://github.com/microsoft/vscode/issues/317161) `#317161`
**ステータス:** Open | **ラベル:** info-needed, UBB

**概要:** クォータ 50%/75% 到達時の警告通知が表示されない。Agents アプリ、vscode chat panel、Copilot ステータスアイコンのいずれでも確認されず、100% 到達時にチャット入力の上部にしか表示されない。

**直近のアクティビティ:** cwebster-99 が Copilot Business アカウントで同様の現象に遭遇したと報告。

**次のアクション:** 通知表示箇所の仕様確認と修正。

---

### ![Issue](icons/issue-opened.svg) [Cloud session model picker shows 0x](https://github.com/microsoft/vscode/issues/317079) `#317079`
**ステータス:** Closed | **ラベル:** bug, UBB

**概要:** UBB ユーザーアカウントで Cloud モデルピッカーが全モデルに対して `0x` 倍率を表示してしまうバグ。

**直近のアクティビティ:** cwebster-99 が PR #318416 で対応済みと報告し、クローズ。

**次のアクション:** 対応済み。

---

### ![Issue](icons/issue-opened.svg) [CB/CE ULB blocked before exhaustion](https://github.com/microsoft/vscode/issues/318360) `#318360`
**ステータス:** Open | **ラベル:** bug, upstream, UBB

**概要:** Copilot Business/Enterprise の ULB ユーザーが 91% 使用時点でクォータ超過のメッセージをチャットレスポンスに受け取る問題。実際には dotcom のダッシュボード上も 91% のままだが、チャット側だけブロックされる。

**直近のアクティビティ:** cwebster-99 が upstream の `github/gitcoin/issues/26888` を関連 issue として報告。

**次のアクション:** GitHub 側の upstream 修正待ち。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Do not show multipliers for cloud model picker for UBB accounts](https://github.com/microsoft/vscode/pull/318427) `#318427`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** UBB アカウント向けに Cloud モデルピッカーの倍率表示を抑制する #318416 を `release/1.122` へ cherry-pick。Fixes #318418。

**直近のアクティビティ:** Copilot レビューはエラーで失敗、rebornix と cwebster-99 が承認しマージ。

**変更ファイル:** `copilotCloudSessionsProvider.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Fix: cross-file inline edit list never disposed (handleListEndOfLifetime not fired)](https://github.com/microsoft/vscode/pull/318434) `#318434`
**ステータス:** Open | **ラベル:** cherry-pick-artifact

**概要:** `InlineCompletionsModel.transplantCompletion` が `addRef()` を呼ぶが対応する `removeRef()` がなく、cross-file インラインエディットで `InlineSuggestionList` が解放されず `handleListEndOfLifetime` が発火しない問題の cherry-pick (#318340)。`release/1.122` 向け。

**直近のアクティビティ:** Copilot レビューはエラー、benibenj と osortega が承認。

**変更ファイル:** `inlineCompletionsModel.ts`、`provideInlineCompletions.ts`

**次のアクション:** マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Skip unchanged mime database updates](https://github.com/microsoft/vscode/pull/318436) `#318436`
**ステータス:** Open | **ラベル:** なし

**概要:** Linux パッケージスクリプトの `update-mime-database` 呼び出しに `-n` フラグを追加し、既に最新の MIME データベースの場合は再構築をスキップする。Debian/RPM の install/update/remove 時の無駄な処理を回避。Fixes #163690。

**直近のアクティビティ:** CODENOTIFY ボットが @rzhao271 へ通知、Copilot レビューが overview を生成。

**変更ファイル:** `resources/linux/debian/postinst.template`、`postrm.template`、`resources/linux/rpm/code.spec.template`

**次のアクション:** レビュー受付とマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Emulation: Context menu should be stateful on whether or not the emulation toolbar is there](https://github.com/microsoft/vscode/issues/318437) `#318437`
**ステータス:** Open | **ラベル:** なし

**概要:** 統合ブラウザのエミュレーションツールバーを表示した状態でコンテキストメニューを開くと、メニュー項目が消えるか「Close Emulation Toolbar」に変わるべきだが、現状はクリックしても何も起きない。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージとコンテキストメニュー実装の修正。

---

### ![Issue](icons/issue-opened.svg) [chat.agent.sandbox.enabled in effect after reload](https://github.com/microsoft/vscode/issues/318430) `#318430`
**ステータス:** Open | **ラベル:** なし

**概要:** `chat.agent.sandbox.enabled` 設定がリロードまたは新規チャットセッション開始まで反映されない挙動を、設定説明に明記してほしいという要望。

**直近のアクティビティ:** dileepyavan が報告者 anthonykim1 に詳細を依頼。

**次のアクション:** 詳細情報の取得と設定スキーマ説明文の更新。

---

### ![PR](icons/git-pull-request.svg) [nes: enable cursor jump by default](https://github.com/microsoft/vscode/pull/318426) `#318426`
**ステータス:** Open | **ラベル:** ~release-cherry-pick

**概要:** NES（Next Edit Suggestions）の「カーソルジャンプ」拡張範囲設定 `github.copilot.nextEditSuggestions.extendedRange` を既定値で有効化する。`release/1.122` へ自動 cherry-pick される予定。

**直近のアクティビティ:** vs-code-engineering ボットが cherry-pick 自動化を通知、Copilot レビューが overview を生成、lramos15 が dismiss、benibenj が承認。

**変更ファイル:** `extensions/copilot/package.json`、`configurationService.ts`、ゴールデンシナリオ JSON、`baseline.json`、シミュレーションキャッシュ

**次のアクション:** マージ後の自動 cherry-pick。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Add xtab275AggressivenessHighLow, which only tags aggressiveness at high/low eagerness levels](https://github.com/microsoft/vscode/pull/318357) `#318357`
**ステータス:** Open | **ラベル:** cherry-pick-artifact

**概要:** xtab プロンプティング戦略で aggressiveness を high/low の eagerness レベルのみでタグ付けする新オプションを追加。共通 postscript のリファクタリングも含む。#315043 の cherry-pick。

**直近のアクティビティ:** Copilot レビューはエラーで失敗、他のレビューは未投稿。

**変更ファイル:** `promptCrafting.ts`、`xtabProvider.ts`、関連テスト、`xtabPromptOptions.ts`

**次のアクション:** 人間レビュアーによる承認待ち。

---

### ![Issue](icons/issue-opened.svg) [Credit limit reached banner shows at 99% usage](https://github.com/microsoft/vscode/issues/318421) `#318421`
**ステータス:** Open | **ラベル:** bug, UBB

**概要:** Copilot EDU アカウントの AI クレジット 199/200 状態（99%）で「上限到達」バナーが表示されるが、実際にはまだ最後の 1 クレジットでメッセージ送信できる。100% でのみバナーを出すべき。

**直近のアクティビティ:** roblourens が再現できなくなったが、以前のログを添付して共有。

**次のアクション:** バナー表示の閾値判定ロジック修正。

---

### ![Issue](icons/issue-opened.svg) [Outline View when Markdown Preview is full screen or focused](https://github.com/microsoft/vscode/issues/316113) `#316113`
**ステータス:** Open | **ラベル:** feature-request, outline, markdown

**概要:** Markdown プレビューがフォーカスまたは全画面表示のとき、アウトラインビューにも内容が反映されてほしいという機能要望。

**直近のアクティビティ:** 報告者 sck-jenzabar が担当アサインを喜び、@mjbvz の Markdown 拡張機能を称賛するコメントを投稿。

**次のアクション:** Markdown プレビューとアウトラインビュー連携の実装検討。

---

### ![PR](icons/git-pull-request.svg) [Fix: cross-file inline edit list never disposed (handleListEndOfLifetime not fired)](https://github.com/microsoft/vscode/pull/318340) `#318340`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** `InlineCompletionsModel.transplantCompletion` で `seedWithCompletion` 側が既に `addRef` を取得しているのに `transplantCompletion` も追加 `addRef` を呼んでいたペアリングミスを修正。refcount が負になった場合に `onUnexpectedError` で可視化する常時インバリアントも追加。Fixes #318341。

**直近のアクティビティ:** hediet が承認、Copilot レビューはエラー、無関係なボットコメント（画像投稿）あり。マージ後 release/1.122 へ自動 cherry-pick。

**変更ファイル:** `inlineCompletionsModel.ts`、`provideInlineCompletions.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![Issue](icons/issue-opened.svg) [Cross-file inline edit list never disposed; handleListEndOfLifetime not fired](https://github.com/microsoft/vscode/issues/318341) `#318341`
**ステータス:** Closed | **ラベル:** bug, unreleased, NES, NES-ux

**概要:** cross-file インラインエディット受諾時に `InlineSuggestionList` が破棄されず、拡張ホスト側の `handleListEndOfLifetime` コールバックが発火しないため、Copilot Chat 拡張の cross-file NES に関するエンドオブライフテレメトリが silently drop されていた。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** PR #318340 で修正されクローズ。

---

### ![PR](icons/git-pull-request.svg) [Change icon for Toggle Developer Tools action](https://github.com/microsoft/vscode/pull/318431) `#318431`
**ステータス:** Merged | **ラベル:** なし

**概要:** Toggle Developer Tools アクションのアイコンを、ターミナルを連想させる現状から、より適切なものに変更。Fixes #317156。

**直近のアクティビティ:** kycutler が承認しマージ。

**変更ファイル:** `browserDevToolsFeature.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![Issue](icons/issue-opened.svg) [We should have a different icon for `toggle developer tools`](https://github.com/microsoft/vscode/issues/317156) `#317156`
**ステータス:** Closed | **ラベル:** ux, polish, verification-needed, unreleased, browser-integration

**概要:** 現在の Developer Tools のアイコンがターミナルを開く動作を連想させて紛らわしいため、別のアイコンに変更してほしいという ux/polish 要望。

**直近のアクティビティ:** vs-code-engineering ボットが PR #318431 のメディアを検証用に共有。

**次のアクション:** 検証担当者の確認のみ。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Only update sandbox config on connect and user settings change](https://github.com/microsoft/vscode/pull/318425) `#318425`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** サンドボックス設定の更新を、Agent Host への接続時とユーザー設定変更時に限定する #318339 の `release/1.122` 向け cherry-pick。

**直近のアクティビティ:** CODENOTIFY ボットが @anthonykim1 へ通知、Copilot レビューはエラー、chrmarti と dmitrivMS が承認しマージ。

**変更ファイル:** `agentHostSandboxForwarder.ts`、`agentHostSandboxForwarder.test.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Update xterm.js to 6.1.0-beta.220](https://github.com/microsoft/vscode/pull/317936) `#317936`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 統合ターミナルが利用する xterm.js を 6.1.0 系の新しいベータ群に更新（関連アドオンも合わせて更新）。Resolves #317740。

**直近のアクティビティ:** Copilot レビューが overview を生成、niclimcy / anthonykim1 がコメント、dbaeumer が dismiss、dmitrivMS が承認。

**変更ファイル:** `package-lock.json`、`package.json`、`remote/package*.json`、`remote/web/package*.json`、`xtermTerminal.ts`

**次のアクション:** 残るレビューの解決とマージ。

---

### ![Issue](icons/issue-opened.svg) [Terminal renders as solid color blocks after switching macOS Spaces; text becomes visible only when selected](https://github.com/microsoft/vscode/issues/317969) `#317969`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** macOS の Spaces を切り替えて戻ると統合ターミナルのビューポートが単色ブロックで埋まり、選択した領域のみ再描画されて文字が見える現象。WebGL レンダラの再描画問題と推測。

**直近のアクティビティ:** gvellut が macOS Sequoia 15.7.5 のスリープ復帰時にも同様の事象が起きると報告。

**次のアクション:** トリアージとレンダラ層の再描画ロジック調査。

---

### ![Issue](icons/issue-opened.svg) [[Error] unhandlederror-Create the logger before logging](https://github.com/microsoft/vscode/issues/318224) `#318224`
**ステータス:** Closed | **ラベル:** bug, verified, error-telemetry, insiders-released, recent-regression, ai-generated, ai-analyzed

**概要:** `logIpc.ts` の `Yh.log` でロガー作成前にログを呼んでしまう unhandled error。AI 分析により最近のリグレッションとして特定された。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders で対応済みクローズ、stable 反映を待つ。

---

### ![Issue](icons/issue-opened.svg) [Bring Custom Endpoint / BYOK Support to Stable VS Code](https://github.com/microsoft/vscode/issues/318165) `#318165`
**ステータス:** Closed | **ラベル:** verified

**概要:** Insiders 限定の Custom Endpoint / BYOK 機能を Stable リリースに早期投入してほしいという要望。OpenAI 互換ゲートウェイ、LiteLLM プロキシ、エンタープライズ AI ルーティング層など実用ユースケースが多い。

**直近のアクティビティ:** vritant24 が「次のリリースで Stable に入る予定」とコメント（#318050）。

**次のアクション:** 次回 Stable リリースで提供予定、クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Snippet variables $LINE_COMMENT, $BLOCK_COMMENT_START, and $BLOCK_COMMENT_END not working in new C# file](https://github.com/microsoft/vscode/issues/236829) `#236829`
**ステータス:** Open | **ラベル:** bug, snippets

**概要:** C# 用のファイルテンプレートスニペットで `$LINE_COMMENT` / `$BLOCK_COMMENT_START` / `$BLOCK_COMMENT_END` が展開されず、生成ファイルにコメント記号が入らないバグ。Linux でも同様。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージ待ち（古いオープン issue）。

---

### ![Issue](icons/issue-opened.svg) [Keep getting "Co-authored-by: Copilot" in commit messages for no reason](https://github.com/microsoft/vscode/issues/313064) `#313064`
**ステータス:** Open | **ラベル:** bug, important, git

**概要:** Copilot を使っていないのにコミットメッセージへ `Co-authored-by: Copilot copilot@github.com` が勝手に挿入される。UI から Copilot を無効化していても発生し、CI 出力で気づくケースあり。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Git extension とコミットメッセージ生成パスの調査。

---

### ![Issue](icons/issue-opened.svg) [The `fetch_webpage` tool becomes unusable when agent sandboxing is enabled.](https://github.com/microsoft/vscode/issues/316257) `#316257`
**ステータス:** Open | **ラベル:** agent-sandbox

**概要:** `chat.agent.sandbox.enabled=on` のとき、ユーザーが明示的に与えた URL でも `chat.agent.allowedNetworkDomains` に含まれなければ fetch がブロックされ、UI のエラー表示も「Invalid URL」と誤解を招くものになる。ユーザー指定 URL は一時的に許可するかプロンプトで確認すべき。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** サンドボックスのネットワークポリシー UX 設計とエラーメッセージ修正。

---

### ![Issue](icons/issue-opened.svg) [BYOK mode's terminal tool call fails when GitHub is not signed in](https://github.com/microsoft/vscode/issues/318373) `#318373`
**ステータス:** Open | **ラベル:** bug, model-byok

**概要:** Anthropic プロバイダ利用時、GitHub Copilot にサインインしていないと `run_in_Terminal` ツール呼び出しが失敗する。サインインすると成功する。トラジェクトリ JSON を添付。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** BYOK 経路のサインイン依存を調査し排除。

---

### ![Issue](icons/issue-opened.svg) [Additional budget strings for CB/CE users](https://github.com/microsoft/vscode/issues/318433) `#318433`
**ステータス:** Open | **ラベル:** polish, UBB

**概要:** Copilot Business（ULB 2000 credits）アカウントのダッシュボードに「additional budget will be used」と出るが、本人が予算を設定/支払っていない Business/Enterprise ユーザーには意味が伝わりにくい。組織が追加利用を許可した旨を伝える文言に変えるべき。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** UBB UI コピーの調整。

---

### ![Issue](icons/issue-opened.svg) ["Open Recent" / "Select to open" (CTRL+R) shows incorrect results - it doesn't consider the file/dir title](https://github.com/microsoft/vscode/issues/318366) `#318366`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Ctrl+R の Open Recent でファイル/ディレクトリ名（basename）ではなくパス全体を優先してマッチングするため、`git` と入力すると `shared-repo` サブディレクトリが先に表示されるなど期待と異なる結果になる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージとマッチング順位の調整。

---

### ![Issue](icons/issue-opened.svg) [Azure BYOK Entra Path Should Mark Responses API Endpoints Correctly](https://github.com/microsoft/vscode/issues/318114) `#318114`
**ステータス:** Open | **ラベル:** model-byok

**概要:** Azure BYOK の API キー認証経路では `/responses` URL を Responses API 対応として正しくマークするが、Entra 認証経路は手動でモデル情報を組み立てるためにこれが反映されない。結果として Entra で認証成功しても Chat Completions 形式の body が送られて Azure 側で `messages` が無効と拒否される。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Azure Entra 経路で `/responses` URL に対し `ModelSupportedEndpoint.Responses` を設定するよう修正。

---

### ![Issue](icons/issue-opened.svg) [Test: Sandboxing should be enabled only when Default Approvals permission is set](https://github.com/microsoft/vscode/issues/318390) `#318390`
**ステータス:** Open | **ラベル:** testplan-item

**概要:** Copilot Chat ウィンドウのパーミッションが `Default Approvals` のときだけサンドボックスが有効になり、`Bypass Approvals` / `Autopilot` 時には無効になることを検証するテストプラン項目。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** macOS 完了済み、Linux テスト残。

---

### ![PR](icons/git-pull-request.svg) [chat: custom trajectory compaction model](https://github.com/microsoft/vscode/pull/316114) `#316114`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** トラジェクトリ（会話履歴）圧縮の呼び出しを専用モデルへルーティングする ExP（実験）設定を追加。初期は Fireworks ホストの `trajectory-compaction-v1` を使用。フォアグラウンドとバックグラウンドの両圧縮経路を共通 helper で endpoint 解決するように統一する。

**直近のアクティビティ:** Copilot レビューが overview を生成（圧縮 endpoint の共有 helper 化を確認）。

**変更ファイル:** `agentIntent.ts`、`editCodeIntent.ts`、`executionSubagentToolCallingLoop.ts`、`searchSubagentToolCallingLoop.ts`、`compactionEndpoint.ts` ほか計 15 ファイル

**次のアクション:** Draft 解除と本レビュー依頼。

---

### ![Issue](icons/issue-opened.svg) [Should we create new terminal when switching in-out of sandbox commands?](https://github.com/microsoft/vscode/issues/318432) `#318432`
**ステータス:** Open | **ラベル:** なし

**概要:** サンドボックス/非サンドボックスのコマンドを切り替える際に、同一ターミナルを使い回すべきか、新規ターミナルを作るべきかの設計議論。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** @meganrogge を交えた設計判断。

---

### ![Issue](icons/issue-opened.svg) [Agents Window Account Hover has wrong cursor type when hovering actions](https://github.com/microsoft/vscode/issues/317017) `#317017`
**ステータス:** Closed | **ラベル:** bug, verified, polish, insiders-released, UBB

**概要:** Agents ウィンドウのアカウントホバー内アクションに `cursor: pointer` が当たっていない。`Action` 概念を再実装している箇所を共通 Actionbar に置き換えるべきという設計指摘付き。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders で対応済みクローズ。

---

### ![Issue](icons/issue-opened.svg) [Upgrade action shows for chat input notification but not the chat dashboard](https://github.com/microsoft/vscode/issues/317179) `#317179`
**ステータス:** Closed | **ラベル:** bug, verified, UBB

**概要:** チャット入力部の通知にはアップグレードアクションが出るが、チャットダッシュボードには出ない。アップグレード不可な場合の文言も別物にすべき。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 修正済みクローズ。

---

### ![PR](icons/git-pull-request.svg) [Fix stale BYOK model selection after vendor resolves with zero models](https://github.com/microsoft/vscode/pull/318424) `#318424`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** BYOK モデル削除後にリロードしてもチャットピッカーの表示モデルが旧選択のまま残るバグの修正。BYOK vendor が空モデルで resolve されるケースを未解決→解決の遷移として扱い `onDidChangeLanguageModels` を発火させ、`resetCurrentLanguageModelIfUnavailable` を走らせる。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `languageModels.ts`、`languageModels.test.ts`

**次のアクション:** Draft 解除と本レビュー依頼。

---

### ![Issue](icons/issue-opened.svg) [Usage compared to ULB not updating](https://github.com/microsoft/vscode/issues/315101) `#315101`
**ステータス:** Open | **ラベル:** bug, upstream, UBB

**概要:** Copilot Business ユーザーで ULB が 300 credits に設定された状態で、ステータスメニュー上の上限表示は正しいが、リクエスト送信後の使用量が更新されない。

**直近のアクティビティ:** cwebster-99 が GitHub 側で本日〜明日にかけて修正をロールアウト予定とコメント。

**次のアクション:** GitHub 側のロールアウト待ち（upstream）。

---

### ![Issue](icons/issue-opened.svg) [Chat session history lost when switching between GitHub-authenticated and BYOK/anonymous mode – sessions still on disk but inaccessible](https://github.com/microsoft/vscode/issues/318152) `#318152`
**ステータス:** Open | **ラベル:** なし

**概要:** PR #317428（air-gapped BYOK）以降、GitHub ログアウト後にチャット履歴が空になるが workspaceStorage の `.json`/`.jsonl` ファイルはディスク上に残っている。匿名 BYOK モードで新規作成したセッションも再起動後に消える可能性がある。永続化が認証プロファイルに紐づくのが原因と分析。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** `ChatSessionStore` のプロファイル依存の見直しと、匿名/BYOK モードでの永続化設計を再検討。

---

### ![Issue](icons/issue-opened.svg) [Repeated SIGABRT crash in Snap build on KDE Plasma Wayland with --ozone-platform=x11](https://github.com/microsoft/vscode/issues/316286) `#316286`
**ステータス:** Closed | **ラベル:** なし

**概要:** Ubuntu 26.04 + KDE Plasma Wayland 環境で Snap 版 VS Code を `--ozone-platform=x11` で起動すると、数時間使用後に SIGABRT で繰り返しクラッシュする。`coredumpctl` の記録あり。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み（重複/Snap パッケージ側案件と推定）。

---

### ![Issue](icons/issue-opened.svg) [Responses API Requests Should Include Explicit Message Item Types](https://github.com/microsoft/vscode/issues/318125) `#318125`
**ステータス:** Open | **ラベル:** なし

**概要:** Azure Foundry のような厳格な Responses API endpoint に対し、user/system メッセージや tool-result 用合成 user メッセージに `type: 'message'` を明示せず送信しているため、`Supported values are: ... 'message'` というエラーで拒否される。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** 共有 Responses コンバータで全 input item に明示的な `type` を付与するよう修正。

---

### ![PR](icons/git-pull-request.svg) [Add .agents workspace and personal path support for prompts, instructions, hooks, and agents](https://github.com/microsoft/vscode/pull/318195) `#318195`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** ワークスペースの `.agents` フォルダ配下にある agents / prompts / instructions / hooks の解決をサポート。skills は既に対応済みで、残りのリソース種別を機能パリティに揃える。

**直近のアクティビティ:** Copilot レビューが overview を生成、cgorican が複数回コメント。

**変更ファイル:** `chat.shared.contribution.ts`、`promptFileLocations.ts`

**次のアクション:** トリアージとレビュー継続。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Show all errors and warnings in project for all JavaScript and TypeScript files, not just opened ones](https://github.com/microsoft/vscode/issues/13953) `#13953`
**ステータス:** Open | **ラベル:** feature-request, typescript

**概要:** 2016 年からの古い要望。プロジェクト全体（未オープン含む）の JS/TS エラー/警告を Problems ビューに表示してほしい。実験的フラグでの回避策はあるが、`node_modules` を除外しないなどの課題あり。

**直近のアクティビティ:** fbf-prog64 と frederikhors が実験機能の使用感をやり取り。

**次のアクション:** 既存実験機能の正式化検討（長期）。

---

### ![PR](icons/git-pull-request.svg) [Do not show multipliers for cloud model picker for UBB accounts](https://github.com/microsoft/vscode/pull/318416) `#318416`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** UBB アカウントの Cloud チャットセッションモデルピッカーで「multiplier」ラベル表示を抑制し、課金セマンティクスと UI を一致させる。Fixes #318418。

**直近のアクティビティ:** Copilot レビューが overview を生成、lramos15 が承認しマージ、release/1.122 へ自動 cherry-pick。

**変更ファイル:** `copilotCloudSessionsProvider.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![Issue](icons/issue-opened.svg) [Multipliers show for UBB accounts in Cloud model picker](https://github.com/microsoft/vscode/issues/318418) `#318418`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased

**概要:** Cloud モデルピッカーで UBB アカウントに対し multiplier が表示されてしまうバグ（スクリーンショットのみ）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** PR #318416 で対応済みクローズ。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Reapply "Fix UBB model details in Copilot CLI (#316452)" (#316698)](https://github.com/microsoft/vscode/pull/318405) `#318405`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** Copilot CLI における UBB モデル詳細表示の修正（過去のリバートを再適用）を `release/1.122` へ cherry-pick。Fixes #315801。

**直近のアクティビティ:** Copilot レビューはエラー、lramos15 と rebornix が承認してマージ。

**変更ファイル:** `copilotcli/node/copilotCli.ts`、`chatModelPicker.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![Issue](icons/issue-opened.svg) [Copilot CLI doesn't show UBB pricing](https://github.com/microsoft/vscode/issues/315801) `#315801`
**ステータス:** Closed | **ラベル:** bug, upstream, candidate, unreleased, UBB

**概要:** Local や Claude エージェントでは UBB 価格が表示されるが、Copilot CLI 経路では表示されないバグ。

**直近のアクティビティ:** vs-code-engineering ボットが PR #318405 / #318399 のメディアを検証用に共有。

**次のアクション:** 修正済みクローズ。

---

### ![Issue](icons/issue-opened.svg) [Diagrams don't respond to theme changes](https://github.com/microsoft/vscode/issues/316968) `#316968`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, mermaid

**概要:** Markdown 内の mermaid 図表が VS Code のテーマ変更に追従しないバグ。

**直近のアクティビティ:** osortega が「別 issue #318398 のため verify できない」と @mjbvz 宛にコメント。

**次のアクション:** Insiders で対応済みクローズ、verify は別 issue 解決待ち。

---

### ![Issue](icons/issue-opened.svg) [Add a `/chronicle` cost optimization sub command](https://github.com/microsoft/vscode/issues/316657) `#316657`
**ステータス:** Closed | **ラベル:** feature-request, on-testplan, UBB

**概要:** 既存の `/chronicle` 機能に、ユーザーの過去セッションに基づくコスト最適化サブコマンドを追加してほしいという要望（Copilot CLI 側 PR を参考）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Only update sandbox config on connect and user settings change](https://github.com/microsoft/vscode/pull/318339) `#318339`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** `agentHostSandboxForwarder` が頻繁にサンドボックス設定を再同期していた挙動を、Agent Host 接続時とユーザー設定変更時のみに限定する。#318338 の対応。

**直近のアクティビティ:** Copilot レビューはエラー、roblourens が承認しマージ、release/1.122 へ自動 cherry-pick。

**変更ファイル:** `agentHostSandboxForwarder.ts`、`agentHostSandboxForwarder.test.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [feat: update to Electron v42](https://github.com/microsoft/vscode/pull/316661) `#316661`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code のランタイム/ビルドツールチェインを Electron 42 + Node 24 に更新する大規模 PR。148 ファイル、+1,202 / -1,057 行。依存メタデータ、ビルド設定、プラットフォームアーティファクト処理、TypeScript ターゲットなどを横断的に変更。Fixes #292445。

**直近のアクティビティ:** Copilot レビューが overview を生成、Yoyokrazy が承認、dmitrivMS が複数の指摘コメントを投下、rzhao271 / kycutler もコメント。dmitrivMS が「とてもクール、致命的でない指摘あり」とまとめ。

**変更ファイル:** Electron / Node ランタイム、ビルド系 (`build/`)、ほぼ全 extension の `package*.json` を含む 148 ファイル

**次のアクション:** dmitrivMS からのコメント対応とビルド検証継続。

---

### ![Issue](icons/issue-opened.svg) [Chronicle SQL: Standup returns empty results 88% of the time](https://github.com/microsoft/vscode/issues/317357) `#317357`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, chronicle

**概要:** `/chronicle` の standup アクション (`source=standup`) が 88% のユーザーで空結果を返してしまう問題。プリフライトチェック、空状態の説明メッセージ、デフォルト期間を 48h に拡張する案が提案された。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** PR #317670 で対応済みクローズ。

---

### ![Issue](icons/issue-opened.svg) [Chronicle SQL: LLM hallucinates schema — 97% of query errors are wrong column/table names](https://github.com/microsoft/vscode/issues/317355) `#317355`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, chronicle

**概要:** Chronicle SQL ツールのエラー 97% は LLM が存在しないカラム/テーブル名（`session_id`, `messages` など）を生成することに起因。CREATE TABLE 文をプロンプトに注入し、エラー時に再試行する案を提示。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Chronicle cloud sync: createCloudSession should gracefully handle policy-blocked users](https://github.com/microsoft/vscode/issues/317331) `#317331`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released

**概要:** エンタープライズポリシーで Cloud session 作成がブロックされている場合、Chronicle が毎セッションで `createCloudSession` を試行し、circuit breaker をトリップさせてしまう問題。policy_blocked を TTL 付きキャッシュし circuit breaker 失敗から除外する設計提案。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** PR #317670 関連で対応済みクローズ。

---

### ![Issue](icons/issue-opened.svg) ["with" button in agents view broken](https://github.com/microsoft/vscode/issues/318423) `#318423`
**ステータス:** Open | **ラベル:** なし

**概要:** リポジトリビューで選択中の状態で agents view の「with」ボタンをクリックしても変更できないバグ。動画添付。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** トリアージとイベントハンドリング確認。

---

### ![Issue](icons/issue-opened.svg) [Azure BYOK Entra Auth Should Support Configurable Audiences and Scopes](https://github.com/microsoft/vscode/issues/318111) `#318111`
**ステータス:** Open | **ラベル:** なし

**概要:** Azure BYOK の Entra 認証で要求するスコープが `https://cognitiveservices.azure.com/.default` にハードコードされており、Azure Government、APIM 前段、独自 Entra アプリ等のシナリオに対応できない。設定可能な audience/scope を導入してほしい。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** スコープ設定可能化の実装検討。

---

### ![Issue](icons/issue-opened.svg) [BYOK UI: Last model should not be selected after removing BYOK models](https://github.com/microsoft/vscode/issues/318417) `#318417`
**ステータス:** Open | **ラベル:** model-byok

**概要:** BYOK モデル削除後にリロードしても、チャットウィジェットに前回利用モデルが表示されてしまうが、クリックすると「Auto」しか出ない不整合。

**直近のアクティビティ:** dmitrivMS が「オフライン BYOK 固有ではない」として vritant24 にアサイン。

**次のアクション:** PR #318424 と連携した対応。

---

### ![Issue](icons/issue-opened.svg) [Integrated terminal automatically sends 'Y' when terminating node batch script](https://github.com/microsoft/vscode/issues/142341) `#142341`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, terminal-conpty, unreleased, terminal-input

**概要:** Windows の統合ターミナルで Ctrl+C で node スクリプトを終了するとき、`Terminate batch job (Y/N)?` に自動的に `Y` が送信されてしまうが、最初の 1 プロセスしか止まらず残り出力が次プロンプトに混入する古いバグ（2022 年起票）。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders で対応済みクローズ。

---

### ![Issue](icons/issue-opened.svg) [VSCode freezes when opening a terminal](https://github.com/microsoft/vscode/issues/239838) `#239838`
**ステータス:** Closed | **ラベル:** important, freeze-slow-crash-leak, candidate, terminal, gpu, unreleased

**概要:** Windows 環境で 1.97 アップデート後、TypeScript プロジェクトを開きターミナルを開こうとすると VS Code 全体がフリーズして使用不能になるバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders で対応済みクローズ。

---

### ![Issue](icons/issue-opened.svg) [Native context menu hijacks custom one in webview editor](https://github.com/microsoft/vscode/issues/239862) `#239862`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, electron, confirmed, regression, unreleased

**概要:** Git Graph などの webview エディタで右クリックすると、カスタムコンテキストメニューではなくテキスト操作のみのネイティブメニューが表示されてしまうリグレッション。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** Insiders で対応済みクローズ。

---

### ![Issue](icons/issue-opened.svg) [BYOK provider cannot be removed in no-auth scenario if only one is present](https://github.com/microsoft/vscode/issues/318375) `#318375`
**ステータス:** Closed | **ラベル:** model-byok

**概要:** サインインなしシナリオで BYOK プロバイダが 1 つしかないとき、削除操作ができない。サインイン状態では Copilot モデルのみで意図的な仕様だったが、Offline / No GitHub Copilot シナリオではグルーピング表示が必要。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: support GitHub Enterprise authentication in Copilot CLI agent](https://github.com/microsoft/vscode/pull/314724) `#314724`
**ステータス:** Open | **ラベル:** upstream, copilot-cli-agent

**概要:** Copilot CLI Agent が GitHub Enterprise (GHE) デプロイに対応していない問題を修正。GHE 用の Copilot API URL を導出し、Copilot JWT トークン交換を行い、`COPILOT_API_URL` 環境変数を GHE 時のみ設定する。Fixes #304075。

**直近のアクティビティ:** Copilot レビューが overview を複数回生成、fighterii が指摘に対して 7 回コメント、DonJayamanne や jzabroski らもコメント。XavierMP14 のスパム的コメントあり。

**変更ファイル:** `extensions/copilot/src/extension/chatSessions/copilotcli/node/copilotCli.ts`、`copilotCliAuth.spec.ts`

**次のアクション:** レビュー指摘の解消と最終承認待ち。

---

### ![PR](icons/git-pull-request.svg) [Update README.md](https://github.com/microsoft/vscode/pull/318412) `#318412`
**ステータス:** Closed | **ラベル:** なし

**概要:** Premiermoney による README 更新を装った PR だが、ベースブランチが `joao/b`、変更ファイルが大量の `.agents/` `.eslint-plugin-local/` `.github/` 配下を含む実質的に無意味な PR（additions=0, deletions=0）。

**直近のアクティビティ:** CODENOTIFY が大量のオーナーへ通知、Copilot レビューはクォータ上限で実行不可と返答。

**次のアクション:** クローズ済み（誤投稿/スパム）。

---

### ![PR](icons/git-pull-request.svg) [Turns off `asyncTokenization` by default. Fixes #235344. Fixes #202938](https://github.com/microsoft/vscode/pull/244217) `#244217`
**ステータス:** Open | **ラベル:** なし

**概要:** `editor.experimental.asyncTokenization` が組み込み言語に問題を起こすため、既定で無効化する 1 行設定変更。Fixes #235344, #202938, #237438, #261007。

**直近のアクティビティ:** Copilot レビューが overview を生成。

**変更ファイル:** `editorConfigurationSchema.ts`

**次のアクション:** 人間レビュアーによる判断とマージ。

---

### ![PR](icons/git-pull-request.svg) [Fix BYOK invalid stateful marker retries](https://github.com/microsoft/vscode/pull/317292) `#317292`
**ステータス:** Open | **ラベル:** なし

**概要:** BYOK Responses API の `InvalidStatefulMarker` リトライ経路で、共有リトライフローが `ignoreStatefulMarker=true` で再送する際に BYOK OpenAI endpoint が値を `false` に上書きしてしまい、古い `previous_response_id` が再送される問題を修正。Fixes #316747。

**直近のアクティビティ:** PenguinDOOM が CI のスタックを報告、Copilot レビューが overview を生成、vritant24 と roblourens が dismiss。

**変更ファイル:** `byok/node/openAIEndpoint.ts`、`openAIEndpoint.spec.ts`

**次のアクション:** dismiss されたレビューを再依頼し再レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(dotenv): add environment variable snippets](https://github.com/microsoft/vscode/pull/318393) `#318393`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `.env` ファイル向けに `NODE_ENV` `PORT` `DATABASE_URL` `REDIS_URL` `API_KEY` `SECRET_KEY` などよく使う環境変数のスニペット 13 種を追加し、タブストップやチョイス値で入力をエルゴノミックにする。

**直近のアクティビティ:** freepinkg が CLA 同意のコメント、Copilot レビューはクォータ上限で実行不可。

**変更ファイル:** `extensions/dotenv/package.json`、`dotenv.code-snippets`

**次のアクション:** トリアージとレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix cutoff chat requests](https://github.com/microsoft/vscode/pull/318385) `#318385`
**ステータス:** Open | **ラベル:** なし

**概要:** `chatListRenderer.ts` の高さ変更通知を調整し、新規レンダリング項目（高さ未保存）でもレイアウト更新がトリガーされるようにすることで、チャットメッセージが途中で切れる問題を修正。

**直近のアクティビティ:** Copilot レビューが overview を生成、Yoyokrazy が承認。

**変更ファイル:** `chatListRenderer.ts`

**次のアクション:** 残レビュー解決とマージ。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Disable walkthrough if ai features disabled on startup](https://github.com/microsoft/vscode/pull/318397) `#318397`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** AI 機能無効化時にウォークスルー/補助バーを非表示にする #318364 の `release/1.122` 向け cherry-pick。`chatEntitlementService.sentiment.hidden` と `chat.disableAIFeatures` を確認するロジックを startup pages と layout に追加。

**直近のアクティビティ:** Copilot レビューはエラー、rebornix と bhavyaus が承認しマージ。

**変更ファイル:** `layout.ts`、`startupPage.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Reapply "Fix UBB model details in Copilot CLI (#316452)" (#316698)](https://github.com/microsoft/vscode/pull/318399) `#318399`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** Copilot CLI のモデルメタデータ（価格カテゴリとトークンコスト）強化およびピッカー UI 表示の改善を、リバート分を含めて再適用する。Fixes #315801。

**直近のアクティビティ:** pwang347 が Copilot 指摘について lramos15 にメンション、Yoyokrazy と lramos15 が承認しマージ、release/1.122 へ自動 cherry-pick 予定。

**変更ファイル:** `copilotcli/node/copilotCli.ts`、`chatModelPicker.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Add mobile multi-diff view](https://github.com/microsoft/vscode/pull/318081) `#318081`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** モバイル向けの複数ファイル差分ビュー `MobileMultiDiffView` を導入。stickyper-file ヘッダ、chevron トグルでの折りたたみ、非同期コンテンツロード、Vite playground エントリを追加し、モバイルオーバーレイ/タイトルバーに接続する。

**直近のアクティビティ:** Copilot レビューが overview を生成（電話向け積層 multi-diff 体験への "changes" エントリ移動を確認）。

**変更ファイル:** `mobile-multi-diff-worker.ts`、`mobileMultiDiffView.css`、`mobileDiffHelpers.ts`、`mobileMultiDiffView.ts`、`mobileMultiDiffVirtualizer.ts` ほか計 14 ファイル

**次のアクション:** Draft 解除と本レビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] release/1.122: catch up to verified Insiders build (183159e)](https://github.com/microsoft/vscode/pull/318400) `#318400`
**ステータス:** Closed | **ラベル:** cherry-pick-artifact

**概要:** `release/1.122` を verified Insiders build (183159e) へ追いつかせる #318394 の自動 cherry-pick。`chat.contribution.ts` でマージコンフリクトが発生し、手動解決が必要となっていた。

**直近のアクティビティ:** Copilot レビューはエラーで失敗。

**変更ファイル:** `src/vs/sessions/contrib/chat/browser/chat.contribution.ts`

**次のアクション:** クローズ済み（直接 #318394 がマージされたため）。

---

### ![PR](icons/git-pull-request.svg) [release/1.122: catch up to verified Insiders build (183159e)](https://github.com/microsoft/vscode/pull/318394) `#318394`
**ステータス:** Merged | **ラベル:** なし

**概要:** Insiders build 183159e に含まれる main の 10 コミットを squash cherry-pick。web_fetch の URL 表示、SSH リモート Agent Host パスフレーズ認証修正、worktreeCreated タスクの設定追加、git タイムアウトエラー改善、LF→CRLF 正規化、SSH リモートホストの X ボタン削除復活、`agent-host-copilotcli` コマンド重複登録修正、launch スクリプト改善、リモートホスト切断時の unread state 保持、ターミナルの codenotify 追加など。

**直近のアクティビティ:** bryanchen-d が承認、Copilot レビューが overview を生成しマージ。

**変更ファイル:** `agentHost/` 配下、`sessions/` 配下、`workbench/contrib/chat/` ほか計 32 ファイル

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Disable walkthrough if ai features disabled on startup](https://github.com/microsoft/vscode/pull/318364) `#318364`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** AI 機能が `chat.disableAIFeatures` などで無効/非表示の場合、ウォークスルーと補助バーの AI フォーカス UI をスキップする。`StartupPageRunnerContribution` に `IChatEntitlementService` を注入。

**直近のアクティビティ:** rzhao271 が変更要求 → dismiss → 承認、bhavyaus も変更要求 → 承認の流れ。`.github/instructions/chat.instructions.md` の Hiding AI Features ガイドへの言及あり。

**変更ファイル:** `layout.ts`、`startupPage.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump uuid and @vscode/deviceid in /remote](https://github.com/microsoft/vscode/pull/318392) `#318392`
**ステータス:** Open | **ラベル:** javascript, dependencies

**概要:** Dependabot による `uuid` 9.0.1 → 14.0.0 と `@vscode/deviceid` の同時更新。`uuid` v14 は global `crypto` を必須とし node@20+ が必要となる破壊的変更を含む。

**直近のアクティビティ:** Copilot レビューはエラー、vritant24 が承認。

**変更ファイル:** `remote/package-lock.json`、`remote/package.json`

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump @nevware21/ts-utils from 0.11.6 to 0.14.0 in /extensions/git](https://github.com/microsoft/vscode/pull/318386) `#318386`
**ステータス:** Merged | **ラベル:** javascript, dependencies

**概要:** Dependabot による `@nevware21/ts-utils` の 0.11.6 → 0.14.0 更新（git extension）。新しい array/string ヘルパー追加が主要差分。

**直近のアクティビティ:** Yoyokrazy と rzhao271 が承認しマージ、CODENOTIFY が @lszomoru へ通知。

**変更ファイル:** `extensions/git/package-lock.json`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump @nevware21/ts-utils from 0.11.6 to 0.14.0 in /extensions/github](https://github.com/microsoft/vscode/pull/318384) `#318384`
**ステータス:** Merged | **ラベル:** javascript, dependencies

**概要:** Dependabot による `@nevware21/ts-utils` の 0.11.6 → 0.14.0 更新（github extension）。

**直近のアクティビティ:** rzhao271 と Yoyokrazy が承認しマージ。

**変更ファイル:** `extensions/github/package-lock.json`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump @nevware21/ts-utils from 0.11.6 to 0.14.0 in /extensions/json-language-features](https://github.com/microsoft/vscode/pull/318276) `#318276`
**ステータス:** Merged | **ラベル:** javascript, dependencies

**概要:** Dependabot による `@nevware21/ts-utils` の 0.11.6 → 0.14.0 更新（json-language-features）。

**直近のアクティビティ:** DonJayamanne と rzhao271 が承認しマージ。

**変更ファイル:** `extensions/json-language-features/package-lock.json`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Chronicle: Circuit breaker improvement and empty result hint](https://github.com/microsoft/vscode/pull/317670) `#317670`
**ステータス:** Merged | **ラベル:** なし

**概要:** Chronicle のサーキットブレーカで HALF_OPEN プローブスロットを無駄遣いしていた問題を、`cancelProbe()` API 追加と rate-limited / 早期 return パスでの呼び出しで修正。`onStateChange` でテレメトリ観測可能化、failureCount を閾値クランプ。standup 結果が空のとき `/chronicle:reindex` 提案を促す skill メモも追加。Fixes #317330, #317357。

**直近のアクティビティ:** connor4312 が「`cockatiel` を保守しているので類似機能で困ったら使って」とコメント、Copilot レビュー overview、dmitrivMS が承認しマージ。

**変更ファイル:** `chronicle/SKILL.md`、`circuitBreaker.ts`、`circuitBreaker.spec.ts`、`remoteSessionExporter.ts`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump @nevware21/ts-utils from 0.11.6 to 0.14.0 in /extensions/typescript-language-features](https://github.com/microsoft/vscode/pull/318141) `#318141`
**ステータス:** Merged | **ラベル:** javascript, dependencies

**概要:** Dependabot による `@nevware21/ts-utils` の 0.11.6 → 0.14.0 更新（typescript-language-features）。

**直近のアクティビティ:** DonJayamanne と rzhao271 が承認しマージ。

**変更ファイル:** `extensions/typescript-language-features/package-lock.json`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Add marketplace ref support for plugin marketplaces](https://github.com/microsoft/vscode/pull/317901) `#317901`
**ステータス:** Merged | **ラベル:** なし

**概要:** プラグインマーケットプレースの参照に Git ref を `#ref` 形式で指定できるようにし、デフォルトを `github/awesome-copilot#marketplace` に変更。GitHub マーケットプレースの fetch が常に `main` を読まず指定 ref から読み込むよう改修、ref ごとに別キャッシュ/クローン位置を保持。

**直近のアクティビティ:** aaronpowell が CI passing 報告、Copilot レビュー overview、connor4312 と Yoyokrazy が承認しマージ。

**変更ファイル:** `agentPluginRepositoryService.ts`、`marketplaceReference.ts`、`pluginMarketplaceService.ts`、`workspacePluginSettingsService.ts` ほか計 8 ファイル

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [fix(breadcrumbs): handle missing tree element in outline picker](https://github.com/microsoft/vscode/pull/318323) `#318323`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `BreadcrumbsOutlinePicker._setInput` で tree に未登録の要素に対し `tree.reveal` / `tree.setFocus` を呼ぶと TreeError "Tree element not found" が発生していたのを try-catch で吸収する修正。Closes #316635。

**直近のアクティビティ:** Copilot レビューが overview を生成。

**変更ファイル:** `breadcrumbsPicker.ts`

**次のアクション:** トリアージとレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Agent host instructions and customization tweaks](https://github.com/microsoft/vscode/pull/318051) `#318051`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agent Host のカスタマイズ周りで複数の修正を一括投入。再接続時の FS provider 投入、暫定セッションでの activeClient publish、ユーザーカスタマイズの Agent Host への配信、instructions のサポート（SDK は未対応のため別途調整中）。closes #316823。

**直近のアクティビティ:** Copilot レビューが overview を生成、dmitrivMS が dismiss、Yoyokrazy が承認しマージ。

**変更ファイル:** `remoteAgentHostProtocolClient.ts`、`copilotAgent.ts`、`copilotPluginConverters.ts`、`protocolServerHandler.ts` ほか計 26 ファイル

**次のアクション:** マージ済み。SDK 側 instructions 対応の進捗フォロー。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump qs from 6.14.2 to 6.15.2 in /extensions/copilot](https://github.com/microsoft/vscode/pull/318275) `#318275`
**ステータス:** Merged | **ラベル:** javascript, dependencies

**概要:** Dependabot による `qs` の 6.14.2 → 6.15.2 更新（copilot extension）。`stringify` / `parse` の各種バグ修正を含む。

**直近のアクティビティ:** DonJayamanne と rzhao271 が承認しマージ。

**変更ファイル:** `extensions/copilot/package-lock.json`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [build(deps-dev): bump qs from 6.14.2 to 6.15.2 in /build](https://github.com/microsoft/vscode/pull/318140) `#318140`
**ステータス:** Merged | **ラベル:** javascript, dependencies

**概要:** Dependabot による `qs` の 6.14.2 → 6.15.2 更新（build 配下の dev dependency）。

**直近のアクティビティ:** DonJayamanne と rzhao271 が承認しマージ。

**変更ファイル:** `build/package-lock.json`

**次のアクション:** マージ済み、対応完了。

---

### ![PR](icons/git-pull-request.svg) [Fix `runTests` tool not handling absolute file paths for remote workspaces](https://github.com/microsoft/vscode/pull/318278) `#318278`
**ステータス:** Open | **ラベル:** なし

**概要:** リモートワークスペースで `runTests` チャットエージェントツールが絶対パスを正しく解決できない問題を修正。#317904 のフォローアップ。

**直近のアクティビティ:** Copilot レビューが overview を含む 6 回のコメントを生成（合計 16 件のコメント指摘）。

**変更ファイル:** `testService.ts`、`testingChatAgentTool.ts`、`testingChatAgentTool.test.ts`

**次のアクション:** Copilot 指摘の対応と人間レビュアーの承認待ち。

---

### ![PR](icons/git-pull-request.svg) [Polish AI Customization modal editor styles](https://github.com/microsoft/vscode/pull/318378) `#318378`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** AI Customization Management Editor 内の埋め込み詳細コンポーネントについてレイアウトとアクセシビリティを改善。modal editor ヘッダの説明を整理し、コンパクトヘッダオプションを導入してレスポンシブにする。

**直近のアクティビティ:** github-actions ボットがスクリーンショット diff レポート（68 件の変更）を投稿、Copilot レビューはエラー。

**変更ファイル:** `editor.ts`、`modalEditorPart.css`、`modalEditorPart.ts`、`aiCustomizationManagementEditor.ts` ほか計 11 ファイル

**次のアクション:** Draft 解除と本レビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [fix(environment): add setter for editSessionId](https://github.com/microsoft/vscode/pull/318325) `#318325`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `editSessionId` プロパティが native / browser 環境サービスで getter のみとして定義されていたため、`editSessions.contribution.ts` の代入で「Cannot set property」エラーが発生していた問題を修正。両環境に setter を追加。Closes #316633。

**直近のアクティビティ:** microsoft-github-policy-service ボットが SpencerJung に CLA 同意を 2 回要求、Copilot レビューが overview を生成。

**変更ファイル:** `environmentService.ts`（platform）、`environmentService.ts`（workbench services）

**次のアクション:** CLA 同意確認とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Deduplicate hooks from multiple discovery paths](https://github.com/microsoft/vscode/pull/303928) `#303928`
**ステータス:** Closed | **ラベル:** なし

**概要:** 同じ hook が `.github/hooks/` と `.claude/settings.json` の両方に定義されると `computeHooks()` が二重発火する問題を、`deduplicateHooks()` 関数で署名ベースに O(n) で重複排除する修正。クロスツール（Copilot CLI/Gemini CLI と Claude Code）併用プロジェクトでの実害解消が目的。Fixes #303926。

**直近のアクティビティ:** Copilot レビューが複数回 overview を生成、lewing が複数コメント、pwang347 もコメント。最終的にクローズ。

**変更ファイル:** `hookSchema.ts`、`promptsServiceImpl.ts`、`hookSchema.test.ts`

**次のアクション:** クローズ済み（別の対応に移行と推定）。

---

### ![PR](icons/git-pull-request.svg) [fix: cap agent session cache serialization to prevent perf regression (fixes #314404)](https://github.com/microsoft/vscode/pull/314780) `#314780`
**ステータス:** Open (Draft) | **ラベル:** errors-fix, *error-fix-driving

**概要:** Window フォーカス時のストレージフラッシュで `safeStringify()` がエージェントセッションデータ全体を同期シリアライズしメインスレッドをブロックして `PerfSampleError` を発火していた問題。エージェントセッション利用増加による 1.118.1 での 52.5 倍スパイクが 477 ユーザーに影響。Fixes #314404。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `agentSessionsModel.ts`

**次のアクション:** Draft 解除と @bpasero のレビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [Fix session folder URI for non-git folders](https://github.com/microsoft/vscode/pull/318369) `#318369`
**ステータス:** Closed | **ラベル:** なし

**概要:** `CopilotChatSessionsProvider._buildWorkspace` が `repositoryPath` メタデータのみを参照し、git リポジトリのないフォルダで `unknown:///` にフォールバックしていた問題を修正。既に存在する `workingDirectoryPath` メタデータから `folderUri` を抽出して使う。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** `copilotChatSessionsProvider.ts`

**次のアクション:** クローズ済み（別経路で対応と推定）。

---
## qmk/qmk_firmware

> 直近24時間でも自作キーボード新規追加 PR が多数寄せられ、加えてマウス HID descriptor 修正やコミュニティモジュール永続化など、コア機能まわりの大きな PR が活発に進行している。

### ![PR](icons/git-pull-request.svg) [add Neo2 as keyboard layout for mac](https://github.com/qmk/qmk_firmware/pull/26233) `#26233`
**ステータス:** Closed | **ラベル:** keymap

**概要:** koph1023 による Keychron Q7 ANSI 向けの Mac 用 Neo2 配列キーマップ追加 PR。PR テンプレートのチェック項目はほぼ未記入のまま提出された。zvecr から既存 PR #22724 への参照コメントがあり、重複扱いでクローズされた。

**直近のアクティビティ:** zvecr が「#22724」と既存 PR を引用し、本 PR は重複としてクローズ済み。

**変更ファイル:** `keyboards/keychron/q7/ansi/keymaps/Neo2/keymap.c`

**次のアクション:** 同様の Neo2 配列が必要なら #22724 側のレビュー進行を追うこと。

---

### ![PR](icons/git-pull-request.svg) [Support for trackpads in the digitizer feature](https://github.com/qmk/qmk_firmware/pull/24964) `#24964`
**ステータス:** Open | **ラベル:** core, keymap, awaiting changes, documentation, awaiting review, breaking_changes_2026q2

**概要:** george-norton による digitizer 機能を拡張してトラックパッドとしてレポートさせる PR の第 1 段。USB feature report と device driver は切り離し、digitizer を pointing device 同様の処理パイプラインに乗せ、マウスへのフォールバックも備える。Windows Precision Touchpad 要件は未充足のため当面は Linux ホスト限定で動作する。

**直近のアクティビティ:** sigprof と george-norton の間で詳細なコードコメントのやり取りが続き、最近 drashna と sigprof から複数の APPROVED レビューが付き、2026q2 breaking change への取り込みが視野に入っている。

**変更ファイル:** `builddefs/common_features.mk`, `docs/features/digitizer.md`, `keyboards/handwired/onekey/keymaps/digitizer/config.h`, `keyboards/handwired/onekey/keymaps/digitizer/keymap.c`, `quantum/digitizer.c`, `quantum/digitizer.h`, `quantum/digitizer_driver.h`, `quantum/digitizer_mouse_fallback.c`, `quantum/digitizer_mouse_fallback.h`, `quantum/keyboard.c`, `quantum/keyboard.h`, `quantum/pointing_device/pointing_device.c`, `quantum/pointing_device/pointing_device.h`, `quantum/quantum.c`, `quantum/split_common/transaction_id_define.h`, `quantum/split_common/transactions.c`, `quantum/split_common/transport.h`, `tmk_core/protocol.mk`, `tmk_core/protocol/chibios/usb_main.c`, `tmk_core/protocol/host.c`, `tmk_core/protocol/host.h`, `tmk_core/protocol/host_driver.h`, `tmk_core/protocol/report.h`, `tmk_core/protocol/usb_descriptor.c`, `tmk_core/protocol/usb_descriptor.h`

**次のアクション:** breaking_changes_2026q2 ブランチへのマージ可否を最終確認し、awaiting changes として残っている指摘の有無を整理する。

---

### ![PR](icons/git-pull-request.svg) [adding a handwired keyboard "handwired/tnx_corne_f401"](https://github.com/qmk/qmk_firmware/pull/26032) `#26032`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** ToYoNiX による handwired/tnx_corne_f401 という自作 Corne 系キーボード（STM32F401）の追加 PR。`keyboards/handwired/` 配下に config・keymap・readme をまとめて投入している。

**直近のアクティビティ:** drashna から「`.c`/`.h` には GPL 互換ライセンスヘッダを付けること」、zvecr から「default keymap の `rules.mk` に `ENCODER_MAP_ENABLE = yes` を追加せよ」と CHANGES_REQUESTED。最終的に drashna が APPROVED を付けた状態。

**変更ファイル:** `keyboards/handwired/tnx_corne_f401/config.h`, `keyboards/handwired/tnx_corne_f401/halconf.h`, `keyboards/handwired/tnx_corne_f401/keyboard.json`, `keyboards/handwired/tnx_corne_f401/keymaps/default/keymap.c`, `keyboards/handwired/tnx_corne_f401/keymaps/default/rules.mk`, `keyboards/handwired/tnx_corne_f401/mcuconf.h`, `keyboards/handwired/tnx_corne_f401/readme.md`

**次のアクション:** zvecr のもう一度の確認を取り、マージ判定を待つ。

---

### ![PR](icons/git-pull-request.svg) [Use unicode to make better, shorter, more readable key labels in `qmk info` output.](https://github.com/qmk/qmk_firmware/pull/25972) `#25972`
**ステータス:** Open | **ラベル:** awaiting review, python, cli, stale, dd, breaking_changes_2026q3

**概要:** spidey3 による `qmk info` のキーボードレイアウト表示改善 PR。QMK のキーコードデータベースを参照して人間可読なラベルを取得し、よく使うキーは Unicode ラベルに置き換え、簡易ヒューリスティクスでラベル短縮、表示幅も拡張している。

**直近のアクティビティ:** drashna から APPROVED が付いているが、stale ラベルも付与されており breaking_changes_2026q3 待ちで停滞気味。

**変更ファイル:** `data/constants/keycodes/keycodes_0.0.1_basic.hjson`, `data/constants/keycodes/keycodes_0.0.1_quantum.hjson`, `lib/python/qmk/keyboard.py`, `lib/python/qmk/tests/test_cli_commands.py`

**次のアクション:** stale 解除と 2026q3 breaking change ウィンドウでのマージ取り込み調整。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open | **ラベル:** needs doc, in progress, core, keyboard, help wanted, keymap, needs testing, documentation, breaking_change, python, cli, feature, CI, no-ci, dd

**概要:** tzarc が主導する XAP（eXtensible Application Protocol）実装トラッキング PR。#11567 を起点に、ホストとファームウェア間でキー再マッピングや RGB 設定などを汎用的にやり取りするプロトコル、関連 Python クライアント、ドキュメント、テンプレートをまとめて投入する大規模 WIP。

**直近のアクティビティ:** t-8ch、tzarc の議論コメントと jock070 の APPROVED が付いているが、長期 WIP のまま進行中で help wanted・needs testing ラベル多数。

**変更ファイル:** `.gitignore`, `builddefs/common_features.mk`, `builddefs/xap.mk`, `data/constants/lighting/led_matrix_0.0.1.hjson`, `data/constants/lighting/rgb_matrix_0.0.1.hjson`, `data/constants/lighting/rgblight_0.0.1.hjson`, `data/mappings/xap_defaults.json`, `data/schemas/definitions.jsonschema`, `data/schemas/xap.jsonschema`, `data/templates/xap/client/python/constants.py.j2`, `data/templates/xap/client/python/routes.py.j2`, `data/templates/xap/client/python/types.py.j2`, `data/templates/xap/docs/broadcast_messages.md.j2`, `data/templates/xap/docs/docs.md.j2`, `data/templates/xap/docs/response_flags.md.j2`, `data/templates/xap/docs/routes.md.j2`, `data/templates/xap/docs/term_definitions.md.j2`, `data/templates/xap/docs/type_docs.md.j2`, `data/templates/xap/docs/versions.md.j2`, `data/templates/xap/firmware/xap_generated.h.j2`, `data/templates/xap/firmware/xap_generated.inl.j2`, `data/xap/xap_0.0.1.hjson`, `data/xap/xap_0.1.0.hjson`, `data/xap/xap_0.2.0.hjson`, `data/xap/xap_0.3.0.hjson`, `docs/_sidebar.json`, `docs/xap.md`, `docs/xap_0.0.1.md`, `docs/xap_0.1.0.md`, `docs/xap_0.2.0.md`, `docs/xap_0.3.0.md`, `docs/xap_protocol.md`, `keyboards/zvecr/zv48/keymaps/xap/keymap.c`, `keyboards/zvecr/zv48/keymaps/xap/rules.mk`, `keyboards/zvecr/zv48/keymaps/xap/xap.hjson`, `keyboards/zvecr/zv48/xap.hjson`, `lib/python/qmk/casing.py`, `lib/python/qmk/cli/__init__.py`, `lib/python/qmk/cli/format/python.py`, `lib/python/qmk/cli/format/text.py`, `lib/python/qmk/cli/generate/api.py`, `lib/python/qmk/cli/generate/lighting_map.py`, `lib/python/qmk/cli/xap/__init__.py`, `lib/python/qmk/cli/xap/generate_docs.py`, `lib/python/qmk/cli/xap/generate_json.py`, `lib/python/qmk/cli/xap/generate_python.py`, `lib/python/qmk/cli/xap/generate_qmk.py`, `lib/python/qmk/cli/xap/xap.py`, `lib/python/qmk/commands.py`, `lib/python/qmk/constants.py`, `lib/python/qmk/info.py`, `lib/python/qmk/keyboard.py`, `lib/python/qmk/lighting.py`, `lib/python/qmk/xap/__init__.py`, `lib/python/qmk/xap/common.py`, `lib/python/qmk/xap/gen_client_js/__init__.py`, `lib/python/qmk/xap/gen_firmware/__init__.py`, `lib/python/qmk/xap/gen_firmware/blob_generator.py`, `lib/python/qmk/xap/jinja2_filters.py`, `lib/python/xap_client/__init__.py`, `lib/python/xap_client/client.py`, `lib/python/xap_client/constants.py`, `lib/python/xap_client/device.py`, `lib/python/xap_client/pyproject.toml`, `lib/python/xap_client/readme.md`, `lib/python/xap_client/routes.py`, `lib/python/xap_client/types.py`, `quantum/dynamic_keymap.c`, `quantum/dynamic_keymap.h`, `quantum/eeconfig.c`, `quantum/led_matrix/lighting_map.h`, `quantum/main.c`, `quantum/nvm/eeprom/nvm_eeconfig.c`, `quantum/nvm/eeprom/nvm_eeprom_eeconfig_internal.h`, `quantum/nvm/nvm_eeconfig.h`, `quantum/quantum.c`, `quantum/quantum.h`, `quantum/rgb_matrix/lighting_map.h`, `quantum/rgblight/lighting_map.h`, `quantum/xap/handlers/audio.c`, `quantum/xap/handlers/core.c`, `quantum/xap/handlers/lighting.c`, `quantum/xap/handlers/remapping.c`, `quantum/xap/xap.c`, `quantum/xap/xap.h`, `quantum/xap/xap_handlers.c`, `requirements.txt`, `tmk_core/protocol/chibios/usb_endpoints.c`, `tmk_core/protocol/chibios/usb_endpoints.h`, `tmk_core/protocol/chibios/usb_main.c`, `tmk_core/protocol/lufa/lufa.c`, `tmk_core/protocol/usb_descriptor.c`, `tmk_core/protocol/usb_descriptor.h`, `tmk_core/protocol/vusb/vusb.c`, `tmk_core/protocol/vusb/vusb.h`, `util/regen.sh`

**次のアクション:** スコープの確定と needs testing 解消に向けた手伝い手の確保、ドキュメント整備の継続。

---

### ![PR](icons/git-pull-request.svg) [Adding Volt 80 Custom PCB Support](https://github.com/qmk/qmk_firmware/pull/26158) `#26158`
**ステータス:** Merged | **ラベル:** keyboard, keymap

**概要:** Jubakuba による Percent Studio 製 Volt 80（aftermarket 系列）カスタム PCB の QMK 対応追加 PR。`keyboards/percent_studio_aftermarket/` 配下に keyboard.json と default keymap、readme を新設している。

**直近のアクティビティ:** drashna と zvecr の複数往復レビュー（一度 CHANGES_REQUESTED）の末、双方の APPROVED で本日マージ完了。

**変更ファイル:** `keyboards/percent_studio_aftermarket/keyboard.json`, `keyboards/percent_studio_aftermarket/keymaps/default/keymap.c`, `keyboards/percent_studio_aftermarket/percent_studio_aftermarket.c`, `keyboards/percent_studio_aftermarket/readme.md`

**次のアクション:** マージ済みのため対応不要。後続のバグ報告に注意。

---

### ![PR](icons/git-pull-request.svg) [Update encoder documents for json config](https://github.com/qmk/qmk_firmware/pull/26188) `#26188`
**ステータス:** Open | **ラベル:** documentation

**概要:** drashna によるエンコーダ機能ドキュメント更新 PR。JSON 設定方式に対応した記述を `docs/features/encoders.md` に追加する小規模ドキュメント変更。

**直近のアクティビティ:** filterpaper から「軽微な表記統一とタイポの修正提案」として CHANGES_REQUESTED が付いた状態。

**変更ファイル:** `docs/features/encoders.md`

**次のアクション:** 指摘されたタイポ・整合性修正を反映して再レビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [Align Mouse USB descriptor](https://github.com/qmk/qmk_firmware/pull/26230) `#26230`
**ステータス:** Open | **ラベル:** bug, core, breaking_changes_2026q2

**概要:** zvecr によるマウス USB ディスクリプタの整合性修正 PR。#26195 で漏れていた水平ホイール（Horizontal wheel）のディスクリプタを補い、合わせて vusb 側も同様の変更を適用する。

**直近のアクティビティ:** drashna が APPROVED を付与済み。breaking_changes_2026q2 タグでのマージ待ち。

**変更ファイル:** `tmk_core/protocol/usb_descriptor.c`, `tmk_core/protocol/vusb/vusb.c`

**次のアクション:** breaking change ウィンドウのタイミングでマージへ。

---

### ![PR](icons/git-pull-request.svg) [Extend core API to enable mouse keymap community modules](https://github.com/qmk/qmk_firmware/pull/26232) `#26232`
**ステータス:** Open | **ラベル:** core

**概要:** schuay による、コミュニティモジュールがホスト側マウス挙動を QMK のアクションパイプラインへブリッジできるようにするためのコア API 追加 PR。`host_last_system_usage`/`host_last_consumer_usage` と同様の `host_last_mouse_buttons()` を導入し、別経路で生じるマウス押下とモーション/ホイールを正しく合成できるようにする。

**直近のアクティビティ:** 直近 24 時間のコメントなし（提出直後の新規 PR）。

**変更ファイル:** `quantum/action_layer.c`, `quantum/action_layer.h`, `tests/mousekeys/test_mousekeys.cpp`, `tmk_core/protocol/host.c`, `tmk_core/protocol/host.h`

**次のアクション:** メンテナによる初回レビューを待つ。あわせて対象モジュール側の参照実装提示があると審査が進みやすい。

---

### ![PR](icons/git-pull-request.svg) [Add Commodore 64 kbd handwired to Arduino Pro Micro](https://github.com/qmk/qmk_firmware/pull/25800) `#25800`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** jeanpihet による、Commodore 64 のキーボードを Arduino Pro Micro 経由で USB 化する handwired キーボード追加 PR。default と full の 2 つのキーマップを同梱する。

**直近のアクティビティ:** zvecr と drashna が CHANGES_REQUESTED を繰り返しており、jeanpihet が複数往復で対応中。drashna からは APPROVED も出ているが zvecr 側のチェックが続いている。

**変更ファイル:** `keyboards/handwired/c64_promicro/keyboard.json`, `keyboards/handwired/c64_promicro/keymaps/default/keymap.c`, `keyboards/handwired/c64_promicro/keymaps/full/keymap.c`, `keyboards/handwired/c64_promicro/readme.md`

**次のアクション:** zvecr が残している指摘への対応と、最終 APPROVED 取得。

---

### ![PR](icons/git-pull-request.svg) [Enable modules to persist data](https://github.com/qmk/qmk_firmware/pull/26201) `#26201`
**ステータス:** Open | **ラベル:** core, keymap, documentation, python, cli, dd, community_module

**概要:** zvecr によるコミュニティモジュールが NVM にデータを永続化できるようにする PR。eeconfig まわりとモジュール用 hook（1.1.3）を拡張し、検証用に `qmk/nvm_test` モジュールと `nvm_test` キーマップ、ドキュメント更新を含む。参考実装として VIA module も提示。

**直近のアクティビティ:** drashna と dexter93 から APPROVED 取得済み。

**変更ファイル:** `builddefs/build_keyboard.mk`, `data/constants/module_hooks/1.1.3.hjson`, `docs/features/community_modules.md`, `keyboards/handwired/onekey/keymaps/nvm_test/keymap.c`, `keyboards/handwired/onekey/keymaps/nvm_test/keymap.json`, `lib/python/qmk/cli/generate/community_modules.py`, `modules/qmk/nvm_test/config.h`, `modules/qmk/nvm_test/nvm_test.c`, `modules/qmk/nvm_test/qmk_module.json`, `quantum/eeconfig.c`, `quantum/nvm/eeprom/nvm_eeprom_eeconfig_internal.h`

**次のアクション:** マージ可能状態のためメンテナのマージ判断を待つ。

---

### ![PR](icons/git-pull-request.svg) [converter: Add Pro micro to Halcyon adapter and remove Imera](https://github.com/qmk/qmk_firmware/pull/26226) `#26226`
**ステータス:** Open | **ラベル:** core, documentation

**概要:** veypatch によるコンバータ機能更新 PR。Pro Micro を Halcyon コントローラ互換に変換するアダプタを追加し（Halcyon シリーズのコントローラを任意の Pro Micro 互換ボードで使えるようにする）、販売中止となった Imera のコンバータ定義を削除する。

**直近のアクティビティ:** drashna が APPROVED、zvecr と veypatch の間でコメントのやり取りあり。

**変更ファイル:** `docs/feature_converters.md`, `platforms/chibios/converters/promicro_to_halcyon/_pin_defs.h`, `platforms/chibios/converters/promicro_to_halcyon/converter.mk`, `platforms/chibios/converters/promicro_to_imera/_pin_defs.h`

**次のアクション:** zvecr の最終確認を待ち、ドキュメント整合性を再チェック。

---

### ![PR](icons/git-pull-request.svg) [add constance_tkl keyboard](https://github.com/qmk/qmk_firmware/pull/25778) `#25778`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** patipan36991 による aiglatsonstudio/constance_tkl キーボード（STM32F072 搭載、出荷済み TKL）追加 PR。default / default_hs / default_sd の 3 つのキーマップと、ホットスワップ・ソルダーの 2 バリアントを含む。

**直近のアクティビティ:** zvecr が複数回 CHANGES_REQUESTED を出し、patipan36991 が応答。drashna からは APPROVED が付いているが、zvecr の最新レビューはまだ CHANGES_REQUESTED 状態。

**変更ファイル:** `keyboards/aiglatsonstudio/constance_tkl/hotswap/keyboard.json`, `keyboards/aiglatsonstudio/constance_tkl/hotswap/keymaps/default/keymap.c`, `keyboards/aiglatsonstudio/constance_tkl/info.json`, `keyboards/aiglatsonstudio/constance_tkl/readme.md`, `keyboards/aiglatsonstudio/constance_tkl/solder/keyboard.json`, `keyboards/aiglatsonstudio/constance_tkl/solder/keymaps/default/keymap.c`

**次のアクション:** zvecr の最新指摘を反映し、再レビュー依頼。

---

### ![PR](icons/git-pull-request.svg) [Add Electra 80 PCB](https://github.com/qmk/qmk_firmware/pull/25596) `#25596`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** PluskbID による Electra 80 PCB（pluskbd 配下、ホットスワップ/ソルダーの 2 バリアント）追加 PR。

**直近のアクティビティ:** zvecr から「2 つの PCB は対応レイアウトが異なるので別の ProductID を割り当てるべき」「基本レイアウトサポートに関する提案」と CHANGES_REQUESTED。drashna は APPROVED 済み。

**変更ファイル:** `keyboards/pluskbd/electra80/electra80.c`, `keyboards/pluskbd/electra80/hotswap/keyboard.json`, `keyboards/pluskbd/electra80/keymaps/default/keymap.c`, `keyboards/pluskbd/electra80/readme.md`, `keyboards/pluskbd/electra80/solder/keymap.json`

**次のアクション:** ProductID 分離とレイアウト追加対応を実施し、zvecr の最終承認を取得する。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Standard functions for analog keyboards](https://github.com/qmk/qmk_firmware/issues/25875) `#25875`
**ステータス:** Closed | **ラベル:** enhancement, help wanted, stale

**概要:** gibs2193 による、アナログキーボード（Hall Effect/TMR/誘導式など）向けの標準関数群（アクチュエーションポイント設定、ラピッドトリガー、SOCD、OKMC、カーブ調整、キャリブレーション）や ADC によるアナログマトリクス走査手段の存在確認 Issue。

**直近のアクティビティ:** 30 日間アクティビティがなかったため `github-actions[bot]` により stale クローズされた。

**次のアクション:** 当該機能が引き続き必要なら本 Issue を再オープン、もしくは設計提案として新規 Issue/Discussion を起票する。

---

### ![PR](icons/git-pull-request.svg) [[Keyboard] Add nomis/rpi_pico_25x1 macropad](https://github.com/qmk/qmk_firmware/pull/25346) `#25346`
**ステータス:** Merged | **ラベル:** keyboard, keymap, awaiting review

**概要:** nomis による Raspberry Pi Pico ベースの 25 キーマクロパッド（nomis/rpi_pico_25x1）追加 PR。default keymap と readme を同梱。

**直近のアクティビティ:** drashna、fauxpark から CHANGES_REQUESTED が出された後、nomis の対応を経て drashna と zvecr がそれぞれ APPROVED を付与し、本日マージ済み。

**変更ファイル:** `keyboards/nomis/rpi_pico_25x1/keyboard.json`, `keyboards/nomis/rpi_pico_25x1/keymaps/default/keymap.c`, `keyboards/nomis/rpi_pico_25x1/readme.md`

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add Minimi40 to QMK](https://github.com/qmk/qmk_firmware/pull/25575) `#25575`
**ステータス:** Merged | **ラベル:** keyboard, keymap, awaiting review, stale

**概要:** awkannan による cannonkeys/minimi40（ortho と staggered の 2 バージョン）追加 PR。フォルダ構成上単一 PR にまとめており、実機テスト済み。

**直近のアクティビティ:** zvecr の CHANGES_REQUESTED と awkannan のコメント往復を経て、drashna と zvecr の双方 APPROVED でマージ完了。

**変更ファイル:** `keyboards/cannonkeys/minimi40/ortho/keyboard.json`, `keyboards/cannonkeys/minimi40/ortho/keymaps/default/keymap.c`, `keyboards/cannonkeys/minimi40/ortho/readme.md`, `keyboards/cannonkeys/minimi40/ortho/rules.mk`, `keyboards/cannonkeys/minimi40/readme.md`, `keyboards/cannonkeys/minimi40/staggered/config.h`, `keyboards/cannonkeys/minimi40/staggered/keyboard.json`, `keyboards/cannonkeys/minimi40/staggered/keymaps/default/keymap.c`, `keyboards/cannonkeys/minimi40/staggered/readme.md`

**次のアクション:** マージ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [[Bug] Mouse key MS_BTN1 ~ MS_BTN8 are not working when PS/2 trackpoint enabled with RP2040 MCU](https://github.com/qmk/qmk_firmware/issues/24599) `#24599`
**ステータス:** Closed | **ラベル:** bug, help wanted

**概要:** tachikomachann による、RP2040（Raspberry Pi Pico）で PS/2 トラックポイントを有効化すると `MS_BTN1`〜`MS_BTN8`（マウスボタン）のキーコードだけが効かなくなる、というバグ報告。`MS_WHLU`/`MS_WHLD` などホイール系は正常で、PS/2 ドライバを無効化するとマウスボタンが復活する。RP2040 の PS/2 vendor ドライバとマウスキー機能との競合が疑われる。

**直近のアクティビティ:** PR #25942 でのマウスクリック消失修正に伴い、本 Issue は Close された。

**次のアクション:** #25942 のマージ後、RP2040 実機で MS_BTN1〜MS_BTN8 の再現テストを実施し、解消を確認する。

---

### ![Issue](icons/issue-opened.svg) [[Bug] Mouse Button presses KC_BTN* only work when mouse cursor is moving](https://github.com/qmk/qmk_firmware/issues/24455) `#24455`
**ステータス:** Closed | **ラベル:** bug, help wanted

**概要:** btittelbach による、マウスカーソル静止時（トラックポイント未操作時）に `KC_BTN*` 押下が効かず、カーソルが動いている間だけマウスボタンが認識されるバグ。コミット `a850f7d69509a99ef46dd2790c2e55171db16e7a` を境にして発生したと特定済みで、RP2040 + ENCODER_MAP/RGBLIGHT/COMBO 構成で再現する。

**直近のアクティビティ:** PR #25942 によるマウスクリック消失修正を経て本 Issue も Close。

**次のアクション:** #25942 マージ後の動作確認と、再発時の追跡用に blame 対象コミット周辺の回帰テスト追加検討。

---

### ![PR](icons/git-pull-request.svg) [fix(input): Resolve lost mouse clicks in PS/2 streaming mode](https://github.com/qmk/qmk_firmware/pull/25942) `#25942`
**ステータス:** Merged | **ラベル:** bug, core

**概要:** klesh による PS/2 ストリーミングモードでマウスクリックが取りこぼされる問題の修正 PR。Issue #24455 と #24599 を解決し、`drivers/ps2/ps2_mouse.c` のみを変更する小さなコア修正。

**直近のアクティビティ:** drashna と zvecr の双方から APPROVED を取得し、関連 Issue の Close と合わせて本日マージ完了。

**変更ファイル:** `drivers/ps2/ps2_mouse.c`

**次のアクション:** マージ済みのため対応不要。回帰テストの追加可否を検討する余地あり。

---

### ![PR](icons/git-pull-request.svg) [Candy87 PCB Firmware](https://github.com/qmk/qmk_firmware/pull/24298) `#24298`
**ステータス:** Merged | **ラベル:** keyboard, keymap, awaiting review

**概要:** rarepotato8de による Candykeys 製 Candy87 PCB（Geonworks F1-8X V2 向け）のファームウェア追加 PR。ANSI / ISO それぞれの f12・f13・tsangan バリアントを含む計 8 キーマップを同梱。

**直近のアクティビティ:** zvecr・dunk2k から CHANGES_REQUESTED、waffle87 から「キーマップに RGB Matrix キーコードと `QK_BOOT` を追加し、キーコードは短縮エイリアスを使うように」と詳細指摘。最終的に drashna と zvecr が APPROVED を付与してマージ済み。

**変更ファイル:** `keyboards/candykeys/candy87/candy87.c`, `keyboards/candykeys/candy87/keyboard.json`, `keyboards/candykeys/candy87/keymaps/default/keymap.c`, `keyboards/candykeys/candy87/keymaps/f12_ansi/keymap.c`, `keyboards/candykeys/candy87/keymaps/f12_ansi_tsangan/keymap.c`, `keyboards/candykeys/candy87/keymaps/f12_iso/keymap.c`, `keyboards/candykeys/candy87/keymaps/f12_iso_tsangan/keymap.c`, `keyboards/candykeys/candy87/keymaps/f13_ansi/keymap.c`, `keyboards/candykeys/candy87/keymaps/f13_ansi_tsangan/keymap.c`, `keyboards/candykeys/candy87/keymaps/f13_iso/keymap.c`, `keyboards/candykeys/candy87/keymaps/f13_iso_tsangan/keymap.c`, `keyboards/candykeys/candy87/readme.md`

**次のアクション:** マージ済みのため対応不要。

---
