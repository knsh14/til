# GitHub ダイジェスト — 2026-06-03

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[feat(terminal): replace `libvterm` with `libghostty-vt`](https://github.com/neovim/neovim/pull/39773)** (neovim/neovim) — 組み込みターミナルの基盤ライブラリを libvterm から libghostty-vt へ全面移行する大型 PR（+2917/-13086、74 ファイル）。各コミットで動作を保ちながら段階的に移植しており、Neovim と Ghostty 双方のエコシステムに影響する。
2. ![Issue](icons/issue-opened.svg) **[tool_search returns non-deterministic, incomplete results for MCP server tools](https://github.com/microsoft/vscode/issues/317998)** (microsoft/vscode) — Copilot Chat の `tool_search` が MCP ツールの一部しか返さず結果が毎回変わるという報告。GPT 系モデルで顕著で Claude では問題ないという複数ユーザーの追随コメントがあり、優先対応を求める声が集まっている。
3. ![Issue](icons/issue-opened.svg) **[BYOK should not invoke Copilot subagents](https://github.com/microsoft/vscode/issues/319650)** (microsoft/vscode) — BYOK 利用中にもかかわらずサブエージェント呼び出しで Copilot クレジットが課金された問題。課金回避策やプロファイル化、ユーティリティモデルの「None」オプション追加などが議論されている。
4. ![PR](icons/git-pull-request.svg) **[Create complementary dependencies for extra-only sources](https://github.com/astral-sh/uv/pull/18272)** (astral-sh/uv) — `[tool.uv.sources]` の `extra = "foo"` をその extra が有効なときだけ適用するよう補完的依存を生成する設計変更。resolver の大きな書き換えを伴い、レビューでロジックの追従しづらさが指摘されている。
5. ![PR](icons/git-pull-request.svg) **[安全性に関わる車種ポート群](https://github.com/commaai/opendbc/pull/3424)** (commaai/opendbc) — Volkswagen MEB プラットフォーム（ID.4）の縦・横制御ポートや Ford のネイティブ curvature 化など、安全性ラベル付きの車種対応 PR が活発に動いている。

---

## sunnypilot/sunnypilot

> commaai/openpilot との大型同期 PR が進行中で、locationd の livePose 初期化バグや HKG の操舵トルク動的化 Issue が動いている。

### ![Issue](icons/issue-opened.svg) [hyundai: Infer Maximum Steering Torque Dynamically](https://github.com/sunnypilot/sunnypilot/issues/1052) `#1052`
**ステータス:** Open | **ラベル:** enhancement, car enhancement

**概要:** HKG 車両の操舵トルク上限を 384/255/270 のような固定クラス分けではなく、3.0 m/s^2 の最大横加速度を満たしつつ車種ごとに動的調整すべきという提案。車種ごとに性能差が大きく現状の一律設定は最適でないとする。

**直近のアクティビティ:** @holux-design が進捗を尋ね、より複雑なモデルへ進むうえでデバイスごとのトルク対応を強く希望するとコメント。

**次のアクション:** 動的トルク制御の設計方針の検討。

---

### ![Issue](icons/issue-opened.svg) [locationd can publish valid livePose before pose filter time is initialized](https://github.com/sunnypilot/sunnypilot/issues/1850) `#1850`
**ステータス:** Closed | **ラベル:** bug

**概要:** 起動直後に車が動き出すと、pose フィルタ時刻が NaN のまま有効な livePose が publish され、`paramsd` が timestamp=0 を採用してフィルタ時刻が大きく飛び、`stiffnessFactor` が破損しうるという不具合。

**直近のアクティビティ:** 報告者 @zappybiby が sunnypilot 固有ではないとして自らクローズ（openpilot 側 PR #38124 で対応）。

**次のアクション:** upstream（openpilot）側の修正追跡。

---

### ![PR](icons/git-pull-request.svg) [Sync: `commaai/openpilot:master` → `sunnypilot/sunnypilot:master`](https://github.com/sunnypilot/sunnypilot/pull/1843) `#1843`
**ステータス:** Open（Draft）

**概要:** upstream の commaai/openpilot を sunnypilot へ取り込む定例同期 PR（+5924/-91177、628 ファイル）。

**直近のアクティビティ:** UI プレビュー bot が mici/big 両方で動画差分の発生を報告。

**変更ファイル:** 628 ファイルにわたる大規模同期。

**次のアクション:** UI 差分の確認とマージ判断。

---

## commaai/openpilot

> 0.11 系の準備が進む中、modeld（モデル実行系）のリファクタや AGNOS 更新、webrtc/teleop 周りの修正がマージされている。

### ![PR](icons/git-pull-request.svg) [test models: add VW MEB angle exception](https://github.com/commaai/openpilot/pull/38119) `#38119`
**ステータス:** Merged | **ラベル:** CI / testing, car

**概要:** VW の curvature ベース MEB プラットフォーム向けに、angle 制御を使わないことを示す例外をモデルテストへ追加。

**直近のアクティビティ:** Process replay diff bot が 66 件パス・変化なしを報告。@sshane と @elkoled のレビューコメントあり。

**変更ファイル:** `selfdrive/car/tests/test_models.py`（+3/-2）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Op model16 deep](https://github.com/commaai/openpilot/pull/38073) `#38073`
**ステータス:** Open

**概要:** 新しい driving モデル（model16 系）を modeld へ組み込む変更。本文は「WORKS NOW」とのみ記載。

**直近のアクティビティ:** モデルリプレイ bot が差分プロットと比較結果を提示し、Process replay は 66 件パス・変化なし。

**変更ファイル:** `selfdrive/modeld/` 配下の各 onnx モデルと modeld.py など（13 ファイル）。

**次のアクション:** モデル差分レビューと統合判断。

---

### ![PR](icons/git-pull-request.svg) [webrtc: initialize on medium bitrate](https://github.com/commaai/openpilot/pull/38129) `#38129`
**ステータス:** Merged

**概要:** webrtc ライブストリームのビットレート制御を medium で初期化し、接続品質に応じて素早く上下できるようにする。

**直近のアクティビティ:** Process replay diff bot が変化なしを報告。

**変更ファイル:** `system/webrtc/webrtcd.py`（+2/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix: webjoystick connection from firefox](https://github.com/commaai/openpilot/pull/38130) `#38130`
**ステータス:** Open | **ラベル:** tools

**概要:** Firefox での mDNS 問題を解消するため STUN サーバを追加し、カメラを wideRoad に切り替える webjoystick の修正。

**直近のアクティビティ:** Process replay diff bot が変化なしを報告。

**変更ファイル:** `tools/bodyteleop/` 配下の controls.js / webrtc.js / web.py（3 ファイル）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Refactor compile_modeld model setup](https://github.com/commaai/openpilot/pull/38128) `#38128`
**ステータス:** Merged

**概要:** `compile_modeld.py` のモデルセットアップ処理をリファクタ。

**直近のアクティビティ:** モデルリプレイ bot のプロットと Process replay diff（変化なし）を報告。

**変更ファイル:** `selfdrive/modeld/compile_modeld.py`（+42/-29）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [A valid livePose should not carry a timestamp produced from uninitialized Kalman filter time](https://github.com/commaai/openpilot/pull/38124) `#38124`
**ステータス:** Open

**概要:** `self.kf.t` が有限になるまで `livePose.valid=false` を維持し、未初期化フィルタ時刻由来の timestamp=0 が下流（paramsd）で観測時刻として扱われる問題を防ぐ。sunnypilot#1850 と同根。

**直近のアクティビティ:** @zappybiby が、単独走行では再現せず paramsd がアクティブで timestamp=0 が先に処理される場合のみ顕在化するとミリ秒単位の競合を説明。

**変更ファイル:** `selfdrive/locationd/locationd.py`（+2/-0）。

**次のアクション:** ガード方針のレビュー。

---

### ![PR](icons/git-pull-request.svg) [Frame drop: only allow 1percent](https://github.com/commaai/openpilot/pull/38127) `#38127`
**ステータス:** Merged

**概要:** フレームドロップの許容を 1% のみに制限する変更。

**直近のアクティビティ:** @adeebshihadeh が現状で何 % のセグメントが該当するか質問。Process replay は変化なし。

**変更ファイル:** `selfdrive/selfdrived/selfdrived.py`（+1/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [agnos 18.4: pin pycapnp to 2.1.0](https://github.com/commaai/openpilot/pull/38126) `#38126`
**ステータス:** Merged

**概要:** AGNOS 18.4 で pycapnp を 2.1.0 に固定。Issue #38114（高メモリ使用）を修正。

**直近のアクティビティ:** モデルリプレイ bot のプロットを報告。

**変更ファイル:** `system/hardware/tici/agnos.json`、`uv.lock`、modeld.py など（8 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [high memory usage](https://github.com/commaai/openpilot/issues/38114) `#38114`
**ステータス:** Closed | **ラベル:** bug

**概要:** release-staging で高メモリ使用エラーが発生し、ルート末尾で disengage する報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #38126 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

## commaai/opendbc

> Volkswagen MEB（ID.4）や Ford のネイティブ curvature 化など、安全性に関わる車種ポートが活発。HKG の各種フィンガープリント/ポートも継続中。

### ![PR](icons/git-pull-request.svg) [Ford native curvature](https://github.com/commaai/opendbc/pull/3405) `#3405`
**ステータス:** Open | **ラベル:** car, ford, car safety

**概要:** Ford を汎用の `apply_std_curvature_limits` へ移行し、Tesla の angle VM safety と同様に横加速度・横ジャーク空間で制御。手選びの 3 つの curvature レート閾値を `MAX_LATERAL_ACCEL = 3.6 m/s^2` と `MAX_LATERAL_JERK = 3.6 m/s^3` の 2 物理定数に置換する。

**直近のアクティビティ:** Car behavior report bot が Ford 10 車種・91 セグメントで変化なしを報告。@sshane と @elkoled がレビューでやり取り。

**変更ファイル:** `opendbc/safety/lateral.h`、`opendbc/car/ford/carcontroller.py`、`opendbc/car/lateral.py` など（12 ファイル）。

**次のアクション:** safety レビューの継続。

---

### ![PR](icons/git-pull-request.svg) [VW: swap specific distance value checks for truthiness checks](https://github.com/commaai/opendbc/pull/3072) `#3072`
**ステータス:** Open | **ラベル:** car, volkswagen

**概要:** VW の車間距離ボタンが車種ごとに 1/2/3 の値を不規則に送る問題に対し、openpilot は +1/-1/toggle を区別しないため真偽値で判定する形に簡素化する。

**直近のアクティビティ:** @jyoung8607 が「正しい方向だが自分の車のログで確認したい」とコメント。

**変更ファイル:** `opendbc/car/volkswagen/values.py`（+3/-3）。

**次のアクション:** 実車ログでの検証。

---

### ![PR](icons/git-pull-request.svg) [VW MQB: work around standstill limit](https://github.com/commaai/opendbc/pull/2764) `#2764`
**ステータス:** Open | **ラベル:** DBC signals, car, volkswagen

**概要:** EPB のない MQB 車が停止保持で ESP が長くブレーキを保持するとフォルトする問題を回避し、EPB なしで full stop-n-go を実現する。openpilot#37800 と opendbc#3322 に依存。

**直近のアクティビティ:** @RJWadley が、検証用に 1 つの大きな PR にまとめており方針が証明できたので分割も可能とコメント。

**変更ファイル:** `opendbc/car/volkswagen/carcontroller.py`、`tests/test_volkswagen.py`、`vw_mqb.dbc` など（7 ファイル）。

**次のアクション:** PR 分割の検討とレビュー。

---

### ![PR](icons/git-pull-request.svg) [car: add max planned speed actuator field](https://github.com/commaai/opendbc/pull/3322) `#3322`
**ステータス:** Open | **ラベル:** car

**概要:** ロールバック保護がなくブレーキジャークも限られる MQB 向けに、走り出す計画があるかを判定して停止からの安全な発進可否を決めるためのフィールドを追加。#2764 の前提となる。

**直近のアクティビティ:** @RJWadley が VW の alpha long では `pcmCruise` 等が常に False になる挙動をコード断片付きで説明。

**変更ファイル:** `opendbc/car/car.capnp`（+1/-0）。

**次のアクション:** レビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [[$10k bounty] Ford F-150 2026 (TRON) support](https://github.com/commaai/opendbc/issues/3426) `#3426`
**ステータス:** Open | **ラベル:** ford, car port, bounty

**概要:** 新型 F-150（TRON プラットフォーム）対応の $10k バウンティ。ハーネス設計、高品質な横制御ポート、縦制御の概念実証が要件で、通常体験で $10k、限定的サポートで $2k。

**直近のアクティビティ:** @0xjc65eth がソフトウェア検証側を担えるとして、CAN FD/TRON のギャップ調査や再現可能な検証ハーネス構築を申し出。

**次のアクション:** 担当者の調整と着手。

---

### ![PR](icons/git-pull-request.svg) [safety: enable 100% branch coverage check](https://github.com/commaai/opendbc/pull/3428) `#3428`
**ステータス:** Closed | **ラベル:** car safety

**概要:** `gcovr` に `--fail-under-branch=100` を追加し、production safety C コードに 100% ブランチカバレッジを要求（#2557 をクローズ）。`safety_interpolate()` の冗長クランプ削除など未到達分岐も整理。

**直近のアクティビティ:** contributing bot がドラフト化やドキュメント確認を促すコメント。

**変更ファイル:** `opendbc/safety/tests/test.sh`、`helpers.h`、`modes/body.h` など（5 ファイル）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3430) `#3430`
**ステータス:** Merged | **ラベル:** can, bot

**概要:** uv.lock 依存関係の週次自動更新。

**直近のアクティビティ:** Car behavior report bot が変化なしを報告。

**変更ファイル:** `uv.lock`、`opendbc/can/parser.py`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/commaai/opendbc/pull/1575) `#1575`
**ステータス:** Open | **ラベル:** car, hyundai, stale, car safety

**概要:** HDA II 非搭載の ccNC 装備車（Kona、Santa Cruz、Sonata、K4/K5、Sorento など）への対応ポート。

**直近のアクティビティ:** @sshane が CHANGES_REQUESTED をレビュー登録。

**変更ファイル:** `opendbc/car/hyundai/` 配下の各ファイル（12 ファイル）。

**次のアクション:** 指摘対応とレビュー再依頼。

---

### ![PR](icons/git-pull-request.svg) [HKG: Hyundai Santa Fe 2022 Fingerprint](https://github.com/commaai/opendbc/pull/3354) `#3354`
**ステータス:** Closed | **ラベル:** car, hyundai, fingerprint

**概要:** Hyundai Santa Fe 2022 のフィンガープリントを追加。

**直近のアクティビティ:** contributing bot のチェックリスト案内。

**変更ファイル:** `opendbc/car/hyundai/fingerprints.py`（+1/-0）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Honda - clear startup Bosch faults](https://github.com/commaai/opendbc/pull/3029) `#3029`
**ステータス:** Open | **ラベル:** car, honda, stale

**概要:** Bosch 車が起動直後に前回ドライブのフォルトを保持する問題に対し、最初の 10 秒間に出るフォルトでは cruise enable を無効化してクリアを待つ。

**直近のアクティビティ:** Car behavior report bot が Honda 多数車種・154 セグメントを検査。

**変更ファイル:** `opendbc/car/honda/carstate.py`（+16/-1）。

**次のアクション:** レビューと検証。

---

### ![PR](icons/git-pull-request.svg) [Volkswagen ID.4 longitudinal](https://github.com/commaai/opendbc/pull/3424) `#3424`
**ステータス:** Open | **ラベル:** DBC signals, car, ford, volkswagen, car safety

**概要:** VW MEB プラットフォーム初の縦制御ポート。AEB 無効化を避けるため当面ゲートウェイハーネスのみ対応し、EA エスカレーション検証まで Dashcam モードに設定。MEB EPS は curvature target を使う。

**直近のアクティビティ:** Car behavior report bot が Ford/VW 多数車種・221 セグメントを検査。

**変更ファイル:** `opendbc/car/volkswagen/carcontroller.py`/`carstate.py`、`docs/CARS.md` など（27 ファイル）。

**次のアクション:** EA エスカレーション検証とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Volkswagen ID.4 lateral](https://github.com/commaai/opendbc/pull/3366) `#3366`
**ステータス:** Open | **ラベル:** DBC signals, car, ford, volkswagen, car safety

**概要:** VW MEB プラットフォーム初の横制御ポート。縦制御 PR #3424 と共通の MEB curvature target ヘルパーを使う。

**直近のアクティビティ:** @sshane が「横制御だけ先にマージし縦は別 PR に」と提案、diff が大きいため分割を希望。@elkoled とやり取り。

**変更ファイル:** `opendbc/car/volkswagen/` 配下と `docs/CARS.md` など（26 ファイル）。

**次のアクション:** 横/縦の分割とリリース投入の方針確認。

---

### ![PR](icons/git-pull-request.svg) [GMC Yukon Update values.py](https://github.com/commaai/opendbc/pull/3124) `#3124`
**ステータス:** Open | **ラベル:** car, gm, stale

**概要:** 2020 Yukon で約 10 か月・6680 マイル試用した wheelbase と tirestiffnessFactor の調整。

**直近のアクティビティ:** Car behavior report bot が GMC 20 セグメントで 10 件の差分（centerToFront 等）を検出。

**変更ファイル:** `opendbc/car/gm/values.py`（+1/-1）。

**次のアクション:** 差分の妥当性確認。

---

## ghostty-org/ghostty

> macOS のアクセシビリティや選択状態の修正、libghostty-vt のカーソル設定追加、glyf ラスタライザの新規実装が動いている。

### ![Issue](icons/issue-opened.svg) [Feature: Support for tmux's Control Mode](https://github.com/ghostty-org/ghostty/issues/1935) `#1935`
**ステータス:** Open | **ラベル:** vt, gui

**概要:** tmux の Control Mode をサポートし、端末エミュレータが tmux のペインを管理できるようにする要望。ネイティブ統合により OS との連携やバッファ管理が容易になる。

**直近のアクティビティ:** @bbkane が iTerm や WezTerm の挙動を引き合いに出してコメント。

**次のアクション:** 設計検討。

---

### ![PR](icons/git-pull-request.svg) [core: send selection_changed notification](https://github.com/ghostty-org/ghostty/pull/12902) `#12902`
**ステータス:** Open | **ラベル:** os/macos, a11y

**概要:** 選択状態が変化したことを apprt へ通知する仕組みがなく、スクリーンリーダーが古い選択を読み続ける問題に対処。payload なしの `selection_changed` アクションを追加し、全経路を `setSelection` 経由に統一する。

**直近のアクティビティ:** @jcollie が LGTM で APPROVED。

**変更ファイル:** `src/Surface.zig`、`macos/Sources/Ghostty/` 配下、`src/apprt/` など（7 ファイル）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [implement +new-window IPC via Unix domain socket for macOS](https://github.com/ghostty-org/ghostty/pull/12903) `#12903`
**ステータス:** Closed

**概要:** GTK の D-Bus 実装と同様の `ghostty +new-window` を macOS で Unix ドメインソケット経由で実装する。

**直近のアクティビティ:** ghostty-vouch bot が、作者が vouch リストに未登録のため自動クローズすると通知。

**変更ファイル:** `macos/Sources/Features/IPC/IPCServer.swift`、`src/apprt/embedded/ipc/new_window.zig` など（7 ファイル）。

**次のアクション:** 作者の vouch 取得後の再提出。

---

### ![PR](icons/git-pull-request.svg) [libghostty-vt: add options to configure default cursor's style and blink](https://github.com/ghostty-org/ghostty/pull/12900) `#12900`
**ステータス:** Open

**概要:** `libghostty-vt` にデフォルトカーソルのスタイルと点滅を設定する 2 オプションを追加。プログラムが明示指定しない場合や DECSCUSR リセット（`CSI 0 q`）時の描画を制御する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `include/ghostty/vt/terminal.h`、`src/terminal/c/terminal.zig` など（3 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [core: fix use-after-free in Surface.setSelection](https://github.com/ghostty-org/ghostty/pull/12894) `#12894`
**ステータス:** Merged

**概要:** `setSelection` が前回選択を保持したまま `Screen.select`（前回選択の pin を deinit）を呼び、解放済み pin と比較していた use-after-free を修正。実際には常に常時コピー動作になっていた最適化を、修復ではなく削除する。

**直近のアクティビティ:** @mitchellh が APPROVED。

**変更ファイル:** `src/Surface.zig`、`src/terminal/Screen.zig`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [font: glyf outline decoder and rasterizer](https://github.com/ghostty-org/ghostty/pull/12893) `#12893`
**ステータス:** Open | **ラベル:** font

**概要:** Glyph Protocol 向けに glyf アウトラインのデコーダとラスタライザを新規実装。FreeType/CoreText の生 glyf ラスタライズ API が貧弱なため自前実装した（パーサ・ラスタライザは手書き、AI は検証とテスト作成を補助）。

**直近のアクティビティ:** @raphamorim がレビューコメント。

**変更ファイル:** `src/font/glyf_rasterize.zig`（+836）、`src/font/opentype/glyf.zig`（+446）など（5 ファイル）。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [macos: fix GHOSTTY_QUICK_TERMINAL not set for quick terminal splits](https://github.com/ghostty-org/ghostty/pull/12896) `#12896`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS で Quick Terminal の分割ペイン（Cmd+D）に `GHOSTTY_QUICK_TERMINAL=1` が継承されない問題（#12895）を修正。GTK は per-window で動的取得するが macOS は最初の surface にしか書き込まないのが原因。

**直近のアクティビティ:** @52dyd と @bo2themax が、ペインのドラッグアウトや再起動時の既知の制限事項を本 PR のスコープ外として整理。@bo2themax が APPROVED。

**変更ファイル:** `macos/Sources/Features/QuickTerminal/QuickTerminalController.swift`（+10/-0）。

**次のアクション:** マージ判断。

---

### ![Issue](icons/issue-opened.svg) [MacOS: GHOSTTY_QUICK_TERMINAL not inherited by split panes in Quick Terminal](https://github.com/ghostty-org/ghostty/issues/12895) `#12895`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS で `new_split`（Cmd+D）作成の分割ペインに `GHOSTTY_QUICK_TERMINAL=1` が継承されない macOS 固有のバグ。GTK/Linux では影響なしと確認されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #12896 で対応中）。

**次のアクション:** PR #12896 のマージ。

---

## astral-sh/uv

> Python リリースの同期や resolver の extra/group スコープ対応、show_settings テストの大幅整理、CI のリンカ最適化（LLD/ICF）など内部整備が活発。

### ![PR](icons/git-pull-request.svg) [chore: fix some minor issues in comments](https://github.com/astral-sh/uv/pull/19656) `#19656`
**ステータス:** Open

**概要:** コード内コメントの軽微な誤りを修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-build-frontend/src/lib.rs`、`crates/uv-globfilter/src/glob_dir_filter.rs`（2 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/19531) `#19531`
**ステータス:** Open

**概要:** Python リリースの自動更新 PR。

**直近のアクティビティ:** @jjhelmus が riscv64 で sysconfig 置換が gcc から clang へ変わった（LLVM ツールチェーン採用）が既存リリースは gcc 置換が必要として、翌日に修正を作ると報告。

**変更ファイル:** `crates/uv-python/download-metadata.json`（+2062/-838）、`generated_mappings.rs` など（4 ファイル）。

**次のアクション:** riscv64 sysconfig の修正対応。

---

### ![PR](icons/git-pull-request.svg) [Create complementary dependencies for extra-only sources](https://github.com/astral-sh/uv/pull/18272) `#18272`
**ステータス:** Open（Draft）| **ラベル:** bug

**概要:** `[tool.uv.sources]` の `extra = "foo"` をその extra が有効なときだけ適用し、`extra != "foo"` 向けにソース未適用の補完的依存を生成する。#17967, #18229 をクローズ予定。

**直近のアクティビティ:** codspeed bot が性能変化なしを報告、astral-sh-bot がテスト 35 件追加を報告。@konstin が「ロジックを追いづらく、レビューというより理解のための質問」とコメント。

**変更ファイル:** `crates/uv-resolver/src/resolver/dependency_builder.rs`（+825）、`pubgrub/dependencies.rs` など（20 ファイル）。

**次のアクション:** ロジック整理とレビュー継続。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate の依存更新と検出済み依存を一覧化する自動 Issue。長期間更新のない放置依存（35 件）も列挙。

**直近のアクティビティ:** 直近 24 時間のコメントなし（bot 更新）。

**次のアクション:** なし（自動管理）。

---

### ![PR](icons/git-pull-request.svg) [fix: prevent activate.fish from changing cwd on relocatable venvs](https://github.com/astral-sh/uv/pull/19632) `#19632`
**ステータス:** Open

**概要:** relocatable venv を fish で source するとカレントディレクトリが venv ディレクトリへ変わる問題（#19631）を修正。fish はコマンド置換をサブシェルなしで評価するため cwd が漏れていた。bash アクティベータと同じ手法に置換。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-virtualenv/src/virtualenv.rs`（+1/-1）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Race condition in uv build --all-packages / maturin / zig](https://github.com/astral-sh/uv/issues/11444) `#11444`
**ステータス:** Closed | **ラベル:** external

**概要:** `uv build --all-packages`（maturin + zig）で、ビルド進行中にビルド環境が早期破棄され `maturin: not found` になる散発的な CI 不具合。

**直近のアクティビティ:** @sharifhsn が cargo-zigbuild#421（0.22.2、maturin 1.13.2 経由で uv に到達）で upstream 修正済みと報告し再現手順を提示。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Remove unused `pub` code from workspace](https://github.com/astral-sh/uv/pull/19565) `#19565`
**ステータス:** Open | **ラベル:** internal, build:release, build:docker

**概要:** hawk ツールでデッドコード（他クレートから未使用の `pub`）削除と可視性低減を行う。例外は `hawk.toml` に追加（自動化前のため未コミット）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** uv-auth/uv-build-backend など多数クレート（146 ファイル、+638/-1287）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Set `TY_UV=1` when invoking `ty` through `uv check`](https://github.com/astral-sh/uv/pull/19655) `#19655`
**ステータス:** Open | **ラベル:** internal

**概要:** ty が uv を呼び出す実験的サポートの opt-in として、`uv check` 経由の `ty` 起動時に `TY_UV=1` を設定する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv/src/commands/project/check/ty.rs`（+2/-0）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Validate locked versions satisfy requirement specifiers](https://github.com/astral-sh/uv/pull/18566) `#18566`
**ステータス:** Open（Draft）

**概要:** ロックされたバージョンが requirement specifier を満たすか検証する（#12276 関連のテスト追加）。

**直近のアクティビティ:** @themavik が、検証が `requirements`/`constraints` のみで `overrides`/`build_constraints` をスキップしており、`override-dependencies` で不整合があってもチェックを通過してしまうと指摘。

**変更ファイル:** `crates/uv-resolver/src/lock/mod.rs`、`crates/uv/tests/it/lock.rs`（+1041）など（3 ファイル）。

**次のアクション:** overrides 対応の検討。

---

### ![PR](icons/git-pull-request.svg) [Speedup discovery of large workspaces](https://github.com/astral-sh/uv/pull/18311) `#18311`
**ステータス:** Open（Draft）| **ラベル:** performance

**概要:** ワークスペースルートとメンバの `pyproject.toml` を重複読込しないようキャッシュし、airflow で `uv run python -V` が約 1.9 倍高速になる。

**直近のアクティビティ:** @EliteTK が `update_project` 周りに確信が持てず再確認したいとコメント。@Gankra が実装の保守性を懸念。

**変更ファイル:** `crates/uv-workspace/src/workspace.rs`（+613/-184）など（19 ファイル）。

**次のアクション:** 実装の保守性レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add Pyodide 2025 target triple](https://github.com/astral-sh/uv/pull/19653) `#19653`
**ステータス:** Merged

**概要:** Pyodide 2025 のターゲットトリプルを認識できるよう追加。

**直近のアクティビティ:** @Viicos が PEP 783 プラットフォームタグとの整合を質問し、@hoodmane が参照リンクを追加。@zanieb が APPROVED。

**変更ファイル:** `crates/uv-configuration/src/target_triple.rs`（+22/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [[RFC] Improve cache-keys interactions with deployments](https://github.com/astral-sh/uv/pull/19642) `#19642`
**ステータス:** Open

**概要:** 開発者向けとデプロイ向けでキャッシュ管理要件が衝突する問題に対処する RFC。ファイルの ctime 利用が新規クローン時に強制再ビルドを招く点を改善する。

**直近のアクティビティ:** @nhukc が dependency-group での cache-keys 設定例を提示し、#9191/#9263 のユーザーストーリー改善につながるとコメント。

**変更ファイル:** `crates/uv-cache-info/src/cache_info.rs`（+279/-35）など（37 ファイル）。

**次のアクション:** RFC のレビュー。

---

### ![PR](icons/git-pull-request.svg) [Remove a bunch of duplication in `show_settings` tests by using diffs](https://github.com/astral-sh/uv/pull/19654) `#19654`
**ステータス:** Open | **ラベル:** internal

**概要:** 巨大な settings ダンプを毎回スナップショットする `show_settings` テストの冗長性を、連続スナップショット間の diff で表現して削減する（+2442/-4749）。

**直近のアクティビティ:** @EliteTK と @zanieb が指摘対応と、キー指定 vs diff のアプローチを議論。

**変更ファイル:** `crates/uv/tests/it/show_settings.rs`（+2408/-4749）、`crates/uv-test/src/lib.rs`（2 ファイル）。

**次のアクション:** レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [Add `assert_diff_snapshot` test helper](https://github.com/astral-sh/uv/pull/19651) `#19651`
**ステータス:** Closed | **ラベル:** internal

**概要:** diff スナップショット用のテストヘルパーを追加する基盤的変更。

**直近のアクティビティ:** @EliteTK が「単体では有用でない」として #19654 の独立変更を優先しクローズ。@zanieb とやり取り。

**変更ファイル:** `crates/uv-test/src/lib.rs`、`crates/uv/tests/it/workflow.rs`（2 ファイル）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(#16963): Add a hint when requires-python contains a free-threaded selector](https://github.com/astral-sh/uv/pull/19647) `#19647`
**ステータス:** Open

**概要:** `requires-python = ">=3.13t"` のような free-threaded 表記（PEP 440 非準拠）で TOML パースが暗黙失敗する問題（#16963）に対し、parse エラー前に末尾 t を検出して具体的ヒントを返す。

**直近のアクティビティ:** Copilot reviewer bot がエラー報告/UX 改善の概要をコメント。

**変更ファイル:** `crates/uv-workspace/src/pyproject.rs`（+354）など（3 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Make `run_with_vars` drop more environment variables](https://github.com/astral-sh/uv/pull/19650) `#19650`
**ステータス:** Merged | **ラベル:** internal

**概要:** Python discovery テストの hermetic 化で conda 固有の環境変数を取りこぼしていた問題を、`EnvVars::all_names` で uv が関知する全変数を eager に drop して解消。

**直近のアクティビティ:** @zanie-oai と @EliteTK が変更点の明確化をやり取り。@zanieb が APPROVED。

**変更ファイル:** `crates/uv-python/src/lib.rs`（+8/-9）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Respect extra- and group-scoped sources for production dependencies](https://github.com/astral-sh/uv/pull/19652) `#19652`
**ステータス:** Open（Draft）

**概要:** production 依存に対して extra/group スコープのソースを尊重するようにする（#18272 の続き）。

**直近のアクティビティ:** astral-sh-bot がテスト 18 件追加を報告。

**変更ファイル:** `crates/uv-distribution/src/metadata/lowering.rs`、`pubgrub/dependencies.rs` など（15 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Use bundled LLD instead of installing mold in CI](https://github.com/astral-sh/uv/pull/19648) `#19648`
**ステータス:** Open（Draft）

**概要:** Rust 1.90 が x86_64 GNU でバンドル LLD を既定化したため mold インストールが不要になり、aarch64 GNU と x86_64 musl のみ rustc 同梱の LLD ラッパへ振り分ける。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/build-dev-binaries.yml`、`scripts/install-mold.sh`（削除）など（3 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Use global preview in `CondaEnvironmentKind::from_prefix_path`](https://github.com/astral-sh/uv/pull/19649) `#19649`
**ステータス:** Open | **ラベル:** internal

**概要:** 影響範囲の広い Python discovery 領域で global preview の利用を拡大する。uv-dev での既定 global preview セットアップも追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-python/src/discovery.rs`、`installation.rs` など（44 ファイル）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Reject `git+file` paths in requirement definitions](https://github.com/astral-sh/uv/issues/11425) `#11425`
**ステータス:** Closed | **ラベル:** error messages

**概要:** Windows の `git+C:\path\to\repo` のようなパス指定が分かりづらいエラーになる問題。

**直近のアクティビティ:** @sharifhsn が #11514 でクローズ済みと確認し再現手順とエラー出力を提示。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Remove miette](https://github.com/astral-sh/uv/pull/17110) `#17110`
**ステータス:** Open（Draft）| **ラベル:** no-build

**概要:** 依存から miette を除去するリファクタ（+2895/-2700）。

**直近のアクティビティ:** codspeed bot が性能変化なしを報告。@zanieb が複数のレビューコメント。

**変更ファイル:** `crates/uv-client/src/error.rs`、`registry_client.rs` など（70 ファイル）。

**次のアクション:** レビュー継続。

---

### ![Issue](icons/issue-opened.svg) [Inconsistent `uv pip install` behavior with workspace member that shadows pypi package](https://github.com/astral-sh/uv/issues/11005) `#11005`
**ステータス:** Open | **ラベル:** bug

**概要:** ワークスペースメンバがルートパッケージを shadow する場合（ローカル anyio 0.1.0 が pypi の 4.8.0 を隠す等）に `uv pip install` の挙動が一貫しない問題。

**直近のアクティビティ:** @sharifhsn が #12176 以降は不整合が解消され、0.6.7 で一貫して制約を強制するようになったと報告。

**次のアクション:** クローズ可否の確認。

---

### ![Issue](icons/issue-opened.svg) [BUG: `uv tree --invert --package ...` sometimes also displays packages I didn't ask for](https://github.com/astral-sh/uv/issues/11349) `#11349`
**ステータス:** Closed | **ラベル:** bug

**概要:** `uv tree --invert --package` で要求していない `asdf-astropy` が混入する問題。

**直近のアクティビティ:** @sharifhsn が #15908 でクローズ済みと報告し、0.9.5→0.9.6 で挙動が直った再現結果を提示。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add Google Artifact Registry credential retrieval](https://github.com/astral-sh/uv/pull/19611) `#19611`
**ステータス:** Open（Draft）

**概要:** Google Artifact Registry の資格情報取得を Rust 側に実装（gauthkeyring を vendoring）。

**直近のアクティビティ:** @zanieb がレビューコメント。

**変更ファイル:** `crates/uv-auth/src/providers.rs`（+575）、`middleware.rs`、`docs/guides/integration/google.md` など（9 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Use ICF in release builds](https://github.com/astral-sh/uv/pull/19615) `#19615`
**ステータス:** Open（Draft）

**概要:** リリースビルドで `--icf=safe` を使用（deno の PR に触発）。macOS は Mach-O LLD、Linux x86_64 GNU はターゲットスコープの rustflags 経由でバンドル LLD に ICF フラグを渡す。

**直近のアクティビティ:** @zanieb がレビューコメント。

**変更ファイル:** `.github/workflows/build-release-binaries.yml`（+13/-2）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [`uv venv`'s runtime import patching hooks are unexpected](https://github.com/astral-sh/uv/issues/6426) `#6426`
**ステータス:** Open | **ラベル:** virtualenv

**概要:** `uv venv` 作成の venv が `_virtualenv.pth`/`_virtualenv.py` を置き、distutils/setuptools を patch する import hook を入れる点が、標準 stdlib venv と異なり想定外という指摘。

**直近のアクティビティ:** @zanieb が必要な箇所を upstream へ反映したい意向、@DavidCEllis が #19628 で venv 起動時に `contextlib` を import するようになった runtime オーバーヘッドを指摘。

**次のアクション:** hook の必要性の精査。

---

### ![PR](icons/git-pull-request.svg) [Make preview features for commands have names that aren't ambiguous with the command](https://github.com/astral-sh/uv/pull/19645) `#19645`
**ステータス:** Merged | **ラベル:** cli

**概要:** preview feature 名を `format-command`/`check-command`/`audit-command` に変更し、コマンド名との混同（#19643）を避ける。旧名も引き続き読まれる。

**直近のアクティビティ:** @charliermarsh が APPROVED。

**変更ファイル:** `crates/uv-preview/src/lib.rs`、`tests/it/check.rs` など（3 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add a test case covering virtual environment hook behavior](https://github.com/astral-sh/uv/pull/19646) `#19646`
**ステータス:** Open（Draft）| **ラベル:** testing

**概要:** venv の import hook 挙動を網羅するテストケースを追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-install-wheel/src/wheel.rs`、`tests/it/venv.rs` など（17 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Vendor packse scenarios and serve them from an in-memory index](https://github.com/astral-sh/uv/pull/18084) `#18084`
**ステータス:** Merged | **ラベル:** testing, build:skip-release, build:skip-docker

**概要:** resolver シナリオ生成を Rust で書き直し、wiremock のメモリ内 index から配信して packse 依存を排除する（+8293/-3457）。

**直近のアクティビティ:** @zanieb が複数のレビューコメント。

**変更ファイル:** `crates/uv-dev/src/generate_scenarios.rs`（+1020）、`uv-test/src/find_links.rs` など（178 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Allow configuring preview features in `uv.toml` and `pyproject.toml`](https://github.com/astral-sh/uv/pull/18437) `#18437`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `preview-features` を `uv.toml`/`pyproject.toml` に追加（bool またはリスト）。既存の `preview` 設定を置き換える想定で両者は競合する。#15767 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-settings/src/settings.rs`、`uv-preview/src/lib.rs` など（11 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Isolate test environment for ecosystem tests](https://github.com/astral-sh/uv/pull/19644) `#19644`
**ステータス:** Merged | **ラベル:** internal

**概要:** ecosystem テストでスタンドアロンコンテキストが環境変数を継承していた問題を分離する。

**直近のアクティビティ:** @zanieb が APPROVED。

**変更ファイル:** `crates/uv/tests/it/ecosystem.rs`（+4/-7）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Skip Unix-specific installation steps when cross-installing Windows Python distributions](https://github.com/astral-sh/uv/pull/19424) `#19424`
**ステータス:** Merged

**概要:** Linux 上で Windows 版 Python をインストールすると `python` バイナリや `_sysconfigdata_*` がなく失敗していた問題を、Unix 固有ステップをスキップして修正。

**直近のアクティビティ:** @anuejn が自前バンドル作成のためにこの修正の取り込みを希望。@zanieb が APPROVED。

**変更ファイル:** `crates/uv-python/src/downloads.rs`、`managed.rs`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Content-address distributions in the archive](https://github.com/astral-sh/uv/pull/16816) `#16816`
**ステータス:** Open | **ラベル:** enhancement, performance

**概要:** 全 wheel に BLAKE3 ハッシュを必ず計算し、展開済み wheel を content-address 位置に格納してディスク節約とキャッシュ再利用を図る。#1061/#13995/#16786 をクローズ。

**直近のアクティビティ:** @charliermarsh と @zanieb がローカル wheel 前提などをレビューでやり取り。

**変更ファイル:** `crates/uv-distribution/src/distribution_database.rs`、`uv-extract/src/hash.rs` など（12 ファイル）。

**次のアクション:** レビュー継続。

---

### ![Issue](icons/issue-opened.svg) [Incorrect suggestion when you invoke `uv check` without `UV_PREVIEW=1` set](https://github.com/astral-sh/uv/issues/19643) `#19643`
**ステータス:** Open | **ラベル:** bug

**概要:** `uv check` 実行時の警告が `--preview-features check` を促すが、それを付けてもサブコマンド不在エラーになる誤った案内。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #19645 で名称整理）。

**次のアクション:** PR #19645 のマージ。

---

### ![Issue](icons/issue-opened.svg) [uv --locked looks in the wrong place for lockfile when symlinked](https://github.com/astral-sh/uv/issues/19222) `#19222`
**ステータス:** Open | **ラベル:** bug

**概要:** スクリプトをシンボリックリンク経由で実行すると、uv が実体ではなくリンク名に基づいて `.lock` を探す不具合。

**直近のアクティビティ:** @Nakuyu が読み書き両方を canonicalize する方が一貫すると述べ、方針が決まれば実装に協力したいとコメント。

**次のアクション:** メンテナによる設計判断。

---

## neovim/neovim

> LSP の RPC 応答や completion 周りの修正、Windows パス/UNC 対応、libghostty-vt 移行、WASM ビルド対応、vim-patch 取り込みが活発。

### ![Issue](icons/issue-opened.svg) [Disabling LSP completion for a specific LSP does not remove it from vim.lsp.omnifunc completions](https://github.com/neovim/neovim/issues/40068) `#40068`
**ステータス:** Open | **ラベル:** documentation, lsp, completion

**概要:** 特定 LSP で `vim.lsp.completion.enable(false)` しても、その補完が `vim.lsp.omnifunc()` に流れ続ける問題。

**直近のアクティビティ:** @glepnir が、omnifunc は `lsp.get_clients` で全クライアントを拾い `get()` は enable 済みのみを使うため経路を統一すると `<C-X><C-O>` が壊れると説明。@justinmk がドキュメントの誤記を PR #40088 で更新。

**次のアクション:** ドキュメント修正の取り込み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): always respond to requests even when the handler throws](https://github.com/neovim/neovim/pull/40076) `#40076`
**ステータス:** Open | **ラベル:** lsp

**概要:** リクエストハンドラ後のバリデーションが `error()`/`assert()` で失敗すると以降が実行されずサーバへ応答が返らない問題に対し、常に応答するよう修正。

**直近のアクティビティ:** @justinmk が rebase を依頼。

**変更ファイル:** `runtime/lua/vim/lsp/rpc.lua`（+26/-49）。

**次のアクション:** rebase 対応。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): save terminal state to file with `:termsave`](https://github.com/neovim/neovim/pull/40097) `#40097`
**ステータス:** Open | **ラベル:** terminal

**概要:** ターミナルバッファに安定した 64bit ID（`b:term_id`）を付与し、ANSI エスケープ済みの状態とメタデータ（cwd/cmd/title 等）を msgpack で `stdpath('state')/term/` へ保存する `:termsave` を追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/terminal.c`（+350）、`runtime/doc/terminal.txt` など（6 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): handle requests with null id](https://github.com/neovim/neovim/pull/40073) `#40073`
**ステータス:** Merged | **ラベル:** lsp

**概要:** #38340 が id:null をすべて無視していたため、id:null のリクエストも method/param に基づいて処理するよう修正。

**直近のアクティビティ:** @justinmk が関係者へ cc し、レビューコメント。

**変更ファイル:** `runtime/lua/vim/lsp/rpc.lua`、`test/functional/plugin/lsp_spec.lua`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Builtin LSP completion should re-query on empty completion item list when isIncomplete=true](https://github.com/neovim/neovim/issues/40096) `#40096`
**ステータス:** Open | **ラベル:** lsp

**概要:** サーバが `{ isIncomplete = true, items = {} }` を返した際、popup 非表示だと `Context.isIncomplete` が設定されず再クエリされない問題（分析部分は LLM 生成と明記）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再クエリ条件の見直し。

---

### ![PR](icons/git-pull-request.svg) [fix(path): prevent infinite loop when re-editing file without drive](https://github.com/neovim/neovim/pull/40075) `#40075`
**ステータス:** Merged

**概要:** ドライブレターつきパスのファイルをドライブなしで再編集すると `path_fnamencmp` が無限ループする問題を、`c1`/`c2` のいずれかが NUL でループを抜けるよう修正。

**直近のアクティビティ:** @ntdiary と @justinmk がレビューでやり取り、@justinmk が APPROVED。

**変更ファイル:** `src/nvim/path.c`、`test/functional/core/path_spec.lua`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(option): Set related options based on detected shell](https://github.com/neovim/neovim/pull/40031) `#40031`
**ステータス:** Open（Draft）

**概要:** `'shellcmdflag'` が doc 通りに `'shell'` 値に応じて設定されない問題を、Vim の検出ロジックを参考に修正。#31528/#28384/#16957 をクローズ予定。

**直近のアクティビティ:** @justinmk がルールテーブル化案に対し、diff を見ないと判断しづらいとコメント。

**変更ファイル:** `src/nvim/option.c`（+15/-0）。

**次のアクション:** 実装方針の検討。

---

### ![PR](icons/git-pull-request.svg) [fix(validate): mention nil in error message when arg is optional](https://github.com/neovim/neovim/pull/40095) `#40095`
**ステータス:** Open | **ラベル:** needs:response

**概要:** `vim.validate` で optional 引数の型不一致時に nil が許容される旨をエラーに含めない問題（#40037）を修正し、`string|nil` のように表示する。

**直近のアクティビティ:** @justinmk が、Issue に常にリンクされる既存 PR #40043 との違いを確認するよう指摘。

**変更ファイル:** `runtime/lua/vim/_core/shared.lua`、各 spec（7 ファイル）。

**次のアクション:** #40043 との重複整理。

---

### ![Issue](icons/issue-opened.svg) [LSP: configure codelens virtual position](https://github.com/neovim/neovim/issues/39477) `#39477`
**ステータス:** Open | **ラベル:** needs:design, lsp, display, options

**概要:** 0.12 で codelens が virtual text から virtual line に変わり、Rust などで空行が増え視線が飛ぶ問題。位置を設定可能にする要望。

**直近のアクティビティ:** @IpFruion が、デフォルト変更の経緯と spacing の修正・revert 方法がない点に困惑とコメント。

**次のアクション:** 汎用的な設定方法の設計検討。

---

### ![PR](icons/git-pull-request.svg) [fix(diagnostic): stack _tags hl-groups in a single extmark](https://github.com/neovim/neovim/pull/38654) `#38654`
**ステータス:** Merged | **ラベル:** diagnostic

**概要:** 診断ハイライト群を `vim.hl.range` で個別に適用していたため `DiagnosticUnnecessary`/`DiagnosticDeprecated` の上書きが効かない問題を、hl-group リストを一括渡しして修正（#38649）。

**直近のアクティビティ:** @Gelio が修正は妥当としつつ functional test が意図を示せていない懸念を表明。@gpanders が APPROVED。

**変更ファイル:** `runtime/lua/vim/diagnostic/_handlers.lua`、`runtime/lua/vim/hl.lua` など（4 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [`DiagnosticUnderlineWarn` has priority over `DiagnosticUnnecessary`](https://github.com/neovim/neovim/issues/38649) `#38649`
**ステータス:** Closed | **ラベル:** highlight, diagnostic

**概要:** 0.12 以降、`DiagnosticUnderlineWarn` が `DiagnosticUnnecessary` より優先され、未使用変数のハイライト上書きができない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #38654 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [docs: misc, lsp](https://github.com/neovim/neovim/pull/40088) `#40088`
**ステータス:** Open（Draft）

**概要:** `vim.lsp.completion.get()` の doc が「omnifunc が get() を使う」「`i_CTRL-X_CTRL-O` がこれを呼ぶ」という 2 つの誤った主張をしていた点を修正。実際は別のクライアント集合を使う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/lsp.txt`、`runtime/lua/vim/lsp/completion.lua` など（7 ファイル）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Terminal buffer SIGSEGV in putglyph after outer terminal resize](https://github.com/neovim/neovim/issues/40069) `#40069`
**ステータス:** Closed | **ラベル:** terminal, has:backtrace, bug-crash

**概要:** `agent-term.nvim` のパネル内で Codex TUI を動かしつつ外側ターミナルをリサイズすると segfault する報告。

**直近のアクティビティ:** @justinmk が PR #39773（libghostty-vt 移行）で直る見込みと示唆し、@alsi-lawr が nightly で再現しなくなったと確認しクローズ。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): vim.on_key callbacks can tell if key will be consumed](https://github.com/neovim/neovim/pull/40089) `#40089`
**ステータス:** Open | **ラベル:** lua, mappings

**概要:** `vim.on_key` コールバックに、キーが既に discard 済みか `getchar()` 等で消費される予定かを示す `discard` 引数を追加（#40010 を修正）。コールバックは新しい順に実行する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_core/editor.lua`、`src/nvim/getchar.c` など（6 ファイル）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Typing at window edges while completion menu is active causes cursor lag](https://github.com/neovim/neovim/issues/40094) `#40094`
**ステータス:** Open | **ラベル:** performance, bug-vim, needs:response, display, completion

**概要:** autocompletion 有効時、画面端に達するとカーソルが入力文字の後ろに遅れて表示される問題。

**直近のアクティビティ:** @justinmk が、Vim には autocompletion がないため最新版を試すよう案内。

**次のアクション:** 再現確認と切り分け。

---

### ![PR](icons/git-pull-request.svg) [fix(build.zig): do not depend on b.install_path on configure time](https://github.com/neovim/neovim/pull/40048) `#40048`
**ステータス:** Open

**概要:** Zig 0.17 で `b.install_path` が `build()` 内から参照できなくなる準備として依存を解消。spurious rebuild も回避できる。

**直近のアクティビティ:** @justinmk が APPROVED。

**変更ファイル:** `build.zig`、`src/build_lua.zig`、`test/run_tests.zig` など（7 ファイル）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:de18ef6: runtime(doc): Tweak documentation style](https://github.com/neovim/neovim/pull/40093) `#40093`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** ドキュメントスタイル調整の vim-patch 取り込み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/insert.txt`、netrw doc（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0581: window height wrong after maximizing and deleting quickfix buffer](https://github.com/neovim/neovim/pull/40092) `#40092`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** quickfix バッファ削除時に winfixheight をリセットし、最大化後の window 高さがおかしくなる問題を修正する vim-patch。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/buffer.c`、`test/old/testdir/test_quickfix.vim`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Windows: track UNC and device path root handling](https://github.com/neovim/neovim/issues/39844) `#39844`
**ステータス:** Open | **ラベル:** platform:windows, filesystem

**概要:** Windows のパスルート/UNC のエッジケース（`//?C:/`、`//?/BootPartition` 等）が各所に散在しており、個別対応では破綻するため体系的に追跡する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 対応すべきパスルートケースの定義。

---

### ![PR](icons/git-pull-request.svg) [fix(path): open swapfile when using device path](https://github.com/neovim/neovim/pull/39982) `#39982`
**ステータス:** Open

**概要:** `//?/` で始まるデバイスパスで `?` が予約文字のため swapfile を開けない問題を、device UNC パスを `//` に置換するなどして修正（#31606/#29495 をクローズ）。

**直近のアクティビティ:** @justinmk と @ntdiary がレビューでやり取り。

**変更ファイル:** `src/nvim/path.c`、`src/nvim/memline.c` など（4 ファイル）。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:7895c9e: runtime(vim): Update ftplugin, add heredocs to b:match_words](https://github.com/neovim/neovim/pull/40091) `#40091`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** vim ftplugin 更新と heredoc を `b:match_words` に追加する vim-patch。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/ftplugin/vim.vim`（+6/-7）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [automate building pdf documentation](https://github.com/neovim/neovim/issues/39950) `#39950`
**ステータス:** Open | **ラベル:** build, documentation, ci

**概要:** `wget` と `pandoc` でドキュメントの PDF 版を自動生成するシェルスクリプトの提案。

**直近のアクティビティ:** @SqAtx がこの Issue を本体リポジトリへ移してもよいとし、中間 JSON 案を捨てて `gen_one()`/`visit_node()` を複製する漸進的アプローチを提案。

**次のアクション:** PDF 出力の段階的実装。

---

### ![PR](icons/git-pull-request.svg) [refactor(docs): separate HTML-specific bits from gen_help_html](https://github.com/neovim/neovim/pull/39946) `#39946`
**ステータス:** Merged | **ラベル:** refactor

**概要:** PDF 出力（#39950）に向け、`gen_help_html.lua` の HTML 依存部分を分離し `gen_one_html()`/`ts_node_to_html()` へリネーム。Typst 出力追加の下準備。

**直近のアクティビティ:** @SqAtx と @justinmk がレビューでやり取り、@justinmk が APPROVED。

**変更ファイル:** `src/gen/gen_help_html.lua`（+97/-60）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(api): expose command preview through nvim__buf_preview_cmd](https://github.com/neovim/neovim/pull/40090) `#40090`
**ステータス:** Open（Draft）| **ラベル:** api

**概要:** コマンドプレビューを API（`nvim__buf_preview_cmd()`）から利用可能にし、cmdwin からのプレビュー表示（#30340）等を実現する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/api/command.c`、`src/nvim/ex_getln.c` など（3 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(inccommand): show command preview in cmdline window](https://github.com/neovim/neovim/pull/40077) `#40077`
**ステータス:** Closed（Draft）| **ラベル:** inccommand

**概要:** コマンドラインウィンドウでも `'inccommand'` プレビューを表示する実装。

**直近のアクティビティ:** @matystroia が、よりシンプルな #40090 を優先してクローズ。@justinmk と cmdwin の特殊性（blocking select）について議論。

**変更ファイル:** `src/nvim/ex_getln.c`（+137/-53）、`test/functional/ui/inccommand_spec.lua` など（9 ファイル）。

**次のアクション:** なし（クローズ済み、#40090 へ移行）。

---

### ![Issue](icons/issue-opened.svg) [build(wasm): Add WebAssembly/Emscripten build target [Tracking issue]](https://github.com/neovim/neovim/issues/39987) `#39987`
**ステータス:** Open | **ラベル:** gsoc, wasm-webassembly, platform:web

**概要:** GSoC 2026 で WASM/Emscripten を公式ビルドターゲット化し、ブラウザで syntax highlighting や Lua スクリプト、xterm.js 端末を備えた `nvim.wasm` を動かす取り組み（#35567 関連）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マイルストーンに沿った実装継続。

---

### ![PR](icons/git-pull-request.svg) [feat(terminal): replace `libvterm` with `libghostty-vt`](https://github.com/neovim/neovim/pull/39773) `#39773`
**ステータス:** Open | **ラベル:** build, defaults, tui, terminal, ci, AI assisted

**概要:** 組み込みターミナルの基盤を libvterm から libghostty-vt へ移行（#33155）。focus 報告、bracketed paste、入力エンコード、レンダリング等の独立サブシステムを段階移植し各コミットで動作を維持（+2917/-13086）。

**直近のアクティビティ:** @gpanders がローカルでビルド・動作確認し APPROVED。@zeertzjq がレビューコメント。

**変更ファイル:** `cmake.deps/CMakeLists.txt`、`.github/workflows/build.yml` など（74 ファイル）。

**次のアクション:** レビュー継続とマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Add `vim.filetype.set_option`](https://github.com/neovim/neovim/issues/22768) `#22768`
**ステータス:** Closed | **ラベル:** events, filetype, options

**概要:** filetype ごとにデフォルトオプションを設定する `vim.filetype.set_option` の要望（after/ftplugin や autocmd の手間を省く）。

**直近のアクティビティ:** @camoz が treesitter ベースのファイルで `commentstring` を動的変更し `gc*` に反映できない点を関連事項として言及。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [ui2: pressing `<CR>` with active `getchar()` can switch focus to command line](https://github.com/neovim/neovim/issues/40010) `#40010`
**ステータス:** Open | **ラベル:** ui2

**概要:** ui2 有効で `cmdheight=0` のとき、`vim.fn.getchar()` 中の `<CR>` がフォーカスをコマンドラインへ移してしまう問題。

**直近のアクティビティ:** @luukvbaal が #40089 でコールバック引数として状態を渡す案を実装したが、interop の仕様が不明なため意味があるか不確かとコメント。

**次のアクション:** #40089 の方針確認。

---

### ![PR](icons/git-pull-request.svg) [fix(pos): various improvements on `pos._util.get_lines()`](https://github.com/neovim/neovim/pull/40083) `#40083`
**ステータス:** Merged | **ラベル:** lsp

**概要:** `get_lines()` をリファクタし、潜在バグを複数修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/pos/_util.lua`、`runtime/lua/vim/lsp/util.lua`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [colorscheme resets to 'default' after ctrl-z in 0.12.2](https://github.com/neovim/neovim/issues/40066) `#40066`
**ステータス:** Open | **ラベル:** needs:response, bug-regression, lifecycle

**概要:** 0.12.2 のテーマ処理変更により、Lazy プラグインで読み込んだ colorscheme が Ctrl-Z 後に 'default' へリセットされる回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 回帰の原因切り分け。

---

### ![PR](icons/git-pull-request.svg) [Async API](https://github.com/neovim/neovim/pull/40056) `#40056`
**ステータス:** Closed

**概要:** Lua 向けのチャネルベース非同期 API（`vim.async.spawn`/`vim.async.chan`）を、コルーチンモデルで実装し `vim._async` プロトタイプを置き換える提案。

**直近のアクティビティ:** @justinmk が既存の参照（タスクモデルの議論）を確認したか問い、長い #19624 への追記を避けて本 PR でやり取りするよう依頼。

**変更ファイル:** `runtime/lua/vim/async.lua`（+430）、`runtime/doc/lua.txt` など（6 ファイル）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Lua: structured concurrency, Promises, task pipelines](https://github.com/neovim/neovim/issues/19624) `#19624`
**ステータス:** Open | **ラベル:** lua, architecture, async

**概要:** タスクの表現やパイプライン（quasi monad）の組成・エラー処理の組み込み抽象がないという課題。コルーチン + libuv を最大限活用する「task」抽象を検討。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40056 と関連議論）。

**次のアクション:** task 抽象の設計議論継続。

---

### ![Issue](icons/issue-opened.svg) [`@markup.heading.*` capture extends to block quote continuation line](https://github.com/neovim/neovim/issues/40084) `#40084`
**ステータス:** Closed | **ラベル:** status:blocked-external, treesitter

**概要:** markdown で block quote 内の heading の `@markup.heading.*` キャプチャが継続行まで及び、2 行目の引用マーカーが異なる装飾になる問題。

**直近のアクティビティ:** @clason がパーサは CommonMark 準拠で、@camoz が CommonMark 仕様で heading が block quote 内にネスト可能と示し upstream へ報告する流れに。

**次のアクション:** upstream（パーサ）への報告。

---

### ![PR](icons/git-pull-request.svg) [build: add wasm/emscripten build support](https://github.com/neovim/neovim/pull/40008) `#40008`
**ステータス:** Merged | **ラベル:** build, gsoc

**概要:** `wasm32-emscripten` ターゲットの初期ビルドサポートを追加。emcc を唯一のリンカとし Zig はコンパイルのみに限定、LuaJIT 非対応のため Lua 5.1 にフォールバック。

**直近のアクティビティ:** @Rawan10101 と @justinmk が BUILD.md への experimental 明記や nightly artifacts への同梱を議論。@bfredl がレビューコメント。

**変更ファイル:** `build.zig`（+101/-14）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Keycap Digit One Emoji (with Combining Enclosing Keycap, U+20E3) display issue](https://github.com/neovim/neovim/issues/34690) `#34690`
**ステータス:** Open | **ラベル:** performance, unicode

**概要:** キーキャップ絵文字「1️⃣」(U+0031, U+FE0F, U+20E3) のサイズが不正でレイアウトが崩れ、`^L` で一時的に復帰する表示不具合。

**直近のアクティビティ:** @filip-sakel が 0.12.2 でも同様で回避策を尋ねるコメント。

**次のアクション:** 表示処理の調査。

---

### ![PR](icons/git-pull-request.svg) [feat(json): add generic JSON-RPC client](https://github.com/neovim/neovim/pull/38525) `#38525`
**ステータス:** Open（Draft）| **ラベル:** lsp

**概要:** MCP や ACP など JSON-RPC ベースの新プロトコルを活用できるよう、LSP 専用だった JSON-RPC を `vim.json.rpc` として汎用クライアントに抽出する。

**直近のアクティビティ:** @TheLeoP と @ofseed がレビューでやり取り。

**変更ファイル:** `runtime/lua/vim/json/rpc.lua`（+454）、`runtime/lua/vim/lsp/rpc.lua`（-318）など（10 ファイル）。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [perf(redraw): don't redraw hidden windows](https://github.com/neovim/neovim/pull/40057) `#40057`
**ステータス:** Merged | **ラベル:** display, ci:skip-news

**概要:** 隠れたウィンドウの不要な再描画を行わないようにする性能改善。

**直近のアクティビティ:** @justinmk が decoration provider や statusline コールバックへの影響とドキュメント化の必要性をコメントし APPROVED。

**変更ファイル:** `src/nvim/drawscreen.c`、`src/nvim/statusline.c` など（9 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [refactor(net): parameterize transport logging](https://github.com/neovim/neovim/pull/40022) `#40022`
**ステータス:** Merged | **ラベル:** refactor, lsp

**概要:** `vim.net.transport` のログが LSP ログに固定されている問題を、`vim.Log` をパラメータ化して別ファイルに記録可能にする（#38525 の前提）。

**直近のアクティビティ:** @justinmk が rebase 要として APPROVED。

**変更ファイル:** `runtime/lua/vim/net/_transport.lua`、`lsp/rpc.lua` など（3 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(pos): create a cursor position by using the current of a window](https://github.com/neovim/neovim/pull/40087) `#40087`
**ステータス:** Open | **ラベル:** lsp

**概要:** 冗長な `vim.pos.cursor(...)` 呼び出しを、`win` を引数に取れるよう `vim.pos.cursor()` をオーバーロードして簡潔にする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/pos.lua`、`runtime/lua/vim/lsp/buf.lua` など（7 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:e9f8e0f: runtime(matchit): Update matchit plugin](https://github.com/neovim/neovim/pull/40085) `#40085`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** matchit プラグイン更新の vim-patch 取り込み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/pack/dist/opt/matchit/` 配下（3 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [ci: bump github/codeql-action from 4.35.5 to 4.36.0](https://github.com/neovim/neovim/pull/40050) `#40050`
**ステータス:** Open | **ラベル:** ci, dependencies, github_actions

**概要:** codeql-action を 4.35.5 から 4.36.0 へ更新する dependabot PR（最小 CodeQL bundle が 2.19.4 に上がる破壊的変更を含む）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/codeql.yml`（+2/-2）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [build(zig): run oldtests in zig builds (RETROMANIA HYPERDRIVE)](https://github.com/neovim/neovim/pull/39074) `#39074`
**ステータス:** Merged | **ラベル:** build, ci

**概要:** Makefile + シェル + vimscript ラッパで動かしていた旧テスト実行を、シンプルな Lua ラッパに置き換える。

**直近のアクティビティ:** @justinmk が cmake から使えば `testdir/Makefile` を削除できるか問い、@bfredl が後続でやる予定と回答。

**変更ファイル:** `.github/workflows/test.yml`、`build.zig.zon` など（10 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): keep synchronized output active across partial flushes](https://github.com/neovim/neovim/pull/40086) `#40086`
**ステータス:** Open | **ラベル:** tui

**概要:** TUI 出力バッファが redraw 中に埋まると flush 時に `CSI ? 2026 l` が出て同期出力が終わり、大きな redraw でちらつく問題を、最終 flush でのみ終了シーケンスを出すよう修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/tui/tui.c`、`test/functional/terminal/tui_spec.lua`（2 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: syntax file updates](https://github.com/neovim/neovim/pull/40082) `#40082`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** sgf syntax 追加や C/C++ の型修飾子・C23 属性分類などの syntax 更新 vim-patch。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/syntax/c.vim`、`cpp.vim`、`sgf.vim` など（4 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): fall back to textDocument/formatting from vim.lsp.formatexpr](https://github.com/neovim/neovim/pull/40079) `#40079`
**ステータス:** Open（Draft）| **ラベル:** lsp

**概要:** `gqal` でバッファ全体を整形する際、`rangeFormatting` 非対応のサーバ（gopls 等）向けに `textDocument/formatting` へフォールバックする。

**直近のアクティビティ:** @ribru17 が LGTM、ただしテスト追加を希望。

**変更ファイル:** `runtime/lua/vim/lsp.lua`、`runtime/doc/news.txt`（2 ファイル）。

**次のアクション:** テスト追加。

---

### ![PR](icons/git-pull-request.svg) [fix(process): avoid inherited stdio for Windows jobs](https://github.com/neovim/neovim/pull/40074) `#40074`
**ステータス:** Open（Draft）

**概要:** Windows で `stdin`/`stderr` の継承ハンドルを渡さず `UV_CREATE_PIPE` で生成し、`CON` へ直接書く子プロセスが TUI を上書きする問題（#40009）を回避（AI 補助・手動検証と明記）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/event/libuv_proc.c`、`src/nvim/channel.c` など（5 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0583: completion: indent not ignored for fuzzy line completion](https://github.com/neovim/neovim/pull/40080) `#40080`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** whole-line 補完（CTRL-X CTRL-L）で fuzzy 一致時にインデントが無視されない問題を修正（#40072）。

**直近のアクティビティ:** @zeertzjq が APPROVED。

**変更ファイル:** `src/nvim/fuzzy.c`、`test/old/testdir/test_ins_complete.vim`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0585: line number wrong after undoing a deletion in quickfix buffer](https://github.com/neovim/neovim/pull/40081) `#40081`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** quickfix バッファの行削除を undo するとエントリが 1 行下を指す問題を、`qf_mark_adjust` でクリア済みエントリをシフトしないよう修正（#40038）。

**直近のアクティビティ:** @zeertzjq が APPROVED。

**変更ファイル:** `src/nvim/quickfix.c`、`test/old/testdir/test_quickfix.vim`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Quickfix list incremented after deleting error line and undoing deletion](https://github.com/neovim/neovim/issues/40038) `#40038`
**ステータス:** Closed | **ラベル:** bug-vim, needs:vim-patch, editor-state, quickfix

**概要:** quickfix エラー行を削除して undo すると該当エントリの行番号が増え、正しい行を指さなくなる問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40081 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Indent is not ignored in line completion when text before cursor is not empty with `completeopt+=fuzzy`](https://github.com/neovim/neovim/issues/40072) `#40072`
**ステータス:** Closed | **ラベル:** bug-vim, needs:vim-patch, completion

**概要:** `completeopt+=fuzzy` で `<C-X><C-L>` 行補完時に先頭スペースがトリムされない問題（Vim も同様）。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #40080 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Unable to open swap file when using UNC path](https://github.com/neovim/neovim/issues/31606) `#31606`
**ステータス:** Open | **ラベル:** platform:windows, filesystem

**概要:** UNC パス経由でアクセスするファイルで nvim が swapfile を作成できずエラーになる問題。

**直近のアクティビティ:** @ntdiary が #39982 で swapfile 名を修正予定とし、libuv のパス解決の挙動と #39844 でのパス解析改善後の見通しを説明。

**次のアクション:** #39982 のマージ。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40055) `#40055`
**ステータス:** Open（Draft）| **ラベル:** vim-patch

**概要:** vim-patch の取り込み状況を反映する version.c の自動更新 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/version.c`（+33/-33）。

**次のアクション:** 該当 vim-patch の取り込み。

---

## nvim-telescope/telescope.nvim

> oldfiles ピッカーのリモートファイル除外バグ修正や、jdt 等 URI 結果の path_display 非対応 Issue が動いている。

### ![Issue](icons/issue-opened.svg) [jdt decompiled results don't follow path_display](https://github.com/nvim-telescope/telescope.nvim/issues/3677) `#3677`
**ステータス:** Open | **ラベル:** bug

**概要:** `path_display` を "hidden" 以外にしても、`jdt://` のデコンパイル結果（lsp_implementations 等）に反映されない問題。

**直近のアクティビティ:** @LukeElrod が、URI 分類のものを意図的に無視しているためと分析し、`utils.is_uri` を上書きする monkeypatch 回避策を提示。

**次のアクション:** URI 結果の整形方法の設計判断。

---

### ![PR](icons/git-pull-request.svg) [Fix oldfiles picker ignoring entries for remote files](https://github.com/nvim-telescope/telescope.nvim/pull/3676) `#3676`
**ステータス:** Open

**概要:** oldfiles ピッカーが `sftp://` などリモートファイルを除外する問題（#3520）を修正し、#3521 を復活させる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `lua/telescope/builtin/__internal.lua`（+14/-4）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Oldfiles picker discards remote files](https://github.com/nvim-telescope/telescope.nvim/issues/3520) `#3520`
**ステータス:** Open | **ラベル:** bug

**概要:** oldfiles ピッカーが `sftp://` 等のリモートファイルを一覧から除外する問題。

**直近のアクティビティ:** @speelbarrow が、メンテナの提案に従い修正コミットを cherry-pick して PR #3676 を作成したと報告。

**次のアクション:** PR #3676 のレビュー・マージ。

---

## qmk/qmk_firmware

> 新規キーボード追加や iPad 用キーコード、RP2040 ブートローダ設定の DD 対応など、キーボード・コア機能の PR が動いている。

### ![Issue](icons/issue-opened.svg) [[Feature Request] Set country on device](https://github.com/qmk/qmk_firmware/issues/26236) `#26236`
**ステータス:** Open | **ラベル:** enhancement, help wanted

**概要:** systemd が UEFI や接続キーボードから言語設定を取得し始めた流れを受け、qmk でも言語/レイアウトのヒントとなる国情報を設定できるようにする要望。

**直近のアクティビティ:** @drashna が実装は十分シンプルそうと参考コミットを提示し、@razzeee がコンパイル時にキーボードのレイアウトへ設定する想定を補足。

**次のアクション:** 実装方針の検討。

---

### ![PR](icons/git-pull-request.svg) [add ipad power and home keycodes](https://github.com/qmk/qmk_firmware/pull/26152) `#26152`
**ステータス:** Open | **ラベル:** core, documentation, dd

**概要:** Apple の Accessory Design Guidelines に基づき、iPad で有用な Consumer レンジの Power/Home キーコードを追加。

**直近のアクティビティ:** @stillinbeta がレビュー待ちとコメント、@tzarc が APPROVED。

**変更ファイル:** `data/constants/keycodes/keycodes_0.0.9_basic.hjson`、`quantum/keycodes.h` など（6 ファイル）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [First support for Manta 65 PCBs](https://github.com/qmk/qmk_firmware/pull/26103) `#26103`
**ステータス:** Open | **ラベル:** keyboard, keymap, stale

**概要:** MANTA65 PCB のサポートを追加。

**直近のアクティビティ:** stale bot が 45 日間活動なしでマークしたとコメント。@drashna が APPROVED。

**変更ファイル:** `keyboards/manta65/` 配下（24 ファイル、+1026）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [New Keyboard: Tiwaz](https://github.com/qmk/qmk_firmware/pull/26221) `#26221`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** ジョイスティック付きの新スプリットキーボード Tiwaz を追加（マクロパッド/ゲーミング用途も可）。

**直近のアクティビティ:** @drashna が CHANGES_REQUESTED、@tstrube が複数のレビューコメントで対応。

**変更ファイル:** `keyboards/tsc/tiwaz/` 配下（6 ファイル、+762）。

**次のアクション:** 指摘対応とレビュー再依頼。

---

### ![PR](icons/git-pull-request.svg) [DD support for RP2040 bootloader options](https://github.com/qmk/qmk_firmware/pull/25660) `#25660`
**ステータス:** Open | **ラベル:** core, keyboard, documentation, awaiting review, python, cli, dd

**概要:** `RP2040_BOOTLOADER_DOUBLE_TAP_RESET` 系の設定を data-driven（keyboard.json）でマッピングし、値の型付けに `UINT32_C()` を使うよう変更。

**直近のアクティビティ:** @filterpaper と @waffle87 がレビューでやり取り。

**変更ファイル:** `data/mappings/info_config.hjson`、`data/schemas/keyboard.jsonschema` など（230 ファイル）。

**次のアクション:** レビュー継続。

---

## microsoft/vscode

> Agents ウィンドウ／Copilot Chat 関連の機能追加・UX 修正・テレメトリが大量に動いており、BYOK 課金や MCP ツール検索、サインイン要求などユーザーからの不満も集中している。リスナーリーク等の安定性修正もマージされている。

### ![PR](icons/git-pull-request.svg) [Resolve agent-host language models before model picker opens](https://github.com/microsoft/vscode/pull/319669) `#319669`
**ステータス:** Open

**概要:** 拡張提供の言語モデルプロバイダの解決が遅く、Agents ウィンドウのモデルピッカーが起動時に BYOK モデルしか表示しない競合を、事前解決で解消する。

**直近のアクティビティ:** Copilot reviewer bot が、拡張提供モデルが起動時に即表示されるようになると概要をコメント。

**変更ファイル:** `agentHostModelPicker.ts`、`mobileChatInputConfigPicker.ts`（2 ファイル）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Allow local usage of Agents Window, for BYOK scenarios](https://github.com/microsoft/vscode/issues/319667) `#319667`
**ステータス:** Open

**概要:** BYOK で Copilot Chat は使えるが、Agents ビューでは強制サインインモーダルが操作をブロックする問題への改善要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [sessions: use updatedAt timestamp to show last active relative time](https://github.com/microsoft/vscode/pull/319668) `#319668`
**ステータス:** Open

**概要:** セッション一覧の相対時刻表示が Created 順ソート時に `createdAt`（静的）を使い更新されない問題（#319520）を、常に `updatedAt`（observable）を使うよう修正。

**直近のアクティビティ:** Copilot reviewer bot がタイムスタンプ計算の変更概要をコメント。

**変更ファイル:** `src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts`（+1/-1）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [menu bar cannot translate to chinese](https://github.com/microsoft/vscode/issues/319501) `#319501`
**ステータス:** Closed

**概要:** メニューバーが中国語に翻訳されない不具合報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Webview extensions blank on Safari/WebKit](https://github.com/microsoft/vscode/issues/316841) `#316841`
**ステータス:** Open | **ラベル:** bug, safari, vscode.dev, code-server-web

**概要:** Safari/WebKit（vscode.dev / code-server / serve-web）で `VSBuffer.slice` が返すビューが extension-host の postMessage transfer で detach され、Webview 拡張が空白になる問題。

**直近のアクティビティ:** @brockoffdev が、Safari で VSCode Server がほぼ使えないとして修正を強く希望。

**次のアクション:** 原因調査と修正。

---

### ![Issue](icons/issue-opened.svg) [The crash expert](https://github.com/microsoft/vscode/issues/319614) `#319614`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** ある拡張を使う任意の機能で VS Code 1.122.1 がクラッシュするという報告。

**直近のアクティビティ:** @deepak1556 がクラッシュダンプの共有を依頼、vs-code-engineering bot が情報不足を案内。

**次のアクション:** クラッシュダンプの提供待ち。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Add a confirmation dialog to the "Archive/Clear" button in Copilot Chat](https://github.com/microsoft/vscode/issues/319666) `#319666`
**ステータス:** Open

**概要:** Copilot Chat の Archive/Clear ボタンに確認ステップがなく、誤クリックでチャット履歴が失われるため確認ダイアログを求める要望。

**直近のアクティビティ:** @gjsjohnmurray が、アーカイブは unarchive で戻せるとドキュメントを案内。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Optimize shared built-in extension dependencies](https://github.com/microsoft/vscode/pull/318045) `#318045`
**ステータス:** Open（Draft）| **ラベル:** perf

**概要:** 組み込み拡張が重複出荷する JS を、共有依存を `extensions/node_modules/` から一度だけ解決する形に外部化し、整合性検証 postinstall を追加する（66 ファイル）。

**直近のアクティビティ:** @mjbvz が同期ずれ防止のツール追加を要望し、@dmitrivMS が preinstall で破綻させる検証を追加したと回答。

**変更ファイル:** `extensions/package.json`、各拡張の package.json/lock など（66 ファイル）。

**次のアクション:** レビュー継続。

---

### ![Issue](icons/issue-opened.svg) [VSCode won't let me rename a variable](https://github.com/microsoft/vscode/issues/319414) `#319414`
**ステータス:** Open

**概要:** F2 で変数をリネームしようとするとポップアップが出てリネームがキャンセルされる不具合。

**直近のアクティビティ:** @JerryWeii-27 が `github.copilot.renameSuggestions.triggerAutomatically` を無効化すると解消すると報告、@dbaeumer が AI による名前提案が原因と推測。

**次のアクション:** Copilot リネーム提案の挙動修正。

---

### ![Issue](icons/issue-opened.svg) [VS Code 1.122: Chat still requires sign-in when using a configured OpenAI-compatible local network model in Portable Mode](https://github.com/microsoft/vscode/issues/319538) `#319538`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** ポータブルモードで OpenAI 互換のローカルネットワークモデルを設定しても、Chat がサインインを要求し応答しない問題。

**直近のアクティビティ:** @BreathBlush が 1.124 insider でも挙動は変わったが問題が残ると報告、@dmitrivMS が 1.123 での再現を確認依頼。

**次のアクション:** 再現確認と修正。

---

### ![Issue](icons/issue-opened.svg) [client tool call stuck](https://github.com/microsoft/vscode/issues/319641) `#319641`
**ステータス:** Open | **ラベル:** bug

**概要:** クライアントのツール呼び出しがスタックする問題（ログ添付）。

**直近のアクティビティ:** vs-code-engineering bot が情報不足を案内。

**次のアクション:** 詳細情報の提供待ち。

---

### ![Issue](icons/issue-opened.svg) [Remote AH server upgrade path not working for SSH](https://github.com/microsoft/vscode/issues/319381) `#319381`
**ステータス:** Closed | **ラベル:** bug, candidate, agent-host

**概要:**「Update Server」で SSH の remote agent host エントリが見つからず更新できない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #319455 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Chat took too long to get ready](https://github.com/microsoft/vscode/issues/288615) `#288615`
**ステータス:** Open

**概要:** Chat の準備に時間がかかりすぎるパフォーマンス問題。

**直近のアクティビティ:** @zachary-wang12 が dev container でもローカルでも同様と追随コメント。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Refactor AgentService to optimize git state checks](https://github.com/microsoft/vscode/pull/319665) `#319665`
**ステータス:** Open

**概要:** agent-host の `AgentService` で git 状態が不変なら `setSessionMeta` を呼ばないようにし、冗長な更新を削減する。

**直近のアクティビティ:** Copilot reviewer bot が概要をコメント。

**変更ファイル:** `agentService.ts`、`baseAgentHostSessionsProvider.ts`（2 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [agentHost: keep SSH connection registered after incompatible handshake (#319455)](https://github.com/microsoft/vscode/pull/319661) `#319661`
**ステータス:** Merged

**概要:** SSH ハンドシェイクがプロトコル非互換で失敗しても接続を 'incompatible' として登録し続け、「Update Server」フローを使えるようにする（#319455 の release/1.123 反映）。

**直近のアクティビティ:** @dileepyavan と @bhavyaus が APPROVED。

**変更ファイル:** `sshRemoteAgentHostServiceImpl.ts`、各 test など（7 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Unwanted new default themes without consent](https://github.com/microsoft/vscode/issues/304791) `#304791`
**ステータス:** Open

**概要:** 同意なしに新しいデフォルトテーマが追加される問題への苦情。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [tool_search returns non-deterministic, incomplete results for MCP server tools](https://github.com/microsoft/vscode/issues/317998) `#317998`
**ステータス:** Open

**概要:** `tool_search` が登録済み MCP ツールの一部（31 中 1〜6）しか返さず、同一クエリでも結果が毎回変わる問題。直前に使ったツールが検索結果に出ないこともある。

**直近のアクティビティ:** @TimofeyK と @sonnyandersson が、GPT 系では不安定だが Claude では常に動くと追随し、優先対応を要望。

**次のアクション:** ツール検索のランキング/決定性の調査。

---

### ![PR](icons/git-pull-request.svg) [Agents: Focus chat input on new session](https://github.com/microsoft/vscode/pull/319645) `#319645`
**ステータス:** Open

**概要:** 新規チャットビューがキーバインドやボタンで起動されたときにチャット入力へフォーカスが移るようにする。

**直近のアクティビティ:** @benibenj が一部アクションの見落としを認め DISMISSED、@hawkticehurst らとやり取り。

**変更ファイル:** `sessionsActions.ts`、`chat.contribution.ts` など（3 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Parameterizing TSA upload stage](https://github.com/microsoft/vscode/pull/319659) `#319659`
**ステータス:** Open（Draft）

**概要:** ビルドパイプラインで TSA 実行を新しい boolean パラメータ `VSCODE_RUN_TSA` で制御可能にする。

**直近のアクティビティ:** Copilot reviewer bot が概要をコメント。

**変更ファイル:** `build/azure-pipelines/product-build.yml`（+6/-2）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Error serializing chat session for storage](https://github.com/microsoft/vscode/issues/319664) `#319664`
**ステータス:** Open | **ラベル:** info-needed

**概要:** チャットセッションの保存時にシリアライズエラーが出て、ウィンドウを閉じるとセッションが失われる旨の報告。

**直近のアクティビティ:** vs-code-engineering bot が情報不足を案内。

**次のアクション:** 詳細情報の提供待ち。

---

### ![Issue](icons/issue-opened.svg) [Markdown Preview pane Scrolling Unhelpfully](https://github.com/microsoft/vscode/issues/143984) `#143984`
**ステータス:** Open | **ラベル:** bug, help wanted, markdown-ext

**概要:** Markdown プレビューがスクロール時に不適切にジャンプする問題。

**直近のアクティビティ:** @alecazam が、sticky scroll 設定をすべて切っても依然ジャンプすると追随。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Browser Search](https://github.com/microsoft/vscode/pull/317408) `#317408`
**ステータス:** Open（Draft）

**概要:** 統合ブラウザのアドレスバー入力を URL かクエリかで分類し、設定済み検索エンジンで検索 URL を構築する第一弾。

**直近のアクティビティ:** @jruales と @kycutler がレビューでやり取り。

**変更ファイル:** `browserSearch.ts`（+463）、`browserUrlBarWidget.ts` など（8 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Browser: Match ExP value for setting `workbench.browser.enableChatTools`](https://github.com/microsoft/vscode/pull/319465) `#319465`
**ステータス:** Open

**概要:** ExP が 100% に達したため `workbench.browser.enableChatTools` のデフォルトをコードでも true に設定。

**直近のアクティビティ:** Copilot reviewer bot が概要をコメント。

**変更ファイル:** `policyData.jsonc`、`browserEditorChatFeatures.ts` など（4 ファイル）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Enhance logging for autorun registration and session disposal in chat service](https://github.com/microsoft/vscode/pull/319663) `#319663`
**ステータス:** Open

**概要:** チャットセッションのライフサイクルとモデル選択同期の診断ログを強化し、古い autorun が共有状態を変更しないようにする。

**直近のアクティビティ:** Copilot reviewer bot が概要をコメント。

**変更ファイル:** `chatInputPart.ts`、`chatServiceImpl.ts`（2 ファイル）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Consider using a stronger style when doing unicode highlights in a diff region](https://github.com/microsoft/vscode/issues/141928) `#141928`
**ステータス:** Open | **ラベル:** feature-request, polish, diff-editor, unicode-highlight

**概要:** diff 領域で Unicode ハイライトをより強調する提案。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [in Persian language the words (آ) and (ه) are displayed in the highlighted form](https://github.com/microsoft/vscode/issues/138905) `#138905`
**ステータス:** Open | **ラベル:** bug, under-discussion, unicode-highlight

**概要:** ペルシャ語の特定文字 (آ)(ه) がハイライト表示されて煩わしい問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Some activity icons invisible](https://github.com/microsoft/vscode/issues/318954) `#318954`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, workbench-views, regression, unreleased

**概要:** 一部のアクティビティアイコンが見えなくなる回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Node v24 exec format error in WSL 1](https://github.com/microsoft/vscode/issues/318494) `#318494`
**ステータス:** Closed | **ラベル:** bug, verified, WSL, unreleased, electron-42-update

**概要:** WSL 1 で Node v24 をロードできるよう特定の ELF note セクションを除去している件の検証（#316661 のフォローアップ）。

**直近のアクティビティ:** @deepak1556 がアイコン問題は #318954 で対応済みと案内、@sanket-bhalerao が WSL1 で問題なく動いたと報告。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Agents: "Open in Agents Window" button is difficult to discover](https://github.com/microsoft/vscode/issues/315762) `#315762`
**ステータス:** Open | **ラベル:** ux

**概要:** ユーザビリティ調査で参加者全員がタイトルバーの「Open in Agents Window」ボタンを見つけられなかったという指摘。配置を chat パネル等に移す検討。

**直近のアクティビティ:** @lflores-ms が、ボタンのアニメーションが他 UI を押しのけ、ウィンドウ移動の妨げになると追随。

**次のアクション:** ボタン配置/挙動の改善。

---

### ![Issue](icons/issue-opened.svg) [CopilotCLISession "Failed to fetch models: 403" in devcontainer despite successful auth](https://github.com/microsoft/vscode/issues/319662) `#319662`
**ステータス:** Open

**概要:** devcontainer で Copilot Chat がモデル取得エンドポイントで断続的に 403 になり、認証成功にもかかわらず Chat がハングする問題。

**直近のアクティビティ:** @tommysullivan が、キャッシュなし再ビルドや最新版でも問題が残ると報告。

**次のアクション:** 再現確認と修正。

---

### ![Issue](icons/issue-opened.svg) [Conversation Context Management for GitHub Copilot Chat](https://github.com/microsoft/vscode/issues/319648) `#319648`
**ステータス:** Open

**概要:** Copilot Chat のコンテキスト管理が compact/clear の二択しかない点を改善し、剪定・ピン留め・スナップショット・永続化を一級機能として提供する提案。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Copilot AH customizations: support agent instructions](https://github.com/microsoft/vscode/pull/319639) `#319639`
**ステータス:** Open

**概要:** Agent Host カスタマイズの一部として「agent instruction」markdown ファイルを発見し、always-apply 等の特別扱いで Copilot に rules として提供する。

**直近のアクティビティ:** @roblourens が APPROVED。

**変更ファイル:** `sessionCustomizationDiscovery.ts`、`copilotAgent.ts` など（4 ファイル）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Make browser sharing availability contextual to the active session](https://github.com/microsoft/vscode/pull/319660) `#319660`
**ステータス:** Merged

**概要:** Agents ウィンドウで、アクティブセッションがブラウザツールを使える場合のみ「Share with agent」を有効化する（#318560）。

**直近のアクティビティ:** CODENOTIFY bot の通知、@jruales が APPROVED。

**変更ファイル:** `browserViewWorkbenchService.ts`（+5/-3）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [agentHost: keep SSH connection registered after incompatible handshake](https://github.com/microsoft/vscode/pull/319455) `#319455`
**ステータス:** Merged

**概要:** SSH の remote agent host がプロトコル非互換でハンドシェイク失敗しても、トランスポートを 'incompatible' として managed connection に登録し「Update Server」を使えるようにする（#319381 を修正）。

**直近のアクティビティ:** Copilot reviewer bot がレビュー（7 件指摘）、@roblourens がやり取り。

**変更ファイル:** `sshRemoteAgentHostServiceImpl.ts`、各 test など（7 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Agents window: only show browser "share with agent" when current session has browser tools](https://github.com/microsoft/vscode/issues/318560) `#318560`
**ステータス:** Closed | **ラベル:** bug, unreleased, browser-integration

**概要:** セッションがブラウザツールを使える場合のみ「share with agent」を出すべきという Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #319660 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [product.json between CI and PR checks has diverged substantially](https://github.com/microsoft/vscode/issues/313465) `#313465`
**ステータス:** Closed | **ラベル:** vscode-build

**概要:** PR チェックは緑だったのに main マージでビルド失敗（undisposed disposables）した、product.json の CI と PR 間の乖離。

**直近のアクティビティ:** @deepak1556 が embedded configuration は #314409 等で既になくなっていると説明。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [BYOK should not invoke Copilot subagents](https://github.com/microsoft/vscode/issues/319650) `#319650`
**ステータス:** Open | **ラベル:** feature-request, model-byok

**概要:** BYOK セッションから開始したサブエージェント呼び出しでユーティリティモデルではなくクレジットが課金される問題。

**直近のアクティビティ:** @dmitrivMS が BYOK 用プロファイル化や `chat.profiles` 案を提示、@cwebster-99 が課金はサブエージェント経由と指摘。ユーティリティモデルの 'None' オプション追加を feature-request とする方向。

**次のアクション:** ユーティリティモデルの None 対応の実装。

---

### ![Issue](icons/issue-opened.svg) [Inline Widgets: Update styling of inline plan summary](https://github.com/microsoft/vscode/issues/318769) `#318769`
**ステータス:** Open

**概要:** インライン plan summary のスタイル更新要望。

**直近のアクティビティ:** @oliviatyu が Planning モードウィジェットのスタイルガイドと Figma リンクを共有。

**次のアクション:** スタイル実装。

---

### ![PR](icons/git-pull-request.svg) [Add new terminal-suggest completion spec for cargo](https://github.com/microsoft/vscode/pull/305309) `#305309`
**ステータス:** Open | **ラベル:** feature-request

**概要:** `terminal-suggest` 拡張に cargo の補完 spec を追加し、サブコマンドやオプション、`cargo read-manifest` 経由のフィーチャ動的補完を提供する。

**直近のアクティビティ:** @dmitrivMS が `--features` の階層など Copilot の指摘対応と CI 失敗の修正を依頼、@Muszic がやり取り。

**変更ファイル:** `extensions/terminal-suggest/src/completions/cargo.ts`、test など（4 ファイル）。

**次のアクション:** 指摘対応と CI 修正。

---

### ![Issue](icons/issue-opened.svg) [Improve observability and transparency of Copilot agent progress messages](https://github.com/microsoft/vscode/issues/317024) `#317024`
**ステータス:** Closed

**概要:** Copilot Chat が「Activating Cortana」「Bribing the hamster」等のユーモラスな進捗メッセージを実際の進捗と同じ場所に表示し、デバッグ時に何をしているか分かりづらいという指摘。

**直近のアクティビティ:** @Tiinusen が、最新版で「Activating Cortana」は削除され `chat.agent.thinking.phrases` で制御できるとの回答に対し、残存する証跡を提示。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Browser: Playwright API metrics](https://github.com/microsoft/vscode/pull/316473) `#316473`
**ステータス:** Merged

**概要:** 統合ブラウザの `run_playwright_code` ツールに、API 使用・成功・遅延・実行時間・コードサイズのテレメトリを追加。

**直近のアクティビティ:** CODENOTIFY bot 通知、@Yoyokrazy と @kycutler が APPROVED。

**変更ファイル:** `playwrightService.ts`（+201）など（3 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Resolver error: Failed to download VS Code Server (Failed to fetch)](https://github.com/microsoft/vscode/issues/230870) `#230870`
**ステータス:** Closed | **ラベル:** bug, confirmation-pending, network

**概要:** VS Code Server のダウンロードに失敗するリゾルバエラー。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [potential listener LEAK detected, having 401 listeners already](https://github.com/microsoft/vscode/issues/214885) `#214885`
**ステータス:** Closed | **ラベル:** bug, error-telemetry

**概要:** menuEntryActionViewItem 周りで 401 個のリスナーが検出されるリーク報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Not clear what the branch does wrt the prompt box + other customizations](https://github.com/microsoft/vscode/issues/309590) `#309590`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released

**概要:** カスタマイズ編集時に表示される branch/worktree 名と、prompt box が常に chat へ送信される挙動が分かりづらいというテスト指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #309677 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Terminal commands should be auto approved when invoked from `ask_questions` tool](https://github.com/microsoft/vscode/issues/309589) `#309589`
**ステータス:** Closed | **ラベル:** bug, candidate, unreleased, chat-terminal

**概要:** `ask_questions` で既にユーザーが回答済みなら `send_to_terminal` の確認を省くべきという指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #309598 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [New language: Dromi](https://github.com/microsoft/vscode/issues/309575) `#309575`
**ステータス:** Closed

**概要:** 新言語 Dromi（拡張子 .dr）を言語セレクタへ追加する要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Requests for many things](https://github.com/microsoft/vscode/issues/309573) `#309573`
**ステータス:** Closed

**概要:** カスタムテーマ作成やカスタム言語定義など複数機能の要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Ctrl+Shift+G does not focus the Changes view](https://github.com/microsoft/vscode/issues/309572) `#309572`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Linux で Ctrl+Shift+G が Changes ビューを開かず、無関係な `workbench.scm.active` を実行するというテスト指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Persist `Autopilot` mode across sessions](https://github.com/microsoft/vscode/issues/309562) `#309562`
**ステータス:** Closed | **ラベル:** feature-request, verified, verification-needed, insiders-released

**概要:** Autopilot モードがセッションをまたいで保持されず毎回設定し直す必要があるため、永続化を求める要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Replying to states incorrect question](https://github.com/microsoft/vscode/issues/309550) `#309550`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `#sendToTerminal` で回答送信時に「replying to」の質問表示が誤るというテスト指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Agents app on Linux prevents returning to classic VS Code experience](https://github.com/microsoft/vscode/issues/309549) `#309549`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, agents-window

**概要:** Ubuntu で Agents app を開いた後、全ウィンドウを閉じて再起動してもクラシック VS Code に戻れない問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Content is minimal](https://github.com/microsoft/vscode/issues/309536) `#309536`
**ステータス:** Closed

**概要:** 新しい walkthrough は軽量だが、初心者向けに設定アクセスやキーボードショートカット説明などの重要コンテンツが欠けているというテスト指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Failing to parse request](https://github.com/microsoft/vscode/issues/309511) `#309511`
**ステータス:** Closed | **ラベル:** bug, chat-request-failed

**概要:** Copilot Chat（Agent / GPT 5.3 Codex）でリクエスト解析に失敗する報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Input has an unescaped $(icon) in the title](https://github.com/microsoft/vscode/issues/309505) `#309505`
**ステータス:** Closed | **ラベル:** bug, verified, insiders-released, chat-terminal

**概要:** タイトルに `$(icon)` がエスケープされず表示されるテスト指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Extension Signature Verification Failed: Prettier](https://github.com/microsoft/vscode/issues/309384) `#309384`
**ステータス:** Closed | **ラベル:** *duplicate

**概要:** すべての拡張で署名検証に失敗するという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み・重複）。

---

### ![Issue](icons/issue-opened.svg) [Sorry, your request failed. Please try again.](https://github.com/microsoft/vscode/issues/309374) `#309374`
**ステータス:** Closed

**概要:** Copilot で何をしてもリクエスト失敗とコンテキスト喪失が繰り返される苦情。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Cannot Uninstall](https://github.com/microsoft/vscode/issues/309373) `#309373`
**ステータス:** Closed

**概要:** GitHub Copilot Chat 拡張をアンインストールできない報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Considering image inspection](https://github.com/microsoft/vscode/issues/309283) `#309283`
**ステータス:** Closed | **ラベル:** bug, chat-request-failed

**概要:** 画像検査に関する内容が誤って Issue として投稿されたもの（エージェントの思考内容が混入）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Claude dont work](https://github.com/microsoft/vscode/issues/309053) `#309053`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Claude に質問しても回答が返らないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [[Unhandled Error] potential listener LEAK detected, dominated](https://github.com/microsoft/vscode/issues/309042) `#309042`
**ステータス:** Closed | **ラベル:** bug, error-telemetry, insiders-released, recent-regression, ai-generated, ai-analyzed

**概要:** statusbarPart で重複ステータス ID によりリスナーリークが起きる error-telemetry。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #309159 で対応・クローズ）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [[TPI] Enable BYOK for business and enterprise users](https://github.com/microsoft/vscode/issues/309023) `#309023`
**ステータス:** Closed | **ラベル:** testplan-item

**概要:** 個人ユーザー限定だった BYOK モデルプロバイダ（Azure OpenAI、OpenAI、Anthropic、Gemini、Ollama 等）をエンタープライズ/ビジネスユーザーでも使えるようにするテスト項目。

**直近のアクティビティ:** 直近 24 時間のコメントなし（テスト完了）。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Offline Mode property / param in settings](https://github.com/microsoft/vscode/issues/309005) `#309005`
**ステータス:** Closed | **ラベル:** triage-needed

**概要:** Microsoft への通信やバッファ/IPC 共有を抑止するオフラインモード設定の要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Language model not available](https://github.com/microsoft/vscode/issues/308997) `#308997`
**ステータス:** Closed | **ラベル:** triage-needed

**概要:** 言語モデルが利用不可になる報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [Hide "Install" Button from MCP Server Gallery when Server provides neither packages nor remotes](https://github.com/microsoft/vscode/issues/308994) `#308994`
**ステータス:** Closed

**概要:** MCP レジストリを allowlist として使う場合、packages も remotes も持たない（websiteUrl のみの）サーバで Install ボタンを隠す要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![Issue](icons/issue-opened.svg) [nothing working](https://github.com/microsoft/vscode/issues/308983) `#308983`
**ステータス:** Closed | **ラベル:** info-needed

**概要:**「何も動かない」という情報不足の不具合報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Update endgame notebook milestones to 1.117.0](https://github.com/microsoft/vscode/pull/309359) `#309359`
**ステータス:** Merged | **ラベル:** triage-needed

**概要:** endgame ノートブックのマイルストーンを 1.117.0 に更新する自動 PR。

**直近のアクティビティ:** @rzhao271 が APPROVED。

**変更ファイル:** `.vscode/notebooks/endgame.github-issues` など（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix: prevent listener leak from duplicate status IDs in language status (#309042)](https://github.com/microsoft/vscode/pull/309159) `#309159`
**ステータス:** Merged

**概要:** `languageStatus.ts` で更新サイクル中に同じステータス ID が二重に現れると accessor の disposable がリークする問題を、現サイクルで処理済みのエントリも確認して修正。

**直近のアクティビティ:** @jrieken と @lszomoru が APPROVED。

**変更ファイル:** `languageStatus.ts`、`languageStatusDedupe.test.ts`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix chat-lib pipeline](https://github.com/microsoft/vscode/pull/309018) `#309018`
**ステータス:** Merged

**概要:** Copilot `chat-lib` の npm パッケージングを、tag ベースではなく VS Code リリースパイプライン実行で駆動できるよう調整。

**直近のアクティビティ:** @dmitrivMS が APPROVED。

**変更ファイル:** `extensions/copilot/build/` 配下（5 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Cache chat progress content part](https://github.com/microsoft/vscode/pull/308942) `#308942`
**ステータス:** Closed（Draft）

**概要:** ツールストリーミング中に `ChatProgressContentPart` をキャッシュ・更新して再生成を減らす性能改善（GC 時間が 35%→3% に低減）。

**直近のアクティビティ:** Copilot reviewer bot が概要をコメント。

**変更ファイル:** `chatToolProgressPart.ts`、`chatToolStreamingSubPart.ts` など（3 ファイル）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add Agents app banner to welcome page footer](https://github.com/microsoft/vscode/pull/308911) `#308911`
**ステータス:** Merged

**概要:** Welcome ページのフッターに Agents app を宣伝するバナーボタンを追加し、`openAgentsWindow` を実行する。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `agentSessionsBanner.ts`、`gettingStarted.ts` など（10 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add performance tests](https://github.com/microsoft/vscode/pull/309700) `#309700`
**ステータス:** Merged

**概要:** モック LLM サーバを使った chat の end-to-end 性能ベンチ・メモリリーク検出フレームワークと CI ワークフローを追加（+7040）。Welch の t 検定で回帰を判定。

**直近のアクティビティ:** @deepak1556 と @amunger がレビューコメント。

**変更ファイル:** `mock-llm-server.js`（+1014）、`.github/workflows/chat-perf.yml` など（20 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump rand from 0.8.5 to 0.9.3 in /cli](https://github.com/microsoft/vscode/pull/309689) `#309689`
**ステータス:** Merged | **ラベル:** dependencies, rust

**概要:** CLI の rand を 0.8.5 から 0.9.3 へ更新する dependabot PR。

**直近のアクティビティ:** @rzhao271 と @rwoll が APPROVED。

**変更ファイル:** `cli/Cargo.lock`、`cli/src/tunnels/` 配下など（8 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Provide the session resource for input states](https://github.com/microsoft/vscode/pull/309687) `#309687`
**ステータス:** Merged

**概要:** 提案中の chat sessions provider API を拡張し、`ChatSessionInputState` が属するセッションリソースを公開する。

**直近のアクティビティ:** @Yoyokrazy が APPROVED。

**変更ファイル:** `extHostChatSessions.ts`、`vscode.proposed.chatSessionsProvider.d.ts` など（5 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Update distro commit (main)](https://github.com/microsoft/vscode/pull/309683) `#309683`
**ステータス:** Merged

**概要:** `package.json` の distro コミットを vscode-distro の main HEAD へ更新する自動 PR。

**直近のアクティビティ:** @rzhao271 が APPROVED。

**変更ファイル:** `package.json`（+1/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat: remove Copilot CLI shim and clean up worker file copying](https://github.com/microsoft/vscode/pull/309681) `#309681`
**ステータス:** Merged

**概要:** Copilot CLI shim を削除し、worker ファイルのコピー処理を簡素化する。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `extensions/copilot/script/postinstall.ts`（+6/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Repo info telemetry update](https://github.com/microsoft/vscode/pull/309678) `#309678`
**ステータス:** Merged

**概要:** `request.repoInfo` テレメトリに diffsJSON / headBranchName / fileRelativePaths を追加。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `repoInfoTelemetry.ts`（+31/-16）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [AI Customization UX fixes batch (#309304)](https://github.com/microsoft/vscode/pull/309677) `#309677`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** AI カスタマイズ体験の UX 修正をまとめて対応（#309416 ほか）。入力ボックスの挙動修正、送信ボタンの tooltip、混乱を招くフォルダピッカー削除、送信後の「Sent to chat ✓」表示など。

**直近のアクティビティ:** @rebornix が APPROVED。

**変更ファイル:** `aiCustomizationManagementEditor.ts`、`aiCustomizationWelcomePage.ts` など（8 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [agentHost: Default worktree branch picker to repo default branch](https://github.com/microsoft/vscode/pull/309676) `#309676`
**ステータス:** Merged

**概要:** isolation が worktree のとき、ブランチピッカーを現在ブランチではなくリポジトリのデフォルトブランチ（`refs/remotes/origin/HEAD`）に既定化する。

**直近のアクティビティ:** @bryanchen-d が APPROVED。

**変更ファイル:** `agentHostGitService.ts`、`copilotAgent.ts` など（3 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(copilotcli): Enable plan mode](https://github.com/microsoft/vscode/pull/309674) `#309674`
**ステータス:** Merged

**概要:** Copilot CLI の plan exit mode をデフォルトで有効化する。

**直近のアクティビティ:** @justschen が APPROVED。

**変更ファイル:** `extensions/copilot/package.json`、`configurationService.ts`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [More VS Code Onboarding TPI Fixes](https://github.com/microsoft/vscode/pull/309673) `#309673`
**ステータス:** Closed（Draft）

**概要:** オンボーディング walkthrough の複数修正（テーマプレビュー SVG のビルド同梱、Finish Setup 表示、Marketplace キーバインドヒント、スクリーンリーダー対応）の release/1.116 cherry-pick。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `gulpfile.vscode.ts`、`onboardingVariationA` 系など（20 ファイル）。

**次のアクション:** なし（クローズ済み）。

---

### ![PR](icons/git-pull-request.svg) [agentHost: support for client-provided tools](https://github.com/microsoft/vscode/pull/309672) `#309672`
**ステータス:** Merged

**概要:** agent host セッションでクライアント提供ツール（runTests / task / problems）をサポートし、ツール定義をレンダラからエージェントホストへ流して実行を所有クライアントへ戻す。

**直近のアクティビティ:** @roblourens が APPROVED。

**変更ファイル:** `copilotAgent.ts`、`agentService.ts` など（16 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Improve terminal unsandbox retry confirmation](https://github.com/microsoft/vscode/pull/309671) `#309671`
**ステータス:** Merged

**概要:** サンドボックス制限で失敗後の自動 unsandbox リトライ確認の UX を改善（#308506）。確認タイトルから markdown リンクを除去し平易にする。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `runInTerminalTool.ts`、各 test（3 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Don't show onboarding on web](https://github.com/microsoft/vscode/pull/309670) `#309670`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** remote authority のない web 環境（github.dev 等）でオンボーディングをスキップする変更の cherry-pick（#309557）。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `startupPage.ts`（+5/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [agentHost: use proper sdk-provided forking/truncation methods](https://github.com/microsoft/vscode/pull/309669) `#309669`
**ステータス:** Merged

**概要:** Agent Host のフォーク/切り詰めを Copilot SDK の RPC で行うようにし、CLI のオンディスクセッションデータ操作をやめる。メタデータ DB のマイグレーションも追加。

**直近のアクティビティ:** @roblourens が APPROVED。

**変更ファイル:** `copilotAgentForking.ts`（-599）、`copilotAgent.ts` など（14 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [chat: route AI customization list through item sources](https://github.com/microsoft/vscode/pull/309666) `#309666`
**ステータス:** Merged

**概要:** AI Customizations 一覧を統一 item-source 抽象（`ICustomizationItemProvider`）経由にし、promptsService / 拡張 API / AHP remote の 3 経路を一本化する（#309627）。

**直近のアクティビティ:** @Yoyokrazy が APPROVED。

**変更ファイル:** `aiCustomizationItemSource.ts`（+402）、`aiCustomizationListWidget.ts`（-781）など（10 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Extract IAgentHostSessionsProvider types out of ISessionsProvider](https://github.com/microsoft/vscode/pull/309660) `#309660`
**ステータス:** Merged

**概要:** agent-host 固有プロパティを共通 `ISessionsProvider` から新 `IAgentHostSessionsProvider` へ分離し、id ベースの type guard で unsafe cast を置き換える。

**直近のアクティビティ:** @justschen が APPROVED。

**変更ファイル:** `agentHostSessionsProvider.ts`（新規）、各 picker/test など（12 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Update rate limit message](https://github.com/microsoft/vscode/pull/309657) `#309657`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** `getRateLimitMessage` を `copilotPlan` 受け取りに変更し、プラン（free / individual / individual_pro）に応じたレート制限メッセージを返す（#309571 の cherry-pick）。

**直近のアクティビティ:** @sbatten が APPROVED。

**変更ファイル:** `commonTypes.ts`（+36/-3）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [More VS Code Onboarding TPI Fixes](https://github.com/microsoft/vscode/pull/309654) `#309654`
**ステータス:** Merged

**概要:** オンボーディング walkthrough のテーマプレビュー SVG をビルド出力に同梱し、サインイン後の Finish Setup 表示を解消するなどの修正。

**直近のアクティビティ:** @joshspicer が CHANGES_REQUESTED 後 APPROVED。

**変更ファイル:** `gulpfile.vscode.ts`、`onboardingVariationA.ts` など（4 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Replace images with placeholders in summarization prompt](https://github.com/microsoft/vscode/pull/309643) `#309643`
**ステータス:** Merged | **ラベル:** release-cherry-pick

**概要:** Gemini が画像 11 枚以上で「最大 10 枚」エラーになる問題を、要約プロンプトの Image を `[Image was attached]` テキストに置換して回避（メインプロンプトは不変）。

**直近のアクティビティ:** @rzhao271 と @dmitrivMS が APPROVED。

**変更ファイル:** `summarizedConversationHistory.tsx`（+12/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [css/html: use built-in node test framework](https://github.com/microsoft/vscode/pull/309625) `#309625`
**ステータス:** Merged

**概要:** CSS/HTML 言語サーバのテストを Mocha から Node 標準の `node:test` へ移行。

**直近のアクティビティ:** @sandy081 が APPROVED。

**変更ファイル:** `css-language-features` / `html-language-features` 配下（27 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [dispose terminal bg event listeners if session is cancelled](https://github.com/microsoft/vscode/pull/309611) `#309611`
**ステータス:** Merged | **ラベル:** on-testplan

**概要:** run-in-terminal セッションがキャンセルされたらターミナル背景通知のリスナーを破棄し、停止後も background steering プロンプトが続くのを防ぐ（#309607）。

**直近のアクティビティ:** @benvillalobos が APPROVED。

**変更ファイル:** `runInTerminalTool.ts`、`outputMonitor.ts` など（4 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Prevent enable si showing up when using pwsh from chat with accessibility mode](https://github.com/microsoft/vscode/pull/309606) `#309606`
**ステータス:** Merged

**概要:** アクセシビリティモードで pwsh を chat から使う際に「shell integration を有効化」の誤った案内が出る問題（#307698）を、設定で既に有効なら出さないよう修正。A11y 用にタイムアウトを 3 秒延長。

**直近のアクティビティ:** @Yoyokrazy が APPROVED。

**変更ファイル:** `toolTerminalCreator.ts`、`runInTerminalTool.ts`（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Skip redundant `send_to_terminal` confirmation when replying to `askQuestions` carousel](https://github.com/microsoft/vscode/pull/309598) `#309598`
**ステータス:** Merged

**概要:** `askQuestions` で回答済みのテキストを `send_to_terminal` で送る際に確認ダイアログが出る冗長性を、同じターミナルの直近回答と相関できれば省く（#309589, #309608）。

**直近のアクティビティ:** @TylerLeonhardt が APPROVED。

**変更ファイル:** `sendToTerminalTool.ts`、test（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Agents - use pathspec file when adding files to the temporary index](https://github.com/microsoft/vscode/pull/309593) `#309593`
**ステータス:** Merged

**概要:** Agent Sessions の temp index で作業ディレクトリ全体ではなく未コミットファイルのみを pathspec ファイルで stage し、diff/snapshot 性能を改善する。

**直近のアクティビティ:** @roblourens が APPROVED。

**変更ファイル:** `chatSessionWorktreeServiceImpl.ts`、`utils.ts` など（4 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Agents - fix version mode picker regression](https://github.com/microsoft/vscode/pull/311173) `#311173`
**ステータス:** Merged

**概要:** Agents の Changes ビューで version mode picker が `when` 句をビューのスコープ付きコンテキストキーで評価しない回帰を、子 instantiation service で修正。

**直近のアクティビティ:** @connor4312 が APPROVED。

**変更ファイル:** `changesView.ts`（+9/-7）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Log when shell type changes for ai clis](https://github.com/microsoft/vscode/pull/309688) `#309688`
**ステータス:** Merged

**概要:** 起動後に検出 shell タイプが変わりうる AI CLI 向けに `terminal/shellTypeChanged` テレメトリを追加。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `terminalTelemetry.ts`（+28/-0）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Refactor CopilotCLIModels for model caching on authentication changes](https://github.com/microsoft/vscode/pull/309668) `#309668`
**ステータス:** Merged

**概要:** 認証変更時に Copilot CLI のモデルリストキャッシュを無効化し、最新の利用可能モデルを取得できるようにする。

**直近のアクティビティ:** @joshspicer が APPROVED。

**変更ファイル:** `copilotCli.ts`、test（2 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Remove old floating "add element to chat" overlay](https://github.com/microsoft/vscode/pull/309664) `#309664`
**ステータス:** Merged

**概要:** 旧「Add element to chat」フローティングオーバーレイと未使用の `browserElements` サービス層を削除（-1860）。

**直近のアクティビティ:** @roblourens が APPROVED。

**変更ファイル:** `nativeBrowserElementsMainService.ts`（-792）など（21 ファイル）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix: hide browser on overlays within shadow roots](https://github.com/microsoft/vscode/pull/311198) `#311198`
**ステータス:** Merged

**概要:** shadow root 内の UI オーバーレイ（コンテキストメニュー等）が誤検出され統合ブラウザビューがその上に残る問題（#311149）を修正。

**直近のアクティビティ:** @bhavyaus が APPROVED。

**変更ファイル:** `overlayManager.ts`（+5/-1）。

**次のアクション:** なし（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Agents - do not show "Mark as Done" action when there are incoming changes](https://github.com/microsoft/vscode/pull/311193) `#311193`
**ステータス:** Merged

**概要:** Agents Sessions の Changes ツールバーで、incoming changes があるときは「Mark as Done」を出さないようにする。

**直近のアクティビティ:** @vijayupadya が APPROVED。

**変更ファイル:** `sessionsViewActions.ts`（+1/-0）。

**次のアクション:** なし（マージ済み）。

---

## commaai/panda

> 直近 24 時間の更新はありません。
