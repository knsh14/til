# GitHub ダイジェスト — 2026-06-25

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Ford: Use Path Angle and Offset for CAN FD Lateral Control](https://github.com/commaai/opendbc/pull/3315)** (commaai/opendbc) — Ford CAN-FD の横方向制御を曲率ベースから heading/offset ベースへ刷新する大規模な実験的変更で、90 度ターンなど現行制御で不可能な操舵を可能にすると主張。セーフティに関わる踏み込んだ提案。
2. ![PR](icons/git-pull-request.svg) **[feat(:restart)!: no bang restores session and window layout](https://github.com/neovim/neovim/pull/40321)** (neovim/neovim) — `:restart` の既定挙動をセッション/ウィンドウレイアウト復元へ変更する破壊的変更。レビューが活発（71 件超）で news.txt 更新待ち。
3. ![PR](icons/git-pull-request.svg) **[Restrict the EXTRAKEY_ENABLE System Control HID usage range.](https://github.com/qmk/qmk_firmware/pull/26295)** (qmk/qmk_firmware) — デフォルトビルドで Linux/Steam がキーボードを gamepad と誤認する長年の不具合（#25277）をマージで解消。多くのユーザーに影響。
4. ![PR](icons/git-pull-request.svg) **[feat(macos): add tab support for QuickTerminal - continued](https://github.com/ghostty-org/ghostty/pull/9857)** (ghostty-org/ghostty) — QuickTerminal のタブ対応という待望の大型機能（+2129 行）。複数レビュアーが APPROVED し、リリースが期待されている。
5. ![PR](icons/git-pull-request.svg) **[Support scoped dependency overrides](https://github.com/astral-sh/uv/pull/19970)** (astral-sh/uv) — `override-dependencies` を特定パッケージスコープに適用できるようにする機能追加。長年の要望（#2686）に応えるもので、関連 PR 群（#19974/#19977 等）のスタックの起点。

---

## sunnypilot/sunnypilot

> Sunnylink リモート設定の表示・書き込み制御の修正と、テザリング NAT の sunnypilot フック化という2件の UI/ネットワーク系 PR が進行中。

### ![PR](icons/git-pull-request.svg) [fix: Sunnylink settings panel stale display and offroad write enforcement](https://github.com/sunnypilot/sunnypilot/pull/1867) `#1867`
**ステータス:** Open | **ラベル:** なし

**概要:** Sunnylink のリモート設定経路にある 2 つの関連バグを修正する PR。(1) リモートで値を変更しても設定パネルを開き直した際に multiple-button / toggle ウィジェットがパラメータを再読込せず、UI プロセスを再起動するまで古い値が表示され続ける問題。(2) `offroad_only` とマークされた設定（走行中は端末上でグレーアウト）がリモート書き込み経路で強制されておらず、`sunnylinkd.saveParams()` が走行状態に関係なく書き込みを受け付けてしまう問題。前者は `show_event()` でパラメータを再読込し、後者は起動時に `settings_ui.json` から `offroad_only` パラメータを読み込み `IsOnroad` が true の間は黙って書き込みを破棄する（1 件以上成功した時のみ `ParamsVersion` を増加）。

**直近のアクティビティ:** 作者 Geomglot が、失敗している 5 つのチェック（`build release`・`build macOS`・`Create UI Report`・`unit tests`・`preview`）はいずれもコードに到達する前の `scons` ビルド段階で `Unexpected non-vendored library 'x264'` / `'avformat'` により落ちており、同時期の無関係な #1811 や master の `prebuilt` 定期実行でも同じエラーが出ているため、sunnypilot 側の CI インフラ問題だと説明（`static analysis` は通過）。

**変更ファイル:** 4 ファイル変更（+84/-10）。`sunnypilot/sunnylink/athena/sunnylinkd.py`（+67/-8）が中心で、`sunnypilot/sunnylink/docs/README.md`、`system/ui/sunnypilot/widgets/list_view.py`、`system/ui/sunnypilot/widgets/toggle.py` を変更。

**次のアクション:** CI ランナー環境の修正後にチェックを再実行し、レビューを進める。

---
### ![PR](icons/git-pull-request.svg) [ui/networking: implement tethering NAT as WifiManagerSP](https://github.com/sunnypilot/sunnypilot/pull/1811) `#1811`
**ステータス:** Open | **ラベル:** ui

**概要:** テザリングの NAT 修正（#1750 由来）を、upstream の `WifiManager` を直接変更するのではなく sunnypilot のフックとして再実装する PR。`WifiManagerSP` を追加し、`_ipv4_forward` でテザリング有効時に `iptables-legacy` の MASQUERADE ルールを追加、無効時に削除する。これは #1750 で Sentry bot が指摘した「テザリング無効化後もルールが残る」懸念にも対処する。スコープは sunnypilot の設定経路に限定（mici 経路は引き続き upstream の `WifiManager` を使用）。

**直近のアクティビティ:** 直近 24 時間のコメントなし（最新は 5/3 の sunnyhaibin によるレビュー）。レビューで sunnyhaibin が「起動時の古い iptables ルールのクリーンアップを検討したか」「`time.sleep(5)` を落としたのは意図的か。upstream は AP 有効化時に NM が `ip_forward` を自動有効化するのに対抗して使っていた」と質問。

**変更ファイル:** 3 ファイル変更（+126/-2）。新規 `system/ui/sunnypilot/lib/wifi_manager.py`（+50）と、`selfdrive/ui/sunnypilot/layouts/settings/network.py`（+73/-2）、`settings.py`（+3）。

**次のアクション:** sunnyhaibin のレビュー指摘（起動時クリーンアップ、`time.sleep(5)` 除去の妥当性）への対応。

---

## commaai/openpilot

> MetaDrive シミュレータの CI 化（#30693）を巡る複数 PR に加え、hardwared のクラッシュ耐性やアラート優先度、eSIM 通知処理などの実機向けバグ修正が活発。運転モデルや longitudinal 制御の実験的変更も見られる。

### ![PR](icons/git-pull-request.svg) [ci: run MetaDrive simulator test in GitHub Actions](https://github.com/commaai/openpilot/pull/38242) `#38242`
**ステータス:** Open | **ラベル:** なし

**概要:** #30693 のクローズを目的とし、Linux 向け GitHub Actions に MetaDrive シミュレータの動作確認ジョブを追加する PR。fork が使う無料の `ubuntu-24.04` ランナー上で安定実行できるよう、CI 専用のシミュレータ経路を強化している。pin した commaai 製 MetaDrive 依存をシミュレータジョブ限定でインストールし、Xvfb 下で `test_metadrive_bridge.py` を実行する `simulator driving` ジョブを追加して、`qlog`/`rlog`/カメラファイルなどの成果物をアップロードする。あわせて観測された CI flake（MetaDrive の `out_of_lane`、modeld のクラッシュ再起動、Athena アップロードのリトライタイミング、C++ params の非同期タイミングアサーションなど）だけに対処している。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 14 ファイル変更（+146/-39）でドラフト。`.github/workflows/tests.yaml`（+25/-4）にジョブを追加し、`openpilot/tools/sim/` 配下の `metadrive_process.py`・`metadrive_world.py`・`common.py` などシミュレータブリッジ群と `tools/sim/tests/test_sim_bridge.py`（+32/-3）を変更。さらに `radard.py`・`selfdrived.py`・`test_athenad.py`・`test_params.cc`・`setup_dependencies.sh` などに flake 対策の小修正を加えている。

**次のアクション:** ドラフト解除とメンテナーによるレビュー。本文では同一コミットで `tests.yaml` を 20 回連続成功させた検証結果（最終ランで `1 passed, 1 skipped in 86.91s`）が示されている。

---
### ![PR](icons/git-pull-request.svg) [2026.05.20 fix compressed_vipc.py by using pyzmq as zmq client](https://github.com/commaai/openpilot/pull/38072) `#38072`
**ステータス:** Open | **ラベル:** tools

**概要:** `tools/compressed_vipc.py` の実行時エラーを修正する PR。このスクリプトは `cereal.messaging` 経由で C++ 実装の `SubSocket`（`msgq.ipc_pyx`）を使うが、zero dependency 化の過程で `msgq` から zmq 実装（`zmq_impl`）が削除されたため `SubSocket` が動かなくなっていた。そこで通信層を書き換え、`SubSocket` への依存をやめて `pyzmq` を直接用いて稼働中の bridge と通信するクライアントを作成する。デコード処理には一切手を加えていない。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。新規の人手コメントはなし。

**変更ファイル:** 1 ファイルのみ変更で、`openpilot/tools/camerastream/compressed_vipc.py`（+123/-24）。

**次のアクション:** メンテナーによるレビュー。修正版で `watch3.py` に 3 つのカメラ映像すべてが表示されることを確認済みと報告されている。

---
### ![PR](icons/git-pull-request.svg) [Process eSIM notifications after profile operations per SGP.22 spec](https://github.com/commaai/openpilot/pull/38241) `#38241`
**ステータス:** Open | **ラベル:** なし

**概要:** GSMA SGP.22 仕様に従い、プロファイル操作後に保留中の eSIM 通知を処理する PR。install／enable・disable／delete などの操作で eUICC に保留通知がたまると eUICC と SM-DP+ サーバの状態が不整合になるため、`download`・`switch`・確定した `delete` の後に保留通知の処理を試みる。また `process_notifications()` が `handleNotification` へ保留通知 TLV の値部分のみを送っていた不具合を修正し、完全な `BF37` TLV を送るようにする（値のみだと HTTP 200 + GSMA failure、完全な TLV だと HTTP 204 受理）。通知処理はベストエフォートで、失敗しても主処理の結果は変えず、操作後に 1 秒のセトリング待ちを入れる。

**直近のアクティビティ:** 作者 fangli が comma 4 実機での手動検証を報告（comma 3 は未検証）。process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。作者自身による COMMENTED レビュー（本文なし）も 1 件あり。

**変更ファイル:** 2 ファイル変更（+18/-4）。`openpilot/common/esim/esim.py`（+17/-3）と `openpilot/common/esim/lpa.py`（+1/-1）。

**次のアクション:** メンテナーによるレビューと、comma 3 を含む他デバイスでの検証。

---
### ![PR](icons/git-pull-request.svg) [hardwared: gracefully handle failed thermal sensor reads](https://github.com/commaai/openpilot/pull/38240) `#38240`
**ステータス:** Open | **ラベル:** なし

**概要:** #36690 を修正する PR。カーネルレベルの PMIC センサ読み取り失敗（`pm8005` ゾーンで `regmap_bulk_read failed`）が起きると sysfs の `temp` 属性が `OSError` を投げるが、`ThermalZone.read()`（`common/hardware/base.py`）は `FileNotFoundError` しか捕捉しておらず、例外が `hardware_thread` を巻き込んで `hardwared` を落としていた。修正として `read()` で `(OSError, ValueError)` を捕捉し `math.nan` を返す。さらに下流側として、`hardwared.py` が `cpuTempC`/`gpuTempC`/`pmicTempC` に対して取っていた生の `max()` は `NaN` を確実にスキップできず `FirstOrderFilter` を永続的に `NaN` で汚染してしまうため、`NaN` を除外する `safe_max()` を導入する。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。新規の人手コメントはなし。

**変更ファイル:** 2 ファイル変更（+16/-7）。`openpilot/common/hardware/base.py`（+4/-2）と `openpilot/system/hardware/hardwared.py`（+12/-5）。

**次のアクション:** メンテナーによるレビュー。`ruff check` の通過と、`safe_max` がフィルタ汚染を防ぐことのスタンドアロンスクリプト検証は完了済み。

---
### ![PR](icons/git-pull-request.svg) [ci: add metadrive simulation test workflow](https://github.com/commaai/openpilot/pull/38236) `#38236`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** MetaDrive シミュレーションブリッジテストを CI で実行する GitHub Actions ワークフローを追加する PR で、#30693 を対象とする。無料の GitHub ホスト Ubuntu ランナー上で動作し、Mesa llvmpipe と Xvfb によるヘッドレスのソフトウェア OpenGL レンダリングを構成し、音声依存プロセス向けに仮想 PulseAudio シンクを起動する。`tools/sim/tests/test_metadrive_bridge.py::TestMetaDriveBridge::test_driving` を実行し、qlog/rlog/カメラ/ログの成果物をアップロードする。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。新規の人手コメントはなし。

**変更ファイル:** 3 ファイル変更（+265/-2）でドラフト。新規追加の `.github/workflows/metadrive-simulation.yml`（+262）が中心で、`tools/sim/tests/test_metadrive_bridge.py` と `test_sim_bridge.py` を各 1 行修正。

**次のアクション:** ドラフト解除と、本 PR 上の GitHub Actions による初期検証（無料ランナーで安定して通るか、タイムアウト内に完了するか、成果物が成功・失敗いずれでもアップロードされるか）。なお同じ #30693 を狙う #38242 と競合する内容。

---
### ![PR](icons/git-pull-request.svg) [modeld: use correct intrinsics for extra transform when wide camera disabled](https://github.com/commaai/openpilot/pull/38239) `#38239`
**ステータス:** Open | **ラベル:** なし

**概要:** `DISABLE_WIDE_ROAD` フラグ使用時に、big_img モデル入力に対して narrow カメラへ ecam の intrinsics を誤って適用していた問題を修正する PR。wide カメラが利用できない場合は適切な intrinsics を使うようにする。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。作者 Quantizr が、38 セグメントでの時間重み付き横方向経路偏差を計測した結果を投稿。修正版 `DISABLE_WIDE_ROAD` は平均偏差 0.037m と、従来版（0.049m）や通常の 2 カメラ構成に近く一貫した改善が見られ、`DISABLE_ROAD`（0.295m）はほぼ使用不能と報告している。

**変更ファイル:** 1 ファイルのみ変更で、`openpilot/selfdrive/modeld/modeld.py`（+2/-1）。

**次のアクション:** メンテナーによるレビューとマージ判断。

---
### ![PR](icons/git-pull-request.svg) [selfdrived: add permanent alert for processNotRunning](https://github.com/commaai/openpilot/pull/38238) `#38238`
**ステータス:** Open | **ラベル:** なし

**概要:** #34751 を修正する PR。`processNotRunning` は `ET.NO_ENTRY` と `ET.SOFT_DISABLE` のアラートしか定義しておらず、`SOFT_DISABLE` は数秒で消える上に `NO_ENTRY` は engage 試行中しか `current_alert_types` に入らない。そのためプロセス停止後に両アラートが切れると、常時有効な `ET.PERMANENT` 用のエントリが残らず、`steerUnavailable` の "LKAS Fault" のような無関係で低重要度の永続アラートだけが表示され、本来の原因が隠れてしまう。`modeldLagging` と同じパターンに倣い、`processNotRunning` にも `Priority.LOW` の `PERMANENT` アラートを追加し、同じクラッシュの副次的な default 優先度の永続アラートより確実に上位に来るようにする。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。新規の人手コメントはなし。

**変更ファイル:** 1 ファイルのみ変更で、`openpilot/selfdrive/selfdrived/events.py`（+7/-0）。

**次のアクション:** メンテナーによるレビュー。`ruff check` の通過、`AlertManager.process_alerts` を辿った before/after の再現、issue のルートでの確認が済んでいる。

---
### ![PR](icons/git-pull-request.svg) [ui: remove interactive timeout callback when driver camera dialog closes](https://github.com/commaai/openpilot/pull/38237) `#38237`
**ステータス:** Open | **ラベル:** ui

**概要:** #37536 を修正する PR。`Device.add_interactive_timeout_callback()` はリストに追加するだけで削除手段がなく、設定から開くたびに作り直される `DriverCameraDialog`（tici／mici 両方）が毎回新しい `gui_app.pop_widget` コールバックを登録するため、開閉を繰り返すと `_interactive_timeout_callbacks` が無制限に増えていた（既存の `# TODO: this can grow unbounded` コメントどおり）。`ui_state.py` に `remove_interactive_timeout_callback()` を追加し、両ダイアログの `hide_event()` から呼び出して TODO コメントを解消する。他の 3 つの呼び出し箇所はプロセス寿命中に 1 回だけ登録されリークしないため変更しない。

**直近のアクティビティ:** UI Preview（github-actions[bot]）では mici は動画一致、big は差異あり（Videos differ、Diff Report リンクあり）。process replay diff は 0 changed / 66 passed / 0 errors。人手コメントはなし。

**変更ファイル:** 3 ファイル変更（+8/-2）。`openpilot/selfdrive/ui/mici/onroad/driver_camera_dialog.py`（+4/-1）、`openpilot/selfdrive/ui/onroad/driver_camera_dialog.py`（+1/-1）、`openpilot/selfdrive/ui/ui_state.py`（+3/-0）。

**次のアクション:** メンテナーによるレビューと、big で UI Preview に差異が出ている点の確認。

---
### ![PR](icons/git-pull-request.svg) [selfdrive/car/tests: add panda safety TX fuzz test](https://github.com/commaai/openpilot/pull/38234) `#38234`
**ステータス:** Closed | **ラベル:** CI / testing, car

**概要:** #32425 をクローズする目的の PR で、`test_panda_safety_carstate_fuzzy` が RX をファジングするのと同様に TX メッセージをファジングする `test_panda_safety_tx_fuzzy` を追加する。既存の固定 `CarControl` を使う `test_panda_safety_tx_cases` ではフレーム間でアクチュエータ値が変化しないため捕捉できない、状態履歴依存の panda safety バグ（例: Toyota LTA トルク winddown バグ）を検出することが狙い。各例は `active` と `cruise_engaged_prev` をトグルし、非アクティブ時は controlsd を模して中立値を送り、アクティブ時は `FuzzyGenerator` で 20 フレーム分の `CarControl.Actuators` を生成しつつ既知の数値フィールドを controlsd が課す現実的な範囲に制限し、各フレームを `CarInterface.apply()` に通して全 CAN メッセージが `safety_tx_hook` を通過することを確認する。

**直近のアクティビティ:** adeebshihadeh が "Indistinguishable from Claude Code."（Claude Code と見分けがつかない）とコメント。process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。PR はクローズされた（merged ではない）。

**変更ファイル:** 1 ファイルのみ変更で、`openpilot/selfdrive/car/tests/test_models.py`（+67/-0）。

**次のアクション:** クローズ済み。マージには至らなかったため、再検討するなら方針調整や再オープンが必要。なお本文では全 247 対応プラットフォームに対し本番設定で実行し 213 passed / 34 skipped / 0 failed と報告されている。

---
### ![Issue](icons/issue-opened.svg) [Honda: LKAS button can cause delayed immediate disable](https://github.com/commaai/openpilot/issues/36015) `#36015`
**ステータス:** Open | **ラベル:** bug

**概要:** Bosch radarless の Honda 車で発生する「ランダムな解除（random disengagement）」の issue。原因はドライバーがステアリングの LKAS ボタンを押すことに辿れる。問題自体は新しくないが、0.10 のリリースノートが engage 中に LKAS ボタンを押すよう促しているため顕在化している。`commaai/opendbc#2192` に修正案があるが未レビューと記載されている。

**直近のアクティビティ:** davkg が本バグに対処する新 PR `commaai/opendbc#3498` を追加したとコメント。解除を示すルート例（`6a3e5d31bf499cfe/00000000--317a4c5e32`）を提示し、最初の解除はカーブ中、2 回目は直線で発生するが STEERING_REQUIRED と BEEP の信号は直線でしか出ない一方、LKAS ボタン押下直後に必ず解除が起きる形で再現性があると報告している。

**次のアクション:** 提案された opendbc 側修正 PR（#3498 や #2192）のレビューと取り込み判断。

---
### ![PR](icons/git-pull-request.svg) [move some tools back to root](https://github.com/commaai/openpilot/pull/38235) `#38235`
**ステータス:** Merged | **ラベル:** なし

**概要:** 一部のツールをリポジトリのルートディレクトリ配下（`tools/`）へ戻す移動中心の PR。本文の説明はなく、`tools/scripts/` や `tools/car_porting/`、`tools/release/` 配下への大量のファイルリネームが主体。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。人手コメントはなし。マージ済み。

**変更ファイル:** 89 ファイル変更（+82/-74）。大半は `tools/` 配下へのリネーム（`tools/op.sh`、`tools/scripts/car/*.py`、`tools/car_porting/*` など）。`.github/workflows/tests.yaml`（+10/-10）や `Jenkinsfile`、`scripts/post-commit`、`test_onroad.py` などのパス参照を更新し、`tools/scripts/mem_usage.py`（+35）を新規追加、`openpilot/selfdrive/test/mem_usage.py`（-29）をリネーム。

**次のアクション:** マージ済みのため対応不要。

---
### ![PR](icons/git-pull-request.svg) [Compile modeld warps with symbolic camera sizes](https://github.com/commaai/openpilot/pull/38078) `#38078`
**ステータス:** Closed | **ラベル:** stale

**概要:** modeld のワープ（warp）をシンボリックなカメラサイズでコンパイルすることを目的とした refactor 系の PR。本文は PR テンプレートのコメントが貼られたままで、具体的な説明文は記入されていない。

**直近のアクティビティ:** github-actions[bot]により非アクティブのため自動クローズされた（再開可能との案内あり）。`stale` ラベルが付いている。

**変更ファイル:** 6 ファイル変更（+168/-85）。`selfdrive/modeld/compile_modeld.py`（+77/-44）が中心で、`compile_dm_warp.py`（+24/-19）、`dmonitoringmodeld.py`（+27/-4）、`modeld.py`（+27/-8）、`SConscript`（+9/-10）を変更し、`camera_frame.py`（+4）を新規追加。

**次のアクション:** 自動クローズ済み。作業を続けるなら再オープンと内容の更新が必要。

---
### ![PR](icons/git-pull-request.svg) [slimmer ffmpeg](https://github.com/commaai/openpilot/pull/38230) `#38230`
**ステータス:** Closed | **ラベル:** bot

**概要:** repo-maintenance の `package_updates` から自動生成された依存更新 PR。本文には `.venv` のサイズ内訳（合計 729M、`av.libs` 82M、`zensical` 77M など）と `uv pip tree` の出力が貼られており、ffmpeg 関連を含む依存を軽量化する狙い。

**直近のアクティビティ:** 直近 24 時間のコメントなし

**変更ファイル:** 2 ファイル変更（+62/-62）。`uv.lock`（+61/-61）と `tinygrad_repo`（+1/-1）。

**次のアクション:** クローズ済み（merged ではない）。自動 PR のため、後続の更新 PR に置き換えられた可能性がある。

---
### ![PR](icons/git-pull-request.svg) [Fix vendored ffmpeg linking](https://github.com/commaai/openpilot/pull/38231) `#38231`
**ステータス:** Closed | **ラベル:** なし

**概要:** vendored（同梱）ffmpeg のリンク方法を修正する PR。本文の説明文は記入されていないが、変更内容から SConstruct とビルド設定でのリンク指定の調整が主旨。

**直近のアクティビティ:** process replay diff は 0 changed / 66 passed / 0 errors（github-actions[bot]）。人手コメントはなし。

**変更ファイル:** 5 ファイル変更（+14/-13）。`SConstruct`（+8/-1）と、`system/loggerd/SConscript`・`tools/cabana/SConscript`・`tools/jotpluggler/SConscript`・`tools/replay/SConscript` 各 SConscript のリンク設定を調整。

**次のアクション:** クローズ済み（ドラフトのままクローズ、merged ではない）。再着手するなら再オープンと説明追記が必要。

---
### ![PR](icons/git-pull-request.svg) [longitudinal: move cruise speed limit out of the MPC](https://github.com/commaai/openpilot/pull/38232) `#38232`
**ステータス:** Open | **ラベル:** なし

**概要:** クルーズ速度制限のロジックを longitudinal MPC の外へ移す PR。本文の説明文はないが、変更内容から `long_mpc.py` 内で扱っていた cruise speed limit を `longitudinal_planner.py` 側へ移動し、よりロジックとして正しい形にすることが狙い。

**直近のアクティビティ:** adeebshihadeh が「自分もこの件のレポートを作っていたが、こちらにもあるか、それともより正しいのでこのまま出すか」と確認し、haraschax が「`mpc_longitudinal_tuning_report.py` はすでにほぼ完成しているはず」と返答。process replay diff は ⚠️ 2 changed / 64 passed / 0 errors で、HYUNDAI セグメントの `longitudinalPlan.aTarget`（933 diffs）や `accels.0/1` に差分が出ている（本 PR の挙動変更に伴うもの）。

**変更ファイル:** 3 ファイル変更（+24/-16）。`openpilot/selfdrive/controls/lib/longitudinal_planner.py`（+20/-1）、`longitudinal_mpc_lib/long_mpc.py`（+3/-14）、`openpilot/selfdrive/car/tests/test_cruise_speed.py`（+1/-1）。

**次のアクション:** process replay の差分が意図どおりか確認し、関連するチューニングレポート（`mpc_longitudinal_tuning_report.py`）をふまえてメンテナーがマージ判断。

---
### ![PR](icons/git-pull-request.svg) [Deeper, Longer, Better?](https://github.com/commaai/openpilot/pull/38233) `#38233`
**ステータス:** Open | **ラベル:** autonomy

**概要:** 運転モデルファイルを差し替える PR。本文は PR テンプレートのコメントが貼られたままで具体的な説明はないが、タイトルと変更内容から、より深く・長い（"Deeper, Longer"）新しい運転モデルを試す実験的な差し替えと見られる。

**直近のアクティビティ:** Model Review（github-actions[bot]）が master と PR ブランチの `driving_supercombo.onnx` / `big_driving_supercombo.onnx` を比較する reporter リンクを掲示（`dmonitoring_model.onnx` は両者同一）。process replay diff は 0 changed / 66 passed / 0 errors。人手コメントはなし。

**変更ファイル:** 1 ファイルのみ変更で、`openpilot/selfdrive/modeld/models/driving_supercombo.onnx`（+2/-2、バイナリモデルの差し替え）。

**次のアクション:** Model Review の比較結果をふまえた評価と、メンテナーによるマージ判断。

---

## commaai/opendbc

> Honda・Nissan・Genesis・Ford など各車種のカーポートとセーフティ修正が中心。stale ラベルの付いた長期 PR が複数あり、Ford の横方向制御刷新など踏み込んだ提案も含む。

### ![PR](icons/git-pull-request.svg) [Nissan: Speed alignment fix](https://github.com/commaai/opendbc/pull/3497) `#3497`
**ステータス:** Open | **ラベル:** DBC signals, car, nissan

**概要:** Nissan 車でクラスタ表示と CAN メッセージの速度が一致しない問題を修正する PR。オフセットを DBC ファイル側へ移し、CAN メッセージが表示信号と整合するようにする。#3173 と同じ内容。

**直近のアクティビティ:** 作者 Sathans が旧ルート（`44211d9f2879657d/0000001e--57462219b2`）を提示し、より新しいビルドのルートは後日アップロードすると述べている。

**変更ファイル:** 2 ファイル変更（+3/-5）。`opendbc/car/nissan/carstate.py`（+2/-4）と `opendbc/dbc/generator/nissan/_nissan_common.dbc`（+1/-1）。

**次のアクション:** 新しいビルドでのルート提示後、レビューとマージ判断。

---
### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Genesis GV80 2025-26 (HDA2, LFA2)](https://github.com/commaai/opendbc/pull/1646) `#1646`
**ステータス:** Closed | **ラベル:** DBC signals, car, hyundai, stale, car safety

**概要:** 2025-26 Genesis GV80（HDA2）のカーポート PR。CAN-FD アーキテクチャで、Hyundai Q（3.5T）/ R（2.5T）ハーネスに対応。コミュニティの GV80 オーナー jkxs の協力による。トルク低減ゲインコントローラ（`torque_reduction_gain.py`）の追加を含む。

**直近のアクティビティ:** 作者 sunnyhaibin が @sshane に再オープンを依頼。過去のレビューで jyoung8607 が「トルク低減ゲインコントローラの概念には賛成だが現状の形が正しい方向かは未定。角度スムージング関数は問題で、カーポートは openpilot から渡されるアクチュエータを安全境界内で忠実に実行すべき。スムージングが必要なら別 PR で、しかもカーポートではなく openpilot 側で全車共通に行うべき」と指摘し CHANGES_REQUESTED していた。

**変更ファイル:** 13 ファイル変更（+731/-33）。`opendbc/safety/tests/test_hyundai_canfd.py`（+361/-7）、`opendbc/car/hyundai/carcontroller.py`（+150/-13）、`opendbc/safety/modes/hyundai_canfd.h`（+74/-5）、新規 `torque_reduction_gain.py`（+50）など。

**次のアクション:** 再オープンの可否判断と、過去レビュー（角度スムージングの分離など）への対応。

---
### ![PR](icons/git-pull-request.svg) [Honda - Acura RDX 22-26 release support](https://github.com/commaai/opendbc/pull/3316) `#3316`
**ステータス:** Open | **ラベル:** car, honda, stale

**概要:** Acura RDX をリリースサポートへ更新する PR。ステアリングを factory の 3840 へ引き上げ、対応年式を 2026 まで拡張（フィンガープリント変更なし）。NHTSA EPS リコール完了を検証できないため 2025 を除外し、より広範な走行のテストルートに更新する。

**直近のアクティビティ:** github-actions[bot]が「60 日間アクティビティがないため 7 日後に自動クローズ」と通知（`stale` ラベル）。Car behavior report は 154 セグメントで 0 changed / 154 passed / 0 errors。

**変更ファイル:** 5 ファイル変更（+17/-8）。`opendbc/car/honda/interface.py`（+9/-4）、`values.py`（+5/-1）、`extra_cars.py`、`tests/routes.py`、`torque_data/override.toml`。

**次のアクション:** 自動クローズを避けるためのステータス更新と、メンテナーによるレビュー。

---
### ![PR](icons/git-pull-request.svg) [Honda: 2015 CRV South Africa](https://github.com/commaai/opendbc/pull/2933) `#2933`
**ステータス:** Open | **ラベル:** car, honda, stale

**概要:** 南アフリカ仕様の 2015 CRV をサポートする PR。USA/EU の CRV とは異なるレガシーなギアボックスを持つが、それ以外は EU CRV のコード構造に揃えている。

**直近のアクティビティ:** github-actions[bot]が「60 日間アクティビティがないため 7 日後に自動クローズ」と通知（`stale` ラベル）。Car behavior report は 154 セグメントで 0 changed / 154 passed / 0 errors。過去のレビューで jyoung8607 が「レガシーなギアボックスメッセージを参照する車種が増えてきたので、当該抽象を見直し UDS ファームウェアを HONDA_CRV に統合したい。近く作業するのでそれまでこの PR は開いたままに」とコメントしていた。

**変更ファイル:** 5 ファイル変更（+19/-3）。`opendbc/car/honda/fingerprints.py`（+8）、`values.py`（+8/-2）、`interface.py`、`tests/routes.py`、`torque_data/substitute.toml`。

**次のアクション:** jyoung8607 が予告したギアボックスメッセージの抽象見直しを待ち、その後に整理・マージ。

---
### ![PR](icons/git-pull-request.svg) [Honda: stop disengagements caused by LKAS steering wheel nag](https://github.com/commaai/opendbc/pull/3498) `#3498`
**ステータス:** Open | **ラベル:** car, honda, car safety

**概要:** Honda Bosch Radarless での「ランダムな解除」（openpilot#36015）に対処する PR。stock LKAS が有効だとタッチステアリングのタイマー/nag により ACC が解除される。ドライバーが LKAS ボタンを押すと無自覚に stock LKAS が有効化され、約 30 秒後に予期しない解除が起きる。対策として (1) engage 中に stock LKAS が有効なら LKAS を無効化するボタン押下をカメラへ送る、(2) engage 中はドライバーの LKAS ボタン押下をカメラへ到達させない。カメラが LKAS ボタン押下を確実に受理するため `SCM_BUTTONS` メッセージの乗っ取りが必要だった（disengage 中は転送を継続）。

**直近のアクティビティ:** github-actions[bot]が Car behavior report（154 セグメントで 0 changed / 154 passed / 0 errors）を投稿。さらに bot が「ドラフトでなければレビュー準備の確認を」「ルート/dongle ID の提示を」といったコントリビューションガイドのリマインダーをレビューとして投稿。

**変更ファイル:** 4 ファイル変更（+47/-8）。`opendbc/car/honda/carcontroller.py`（+23/-3）、`opendbc/safety/modes/honda.h`（+13/-1）、`opendbc/safety/tests/test_honda.py`（+8/-1）、`hondacan.py`。

**次のアクション:** メンテナーによるセーフティレビュー（ボタン信号の遮断・条件付き転送という珍しい実装の妥当性確認）。

---
### ![PR](icons/git-pull-request.svg) [Honda: Pilot 2023-2025 (canfd)](https://github.com/commaai/opendbc/pull/2636) `#2636`
**ステータス:** Merged | **ラベル:** car, honda

**概要:** 2023-2025 Honda Pilot（CAN-FD）をサポートする PR。クローズされた #2331 をベースに #2610 と整合する形で変更し、75 分のログでパラメータを設定。テスターからは factory LKAS よりコミュニティビルドの方がステアリングが良いとの報告があり、EPS が factory LKAS の指示より大きな値を許容している可能性が示唆されている。

**直近のアクティビティ:** sshane と作者 mvl-boston が COMMENTED（いずれも本文なし）でやり取り。

**変更ファイル:** 5 ファイル変更（+11/-9）。`opendbc/car/honda/interface.py`（+6/-4）、`values.py`（+3/-3）、`fingerprints.py`、`extra_cars.py`、`torque_data/override.toml`。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [Ford: Use Path Angle and Offset for CAN FD Lateral Control](https://github.com/commaai/opendbc/pull/3315) `#3315`
**ステータス:** Open（Draft）| **ラベル:** car, ford, car safety

**概要:** Ford の CAN-FD 車の横方向制御を、曲率（c2）のみ送る現行方式から、モデルの予測軌道を heading（c1, path_angle）と lateral offset（c0, path_offset）として直接送る方式へ変更する大規模な実験的 PR。曲率は PSCM に内部状態を蓄積させカーブ後にドリフト/プルを起こすが、c1/c0 ベースに移すことでこの問題群を解消し、90 度ターンや緩い S 字など現行制御では不可能な操舵が可能になると主張。非 CAN-FD 車は従来の曲率パイプラインを維持。`mode=2`（PathFollowingExtended）と `ramp_type=3`（Immediate）を使い、c1 を 95%・c0 を 90% にクリップして状態蓄積を抑える。低速での先読み距離に 7m の下限を設けるなど詳細なチューニングを伴う。

**直近のアクティビティ:** github-actions[bot]が Car behavior report（91 セグメントで 0 changed / 91 passed / 0 errors）を投稿。人手の新規コメントはなし。

**変更ファイル:** 6 ファイル変更（+330/-42）。新規 `opendbc/car/ford/lateral_bal.py`（+119）、`opendbc/car/ford/carcontroller.py`（+68/-26）、`opendbc/safety/tests/test_ford.py`（+55/-6）、新規 `tests/test_lateral_bal.py`（+56）、`opendbc/safety/modes/ford.h`（+27/-6）など。

**次のアクション:** Draft かつ未テストブランチ警告付き。検証ルートの公開可否を含め、セーフティ面の精査とレビューが必要。

---

## commaai/panda

> 直近24時間の更新はありません。


---

## neovim/neovim

> LSP の `LspNotify` 周りの一連の修正とリグレッション対応、cmdwin の通常バッファ化やディレクトリブラウザ追加といった大型機能、多数の vim-patch 取り込みとバックポートが並行して進む。`actions/checkout@v7` 対応の CI 修正も目立つ。

### ![PR](icons/git-pull-request.svg) [feat(cmdwin): implement cmdwin as a normal buf+win](https://github.com/neovim/neovim/pull/40325) `#40325`
**ステータス:** Open | **ラベル:** AI assisted 🤖

**概要:** cmdwin（`q:` のコマンドラインバッファ）は実装上の制約が多く、コードベース全体で特殊処理を必要としており、非同期プラグインがバッファ/ウィンドウを生成しようとした際に `E11: Invalid in command-line window` で壊れる問題がある。本 PR は cmdwin を `'winfixbuf'`・`'bufhidden=wipe'`・`'buftype=nofile'` を持つ通常のバッファ+ウィンドウとして再実装し、`<c-r>=` などの再帰的な「インセプション」用途を意図的に切り捨てることで、core とプラグイン双方から cmdwin の特殊処理を排除する。副次効果として垂直セパレータのマウスドラッグや cmdwin 内 inccommand が正しく動くようになる（#40077 が不要になる）。

**直近のアクティビティ:** コメントなし。justinmk が 6 回 COMMENTED（いずれも本文は空）でセルフレビューを継続中。

**変更ファイル:** 65 ファイル（+1188 / -1548）。新規 `runtime/lua/vim/_core/cmdwin.lua`（+225）でコア実装を Lua 化し、`src/nvim/ex_getln.c`（+76 / -290）や `src/nvim/normal.c`、`src/nvim/window.c` など多数の C ファイルから cmdwin 特殊処理を削除。新規テスト `test/functional/editor/cmdwin_spec.lua`（+156）を追加し、旧 `test/functional/legacy/cmdwin_spec.lua`（-97）を削除。`runtime/doc/cmdline.txt` 等のドキュメントも更新。

**次のアクション:** ドラフト状態のため、設計（破壊的変更を含むインセプション非対応の是非）とレビューの完了が必要。`nvim__cmdwin_set` 削除や `CHECK_CMDWIN` 撤廃などのフォローアップ（#40407）は別途対応。

---
### ![PR](icons/git-pull-request.svg) [ci(reviewers): inline scripts, drop checkout](https://github.com/neovim/neovim/pull/40413) `#40413`
**ステータス:** Merged | **ラベル:** ci

**概要:** `reviewers_add`/`reviewers_remove` の CI ジョブが `require('…/reviewers_add.js')` のためだけにリポジトリを checkout しており、さらに `reviewers_remove` は `closed` でも走るため、マージ済み fork PR で `actions/checkout@v7` の新しい allow-unsafe-pr-checkout ガードに引っかかる。本 PR は `actions/checkout` の使用をやめ、スクリプトを CI YAML 定義内にインライン化する。あわせて、これまで fire-and-forget だった変更系呼び出しを await し、空のレビュアーリストをガードする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 4 ファイル（+65 / -146）。`.github/scripts/reviewers_add.js`（-118）と `.github/scripts/reviewers_remove.js`（-16）を削除し、`.github/workflows/reviewers_add.yml`（+50）と `.github/workflows/reviewers_remove.yml`（+15）にロジックをインライン化。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [feat(api): nvim_exec2() accepts args](https://github.com/neovim/neovim/pull/40401) `#40401`
**ステータス:** Open | **ラベル:** api

**概要:** 現状、Lua の `nvim_exec_lua()` のように任意の Vimscript コードへ引数を渡すエルゴノミックな手段がなく、文字列を組み立てる必要がある。本 PR は `nvim_exec2()` を拡張して引数リストを受け取れるようにし、新しい特殊グローバル変数 `v:args` と `v:result` を通じて引数の受け渡しと戻り値の返却を可能にする。#40159 をクローズする。#33392 を前提とする。

**直近のアクティビティ:** コメントなし。zeertzjq・justinmk・作者 dqnne がそれぞれ 1 回 COMMENTED（いずれも本文は空）。

**変更ファイル:** 34 ファイル（+355 / -200）。`src/nvim/api/vimscript.c`（+64 / -13）が中心で、`src/nvim/vvars.lua`（+20）に `v:args`/`v:result` を追加、`src/nvim/api/deprecated.c`（+23 / -3）も更新。`test/functional/api/vim_spec.lua`（+134 / -77）でテストを大幅に追加。`runtime/doc/vvars.txt` も更新。

**次のアクション:** ドラフト状態かつ #33392 待ち。レビューコメントへの対応と非ドラフト化が必要。

---
### ![PR](icons/git-pull-request.svg) [refactor(treesitter): improve memory usage of incremental selection history](https://github.com/neovim/neovim/pull/40409) `#40409`
**ステータス:** Open | **ラベル:** refactor, treesitter

**概要:** `treesitter/_select.lua` の `history` テーブルは過去に選択した `TSNode` への参照を保持しているが、`TSNode` はその `TSTree` 全体をメモリ上に保持し続けるため、対応バッファを閉じた後もメモリが解放されない。本 PR は `TSNode` そのものを参照せず、選択履歴を遡るのに必要な情報（範囲と ID）だけを保持するように変更する。

**直近のアクティビティ:** dmitmel が PR 説明とコミットを problem/solution 形式に書き直し「こんな感じ?」と確認、さらに表現について助言を求めている。justinmk は CONTRIBUTING.md に沿った problem/solution 形式への整形を依頼し、@altermo に cc、数日でマージされなければ ping するよう案内。レビューは justinmk・dmitmel が各 1 回 COMMENTED（本文は空）。

**変更ファイル:** 1 ファイル（+10 / -6）。`runtime/lua/vim/treesitter/_select.lua` のみ。

**次のアクション:** altermo らによるレビューとマージ。数日以内に動きがなければ justinmk へ ping。

---
### ![PR](icons/git-pull-request.svg) [backport: fix(lsp): skip invalid file watcher globs #40376](https://github.com/neovim/neovim/pull/40411) `#40411`
**ステータス:** Merged | **ラベル:** lsp, target:release

**概要:** #40396 を `release-0.12` ブランチへバックポートする PR。解析できない glob パターンを持つファイルウォッチャをスキップする修正を安定版へ反映する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル（+81 / -16）。`runtime/lua/vim/lsp/_watchfiles.lua`（+52 / -12）と `test/functional/plugin/lsp_spec.lua`（+29 / -4）。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [fix(shada): plugin/shada.lua does not respect the `+` flag of cpoptions](https://github.com/neovim/neovim/pull/40412) `#40412`
**ステータス:** Open | **ラベル:** なし

**概要:** `plugin/shada.lua` が `*.shada` バッファを別ファイルへ書き出す際に `'cpoptions'` の `+` フラグを尊重していない。本 PR は `*.shada` の `BufWriteCmd` 内で、バッファの `'modified'` をリセットする前に `'cpoptions'` に `+` があるか、かつバッファが別ファイルへ書き込まれたかをチェックする。この不具合は #34725 で `plugin/shada.vim` が Lua へ書き換えられた後に発生した。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル（+42 / -1）。`runtime/plugin/shada.lua`（+8 / -1）と `test/functional/plugin/shada_spec.lua`（+34）。

**次のアクション:** レビューとマージ。

---
### ![PR](icons/git-pull-request.svg) [ci(backports): "Refusing to checkout from 'pull_request_target'"](https://github.com/neovim/neovim/pull/40410) `#40410`
**ステータス:** Merged | **ラベル:** ci, AI assisted 🤖, ci:backport release-0.12

**概要:** `actions/checkout@v7.0.0` への更新後、`pull_request_target`（closed + merged）で動くバックポートワークフローが `Refusing to check out fork pull request code from a 'pull_request_target' workflow` で失敗するようになった。v7.0.0 で追加された `allow-unsafe-pr-checkout` ガードは、解決された checkout SHA が PR の head.sha または merge_commit_sha と一致すると拒否する。`ref` 未指定だと base ブランチ tip（`github.sha`）に解決され、マージ済み PR ではそれが merge commit となるため拒否される。本 PR は base ブランチを名前で checkout することでガードを回避する（バックポートはマージ先 ref から分岐すべきという点で意味的にも正しい）。

**直近のアクティビティ:** justinmk が「checkout ステップは通過したが backport ステップが失敗。これはバックポートワークフローが `.github/workflows/**` への push を許可されていないためで、これは良いこと。`release-0.12` ブランチに backport ワークフローは不要なので問題ない」とコメント。neovim-backports[bot]が `Git push to origin failed for release-0.12 with exitcode 1` を報告。

**変更ファイル:** 1 ファイル（+6）。`.github/workflows/backport.yml`。

**次のアクション:** マージ済み。push 権限の制約は意図通りのため追加対応なし。

---
### ![PR](icons/git-pull-request.svg) [fix(lsp): skip invalid file watcher globs #40376](https://github.com/neovim/neovim/pull/40396) `#40396`
**ステータス:** Merged | **ラベル:** lsp, ci:backport release-0.12

**概要:** #40376 をクローズする修正。`vim.glob.to_lpeg` が解析できない `globPattern`（例: tsgo の `bundled:///libs/**/*`）が `client/registerCapability` リクエスト全体を中断させてしまう問題に対し、無効なファイルウォッチャ glob をスキップするようにする。本文では作者がバックログを 1.6k から 1.5k に減らしたいと冗談を添えている。

**直近のアクティビティ:** neovim-backports[bot]が `release-0.12` 向けバックポート PR #40411 の作成成功を報告。justinmk が「ありがとう」とコメント。

**変更ファイル:** 2 ファイル（+81 / -16）。`runtime/lua/vim/lsp/_watchfiles.lua`（+52 / -12）と `test/functional/plugin/lsp_spec.lua`（+29 / -4）。

**次のアクション:** マージ・バックポート済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [feat(lsp): use LspNotify for semantic tokens](https://github.com/neovim/neovim/pull/40224) `#40224`
**ステータス:** Merged | **ラベル:** lsp, ci:backport release-0.12

**概要:** セマンティックトークンモジュールは on_lines イベント用に独自の debounce タイマーを使っており、その内部 debounce が changetracking モジュールの debounce より短いと、`textDocument/didChange` 通知がサーバへ送られる前にセマンティックトークン要求が発火しうる。本 PR はメソッドが didChange または didOpen 通知のときに `LspNotify` autocmd からセマンティックトークン要求をトリガーし、同期変更通知→トークン要求という厳密な happens-before 関係を強制する。サーバ refresh 通知の debounce や範囲要求時の WinScrolled 処理など、機能固有のニーズ向けの内部 debounce 機構は残る。

**直近のアクティビティ:** jdrouhard が「LspNotify だけでは従来のカスタムコールバックを完全には置き換えられなかったが、ようやく不具合を解消した。最新 master を試してほしい」とコメント。folke が「いいね、まだ変な挙動があれば知らせる」と応答。

**変更ファイル:** 3 ファイル（+141 / -77）。`runtime/lua/vim/lsp/semantic_tokens.lua`（+16 / -22）、`test/functional/plugin/lsp/semantic_tokens_spec.lua`（+124 / -55）、`test/functional/plugin/lsp/testutil.lua`（+1）。

**次のアクション:** マージ済み。folke のフィードバック待ちだが対応は完了。

---
### ![Issue](icons/issue-opened.svg) [Use lua-compat-5.3 to enable Lua 5.1 interpreters to support LuaJIT's extended features](https://github.com/neovim/neovim/issues/40408) `#40408`
**ステータス:** Open | **ラベル:** lua

**概要:** wasm など LuaJIT 非対応プラットフォームをサポートするため、nvim はコードベースを PUC Lua 5.1 互換に保っており、LuaJIT の拡張構文の一部が使えない。本 issue は lua-compat-5.3 を PUC Lua 5.1 ビルドへ組み込むことで、`table.pack`/`table.unpack`/`table.move`/`rawlen`/`package.searchpath` などの Lua API や `lua_copy`/`lua_absindex`/`luaL_setfuncs` などの C API を PUC Lua ビルドでも利用可能にすることを提案する（対応表は ChatGPT で要約）。

**直近のアクティビティ:** alarcritty が実装を引き受けたいと表明。`lua-compat-5.3 v0.13` は既に `cmake.deps/deps.txt` で vendor されているが luv ビルドにしか配線されていないため、`src/nvim/CMakeLists.txt` の `PREFER_LUA` ブロックを拡張して `compat-5.3.c` を nvim コアにコンパイルし、`src/build_lua.zig` の `use_luajit == false` パスにも反映する計画を提示し、PR を開く前に方針の妥当性を確認している。

**次のアクション:** メンテナによる alarcritty の実装方針への承認、その後の PR 作成。

---
### ![PR](icons/git-pull-request.svg) [build(wasm): add missing platform stubs](https://github.com/neovim/neovim/pull/40306) `#40306`
**ステータス:** Open | **ラベル:** gsoc

**概要:** 本 PR は Link Time Optimization (LTO) を有効化し、より適切なメモリ・スタック上限を設定し、Asyncify と例外処理を有効化し、リンク時に欠落依存を検出できるよう厳密な undefined-symbol チェックを復元する。あわせて `wasm_stubs.c` を導入し、WASM 環境で利用できない libuv やプラットフォーム固有関数の Emscripten 互換代替を提供する。

**直近のアクティビティ:** Rawan10101 が「ブラウザでローカルに nvim.wasm+nvim.js を試すための `app.html` のようなスタブはあるか」という問いに「持っているのですぐ push する」と応答。

**変更ファイル:** 3 ファイル（+216 / -5）。`build.zig`（+22 / -5）、新規 `contrib/test_wasm.html`（+68）、新規 `src/wasm_stubs.c`（+126）。

**次のアクション:** justinmk の 1 件のレビューコメントへの対応と、ブラウザ動作確認用 HTML の push、レビュー継続。

---
### ![Issue](icons/issue-opened.svg) [cmdwin freedom](https://github.com/neovim/neovim/issues/40407) `#40407`
**ステータス:** Open | **ラベル:** refactor, cmdline-mode

**概要:** #40325 に対する潜在的なフォローアップを列挙したトラッキング issue。`nvim__cmdwin_set` の削除、`CHECK_CMDWIN`（`E11: Invalid in command-line window`）の撤廃、`BUFLOCK_OK` / `LOCK_OK` の統合、式レジスタからの cmdwin（`<c-r>= c-f`）対応の 4 項目をチェックリスト化している。一部は実需要次第で着手する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #40325 のマージ後、各フォローアップ項目を需要に応じて着手。

---
### ![PR](icons/git-pull-request.svg) [fix(fs): preserve UNC share roots in `fnamemodify()`](https://github.com/neovim/neovim/pull/39841) `#39841`
**ステータス:** Open | **ラベル:** vimscript

**概要:** 最近の `fnamemodify(..., ':h')` のルート処理の続きだが、Windows の UNC パスはサーバ/共有のペアがルートになるという別のルールを持つ。`fnamemodify(..., ':h')` が共有名を通常のパス要素として扱い、`//foo/C$` を `//foo` まで切り詰めてしまう問題に対し、本 PR は head 計算時に基本的な Windows UNC 共有ルートを保持する。#39819 をクローズし、vim/vim#20231 に関連。

**直近のアクティビティ:** ntdiary が COMMENTED（本文は空、6/25）。これに先立ち barrettruth・kylesower・ntdiary によるレビューコメント（いずれも本文は空）が続いている。

**変更ファイル:** 2 ファイル（+83 / -3）。`src/nvim/eval/fs.c`（+66）と `test/functional/vimscript/fnamemodify_spec.lua`（+17 / -3）。

**次のアクション:** ntdiary のレビューコメントへの対応とレビュー完了。

---
### ![PR](icons/git-pull-request.svg) [fix(lsp): fire LspNotify didChange autocmds after undo/redo](https://github.com/neovim/neovim/pull/40404) `#40404`
**ステータス:** Merged | **ラベル:** lsp, ci:backport release-0.12

**概要:** undo/redo（および他のアクション）時に didChange リクエストの `LspNotify` autocmd が発火しない問題があった。これは undo/redo 実行中に `on_lines()` コールバックを呼ぶ際 autocmd がブロックされるため。本 PR はアクション完了後まで autocmd の発火を遅延させ、遅延関数内でクライアントがまだアクティブでバッファがアタッチされているかをチェックしてから発火する。

**直近のアクティビティ:** clason が「これで `ts_query_ls` で気づいた診断のリグレッションが直った」とコメント。neovim-backports[bot]が `release-0.12` 向けバックポート PR #40405 の作成成功を報告。

**変更ファイル:** 2 ファイル（+15 / -9）。`runtime/lua/vim/lsp/client.lua`（+13 / -9）と `test/functional/plugin/lsp/semantic_tokens_spec.lua`（+2）。

**次のアクション:** マージ・バックポート済みのため対応なし。

---
### ![Issue](icons/issue-opened.svg) [ci: slow `mlugg/setup-zig` step because of cache size](https://github.com/neovim/neovim/issues/40206) `#40206`
**ステータス:** Open | **ラベル:** ci

**概要:** zig ジョブが大量のキャッシュを保存しており、各実行に 10〜20 秒を追加している。キャッシュディレクトリは約 1.14GB で上限 2GB 未満のため維持されるが、本当に必要か疑問視している。ログには `Saving Zig cache with key 'setup-zig-cache-v2-...'` の保存処理が示されている。

**直近のアクティビティ:** bfredl が「再コンパイル時間の節約は少なくとも 50 秒。現在の上限は圧縮後約 450MB で大きすぎるが、時間としては両端で 12〜15 秒程度（zig 自体のインストールに常時 +5 秒）。これは時間というよりサイズの問題だが、Microsoft は今のところ GHA キャッシュ容量を気にしていないようだ」とコメント。別途 huangruiteng が「LoopX」なる自動化システムの宣伝とトリアージノートの提案を投稿している（スパム的内容）。

**次のアクション:** キャッシュ容量と再コンパイル時間のトレードオフをどう扱うか方針決定。現状は容量上の懸念にとどまる。

---
### ![Issue](icons/issue-opened.svg) [Zig test harness loses runtime/Lua paths after cwd changes](https://github.com/neovim/neovim/issues/40381) `#40381`
**ステータス:** Open | **ラベル:** test, ci

**概要:** Zig の機能テストハーネスはランタイムと Lua の検索パスをプロセスの作業ディレクトリ相対（`VIMRUNTIME=runtime`、`--lpath=./runtime/lua/?.lua` 等）で起動するため、cwd を変更するテストがランタイムファイルや Lua モジュールへのアクセスを失い、`module 'nvim.dir' not found` や `Unknown function: netrw#Explore` といった失敗が起きる。CMake ベースのジョブは絶対パスで設定し `Xtest_xdg` 作業ディレクトリから実行するが、Zig ハーネスはこれを再現していない。Zig 機能テストは個々のテストによる cwd 変更とは独立してランタイム/ヘルパ/Lua モジュールを解決すべき。

**直近のアクティビティ:** bfredl が「おそらく `b.path(...)` 引数とともに `addPrefixedDirectoryArg`（または `?.lua` をサフィックスと見なす `Decorated`）を使うべき」とコメント。

**次のアクション:** bfredl の提案に沿った Zig ハーネスのパス指定修正。#39723 のブロッカーでもある。

---
### ![PR](icons/git-pull-request.svg) [build(deps): bump luajit to 8f421c81e](https://github.com/neovim/neovim/pull/39994) `#39994`
**ステータス:** Open | **ラベル:** build

**概要:** LuaJIT を `8f421c81e` へ更新する。`os.time()` のチェック整合、iOS のマクロ名衝突回避、`jit.util.tracesnap()` での PC 位置返却オプション、スタックオーバーフローの relimit 処理修正、FFI/MacOS の enum 向け呼び出し規約修正、各種 sanitizer 警告抑制、プロファイラスレッドの競合回避、FFI の各種 ABI・呼び出し規約修正などを含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし（最新コメントは 5/30、clason が「新コミットで別の macOS unittest 失敗が出た」と報告）。

**変更ファイル:** 1 ファイル（+2 / -2）。`cmake.deps/deps.txt`。

**次のアクション:** macOS unittest の失敗の解消とレビュー。

---
### ![PR](icons/git-pull-request.svg) [backport: vim-patch:9.2.0718: :syn sync without an argument also lists syntax cluster](https://github.com/neovim/neovim/pull/40406) `#40406`
**ステータス:** Merged | **ラベル:** target:release

**概要:** #40399 を `release-0.12` ブランチへバックポートする PR。引数なしの `:syn sync` が定義済みクラスタも列挙してしまう問題の修正を安定版へ反映する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル（+35 / -13）。`src/nvim/syntax.c`（+16 / -13）と `test/old/testdir/test_syntax.vim`（+19）。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0718: :syn sync without an argument also lists syntax cluster](https://github.com/neovim/neovim/pull/40399) `#40399`
**ステータス:** Merged | **ラベル:** vim-patch, ci:backport release-0.12

**概要:** 引数なしの `:syn sync` が定義済みクラスタもすべて列挙してしまう問題に対し、`syn_cmd_list()` の制御フローを修正し、`:syn sync` から呼ばれた際は同期項目のみを出力するようにする vim パッチ 9.2.0718 の取り込み。vim/vim#20614 をクローズ。

**直近のアクティビティ:** neovim-backports[bot]が `release-0.12` 向けバックポート PR #40406 の作成成功を報告。dmitmel が「v0.12.4 にバックポートできるか、何か手伝えるか」と質問。

**変更ファイル:** 2 ファイル（+35 / -13）。`src/nvim/syntax.c`（+16 / -13）と `test/old/testdir/test_syntax.vim`（+19）。

**次のアクション:** マージ・バックポート済み。リリースへの取り込み有無の確認。レビューは zeertzjq が APPROVED（コメント 2 件は本文空）。

---
### ![PR](icons/git-pull-request.svg) [backport: fix(lsp): fire LspNotify didChange autocmds after undo/redo](https://github.com/neovim/neovim/pull/40405) `#40405`
**ステータス:** Merged | **ラベル:** lsp, target:release

**概要:** #40404 を `release-0.12` ブランチへバックポートする PR。undo/redo 後に didChange の `LspNotify` autocmd を発火させる修正を安定版へ反映する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル（+15 / -9）。`runtime/lua/vim/lsp/client.lua`（+13 / -9）と `test/functional/plugin/lsp/semantic_tokens_spec.lua`（+2）。

**次のアクション:** マージ済みのため対応なし。

---
### ![Issue](icons/issue-opened.svg) [`nvim` prints semicolon-delimited strings and fails to start.](https://github.com/neovim/neovim/issues/33993) `#33993`
**ステータス:** Closed | **ラベル:** needs:repro

**概要:** NixOS 上の nvim 0.11.1 で `nvim --clean test.txt` を実行すると、`;80;159;2080;1908` のようなセミコロン区切りの数値を出力して終了する（出力は非決定的）。期待される動作は test.txt が開かれること。Vim 9.1.1336 では再現しない。

**直近のアクティビティ:** barrettruth が「nix を使っているが本日時点では再現できない。再試行して動けばクローズを」とコメント。justinmk が「起動時のターミナルクエリ周りの最近の作業で直った可能性があるので最新 nightly を試してほしい」とコメント。報告者 AgentElement が「v0.12.3 で修正されたようなのでクローズする」と返答しクローズ。

**次のアクション:** クローズ済みのため対応なし。

---
### ![Issue](icons/issue-opened.svg) [LSP: Buffer diagnostics are not set if cursor is not on the opened buffer during `textDocument/didOpen` with an LSP that uses `textDocument/diagnostics`](https://github.com/neovim/neovim/issues/40391) `#40391`
**ステータス:** Open | **ラベル:** lsp

**概要:** `textDocument/diagnostics` を使う LSP（例: eslint）で、バッファを開いた際にカーソルがそのバッファ上にないと、`LspNotify` の `textDocument/didOpen` メソッド呼び出し時にファイルへ診断が設定されない。原因は `vim.lsp.diagnostic._enable(bufnr)` のロジックで、生成された `LspNotify` autocmd 発火時の `bufnr` がカレントウィンドウのものになるため。解決案として `LspAttach` の利用、または `client` を `_enable` に渡すことを提案している。

**直近のアクティビティ:** clason が「`ts_query_ls` でクエリファイルを開き `(comment)` を `(omment)` に改名・保存して再オープンすれば再現できる。リグレッション元の #40332 は `release-0.12` にバックポート済みなので、バグ修正も同様にすべき」とコメント。jdrouhard が「リファクタがバグを直すならそれで良い。診断だけが特別扱いで散在しているので capability 化すれば簡素になる」と返答し、`_set_defaults()` を `_text_document_did_open_handler` の前に呼ぶ等の対応案を提示。報告者 ivangeorgiew は「まず `_set_defaults()` を前に移すバグ修正を先行し、capability 化は別機能とすべき」と主張。

**次のアクション:** バグ修正（`_set_defaults()` の呼び出し順変更）とリファクタ（診断の capability 化）の分離方針を確定し実装。jdrouhard が対応中とみられる。

---
### ![Issue](icons/issue-opened.svg) [lsp: unparseable globPattern (e.g. tsgo "bundled://...") aborts entire client/registerCapability](https://github.com/neovim/neovim/issues/40376) `#40376`
**ステータス:** Closed | **ラベル:** lsp

**概要:** `workspace/didChangeWatchedFiles` ウォッチャを `vim.glob.to_lpeg` が解析できない `globPattern` で登録する LSP サーバ（例: tsgo の `bundled:///libs/**/*`）が、`runtime/lua/vim/glob.lua:373` の `assert` に到達し、`client/registerCapability` リクエスト全体を中断させる。`_watchfiles.lua` の `register()` 内の各 `glob.to_lpeg(glob_pattern)` を `pcall` で包み、失敗時はログを 1 度出して `continue` する修正を提案。同じクラッシュ箇所として #37204・#37238 を挙げている。

**直近のアクティビティ:** 直近 24 時間のコメントなし（コメント 0 件）。#40396 でクローズ。

**次のアクション:** クローズ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [fix(lsp): make LspNotify more robust](https://github.com/neovim/neovim/pull/40332) `#40332`
**ステータス:** Merged | **ラベル:** lsp, ci:backport release-0.12

**概要:** `LspNotify` が autocmd 実行時にバッファを渡しておらず、バッファローカルな `LspNotify` 購読でイベントメタデータの buf が正しくなかった。また `schedule()` で包まれていたため実際の autocmd がイベントループ後まで遅延し、バッファ削除時の `textDocument/didClose` 通知がバッファ消滅後に発火する問題があった。本 PR は特定バッファに関わる LSP 通知では autocmd 実行時に buf を設定し、`vim.schedule` のラップを除去してインライン呼び出しする。

**直近のアクティビティ:** clason が「これが `ts_query_ls` の診断を壊した（bisect 済み）」とコメント。justinmk が「bisect ありがとう、#40391 が関連で jdrouhard が対応中と思われる」と返答。

**変更ファイル:** 6 ファイル（+27 / -29）。`runtime/lua/vim/lsp/client.lua`（+16 / -16）、`runtime/lua/vim/lsp/_changetracking.lua`、`runtime/lua/vim/lsp/semantic_tokens.lua`、`runtime/lua/vim/lsp.lua`、`runtime/doc/lsp.txt`、テスト。レビューは justinmk が APPROVED（"nice changes!"）と COMMENTED 各 1 件。

**次のアクション:** マージ済みだがリグレッション（#40391）を誘発したため、後続の修正で対応。

---
### ![PR](icons/git-pull-request.svg) [feat(lua): ordereddict](https://github.com/neovim/neovim/pull/35464) `#35464`
**ステータス:** Open | **ラベル:** lua, lsp

**概要:** より柔軟な「Log once」インターフェースが必要だが、`vim.ringbuf` はキー・バリューアクセスを持たず、最大値到達時にコールバック/フックで通知もしない。本 PR は `ordereddict` を導入する（代替案として `vim.ringbuf` の拡張も検討）。WIP として早期フィードバックを得るために投稿されている。

**直近のアクティビティ:** barrettruth が「ここでの `remove()` はまだ `O(k)` か。LinkedHashMap なら挿入・削除・取得すべて定数時間で保証される」とコメント。justinmk が「ここの実装は古いモデルが書いた vibe slop なので無視してほしい。少数の柔軟で高性能なデータ構造があると良く、だからこそ `vim.ringbuf` 拡張に興味があったが、悪手かもしれない」と返答。

**変更ファイル:** 5 ファイル（+184 / -23）。新規 `runtime/lua/vim/_ordereddict.lua`（+71）、`runtime/lua/vim/lsp/log.lua`（+21 / -20）、新規テスト `test/functional/lua/dict_spec.lua`（+89）など。レビューは lewis6991 が 5 件 COMMENTED（うち 1 件「特に大きな必要性は感じないが実装はシンプル」）。

**次のアクション:** ドラフト・WIP。実装方針（`ordereddict` 新設か `vim.ringbuf` 拡張か）の確定とレビュー。

---
### ![PR](icons/git-pull-request.svg) [feat(:restart)!: no bang restores session and window layout](https://github.com/neovim/neovim/pull/40321) `#40321`
**ステータス:** Open | **ラベル:** breaking-change

**概要:** 多くのユーザは `:restart` でセッションとウィンドウレイアウトを復元したいため、カスタムコマンドやマッピングを自作している。本 PR は `:restart` をセッション・ウィンドウレイアウト復元するよう変更し、従来の動作は `:restart!` として追加する。`v:startreason` に新しい値を導入し、`ZR` を `:restart!` にマップする。Lua 実装は mini.sessions から移植（MIT ライセンス、Codex による AI 支援）。#38940 をクローズ。

**直近のアクティビティ:** nathanzeng が、`:restart` 前にセッションファイルを書き出すべきかについて「自分はセッションを使わないので、利用者の意見が欲しい。セッションファイルはユーザ管理だと思って書き込まなかった」とコメント。`v:this_session` の確立した挙動の文書化とテスト追加も対応予定と述べている。レビューは多数（superatomic が APPROVED、justinmk・nathanzeng・superatomic・zeertzjq・echasnovski による大量の COMMENTED が継続。superatomic は「`runtime/doc/news.txt` の更新が漏れている」と指摘）。

**変更ファイル:** 13 ファイル（+214 / -59）。`runtime/lua/vim/_core/server.lua`（+61）、`src/nvim/ex_docmd.c`（+17 / -1）、`test/functional/terminal/tui_spec.lua`（+111 / -38）が中心。`runtime/doc/editing.txt`/`gui.txt`/`vvars.txt` 等も更新。

**次のアクション:** `news.txt` 更新、`v:this_session` のテスト追加、エスケープ整理など残レビューコメントへの対応とマージ。

---
### ![Issue](icons/issue-opened.svg) [`sudo make install` breaks subsequent `make` invocations when no `.git` directory is present](https://github.com/neovim/neovim/issues/32655) `#32655`
**ステータス:** Closed | **ラベル:** build

**概要:** `.git` ディレクトリを含まないクローン（例: `--colocate` なしの jj 利用）でビルド後に `sudo make install` を実行すると、一部のビルドファイルが root 所有になり、以降の `make` が `version.i` を `Operation not permitted` で開けず失敗する。`.git` の有無にかかわらず `sudo make install` 後の `make` が失敗しないことが期待される。

**直近のアクティビティ:** barrettruth が「同じ jj ユーザだが、`79c8159f41` 時点では再現しない」とコメント（これがクローズに至る唯一の最近のコメント）。

**次のアクション:** クローズ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [feat(runtime): directory browser (replace netrw with a very small script)](https://github.com/neovim/neovim/pull/39723) `#39723`
**ステータス:** Open | **ラベル:** runtime, AI assisted 🤖

**概要:** 現状 `:edit <dir>` や `nvim <dir>` はディレクトリ内容の表示を netrw に依存している。本 PR はローカルディレクトリバッファ向けに小さな組み込み Lua ディレクトリリスティングを追加し、`filetype=directory`、エントリのオープン、親ディレクトリへの移動、リフレッシュを提供する。`netrw` は `:Explore`・リモートパス・アーカイブ・ファイル操作向けに残す。#31621・#35747 をクローズ。

**直近のアクティビティ:** barrettruth が「we ready 😎」とコメントし、マージ準備完了を示唆。直前には「Zig がパス問題で失敗している。残る失敗は swapfile-transient のみで CI 再実行で通った。非ブロッカーのためこれらのテストはスキップし #40381 を作成した」とコメント。

**変更ファイル:** 9 ファイル（+720 / -103）。新規 `runtime/lua/nvim/dir.lua`（+248）、新規 `runtime/plugin/dir.lua`（+74）、新規 `runtime/syntax/directory.vim`（+13）、新規テスト `test/functional/plugin/dir_spec.lua`（+328）、`runtime/doc/usr_22.txt`（+15 / -88）など。レビューは膨大（justinmk が複数回 APPROVED、superatomic が APPROVED 後に CHANGES_REQUESTED、barrettruth/yochem/comfysage 含め多数の本文空 COMMENTED が長期間継続）。

**次のアクション:** Zig パス問題（#40381）はスキップで回避済みのため、最終レビュー承認とマージ。

---
### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40384) `#40384`
**ステータス:** Open | **ラベル:** vim-patch

**概要:** vim パッチの取り込み状況を記録する自動生成 PR（marvim による）。`ccdc81701 CI: Restore daily Coverity Scan`、`9.2.0710: GTK4 GUI resize handling can be improved`、`9.2.0712: GTK4: dialogs not handling mnemonics correctly` を `src/nvim/version.c` に反映する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+18 / -16）。`src/nvim/version.c`。

**次のアクション:** ドラフト状態。対象パッチの取り込み判断後にマージ。

---
### ![PR](icons/git-pull-request.svg) [fix(ui): track detected background per UI](https://github.com/neovim/neovim/pull/40388) `#40388`
**ステータス:** Open | **ラベル:** defaults, ui, image

**概要:** UI ごとに「自分の」カラースキームや背景という状態概念がない。これは検出されたターミナル背景がグローバル状態に記録されるため。ターミナル応答を生成元の TTY UI チャンネルに紐づけられるようになったので、本 PR は dark/light の検出結果を `RemoteUI` 経由で対応 UI に保存し、`nvim_list_uis` の新フィールドとして公開する。エンドユーザから見た挙動は変えず、「per-ui」オプションパイプラインも実装しない（同一サーバに接続した 2 つのリモート UI は依然として同じ背景・カラースキームを共有する）。#31652 に関連、#40356 に依存。

**直近のアクティビティ:** コメントなし。barrettruth がセルフレビューで複数回 COMMENTED（6/24〜6/25、本文は空）、zeertzjq が 1 回 COMMENTED（本文は空）。

**変更ファイル:** 16 ファイル（+254 / -98）。`src/nvim/api/events.c`（+85 / -1）、`runtime/lua/vim/_core/defaults.lua`（+8 / -85）、`src/nvim/ui_defs.h`（+8）、`test/functional/api/ui_spec.lua`（+59 / -4）など。

**次のアクション:** #40356 はマージ済みのため、レビューコメントへの対応とレビュー完了、マージ。

---
### ![PR](icons/git-pull-request.svg) [feat(lua): add job and detached modes to vim.system](https://github.com/neovim/neovim/pull/40267) `#40267`
**ステータス:** Closed | **ラベル:** lua

**概要:** `vim.system()` は生の `uv.spawn`（mode='process'）しかサポートせず、`SystemObj:kill()` が子孫プロセスを終了できずプロセスツリーが孤児化する。`jobstart`/`jobstop` と異なり子プロセスの teardown 所有権がない。本 PR は内部のジョブ制御層を使う mode='job' と、teardown 所有権なしでプロセスグループ kill する mode='detached' を追加し、既定の mode='process' は従来動作を維持する。#39552 の解決を試みたもの（過去の試み #40209）。

**直近のアクティビティ:** 作者 ManojPanda3 が「時間を無駄にしてすみません。専門家が引き継げるようこの PR をクローズします。自分の提案はプロジェクトの目標と合致していないと思う」とコメントしてクローズ。

**変更ファイル:** 11 ファイル（+601 / -280）。`runtime/lua/vim/_core/system.lua`（+179 / -253）、`src/nvim/api/vim.c`（+104）、新規 `src/nvim/jobs.c`（+59）/`src/nvim/jobs.h`（+27）など。レビューは justinmk・ManojPanda3 が各 1 件 COMMENTED（本文は空）。

**次のアクション:** クローズ済み。#39552 は別の担当者による対応が必要。

---
### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0725: [security]: Stack out-of-bounds write in spell_soundfold_sal()](https://github.com/neovim/neovim/pull/40402) `#40402`
**ステータス:** Merged | **ラベル:** test, vim-patch, AI assisted 🤖

**概要:** 細工された spell ファイルの非 collapsing SAL ルールにより `soundfold()` が MAXWLEN 結果バッファの末尾を 1 バイト超えて書き込めるセキュリティ問題（GHSA-m3hf-xcm3-xhm2）への vim パッチ 9.2.0725 の取り込み。単一バイト SAL 結果と終端 NUL の書き込みを MAXWLEN - 1 に制限する。ただし Nvim には `!has_mbyte` パスでのみ使われる `spell_soundfold_sal()` が存在しないため修正自体は N/A で、本 PR はテストのみを追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+25）。`test/old/testdir/test_spellfile.vim` のみ。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0717: tests: strange indent in Test_autocmd_dup_arg()](https://github.com/neovim/neovim/pull/40400) `#40400`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** v9.2.0708 以降の `Test_autocmd_dup_arg()` のインデントが不自然になっていた問題に対し、modeline の設定でインデントし直す vim パッチ 9.2.0717 の取り込み。vim/vim#20619 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+28 / -28）。`test/old/testdir/test_autocmd.vim`。

**次のアクション:** マージ済みのため対応なし。

---
### ![Issue](icons/issue-opened.svg) [Implement `--remote-wait`](https://github.com/neovim/neovim/issues/24788) `#24788`
**ステータス:** Open | **ラベル:** remote

**概要:** `:h --remote-wait` が「Not yet supported by Nvim」となっている。`nvim --remote` は起動後すぐ終了するため git エディタとして使えず「Aborting commit due to empty commit message」になる。`--remote-wait` が Nvim でサポートされることが期待される。

**直近のアクティビティ:** gitmpr が「Neovim 0.12 が出荷され、#37951 の eager-merge の前提条件が満たされた。あの PR はまさにこれ（既定で `--remote` をブロックし `+async` でオプトアウト）を実装し好意的レビューも得たが、リベース中に作者が沈黙し bot により自動クローズされた。`nvr`（mhinz/neovim-remote）は 2022 年 5 月以降コミットがなく事実上放棄されており、`--remote-wait` だけが残る未対応機能。#37951 の設計を引き継ぐ新 PR を歓迎してもらえるか」とコメント。justinmk が「#37951 を見直す予定。能動的で応答性の高い復活を歓迎する」と返答。

**次のアクション:** #37951 の設計を引き継ぐ新 PR の作成可否を担当者間で調整。

---
### ![PR](icons/git-pull-request.svg) [fix(ui): avoid OSC 11 background races](https://github.com/neovim/neovim/pull/40394) `#40394`
**ステータス:** Open | **ラベル:** defaults, ui

**概要:** OSC/リモート UI 関連の一連の変更（#40356 でソースチャンネルを `TermResponse` に含める、#40388 で OSC11 検出の dark/light を保存）を踏まえ、TTY UI ごとに正しい dark/light を割り当てる。本 PR はアクティブな TTY UI の検出背景のみが自動グローバル `'background'` 値を更新できるようにする（他の TTY UI は自分の検出メタデータは更新できるがグローバル背景の競争には勝てない）。`'background'`・ハイライト・カラースキーム・レンダリングを UI ローカル化したり per-UI オプションパイプラインを導入したりはしない。#40388 に依存、#40175 に関連。

**直近のアクティビティ:** justinmk が「まず `nvim__ui_get_detected_background` 等がなぜ必要か。PR やコミットに手がかりがない。次に、これは一見複雑で新しいレイヤーを追加しているように見える。`nvim_set_option` などがチャンネルをチェックできないのか、なぜ新 API が必要か」と質問。barrettruth が「#40388 がマージされたら改めて話そう。コメントは検討する」と返答。

**変更ファイル:** 8 ファイル（+254 / -55）。`runtime/lua/vim/_core/defaults.lua`（+84 / -53）、`src/nvim/api/ui.c`（+43 / -1）、`runtime/doc/api.txt`（+21 / -1）など。

**次のアクション:** ドラフト。依存先 #40388 のマージ後に再議論し、新 API の必要性に関する justinmk の疑問への回答とレビューを進める。

---
### ![PR](icons/git-pull-request.svg) [feat(messages) add nil to validate error message if arg is optional](https://github.com/neovim/neovim/pull/40398) `#40398`
**ステータス:** Closed | **ラベル:** なし

**概要:** 引数がオプショナルな場合、バリデータを満たさないときに `vim.validate` が生成するエラーメッセージにその旨が反映されない。本 PR は `is_valid` 関数に `optional` パラメータを追加し、その値に基づいて型不一致エラーメッセージを構築する。#40037 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+11 / -14）。`runtime/lua/vim/_core/shared.lua`。

**次のアクション:** ドラフトのままクローズ済みのため対応なし。

---
### ![Issue](icons/issue-opened.svg) [Resize windows "upwards"](https://github.com/neovim/neovim/issues/6645) `#6645`
**ステータス:** Open | **ラベル:** ui

**概要:** ウィンドウのリサイズは常に「下方向」に作用し、ウィンドウを拡大/縮小すると下のバッファが縮小/拡大される（下に空きがない場合のみ上が縮む）。しかし上のバッファを縮小したい場合もある。`:resize up +3` のように上方向にリサイズするフラグがあってもよいのではという提案。

**直近のアクティビティ:** XiaowenHu96 が「これを引き受けたい。`nvim_win_set_height`/`nvim_win_set_width` はどちらも `FUNC_API_SINCE(1)`。固定アンカーを選べ既定で現行動作にできる新 API `nvim_win_resize(win, {height, width, anchor})` を提案する。これで `resize up +n` も実現できる」とコメント。justinmk が「いいね。それで `nvim_win_set_height`/`nvim_win_set_width` を非推奨にできるか」と返答。

**次のアクション:** 新 API `nvim_win_resize` の設計確定（既存 API 非推奨化の可否を含む）と PR 作成。

---
### ![Issue](icons/issue-opened.svg) [LSP: `lsp.log` gets filled with `"semantic_tokens"	{ code = -32801, message = "Content modified."` on fast text change](https://github.com/neovim/neovim/issues/40208) `#40208`
**ステータス:** Open | **ラベル:** performance, bug-regression, lsp, logging

**概要:** #38354 以降セマンティックトークンのエラーがログ出力されるようになったが、その結果 `lua-language-server`（他の LSP も同様の可能性）使用時に高速なテキスト変更で `Content modified` エラーが `lsp.log` を埋め尽くす。これらは実際にはエラーではない。解決策として `semantic_tokens.lua` の `vim.lsp.log.error("semantic_tokens", err)` 行を削除するか `log.debug`/`log.info` などに変更する。

**直近のアクティビティ:** justinmk が LuaLS の議論からのメモとして「LuaLS の特定コミット以降リクエストに応答が返らないようで、Neovim はこの状況をまだ処理しておらず `runtime/lua/vim/lsp/rpc.lua` に TODO が残っている」と引用付きでコメント。

**次のアクション:** エラーログのレベル変更（または削除）、および応答が返らないケースの `rpc.lua` の TODO 対応。

---
### ![Issue](icons/issue-opened.svg) [`tags_lsp_to_vim` crashes when LSP server sends `"tags": null`](https://github.com/neovim/neovim/issues/40390) `#40390`
**ステータス:** Open | **ラベル:** lsp

**概要:** `runtime/lua/vim/lsp/diagnostic.lua` の `tags_lsp_to_vim` は `ipairs(diagnostic.tags or {})` を使うが、`or` は Lua の `nil` しか捕捉しない。サーバが `"tags": null` を送ると Neovim の JSON デコーダが `vim.NIL`（truthy な userdata）を生成し、`vim.NIL or {}` が `vim.NIL` を返して `ipairs()` が `table expected, got userdata` でクラッシュする（`psalm-language-server` で確認）。`"tags": null` は欠落した `tags` フィールドと同じに扱われるべき。

**直近のアクティビティ:** kimabrandt が「LSP 仕様で `tags: null` は許可されていないと思う」とし、`vim.isnil` を使う防御的修正、`type(...) == 'table'` を使う修正、`assert` で源流（psalm）に修正を促す修正の 3 案を diff 付きで提示し、`vim.diagnostic.open_float()` の `relatedInformation` でも同種の問題があると報告。justinmk が「LSP 仕様で許可されないなら、情報的メッセージで fail fast する（=assert）方が一般に望ましい」とコメント。barrettruth は「アプローチ 3 が最もきれい」とコメント。

**次のアクション:** assert ベース（アプローチ 3）の修正を PR 化。`relatedInformation` 側も同様に対応。

---
### ![PR](icons/git-pull-request.svg) [fix(ui): clear option query message tail](https://github.com/neovim/neovim/pull/40397) `#40397`
**ステータス:** Closed | **ラベル:** ui

**概要:** `ext_multigrid` と `ext_cmdline` が有効なとき、短いオプションクエリメッセージがメッセージグリッド上に直前のより長いメッセージの古いテキストを残してしまう。本 PR は直接的なオプションクエリ出力の後にメッセージ領域の残りをクリアする。リンク先 issue の akiyosi による再現手順で修正を確認済み。#40292 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル（+21）。`src/nvim/option.c`（+2）と `test/functional/ui/multigrid_spec.lua`（+19）。

**次のアクション:** クローズ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [feat(vim.net): add response status code and headers in on_response callback](https://github.com/neovim/neovim/pull/39685) `#39685`
**ステータス:** Open | **ラベル:** なし

**概要:** #38946 の継続イテレーション。`on_response` コールバックに `status` と `headers` を追加し、HTTP ステータスコード用の新しい `vim.net.status` enum を追加し、テストを増やす。

**直近のアクティビティ:** 直近 24 時間のコメントなし（最新の活動は 6/11 の ellisonleao による COMMENTED）。レビューは deathbeam・yochem・justinmk・ellisonleao による多数の COMMENTED（いずれも本文は空）。

**変更ファイル:** 4 ファイル（+128 / -44）。`runtime/lua/vim/net.lua`（+85 / -12）、`test/functional/lua/net_spec.lua`（+28 / -22）、`runtime/doc/lua.txt`、`runtime/doc/news.txt`。

**次のアクション:** レビューコメントへの対応とレビュー完了、マージ。

---
### ![PR](icons/git-pull-request.svg) [docs: nvim_open_term, terminal jobs, misc](https://github.com/neovim/neovim/pull/40214) `#40214`
**ステータス:** Open | **ラベル:** なし

**概要:** 本文の記載なし。タイトルと変更ファイルから、`nvim_open_term`、ターミナルジョブ、その他に関するドキュメント整備の PR と判断される。`runtime/doc/terminal.txt` への追記や `runtime/doc/api.txt`、各種 `_meta` 生成ファイルの更新が中心。

**直近のアクティビティ:** 直近 24 時間のコメントなし（最新の活動は 6/21 の zeertzjq による COMMENTED）。レビューは justinmk・zeertzjq による COMMENTED（いずれも本文は空）。

**変更ファイル:** 27 ファイル（+394 / -230）。`runtime/doc/api.txt`（+65 / -33）、`runtime/doc/terminal.txt`（+55）、`runtime/lua/vim/_meta/api.gen.lua`（+55 / -24）、`src/nvim/api/vim.c`（+50 / -21）、`src/nvim/eval.lua`（+21 / -17）など、ドキュメントと生成ファイルが中心。

**次のアクション:** ドラフト状態。レビュー完了後にマージ。

---
### ![Issue](icons/issue-opened.svg) [position of `syntax on` in `init.vim` affects other options `eol`/`noeol`/`fixeol`/`nofixeol`](https://github.com/neovim/neovim/issues/40170) `#40170`
**ステータス:** Open | **ラベル:** has:workaround, lifecycle, treesitter, events

**概要:** Neovim 0.12 で `init.vim` 中の `syntax on` の位置が `eol`/`noeol`/`fixeol`/`nofixeol` の挙動に影響する。`syntax on` を先頭に置くと、`.md`/`.lua` など特定拡張子のファイルで末尾改行付きファイルを開いた際に改行が除去されてしまう（`syntax on` を最後に置くと期待通り）。Vim では一度も見られない挙動。期待は `syntax on` の位置によらず両ケースとも `endofline` になること。

**直近のアクティビティ:** 報告者 v0lker が回避策に関するやり取りで「`$VIMRUNTIME/ftplugin/markdown.lua` の削除は markdown のみ・一時的で、ハイライトも失う。`syntax on` を設定に書かないのが最良の回避策のようだ」とコメント。さらに「`syntax on` のアクションが（無効化しない限り）設定読込後に実行されるなら、設定内の `syntax on` は無視（または警告）すべきでは」と問題提起している。

**次のアクション:** `syntax on` の実行タイミングとオプション設定順序の相互作用の根本対応の検討（警告の是非を含む）。回避策ありのラベル付き。

---
### ![PR](icons/git-pull-request.svg) [refactor(stl): rename `redraw_custom_statusline`, `win_redr_custom`](https://github.com/neovim/neovim/pull/40393) `#40393`
**ステータス:** Merged | **ラベル:** refactor

**概要:** 組み込みステータスラインは常に「custom」であるため `redraw_custom_statusline` と `win_redr_custom` という名前は誤解を招く。`'tabline'` や `'ruler'` が同様に移行されればこの用語はさらに無意味になる。本 PR はこれらをリネームする。#40247 関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル（+9 / -24）。`src/nvim/statusline.c`。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [fix(autocmd): no cleanup for redraw curwin change](https://github.com/neovim/neovim/pull/40389) `#40389`
**ステータス:** Merged | **ラベル:** display, events

**概要:** 再描画目的でカレントウィンドウを復元する際に（`28ffb334` 以降）不要・想定外のクリーンアップが発生していた。本 PR は `autocmd_restbuf()` を呼ばずに直接カレントウィンドウを復元するよう修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。レビューは justinmk が 2 件 COMMENTED（いずれも本文は空）。

**変更ファイル:** 6 ファイル（+44 / -19）。`src/nvim/autocmd.c`（+9 / -6）、`src/nvim/drawscreen.c`（+8 / -4）、`src/nvim/statusline.c`（+8 / -4）、`test/functional/api/autocmd_spec.lua`（+16）など。

**次のアクション:** マージ済みのため対応なし。

---
### ![Issue](icons/issue-opened.svg) [vim.pack: set filetype=gitcommit for K diff](https://github.com/neovim/neovim/issues/40239) `#40239`
**ステータス:** Open | **ラベル:** ui, lsp, treesitter, packages

**概要:** K で表示される diff が `filetype=markdown` の markdown「フェンス」（` ```diff `）内に表示される。実際の diff 内容を表示し `filetype=gitcommit` を設定すべきではという提案。

**直近のアクティビティ:** echasnovski が、`MarkedString` は `@deprecated`（`MarkupContent` 推奨）で、`MarkupContent` は plaintext/markdown しかサポートしないため `vim.lsp.buf.hover` 拡張は不適と説明し、解決策として「1. `diff` tree-sitter parser+queries をバンドル」を最善とし、ほか in-process LSP のハック等 5 案を列挙。短期的には diff ハイライトに `diff` parser+queries が必要と文書化、長期的にはバンドルか公式推奨インストール手段を提案。justinmk が「同意。`diff` ts parser のバンドルに賛成だが、clason が指摘した問題があった気がする」と返答。

**次のアクション:** `diff` tree-sitter parser+queries のバンドル可否（clason 指摘の問題の確認）の判断と、短期的なドキュメント追記。

---
### ![PR](icons/git-pull-request.svg) [fix(tty): filter terminal probes by channel](https://github.com/neovim/neovim/pull/40356) `#40356`
**ステータス:** Merged | **ラベル:** defaults, image

**概要:** `nvim_ui_send()` で送られるターミナルプローブが複数の stdout TTY UI に届きうる。既知の TTY UI オーナーを持つプローブは、無関係な UI チャンネルからの `TermResponse` を受け付けるべきでない。本 PR は既存の `chan` フィルタを所有プローブに通し、起動/アタッチ時の背景検出、フォールバックの truecolor 検出、`vim.tty.query()` の `vim.tty.request()` へのオプション転送をカバーする。OSC 52（クリップボード）検出やペーストはフォローアップとして残す。#40330 に依存。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 4 ファイル（+37 / -16）。`runtime/lua/vim/tty.lua`（+18 / -2）、`runtime/lua/vim/_core/defaults.lua`（+14 / -10）、`runtime/lua/vim/ui/img/_kitty.lua`、`runtime/lua/vim/ui/img.lua`。

**次のアクション:** マージ済み。OSC 52 検出/ペーストの対応は後続 PR（#40394 等）で継続。

---

## nvim-telescope/telescope.nvim

> Plenary の archive を見据えた依存整理（vendor 化）の大型 PR が単独で進行中。

### ![PR](icons/git-pull-request.svg) [feat: vendor and clean up plenary](https://github.com/nvim-telescope/telescope.nvim/pull/3647) `#3647`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** Plenary が archive される見込みを受け、telescope が Plenary から実際に使っている部分を調査し、現行 Neovim API で置き換えられる箇所を整理する PR。`class.lua`・`path.lua`・`scandir.lua` の利用を排除し、`border`/`popup`・`job`・`log`・`async` を整理して拡張機能向けに公開する計画。モジュールは `plenary` と衝突しないよう `neoplen` にリネームし、FFI も廃止。`scandir.lua` が使われていたディレクトリプレビューは、メンテナンスされていない telescope-file-browser 拡張でのみ使われるためコア対象外と判断。`job` は将来 `vim.system` の薄いラッパーに、`log`/`async` は upstream の `vim.async`/`vim.log` 安定後に移行予定。テスト専用依存として外部の `plentest.nvim` を導入。

**直近のアクティビティ:** emmanueltouzery がこのブランチへ移行する中で `lua/telescope/make_entry.lua` 281 行目のバグ（`fn.isabsolutepath(t.filename)` は 0/1 を返すため `== 1` 比較が必要、0 は truthy）を報告。作者 clason が修正済みと応答し、ブランチのテストに感謝を述べている。

**変更ファイル:** 76 ファイル変更（+3156/-596）。`lua/neoplen/job.lua`（+678）、`lua/neoplen/popup.lua`（+418）、`lua/neoplen/border.lua`（+304）などを新規追加し、`lua/telescope/utils.lua`（+73/-38）や各 builtin・previewer を修正。テストは `tests/automated/` へリネーム。

**次のアクション:** Draft のため、`class`/`path`/`scandir` 利用の完全排除と、拡張機能が依存する関数の復元を進める。外部テスターからのフィードバック反映も継続。

---

## ghostty-org/ghostty

> 非メンテナーの issue 自動クローズが続く一方、QuickTerminal のタブ対応という大型 PR が複数の承認を得てリリース待ち。折り返し行でのカーソル移動の不具合報告も継続。

### ![Issue](icons/issue-opened.svg) [Keyboard input from remote control apps (CGEvent-injected NSEvents) not processed](https://github.com/ghostty-org/ghostty/issues/13087) `#13087`
**ステータス:** Closed | **ラベル:** なし

**概要:** リモートコントロールアプリ（CGEvent で注入される NSEvent）からのキーボード入力が処理されないという issue。本文は「Test」のみ。

**直近のアクティビティ:** ghostty-vouch[bot]が「非メンテナーは issue を作成できず、まず discussion を作成してほしい（理由は #3558 と CONTRIBUTING.md 参照）」と案内し、自動クローズした。

**次のアクション:** クローズ済み（運用方針による自動クローズ）。対応不要。

---
### ![Issue](icons/issue-opened.svg) [Additional context and explanation regarding the reported issue](https://github.com/ghostty-org/ghostty/issues/13085) `#13085`
**ステータス:** Closed | **ラベル:** なし

**概要:** discussion #13083 から転記された投稿で、報告者が「これは単なる AI 生成 issue ではなく、複数回遭遇して作業に深刻な影響が出た。Zig に不慣れなため AI を使って数ラウンドかけて問題を追跡・要約した」と弁明している内容。

**直近のアクティビティ:** 00-kat が「修正してほしいなら AI 支援なしで自分で書いた新しい discussion を開いてほしい」とコメント。ghostty-vouch[bot]が非メンテナーの issue 作成不可を案内し自動クローズした。

**次のアクション:** クローズ済み。修正を望む場合は AI 支援なしの discussion 起票が必要。

---
### ![Issue](icons/issue-opened.svg) [Click to move cursor doesn't work properly when command is too long](https://github.com/ghostty-org/ghostty/issues/2582) `#2582`
**ステータス:** Open | **ラベル:** なし

**概要:** 複数行に折り返す長いコマンドで `alt+click` によるカーソル移動が正しく動作しない issue。カーソルが誤った行に飛んだり、画面最終列に飛んで他行へ移動できなくなったりする（動画で実演）。

**直近のアクティビティ:** Jaharmi が、複数の TUI エージェントツール（Pi・Hermes・Claude Code）で click-to-move-cursor が動かないと報告。Apple Terminal では全コンテキストで Option-click 再配置が効くが、これらの TUI のテキスト入力エリアでは効かず矩形選択になると述べ、OSC 133 や `cl=line`、プロンプト検出・マウスレポートに言及しつつ、グローバルまたは実行ファイル単位で「矢印キー送出」方式に切り替えるモードが欲しいと要望している。

**次のアクション:** 折り返し行でのカーソル移動ロジックの修正、および TUI プロンプト向けの代替挙動の検討。

---
### ![PR](icons/git-pull-request.svg) [feat(macos): add tab support for QuickTerminal - continued](https://github.com/ghostty-org/ghostty/pull/9857) `#9857`
**ステータス:** Open | **ラベル:** os/macos

**概要:** QuickTerminal にタブサポートを追加する PR（#8469 の継続で、これをマージすると #8469 もマージ扱いになる）。マージ衝突を解消し、複数タブが開いている時の cmd+w によるタブクローズ体験を改善（残り 1 タブなら QuickTerminal を隠す既存挙動）。`goto_tab:N`・previous/next_tab キーバインド対応、File => New Tab 対応、タブのドラッグ&ドロップによる別ウィンドウ化や他ウィンドウのタブバーへの追加、状態復元、タブタイトル/色の変更、glass 効果などを実装。ドラッグ&ドロップは SwiftUI の制約が大きく AppKit の NSDrag を使用。Claude Code を活用して多くの修正を実装したと開示。

**直近のアクティビティ:** memdealer が「数週間ブランチを使っているが快適。リリース予定に含まれるか」とコメント。レビューは多数で、aeroith・bugrauluyurt・wammy67 が APPROVED、bo2themax は複数回の CHANGES_REQUESTED 後に APPROVED（Liquid Glass での active/inactive タブの視認性、undo/redo 非対応、検索オーバーレイ上でのドロップ挙動、SwiftLint 違反など多数の指摘を経て解消）。

**変更ファイル:** 19 ファイル変更（+2129/-88）。新規 `QuickTerminal/Tab/QuickTerminalTabManager.swift`（+614）、`QuickTerminalTabBarView.swift`（+386）、`QuickTerminalTabDragDelegate.swift`（+267）、`QuickTerminalTabItemView.swift`（+163）などを追加し、`QuickTerminalController.swift`（+136/-38）等を修正。

**次のアクション:** 複数の APPROVED を得ており、メンテナーによる最終確認とリリース判断。

---

## astral-sh/uv

> `uv tree` の JSON 出力とそれに伴うセマンティクス課題、スコープ付き依存オーバーライド/除外のスタック PR 群、ロックファイルのマーカー簡約といった解決系の機能が活発。ホイール検証の厳格化や `uv check` のロック済み ty 利用も進む。

### ![PR](icons/git-pull-request.svg) [Add json output to `uv tree`](https://github.com/astral-sh/uv/pull/19978) `#19978`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv tree` に JSON 出力を追加する PR で、先行する #19904 を `uv workspace metadata` 形式を用いて作り直したもの。実際のセマンティクスを記述しようとする過程で多数の課題（#19972, #19973, #19975, #19976）に直面したため、それらは当面先送りし、まずはこの機能をマージしてユースケースをアンブロックすることを提案している。作者は導入した doc-comment が本 PR の意図する挙動であるという点のみを強く主張しており、セマンティクスやテキスト出力との整合性については議論の余地があると認めている。

**直近のアクティビティ:** astral-sh-bot[bot]によるテストインベントリ変更（test inventory: +22/-1 tests across 7 suites）。`uv::project::tree` に `json_output` 系を中心とした多数のテストが追加されている。

**変更ファイル:** 10 ファイル変更（+2454/-57）。`crates/uv-resolver/src/lock/tree.rs`（+670）と `crates/uv/tests/project/tree.rs`（+1558）が中心で、`crates/uv-resolver/src/lock/export/metadata.rs`、`crates/uv-cli/src/lib.rs`、`crates/uv/src/commands/project/tree.rs`、`crates/uv/src/settings.rs` などにも変更がある。

**次のアクション:** セマンティクス（先送りした課題の扱いやテキスト出力との整合性）についてレビュアーの合意を得る必要がある。

---
### ![Issue](icons/issue-opened.svg) [Add --output-format= to check subcommand](https://github.com/astral-sh/uv/issues/19985) `#19985`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv check` と `uv format --check` が `--output-format=` 引数をサポートしていないという要望。これらが内部で呼び出すツール（`ty` と `ruff`）は当該引数をサポートしている。フォーマットチェックでは `uv format --check -- --output-format=...` のように追加引数を渡せるが、`uv check` ではそれすらも機能しない。

**直近のアクティビティ:** zanieb が、複数の子ツールをサポートしたり複数の ty 呼び出しを束ねたりする可能性があるため、子プロセスの出力を正しく収集・表現できると確信できるまで実装は待つ必要があるとコメントしている。

**次のアクション:** 子ツールの出力を正しく集約・表現する方法を固めてから実装に着手する。

---
### ![Issue](icons/issue-opened.svg) [`uv add` adds `[[tool.uv.index]]` entry when `UV_INDEX` is set](https://github.com/astral-sh/uv/issues/13064) `#13064`
**ステータス:** Open | **ラベル:** bug, needs-decision

**概要:** モノレポで内部リポジトリにパッケージをホストし、各 `pyproject.toml` にインデックス設定を複製しないよう `UV_INDEX` 環境変数を設定している構成での報告。この状態で `uv add` を実行すると、`UV_INDEX` で指定したインデックスを指す `[[tool.uv.index]]` セクションが `pyproject.toml` に追記されてしまう。これを抑止する方法が欲しいという要望。

**直近のアクティビティ:** andyblackheel が、modern clap の `ArgMatches::value_source` で値の由来（`ValueSource::CommandLine` / `EnvVariable` / `DefaultValue`）を区別できるため、`--index` ではなく `UV_INDEX` 由来の場合にエントリ追加をスキップするのは実現可能だと指摘。clap 3.0 以降で利用でき uv は既に clap 4.5.17 なのでアップグレード不要だが、derive API では `ArgMatches` を直接参照する配線が必要とのこと。zanieb は環境変数処理を Clap の外に出してきた経緯があると応答している。

**次のアクション:** 値の由来に基づいてインデックス追記をスキップする設計の是非を判断する（needs-decision）。

---
### ![Issue](icons/issue-opened.svg) [uv resolves torchvision to 0.27.1+df56172 on Linux from the PyTorch CPU index](https://github.com/astral-sh/uv/issues/19988) `#19988`
**ステータス:** Open | **ラベル:** bug

**概要:** PyTorch CPU インデックス向けの推奨 `tool.uv.sources` 設定を使うと、`torch` は正しく `2.12.1+cpu` に解決されるが、`torchvision` が Linux CPU ビルド（`+cpu`）ではなく Windows ARM のローカルバージョンビルド（`0.27.1+df56172`、`win_arm64` のみ）に解決されてしまう。その結果、現在のプラットフォーム向けの配布物がないとしてインストールが失敗する。uv 0.9.0 から 0.11.24 へのアップグレードやキャッシュ削除を行っても挙動は変わらない。

**直近のアクティビティ:** charliermarsh が、当該 `win_arm64` ホイールが現在インデックスに誤って含まれているのではないかと @atalman に確認。ローカルバージョンは辞書順でソートされる仕様のため技術的には正しい挙動であり、`required-environments` を Linux に設定することで回避できる可能性があると述べている。

**次のアクション:** インデックス側に誤って含まれたホイールの確認・対処と、`required-environments` 設定による回避策の提示。

---
### ![PR](icons/git-pull-request.svg) [Support scoped dependency overrides](https://github.com/astral-sh/uv/pull/19970) `#19970`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 現在の `override-dependencies` は全オーバーライドをグローバルに適用するため、ある依存パッケージの特定バージョンに対する要件だけを直接要件や他パッケージの要件に影響を与えずにパッチできない。本 PR は既存の要件文字列と並べて構造化エントリ（`{ name = "anyio", version = "3.7.0", requires-dist = ["idna==3.2"] }` など）を記述できるようにする。構造化エントリはマッチするパッケージが宣言する依存にのみ `requires-dist` オーバーライドを適用し、version は省略可能（省略時は全バージョンにマッチ）、厳密バージョン指定がバージョンレス指定より優先され、スコープ付き要件がグローバルオーバーライドより優先される。両形式は共通表現に落とし込まれ、リゾルバ・lookahead・インストール済み環境の鮮度チェック・pip ワークフロー・ロックファイル永続化で使われる。

**直近のアクティビティ:** astral-sh-bot[bot]のテストインベントリ（test inventory: +3/-2 tests across 3 suites、`uv::lock` にスコープ付きオーバーライドのテスト追加）。レビューは zsol が CHANGES_REQUESTED、konsti-openai と charliermarsh が COMMENTED（いずれも本文なし）。

**変更ファイル:** 30 ファイル変更（+1078/-262）。`crates/uv-configuration/src/overrides.rs`（+220）が中核で、`crates/uv/tests/lock/lock.rs`（+259）、`crates/uv/src/commands/project/mod.rs`、`crates/uv/src/settings.rs`、`crates/uv-resolver/src/resolver/urls.rs`、`docs/concepts/resolution.md`、`uv.schema.json` などに変更がある。

**次のアクション:** zsol の変更要求に対応する必要がある。

---
### ![PR](icons/git-pull-request.svg) [Reject wheels with multiple .dist-info directories](https://github.com/astral-sh/uv/pull/19986) `#19986`
**ステータス:** Open | **ラベル:** compatibility

**概要:** 複数の dist-info ディレクトリは別パッケージの存在を装うことになり紛らわしく、正しさの問題を引き起こしうる。これらはホイール仕様で許可されておらず、uv もサポートすべきでないとして拒否する変更。pip も複数 dist-info ディレクトリをブロックしているため、大きな互換性破壊にはならないとしている。

**直近のアクティビティ:** astral-sh-bot[bot]のテストインベントリ（test inventory: +1/-2 tests in `uv::pip_install`）。`reject_wheel_with_multiple_dist_info_directories` が追加されている。charliermarsh が APPROVED（本文なし）。

**変更ファイル:** 2 ファイル変更（+97/-16）。`crates/uv-install-wheel/src/wheel.rs`（+32/-16）と `crates/uv/tests/pip_install/pip_install.rs`（+65）。

**次のアクション:** 承認済みのためマージ可能。なお同等内容の #19987 と重複している。

---
### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** renovate[bot]が自動生成・管理する Renovate のダッシュボード issue。Renovate による更新と検出された依存関係を一覧し、放置された（abandoned）依存関係（33 件）なども含めて表示する。Mend.io Web ポータルへのリンクなどが記載されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** bot 管理の追跡用 issue であり、特別な対応は不要。

---
### ![PR](icons/git-pull-request.svg) [Reject wheels with multiple dist-info directories](https://github.com/astral-sh/uv/pull/19987) `#19987`
**ステータス:** Closed | **ラベル:** compatibility

**概要:** 従来のホイールインストーラは展開されたホイール内で最初に見つかったトップレベル `.dist-info` ディレクトリを選び、他を黙って無視していた。これによりインストールがファイルシステムの走査順に依存し、複数メタデータディレクトリを持つ不正なホイールが検証を通過していた。本 PR はすべてのトップレベル `.dist-info` を収集し、ちょうど 1 つの場合のみ許可するよう変更する（pip のホイール検証に合わせる）。診断は競合ディレクトリを決定的な順序で列挙する。

**直近のアクティビティ:** charliermarsh が「#19986 に気づかなかった :)」とコメントし、重複に気づいた様子。

**変更ファイル:** 1 ファイル変更（+50/-16）。`crates/uv-install-wheel/src/wheel.rs`。

**次のアクション:** #19986 と重複しているためクローズ済み。対応不要。

---
### ![PR](icons/git-pull-request.svg) [Update MSRV to v1.94.1](https://github.com/astral-sh/uv/pull/19937) `#19937`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** renovate[bot]による自動更新 PR で、MSRV（最低サポート Rust バージョン）を `1.94.0` から `1.94.1` のパッチ更新へ引き上げるもの。リリースノートへのリンクが添えられている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 1 ファイル変更（+1/-1）。`Cargo.toml`。

**次のアクション:** CI 通過を確認してマージ。

---
### ![PR](icons/git-pull-request.svg) [Consolidate locked dependency selection APIs](https://github.com/astral-sh/uv/pull/19982) `#19982`
**ステータス:** Open | **ラベル:** なし

**概要:** `project::toolchain` は現在、優先される依存グループパッケージの選択、プロダクション依存へのフォールバック、有効グループによって厳密パッケージが既に選択済みかの判定に、3 つの個別の `Lock` API を使っており、クエリがリゾルバとコマンドコードに分散して同じパッケージを複数回発見しうる。本 PR はこれらを `Lock::dependency_selection` に置き換え、マーカー環境に対するプロダクションおよび依存グループのパッケージを一度に解決する。返される `DependencySelection` は直接参照と反復をサポートし、materialize 時の曖昧さは操作を失敗させる。#19884 のフォローアップ。

**直近のアクティビティ:** astral-sh-bot[bot]のテストインベントリ（test inventory: +2/-2 tests across 2 suites、`uv-resolver::lock::tests` に `dependency_selection_*` を追加）。

**変更ファイル:** 3 ファイル変更（+208/-118）。`crates/uv-resolver/src/lock/mod.rs`（+197/-99）が中心で、`crates/uv-resolver/src/lib.rs` と `crates/uv/src/commands/project/toolchain.rs`。

**次のアクション:** レビューを受ける必要がある。

---
### ![PR](icons/git-pull-request.svg) [Added Testing for No interpreter and No space left](https://github.com/astral-sh/uv/pull/19984) `#19984`
**ステータス:** Open | **ラベル:** なし

**概要:** `uv pip install --target` のエラーパス挙動をカバーする 2 つのテストを追加する。(1) インタプリタが見つからず Python ダウンロードが `never` のとき、汎用的なメッセージではなく原因を示す明確なヒントが出ることを検証。(2) ターゲットディレクトリのディスク容量が尽きたとき、明確で実行可能なエラーが出ることを検証。ENOSPC テストは Linux 限定で、非特権ユーザー名前空間（`unshare --user --map-root-user --mount`）でターゲットに 16KB の tmpfs をマウントし、`unshare` が無い場合や非特権ユーザー名前空間が無効な場合はスキップする。`--link-mode=copy` でハードリンク試行をスキップし、`--no-cache` で実際にバイトを書き込ませる点が要点。

**直近のアクティビティ:** 作者 SaisakthiM が「finally fixed」とコメント。

**変更ファイル:** 1 ファイル変更（+86）。`crates/uv/tests/pip_install/pip_install.rs`。

**次のアクション:** レビューを受ける必要がある（head ブランチが `main` である点も注意）。

---
### ![PR](icons/git-pull-request.svg) [Use srs for Linux and macOS CI](https://github.com/astral-sh/uv/pull/19754) `#19754`
**ステータス:** Open（Draft）| **ラベル:** test:macos

**概要:** サポート対象のネイティブ Linux x86_64 および macOS arm64 の CI ジョブで SRS `2026.06.24` リリースを使うようにする PR。変換した Linux ジョブでは mold のインストールに頼らず SRS と SLD を利用し、`Swatinem/rust-cache` をピン留めした生の `actions/cache` restore/save に置き換え、ポータブルな SRS アーティファクトキャッシュとワークロード単位の Cargo target キャッシュを組み合わせる。ローカルセットアップアクションは対応する SRS リリースアーカイブをダウンロードし、ピン留めした SHA-256 チェックサムを検証して rustup にリンクする。

**直近のアクティビティ:** codspeed-hq[bot]が「マージするとパフォーマンスが 82.66% 劣化する」と報告（回帰 6 件 / 不変 7 件）。ただし「異なるランタイム環境が検出された」ため結果の精度に影響する可能性があると注記されている。

**変更ファイル:** 11 ファイル変更（+330/-40）。`.github/actions/setup-srs/action.yml`（新規、+57）、`.github/actions/save-srs-cache/action.yml`（新規、+49）、`.github/actions/setup-srs/install.sh`（新規、+56）を追加し、`.github/workflows/test.yml`、`build-dev-binaries.yml`、`bench.yml`、`check-*.yml`、`ci.yml` 等を修正。

**次のアクション:** Draft のため作業継続。CodSpeed が報告したパフォーマンス回帰（環境差の可能性含む）の精査が必要。

---
### ![PR](icons/git-pull-request.svg) [Explain why files are skipped in registry index parsing](https://github.com/astral-sh/uv/pull/19983) `#19983`
**ステータス:** Open | **ラベル:** なし

**概要:** レジストリの simple index を解析する際、有効なホイール名やソース配布物名でないエントリが、理由を示さない不透明な `Skipping file for <pkg>: <name>` デバッグ行を残すだけで黙ってスキップされていた。これにより、devpi のインデックス管理リンク（`+searchhelp` 等）や素のバージョンディレクトリリンク、未知の拡張子のファイルを配信するレジストリの診断が困難だった。本 PR は拒否理由（拡張子未認識、無効なホイール、無効なソース配布物）を説明する型付き `DistFilenameError` を返す `DistFilename::try_from_filename_with_reason` を追加し、既存の `try_from_filename` はその `.ok()` ラッパーとして残す。なお AI コーディング支援を用いて準備した旨が開示されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 2 ファイル変更（+116/-20）。`crates/uv-distribution-filename/src/lib.rs`（+94）と `crates/uv-client/src/registry_client.rs`。

**次のアクション:** レビューを受ける必要がある。

---
### ![PR](icons/git-pull-request.svg) [Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693) `#19693`
**ステータス:** Open | **ラベル:** enhancement, performance, preview

**概要:** 従来は展開済みホイールが `archive-v0` 配下にランダム生成 ID で永続化され、異なるキャッシュエントリ経由で到達した同一ホイールを複数回保存しうる問題があった。本 PR は ZIP ホイール展開時に決定的なディレクトリダイジェスト（dirhash）を計算しアーカイブ ID として使う。ダイジェストは正規化パス・ファイルサイズ・実行ビット・内容・明示的な空リーフディレクトリを取り込み、ZIP エントリ順や無関係なメタデータは無視する。先行 PR #16816 がアーカイブ全体をハッシュ化したのに対し、本 PR は並列化のためと将来の大容量ファイル重複排除（#19694）のため、ファイル内容単位でハッシュ化する。挙動は `content-addressed-cache` プレビュー機能でゲートされる。M3 上のベンチでは性能差はほぼ無い。

**直近のアクティビティ:** 直近 24 時間のコメントなし。レビューは charliermarsh, woodruffw（2 回）, EliteTK（2 回）がいずれも COMMENTED（本文なし）。

**変更ファイル:** 25 ファイル変更（+1763/-316）。`crates/uv-extract/src/dirhash/seek.rs`（新規、+796）、`crates/uv-extract/src/dirhash/mod.rs`（新規、+349）、`crates/uv-extract/src/archive_path.rs`（新規、+88）を追加し、`crates/uv-extract/src/stream.rs`（+190/-96）、`crates/uv-distribution/src/distribution_database.rs`（+153/-55）、`crates/uv-extract/src/sync.rs`、`Cargo.lock` 等を修正。

**次のアクション:** レビューコメントへの対応とレビュー完了を待つ。

---
### ![PR](icons/git-pull-request.svg) [Replace astral-tokio-tar with tar-codec](https://github.com/astral-sh/uv/pull/19979) `#19979`
**ステータス:** Open（Draft）| **ラベル:** internal

**概要:** `astral-tokio-tar` を `tar-codec` に置き換える PR。まだ WIP で読み取りパスは実装済みだが書き込みパスは未完成。既存テストを壊さないことに加えて新規テストを追加する計画。

**直近のアクティビティ:** プレビュー機能の背後で実装すべきかが議論されている。zanieb は新実装の方が制限が厳しく「確実に一部ユーザーを壊す」と懸念し、プレビューにすれば安定化時に旧コードパスを残してオプトインできる利点があると指摘。charliermarsh は十分なテストがあればプレビュー外でも良いとしつつ、安定化判断に必要なフィードバックが得られないことを懸念。woodruffw はプレビュー機能の方向に傾くと述べ、いったん動く形にしてから旧 tokio-tar パスを戻すとしている。codspeed-hq[bot]は「マージするとパフォーマンスが ×2 改善する」と報告（ランタイム環境差の注記あり）。

**変更ファイル:** 23 ファイル変更（+1173/-439）。`Cargo.lock`（+232/-24）、`Cargo.toml` ほか tar 処理関連を広範に修正。

**次のアクション:** 書き込みパスの実装完了と、プレビュー機能でゲートするかの方針確定。

---
### ![Issue](icons/issue-opened.svg) [`uv audit`: Add support for `--vex` to  ignore vulnerabilities from a vex file](https://github.com/astral-sh/uv/issues/19981) `#19981`
**ステータス:** Open | **ラベル:** enhancement, uv audit

**概要:** `uv audit` で脆弱性を無視する際、現状のインライン引数 `--ignore` / `--ignore-until-fixed` は煩雑でリポジトリ内で明示的でなく、無視する理由も記録されないという課題提起。VEX ファイル（OpenVEX や CycloneDX VEX など、脆弱性が悪用可能かを機械可読形式で各脆弱性の理由付きで示す）のパスを渡す `--vex` 引数の追加を提案している。trivy など他ツールも `--vex` をサポートしており、サプライチェーン管理に適すると主張。

**直近のアクティビティ:** woodruffw が、1 回限りの監査以外では CLI ではなく `uv.toml` / `pyproject.toml` の `[tool.uv.audit] ignore` 設定を推奨すると応答。各 ID に構造化した理由（`{ id = ..., reason = ... }`）を持たせる改善は価値があるかもしれないとしつつ、フォーマットの業界合意が不足し独自設定が既にある中で別のファイル形式をサポートするのは恐らく避けたいとの見解を示している。

**次のアクション:** 既存設定で埋められないギャップがあるかを見極め、新フォーマット対応の是非を判断する。

---
### ![PR](icons/git-pull-request.svg) [Handle missing archives for URL- and path-specified wheels](https://github.com/astral-sh/uv/pull/19518) `#19518`
**ステータス:** Open | **ラベル:** なし

**概要:** #6147 のコメントで説明された問題を解決する PR。URL 指定およびパス指定のホイールについて、キャッシュ内のアーカイブ（`archive-v0`）が欠落している場合を適切に処理する。再現手順とそれに対応する回帰テストが追加されている。

**直近のアクティビティ:** 作者 sandyharvie が、自組織が独自フォークの運用をやめられるようマージを希望すると charliermarsh と zanieb にコメント。astral-sh-bot[bot]のテストインベントリ（test inventory: +2/-0、`install_path_wheel_missing_archive` と `install_url_wheel_missing_archive` を追加）。

**変更ファイル:** 3 ファイル変更（+151/-25）。`crates/uv/tests/pip_install/pip_install.rs`（+113）、`crates/uv-installer/src/plan.rs`（+25/-23）、`crates/uv-distribution/src/distribution_database.rs`。

**次のアクション:** メンテナによるレビューとマージ判断を待つ。

---
### ![PR](icons/git-pull-request.svg) [Factor supported environments out of lockfile markers](https://github.com/astral-sh/uv/pull/19969) `#19969`
**ステータス:** Open | **ラベル:** enhancement

**概要:** #19933 にスタックした PR。#19933 は設定環境全体で真となる依存マーカーを省略するが、条件付き依存はその環境を依然として完全に繰り返す。本 PR はマーカー代数に「ある仮定の下での一般的なマーカー簡約」を追加し、ロックのシリアライズ時に各依存マーカーを解決環境の下で簡約し、デシリアライズ時にその環境を再度結合してマーカー本来の意味を復元する。これにより挙動を変えずに残余条件のみを永続化できる。

**直近のアクティビティ:** astral-sh-bot[bot]のテストインベントリ（`konsti/remove-universe-markers` ベースラインに対し test inventory: +2/-4 across 4 suites。`uv-pep508` に `simplify_with_assumption` 追加）。ibraheemdev が APPROVED（本文なし）。

**変更ファイル:** 6 ファイル変更（+227/-29）。`crates/uv-pep508/src/marker/algebra.rs`（+110）、`crates/uv-pep508/src/marker/tree.rs`（+68）、`crates/uv-resolver/src/lock/mod.rs`（+38/-23）等。ベースブランチは `konsti/remove-universe-markers`。

**次のアクション:** 承認済み。先行 PR #19933 のマージ後に取り込み可能。

---
### ![Issue](icons/issue-opened.svg) [Resolver pins direct dependency instead of backtracking, causing Python-incompatible resolution](https://github.com/astral-sh/uv/issues/17649) `#17649`
**ステータス:** Open | **ラベル:** bug

**概要:** `requires-python = ">=3.12"` で `openai-whisper>=20250625` と `numpy>=1.26.4` を依存に持つとき、uv は `numpy` を 2.4.1 に固定し、`numba` を 0.53.1 まで後退させて Python <3.10 専用の `llvmlite==0.36.0` を引き込み、ビルド時に失敗する。報告者は、`numpy==2.3.5` が制約を満たし有効な解が存在するにもかかわらず、uv が直接依存の `numpy` をバックトラックしないことが問題だと主張している。`numpy` に上限を付けるか `numba` に下限を付けると回避できる。

**直近のアクティビティ:** amotl が、CI/GHA 上で突然同じ現象を観測し始めたと報告し、関連 issue へのリンクを添えている。

**次のアクション:** リゾルバのバックトラック挙動について調査・判断が必要。

---
### ![Issue](icons/issue-opened.svg) [`uv tool install --in` to add packages to an existing tool environment](https://github.com/astral-sh/uv/issues/14746) `#14746`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** `uv tool install` で隔離環境にツールを入れた後、同じ環境に後からプラグインや追加パッケージ（例: `llm` に `llm-tr`、`jupyter` 等）を入れる直接的な方法がないという課題。`uv tool install --in llm llm-tr` のように既存ツール環境へインストール（および upgrade / uninstall）できるようにする提案。

**直近のアクティビティ:** LeXofLeviafan と woodruffw の間で、依存リストの記述に `requirements.txt` が最適か、`pyproject.toml` や Pipfile、ロックファイル形式が適切かが議論された。woodruffw は requirements.txt が PyPA として「設計された」フォーマットとは言い難く pip の非標準な実装詳細だと述べ、議論が本 issue（#7312 の重複）の範囲を超えるとして、ツール環境変更の UX 改善を追跡する別 issue を立てるとした。

**次のアクション:** クローズ済み。後継として #19980 が立てられた。

---
### ![Issue](icons/issue-opened.svg) [`uv tool`: give users a flexible way to install tools with modifications](https://github.com/astral-sh/uv/issues/19980) `#19980`
**ステータス:** Open | **ラベル:** needs-design, needs-decision, cli, uv tool

**概要:** ユーザーは `uv tool install` に対する `pipx inject` 相当を望むが、これは宣言的パッケージングのイディオムに反し健全な解決を保証しにくいため追加したくない、という整理。`uv tool inject` を求める要望の多くは XY 問題であり、実際に欲しいのは長い `--with` リストを覚えたり保存したりせずに特定の修正を加えてツールを入れる手段だと分析している。解決策として、ツールインストール専用の `pyproject.toml` 的なファイルが考えられ、どこまでの情報（非デフォルトインデックス等）を含めるかが論点。#14746 と #7312 を背景に挙げている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 設計と方針の決定（needs-design / needs-decision）が必要。

---
### ![PR](icons/git-pull-request.svg) [Support scoped dependency exclusions](https://github.com/astral-sh/uv/pull/19977) `#19977`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** 現在の `exclude-dependencies` は依存をグローバルに削除するため、ある 1 パッケージの依存エッジだけを直接要件や他パッケージの要件に影響を与えずに削除できない。本 PR は既存のパッケージ名文字列と並べて構造化エントリ（`{ name = "bar", version = "0.0.5", requires-dist = ["lightning"] }` など）を記述できるようにし、選択した親パッケージからのみマッチする依存を削除する。version は省略可能、厳密バージョン指定がバージョンレス指定より優先され、グローバル除外は引き続き全体に適用される。#19974 の加算的スコープ付きオーバーライドと組み合わせることで、`lightning` を除外し `pytorch-lightning` を追加するといった依存の置換が可能。除外はオーバーライドの後に適用される。#19974 にスタック。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 28 ファイル変更（+377/-72）。`crates/uv-configuration/src/excludes.rs`（+115/-4）が中核で、`crates/uv/tests/project/tree.rs`（+82）、`docs/concepts/resolution.md`、`uv.schema.json`、各 `crates/uv/src/commands/` 配下を修正。ベースブランチは `charlie/scoped-overrides-add-dependencies`。

**次のアクション:** Draft のため、先行スタック PR（#19970 → #19974）のマージ後に対応。

---
### ![PR](icons/git-pull-request.svg) [Allow scoped overrides to add dependencies](https://github.com/astral-sh/uv/pull/19974) `#19974`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** #19970 のパッケージスコープオーバーライドは選択した親が既に宣言する依存しか置換できず、新規依存に対するスコープ付き要件は黙って無視される。本 PR はスコープ付き `requires-dist` エントリを部分的なメタデータパッチとして扱い、名前が一致する要件は既存宣言を置換し、新しい名前の要件は選択パッケージの依存に追加する。グローバルオーバーライドは引き続き非加算的で、スコープエントリから省略された依存は変更されない。統合テストでは AnyIO に `typing-extensions` を追加しつつ既存の IDNA 要件を置換している。#19970 にスタック。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル変更（+82/-46）。`crates/uv-configuration/src/overrides.rs`（+65/-35）、`crates/uv/tests/pip_compile/pip_compile.rs`、`docs/concepts/resolution.md`、`uv.schema.json` 等。ベースブランチは `charlie/scoped-overrides`。

**次のアクション:** Draft のため、先行 PR #19970 のマージ後に対応。

---
### ![Issue](icons/issue-opened.svg) [`uv tree`'s --depth behaviour is strange on scripts and workspace-groups](https://github.com/astral-sh/uv/issues/19976) `#19976`
**ステータス:** Open | **ラベル:** bug

**概要:** #19973 の後半で触れた問題の詳細化。`uv tree --depth=0` の挙動が対象により一貫しない: パッケージでは当該パッケージのみ、ワークスペースではメンバー一覧、`--script=myscript` ではスクリプトの直接依存、依存グループを定義する仮想ワークスペースではデフォルトグループの内容が出力される。報告者はいずれの場合も依存を出力しない（あるいは子のない「script」「workspace」ノードを示す）べきだと考えている。ワークスペース例とスクリプト例が添えられている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** `--depth=0` 時の出力セマンティクスを統一する設計判断が必要。#19978（JSON 出力）とも関連。

---
### ![Issue](icons/issue-opened.svg) [`uv tree` doesn't work with dependency-group-only pyproject.tomls](https://github.com/astral-sh/uv/issues/19975) `#19975`
**ステータス:** Open | **ラベル:** bug

**概要:** PEP 735 では「`[dependency-groups]` のみで他テーブルを持たない `pyproject.toml` は有効」とされているが、`[dependency-groups]` だけを宣言した `pyproject.toml` に対して `uv tree` を実行すると `error: No 'project' table found in: ...` で失敗する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 依存グループのみの `pyproject.toml` を `uv tree` がサポートするよう修正が必要。

---
### ![Issue](icons/issue-opened.svg) [Consider supporting OSC 9;4 for progress bar](https://github.com/astral-sh/uv/issues/17731) `#17731`
**ステータス:** Open | **ラベル:** enhancement

**概要:** ターミナルにプログレスバーを表示する新しい仕組み OSC 9;4 のサポート提案。Windows Terminal、iTerm2、Ghostty で少なくとも対応しており、対応予定のターミナルも増えている。uv に強く影響を受けたツール rv のテスト中に見つけたものとのこと。

**直近のアクティビティ:** epage が、cargo のこの機能対応を `anstyle-progress` クレートとして切り出しており、コード生成とターミナル対応検出を扱うとコメント。

**次のアクション:** `anstyle-progress` の活用を含め、OSC 9;4 対応の実装是非を検討する。

---
### ![PR](icons/git-pull-request.svg) [Simplify dependency markers under parent reachability](https://github.com/astral-sh/uv/pull/19971) `#19971`
**ステータス:** Open | **ラベル:** なし

**概要:** #19969 にスタックした PR。依存エッジは親ノードが選択されて初めて評価され、リゾルバは各パッケージ・extra・依存グループノードの正確な到達可能性マーカーを既に計算しているため、その到達条件から含意される節は出力エッジ上で冗長。本 PR はロック構築前に各出力依存マーカーを親ノードの到達可能性の下で簡約する。トップレベルの解決環境は #19969 でワイヤ上から括り出され、ロックの読み書きの等価性が保たれる。Black のロックは依存マーカー 51→24・141,723→140,302 バイトに、Transformers は 505→325 マーカー・939,630→918,634 バイトに縮小する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 9 ファイル変更（+658/-672）。`crates/uv-resolver/src/lock/mod.rs`（+51/-12）に加え、スナップショット更新が大半（`it__ecosystem__transformers-lock-file.snap` +184/-184、`lock_scenarios/lock_conflict.rs` +185/-185、`lock/lock.rs` +141/-141 等）。ベースブランチは `charlie/simplify-environment-markers`。

**次のアクション:** 先行 PR（#19969）のマージ後にレビュー・取り込み。

---
### ![PR](icons/git-pull-request.svg) [Make tool environments relocatable under preview](https://github.com/astral-sh/uv/pull/19967) `#19967`
**ステータス:** Closed | **ラベル:** なし

**概要:** インストール済みツール環境の作成がグローバルなプレビュー状態から `relocatable-envs-default` を読むようにし、エクスポートされた Windows ツールエントリポイントを、外部の場所向けに relocatable ランチャーを書き直すことで保持する変更。これによりツールインストールおよびインタプリタ変更を伴うアップグレードが一貫してプレビュー機能を尊重する。既存のツール環境は変更されず、再作成時に relocatable 性を獲得する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル変更（+120/-3）。`crates/uv/src/commands/tool/common.rs`（+65/-2）、`crates/uv/tests/tool/tool_install.rs`（+51）、`crates/uv-tool/src/lib.rs`、`Cargo.lock` 等。

**次のアクション:** Draft のままクローズされている。対応不要（必要なら別 PR で再提出）。

---
### ![Issue](icons/issue-opened.svg) [`uv tree`'s flags for extra/groups are weird](https://github.com/astral-sh/uv/issues/19973) `#19973`
**ステータス:** Open | **ラベル:** bug

**概要:** `uv tree` はグループについて他コマンドの「標準」セマンティクス（デフォルトグループ有効、`--all-groups` / `--no-default-groups` を提供）を使う一方、extra の選択フラグが無く実質 `--all-extras` でオプトアウトもできない、という非対称性の指摘。`--all-groups` デフォルト、`--no-default-groups` デフォルト、extra なしデフォルトのいずれにも筋は通るが現状は紛らわしいとしている。また `--depth=0` で依存グループ（や extra）の存在が示されない点も、ノードとして分離している JSON 出力作業との関連で問題視している。

**直近のアクティビティ:** 作者 Gankra が、グループ/extra のこの非対称性は、グループフラグを uv 全体に追加した際に `uv tree` を深く考えなかった歴史的偶然のようだと（Slack での議論を踏まえて）コメント。

**次のアクション:** extra/group フラグのセマンティクスをどう揃えるかの設計判断が必要。

---
### ![Issue](icons/issue-opened.svg) [`uv tree --invert` silently discards leaf-loops](https://github.com/astral-sh/uv/issues/19972) `#19972`
**ステータス:** Open | **ラベル:** bug

**概要:** `A -> B <-> C` のようなリーフ的な循環を含むグラフに `uv tree --invert` を実行すると、グラフを反転して「入次数 0 のノード」を根として探すため該当ノードが存在せず、何も出力されない（終了コードは 0）。`A -> B <-> C -> D` のように循環の外に出るノードがあれば正しく出力される。根本問題は `--invert` が「リーフを根に変えて辺を反転し通常実行する」挙動である点で、リーフ的循環ではどのメンバーもリーフでない。非反転の `uv tree` は循環を「既に表示済み」として打ち切る仕組みを持つため、警告するか任意のタイブレークで循環の 1 メンバーをリーフと宣言すべきとしている。再現スクリプトを添付。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** リーフ的循環に対する根の決定方法（タイブレークまたは警告）を実装する。

---
### ![Issue](icons/issue-opened.svg) [Allow per-package overrides](https://github.com/astral-sh/uv/issues/2686) `#2686`
**ステータス:** Open | **ラベル:** enhancement

**概要:** #511 のコメントで議論された第二のタイプの依存オーバーライド、すなわち「`bar` 内の `foo==1.2.3` への要件を `foo==4.5.6` で置き換える（bar は foo 4.5.6 とも互換であることを確認済みと宣言する）」機能の要望。親 issue はグローバルオーバーライド（第一案）で解決されたが、この第二案はよりリスクが低く、新たな foo への要件が bar 外で追加された際に良いエラーメッセージを出せるとして追跡を求めている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** この issue が要望するスコープ付きオーバーライドは現在 #19970 系の PR で実装が進行中であり、その完了を待つ。

---
### ![PR](icons/git-pull-request.svg) [Add support for locking build dependencies](https://github.com/astral-sh/uv/pull/17854) `#17854`
**ステータス:** Open（Draft）| **ラベル:** build:skip-release

**概要:** ビルド依存（build dependencies）のロックをサポートする大規模 PR。作者自身のレビュー用に投稿したものでまだ反復が必要とされている。複数の RFC ドキュメントを伴い、ビルド解決のコンテキスト ID やマルチ解決といった仕組みを導入する。

**直近のアクティビティ:** astral-sh-bot[bot]のテストインベントリ（test inventory: +121/-0 across 4 suites。`uv::lock::lock_build` に 82 件、`uv-resolver::lock::tests` に 35 件等を追加）。codspeed-hq[bot]は「パフォーマンスは変化しない」と報告。レビューは合計 47 件で全て COMMENTED、うち 46 件が zanieb・1 件が Gankra（いずれも本文なし）。

**変更ファイル:** 86 ファイル変更（+34099/-7888）と非常に大規模。設計文書として `RFC-MULTIPLE-RESOLVES.md`（+1084）、`RFC-BUILD-DEPS-LOCK.md`（+574）、`RFC-BUILD-RESOLUTION-ID.md`（+544）、`RFC.md` を新規追加し、`crates/uv/tests/lock/lock_build.rs` を新設。`Cargo.lock` 等も変更。

**次のアクション:** Draft かつ大規模なため、RFC のセマンティクスを固めつつ分割・反復を継続する必要がある。

---
### ![PR](icons/git-pull-request.svg) [Fix pythonw launcher mode for managed Windows venv links](https://github.com/astral-sh/uv/pull/19235) `#19235`
**ステータス:** Open | **ラベル:** なし

**概要:** Windows の managed venv リンクにおける pythonw ランチャーモードの修正。サブシステムの決定を出力ファイル名からの推測ではなく呼び出し側に移す。`uv-python::managed` が `LauncherSubsystem::{Console, Windowed}` を持つ `LauncherTarget` を公開し、`create_link_to_executable` / `replace_link_to_executable` が `LauncherTarget` を直接受け取る。呼び出し側はスコープ内に既にある `WindowsExecutable` のバリアントに基づき console / windowed を渡す。従来の名前ベースのヘルパとそのテストは削除。#19226 をクローズする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 3 ファイル変更（+90/-22）。`crates/uv-python/src/managed.rs`（+59/-8）、`crates/uv-virtualenv/src/virtualenv.rs`（+26/-11）、`crates/uv/src/commands/python/install.rs`。

**次のアクション:** レビューを受ける必要がある。

---
### ![Issue](icons/issue-opened.svg) [Should lock file be committed to source control?](https://github.com/astral-sh/uv/issues/9797) `#9797`
**ステータス:** Closed | **ラベル:** question

**概要:** ロックファイルをソース管理にコミットすべきかという質問。ドキュメントに情報が見つからないという内容。

**直近のアクティビティ:** Avasam が、CI が予期せぬライブラリ更新で壊れることやサプライチェーン攻撃を気にするかどうか次第だとコメント。Renovate や Dependabot で定期的にまとめて更新・テストする運用や、ライブラリでは二段テストを好むと述べている。

**次のアクション:** クローズ済み。対応不要。

---
### ![PR](icons/git-pull-request.svg) [Use locked ty versions in uv check](https://github.com/astral-sh/uv/pull/19884) `#19884`
**ステータス:** Merged | **ラベル:** preview

**概要:** `uv check` は従来、プロジェクトのロック済み開発依存が別バージョンやソースを選んでいても、uv 組み込みの互換範囲から standalone な `ty` 実行ファイルを解決していた。本 PR により、`uv.lock` に該当する `ty` パッケージがある場合は `uv check` がその厳密なロック済みパッケージを実行する（まず `dev` グループを見て、次に直接ランタイム依存にフォールバック）。`--no-sync` 時（または選択グループで `ty` が除外される場合）は、プロジェクト環境を変更せずロック済みの `ty` サブグラフのみをキャッシュ環境に materialize する。ty バージョン選択の優先順位は `TY` 実行ファイルオーバーライド → `--ty-version` → `uv.lock` の該当 ty → uv のデフォルト制約範囲。作業は #19907, #19909, #19892, #19913 の複数 PR に分割されている。

**直近のアクティビティ:** zsol が、これらの API を整理するアイデアはあるがまず本 PR をマージし、変更は別途フォローアップするとコメント。レビューは合計 48 件（zanieb 32 件、zsol 16 件）で、47 件が COMMENTED・1 件が zanieb による APPROVED。承認時のコメントは「いくつか nit があるだけ。これらの API の形にはまだ懸念があるが、後で整理できる」。

**変更ファイル:** 8 ファイル変更（+657/-9）。`crates/uv-resolver/src/lock/mod.rs`（+187）、`crates/uv/tests/project/check.rs`（+292）、`crates/uv/src/commands/project/toolchain.rs`（新規、+101）、`crates/uv/src/commands/project/check.rs`（+71/-3）等。

**次のアクション:** マージ済み。zsol が予告した API 整理のフォローアップ（#19982 が該当）を進める。

---

## microsoft/vscode

> Agents / Copilot Chat ワークストリーム（sessions、changes view、agent-host、model picker など）に活動が集中。加えて fontconfig/スモークテスト関連の CI 修正、`info-needed` で自動クローズされた補完フィードバック群、dependabot の Actions バンプが含まれる。

### ![PR](icons/git-pull-request.svg) [Hide long context for models which have the same cost regardless](https://github.com/microsoft/vscode/pull/322950) `#322950`
**ステータス:** Open | **ラベル:** なし

**概要:** 200k と 1M のコンテキスト長が同一価格となるモデルでは、常に 1M を使うようにしてモデルピッカーから長コンテキストの選択肢を隠す変更です。Issue #322951 に対応します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/vs/platform/agentHost/common/agentModelPricing.ts` や `copilotAgent.ts`、`languageModelAccess.ts` などを編集し、`modelPickerWidget.ts` から該当行を削除。テストも追加(計 8 ファイル、+158/-56)。

**次のアクション:** レビューとマージを待つ。

---

### ![Issue](icons/issue-opened.svg) [Hide long context on models that have same long context pricing](https://github.com/microsoft/vscode/issues/322951) `#322951`
**ステータス:** Open | **ラベル:** feature-request, model-picker, UBB

**概要:** モデルが 200k と 1M を同一価格で提供する場合、常に 1M を使うべきという要望です。上記 PR #322950 の元となる Issue です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #322950 のマージで解決見込み。

---

### ![Issue](icons/issue-opened.svg) [Contextual inline code embedding in Copilot Chat](https://github.com/microsoft/vscode/issues/322946) `#322946`
**ステータス:** Open | **ラベル:** なし

**概要:** 現在「Add Selection to Chat」は選択コードをファイル添付として付けるため、複数ファイルを扱うとどの添付がどのファイルか区別しにくいという問題提起です。選択コードを `@Backend\mes\basis\controller\MachineController.cs:L22-35` のようにチャット入力欄へインラインで挿入してほしいという要望です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージと実現可否の検討。

---

### ![PR](icons/git-pull-request.svg) [Agents - extract the ChangesViewModel into a service](https://github.com/microsoft/vscode/pull/322886) `#322886`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents の「Changes」機能で `ChangesViewModel` の所有権をシングルトンサービスに集約し、Changes ビューと multi-diff ソースリゾルバが(復元時にビューが閉じていても)同一モデルを共有できるようにするリファクタリングです。`IChangesViewService` / `ChangesViewService` を新設します。

**直近のアクティビティ:** KainosSLS が「Ok.」とコメント。copilot のレビュー(2 件のコメント生成)と chrmarti による承認。

**変更ファイル:** `changesViewService.ts` を新規追加(+50)、`changesView.ts`、`changesActions.ts`、`changes.contribution.ts` を編集(計 4 ファイル、+62/-9)。

**次のアクション:** マージ済み。後続の統合作業へ。

---

### ![PR](icons/git-pull-request.svg) [fix: extension Disable dropdown shows only relevant action per state](https://github.com/microsoft/vscode/pull/322912) `#322912`
**ステータス:** Open | **ラベル:** なし

**概要:** 拡張機能がグローバルで無効かつワークスペースで有効な場合に、Disable ボタンのドロップダウンに Disable と Disable (Workspace) が両方表示されてしまうバグ(#244138)の修正です。`DisableGloballyAction` と `DisableForWorkspaceAction` の `update()` で適切に `hidden` を設定し、`.some()` コールバック内に `getWorkbenchState()` を置いていた括弧位置の誤りも修正します。

**直近のアクティビティ:** 投稿者 MeGaurav4 が CLA に同意。copilot レビューと、投稿者による複数のレビューコメント。

**変更ファイル:** `extensionsActions.ts`(+12/-5)とそのテスト(+45)を編集(計 2 ファイル)。

**次のアクション:** メンテナのレビューとマージ判断。

---

### ![Issue](icons/issue-opened.svg) [Agent plugin skill files are inaccessible when connected to WSL/SSH remote](https://github.com/microsoft/vscode/issues/305168) `#305168`
**ステータス:** Closed | **ラベル:** insiders-released

**概要:** WSL/SSH リモート接続時に、マーケットプレイスから入れたエージェントプラグインのキャッシュが Windows ホスト側に置かれるため、スキルファイルの URI が Windows パスのまま渡され、Linux リモート上の `read_file` で解決できないという不具合です。さらにワークスペース外のため「Allow reading external files?」確認ダイアログがブロックする問題も指摘しています。

**直近のアクティビティ:** joshmorningstar が 1.126.0 で PR #309465 により完全に解決したことを確認、スクリーンショット付きで報告。

**次のアクション:** 修正確認済みでクローズ。

---

### ![PR](icons/git-pull-request.svg) [smoke: fail fast with actionable error when Electron crashes during launch](https://github.com/microsoft/vscode/pull/322905) `#322905`
**ステータス:** Open | **ラベル:** なし

**概要:** Electron スモークテストの `before all` フックが `_electron.launch` を `timeout: 0` で呼んでいたため、起動時の SIGSEGV クラッシュが表面化せず 120 秒の不透明な Mocha タイムアウトになっていた問題への対応です。launch と最初のウィンドウ待機に有限の `LAUNCH_TIMEOUT = 60s` を設け、失敗時はクラッシュダンプ・トレースを指す実用的なエラーで再スローします。

**直近のアクティビティ:** copilot レビュー(2 件)に加え、alexr00 と lszomoru が承認。

**変更ファイル:** `test/automation/src/playwrightElectron.ts` のみ(+48/-12)。

**次のアクション:** マージ。リトライ追加や根本の起動クラッシュ調査は別途。

---

### ![PR](icons/git-pull-request.svg) [sessions: propose Draft Service for preserving unstarted session drafts](https://github.com/microsoft/vscode/pull/322945) `#322945`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents Window で未開始/保留中のセッション間を移動した際に、未送信メッセージ(inputText と添付)を保持する `IAgentSessionsDraftService` を提案する草稿実装です。現状 `NewChatWidget` は `sessions.draftState` という単一グローバルキーを使うため、サブチャット間でドラフトが衝突したり、ウィジェット破棄で即座に失われたりする問題に対処します。#315996 をクローズ予定。

**直近のアクティビティ:** policy bot が CLA への同意を要求。copilot レビュー(3 件のコメント生成)。

**変更ファイル:** `sessionsDraftService.ts` を新規追加(+47)、`newChatInput.ts` を編集(+55/-1、計 2 ファイル)。

**次のアクション:** チームからの早期フィードバックを受けて方向性を確定し、本実装へ。

---

### ![Issue](icons/issue-opened.svg) [why is chat history lost after an update?](https://github.com/microsoft/vscode/issues/322949) `#322949`
**ステータス:** Open | **ラベル:** なし

**概要:** アップデート後にチャット履歴が失われるのはなぜか、という本文記載のない短い問い合わせです。本文の記載なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージと再現情報の確認が必要。

---

### ![Issue](icons/issue-opened.svg) [PHP Nowdoc syntax misinterpreted when labels contain UTF8](https://github.com/microsoft/vscode/issues/322943) `#322943`
**ステータス:** Open | **ラベル:** なし

**概要:** PHP の HEREDOC では UTF8 を含むラベル(例: `<<<HERE📄`)でコメント開始が正しく扱われるが、NOWDOC(`<<<'NOW📄'`)では失敗し、`/*` 以降の関数が誤ってコメント扱いされるという文法ハイライトのバグです。パーサが nowdoc セクションでシングルクォート扱いに切り替わるべきだと指摘しています。

**直近のアクティビティ:** 投稿者 Korkman が 1.126.0 でも同様と報告。エンジニアリング bot が旧バージョンの可能性を案内。

**次のアクション:** PHP grammar の修正検討。

---

### ![Issue](icons/issue-opened.svg) [why are increased budgets not immediately usable?](https://github.com/microsoft/vscode/issues/322948) `#322948`
**ステータス:** Open | **ラベル:** なし

**概要:** 予算(budget)を増やしても即座に使えないのはなぜか、という本文記載のない問い合わせです。本文の記載なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。同一投稿者の関連 Issue 群とまとめて扱う可能性。

---

### ![Issue](icons/issue-opened.svg) [why are funds used even when no work is being done eg overnight ?](https://github.com/microsoft/vscode/issues/322947) `#322947`
**ステータス:** Open | **ラベル:** なし

**概要:** 夜間など作業していないときにも資金(funds)が消費されるのはなぜか、という本文記載のない問い合わせです。本文の記載なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージと課金挙動の説明が必要。

---

### ![Issue](icons/issue-opened.svg) [AI is making using VS Code very hard. I cannot do anything without it suggesting and making my code worse](https://github.com/microsoft/vscode/issues/322937) `#322937`
**ステータス:** Open | **ラベル:** triage-needed, new release

**概要:** AI 提案が邪魔で作業しづらいという不満で、AI 提案をすべて削除し、わからないときに相談できるチャットだけ残してほしいという要望です。1.126.0、Windows、C/C++ 拡張環境からの報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。インライン提案の無効化方法の案内や設定改善の検討。

---

### ![Issue](icons/issue-opened.svg) [Orchestrator Doesn't Respect Delegate Agent Tools](https://github.com/microsoft/vscode/issues/284093) `#284093`
**ステータス:** Closed | **ラベル:** bug, chat-subagents

**概要:** オーケストレータが委譲先のエージェント(frontmatter で別ツールを定義)を呼ぶ際、委譲先が親エージェントのツールを継承してしまい、`runSubagent` で委譲先固有のツール定義が無視されるという不具合報告です。

**直近のアクティビティ:** 投稿者 joshmorningstar が約半年後に再現を試み、1 月以降のリリースで解決済みと判断してクローズ。

**次のアクション:** 解決済みでクローズ。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Engineering - update code owners](https://github.com/microsoft/vscode/pull/322944) `#322944`
**ステータス:** Merged | **ラベル:** なし

**概要:** リポジトリの所有権・通知設定を現在のレビュー担当に合わせて更新する変更です。複数の GitHub Actions ワークフローや cache 関連ファイルの CODEOWNERS を調整し(`@joaomoreno` 削除、一部で `@sandy081` 追加)、CODENOTIFY と `.mailmap` のエントリも更新します。

**直近のアクティビティ:** copilot レビュー(1 件)と chrmarti による承認。

**変更ファイル:** `.github/CODEOWNERS`、`.github/CODENOTIFY`、`.mailmap`、`extensions/copilot/.github/CODEOWNERS`(計 4 ファイル、+21/-22)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Show progress while downloading agent SDK binaries](https://github.com/microsoft/vscode/pull/322919) `#322919`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code は Claude/Codex エージェントを同梱するがネイティブバイナリ(圧縮 70〜95MB)は初回オンデマンドで CDN からダウンロードするため、コールドキャッシュ時に無反応に見える問題への対応です。新しい AHP 通知 `root/downloadProgress` を介して agent host からレンダラへ進捗を送り、Agents window でトーストとして表示します。

**直近のアクティビティ:** 直近 24 時間のコメントなし(copilot レビュー2 件は記録あり)。

**変更ファイル:** `agentSdkDownloader.ts`(+174)、`notifications.ts`(+118)、`baseAgentHostSessionsProvider.ts`(+102)など多数(計 23 ファイル、+652/-26)。

**次のアクション:** 依存する microsoft/agent-host-protocol#263 のマージ後に protocol を再同期し、ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [Fix: CancellationToken not propagating to `provideLanguageModelChatResponse` on chat stop](https://github.com/microsoft/vscode/pull/319098) `#319098`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットの停止ボタンが `provideLanguageModelChatRequest` のプロバイダ側 `CancellationToken` にキャンセルを伝播しない問題(#291713、#277872)の修正です。`$startChatRequest` が即座に resolve するため RPC のキャンセルハンドラが削除されること、MainThread と ExtensionHost で requestId が異なることが原因で、新規 RPC `$cancelLanguageModelChatRequest` と両側のローカル CTS で橋渡しします。

**直近のアクティビティ:** 投稿者 tamuratak が「done」とコメント。lramos15 が `CancellableRequestMap` の抽象は不要で、speech/notebooks/testing の既存パターン(`DisposableMap<number, CTS>`)に倣うよう提案。vritant24 が CHANGES_REQUESTED でユニットテスト追加を要請。copilot の繰り返しレビュー。

**変更ファイル:** `mainThreadLanguageModels.ts`(+40/-4)、`extHostLanguageModels.ts`(+31/-5)、テスト(+73)など(計 6 ファイル、+154/-12)。

**次のアクション:** レビュー指摘に沿って既存パターンへリファクタし、ユニットテストを追加。

---

### ![PR](icons/git-pull-request.svg) [agents: fix input banner button color 3e80d66b](https://github.com/microsoft/vscode/pull/322936) `#322936`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents window の CI セッション入力バナーで、主アクションボタンのオレンジ系アクセント色を CSS から `Button` のスタイルオプション(生成時)へ移し、`Button` のインラインスタイルに上書きされない問題を修正します。冗長になった `.accent-orange` CSS ルールを削除します。

**直近のアクティビティ:** github-actions bot がスクリーンショット差分(6 件変更)を報告。copilot レビュー(2 件)、alexr00 が DISMISSED、chrmarti が承認。

**変更ファイル:** `sessionInputBannerWidget.ts`(+7)、`sessionInputBanners.ts`(+29/-6)、`sessionInputBanners.css`(-10)など(計 4 ファイル、+42/-21)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chat: add typed scrollbar markers to the Chat View transcript](https://github.com/microsoft/vscode/pull/322312) `#322312`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Chat View トランスクリプトに、Monaco のオーバービュールーラに相当する型付きスクロールバーマーカーを追加します。5 種(`prompt`、`askQuestion`、`fileChange`、`compaction`、`error`)を 3 レーン(right/left/full)に割り当て、`chat.scrollbarPromptMarkers.enabled`(既定 false、experimental)で制御し、list/tree に読み取り専用ジオメトリ API を通します。

**直近のアクティビティ:** copilot と投稿者 matthewcorven による非常に多数の往復レビュー(数十件、直近は 6/25)。本文のないレビューコメント多数。

**変更ファイル:** `chatScrollbarPromptMarkerController.ts` を新規(+519)、関連テスト(+1120、+480)、`chatListWidget.ts`(+405/-196)、`chatPromptNavigationActions.ts`(+575/-41)など(計 14 ファイル、+3230/-239)。

**次のアクション:** レビュー指摘の解消とドラフト解除、検証チェックリストの完了。

---

### ![Issue](icons/issue-opened.svg) [Infinite loading](https://github.com/microsoft/vscode/issues/322942) `#322942`
**ステータス:** Open | **ラベル:** info-needed

**概要:** ずっと読み込み中のまま応答もアクションもないという、詳細情報の乏しいバグ報告です。拡張 0.30.3、VS Code 1.103.2(macOS arm64)という古い環境からの報告です。

**直近のアクティビティ:** エンジニアリング bot が報告ガイドライン不足と旧バージョン利用の可能性を 2 件案内。

**次のアクション:** 投稿者からの追加情報待ち(現状 info-needed)。

---

### ![Issue](icons/issue-opened.svg) [Agents: "Open in Agents Window" button is difficult to discover in VS Code](https://github.com/microsoft/vscode/issues/315762) `#315762`
**ステータス:** Open | **ラベル:** ux

**概要:** ユーザビリティ調査で参加者全員が「Open in Agents Window」ボタンを見つけられず、拡張ビューレットやファイル、メインメニュー、チャットパネルなど誤った場所を探したという指摘です。タイトルバーのアイコン配置は直感的でなく、チャットパネルや View メニューへの追加/移動を検討してほしいという要望です。

**直近のアクティビティ:** 投稿者 stevencl が 2026 年 6 月の UX 調査でも依然として見つけにくく、ほとんどの参加者が場所を教えてもらう必要があったと追記。

**次のアクション:** ボタン配置の再設計とメニューコマンド追加の検討。

---

### ![PR](icons/git-pull-request.svg) [Agents - unify the view mode and the view service](https://github.com/microsoft/vscode/pull/322910) `#322910`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents window の「Changes」機能で、選択中の changeset・ビューモード・アクティブセッション派生状態を単一の `IChangesViewService`(ビュー非依存のライフサイクル)に集約するリファクタリングです。従来の `ChangesViewModel` を廃止し、同等の observable とセッターを持つ `ChangesViewService` 実装に置き換えます。

**直近のアクティビティ:** copilot レビュー(3 件)、joaomoreno と ulugbekna が DISMISSED、alexr00 が承認。

**変更ファイル:** `changesViewModel.ts` を削除(-267)、`changesViewService.ts`(browser)(+240/-33)、common 側 `changesViewService.ts` を新規(+51)など(計 10 ファイル、+369/-380)。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [bug](https://github.com/microsoft/vscode/issues/322941) `#322941`
**ステータス:** Closed | **ラベル:** spam

**概要:**「bug issue」「not workig」とだけ書かれた中身のないスパム報告です。拡張 0.29.1、古い VS Code 1.102.3 環境です。

**直近のアクティビティ:** 投稿者が「not workig」とコメント。エンジニアリング bot が旧バージョン案内と、コミュニティガイドライン違反/スパムとしてのクローズ通知。

**次のアクション:** スパムとしてクローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Custom agent frontmatter is not automatically loaded on VSCode startup.](https://github.com/microsoft/vscode/issues/321765) `#321765`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code 再起動後、カスタムエージェントを選択しても frontmatter のメタデータ(model、tools、`user-invocable: false` など)がチャット UI に確実に反映されない不具合です。該当の `.agent.md` を開いて無変更で保存し直すと初めて適用されるようになり、サブエージェントの model/tools も起動時のチャット設定が使われてしまうと報告しています。

**直近のアクティビティ:** aiday-mar がローカルハーネスで再現を試み、test/test2 エージェント切替で UI は正しく変わるように見えるが設定を見落としている可能性があるとコメント(動画添付)。

**次のアクション:** 正しい再現手順の確立と、起動時の frontmatter ロード処理の調査。

---

### ![PR](icons/git-pull-request.svg) [ci: drop external DTD from minimal fontconfig to avoid expat NULL-deref crash](https://github.com/microsoft/vscode/pull/322909) `#322909`
**ステータス:** Merged | **ラベル:** なし

**概要:** Linux スモークテストが入れる最小 fontconfig 設定から外部 DTD 宣言(`<!DOCTYPE … SYSTEM "urn:fontconfig:fonts.dtd">`)を削除し、expat 2.6.1 の NULL-deref CVE(CVE-2026-32776/32778)経路を断つ変更です。クラッシュダンプ解析で、Chromium のブラウザプロセスのフォント初期化中に libexpat 経由で SIGSEGV が発生していたと判明しています。

**直近のアクティビティ:** copilot レビュー(0 件)、ulugbekna と lszomoru が承認。

**変更ファイル:** `.github/workflows/pr-linux-test.yml` のみ(+16/-7)。

**次のアクション:** マージ済み。恒久対応はランナーへの libexpat >= 2.7.5 導入。

---

### ![PR](icons/git-pull-request.svg) [agents: add second onboarding scenario 013d3d83](https://github.com/microsoft/vscode/pull/322917) `#322917`
**ステータス:** Merged | **ラベル:** なし

**概要:** オンボーディングエンジンに複数シナリオ間で共有する「一度だけ表示」状態(`seenKey`)を追加し、新規ユーザーが新セッションビューに直接到達した際に走る 2 つ目の Agents window オンボーディングツアーを追加します。同じ `seenKey` を共有することで、ユーザーにはどちらか一方だけが表示されます。

**直近のアクティビティ:** copilot レビュー(6 件)、hediet と aiday-mar が承認。

**変更ファイル:** `newSessionViewTourContribution.ts`(+146)、`newSessionViewTour.ts`(+83)を新規、`onboardingService.ts`(+47/-8)など(計 8 ファイル、+305/-11)。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat renders Ollama streaming reasoning chunks as normal output after briefly showing Thinking UI](https://github.com/microsoft/vscode/issues/322938) `#322938`
**ステータス:** Open | **ラベル:** new release

**概要:** Ollama のローカルモデル(MiniMax M3)で、reasoning テキストが一瞬 Thinking UI に出た後、通常のアシスタント出力として描画される不具合です。Ollama は native `/api/chat` の `message.thinking` でも OpenAI 互換 `/v1/chat/completions` の `choices[0].delta.reasoning` でも reasoning を正しく分離しているのに、VS Code 側がすべての `delta.reasoning` を Thinking UI に表示しきれていないと指摘します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ストリーミング reasoning チャンクのレンダリング処理の調査。

---

### ![PR](icons/git-pull-request.svg) [Add terminal test to chat scenario tests](https://github.com/microsoft/vscode/pull/322940) `#322940`
**ステータス:** Open | **ラベル:** なし

**概要:** chat/agent セッションのスモークテストを拡張し、再利用可能なモック LLM の「shell echo」シナリオ(`run_in_terminal` バリアント含む)を導入して、モデル駆動のターミナルツール実行がチャット UI に描画されることを検証するテストを追加します。本文の記載なしですが、ファイル構成から CI で 20 回反復するプローブも追加します。

**直近のアクティビティ:** copilot レビュー(3 件)。

**変更ファイル:** `shellScenarios.ts` を新規(+84)、`chatSessions.test.ts`(+82)、`agentsWindow.test.ts`(+111/-63)、Azure Pipelines の yml 2 件(計 5 ファイル、+299/-64)。

**次のアクション:** レビュー対応とドラフト解除。

---

### ![Issue](icons/issue-opened.svg) [Custom BYOK endpoint: "Sorry, no response was returned" when empty tool_calls: [] on content deltas](https://github.com/microsoft/vscode/issues/322728) `#322728`
**ステータス:** Open | **ラベル:** なし

**概要:** vLLM バックエンドの BYOK エンドポイント(moonshotai/Kimi-K2.6)で、各 content delta に空の `"tool_calls": []` が付くと、VS Code が tool_calls の存在をツール呼び出しと解釈して content を破棄し、`badRequest` として「Sorry, no response was returned」になる不具合です。空配列を除去するプロキシを挟むと成功すると確認しており、`tool_calls.length > 0` のときだけ処理する修正を提案します(関連 #255934、#253126)。

**直近のアクティビティ:** sanny-io が #253126 はクローズされていないと指摘し、自分は Microsoft の人間ではないとコメント。投稿者 ssc-esiemiat が 1.126.0 でも再現すると報告。

**次のアクション:** 空 tool_calls 配列のハンドリング修正の検討。

---

### ![Issue](icons/issue-opened.svg) ["Delete Permanently" has misguiding description](https://github.com/microsoft/vscode/issues/194999) `#194999`
**ステータス:** Open | **ラベル:** bug, file-explorer

**概要:** ファイルを Shift キーで完全削除する際のダイアログ説明が、復元できないことを示しておらず誤解を招くという指摘です。フォルダの完全削除では説明が適切なのにファイルでは不適切で、文言が不整合だという報告です(2023 年からの長期 Issue)。

**直近のアクティビティ:** lczech が「permanently」は取り消せないことを示唆するので、いずれにせよ文言が不整合で紛らわしいとコメント。

**次のアクション:** 確認ダイアログ文言の修正。

---

### ![PR](icons/git-pull-request.svg) [agents: new session action group toolbar f3219f4a](https://github.com/microsoft/vscode/pull/322920) `#322920`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents のセッションリストで、選択中のユーザー作成グループに自動参加する「New Session」をグループヘッダーのツールバーアクションとして追加します。コンポーザのドラフトが破棄された際に意図をクリアするため、`ISessionsManagementService.onDidDiscardNewSession` を新設して連携します。

**直近のアクティビティ:** エンジニアリング bot の CODENOTIFY(@sandy081、@lszomoru)。copilot レビュー(2 件)と hediet による承認。

**変更ファイル:** `sessionGroupsService.ts`(+80)、`sessionsViewActions.ts`(+37/-2)、テスト(+96)など(計 8 ファイル、+273/-2)。

**次のアクション:** 残レビュー対応とマージ。

---

### ![Issue](icons/issue-opened.svg) [Copilot chat Tools definition context bloat](https://github.com/microsoft/vscode/issues/322731) `#322731`
**ステータス:** Open | **ラベル:** new release

**概要:** 自動圧縮が `maxInputTokens` で発動するものの、手動 compact と違い「Tools definition」コンテキストを削減/重複排除せず、長いツール有効 BYOK チャットで tools セグメントが単調増加して利用可能コンテキストを圧迫するという報告です。

**直近のアクティビティ:** 投稿者 SylvanBrocard が追加調査の結果、実際の tools セクションは 15k トークンで、`previous_response_id` を使うため HTTP リクエスト中の割合が 29.7% と表示されるだけの「レポーティング問題」かもしれないが、誤解を招くのでバグと考えると追記。

**次のアクション:** コンテキストウィンドウ内訳の表示計算が誤っていないか調査。

---

### ![Issue](icons/issue-opened.svg) [CLI seems to have problems loading diagnostics](https://github.com/microsoft/vscode/issues/322939) `#322939`
**ステータス:** Open | **ラベル:** なし

**概要:** eslint の問題を作って CLI に問題を尋ねても「ない」と答え、problem ツールすら呼ばないことがあるという、社内メンバー(lramos15)からのバグ報告です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** CLI の診断情報ロード処理の調査。

---

### ![Issue](icons/issue-opened.svg) [Error while installing VS Code Server on WSL](https://github.com/microsoft/vscode/issues/322898) `#322898`
**ステータス:** Open | **ラベル:** なし

**概要:** WSL(Ubuntu 24.04)上のプロジェクトを開くと「VS Code Server for WSL closed unexpectedly」となり、サーバーのダウンロードが 404 Not Found で失敗する不具合です。Insiders 1.127.0(commit fcff1a77…)のサーバーバイナリが CDN にまだ無い状態が原因とみられます。

**直近のアクティビティ:** YJolain が最新アップデートで解決したと報告する一方、M4RC0Sx は自分の環境では未解決(12:54 時点でも 404)とコメント。

**次のアクション:** サーバービルドの公開待ちと状況確認(関連 #322810、#322795)。

---

### ![Issue](icons/issue-opened.svg) [Model picker: add settings to disable "Auto" grouping and expand "Other Models" by default](https://github.com/microsoft/vscode/issues/322935) `#322935`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot モデルピッカーが最近使ったモデルを「Auto」セクションに昇格させ、同じモデルを下の「Other Models」一覧から隠すうえ、「Other Models」が既定で折りたたまれている挙動を改善する要望です。`github.copilot.modelPicker.showRecentlyUsedGroup` と `expandOtherModels` の追加を提案しています(関連 #312908、#266950)。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 設定追加の妥当性検討とトリアージ。

---

### ![Issue](icons/issue-opened.svg) [can't disable quickSuggestions](https://github.com/microsoft/vscode/issues/320783) `#320783`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `editor.quickSuggestions` を other/comments/strings すべて off にし `suggestOnTriggerCharacters` も false にしてもクイックサジェストが表示されるという報告です。1.124.0、Linux 環境です。

**直近のアクティビティ:** 投稿者 geovex が「情報が足りなかったのか?」とコメント。

**次のアクション:** info-needed のままクローズ。必要なら追加情報で再オープン。

---

### ![Issue](icons/issue-opened.svg) [Not make changes in code](https://github.com/microsoft/vscode/issues/322933) `#322933`
**ステータス:** Open | **ラベル:** info-needed

**概要:**「問題を直せず、いかなる変更も行わない」という、詳細のほぼないバグ報告です。拡張 0.32.5、古い VS Code 1.105.1 環境です。

**直近のアクティビティ:** エンジニアリング bot が報告ガイドライン不足と旧バージョン利用の可能性を 2 件案内。

**次のアクション:** 投稿者からの追加情報待ち(info-needed)。

---

### ![Issue](icons/issue-opened.svg) [Communicate session and chat model through clear UI structure](https://github.com/microsoft/vscode/issues/322934) `#322934`
**ステータス:** Open | **ラベル:** ux, agents-window

**概要:** 2026 年 6 月の UX 調査で、参加者は「session」と「chat」を同一概念として扱い(5 人中 4 人が 1:1 とみなす)、1 セッションが複数チャットを持つというマルチチャットモデルを理解できないという問題提起です。用語を変えずに、レイアウトのネストやオンボーディング、視覚言語、既定命名で関係性を可視化する設計探索を提案します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** session/chat 関係を伝える UI 構造の設計探索。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/322932) `#322932`
**ステータス:** Open | **ラベル:** info-needed

**概要:** `justify-content: center;` という補完テキストに対する、本文の説明がほぼ空の Copilot 補完フィードバックです。1.125.1、Windows、Web 開発環境からの自動報告です。

**直近のアクティビティ:** エンジニアリング bot が報告ガイドライン不足と旧バージョン利用の可能性を案内。

**次のアクション:** 投稿者からの追加情報待ち(info-needed)。

---

### ![Issue](icons/issue-opened.svg) [Design the optimal experience for parallel work in the Agents window](https://github.com/microsoft/vscode/issues/322931) `#322931`
**ステータス:** Open | **ラベル:** ux, agents-window

**概要:** 複数の調査で参加者が Agents window の並列作業管理に苦労し、全員(5/5)が直列作業を好んだという知見に基づく設計探索です。並列ストリームの同時監視・変更の帰属・各エージェントの状況把握ができない点を、横並びアクティビティビュー、ストリーム別サマリー、自動 worktree 分離、進捗通知などで改善することを提案します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 並列作業を管理しやすくする設計探索。

---

### ![Issue](icons/issue-opened.svg) [Make the changes that an agent has made immediately obvious](https://github.com/microsoft/vscode/issues/322930) `#322930`
**ステータス:** Open | **ラベル:** ux, agents-window

**概要:** 2026 年 6 月の UX 調査で参加者全員(5/5)がエージェントの変更を素早く直接見たがったが、最新の diff にたどり着くのが難しかったという問題提起です。diff をチャット/アクティビティから 1 クリックで開け、承認前にプレビューを示し、追加変更時にリアルタイム更新することを提案します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** diff へのアクセス性と承認前プレビュー、リアルタイム更新の実装検討。

---

### ![PR](icons/git-pull-request.svg) [Add setting to default to recursive directory collapsing](https://github.com/microsoft/vscode/pull/231684) `#231684`
**ステータス:** Open | **ラベル:** なし

**概要:** ディレクトリを常に再帰的に折りたたむ設定を追加する変更です(#224267 をクローズ)。本文には PR ガイドラインのテンプレートのみ。tree 系の API に再帰折りたたみのオプションを通します。

**直近のアクティビティ:** 直近 24 時間のコメントなし(2024 年作成の長期 PR)。

**変更ファイル:** `abstractTree.ts`、`asyncDataTree.ts`、`indexTreeModel.ts`、`listService.ts`(計 4 ファイル、+18/-0)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Add `"packageManager"` field in `package.json` for corepack](https://github.com/microsoft/vscode/pull/222330) `#222330`
**ステータス:** Closed | **ラベル:** なし

**概要:** Corepack 有効な開発者向けに、`package.json` の `"packageManager"` フィールドを設定し、`yarn` 実行時に Corepack がワーキングツリーを汚す自動追記を防ぐ変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし(2024 年作成)。

**変更ファイル:** `package.json` のみ(+1/-0)。

**次のアクション:** クローズ済み。対応不要。

---

### ![PR](icons/git-pull-request.svg) [Filter bash completion by current text](https://github.com/microsoft/vscode/pull/222563) `#222563`
**ステータス:** Open | **ラベル:** なし

**概要:** `compgen` の WORD 引数を使い、`code --log criti<TAB>` のように現在の入力テキストで bash 補完候補を絞り込む変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし(2024 年作成)。

**変更ファイル:** `resources/completions/bash/code` のみ(+2/-2)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix bug where ending drag on a menu item causes click in Firefox](https://github.com/microsoft/vscode/pull/211245) `#211245`
**ステータス:** Open | **ラベル:** なし

**概要:** Firefox で、ドラッグ後にサイドバー/ステータスバー/アクティビティバー上で離すと意図しないクリックが発生するバグ(#180833)の修正です。アクティビティバー/サイドバー/ステータスバーのボタン、コマンドセンター、レイアウトトグルなどを修正対象とします。

**直近のアクティビティ:** 直近 24 時間のコメントなし(2024 年作成、sanqro が承認済み)。

**変更ファイル:** `actionViewItems.ts`(+19/-2)、`button.ts`(+19/-3)、`statusbarItem.ts`(+27/-2)など(計 7 ファイル、+104/-10)。

**次のアクション:** メンテナの再レビューとマージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix: fix microsoft#214741 declare vscode capbility on linux properly](https://github.com/microsoft/vscode/pull/214784) `#214784`
**ステータス:** Open | **ラベル:** なし

**概要:** Linux のデスクトップエントリで、`.go`/`.php`/`.py` などのコーディングファイルを開く際の候補リストに VS Code が出るよう、ケイパビリティ宣言を適切にする変更です(#214741)。

**直近のアクティビティ:** 直近 24 時間のコメントなし(2024 年作成)。

**変更ファイル:** `resources/linux/code.desktop` のみ(+1/-1)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix pty output make main process oom](https://github.com/microsoft/vscode/pull/285419) `#285419`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** ローカル pty プロセスはレンダラと直接通信するのに、メインプロセスが pty 出力を消費者なしにバッファし続けて OOM を起こす問題(#234393)の修正です。`ProxyChannel.fromService` に `skip` オプションを追加し、メインプロセスの PTY チャンネル登録から `onProcessData` を除外します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。過去には Tyriar が lgtm(DISMISSED)、bpasero と joaomoreno が CHANGES_REQUESTED で、`skip` の文字列マッチより `@IPCNoBuffer` デコレータや手書きチャンネルを提案。

**変更ファイル:** `ipc.ts`(+11/-1)、`ipc.test.ts`(+39)、`app.ts`(+3/-1)(計 3 ファイル、+53/-2)。

**次のアクション:** レビュー指摘に沿って lazy/skip 方式を見直す。

---

### ![PR](icons/git-pull-request.svg) [Fix: correct powershell type identifiers as follows in issue #237199](https://github.com/microsoft/vscode/pull/301321) `#301321`
**ステータス:** Open | **ラベル:** なし

**概要:** macOS で PowerShell の type identifier が機能しない問題(#237199)に対し、UTI を追加する変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build/lib/electron.ts` のみ(+7/-1)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix sash hover flicker at T-junctions](https://github.com/microsoft/vscode/pull/305279) `#305279`
**ステータス:** Open | **ラベル:** なし

**概要:** 貫通する sash から T 字接合部へポインタを動かすとホバーハイライトが一瞬消えてちらつくバグ(#305055)の修正です。`Sash.onMouseLeave` のホバークラス削除を `hoverDelayer.trigger(..., 0)` 経由でスケジュールし、直後の mouseenter が削除をキャンセルできるようにします。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/vs/base/browser/ui/sash/sash.ts` のみ(+1/-1)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [GitHub Enterprise should allow multiple servers](https://github.com/microsoft/vscode/issues/277435) `#277435`
**ステータス:** Open | **ラベル:** feature-request, authentication-github

**概要:** 2 つの GitHub Enterprise アカウントをそれぞれ別拡張(Copilot と Pull Requests)で使いたいが、`github-enterprise.uri` 設定が 1 サーバーしか指定できず実現できないという要望です。

**直近のアクティビティ:** fdecollibus が「+1、同じ問題なのでこの修正のマージを優先してほしい」とコメント。

**次のアクション:** 複数サーバー対応のフィーチャー検討。

---

### ![PR](icons/git-pull-request.svg) [fix: settings editor overflow in 2x2 grid layout](https://github.com/microsoft/vscode/pull/307246) `#307246`
**ステータス:** Open | **ラベル:** なし

**概要:** 2x2 エディタグリッドで設定エディタの内側 splitview がグリッドセルからはみ出す問題(#194412)の修正です。新たに `SETTINGS_TREE_MIN_WIDTH`(200px)を導入し、`minimumWidth` 上書きと splitview の `minimumSize` に使う一方、TOC 非表示判定用の `EDITOR_MIN_WIDTH`(500px)は維持します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `settingsEditor2.ts` のみ(+6/-2)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Reduce CPU usage from spinner animations with GPU hints and prefers-reduced-motion](https://github.com/microsoft/vscode/pull/305610) `#305610`
**ステータス:** Open | **ラベル:** なし

**概要:** スピナーアニメーションの GPU プロセス CPU 使用を抑える変更(#178311)です。全スピナーに `will-change: transform` と `contain: paint layout` を付与し、`prefers-reduced-motion` で `animation-duration: 0s` にし、session 系スピナーの timing を `linear` から `steps()` に変更します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `codicon-modifiers.css`、`tree.css`、`testing.css`、`chatWidget.css` などの CSS と、spec.md 新規(+210)、`extensionsActions.ts`(計 8 ファイル、+273/-4)。

**次のアクション:** メンテナのレビュー待ち(類似 PR #269494 と競合)。

---

### ![PR](icons/git-pull-request.svg) [Add `compile-copilot-extension-full-build` task for copilot](https://github.com/microsoft/vscode/pull/310159) `#310159`
**ステータス:** Open | **ラベル:** なし

**概要:** 同梱 copilot 拡張をソースからビルドし、`node_modules` を `.vscodeignore` で正しくフィルタした CI 同等の出力を生成する新 gulp タスク `compile-copilot-extension-full-build` を追加します(#310156)。`PackageManager.Npm` を使い、node-pty/ripgrep のネイティブシムも展開します。

**直近のアクティビティ:** 直近 24 時間のコメントなし(copilot レビュー0 件)。

**変更ファイル:** `build/lib/extensions.ts`(+111)、`build/gulpfile.extensions.ts`(+23)(計 2 ファイル、+134/-0)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [GitHub: rank user and org repos above public search results in clone picker](https://github.com/microsoft/vscode/pull/310787) `#310787`
**ステータス:** Open | **ラベル:** なし

**概要:**「Clone from GitHub」ピッカーで、認証ユーザー/組織のリポジトリを、GitHub の検索ランキングに関係なく公開リポジトリより前に表示する変更です(#141754)。`getRemoteSources` の Map にユーザーリポジトリを先に入れ、検索結果は未登録分のみ追加する挿入順入れ替えです。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `extensions/github/src/remoteSourceProvider.ts` のみ(+24/-5)。

**次のアクション:** メンテナのレビュー待ち(#313840 と重複対応)。

---

### ![PR](icons/git-pull-request.svg) [fix(github): prioritize user repos in clone search](https://github.com/microsoft/vscode/pull/313840) `#313840`
**ステータス:** Open | **ラベル:** なし

**概要:** 同じく #141754 への対応で、クローン検索でユーザー/組織のリポジトリを GitHub 検索結果より優先する変更です。`organization_member` affiliation を含め、入力時にキャッシュ済みユーザーリポジトリをローカルでクエリマッチします。

**直近のアクティビティ:** 直近 24 時間のコメントなし(copilot レビューと投稿者 PaulCutcliffe のコメントは過去分)。

**変更ファイル:** `extensions/github/src/remoteSourceProvider.ts` のみ(+8/-3)。

**次のアクション:** ドラフト解除とレビュー(#310787 と整理)。

---

### ![PR](icons/git-pull-request.svg) [tree: reduce O(n) linear scan to O(1) in node location resolution](https://github.com/microsoft/vscode/pull/304403) `#304403`
**ステータス:** Open | **ラベル:** なし

**概要:** `getNodeLocation()` が各階層で `parent.children.indexOf(node)` を呼ぶ O(K) の線形走査を、`IIndexTreeNode` に位置をキャッシュする `childIndex` フィールドを追加して O(1) にする最適化です(`// TODO@joao perf!` への対応)。500 ファイル超のディレクトリでクリックやキー操作のたびに O(500) 走査していました。

**直近のアクティビティ:** 直近 24 時間のコメントなし(copilot レビューと投稿者コメントは過去分)。

**変更ファイル:** `src/vs/base/browser/ui/tree/indexTreeModel.ts` のみ(+15/-2)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Give parallel agents more prominent visual identity](https://github.com/microsoft/vscode/issues/322929) `#322929`
**ステータス:** Open | **ラベル:** ux, agents-window

**概要:** 2026 年 6 月の UX 調査で、複数セッションや 2 つ以上のサブエージェントを起動したチャットで何が起きているか追いにくく、出力が「区別のつかないテキストの壁」になるという問題提起です。エージェントごとの色分け/アイコン、スイムレーン、ステータス表示、停止/エラー時の通知を提案します。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** エージェントごとの視覚的アイデンティティの設計探索。

---

### ![PR](icons/git-pull-request.svg) [fix: update github-issues notebooks to use OR between repo filters](https://github.com/microsoft/vscode/pull/317552) `#317552`
**ステータス:** Open | **ラベル:** なし

**概要:** GitHub の issue 検索 API の破壊的変更(`repo:` フィルタ間のスペースが AND 扱いになった)に対応し、`.vscode/notebooks/` の GitHub Issue Notebook の `$repos`/`$REPOS` マクロで明示的に `OR` を使うよう更新します(#252667)。

**直近のアクティビティ:** 直近 24 時間のコメントなし。copilot レビューが、両ノートブックの JSON が途中で切れて不正になっている(低確信度コメント 2 件)と指摘。

**変更ファイル:** `grooming.github-issues`(+3/-3)、`my-work.github-issues`(+1/-127)(計 2 ファイル、+4/-130)。

**次のアクション:** 切り詰められた JSON を復元し有効化、その後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add .mpp and .mxx extensions to C++](https://github.com/microsoft/vscode/pull/286872) `#286872`
**ステータス:** Open | **ラベル:** なし

**概要:** C++20 モジュールファイルの一般的な拡張子 `.mpp`(xmake 既定)と `.mxx`(build2 既定)を組み込み C++ 言語に登録する変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし(copilot レビュー0 件)。

**変更ファイル:** `extensions/cpp/package.json` のみ(+2/-0)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [[OSS Tool] fix: add complete license text with copyright for blackbird-external-ingest-utils and emitter-listener](https://github.com/microsoft/vscode/pull/311679) `#311679`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** OSS ツール実行で検出されたライセンスエラーの修正で、`@github/blackbird-external-ingest-utils@0.3.0`(MIT)と `emitter-listener@1.1.2`(BSD-2-Clause)のプレースホルダだった `fullLicenseText` を、著作権表記付きの完全なライセンス本文に置き換えます。OSS License Fixer による自動生成 PR です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `cglicenses.json` のみ(+52/-6)。ベースは `release/1.117`。

**次のアクション:** マージ後に OSS ツールワークフローを再実行し ThirdPartyNotices.txt を再生成。

---

### ![PR](icons/git-pull-request.svg) [244332 - This is for a bug I rased about Rdp and multiple sessions](https://github.com/microsoft/vscode/pull/247528) `#247528`
**ステータス:** Open | **ラベル:** なし

**概要:** 同一ユーザーで 2 つの RDP セッションにログインし、それぞれから VS Code を起動すると 2 つ目がセッション 1 で開いてしまうバグ(#244332)の修正です。名前付きパイプにセッション固有名を含めてスコープ化します。

**直近のアクティビティ:** 直近 24 時間のコメントなし(2025 年作成)。

**変更ファイル:** `src/vs/base/parts/ipc/node/ipc.net.ts` のみ(+2/-1)。

**次のアクション:** メンテナのレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix build script for build VSCode in Nix](https://github.com/microsoft/vscode/pull/267663) `#267663`
**ステータス:** Open | **ラベル:** なし

**概要:** Nix 環境で VS Code をビルド可能にする変更です。`.git` なしの tarball ビルド、macOS clang での gcc エイリアス不要化、node-pty の postinstall 用 node-gyp 追加、`@emmetio/css-parser` を git+ssh から https tarball へ、決定論ビルド用の `BUILD_DATE` 環境変数などを含みます。

**直近のアクティビティ:** 直近 24 時間のコメントなし(copilot レビューは過去分)。

**変更ファイル:** `package-lock.json`(+680)、`build/npm/postinstall.ts`、`build/lib/date.ts` など多数(計 13 ファイル、+712/-18)。

**次のアクション:** メンテナのレビューと指摘対応。

---

### ![PR](icons/git-pull-request.svg) [Reduce CPU usage from spinner animations](https://github.com/microsoft/vscode/pull/269494) `#269494`
**ステータス:** Open | **ラベル:** なし

**概要:** スピナーアニメーションの GPU プロセス CPU 使用削減(#178311)で、`will-change`、`contain: layout style paint` の付与とフレームレート低減(steps 30→20)、reduce-motion 対応を行う変更です(#305610 と同趣旨の別 PR)。

**直近のアクティビティ:** policy bot が CLA 同意を要求。過去に benibenj が CHANGES_REQUESTED。

**変更ファイル:** `codicon-modifiers.css`、`tree.css`、`testing.css`(計 3 ファイル、+12/-0)。

**次のアクション:** CLA 同意とレビュー指摘対応。#305610 と統合整理。

---

### ![PR](icons/git-pull-request.svg) [build/npm: add explicit npm version check to preinstall.js](https://github.com/microsoft/vscode/pull/278100) `#278100`
**ステータス:** Open | **ラベル:** なし

**概要:** 既存の Node.js バージョンチェックに倣い、`build/npm/preinstall.js` に明示的な npm バージョン検証を追加します(#252372)。`npm_config_user_agent` から npm 版を読み、`semver` で v10.5.0 未満を弾きます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build/npm/preinstall.js` のみ(+37/-30)。

**次のアクション:** メンテナのレビュー待ち(#277602 と重複対応)。

---

### ![PR](icons/git-pull-request.svg) [Add npm 10.5.0 minimum to preinstall](https://github.com/microsoft/vscode/pull/277602) `#277602`
**ステータス:** Open | **ラベル:** なし

**概要:** 同じく #252372 への対応で、`npm_config_user_agent` から npm 版を検出し、semver で npm 10.5.0 以上を要求して古い場合は明確なエラーで終了する変更です。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build/npm/preinstall.js` のみ(+12/-0)。

**次のアクション:** #278100 と整理してレビュー。

---

### ![Issue](icons/issue-opened.svg) [Study participants wanted to see changes that were made per chat/session](https://github.com/microsoft/vscode/issues/322927) `#322927`
**ステータス:** Open | **ラベル:** ux, agents-window

**概要:** 2026 年 6 月の UX 調査で、1 セッション内で複数チャットを動かすと、参加者(5/5)はワークスペース全体ではなく「このチャットが何を変えたか」を見たがったという問題提起です。Changes ビューのストリーム別フィルタ、ターンごとのインライン変更サマリー、チャット別 changes タブなどを設計方向として挙げます。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** チャット/セッション単位の変更可視化の設計探索。

---

### ![PR](icons/git-pull-request.svg) [sessions: drag & drop to reorder workspaces and groups in the sessions list](https://github.com/microsoft/vscode/pull/322916) `#322916`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents window のセッションリストで、ワークスペースセクションヘッダーとユーザーグループヘッダーをドラッグ&ドロップで並べ替え可能にする変更です。新 `ISessionSectionOrderService` が `group:<id>` / `workspace:<label>` の明示的な順序リストと昇格ワークスペース集合を所有し、グループ順は両グルーピングモードで共有されます。

**直近のアクティビティ:** エンジニアリング bot の CODENOTIFY。copilot レビュー(4 件)と aiday-mar による承認。

**変更ファイル:** `sessionSectionOrderService.ts` を新規(+258)、`sessionsList.ts`(+165/-120)、`sessionGroupsService.ts`(+10/-57)など(計 7 ファイル、+533/-198)。

**次のアクション:** マージ済み。フォローアップでキーボード並べ替え対応。

---

### ![PR](icons/git-pull-request.svg) [Add handling for editor frame border width in modern UI layout](https://github.com/microsoft/vscode/pull/322918) `#322918`
**ステータス:** Merged | **ラベル:** なし

**概要:** Modern UI Update 実験が有効なときにエディタ内容がボーダーで切れないようにするレイアウト調整です。ボーダー幅の定数 `EDITOR_FRAME_BORDER_WIDTH`(1px、CSS 変数 `--vscode-strokeThickness` と一致)を追加し、非モーダル時に幅と高さからボーダー分を引きます。

**直近のアクティビティ:** エンジニアリング bot の CODENOTIFY(@benibenj)。copilot レビューと aiday-mar による承認。

**変更ファイル:** `src/vs/workbench/browser/parts/editor/editorPart.ts` のみ(+15/-0)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [sessions: default to Changes after session creation](https://github.com/microsoft/vscode/pull/322913) `#322913`
**ステータス:** Merged | **ラベル:** なし

**概要:** 新規/未作成セッションは Files ビュー既定のまま、最初のリクエストでセッションが作成済みになった時点で補助ビューの既定を Changes に切り替える変更です。Files に戻すなどセッション単位のユーザー選択は復元後も保持します。

**直近のアクティビティ:** copilot レビュー(1 件)と joaomoreno による承認。

**変更ファイル:** `desktopSessionLayoutController.ts`(+51/-35)、そのテスト(+94/-8)、`LAYOUT_CONTROLLER.md` など(計 5 ファイル、+205/-87)。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/322925) `#322925`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** Java の `main` メソッド(Vehicle/Car/Bike のポリモーフィズム例)という補完テキストに対する、説明が空のパフォーマンス系 Copilot 補完フィードバックです。1.126.0、Windows、Java 開発環境からの自動報告です。

**直近のアクティビティ:** エンジニアリング bot が報告ガイドライン不足の可能性を案内。

**次のアクション:** 投稿者からの追加情報待ち(info-needed)。

---

### ![PR](icons/git-pull-request.svg) [Update input needed session animations with pulsing background](https://github.com/microsoft/vscode/pull/322924) `#322924`
**ステータス:** Open | **ラベル:** なし

**概要:** 入力が必要なセッション行に、パルスする黄色背景アニメーションを追加して視覚的に示す変更です(#322242)。行が選択中のときや reduced-motion 設定時はアニメーションを抑制し、`session-needs-input-accent-pulse` などの `@keyframes` を導入します。

**直近のアクティビティ:** copilot レビュー(4 件)と hediet による承認。

**変更ファイル:** `sessionsList.css`(+22)、`agentsessionsviewer.css`(+22)(計 2 ファイル、+44/-0)。

**次のアクション:** レビュー指摘対応とマージ。

---

### ![Issue](icons/issue-opened.svg) [I thought i had calude sonnet 4.5 selected](https://github.com/microsoft/vscode/issues/293979) `#293979`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Claude Sonnet 4.5 を選択したつもりが「family selection gpt-4o-mini」と表示されるという、ほぼ説明のないバグ報告です。拡張 0.37.1、1.109.0 環境です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Linux agent sandboxing broken when `allowNetwork` mode is set](https://github.com/microsoft/vscode/issues/320504) `#320504`
**ステータス:** Closed | **ラベル:** info-needed, agent-sandbox

**概要:** `chat.agent.sandbox.enabled` を `allowNetwork` にすると、`denyRead: ["~"]` などの設定や `allowUnsandboxedCommands: false` にもかかわらずワークスペース外ファイルが読めてしまい、ファイルシステム分離が強制されない不具合です(#314041 の検証中に発見、bubblewrap 利用)。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。必要なら再オープン。

---

### ![Issue](icons/issue-opened.svg) [converto to lazy logging](https://github.com/microsoft/vscode/issues/321753) `#321753`
**ステータス:** Closed | **ラベル:** info-needed

**概要:**「request fialed: 400」とだけ書かれた、内容の乏しいバグ報告です。拡張 0.36.2、1.108.1 環境です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [[Codespaces] Copilot model selector not populated until first message is sent](https://github.com/microsoft/vscode/issues/321822) `#321822`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** GitHub Codespace 起動直後、ダミーメッセージを送るまで Copilot のモデルセレクタに全モデルが表示されないという不具合です。「hi」などを一度送ると全モデルが出るとのこと。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。必要なら再オープン。

---

### ![Issue](icons/issue-opened.svg) [claude not loding chat in window](https://github.com/microsoft/vscode/issues/321840) `#321840`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** リロードや PC 再起動、VS Code 再起動をしてもチャットが何も動かないという、詳細の乏しいバグ報告です。Claude Code 拡張入り、1.125.0 環境です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat: auto-generated title corrupted with wrong-language characters ("原因" appended)](https://github.com/microsoft/vscode/issues/321853) `#321853`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Copilot Chat の自動生成タイトルが、英語の会話なのに「Microsoft service outage 原因」のように中国語(原因)が末尾に付いて壊れたという報告です。ストリーミングのデータ破損が原因ではないかと仮説を立てています(関連 #316994、#316393、#312948)。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。再現できれば再オープン。

---

### ![Issue](icons/issue-opened.svg) [kill terminal proplem](https://github.com/microsoft/vscode/issues/321859) `#321859`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** タイトルと再現手順がほぼ空のターミナル関連バグ報告です。本文はテンプレートのみで具体的な情報がありません。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/321863) `#321863`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Python の `def calcularCosto(self):` という補完テキストに対する、説明が空の Copilot 補完フィードバックです。1.125.0 環境からの自動報告です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Selecting code makes the markdown editor scroll up](https://github.com/microsoft/vscode/issues/322928) `#322928`
**ステータス:** Open | **ラベル:** なし

**概要:** マウスで上方向にドラッグして選択すると、Markdown エディタのスクロール位置がジャンプするバグです(社内メンバー alexdima、動画添付、Insiders 1.127.0)。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** スクロール位置ジャンプの原因調査。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/321884) `#321884`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Java のバブルソート(`Display`/swap 出力付き)という補完テキストに対する、説明が空の Copilot 補完フィードバックです。1.125.0 環境からの自動報告です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/321895) `#321895`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Python の `class CurrentAccount(BankAccount):` という補完テキストに対する、説明が空の Copilot 補完フィードバックです。1.125.0 環境からの自動報告です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Copilot completion feedback](https://github.com/microsoft/vscode/issues/321911) `#321911`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `const API = "http://localhost:3000/products"` という補完テキストに対する、説明が空の Copilot 補完フィードバックです。1.125.0 環境からの自動報告です。

**直近のアクティビティ:** エンジニアリング bot が、info-needed のまま 7 日間追加情報がなかったためクローズしたと通知。

**次のアクション:** 追加情報なしでクローズ。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Extension Host Restart: kills blocked extension host on restart extension host (issue #296681)](https://github.com/microsoft/vscode/issues/322926) `#322926`
**ステータス:** Open | **ラベル:** important, smoke-test-failure

**概要:** スモークテスト「Extension Host Restart: kills blocked extension host on restart extension host」(#296681 関連)の失敗を記録した Issue です。本文は失敗した CI ジョブの URL のみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** フレーキー/失敗テストの調査と修正。

---

### ![Issue](icons/issue-opened.svg) [Installing VS Code Server for Linux x64 fails (404 Not Found)](https://github.com/microsoft/vscode/issues/322810) `#322810`
**ステータス:** Closed | **ラベル:** なし

**概要:** WSL2(Ubuntu-20.04)で `code-insiders .` を実行すると、サーバーのダウンロードが 404 Not Found で失敗してウィンドウが開かない不具合です。Insiders 1.127.0(commit fcff1a77…)のサーバーバイナリが未公開なのが原因とみられます(#322898 と同根)。

**直近のアクティビティ:** TheCaptainCat/cirolosapio が CI パイプラインについてやり取りし、JeffreyCA が旧 Insiders 版での回避策を提示。aiday-mar が最新 Insiders で修正されるはずとコメント。

**次のアクション:** サーバービルド公開で解決としてクローズ。

---

### ![Issue](icons/issue-opened.svg) [Data Loss (insiders -> insiders: verifies editors can save and restore is flaky](https://github.com/microsoft/vscode/issues/322923) `#322923`
**ステータス:** Open | **ラベル:** important, smoke-test-failure

**概要:** スモークテスト「Data Loss (insiders -> insiders): verifies editors can save and restore」がフレーキーだという Issue です。本文は失敗した CI ジョブの URL のみ。

**直近のアクティビティ:** alexr00 が、テストファイルを最後に触った TylerLeonhardt にアサインし、エージェントも投入すると追記。

**次のアクション:** フレーキーテストの原因調査と修正。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump actions/checkout from 6 to 7](https://github.com/microsoft/vscode/pull/322922) `#322922`
**ステータス:** Open | **ラベル:** dependencies, github_actions

**概要:** dependabot による `actions/checkout` の 6→7 バンプです。v7 では ESM 移行や、pull_request_target/workflow_run でのフォーク PR チェックアウトブロックなどの変更があります。

**直近のアクティビティ:** copilot がレビューでエラーに遭遇しレビューできなかった旨を報告。

**変更ファイル:** `pr.yml`、`pr-linux-test.yml`、`pr-node-modules.yml` など 14 個のワークフロー(計 14 ファイル、+26/-26)。

**次のアクション:** CI 確認とマージ(自動マージ対象)。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump actions/cache from 5 to 6](https://github.com/microsoft/vscode/pull/322921) `#322921`
**ステータス:** Open | **ラベル:** dependencies, github_actions

**概要:** dependabot による `actions/cache` の 5→6 バンプです。v6 では ESM 移行とパッケージ更新が行われています。

**直近のアクティビティ:** copilot がレビューでエラーに遭遇しレビューできなかった旨を報告。

**変更ファイル:** `chat-perf.yml`、`pr.yml`、`monaco-editor.yml` など 8 個のワークフロー(計 8 ファイル、+14/-14)。

**次のアクション:** CI 確認とマージ(自動マージ対象)。

---

### ![PR](icons/git-pull-request.svg) [Improve activity bar styles for consistency and appearance](https://github.com/microsoft/vscode/pull/322711) `#322711`
**ステータス:** Merged | **ラベル:** なし

**概要:** 水平アクティビティバー(上/下配置)のアクティブ/ホバー項目を、デフォルト(垂直)アクティビティバーと視覚的に揃える Modern UI スタイル調整です。フラットな塗りと境界線を、インセットの角丸背景「チップ」に置き換え、アイコンの色/サイズを正規化します。エディタタブ高さも 26px→24px に縮小します。

**直近のアクティビティ:** copilot レビュー(2 件)と dbaeumer による承認。

**変更ファイル:** `activityBar.css`(+217/-1)、`tabs.css`(+1/-1)(計 2 ファイル、+218/-2)。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Terminal Font ligature not working](https://github.com/microsoft/vscode/issues/320968) `#320968`
**ステータス:** Open | **ラベル:** なし

**概要:** ターミナルのリガチャを有効にし JetBrains Mono などのフォントを使っても、`calt` 指定の有無にかかわらずリガチャが機能しないという報告です。GPU アクセラレーション有効との関連を疑っています。

**直近のアクティビティ:** dmitrivMS が 1.125.1 でも再現するか確認し、投稿者 UsmanTariq2 が 1.125.1 で動作するようになったと報告。

**次のアクション:** 解決の可能性が高いが、Open のまま。クローズ可否の確認。

---

### ![Issue](icons/issue-opened.svg) [Local Model Problem](https://github.com/microsoft/vscode/issues/256741) `#256741`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Qwen2.5-Coder 使用時、チャットでファイル修正を依頼すると「Sorry, I can't assist with that」と返されるという報告です。拡張 0.29.1、1.102.1 環境です。

**直近のアクティビティ:** 直近 24 時間のコメントなし(クローズ済み)。

**次のアクション:** クローズ済み。対応不要。

---

### ![Issue](icons/issue-opened.svg) [Agent Window : MCP servers setting and custom agents aren't loaded in remote ssh sessions](https://github.com/microsoft/vscode/issues/316880) `#316880`
**ステータス:** Open | **ラベル:** bug, agents-window, agent-host

**概要:** Copilot CLI 用にグローバル設定したカスタムエージェントと MCP サーバーが、Agents window でリモート SSH ワークスペースを選んだときに読み込まれず、skills だけが読み込まれるという不具合です。リモートマシンの `/home/user/.copilot` に定義されており、CLI 直接や通常のリモート SSH セッションでは正しく読み込まれます。

**直近のアクティビティ:** aeschli がカスタムエージェントの場所を質問し、投稿者 ibnelwalid がリモートマシン上の `.copilot` 配下だと回答(agent ファイルは読まれるが skills と MCP は読まれない)。

**次のアクション:** Agents window のリモート設定ロードパスの調査。

---

### ![Issue](icons/issue-opened.svg) [Integrated terminal automatically sends 'Y' when terminating node batch script](https://github.com/microsoft/vscode/issues/142341) `#142341`
**ステータス:** Closed | **ラベル:** bug, verified, candidate, windows, terminal-conpty, unreleased, terminal-input

**概要:** 統合ターミナルで node のバッチスクリプトを Ctrl+C で終了する際、「Terminate batch job (Y/N)?」に `Y` が自動送信され、最初のスクリプトだけ終了して残出力が次行のプロンプトに混入するバグです(2022 年からの検証済み Windows/conpty 問題)。

**直近のアクティビティ:** 直近 24 時間のコメントなし(クローズ済み)。

**次のアクション:** クローズ済み(unreleased ラベル付き、修正は未リリース扱い)。

---

### ![Issue](icons/issue-opened.svg) [VSCode freezes when opening a terminal](https://github.com/microsoft/vscode/issues/239838) `#239838`
**ステータス:** Closed | **ラベル:** important, freeze-slow-crash-leak, candidate, terminal, gpu, unreleased

**概要:** 1.97.0 にアップデート後、TypeScript プロジェクトフォルダを開くと VS Code がフリーズして使用不能になるという、ターミナル/GPU 関連の重要なバグ報告です(2025 年初)。

**直近のアクティビティ:** 直近 24 時間のコメントなし(クローズ済み)。

**次のアクション:** クローズ済み(unreleased ラベル付き)。

---

### ![PR](icons/git-pull-request.svg) [sessions: add 'Run VS Code (Web)' agents window task](https://github.com/microsoft/vscode/pull/322652) `#322652`
**ステータス:** Merged | **ラベル:** なし

**概要:** `.vscode/tasks.json` に、Agents window に表示される(`inAgents: true`)「Run VS Code (Web)」タスクを追加します。`./scripts/code-server.sh`(Windows は `.bat`)を `--connection-token dev-token --port 8080` で実行し、`Web UI available at …` 出力で完了するバックグラウンドタスクです。

**直近のアクティビティ:** エンジニアリング bot の CODENOTIFY(@lszomoru)。copilot レビュー(1 件)と DonJayamanne による承認。

**変更ファイル:** `.vscode/tasks.json` のみ(+29/-0)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [editor: show editor layout actions in single tab mode](https://github.com/microsoft/vscode/pull/322908) `#322908`
**ステータス:** Merged | **ラベル:** なし

**概要:** `SingleEditorTabsControl.prepareEditorLayoutActions` が常に空を返していたため、`workbench.editor.showTabs: single` のとき Agents window が `MenuId.EditorTitleLayout` に寄与するレイアウトアクション(Maximize/Restore/Close Editor Area、Open in Modal Editor、副サイドバートグル)が消えていた問題の修正です。通常の VS Code エディタには影響しません。

**直近のアクティビティ:** エンジニアリング bot の CODENOTIFY(@benibenj)。copilot レビュー(1 件)と alexdima による承認。

**変更ファイル:** `src/vs/workbench/browser/parts/editor/singleEditorTabsControl.ts` のみ(+6/-5)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [List biggest consumers of disk space before/after smoke tests run](https://github.com/microsoft/vscode/pull/322903) `#322903`
**ステータス:** Merged | **ラベル:** なし

**概要:** Linux の Azure Pipelines スモークテストジョブで、「No space left on device」障害を診断・緩和する変更です(#322880 のフォローアップ)。スモーク前後にディスク使用状況と上位消費を報告し、`TMPDIR` を `$(Agent.TempDirectory)` に設定してスモークの一時データを `/tmp` から逃がします。

**直近のアクティビティ:** エンジニアリング bot の CODENOTIFY(@lszomoru)。copilot レビュー(0 件)と lszomoru による承認。

**変更ファイル:** `build/azure-pipelines/linux/steps/product-build-linux-test.yml` のみ(+19/-0)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [AgentHost - handle edge case to disable action while chat is in progress](https://github.com/microsoft/vscode/pull/322881) `#322881`
**ステータス:** Merged | **ラベル:** なし

**概要:** agent host の changeset 操作の安全性を高め、チャットターンが進行中の間は操作を実行不可に保つ変更です。ターン途中でチャット状態が削除される場合や、操作ステータスが idle に戻る場合のエッジケースに対応し、`AgentHostChangesetOperationService` に呼び出し時のアクティブターンガードを追加します。

**直近のアクティビティ:** copilot レビュー(0 件)と chrmarti による承認。

**変更ファイル:** `agentHostStateManager.ts`(+35/-13)、`agentHostChangesetOperationService.ts`(+11)、両テスト(+85、+39)(計 4 ファイル、+170/-14)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [agents: remove session count header ca108775](https://github.com/microsoft/vscode/pull/322899) `#322899`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agents window のセッションリストから、セッショングループの「件数」UI 要素を削除する変更です。`ISessionGroupTemplate` から `count` を外し、`.session-section-count` span のレンダリングをやめます(セッション/グループモデルは不変)。

**直近のアクティビティ:** copilot レビュー(0 件)と chrmarti による承認。

**変更ファイル:** `src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts` のみ(+1/-4)。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix context size default + reload behaviour, add defensive fallbacks](https://github.com/microsoft/vscode/pull/322882) `#322882`
**ステータス:** Merged | **ラベル:** なし

**概要:** 設定/スキーマが非同期ロードされる際の chat「context size」導出を調整する変更です。`ChatModelConfigurationStore` が言語モデル変更時に新規スキーマ既定をキャッシュ済みスナップショットへマージし、context-usage ウィジェットは未ロード時にスキーマ既定 `contextSize` へフォールバック、Copilot の `applyContextSizeOverride` は `tokenPricing.default.contextMax` へフォールバックします。

**直近のアクティビティ:** copilot レビュー(2 件、低確信度 1 件含む)と chrmarti による承認。

**変更ファイル:** `chatModelConfigurationStore.test.ts`(+311)、`chatModelConfigurationStore.ts`(+38/-24)、`agentIntent.ts`(+18/-4)、`chatContextUsageWidget.ts`(+31/-3)など(計 6 ファイル、+495/-33)。

**次のアクション:** マージ済み。

---

---

## qmk/qmk_firmware

> Linux/Steam の gamepad 誤認バグ修正がマージされたほか、データブロック初期化の一貫化、マルチプロファイル Bluetooth、framebuffer エフェクト改善など core 寄りの変更が中心。長期 WIP の XAP も活動あり。

### ![PR](icons/git-pull-request.svg) [Make framebuffer effects honour `rgb_matrix_map_row_column_to_led`](https://github.com/qmk/qmk_firmware/pull/26294) `#26294`
**ステータス:** Open | **ラベル:** enhancement, core

**概要:** `rgb_matrix_map_row_column_to_led`（1 つのマトリクス位置に複数 LED を対応づける仕組み）が typing heatmap で未対応、digital rain でも部分実装だった問題に対処する PR。また typing heatmap で `g_led_config.matrix_co[row][col]` が `NO_LED`（255）を返すと `g_led_config.flags` を範囲外参照しうる問題も改善する。

**直近のアクティビティ:** drashna が「この変更が（少なくとも split ボードで）これらのアニメーションを壊す」と報告。作者 zvecr が「重複マトリクスエントリがなければ digital rain の挙動は変わらないはずで、どう壊れるのか具体的に教えてほしい。`decrease heatmap` ロジックが頻繁に動きすぎていた点は修正した」と応答。

**変更ファイル:** 2 ファイル変更（+29/-12）。`quantum/rgb_matrix/animations/typing_heatmap_anim.h`（+22/-9）と `digital_rain_anim.h`（+7/-3）。

**次のアクション:** split ボードでアニメーションが壊れる原因の切り分けと修正。

---
### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open（Draft）| **ラベル:** core, keyboard, keymap, breaking_change, python, cli, feature, ほか

**概要:** XAP（eXtensible Application Protocol）の実装作業を追跡する WIP PR（#11567 の実装）。スコーピング文書に基づき進行中で、ファームウェア・Python クライアント・ドキュメントの生成や、lighting map、各種 USB プロトコル（ChibiOS / LUFA / V-USB）対応などを含む大規模な変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし（最新の活動はレビュー）。レビューは過去に t-8ch・tzarc が COMMENTED、jock070 が APPROVED。

**変更ファイル:** 96 ファイル変更（+7548/-26）と非常に大規模。`data/xap/*.hjson` や `lib/python/qmk/xap/`、`quantum/xap/`、`tmk_core/protocol/` 配下を広範に新規追加・修正。

**次のアクション:** WIP のため実装・ドキュメント・テストの継続。長期にわたる大規模 PR。

---
### ![Issue](icons/issue-opened.svg) [[Bug]  EXTRAKEY_ENABLE build flag is breaking steaminput on linux](https://github.com/qmk/qmk_firmware/issues/25277) `#25277`
**ステータス:** Closed | **ラベル:** bug, help wanted

**概要:** デフォルトの `EXTRAKEY_ENABLE` でビルドするとキーボードが利用可能な全 HID をサポートすると報告し、Linux で steaminput を壊す bug。Steam や KDE システム設定がキーボードを joystick / gamepad と誤認し、例えば DualShock 4 が Dark Souls 2 で動かなくなる。

**直近のアクティビティ:** tzarc が「#26295 でクローズ」とコメント。

**次のアクション:** #26295 のマージにより解決済み。対応不要。

---
### ![PR](icons/git-pull-request.svg) [Restrict the EXTRAKEY_ENABLE System Control HID usage range.](https://github.com/qmk/qmk_firmware/pull/26295) `#26295`
**ステータス:** Merged | **ラベル:** core, documentation

**概要:** #25277 を修正する PR。`EXTRAKEY_ENABLE` の System Control HID usage 範囲を `0x0081`-`0x008F` に制限し、Linux/Steam がキーボードを gamepad と誤認する原因だった Generic Desktop の D-pad usage（`0x90`+）に跨らないようにする。範囲は `SYSTEM_CONTROL_USAGE_MINIMUM`/`_MAXIMUM` で上書き可能で、LUFA/ChibiOS と V-USB の両ディスクリプタに適用。

**直近のアクティビティ:** zvecr が APPROVED（本文なし）。

**変更ファイル:** 4 ファイル変更（+36/-16）。`tmk_core/protocol/usb_descriptor_common.h`（+16）、`tmk_core/protocol/vusb/vusb.c`（+12/-12）、`tmk_core/protocol/usb_descriptor.c`、`docs/config_options.md`。

**次のアクション:** マージ済みのため対応なし。

---
### ![PR](icons/git-pull-request.svg) [feat: implement multi profile bluetooth support](https://github.com/qmk/qmk_firmware/pull/26199) `#26199`
**ステータス:** Open | **ラベル:** core, documentation, stale

**概要:** 予約済みで未配線だった `QK_BLUETOOTH_PROFILE_*` と `QK_BLUETOOTH_UNPAIR` キーコードを実装する PR。`connection_config_t` に 4bit の `bluetooth_profile` フィールドを既存 1 バイトスロットに同居させ（EEPROM レイアウトのシフトなし）、`bluetooth.h` に新 API（`bluetooth_get_max_profile` 等）を追加。`rn42` と `bluefruit_le` は weak デフォルトで単一プロファイルのまま動作し、ドライバが 4 関数を上書きすればマルチプロファイル化できる。`BT_PRF1` を USB から押すと bluetooth ホストへ切り替えつつ選択する。

**直近のアクティビティ:** github-actions[bot]が「45 日間アクティビティがないため stale。30 日後にクローズ」と通知（`stale` ラベル）。

**変更ファイル:** 7 ファイル変更（+224/-24）。`quantum/connection/connection.c`（+81/-4）、`connection.h`（+41/-2）、`drivers/bluetooth/bluetooth.h`（+30）、`docs/features/wireless.md`（+29/-8）など。

**次のアクション:** 自動クローズを避けるためのステータス更新と、メンテナーによるレビュー。

---
### ![PR](icons/git-pull-request.svg) [Add "handwired" TwentyPad macro pad](https://github.com/qmk/qmk_firmware/pull/26264) `#26264`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** 趣味で作成した 20 キーのマクロパッド TwentyPad を追加する PR。ez_maker/directpins/teensy_2 をベースに、bootloader とレイアウトを変更している。

**直近のアクティビティ:** レビューで zvecr・tzarc・drashna が CHANGES_REQUESTED と COMMENTED を繰り返し、zvecr は一度 APPROVED したものの drashna が再度 CHANGES_REQUESTED。作者 HarryCutts が各指摘に COMMENTED で応答している。

**変更ファイル:** 3 ファイル新規追加（+94）。`keyboards/handwired/twentypad/keyboard.json`（+54）、`keymaps/default/keymap.json`（+14）、`readme.md`（+26）。

**次のアクション:** drashna の最新の変更要求への対応。

---
### ![PR](icons/git-pull-request.svg) [Consistently handle datablock init](https://github.com/qmk/qmk_firmware/pull/26282) `#26282`
**ステータス:** Merged | **ラベル:** core, documentation, python, cli, community_module

**概要:** 各データブロックの初期化で「`eeconfig_is_*_datablock_valid()` が false なら `eeconfig_init_*_datablock()` を呼ぶ」処理が起動時のどこかで正しく実装されることを期待する脆い構造を、全データブロックで自動的に行うようにする PR。あわせて `eeconfig_init_<module>_datablock` を kb/user API に倣い weak に揃える。

**直近のアクティビティ:** drashna が「テスト済みで期待通り動作する」と APPROVED、tzarc も「軽微な nit」付きで APPROVED。drashna・zvecr が COMMENTED でやり取り。

**変更ファイル:** 6 ファイル変更（+64/-13）。`quantum/eeconfig.c`（+18）、`docs/feature_eeprom.md`（+20/-5）、`lib/python/qmk/cli/generate/community_modules.py`（+15/-4）など。

**次のアクション:** マージ済みのため対応なし。

---
