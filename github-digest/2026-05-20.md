# GitHub ダイジェスト — 2026-05-20

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[initial usbgpu support](https://github.com/commaai/openpilot/pull/37906)** (commaai/openpilot) — USB 接続 GPU を `modeld` / `dmonitoringmodeld` で利用するための大規模対応（+182 / -57、17 ファイル）。big モデルの起動時コンパイル、ホットプラグ、UI 状態表示までを束ねる基盤 PR。
2. ![PR](icons/git-pull-request.svg) **[VW MQB: work around standstill limit](https://github.com/commaai/opendbc/pull/2764)** (commaai/opendbc) — EPB を持たない VW MQB 車両で停止時のブレーキ保持が ESP フォルトを引き起こす問題を回避し、完全な stop-and-go を実現する大規模変更（+622 / -25）。RJWadley・jyoung8607 らが詳細レビュー中。
3. ![PR](icons/git-pull-request.svg) **[Update to Zig 0.16.0](https://github.com/ghostty-org/ghostty/pull/12726)** (ghostty-org/ghostty) — Ghostty を Zig 0.16.0 に移行する大規模 PR（+4062 / -2259、100 ファイル）。stdlib I/O 必須化への対応や互換レイヤ `src/lib/compat` の整備など段階的アップグレードを設計。
4. ![PR](icons/git-pull-request.svg) **[Reject locked malware installations](https://github.com/astral-sh/uv/pull/18936)** (astral-sh/uv) — `uv sync` / `uv add` の同期処理に OSV ベースで `MAL-` レポートを判定するチェックを組み込み、検出時はインストール拒否。`UV_MALWARE_CHECK` / `UV_MALWARE_CHECK_URL` で制御可能なサプライチェーン防御。
5. ![Issue](icons/issue-opened.svg) **[Uninstalling Nightly and Trying to Connect to Wi-Fi network stuck on Waiting for Internet (C4 and C3X)](https://github.com/commaai/openpilot/issues/38034)** (commaai/openpilot) — `modem.py` (#37811) の変更で `get_network_type()` が常にモデム状態を先に参照する仕様となり、その副作用で setup 画面の Wi-Fi 接続待ちが永久停止する回帰報告。`Codex` が原因コミットを特定。

---

## commaai/openpilot

> USB GPU の `modeld` 統合、`agnos 18.3` のリリース、modem.py 周りの roaming/ICCID 修正、Connect でのリモートテレオペレーション拡張、release ビルドのファイルサイズ検査強化など、デバイスソフトウェアと CI の両面で動きがある。

### ![PR](icons/git-pull-request.svg) [initial usbgpu support](https://github.com/commaai/openpilot/pull/37906) `#37906`
**ステータス:** Open | **変更量:** +182 / -57 (17 files)

**概要:** USB GPU が接続されているデバイスで起動時に big モデルをコンパイルし、オンロード遷移時に big モデルを選択して UI に状態表示する。`modeld` / `dmonitoringmodeld` に異なるデバイスを割り当て可能にし、ホットプラグや AGNOS への firmware 同梱まで含む大規模対応。

**直近のアクティビティ:** ボットによる model replay 参照、UI プレビュー（mici は同一、big は差分あり）、process replay 差分なしの自動コメントのみ。

**変更ファイル:** `selfdrive/modeld/` 配下のスクリプト群、`modeld.py`、`dmonitoringmodeld.py`、`big_driving_policy.onnx`、`big_driving_vision.onnx` などのモデルアセット。

**次のアクション:** TODO リスト（UI アイコン追加、model replay 対応、bf16/fp16 再エクスポート）の消化と human review。

---

### ![PR](icons/git-pull-request.svg) [loggerd: fix flaky test case](https://github.com/commaai/openpilot/pull/38068) `#38068`
**ステータス:** Merged | **ラベル:** CI / testing | **変更量:** +7 / -1

**概要:** `system/loggerd/tests/test_loggerd.py` の flaky なテストケースを修正。

**直近のアクティビティ:** process replay 差分なしの自動コメントのみ。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [modeld/SConscript: fix pkl chunking](https://github.com/commaai/openpilot/pull/38067) `#38067`
**ステータス:** Merged | **変更量:** +7 / -9

**概要:** `selfdrive/modeld/SConscript` の pkl チャンク分割処理を修正。

**直近のアクティビティ:** 自動ボットコメントのみ（model replay 参照と process replay 差分なし）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build_release: check file sizes in CI](https://github.com/commaai/openpilot/pull/38066) `#38066`
**ステータス:** Open | **変更量:** +18 / -0

**概要:** `build_release.sh` は Jenkins で nightly 後にしか動かないが、`build_stripped.sh` は GH Actions で走るがビルドを行わない。ビルド時のファイルサイズ問題を Jenkins より手前で表面化させるため、`release/check-file-sizes.sh` を追加し CI ワークフローから呼ぶ。

**直近のアクティビティ:** `adeebshihadeh` が「scons 側に組み込んだら？」と提案、`sshane` が「PC でのピクルはデバイスより小さい」と補足。

**変更ファイル:** `.github/workflows/tests.yaml`、`release/check-file-sizes.sh`。

**次のアクション:** scons への組み込み可否を含む方針確定。

---

### ![PR](icons/git-pull-request.svg) [agnos 18.3](https://github.com/commaai/openpilot/pull/38063) `#38063`
**ステータス:** Merged | **変更量:** +13 / -13

**概要:** AGNOS 18.3 リリース。`test_onroad` パス、Wi-Fi、image size 不増、ファクトリリセット、60FPS 表示などのリリースチェックリストを通過した状態でマージ。

**直近のアクティビティ:** process replay 差分なしの自動コメントのみ。

**変更ファイル:** `launch_env.sh`、`system/hardware/tici/agnos.json`、`all-partitions.json`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [minimal build by default on device](https://github.com/commaai/openpilot/pull/38023) `#38023`
**ステータス:** Merged | **変更量:** +4 / -2

**概要:** デバイス上では既定でミニマルビルドにする変更。`SConstruct` のみ修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [remote body teleop from connect support](https://github.com/commaai/openpilot/pull/38011) `#38011`
**ステータス:** Open | **変更量:** +93 / -21

**概要:** Connect 経由でジョイスティックライブストリームを使った body のテレオペレーションを実現。`athena` での `getNotCar` / `startJoystick` ディスパッチ、別ストリーム起動時の前ストリーム掃除、エラー伝達改善を含む。

**直近のアクティビティ:** human review 5 件（COMMENTED のみ、本文なし）。process replay 差分なし。

**変更ファイル:** `system/athena/athenad.py`、`system/webrtc/webrtcd.py`。

**次のアクション:** レビュアーからのコメント解消とドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [modem.py: roaming is always allowed for prime](https://github.com/commaai/openpilot/pull/38065) `#38065`
**ステータス:** Merged | **変更量:** +2 / -0

**概要:** Prime ユーザーに対しては常にローミングを許可するよう `modem.py` を変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [modem.py: roaming is always allowed for prime](https://github.com/commaai/openpilot/pull/38064) `#38064`
**ステータス:** Merged | **変更量:** +4 / -0

**概要:** #38065 と同名タイトルでの先行マージ PR。Prime 利用者向けにローミングを常時許可する `modem.py` 変更。

**直近のアクティビティ:** process replay 差分なしの自動コメントのみ。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: resolve bounty issue #30302 - Ford Q4 harness relay issue on F150 Lightning](https://github.com/commaai/openpilot/pull/38061) `#38061`
**ステータス:** Closed | **変更量:** +2 / -2

**概要:** Ford Q4 ハーネス（CAN-FD）で openpilot が SILENT / NOOUTPUT モードのときリレーが非駆動となり F150 Lightning でトラクション制御や Park Assist のフォールトが出る問題への対応案。`panda/board/main.c` で Ford CAN-FD 用 safety フラグを見てリレー駆動を維持し、`opendbc` 側の F150 Lightning ドキュメント除外ハックを撤去。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `opendbc_repo`、`panda`（サブモジュール参照）。

**次のアクション:** Closed（採用見送り）。

---

### ![PR](icons/git-pull-request.svg) [Use pandacan as panda dependency](https://github.com/commaai/openpilot/pull/38062) `#38062`
**ステータス:** Closed | **変更量:** +1 / -6

**概要:** `panda` サブモジュールを取り除き、firmware アーティファクトを同梱した `pandacan` パッケージへの依存に切り替える検証 PR。`premkiran2/panda@codex/pip-package-fw` ブランチ依存。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.gitmodules`、`panda`、`pyproject.toml`。

**次のアクション:** Closed（pandacan 0.0.11 公開待ちでの一時クローズ）。

---

### ![PR](icons/git-pull-request.svg) [modeld: no runtime compile](https://github.com/commaai/openpilot/pull/38060) `#38060`
**ステータス:** Merged | **変更量:** +6 / -6

**概要:** ゼロテンソル生成にコンパイルとカーネル実行が必要だったため、`modeld` でこれをランタイム時の `numpy` 配列コピーに置き換える変更。amdgpu ではコンパイラ設定も不要になる。

**直近のアクティビティ:** `Armandpl` が起動時間への寄与を 0.2 秒と試算。`adeebshihadeh` が「0.5 秒の起動時間に効いていたのか」と質問。

**次のアクション:** マージ済み。残りの起動時間退行原因（#38041 の 0.7 秒など）の調査継続。

---

### ![PR](icons/git-pull-request.svg) [agnos: lazy-import casync so standalone CLI works without pycryptodome](https://github.com/commaai/openpilot/pull/38048) `#38048`
**ステータス:** Closed | **変更量:** +2 / -2

**概要:** `agnos.py` の standalone CLI（`--swap` / `--verify`）が `pycryptodome` 不在環境で `ModuleNotFoundError: No module named 'Crypto'` で起動しない問題への対応案。`casync` import を `extract_casync_image()` 内に遅延させて解消する。

**直近のアクティビティ:** `adeebshihadeh` が「venv をアクティベートしていないだけでは？」と指摘。`brzdev` が「依存解決に手間取っていたが結局動いた」として自身でクローズ。

**変更ファイル:** `system/hardware/tici/agnos.py`。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [comma four: warning for enabling experimental mode](https://github.com/commaai/openpilot/pull/37961) `#37961`
**ステータス:** Open | **ラベル:** ui | **変更量:** +47 / -3

**概要:** Comma four で experimental モードを有効化する際に警告を出す UI 変更。

**直近のアクティビティ:** UI プレビュー（mici は同一、big は差分あり）、process replay 差分なしの自動コメントのみ。

**変更ファイル:** `selfdrive/ui/mici/layouts/home.py`、`settings/toggles.py`、`ui/ui_state.py`。

**次のアクション:** UI プレビューの差分確認とレビュー。

---

### ![Issue](icons/issue-opened.svg) [Masda Cx5 adaptive cruise control not working](https://github.com/commaai/openpilot/issues/29122) `#29122`
**ステータス:** Closed | **ラベル:** bug

**概要:** 2022 Mazda CX-5 で openpilot を有効化したときアダプティブクルーズコントロールが前車に対して停止しないというバグ報告。openpilot 0.9.3。

**直近のアクティビティ:** `satishmantri` が 3 月 30 日の事故映像（ダッシュカム）の取得支援を求める長文コメントを投稿。

**次のアクション:** Closed（古い issue で対応外）。事故映像は openpilot のチャネルで別途要請が必要。

---

### ![Issue](icons/issue-opened.svg) [Uninstalling Nightly and Trying to Connect to Wi-Fi network stuck on Waiting for Internet (C4 and C3X)](https://github.com/commaai/openpilot/issues/38034) `#38034`
**ステータス:** Closed | **ラベル:** bug

**概要:** Comma four で nightly インストール後にアンインストールしてから Wi-Fi 接続を試みると「Waiting for Internet」で固まる問題。`Codex` が原因コミットを `modem.py (#37811, 2026-05-02)` と特定。`get_network_type()` が常に `get_modem_state()` を先に呼ぶ仕様へ変わり、`/dev/shm/modem` 不在の状況で `wifi` を返せなくなったのが直接原因。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed（修正パスへ移行）。

---

### ![Issue](icons/issue-opened.svg) [setup: custom branch install crashes with raylib/Wayland error](https://github.com/commaai/openpilot/issues/38059) `#38059`
**ステータス:** Closed

**概要:** AGNOS 18.1.3 の mici で setup から `commaai/master` を選ぶと、`installer.comma.ai` からダウンロードされた installer が `FATAL: COMMA: Failed to initialize Wayland` で即クラッシュする。setup 本体（pyray ベース）は動作する。

**直近のアクティビティ:** `adeebshihadeh` が「installer サーバが `18.1.3` の小数 3 段に対応していない」と原因を指摘。

**次のアクション:** Closed（サーバ側対応へ）。

---

## commaai/opendbc

> VW MQB の停止保持回避という大きな車制御変更が進行中。Cabana 表示と DBC 信号定義の整合性、Hyundai/Kia 系のフィンガープリント追加など車種拡張も並行。

### ![PR](icons/git-pull-request.svg) [fix: Remove erroneous overlapping signal from vw_mqb.dbc](https://github.com/commaai/opendbc/pull/3392) `#3392`
**ステータス:** Closed | **ラベル:** DBC signals | **変更量:** +0 / -1

**概要:** `cantools dump opendbc/dbc/vw_mqb.dbc` で `PLA_Bremsverzoegerung` と `PLA_Bremsmoment` が `PLA_01` メッセージ内で重なるエラーが出ていたため、リバースエンジニアリングのアーティファクトと判断した `PLA_Bremsmoment` を削除する案。

**直近のアクティビティ:** `tealtwo` が「この信号重複は OEM の定義として『正しい』。cabana 側の扱いを直すべき」と指摘し、後継 PR #3398 を提示。作者 `dandwyer` が #3398 を支持して本 PR をクローズ。

**変更ファイル:** `opendbc/dbc/vw_mqb.dbc`。

**次のアクション:** Closed。#3398 で対応継続。

---

### ![PR](icons/git-pull-request.svg) [VW MQB: work around standstill limit](https://github.com/commaai/opendbc/pull/2764) `#2764`
**ステータス:** Open | **ラベル:** DBC signals, car, volkswagen | **変更量:** +622 / -25 (7 files)

**概要:** EPB を持たない MQB 車両で ESP がブレーキを保持し続けるとフォルトが出る問題を回避し、stop-and-go を完全対応させる大規模変更。車両が完全停止する前に stopping から starting へ遷移すれば無制限にブレーキ要求可能という挙動を利用。急坂でのロールバック対策として最低速度推定で停止強制し、ACC type 1 のプリイネーブル状態では `long_active` を抑制。

**直近のアクティビティ:** `RJWadley` が複数行レビュー（本文なし、ソースの個別箇所）を多数。`jyoung8607` が「ホールド方法を見つけたのは素晴らしい。これは FtS（Full-stop-Then-Start）対応の良い代替になりそう」と肯定的にコメント。`tealtwo` もレビュー参加。Car behavior report で 130 セグメントを 9 種類の VW 車種で再生中。

**変更ファイル:** `volkswagen/carcontroller.py`、`carstate.py`、`mqbcan.py`、`pqcan.py`、`mlbcan.py`、`tests/test_volkswagen.py`、`dbc/vw_mqb.dbc`。

**次のアクション:** レビュアーコメントの解消と car behavior report 結果確認。

---

### ![PR](icons/git-pull-request.svg) [Hyundai: Car Port for Creta (Non-US only) 2024](https://github.com/commaai/opendbc/pull/1139) `#1139`
**ステータス:** Closed | **ラベル:** car, hyundai, stale | **変更量:** +17 / -2

**概要:** 2024 Hyundai Creta（非米国仕様）のポート。ストックシステムでのルートのみで openpilot ルートはなし。`Harness A` を使用。

**直近のアクティビティ:** `CodeCuration` が「2024 Creta（ADAS, Smart cruise control, LKA 装備）を所有しているのでテストに協力できる」と申し出。

**次のアクション:** Closed（stale）。

---

### ![PR](icons/git-pull-request.svg) [Add new fingerprint data for Kia Niro EV](https://github.com/commaai/opendbc/pull/3307) `#3307`
**ステータス:** Open | **ラベル:** car, hyundai, fingerprint | **変更量:** +2 / -0

**概要:** Kia Niro EV (wind, HDA2) の新しいフィンガープリントを `opendbc/car/hyundai/fingerprints.py` に追加。

**直近のアクティビティ:** `holux-design` が「該当メッセージを『スキップ』できたのか、それともそこで止まったのか」と質問。

**次のアクション:** 質問への回答とレビュー。

---

### ![PR](icons/git-pull-request.svg) [VW MQB: fix bit overlap for cabana](https://github.com/commaai/opendbc/pull/3398) `#3398`
**ステータス:** Open | **ラベル:** DBC signals | **変更量:** +5 / -4

**概要:** Cabana で表示される `PLA_01` メッセージのビット重複表示を修正し、どのブレーキングタイプが有効かを説明する記述を追加。#3392 で議論された OEM 由来の重複を消すのではなく cabana 側の表現を直すアプローチ。

**直近のアクティビティ:** car behavior report で「差分なし」を確認。

**変更ファイル:** `opendbc/dbc/vw_mqb.dbc`。

**次のアクション:** レビュー。

---

## commaai/panda

> `pandacan` パッケージへの firmware 同梱を進める PR が連投。Python パッケージ単体で flash / リカバリができるようにする取り組み。

### ![PR](icons/git-pull-request.svg) [Include firmware in pandacan package](https://github.com/commaai/panda/pull/2402) `#2402`
**ステータス:** Open | **変更量:** +10 / -7 (2 files)

**概要:** 現状の `pandacan` wheel は Python API とパケットヘッダのみで、panda / jungle / body をフラッシュ・リカバリするために必要な生成済み firmware アーティファクトを含んでいない。署名済みアプリ、bootstub、firmware バージョンファイルなどを wheel に取り込むパッケージ構成変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Include firmware in pandacan package](https://github.com/commaai/panda/pull/2401) `#2401`
**ステータス:** Closed | **変更量:** +10 / -7

**概要:** #2402 の前段 PR。同じく `pandacan` wheel に firmware アーティファクトを同梱する変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed（#2402 で継続）。

---

## nvim-telescope/telescope.nvim

> 直近 24 時間の更新はありません。

---

## ghostty-org/ghostty

> macOS 関連の UX 改善が集中。Finder/Services 統合の修正、Find バーのペーストボード連携、ClipboardConfirmation の NSAlert 化、Zig 0.16.0 への大規模アップグレードが並行する。

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/12744) `#12744`
**ステータス:** Merged | **変更量:** +1 / -0

**概要:** `.github/VOUCHED.td` に新しい vouch エントリ（`@b0uks`）を追加する自動 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macOS: review windows when quitting](https://github.com/ghostty-org/ghostty/pull/12742) `#12742`
**ステータス:** Open | **ラベル:** os/macos | **変更量:** +116 / -36

**概要:** macOS の `Terminal.app` 同様、終了時にウィンドウを順番に確認できる機能。`BaseTerminalController` を改修し、Swift Concurrency でチェーン処理できるように整理。タブにも対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `AppDelegate.swift`、`BaseTerminalController.swift`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: use find pasteboard for search needle](https://github.com/ghostty-org/ghostty/pull/12712) `#12712`
**ステータス:** Open | **ラベル:** os/macos | **変更量:** +224 / -4 (7 files)

**概要:** Find バーの検索文字列を `OSPasteboard`（NSPasteboard / UIPasteboard を抽象化）に書き出し、他アプリ（Xcode、Notes、Terminal、Safari など）と同期。検索オーバレイの開閉やアプリアクティブ化時に読み戻す。`SearchState` を `MainActor` 化してスレッド安全性を担保。`BackportSelectionTextField` で macOS 15 / iOS 18 以降の選択対応も導入し、ユニットテストを追加。

**直近のアクティビティ:** `bo2themax` が `CHANGES_REQUESTED` レビューで「あとは小さなスタイル指摘のみ。iOS でも選択が動くので採用してよいのでは」とコメント。

**変更ファイル:** `OSSurfaceView.swift`、`SurfaceView.swift`、`Backport.swift`、`CrossKit.swift`、`OSPasteboard+Extension.swift`、`SurfaceView+SearchStateTests.swift`、Xcode プロジェクト。

**次のアクション:** スタイル指摘の修正と再レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: check the resource the URL refers to](https://github.com/ghostty-org/ghostty/pull/12731) `#12731`
**ステータス:** Merged | **ラベル:** os/macos | **変更量:** +12 / -1

**概要:** #12727 修正。`NSURL.hasDirectoryPath` は実体を確認しないため、対象が実ディレクトリか確認し、ディレクトリが無ければ正しくエラーを返すように `ServiceProvider.swift` を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update iTerm2 colorschemes](https://github.com/ghostty-org/ghostty/pull/12711) `#12711`
**ステータス:** Merged | **ラベル:** dependencies | **変更量:** +12 / -12

**概要:** `iTerm2-Color-Schemes` の upstream リリース `release-20260511-160054-2671288` を取り込む依存更新。`build.zig.zon` 系を更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [macOS: convert clipboard confirmation to an alert](https://github.com/ghostty-org/ghostty/pull/12736) `#12736`
**ステータス:** Open | **ラベル:** os/macos | **変更量:** +110 / -160

**概要:** `ClipboardConfirmationView` を `SwiftUI.TextEditor`（選択可・編集不可 API がない）の回避策から `NSAlert` + `NSTextView` ベースへ書き換え。xib を整理しヘルプボタンも追加しやすくする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `ClipboardConfirmation.xib`、`ClipboardConfirmationController.swift`、`ClipboardConfirmationView.swift`、`BaseTerminalController.swift`、Xcode プロジェクト。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Update to Zig 0.16.0](https://github.com/ghostty-org/ghostty/pull/12726) `#12726`
**ステータス:** Open | **変更量:** +4062 / -2259 (100 files)

**概要:** Ghostty を Zig 0.16.0 へアップグレードする大規模 PR（#12228 close / #12388 supersede）。stdlib の I/O 要求に対し `std.Io.Threaded.global_single_threaded` を静的に渡すアプローチ、削除された `SegmentedList` などを `src/lib/compat` に切り出して段階移行する設計。Linux ではテスト全パス、他プラットフォーム要追加対応。

**直近のアクティビティ:** `vancluever` が arocc 側の修正 PR（`Vexu/arocc#1010`）を案内し、その後 zig 翻訳成果物で `expected ';' after statement` エラーが出ていることを報告。`jacobsandlund` が「uucode 関連の変更は問題なし」とレビュー。

**変更ファイル:** `build.zig`、`build.zig.zon`、`flake.nix`、`pkg/*/build.zig`（android-ndk、breakpad、dcimgui、fontconfig、freetype、glslang、harfbuzz、highway など）多数。

**次のアクション:** arocc 修正の取り込みと残プラットフォーム対応。

---

### ![Issue](icons/issue-opened.svg) [macOS tip regression: Backspace incorrectly deletes previously committed character when used during active IME composition](https://github.com/ghostty-org/ghostty/issues/7225) `#7225`
**ステータス:** Closed | **ラベル:** os/macos, input

**概要:** macOS で中国語ピンイン IME 入力中、コンポジションが有効な状態で Backspace を押すと、進行中のコンポジションではなく直前にコミットされた文字が消える。`echo '你好` の直後にピンイン再入力で `ni h` を打って Backspace するという具体的な再現手順。

**直近のアクティビティ:** `tunglt1810` が macOS 26 のベトナム語 telex キーボードでも同様の問題（Backspace でコンポジション全体が消える）が発生していると報告。

**次のアクション:** Closed（discussion へ誘導）。

---

### ![Issue](icons/issue-opened.svg) [`New Ghostty Tab/Window Here` services called with plain-text pasteboard use wrong working directory](https://github.com/ghostty-org/ghostty/issues/12727) `#12727`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** macOS の `New Ghostty Tab/Window Here` サービスを `NSPerformService` 経由のプレーンテキストパスで呼び出すと（`(ns-perform-service "New Ghostty Window Here" "/usr/local")` の例）、意図したディレクトリではなくその親ディレクトリで開く。Finder 経由（`NSURL` 渡し）では正しい挙動。Terminal.app / iTerm2 は両形式とも正しく扱う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed（#12731 で修正済み）。

---

## qmk/qmk_firmware

> 新 MCU（ES32VF2264, RISC-V）対応、2.4GHz ワイヤレストランスポート API 新設、コミュニティモジュールのデータ永続化、Halcyon 用 Pro Micro アダプタ、Apple Extended Keyboard の ISO レイアウト対応など、コア機能とハードウェア対応が幅広く動いている。

### ![PR](icons/git-pull-request.svg) [converter: Add Pro micro to Halcyon adapter and remove Imera](https://github.com/qmk/qmk_firmware/pull/26226) `#26226`
**ステータス:** Open | **ラベル:** core, documentation | **変更量:** +43 / -39

**概要:** Halcyon シリーズキーボードと同じコントローラを任意の Pro Micro 互換ボードで使えるようにする「Pro Micro to Halcyon」アダプタを追加し、未発売でキャンセルとなった Imera を削除。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/feature_converters.md`、`promicro_to_halcyon/_pin_defs.h`、`converter.mk`、`promicro_to_imera/_pin_defs.h`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [ISO layout support for Apple Extended Keyboard (AEK) in converter/adb_usb](https://github.com/qmk/qmk_firmware/pull/26224) `#26224`
**ステータス:** Open | **ラベル:** keyboard, keymap | **変更量:** +234 / -111

**概要:** Apple ADB キーボード（AEK II M3501、Apple Keyboard M0116/M0118）の ISO レイアウトを 2 キー入れ替えのアドホックな対応ではなく、専用キーマップで正式サポート。tabulation の上のキー欠落も解消。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `keyboards/converter/adb_usb/` 配下の `info.json`、`keymaps/default/keymap.c`、`keymaps/iso/keymap.c`、`matrix.c`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [support MCU ES32VF2264](https://github.com/qmk/qmk_firmware/pull/26225) `#26225`
**ステータス:** Open | **ラベル:** core, keyboard, documentation, python, cli, dd | **変更量:** +231 / -2 (12 files)

**概要:** RISC-V ベースの MCU `ES32VF2264` を QMK でサポート。`compatible_microcontrollers.md` への追記、`keyboards/handwired/onekey/essemi_es32vf2264` の追加、ChibiOS 側コンフィグなど。`ChibiOS/ChibiOS-Contrib#441` 待ち。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ChibiOS 側 PR のマージ後に再評価。

---

### ![PR](icons/git-pull-request.svg) [feat: enable/implement 2.4ghz wireless dongle api](https://github.com/qmk/qmk_firmware/pull/26203) `#26203`
**ステータス:** Open | **ラベル:** core, documentation, dd | **変更量:** +538 / -27 (22 files)

**概要:** 既存の Bluetooth ドライバ API をモデルにした 2.4GHz ワイヤレストランスポート用ディスパッチャを新設。`drivers/wireless/wireless_2p4ghz.{c,h}` で init / task / is_connected / can_send_nkro / 各種 HID 送信などの 12 個のフックを定義。`active_host == CONNECTION_HOST_2P4GHZ` で HID レポートを当該ドライバ経由に切り替え、新キーコード `QK_2P4GHZ_UNPAIR` を追加。`WIRELESS_2P4GHZ_ENABLE=yes` で有効化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [[RFC] Keyboard editor TUI](https://github.com/qmk/qmk_firmware/pull/26043) `#26043`
**ステータス:** Closed | **ラベル:** documentation, python, cli, stale | **変更量:** +4650 / -0

**概要:** `data/schemas/keyboard.jsonschema` を実行時に読み込み、スキーマ駆動で `info.json` / `keyboard.json` を編集する `qmk edit` TUI の RFC 実装（約 1700 行）。文字列・bool・数値・enum・配列・ネストオブジェクトの編集、JSON Schema 検証、検索/フィルタを備える。

**直近のアクティビティ:** 30 日間活動なしの自動クローズ（stale-action）。

**次のアクション:** Closed（再開には新規 PR またはステータス更新が必要）。

---

### ![PR](icons/git-pull-request.svg) [Fix info.json in subfolder not overriding info.json in top-level folder](https://github.com/qmk/qmk_firmware/pull/26044) `#26044`
**ステータス:** Open | **ラベル:** keyboard, keymap, awaiting review, python, cli, breaking_changes_2026q3 | **変更量:** +79 / -1

**概要:** `info.json` のマージ順を反転し、子フォルダ（より具体的）の設定が親（一般）を上書きするように修正。`find_info_json` の戻り値が子→親順なのを `merge_info_json` で逆順に処理する。#19256 を修正。`breaking_changes_2026q3` ラベル付き。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `lib/python/qmk/info.py`、テスト、`keyboards/handwired/pytest/override/` 配下のフィクスチャ。

**次のアクション:** レビューと breaking changes プロセス。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open | **ラベル:** needs doc, in progress, core, keyboard, help wanted, keymap, needs testing, documentation, breaking_change, python, cli, feature, CI, no-ci, dd | **変更量:** +7548 / -25 (96 files)

**概要:** QMK の XAP (eXtensible Application Protocol) 実装トラッキング PR（#11567）。スキーマ駆動でクライアントコード、ドキュメント、ファームウェアを生成する大規模機能。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 引き続き WIP。

---

### ![PR](icons/git-pull-request.svg) [Enable modules to persist data](https://github.com/qmk/qmk_firmware/pull/26201) `#26201`
**ステータス:** Open | **ラベル:** core, keymap, documentation, python, cli, dd, community_module | **変更量:** +302 / -32 (11 files)

**概要:** コミュニティモジュールが NVM にデータを永続化できるようにする機能。VIA モジュールを実装例として実装検証。`onekey/keymaps/nvm_test` を追加し、`eeconfig.c` と `nvm_eeprom_eeconfig_internal.h` を拡張。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add `EECONFIG_{KB,USER}_DATA_SIZE` docs](https://github.com/qmk/qmk_firmware/pull/26200) `#26200`
**ステータス:** Merged | **ラベル:** documentation | **変更量:** +114 / -6

**概要:** `feature_eeprom.md` に `EECONFIG_{KB,USER}_DATA_SIZE` の使い方ドキュメントを追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

## astral-sh/uv

> PEP 740 attestation 生成、`uv audit --fix` 構想、locked malware 検出、Wheel-Version の数値比較バグ修正、CPython リリース情報のネットワーク取得、`uv build` のデフォルト除外/空ディレクトリ処理など、リゾルバ・パッケージング・セキュリティ・CLI まわりが幅広く活発。

### ![PR](icons/git-pull-request.svg) [Generate PEP 740 attestations](https://github.com/astral-sh/uv/pull/19489) `#19489`
**ステータス:** Open | **変更量:** +1155 / -202 (10 files)

**概要:** `uv publish` が PEP 740 attestation を生成するための初期実装。`attest-action` などを別途使う必要をなくす WIP。`chrono` を `jiff` に置き換える依存削減、`--no-attestations` 以外の CLI フラグ整備、ユニット/統合テスト整備が TODO。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 新規 `crates/uv-pep740/`（`sign.rs`、`types.rs`）、`uv-publish`、`uv-cli`、`commands/publish.rs`。

**次のアクション:** 依存整理と CLI/テスト整備。

---

### ![PR](icons/git-pull-request.svg) [Document and test relative exclude-newer support for uv pip](https://github.com/astral-sh/uv/pull/19475) `#19475`
**ステータス:** Merged | **ラベル:** documentation | **変更量:** +144 / -7

**概要:** `uv pip` で既に動いている相対 `exclude-newer`（friendly / ISO 8601 期間）について、設定メタデータと標準 `uv` 表現を揃え、CLI 引数と `[tool.uv.pip]` 双方の挙動を検証する統合テストを追加。挙動変更なし。

**直近のアクティビティ:** `woodruffw` が「ドラフトを外したら知らせて。一見 LGTM」とコメントし、最終的に `APPROVED`。

**変更ファイル:** `uv-settings/src/settings.rs`、`pip_exclude_newer_relative.rs`、`uv.schema.json`。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: remove '7 days' as possible value for 'exclude-newer'](https://github.com/astral-sh/uv/pull/19488) `#19488`
**ステータス:** Closed | **ラベル:** question | **変更量:** +2 / -2

**概要:** `exclude-newer` の値として「7 days」のような相対表記が動かないと誤解した PR。実際は 0.9.17 以降で相対値をサポートしているため、ドキュメントが正しい。

**直近のアクティビティ:** `zanieb` が「相対期間は許可している。古いバージョンでは？」と確認、`woodruffw` と `charliermarsh` が 0.9.17 以降への更新を案内。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [Refactor hint handling](https://github.com/astral-sh/uv/pull/18090) `#18090`
**ステータス:** Open | **ラベル:** error messages | **変更量:** +1668 / -1043 (72 files)

**概要:** ヒントをエラーメッセージのアドホックな付加情報ではなく一級の概念として扱うための抽象化（`Hints` / `Hint` トレイト）。エラー型に `impl Hint` を持たせ、表示時に既知の型にダウンキャストしてヒントを収集してエラーチェーンの外に表示する。#17110 のブロッカー。

**直近のアクティビティ:** human レビュー 21 件（本文なしのコメント）。

**変更ファイル:** `uv-build-frontend`、`uv-dispatch`、`uv-distribution-types/src/dist_error.rs`、新規 `uv-errors` クレートなど多数。

**次のアクション:** miette の `help:` プロンプト除去フォローアップ。

---

### ![PR](icons/git-pull-request.svg) [Test if C:/uv ALT_FS would work for junction quirks tests](https://github.com/astral-sh/uv/pull/19487) `#19487`
**ステータス:** Open | **ラベル:** internal, do-not-merge | **変更量:** +67 / -0

**概要:** Windows のジャンクション関連テストのために `C:/uv` を代替 FS として使えるか CI で検証する WIP。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** CI 結果待ち。

---

### ![PR](icons/git-pull-request.svg) [Skip empty directories in uv build outputs](https://github.com/astral-sh/uv/pull/19433) `#19433`
**ステータス:** Closed | **変更量:** +87 / -36

**概要:** `uv-build` のアーカイブにおいて、含まれるファイルが必要になるまでディレクトリエントリ作成を遅延し、空モジュール/data/license ディレクトリや除外で空になった `__pycache__` などを sdist / wheel に含めない変更。#19404 を修正。

**直近のアクティビティ:** 作者 `Sean-Kenneth-Doherty` が「Ruff 側のレビューで Astral の AI 寄稿ポリシーに気付いたため取り下げる」と表明し自身でクローズ。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [Exclude hidden paths from uv build outputs](https://github.com/astral-sh/uv/pull/19431) `#19431`
**ステータス:** Closed | **変更量:** +110 / -22

**概要:** `uv-build` のデフォルト除外に `.*`（隠しファイル/ディレクトリ）を追加し、`.mypy_cache` を含めたデフォルト除外フィクスチャを用意。`tool.uv.build-backend.default-excludes = false` でオプトアウト可能。

**直近のアクティビティ:** 同じ作者の `Sean-Kenneth-Doherty` が AI 寄稿ポリシーを理由に取り下げ。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [Support GH_HOST for Gist resolution](https://github.com/astral-sh/uv/pull/19432) `#19432`
**ステータス:** Closed | **変更量:** +217 / -12

**概要:** `GH_HOST` 環境変数で GitHub Enterprise の Gist URL（`gist.<host>/...` または `<host>/gist(s)/...`）を解決し、API リクエストを `<host>/api/v3/gists/{id}` に向ける対応。#15109 を修正。

**直近のアクティビティ:** 同じ作者が AI 寄稿ポリシーを理由に取り下げ。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [Add first-party line wrapping](https://github.com/astral-sh/uv/pull/17535) `#17535`
**ステータス:** Open | **変更量:** +1948 / -1019 (78 files)

**概要:** miette がやっていたエラーメッセージ行の自動折り返しを自前実装で行う変更。#17110 を前提に、miette を外す前段としての準備。

**直近のアクティビティ:** human レビュー 4 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add a benchmark interface to the `uv` crate](https://github.com/astral-sh/uv/pull/19486) `#19486`
**ステータス:** Open | **ラベル:** performance, testing | **変更量:** +168 / -46

**概要:** `crates/uv-bench/benches/invocation.rs` を追加し、`uv` クレートに対するベンチマークインターフェースを整える。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Allow passing `--runs` to hyperfine in the benchmarking harness](https://github.com/astral-sh/uv/pull/19485) `#19485`
**ステータス:** Open | **ラベル:** bug, internal | **変更量:** +8 / -4

**概要:** `hyperfine` は `--min-runs` と `--runs` の両指定でエラーとなるため、`--min-runs` を条件付きデフォルトに変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Remove unused reqwest blocking feature](https://github.com/astral-sh/uv/pull/19484) `#19484`
**ステータス:** Merged | **変更量:** +1 / -2

**概要:** `uv-git` で使われていない `reqwest` の `blocking` フィーチャーを削除。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Remove unused tracing JSON feature](https://github.com/astral-sh/uv/pull/19483) `#19483`
**ステータス:** Merged | **ラベル:** internal | **変更量:** +1 / -14

**概要:** `tracing-subscriber` の未使用 `json` フィーチャーを削除。バイナリには影響しないがロックファイルの不要エントリが削減される。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Move `wiremock` to dev dependencies](https://github.com/astral-sh/uv/pull/19481) `#19481`
**ステータス:** Merged | **ラベル:** internal | **変更量:** +2 / -2

**概要:** `wiremock` を `uv-publish` の dev-dependencies に移動。

**直近のアクティビティ:** `charliermarsh` が「テスト失敗は無関係」と確認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fetch available CPython releases from the network](https://github.com/astral-sh/uv/pull/19148) `#19148`
**ステータス:** Open | **変更量:** +3194 / -169 (20 files)

**概要:** `releases.astral.sh` に置かれた PBS 用 ndjson メタデータを取得（取得失敗時は GitHub にフォールバック）し、ストリーミングで既存キャッシュに追記する仕組み。組み込みメタデータはフォールバックとして維持。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Avoid including `schemars` in the production build](https://github.com/astral-sh/uv/pull/19482) `#19482`
**ステータス:** Merged | **変更量:** +47 / -19

**概要:** 開発時のみ必要な `schemars` をプロダクションビルドから外し、バイナリを 100KB 削減。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Allow disabling reading the system config with `UV_NO_SYSTEM_CONFIG`](https://github.com/astral-sh/uv/pull/19476) `#19476`
**ステータス:** Open | **ラベル:** configuration | **変更量:** +387 / -0

**概要:** システム設定の読み込みを `UV_NO_SYSTEM_CONFIG` で無効化可能にする。

**直近のアクティビティ:** `EliteTK` が「`--config-file` / `UV_CONFIG_FILE` で全設定無視できるし、優先度の高い設定で上書きできる。本フラグの想定ユースケースは？」と質問。

**次のアクション:** ユースケースの議論。

---

### ![PR](icons/git-pull-request.svg) [Vendor packse scenarios and serve them from an in-memory index](https://github.com/astral-sh/uv/pull/18084) `#18084`
**ステータス:** Open | **ラベル:** testing, build:skip-release | **変更量:** +7835 / -3454 (100 files)

**概要:** `packse` のテストシナリオをベンダリングし、インメモリインデックスから提供する大規模変更。新規 `uv-test/src/packse/` 以下に scenario / server / wheel 実装を追加し、`uv-dev/src/generate_scenarios.rs` を追加。

**直近のアクティビティ:** human レビュー 8 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(install-wheel): parse wheel version components as integers before comparison](https://github.com/astral-sh/uv/pull/19479) `#19479`
**ステータス:** Open | **変更量:** +13 / -5

**概要:** `WHEEL` メタデータの `Wheel-Version` を `&str` のまま比較していたためレキシコグラフィカル順で `"10" > "9"` が false になり `Wheel-Version: 1.10` が unsupported 警告をスキップしていたバグ修正。`u32` にパースして比較。

**直近のアクティビティ:** `zanieb` が「本当に `1.10` がスキップされていた？　Codex はそう言わない。pip はバージョン長を強制していない。テストなしの PR は出さないで」と指摘。作者 `Dev-X25874` が「インパクトを誇張した。非数値版（`1.invalid`）の検証漏れ修正という位置付け」と訂正しテスト追加を約束。

**次のアクション:** テスト追加。

---

### ![PR](icons/git-pull-request.svg) [Use the `fast-build` profile as the default build](https://github.com/astral-sh/uv/pull/19467) `#19467`
**ステータス:** Open | **変更量:** +106 / -65 (13 files)

**概要:** GitHub Actions ワークフローと `CONTRIBUTING.md` で `cargo build` のデフォルトプロファイルを `fast-build` に切り替え。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add support for locking build dependencies](https://github.com/astral-sh/uv/pull/17854) `#17854`
**ステータス:** Open | **ラベル:** build:skip-release | **変更量:** +6739 / -143 (37 files)

**概要:** ビルド依存を `uv.lock` に固定するための大規模変更（作者は「まだ反復が必要、自己レビュー用」と明言）。リゾルバ・ロック・ディスパッチ・ビルドフロントエンドにまたがる変更。

**直近のアクティビティ:** human レビュー 47 件（本文なし）。

**次のアクション:** 引き続き作者反復。

---

### ![PR](icons/git-pull-request.svg) [Infer the version on release workflow instead of requiring user input](https://github.com/astral-sh/uv/pull/19477) `#19477`
**ステータス:** Open | **ラベル:** internal | **変更量:** +31 / -33

**概要:** リリースワークフローで版番号を手動入力させずに推論する変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Rename the job for checksum manifest generation](https://github.com/astral-sh/uv/pull/19478) `#19478`
**ステータス:** Merged | **ラベル:** internal | **変更量:** +2 / -2

**概要:** リリースワークフロー内のチェックサムマニフェスト生成ジョブ名をリネーム。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Reject locked malware installations](https://github.com/astral-sh/uv/pull/18936) `#18936`
**ステータス:** Open | **ラベル:** enhancement, preview | **変更量:** +1104 / -74 (25 files)

**概要:** `uv sync` / `uv add` のコアプロジェクト同期に malware チェックを組み込む。`osv.dev` に対し 1 リクエストで `MAL-` レポートをフィルタし、ヒットすればインストールを拒否。`UV_MALWARE_CHECK=0` で無効化、`UV_MALWARE_CHECK_URL` でカスタムエンドポイント指定可能。プライベートパッケージは対象外。

**直近のアクティビティ:** human レビュー 32 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Reduce scope of public interfaces](https://github.com/astral-sh/uv/pull/19460) `#19460`
**ステータス:** Merged | **ラベル:** internal | **変更量:** +705 / -1755 (100 files)

**概要:** ワークスペース内クレートの `pub` を削減する Codex 駆動 PR。意図せず公開していた API を絞り、結果的に 1,000 行以上の未使用コードを削除。明示的なリクエストで残した部分のみ残置。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Content-address distributions in the archive](https://github.com/astral-sh/uv/pull/16816) `#16816`
**ステータス:** Open | **ラベル:** enhancement, performance | **変更量:** +421 / -223 (19 files)

**概要:** 全 wheel に対して常に BLAKE3 ハッシュを計算し、展開済み wheel をコンテンツアドレス指定でアーカイブに格納する。`uv pip install` と `uv sync` の間で展開済み配布のキャッシュ再利用を可能にし、重複ストレージを削減。#1061 / #13995 / #16786 を close する想定。

**直近のアクティビティ:** `woodruffw` が「これをやるなら `seahash` を外して全部 BLAKE3 に統一しても良いかも」と提案。human レビュー 19 件。

**次のアクション:** 議論継続。

---

### ![PR](icons/git-pull-request.svg) [Bump rand/digest/hashing crates](https://github.com/astral-sh/uv/pull/19304) `#19304`
**ステータス:** Open | **ラベル:** dependencies | **変更量:** +240 / -73 (14 files)

**概要:** `rand`、`digest`、ハッシュ系クレートを更新。`digest` から `LowerHex` が外れたため `base16ct` を追加。debloat（古い rand バージョン除去）は `retry-policies` / `quinn` / `fastbloom` 側 PR 待ち。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 依存先 PR を待つ。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate が管理する依存更新ダッシュボード。abandoned dependencies 37 件（`arcstr`、`arrayvec`、`assert_fs`、`base64`、`byteorder` など）を含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。Renovate ボットによる更新。

**次のアクション:** 通常運用。

---

### ![Issue](icons/issue-opened.svg) [BUG/UX: confusing warning in `uvx` running `<tool>@latest` with `--resolution=lowest-direct`](https://github.com/astral-sh/uv/issues/7164) `#7164`
**ステータス:** Closed | **ラベル:** bug, error messages

**概要:** `uvx --resolution=lowest-direct pytest@latest` で「直接依存がアンピンド」と警告が出て古いバージョンを引いて失敗する問題。

**直近のアクティビティ:** `sharifhsn` が「#17114 でクローズ。`@latest` を pinned constraint として扱うように修正済み」と報告。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Config and command to install dependencies with alternative package indexes](https://github.com/astral-sh/uv/issues/7149) `#7149`
**ステータス:** Closed

**概要:** AWS CodeArtifact など代替インデックスでスクリプトを介さず依存をインストールしたいという要望。

**直近のアクティビティ:** `sharifhsn` が「#7481 でクローズ。AWS 用ガイドに記載」と案内。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Add support for `--no-install` specific sources, e.g., path dependencies](https://github.com/astral-sh/uv/issues/6695) `#6695`
**ステータス:** Open | **ラベル:** wish

**概要:** `--no-install-paths` または `--no-install-source path` のような、ソース種別に応じた除外フラグ。

**直近のアクティビティ:** `sharifhsn` が「#15328 で `--no-install-local` を実装済み（パス依存に有効）。汎用的な解決ではないので必要なら open のままで」と整理。

**次のアクション:** 汎用化要否の判断。

---

### ![Issue](icons/issue-opened.svg) [Add example of `find-links` with `uv lock` for local wheels](https://github.com/astral-sh/uv/issues/6447) `#6447`
**ステータス:** Closed | **ラベル:** documentation

**概要:** ローカル wheel に対する `find-links` の `uv lock` 用ドキュメント要望。

**直近のアクティビティ:** `sharifhsn` が「#12407 でクローズ。`[[tool.uv.index]] format = \"flat\"` を `uv lock` がサポート。`find-links` 構文そのものではないが目的は同じ」と説明。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Respect `Content-Disposition` for direct URL installations?](https://github.com/astral-sh/uv/issues/17526) `#17526`
**ステータス:** Open | **ラベル:** compatibility, needs-decision

**概要:** URL にファイル名サフィックスがない場合に sdist / wheel を判断できず拒否される問題。pip は `Content-Disposition` ヘッダの `filename` を優先する。`filename*` などの拡張、PEP 508 との相互作用、ロックファイル時の扱いなどの整理が必要。

**直近のアクティビティ:** `notatallshaw` が pip の最新の優先順序（Content-Type → 拡張子 → stdlib のコンテンツタイプ検出）を共有。`woodruffw` が「tar.gz 仮定は不健全。サーバからのシグナル必須」と整理。`lindenr` が「`tar.gz` 仮定でフォーマット不一致はエラー、という最小対応でも現状より良い」と要望。

**次のアクション:** デザイン決定。

---

### ![Issue](icons/issue-opened.svg) [Add documentation for `uv lock` with pre-commit](https://github.com/astral-sh/uv/issues/6355) `#6355`
**ステータス:** Open | **ラベル:** documentation

**概要:** `uv lock` を pre-commit と組み合わせて使うドキュメント要望。

**直近のアクティビティ:** `sharifhsn` が「#9872 で対応済み」とコメント。

**次のアクション:** クローズ可否確認。

---

### ![Issue](icons/issue-opened.svg) [Check if `python -m uv run python` REPL works with Ctrl-C on Windows](https://github.com/astral-sh/uv/issues/14721) `#14721`
**ステータス:** Open | **ラベル:** good first issue, windows

**概要:** Windows で `python -m uv run python` REPL の Ctrl-C 挙動を確認する課題。

**直近のアクティビティ:** `Caltsic` が `uv 0.11.15+5` / Python 3.13.5 で Windows 疑似コンソール経由で検証し「アイドル時 / `time.sleep` 中とも正常に `KeyboardInterrupt` を返し REPL は継続。余分なトレースバックなし」と報告。

**次のアクション:** クローズ可否確認。

---

### ![Issue](icons/issue-opened.svg) [Support a `--fix` type flag for `audit` to allow automatic fixing of vulnerabilities according to semantic versioning](https://github.com/astral-sh/uv/issues/19428) `#19428`
**ステータス:** Open | **ラベル:** enhancement, uv audit

**概要:** `uv audit --fix` でセマンティックバージョニングに沿って脆弱性を自動修正する案。`audit` ロードマップ（#18506）に挙げられているが未割当。

**直近のアクティビティ:** `woodruffw` が「lockfile は必ず更新するとして、`pyproject` の更新方針・transitive な制約利用の可否を整理する必要あり」と論点提示。`crispyricepc` が `uv lock --upgrade-package` ベースのオプション 4 提案を支持し、`constraint-dependencies` の活用例を共有。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [Alternatives for handling issues with `uv lock [--check]` and `exclude-newer`](https://github.com/astral-sh/uv/issues/18775) `#18775`
**ステータス:** Open | **ラベル:** enhancement

**概要:** クライアントで `uv.lock` を生成し、コンテナで `uv lock --check` を回すワークフローで、ホストの `~/.config/uv/uv.toml` に `exclude-newer = "7 days"` を設定するとコンテナビルドが「removal of global exclude newer」エラーで失敗する問題。

**直近のアクティビティ:** 作者 `jklaiho` が uv 0.11.15 では本質的に解消されないこと、グローバル `exclude-newer` の運用方針を明確化したい旨を補足。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [Write migration guides](https://github.com/astral-sh/uv/issues/5200) `#5200`
**ステータス:** Open | **ラベル:** documentation

**概要:** Poetry / pip-tools からの移行ガイド作成。

**直近のアクティビティ:** スパムコメント 1 件（無関係リンク）。

**次のアクション:** 通常運用。

---

### ![Issue](icons/issue-opened.svg) [Does `uv tool install` read `[[tool.uv]]` section of target `pyproject.toml`](https://github.com/astral-sh/uv/issues/19480) `#19480`
**ステータス:** Open | **ラベル:** question

**概要:** `uv tool install .` や `uv tool install "pkg @ git+ssh://..."` の挙動についての質問。ドキュメントの「tool コマンドはローカル設定を無視する」記述に反し、対象パッケージの `pyproject.toml` の `[tool.uv]` セクション（`tool.uv.sources`、`tool.uv.index` など）を読んでいるように見える。

**直近のアクティビティ:** `charliermarsh` が「Git/ローカルパスからのインストールは読む、レジストリからのインストールは読まない」と挙動を確認。`MauroPfister` がドキュメント化を要望。

**次のアクション:** 挙動のドキュメント化。

---

### ![Issue](icons/issue-opened.svg) [Support `editable: false` for workspace dependencies](https://github.com/astral-sh/uv/issues/5988) `#5988`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** ワークスペース依存に対する `editable: false` サポート要望。

**直近のアクティビティ:** `sharifhsn` が「#15708 でクローズ」と報告。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Add support for customizing Astral mirror URLs](https://github.com/astral-sh/uv/issues/18525) `#18525`
**ステータス:** Closed | **ラベル:** enhancement, configuration, network

**概要:** Astral ミラー（Ruff / CPython / uv 自身のメタデータ取得先）の URL を `UV_ASTRAL_MIRROR_URL` 等で書き換え可能にする要望。閉鎖ネットワーク環境での内部プロキシ利用のため。

**直近のアクティビティ:** `iodbh` が「`curl ... | sh` 形式で常に最新版をインストールするインストラクションをミラー経由で配りたい」と補足。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Test coverage for multi-index scenarios for `uv.lock`](https://github.com/astral-sh/uv/issues/5882) `#5882`
**ステータス:** Open

**概要:** マルチインデックス + `uv.lock` のテストカバレッジ追加。

**直近のアクティビティ:** `sharifhsn` が「#6016 / #7481 / #7769 などで多数追加済み。トラッキング issue として継続するかクローズするか判断を」と整理。

**次のアクション:** クローズ可否判断。

---

### ![Issue](icons/issue-opened.svg) [Make error caused by `--exclude-newer` clearer](https://github.com/astral-sh/uv/issues/5878) `#5878`
**ステータス:** Closed | **ラベル:** help wanted, error messages

**概要:** `UV_EXCLUDE_NEWER` 設定時の依存解決失敗メッセージが分かりにくい問題。

**直近のアクティビティ:** `sharifhsn` が「#18217 / #18952 でクローズ。設定名・カットオフ日・要求バージョンの公開日を表示し `exclude-newer-package` を案内するように改善済み」と報告。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Move "Searching for..." `python install` logging to verbose](https://github.com/astral-sh/uv/issues/5877) `#5877`
**ステータス:** Closed | **ラベル:** tracing, cli

**概要:** `uv python install` の "Searching for…" 出力を verbose に移す要望。

**直近のアクティビティ:** `sharifhsn` が「#8701 でクローズ。`Searching for…` を除去し TTY 進捗バー (#4840) と非対話進捗 (#11165) を整備済み」と報告。

**次のアクション:** Closed。

---

## neovim/neovim

> ディレクトリバッファや `--remote` の刷新、`vim.pos` / `vim.Range` API 整理、ui2 まわりのフォローアップ、`l` テキストオブジェクト追加と関連議論、各種コンパイラ警告解消、LSP / autocmd / API 拡張など、0.13 リリースに向けた機能・API・基盤改善が広範に進行。

### ![PR](icons/git-pull-request.svg) [feat(runtime): add directory buffer for editing directories](https://github.com/neovim/neovim/pull/39723) `#39723`
**ステータス:** Open | **ラベル:** runtime | **変更量:** +666 / -105 (9 files)

**概要:** `:edit <dir>` / `nvim <dir>` で netrw に依存していたローカルディレクトリ表示を、Lua の組み込みディレクトリリスティングに置き換える。

**直近のアクティビティ:** `superatomic` が「buftype の OptionSet autocommand がこのプラグインに対しトリガしない（`nvim /` で何も出力されない）」と再現コードつきで報告。human レビュー 19 件。

**次のアクション:** OptionSet トリガ問題の修正とレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): make --remote wait by default with +async opt-out](https://github.com/neovim/neovim/pull/37951) `#37951`
**ステータス:** Open | **ラベル:** needs:response, remote, AI assisted | **変更量:** +818 / -263 (14 files)

**概要:** `mhinz/neovim-remote` 不要の単一 `--remote` ワークフロー実装。`GIT_EDITOR = "nvim --remote"` で待機がデフォルト、`+async` でオプトアウト。#24788 をクローズ。

**直近のアクティビティ:** human レビュー 14 件（本文なし）。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(proc): avoid maybe-uninitialized spawn status](https://github.com/neovim/neovim/pull/39898) `#39898`
**ステータス:** Open | **変更量:** +13 / -9

**概要:** GCC 15.2.0 / `-Werror` で `src/nvim/CMakeFiles/nvim_bin.dir/os/proc.c.o` がビルドできない警告を解消するシリーズの一環。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(channel): avoid GCC warnings in channel info](https://github.com/neovim/neovim/pull/39897) `#39897`
**ステータス:** Open | **変更量:** +27 / -23

**概要:** GCC 15.2.0 で `channel.c.o` が 2 種類の警告（promoted-uninitialized など）でビルドに失敗する問題を解消。

**直近のアクティビティ:** human レビュー 1 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(autoread): use file watchers for real-time change detection](https://github.com/neovim/neovim/pull/37971) `#37971`
**ステータス:** Open | **変更量:** +428 / -3 (8 files)

**概要:** `'autoread'` を `FocusGained` / `:checktime` 受動検知から、libuv の file watcher を使ったリアルタイム検知に拡張。他エディタ・Git 操作・ビルドツール経由の変更も即時検知できる。

**直近のアクティビティ:** 作者 `OleksandrChekhovskyi` が「リベース + 残コメント対応済み」と報告。`justinmk` が「これは十分シンプル。`:checkhealth` で watcher 数が見えると安心」と APPROVED（後にその要望は実装済み）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): flush change notifications on save](https://github.com/neovim/neovim/pull/39894) `#39894`
**ステータス:** Open | **ラベル:** lsp | **変更量:** +2 / -0

**概要:** LSP の save 通知前に didChange を flush する 2 行修正。

**直近のアクティビティ:** `MariaSolOs` が `liskin` の指摘に感謝し、テストは後続 PR でも可と提案。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [refactor: introduce nvim_on internally](https://github.com/neovim/neovim/pull/39883) `#39883`
**ステータス:** Open | **ラベル:** defaults, refactor, lsp, treesitter, diagnostic, snippet, plugin-editorconfig, ux-visibility, image, ui2 | **変更量:** +834 / -1032 (31 files)

**概要:** `nvim_create_autocmd` がコールバックのネストを強要し冗長な点を解消する `nvim_on` を導入し、内部で先行採用して使い心地を検証する。

**直近のアクティビティ:** `MariaSolOs` が「ここで言う『余分なネスト』とは何か？」と質問。human レビュー 3 件。

**次のアクション:** ネスト問題の合意形成とレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(api): avoid maybe-uninitialized warning in win_config](https://github.com/neovim/neovim/pull/39896) `#39896`
**ステータス:** Open | **ラベル:** api | **変更量:** +14 / -13

**概要:** GCC 15.2.0 で `api/win_config.c.o` ビルド時の `maybe-uninitialized` 警告を解消。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.net): add response status code and headers in on_response callback](https://github.com/neovim/neovim/pull/39685) `#39685`
**ステータス:** Open | **変更量:** +134 / -37 (4 files)

**概要:** `vim.net` の `on_response` コールバックに HTTP ステータスコードとレスポンスヘッダを渡せるようにする拡張。

**直近のアクティビティ:** human レビュー 10 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): defer exit from fast callbacks](https://github.com/neovim/neovim/pull/39895) `#39895`
**ステータス:** Open | **ラベル:** lua | **変更量:** +192 / -0

**概要:** fast コールバックからの exit を遅延する修正。#39783 をクローズ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(fold): virtual lines duplicate foldopen](https://github.com/neovim/neovim/pull/39891) `#39891`
**ステータス:** Open | **変更量:** +136 / -1

**概要:** 仮想行が常に下行の `foldcolumn` を引き継いで `foldopen` が重複表示される問題（#39892）の修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): implement native SSH orchestration (remote ssh)](https://github.com/neovim/neovim/pull/39378) `#39378`
**ステータス:** Open | **ラベル:** remote, ux-visibility, AI assisted, remote-ssh | **変更量:** +887 / -43 (10 files)

**概要:** VSCode Remote SSH 相当のリモート編集機能を Neovim に組み込む実装。#21635 を修正。

**直近のアクティビティ:** human レビュー 76 件（本文なし、コメント主流）。

**次のアクション:** 大規模レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [refactor(pos,range): extract `vim.pos._util`](https://github.com/neovim/neovim/pull/39889) `#39889`
**ステータス:** Open | **ラベル:** refactor, lsp, treesitter | **変更量:** +536 / -422 (10 files)

**概要:** `vim.Pos` / `vim.Range` の開発がほぼ完了したとして、共通処理を `vim.pos._util` に抽出するリファクタ。

**直近のアクティビティ:** `clason` が「ついでに `vim.pos` のドキュメント先頭にインデキシング規約（0-based / 1-based、バイト/文字単位など）を比較する概観セクションを追加してほしい」と要望。human レビュー 10 件。

**次のアクション:** ドキュメント追記とレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(warnings): annotate or ignore all uses of printf formatting](https://github.com/neovim/neovim/pull/39887) `#39887`
**ステータス:** Open | **変更量:** +34 / -28 (6 files)

**概要:** Clang 22 の `format(printf, 3, 4)` 関連警告を解消するため、すべての printf-style 関数に属性を付与または無視指定する。`dialog_msg()` ほか調整。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(pack): code action for updating all plugins](https://github.com/neovim/neovim/pull/39848) `#39848`
**ステータス:** Closed | **ラベル:** packages | **変更量:** +90 / -22

**概要:** 全プラグイン更新用 code action の追加 PR。

**直近のアクティビティ:** `echasnovski` が「一晩寝て考えても、入れるメリットは in-process LSP に対する `Source` kind code action のリファレンスになるくらいで、それでも不要と感じる」と否定的に判断。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [fix(api): make `nvim_exec_autocmds({ buf = ... })` use target buffer context](https://github.com/neovim/neovim/pull/39061) `#39061`
**ステータス:** Open | **ラベル:** api | **変更量:** +206 / -31 (9 files)

**概要:** `nvim_exec_autocmds({ buf = ... })` が `<abuf>` などには対象 buffer を使うが、コンテキストとしての実行は別 buffer のまま起こる挙動を修正する。

**直近のアクティビティ:** `justinmk` が「lintcommit ジョブは informational なので無視可」とコメント。human レビュー 26 件。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [feat(fs): glob/wildcard patterns in `vim.fs.find()`](https://github.com/neovim/neovim/pull/33771) `#33771`
**ステータス:** Open | **ラベル:** revisit-at-release, filesystem | **変更量:** +31 / -3 (4 files)

**概要:** `vim.fs.find()` の最初の引数にグロブパターンを受け付けるようにする拡張。

**直近のアクティビティ:** `gpanders` が「最初の引数をテーブルで上書きするのではなく、文字列をグロブとして解釈する `glob` オプションを第二引数に追加するのはどうか」と API 案を提示。human レビュー 9 件。

**次のアクション:** API 確定。

---

### ![PR](icons/git-pull-request.svg) [build(zig): add formatc and lintc steps](https://github.com/neovim/neovim/pull/38546) `#38546`
**ステータス:** Open | **ラベル:** ci:skip-news | **変更量:** +494 / -15 (12 files)

**概要:** Zig ビルドに `formatc` / `lintc` ステップを追加。

**直近のアクティビティ:** human レビュー 29 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add ev.win to autocmd callback args](https://github.com/neovim/neovim/pull/39826) `#39826`
**ステータス:** Open | **ラベル:** api | **変更量:** +309 / -175 (48 files)

**概要:** autocmd コールバックの引数に `ev.win` を追加。`WinNew` / `WinClosed` / `WinResized` / `WinScrolled` でコールバック実行時の `curwin` が変わってしまう問題を解消。

**直近のアクティビティ:** human レビュー 8 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): resolve lsp config](https://github.com/neovim/neovim/pull/39391) `#39391`
**ステータス:** Open | **ラベル:** lsp | **変更量:** +65 / -46 (3 files)

**概要:** LSP 設定の resolve ロジックを整理（#33578 の後継）。

**直近のアクティビティ:** human レビュー 8 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(messages): avoid recursive rtp build due to msg_show](https://github.com/neovim/neovim/pull/39888) `#39888`
**ステータス:** Open | **ラベル:** runtime, ui-extensibility, messages, ci:backport release-0.12 | **変更量:** +13 / -0

**概要:** ランタイム検索パスのビルド開始時に保留メッセージがあると `msg_show` コールバックが `runtime_search_path_validate()` を再帰的に呼ぶ問題を回避。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビューと 0.12 バックポート。

---

### ![PR](icons/git-pull-request.svg) [feat(diagnostics): add ui2 msg area as diagnostic handler](https://github.com/neovim/neovim/pull/39871) `#39871`
**ステータス:** Open | **ラベル:** diagnostic, ui2 | **変更量:** +122 / -0 (3 files)

**概要:** ui2 のメッセージエリアを diagnostic handler として登録する WIP（作者が ui2 diagnostics を常用しているという経緯）。

**直近のアクティビティ:** human レビュー 2 件（本文なし）。

**次のアクション:** 引き続き WIP。

---

### ![PR](icons/git-pull-request.svg) [fix(mouse): mouse=n should not adjust visual selection](https://github.com/neovim/neovim/pull/39429) `#39429`
**ステータス:** Merged | **ラベル:** mouse | **変更量:** +195 / -19 (3 files)

**概要:** `mouse=n` 設定時にマウスドラッグでビジュアルモードに入って以降のマウスイベントを失う問題、ダブル/トリプル/クアッドクリックや既存ビジュアル領域内クリックでの誤動作を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(api): support builtin commands and filters in nvim_get_commands()](https://github.com/neovim/neovim/pull/39672) `#39672`
**ステータス:** Open | **ラベル:** api | **変更量:** +350 / -114 (10 files)

**概要:** `nvim_get_commands()` がユーザコマンドのみを返していた制約を撤廃し、ビルトインコマンド対応・名前指定取得・フィルタを追加。

**直近のアクティビティ:** `justinmk` が「リベース必要。マージは概ね OK だが `argt_fields_to_dict` 周辺の C 変更がもう少し統一されると安心」と APPROVED 寄りコメント。human レビュー 22 件。

**次のアクション:** リベースと C 側の統一作業。

---

### ![PR](icons/git-pull-request.svg) [fix(test): support multiple --filter-out](https://github.com/neovim/neovim/pull/39885) `#39885`
**ステータス:** Open | **ラベル:** test | **変更量:** +38 / -4

**概要:** テストランナーで `--filter-out` の複数指定をサポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat: keymap parse](https://github.com/neovim/neovim/pull/38636) `#38636`
**ステータス:** Open | **変更量:** +263 / -35 (14 files)

**概要:** キーコンビネーションを「キーコード列」ではなく「修飾キー + キー」として個別取り出すパース機能。#26575 を修正。

**直近のアクティビティ:** `clason` が `CHANGES_REQUESTED` で「`vim.keycode` と何が違うのか、なぜ非標準名で別関数化が必要なのか？」と問う。human レビュー 25 件。

**次のアクション:** 設計再整理。

---

### ![PR](icons/git-pull-request.svg) [refactor: simplify aucmd_restbuf usage](https://github.com/neovim/neovim/pull/39879) `#39879`
**ステータス:** Merged | **ラベル:** refactor | **変更量:** +48 / -40 (15 files)

**概要:** `aucmd_restbuf` を `aucmd_prepbuf` が呼ばれていない場合に no-op として扱えるよう変更し、ガードコードを削減。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(normal): add line (`l`) text objects](https://github.com/neovim/neovim/pull/39845) `#39845`
**ステータス:** Merged | **変更量:** +204 / -0 (11 files)

**概要:** バッファ全体や行に対するテキストオブジェクトとして `l` を追加。`il` は文字単位の現在行、`al` は行単位のバッファ全体相当。#39209 をクローズ。

**直近のアクティビティ:** `zeertzjq` が `DISMISSED` で「Nvim 専用機能のテストは `test/old` に置くべきでない」と指摘。

**次のアクション:** マージ済み。続編議論 #39893 で `al` の改名検討。

---

### ![PR](icons/git-pull-request.svg) [fix(pack): show "Removed plugin" messages on one line](https://github.com/neovim/neovim/pull/39840) `#39840`
**ステータス:** Open | **ラベル:** messages, packages | **変更量:** +10 / -4

**概要:** `Removed plugin` メッセージを 1 行にまとめる。`E5810` のケースは別 PR で対応予定。

**直近のアクティビティ:** `echasnovski` が `APPROVED` で「妥当な改善」とコメント。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [fix(float): keep the calling float focused on buf_delete](https://github.com/neovim/neovim/pull/39858) `#39858`
**ステータス:** Open | **変更量:** +55 / -10

**概要:** フローティングから別 buffer を削除するとフォーカスがホルダーウィンドウに移り、削除対象 buffer に対する `BufEnter` が誤発火する問題を `switch_win_noblock()` で修正。

**直近のアクティビティ:** `seandewar` が `COMMENTED` で「ちょい外出中なのでドライブバイ程度」とレビュー。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39776) `#39776`
**ステータス:** Open | **ラベル:** vim-patch | **変更量:** +17 / -13

**概要:** `version.c` 自動更新（`github-actions[bot]` 作）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add operation field to nvim_set_option_value](https://github.com/neovim/neovim/pull/39849) `#39849`
**ステータス:** Open | **ラベル:** api | **変更量:** +206 / -189 (6 files)

**概要:** `nvim_set_option_value` に `operation` フィールドを追加し、`"set"` / `"prepend"` / `"append"` / `"remove"` などの操作を選択可能にする（他のオプション設定 API との整合性）。#38420 関連。

**直近のアクティビティ:** human レビュー 3 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [fix(map): remove unused MAP_DECLS specializations (#26452)](https://github.com/neovim/neovim/pull/39865) `#39865`
**ステータス:** Merged | **ラベル:** AI assisted | **変更量:** +0 / -14

**概要:** `map_defs.h` の未使用 `MAP_DECLS(T, U)` 特殊化を削除し `unused function` 警告を解消。#26452 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(secure) strengthen the security of trust action 'allow'](https://github.com/neovim/neovim/pull/38855) `#38855`
**ステータス:** Closed | **ラベル:** needs:response | **変更量:** +43 / -16

**概要:** `:trust` の `allow` アクションを強化する PR（受動警告、ロード済みなら on-disk ではなくバッファ内容をハッシュ）。#38001 のフォローアップ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [docs: remove deprecated vim.hl.on_yank from doc examples](https://github.com/neovim/neovim/pull/39882) `#39882`
**ステータス:** Open | **ラベル:** documentation, typo | **変更量:** +2 / -2

**概要:** `vim.hl.on_yank` が deprecated になったため、`:help` 内のサンプル autocmd 例を更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [backport: vim-patch:9.2.0500: filetype: some html files wrongly recognized as htmlangular](https://github.com/neovim/neovim/pull/39881) `#39881`
**ステータス:** Merged | **ラベル:** filetype, target:release | **変更量:** +22 / -1

**概要:** #39880 の `release-0.12` バックポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0500: filetype: some html files wrongly recognized as htmlangular](https://github.com/neovim/neovim/pull/39880) `#39880`
**ステータス:** Merged | **ラベル:** filetype, ci:backport release-0.12 | **変更量:** +22 / -1

**概要:** `ng-template` などを含む通常 HTML が `htmlangular` 扱いになる Vim パッチ 9.2.0500 を取り込み。#39778 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(api): nvim_get_option_value tab-local 'cmdheight'](https://github.com/neovim/neovim/pull/39259) `#39259`
**ステータス:** Merged | **ラベル:** api, AI assisted | **変更量:** +120 / -18 (6 files)

**概要:** `nvim_get_option_value()` で tabpage-local `cmdheight` などの取得を可能にする `Dict(option)` 拡張。

**直近のアクティビティ:** `justinmk` が「リベースとコメント反映後 LGTM」と APPROVED。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: misc, custom text-object](https://github.com/neovim/neovim/pull/39877) `#39877`
**ステータス:** Open | **ラベル:** documentation | **変更量:** +47 / -26 (4 files)

**概要:** カスタムテキストオブジェクトに関する細々したドキュメント更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [docs: --remote alternatives](https://github.com/neovim/neovim/pull/18414) `#18414`
**ステータス:** Closed | **ラベル:** documentation, needs:design, remote | **変更量:** +24 / -56

**概要:** `--remote-*` バリアントの整理スケッチ（#17856 のフォローアップ）。

**直近のアクティビティ:** `justinmk` が「#37951 に統合」としてクローズ。

**次のアクション:** Closed（#37951 で継続）。

---

### ![Issue](icons/issue-opened.svg) [Add LSP client flag to pull diagnostics on save](https://github.com/neovim/neovim/issues/29792) `#29792`
**ステータス:** Open | **ラベル:** lsp

**概要:** Neovim 10 の pull diagnostics について、診断リクエストの頻度を制御するフラグの追加要望。

**直近のアクティビティ:** `MariaSolOs` が「`liskin` のパッチはシンプルなので試して問題なければ採用、ダメなら revert で進めても良い」とコメント。

**次のアクション:** 実装 PR を待つ。

---

### ![Issue](icons/issue-opened.svg) [rename `al` text-object and add a better pair for `il`](https://github.com/neovim/neovim/issues/39893) `#39893`
**ステータス:** Open | **ラベル:** defaults, normal-mode, editor

**概要:** #39845 でマージされた `l` テキストオブジェクト（`al` = バッファ全体）の `al` を別の名前に変え、`il` と対をなすより良いキーを採用すべきという議論。

**直近のアクティビティ:** `superatomic` が「`dal` で行内容削除という案について `shift+vd` で代替可能、`al` は文字単位なので誤解しないように」と整理。長めの議論進行中（コメント 11 件）。

**次のアクション:** デフォルト確定。

---

### ![Issue](icons/issue-opened.svg) [text-object for the entire buffer](https://github.com/neovim/neovim/issues/39209) `#39209`
**ステータス:** Closed | **ラベル:** defaults, normal-mode, editor

**概要:** バッファ全体を操作対象とするテキストオブジェクトがなく、`ggVG` の代替が欲しいという要望。

**直近のアクティビティ:** `superatomic` が「#39893 で継続」。

**次のアクション:** Closed（#39893 / #39845 で対応）。

---

### ![Issue](icons/issue-opened.svg) [fold: virtual lines duplicate `foldopen` in `foldcolumn`](https://github.com/neovim/neovim/issues/39892) `#39892`
**ステータス:** Open

**概要:** 仮想行で `foldcolumn` の `foldopen` が重複表示される問題。再現コードあり。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #39891 で修正中。

---

### ![Issue](icons/issue-opened.svg) [`vim.fs.{dir,find}`: allow callers to observe traversal errors](https://github.com/neovim/neovim/issues/39878) `#39878`
**ステータス:** Open

**概要:** `vim.fs.dir()` が `uv.fs_scandir()` のエラーを握り潰しているため、読めないディレクトリと空ディレクトリが区別できない。エラーを観測する API が欲しい。#39723 関連。

**直近のアクティビティ:** `gpanders` が「`on_error` コールバックではなく、イテレータの第三戻り値 `err` で受け渡す API が良い」と提案（コード例つき）。

**次のアクション:** API 確定。

---

### ![Issue](icons/issue-opened.svg) [Option for the `confirm` option to always confirm Neovim exit, regardless of buffer status](https://github.com/neovim/neovim/issues/17256) `#17256`
**ステータス:** Open

**概要:** 未保存 buffer の有無にかかわらず終了時の確認を常に表示する `confirm` オプション要望。

**直近のアクティビティ:** `heygarrett` が「コミットメッセージ buffer で commitlint の診断を見落としたまま閉じることを防ぎたいので同感」と支持。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [vim.fn.filereadable returns 0 or 1 despite docs saying it returns true or false.](https://github.com/neovim/neovim/issues/39890) `#39890`
**ステータス:** Open

**概要:** `vim.fn.filereadable` のドキュメントが `boolean` 風に記述されているが、実体は 0/1 の数値を返すというドキュメントと実装の乖離。

**直近のアクティビティ:** `andradei` がドキュメントの明確化を要望。

**次のアクション:** ドキュメント修正。

---

### ![Issue](icons/issue-opened.svg) [alternative to C for Nvim core](https://github.com/neovim/neovim/issues/30678) `#30678`
**ステータス:** Open | **ラベル:** architecture

**概要:** Nvim core を C 以外の言語に置き換える議論のトラッキング issue。

**直近のアクティビティ:** `rogue-sly` が C3 言語の検討を提案。

**次のアクション:** 議論。

---

### ![Issue](icons/issue-opened.svg) [Regression: dragging mouse with mouse=n goes to visual mode](https://github.com/neovim/neovim/issues/15007) `#15007`
**ステータス:** Closed | **ラベル:** bug-regression, mouse, has:bisected

**概要:** `mouse=n` でマウスドラッグするとビジュアルモードに入る回帰。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #39429 で修正済み（Closed）。

---

### ![Issue](icons/issue-opened.svg) [LSP: purescript-language-server "Sort/reformat imports" code action crashes with attempt to index local 'workspace_edit' (a userdata value)](https://github.com/neovim/neovim/issues/39884) `#39884`
**ステータス:** Open | **ラベル:** lsp

**概要:** `purescript-language-server` の sort/reformat imports code action が Neovim で `workspace_edit` ローカルへの index アクセスでクラッシュ。

**直近のアクティビティ:** `glepnir` が `purescript-language-server` の該当行（`CodeActions.purs` L213）をトレースし、上流側コマンド生成の問題と特定。

**次のアクション:** 上流での修正要請。

---

### ![Issue](icons/issue-opened.svg) [Windows: console: <C-2>, <C-/>](https://github.com/neovim/neovim/issues/8435) `#8435`
**ステータス:** Open | **ラベル:** status:blocked-external, platform:windows, tui, needs:response

**概要:** Windows コンソールで `<C-Space>` などのコントロール組み合わせが取れない問題（外部要因によりブロック中）。

**直近のアクティビティ:** `T1ckbase` が Alacritty では `OpenConsole.exe` と `conpty.dll` を `node-pty/third_party/conpty` から並置するワークアラウンドで解消、と共有。

**次のアクション:** 上流改善待ち。

---

### ![Issue](icons/issue-opened.svg) [vim.pos, vim.range followup 2](https://github.com/neovim/neovim/issues/39867) `#39867`
**ステータス:** Open | **ラベル:** lua

**概要:** `vim.pos` / `vim.range` 実装が概ね完了したことを受けた次フェーズ議論（#25509, #38561 に続く）。

**直近のアクティビティ:** `ofseed` が「ビジュアル位置は `vim.Pos`/`vim.Range` だけでは解決しにくい。エンコーディングや extmark などを考える必要あり」と指摘。

**次のアクション:** 議論継続。

---

### ![Issue](icons/issue-opened.svg) [crash in `:runtime` with ui2 + invalid rtp + syntax](https://github.com/neovim/neovim/issues/39815) `#39815`
**ステータス:** Open | **ラベル:** has:backtrace, bug-crash, ui2

**概要:** ui2 + 不正な rtp + syntax 有効化で `:runtime` 中にクラッシュする再現。

**直近のアクティビティ:** `Mrpaoo` が「再現できないが、`runtime_search_path_validate` の再入が見つかった。バックトレース先頭で `path_free` が `runtime_search_path` 内のパスを解放しており、`path_build` がまだその領域に書き込もうとしている」と原因仮説を提示。

**次のアクション:** #39888 で再帰回避を実装中。

---

### ![Issue](icons/issue-opened.svg) [Powershell terminal buffer contents collapsed after closing the window](https://github.com/neovim/neovim/issues/19288) `#19288`
**ステータス:** Closed | **ラベル:** platform:windows, terminal, needs:response, has:plan

**概要:** Windows Terminal の Powershell ターミナル buffer がウィンドウクローズ後に縮退する古い問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Accessors for nvim_win and nvim_buf api functions](https://github.com/neovim/neovim/issues/20632) `#20632`
**ステータス:** Open | **ラベル:** lua

**概要:** `vim.win` / `vim.buf` のような `nvim_win_*` / `nvim_buf_*` 用アクセサ追加の要望。

**直近のアクティビティ:** `lucastrvsn` が支持と自プラグインでの実装、コントリビュート意欲を表明。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [.html file incorrectly parsed as "htmlangular" filetype](https://github.com/neovim/neovim/issues/39778) `#39778`
**ステータス:** Closed | **ラベル:** has:vim-patch, bug-vim, filetype

**概要:** `song-template` ID 内の `ng-template` で `htmlangular` 誤判定される問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #39880 / #39881 で修正済み（Closed）。

---

### ![Issue](icons/issue-opened.svg) [Windows: track UNC and device path root handling](https://github.com/neovim/neovim/issues/39844) `#39844`
**ステータス:** Open | **ラベル:** platform:windows, filesystem

**概要:** Windows のパスルート/UNC 処理が `fnamemodify()` / `vim.fs.normalize()` などに散在する問題のトラッキング issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 個別 PR で改善。

---

### ![Issue](icons/issue-opened.svg) [remote-ssh: use formal grammar (LPeg/Tree-sitter) for SSH config parsing](https://github.com/neovim/neovim/issues/39648) `#39648`
**ステータス:** Open | **ラベル:** complexity:low, syntax, treesitter, remote-ssh

**概要:** `runtime/lua/vim/net/_ssh.lua`（#39378）の SSH config パーサが手動文字列操作なので、tree-sitter ベースの正式文法に置き換える提案。

**直近のアクティビティ:** `eamonburns` が tree-sitter ベースの実装例（`host_declaration` クエリ）を提示。

**次のアクション:** 実装。

---

## microsoft/vscode

> 1.121.0 endgame に向けて agent-host / Copilot CLI / chat 周りの実装と sanity test が活発。MCP セキュリティ強化（URI 許可制、Windows シェル引数エスケープ）、Cloud Agent の Tasks API 切替、Model Picker への可視性コントロール再導入、Browser View の機能拡張、Octokit バージョンアップに伴う API 整合性、Issue Reporter ウィザード、Powershell hooks の ExecutionPolicy 整合性、Search 機能拡張、依存更新と spam issue クローズが多数。

### ![PR](icons/git-pull-request.svg) [Enable chronicle search](https://github.com/microsoft/vscode/pull/317406) `#317406`
**ステータス:** Open | **変更量:** +83 / -3

**概要:** chat に `/chronicle:search <query>` スラッシュコマンドを追加し、キーワード・ファイルパス・PR/Issue 参照で過去セッションを検索可能にする。既存 chronicle skill を再利用。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [chore: exclude on-testplan and error-telemetry issues](https://github.com/microsoft/vscode/pull/317404) `#317404`
**ステータス:** Merged | **変更量:** +1 / -1

**概要:** issue クエリから `on-testplan` / `error-telemetry` を除外する微修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [test: add Copilot CLI chat sanity regression test](https://github.com/microsoft/vscode/pull/317120) `#317120`
**ステータス:** Open | **変更量:** +427 / -11

**概要:** `workbench.action.chat.openSessionWithPrompt.copilotcli` 経由で実プロダクション CLI チャット参加者を駆動し、SDK が `(query) Execution failed` エラーログを出さないかを検証する sanity 回帰テスト。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Sanity test: enrich agent-mode timeout error with stream diagnostics](https://github.com/microsoft/vscode/pull/317407) `#317407`
**ステータス:** Open | **変更量:** +46 / -12

**概要:** `E2E Production agent mode` sanity テストでタイムアウト時にストリーム診断情報をエラーに添える改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Browser Search](https://github.com/microsoft/vscode/pull/317408) `#317408`
**ステータス:** Open | **変更量:** +853 / -15

**概要:** Browser View に検索機能を追加する大規模変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [mcp: fix rendering of mcp hover descriptions](https://github.com/microsoft/vscode/pull/317397) `#317397`
**ステータス:** Merged | **変更量:** +4 / -4

**概要:** MCP のホバー表示で description のレンダリングを修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Issue reporter wizard](https://github.com/microsoft/vscode/pull/317381) `#317381`
**ステータス:** Open | **変更量:** +8783 / -109

**概要:** Issue Reporter をウィザード形式に刷新する大規模 PR。

**直近のアクティビティ:** human レビュー 22 件（本文なし）。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [plugins: fix not showing confirmation for single-plugin repos](https://github.com/microsoft/vscode/pull/317401) `#317401`
**ステータス:** Merged | **変更量:** +25 / -9

**概要:** 単一プラグインリポジトリで確認ダイアログが出なかった問題を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Browser: support element selection in subframes](https://github.com/microsoft/vscode/pull/317405) `#317405`
**ステータス:** Open | **変更量:** +989 / -586

**概要:** Browser View でサブフレーム内の要素選択をサポートする変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [chore: bump Octokit APIs](https://github.com/microsoft/vscode/pull/317403) `#317403`
**ステータス:** Open | **変更量:** +431 / -569

**概要:** `octokit/request` バンプに伴う API 利用箇所の更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Revert "chore: bump octokit and engine versions"](https://github.com/microsoft/vscode/pull/317402) `#317402`
**ステータス:** Open | **変更量:** +525 / -363

**概要:** `auto-merge` 設定を切り忘れたまま CCR フィードバックを受けたため #317359 をリバート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] [OSS Tool] fix: add copyright for `@iconify-json/mdi` in cglicenses.json](https://github.com/microsoft/vscode/pull/317232) `#317232`
**ステータス:** Open | **ラベル:** cherry-pick-artifact | **変更量:** +5 / -1

**概要:** OSS ツールが検出した `@iconify-json/mdi` のコピーライト記載漏れ修正を `release/1.121` から cherry-pick。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ。

---

### ![PR](icons/git-pull-request.svg) [Bump Agent host Copilot CLI to 1.0.48](https://github.com/microsoft/vscode/pull/317241) `#317241`
**ステータス:** Open | **変更量:** +114 / -78

**概要:** Agent host 用の Copilot CLI を 1.0.48 に更新。

**直近のアクティビティ:** 作者 `anthonykim1` が「DO NOT MERGE: スクリーンショットの不具合あり」とブロック。

**次のアクション:** 不具合修正後にマージ判断。

---

### ![PR](icons/git-pull-request.svg) [(feature) Enable to filter search within changed files of source tree](https://github.com/microsoft/vscode/pull/314790) `#314790`
**ステータス:** Open | **変更量:** +104 / -2

**概要:** 検索パネルに「ソース管理で変更されたファイルのみを検索」トグルを追加。

**直近のアクティビティ:** `osortega` が `COMMENTED` で「Agentic フローにとても有用。いくつかコメントあり」と肯定的レビュー。human レビュー 20 件。

**次のアクション:** コメント解消。

---

### ![PR](icons/git-pull-request.svg) [remove input chevrons and add dividers between each dropdown label](https://github.com/microsoft/vscode/pull/315599) `#315599`
**ステータス:** Merged | **変更量:** +164 / -131

**概要:** 入力チェブロンを除去し、ドロップダウン各ラベル間にディバイダーを追加する UI 変更。

**直近のアクティビティ:** `Copilot` が `@copilot+claude-opus-4.7` 指示でマージコンフリクト（`chatWidget.css` ほか）を解消。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [mcp: escape comman arguments for shell servers on windows](https://github.com/microsoft/vscode/pull/317400) `#317400`
**ステータス:** Merged | **変更量:** +78 / -3

**概要:** Windows でシェル MCP サーバへ渡す引数を適切にエスケープするセキュリティ修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [tools: add additional patterns to default edit restrictions](https://github.com/microsoft/vscode/pull/317396) `#317396`
**ステータス:** Merged | **変更量:** +4 / -2

**概要:** デフォルトの編集制限パターンを追加し、危険なファイル変更を抑止。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: preserve gallery field in MCP protocol install fallback path](https://github.com/microsoft/vscode/pull/317395) `#317395`
**ステータス:** Open | **変更量:** +22 / -2

**概要:** `vscode:mcp/install` プロトコル URL での `gallery` フィールドが gallery サービス不在時の fallback で消える問題を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [mcp: allowlist mcp openable uris](https://github.com/microsoft/vscode/pull/317391) `#317391`
**ステータス:** Merged | **変更量:** +16 / -1

**概要:** MCP で開ける URI を明示的なアローリストに制限。

**直近のアクティビティ:** `dmitrivMS` が「テストも追加して？」とコメント。

**次のアクション:** マージ済み。テストはフォローアップ。

---

### ![PR](icons/git-pull-request.svg) [cli: validate paths when pulling update](https://github.com/microsoft/vscode/pull/317393) `#317393`
**ステータス:** Merged | **変更量:** +206 / -6

**概要:** CLI のアップデート取得時にパスを検証する強化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat: Enhance agent host session management with custom agent support](https://github.com/microsoft/vscode/pull/317398) `#317398`
**ステータス:** Open | **変更量:** +1323 / -31

**概要:** `ISessionAgentRef` インターフェースを導入し、`AgentHostSessionAdapter` でエージェント選択と状態更新を扱えるようにする。`setAgent` 経由でカスタムエージェント対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add ExecutionPolicy Bypass for powershell hooks](https://github.com/microsoft/vscode/pull/317383) `#317383`
**ステータス:** Open | **変更量:** +82 / -15

**概要:** PowerShell フックで `-ExecutionPolicy Bypass` を渡すように調整。Copilot CLI のフック挙動と一致させる狙い。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Forward session sync setting to copilot-sdk via remote flag](https://github.com/microsoft/vscode/pull/317390) `#317390`
**ステータス:** Open | **変更量:** +79 / -5

**概要:** `chat.sessionSync.enabled` が true のとき `CopilotClientOptions.remote=true` を渡し、SDK 側でセッションをクラウドストレージへエクスポートさせる変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Bump openssl from 0.10.79 to 0.10.80 in /cli](https://github.com/microsoft/vscode/pull/317394) `#317394`
**ステータス:** Open | **ラベル:** dependencies, rust | **変更量:** +4 / -4

**概要:** Dependabot による Rust 製 `openssl` クレート更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [[Search Subagent] Don't disclose paths outside of current workspace](https://github.com/microsoft/vscode/pull/317213) `#317213`
**ステータス:** Open | **変更量:** +103 / -9

**概要:** `search_subagent` ツールがワークスペース外の絶対パスを処理して開ける問題を、read-only ファイルアクセスポリシーで強制する変更。

**直近のアクティビティ:** human レビュー 3 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Implement device emulation support in browser view](https://github.com/microsoft/vscode/pull/316749) `#316749`
**ステータス:** Open | **変更量:** +485 / -17

**概要:** Browser View でデバイスエミュレーションを可能にする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix secret storage persistence in VS Code Server web](https://github.com/microsoft/vscode/pull/317333) `#317333`
**ステータス:** Open | **変更量:** +1 / -3

**概要:** `server-linux-x64-web` をブラウザから利用するとき、`remoteAuthority` が設定されていると `secretStorageProvider` が undefined になる回帰を修正。#317329 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Route system-initiated chat requests through utility small model](https://github.com/microsoft/vscode/pull/317389) `#317389`
**ステータス:** Open | **ラベル:** ~requires-eval-assessment | **変更量:** +65 / -1

**概要:** `isSystemInitiated: true` のチャットリクエスト（ターミナルツールの input-needed / command-completed / terminal-exited）を `ChatServiceImpl` でユーティリティ小モデルに振り替える。#317059 を修正。

**直近のアクティビティ:** `meganrogge` が `/requires-eval-assessment terminalbench2 gpt-5.4,claude-opus-4.6,claude-opus-4.7` で eval をリクエスト。

**次のアクション:** eval 結果待ち。

---

### ![PR](icons/git-pull-request.svg) [fix: Git - change regexp to be compatible with SHA256 commits (#227281)](https://github.com/microsoft/vscode/pull/317363) `#317363`
**ステータス:** Open | **変更量:** +65 / -10

**概要:** Git コミットハッシュ regex を 40 文字（SHA-1）だけでなく 64 文字（SHA-256）もマッチするように修正。#227281 を修正。

**直近のアクティビティ:** `PositroniumJS` が CLA 同意コメント。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Address GHE onboarding issues](https://github.com/microsoft/vscode/pull/317205) `#317205`
**ステータス:** Open | **変更量:** +413 / -92

**概要:** GitHub Enterprise オンボーディングの問題対応。

**直近のアクティビティ:** human レビュー 10 件（本文なし）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [sticky scroll in chat](https://github.com/microsoft/vscode/pull/296433) `#296433`
**ステータス:** Open | **変更量:** +335 / -49

**概要:** チャットビューに sticky scroll を実装する古い PR。

**直近のアクティビティ:** `Copilot` が `@copilot+claude-opus-4.7` でマージコンフリクト（`chat.contribution.ts`）を解消。

**次のアクション:** レビュー再開。

---

### ![PR](icons/git-pull-request.svg) [customization UI: avoid excessive calls to provideChatSessionCustomizations](https://github.com/microsoft/vscode/pull/317360) `#317360`
**ステータス:** Merged | **変更量:** +241 / -104

**概要:** `provideChatSessionCustomizations` の過剰な呼び出しを抑制し、UI のレスポンスを改善。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chore: bump octokit and engine versions](https://github.com/microsoft/vscode/pull/317359) `#317359`
**ステータス:** Merged | **変更量:** +363 / -525

**概要:** Octokit とエンジンバージョンの更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み（後に #317402 でリバート提案）。

---

### ![PR](icons/git-pull-request.svg) [Copilot CLI sanity testing](https://github.com/microsoft/vscode/pull/317140) `#317140`
**ステータス:** Open | **変更量:** +435 / -22

**概要:** Copilot CLI 拡張ホストの sanity テストを追加。実 `copilotcli` チャット参加者を駆動。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Show Model Manager in Agents Window](https://github.com/microsoft/vscode/pull/317382) `#317382`
**ステータス:** Open | **変更量:** +56 / -4

**概要:** Agents Window で Model Manager を表示し、プロファイル別ストレージで `chatLanguageModels.json` を共有/分離する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Cloud Agent: Tasks API experimental setting and backend](https://github.com/microsoft/vscode/pull/317206) `#317206`
**ステータス:** Merged | **変更量:** +970 / -233

**概要:** `CopilotCloudSessionsProvider` にバックエンドシームを設け、Cloud Agent が従来の Jobs API か新しい Mission Control Task API を選択できるように。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Implement upgrade redirect](https://github.com/microsoft/vscode/pull/317365) `#317365`
**ステータス:** Merged | **変更量:** +148 / -10

**概要:** クォータ枯渇時のアップグレード遷移を実装。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [plugins: support git-subdir marketplace source kind](https://github.com/microsoft/vscode/pull/317362) `#317362`
**ステータス:** Merged | **変更量:** +69 / -9

**概要:** Claude Code marketplace スキーマに合わせて `git-subdir` プラグインソース種別をサポート。#310850 関連。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Update Copilot CLI terminal hint experiment to default](https://github.com/microsoft/vscode/pull/317366) `#317366`
**ステータス:** Open | **変更量:** +29 / -100

**概要:** `terminal.integrated.initialHintCopilotCli` 設定を削除し、Copilot CLI ヒントを常時表示するように単純化。

**直近のアクティビティ:** `meganrogge` が「LGTM。Copilot に細かな修正を任せている」とコメント。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [agentHost: apply organized AHP types](https://github.com/microsoft/vscode/pull/317354) `#317354`
**ステータス:** Merged | **変更量:** +6467 / -6132

**概要:** `agent-host-protocol/pull/132` の整理済み AHP 型定義を取り込み。ランタイム変更なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Re-introduce model visibility control for chat window model picker](https://github.com/microsoft/vscode/pull/317308) `#317308`
**ステータス:** Merged | **変更量:** +424 / -17

**概要:** チャットモデルピッカーで個別モデル・プロバイダーグループを非表示にできる機能を復活。`Manage Models` から到達可能。#314825 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Use raw quota values](https://github.com/microsoft/vscode/pull/317344) `#317344`
**ステータス:** Merged | **変更量:** +47 / -1

**概要:** クォータ表示で生値を使うように変更（スクリーンショット付き）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [agentHost: enable subagent integration test for Claude real SDK](https://github.com/microsoft/vscode/pull/317353) `#317353`
**ステータス:** Merged | **変更量:** +14 / -10

**概要:** Claude 用 subagent ルーティングが整備されたため、Claude real-SDK で共有 subagent 統合テストを有効化。`IRealSdkProviderConfig.subagentToolName` を文字列でリファクタ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix BYOK invalid stateful marker retries](https://github.com/microsoft/vscode/pull/317292) `#317292`
**ステータス:** Open | **変更量:** +168 / -5

**概要:** BYOK の Responses リトライパスで `InvalidStatefulMarker` エラー時、共有リトライフローが `ignoreStatefulMarker=true` で再試行する際、OpenAI BYOK エンドポイントが明示抑制を維持するように修正。

**直近のアクティビティ:** `PenguinDOOM` が「ローカルでビルドしテストして問題なし」と確認。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add custom agent mcp linting](https://github.com/microsoft/vscode/pull/317343) `#317343`
**ステータス:** Closed | **変更量:** +34 / -1

**概要:** カスタムエージェント向け MCP リンティング追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [chat: custom trajectory compaction model](https://github.com/microsoft/vscode/pull/316114) `#316114`
**ステータス:** Open | **変更量:** +634 / -36

**概要:** trajectory（会話履歴）コンパクションを専用モデル（Fireworks ホストの `accounts/msft/deployments/ihfptseo`）にルーティングする ExP オプトイン対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Bump webpack-dev-server and @rspack/cli in /build/rspack](https://github.com/microsoft/vscode/pull/317034) `#317034`
**ステータス:** Open | **ラベル:** javascript, dependencies | **変更量:** +117 / -4083

**概要:** Dependabot による `@rspack/cli` 更新と、`webpack-dev-server` の削除。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Bump ws from 8.19.0 to 8.20.1 in /remote](https://github.com/microsoft/vscode/pull/317132) `#317132`
**ステータス:** Closed | **ラベル:** javascript, dependencies | **変更量:** +8 / -5

**概要:** Dependabot による `ws` ライブラリ更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [Bump ws from 8.19.0 to 8.20.1](https://github.com/microsoft/vscode/pull/317146) `#317146`
**ステータス:** Closed | **ラベル:** javascript, dependencies | **変更量:** +8 / -5

**概要:** `ws` の重複更新 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![PR](icons/git-pull-request.svg) [Use numeric comparator when re-indexing resolved extensions across pages](https://github.com/microsoft/vscode/pull/316827) `#316827`
**ステータス:** Merged | **ラベル:** extensions-view | **変更量:** +1 / -1

**概要:** `PreferredExtensionsPagedModel.populateResolvedExtensions` で `Map<number, IExtension>` を再 index する際、辞書順比較で順序が崩れる問題を数値比較で修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Issue](https://github.com/microsoft/vscode/issues/317399) `#317399`
**ステータス:** Closed | **ラベル:** info-needed, spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [undoRedoService can hold large array buffers in RAM](https://github.com/microsoft/vscode/issues/317376) `#317376`
**ステータス:** Open

**概要:** VS Code 1.120.0 で `undoRedoService` がモデル開放後も大きな ArrayBuffer を RAM に保持する。

**直近のアクティビティ:** `dmitrivMS` が「モデルが開いている間 undo stack は無制限。閉じれば制限がかかる。このようなシナリオは厳しい。メモリ上限を入れるか、別アプローチが必要」とコメント。

**次のアクション:** 設計議論。

---

### ![Issue](icons/issue-opened.svg) [Model picker: cannot show/hide models and gear `eye` icon missing on 1.120.0 (Linux snap, LiteLLM + llama.cpp)](https://github.com/microsoft/vscode/issues/316663) `#316663`
**ステータス:** Closed | **ラベル:** model-byok

**概要:** 1.120.0 で Manage Models の歯車/目アイコンによるモデル非表示制御ができなくなった回帰。

**直近のアクティビティ:** `vritant24` が「Insiders で修正済み、次の 1.121 stable で配布」と返答。

**次のアクション:** Closed（#314825, #317308 で対応済み）。

---

### ![Issue](icons/issue-opened.svg) [Agents: Support read-only sub-sessions for viewing sub-agent chats](https://github.com/microsoft/vscode/issues/317392) `#317392`
**ステータス:** Open | **ラベル:** feature-request, agents-window, agents-app

**概要:** セッションがサブエージェントへ作業を委譲したとき、その会話を read-only サブセッションとして表示する要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン。

---

### ![Issue](icons/issue-opened.svg) [I cannot change models using copilot](https://github.com/microsoft/vscode/issues/316030) `#316030`
**ステータス:** Closed | **ラベル:** bug, accessibility, chat

**概要:** Copilot で UI からモデル切替できないバグ。`nextmodel` ショートカットからのみ切替可能。

**直近のアクティビティ:** `DalisonCPU` が「キーボードショートカット問題ではなく、UI からのモデル切替が動かない」と詳説。コメント 8 件。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [git keeps opening other worktrees immediately after i do "Close Other Repositories"](https://github.com/microsoft/vscode/issues/317388) `#317388`
**ステータス:** Open

**概要:** 1.120 回帰として、`Close Other Repositories` 直後に 20 程度の他 worktree が即時再オープンされる。

**直近のアクティビティ:** `Spongman` が「`Disable (Github Copilot) AI Features` で回避できる」と報告。

**次のアクション:** Copilot 側の再オープン抑制要修正。

---

### ![Issue](icons/issue-opened.svg) [Improve communication for continuation options based on past usage](https://github.com/microsoft/vscode/issues/315079) `#315079`
**ステータス:** Open | **ラベル:** feature-request, under-discussion, UBB

**概要:** UBB（使用量ベース課金）の世界でクォータ消費の事前通知を強化する要望。

**直近のアクティビティ:** `cwebster-99` が「プロンプトとモデルの相性を事前評価する pre-prompt nudging も併せて」とコメント。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [Steering message for terminal uses default model](https://github.com/microsoft/vscode/issues/317059) `#317059`
**ステータス:** Open | **ラベル:** feature-request, verification-needed, chat-terminal

**概要:** 永続非同期ターミナルを killed したあとに別ターンが発生する挙動と、それが既定モデルで実行される問題。

**直近のアクティビティ:** `meganrogge` が Insiders での検証手順（`Developer: Set Log Level` → Trace、async terminal の起動等）を提示。

**次のアクション:** #317389 で対応中。

---

### ![Issue](icons/issue-opened.svg) [Extension Signature Verification Failed: Text2Code](https://github.com/microsoft/vscode/issues/317384) `#317384`
**ステータス:** Open | **ラベル:** info-needed

**概要:** Insiders 1.121.0 で `Text2Code` 拡張の署名検証エラー。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 情報待ち。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: ask_user question is presented as plan](https://github.com/microsoft/vscode/issues/316544) `#316544`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** Agent Host の `ask_user` 質問が plan として表示されてしまう問題。

**直近のアクティビティ:** `connor4312` が「question ウィジェットを使っているが、データが全部スクロール不可な `title` に入っている」と原因コメント。

**次のアクション:** 修正。

---

### ![Issue](icons/issue-opened.svg) [Not able to hide models from the Manage Models menu anymore](https://github.com/microsoft/vscode/issues/314825) `#314825`
**ステータス:** Closed | **ラベル:** verification-needed, unreleased, model-byok

**概要:** Manage Models からモデルを非表示にする機能がなくなった回帰。

**直近のアクティビティ:** `vritant24` が「カスタムモデルがリスト見えるべきかの議論あり、復活に取り組み中」とコメント。

**次のアクション:** Closed（#317308 で対応）。

---

### ![Issue](icons/issue-opened.svg) [Allow BYOK in Agents App.](https://github.com/microsoft/vscode/issues/317223) `#317223`
**ステータス:** Open | **ラベル:** model-byok

**概要:** VS Code エディタウィンドウで既に許可されている BYOK を Agents App でも有効化したいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: subsession support](https://github.com/microsoft/vscode/issues/317349) `#317349`
**ステータス:** Closed | **ラベル:** *duplicate, agent-host

**概要:** Agent Host の subsession サポート。

**直近のアクティビティ:** `roblourens` が #316670 への重複として誘導。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Cancelled Claude CLI turns do not show any model details](https://github.com/microsoft/vscode/issues/317387) `#317387`
**ステータス:** Open | **ラベル:** bug

**概要:** Claude CLI のキャンセル済みターンでモデル ID などのメタデータが表示されない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正。

---

### ![Issue](icons/issue-opened.svg) [Why show "+1 more workspaces"](https://github.com/microsoft/vscode/issues/317386) `#317386`
**ステータス:** Open

**概要:** `+1 more workspaces` 表示の意義に関する内部疑問（`roblourens`）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 議論。

---

### ![Issue](icons/issue-opened.svg) [Cancelled Copilot CLI requests do not show any metadata](https://github.com/microsoft/vscode/issues/317385) `#317385`
**ステータス:** Open | **ラベル:** bug

**概要:** キャンセル済みの Copilot CLI セッションでモデル ID 等のメタデータが表示されない、また最初のメッセージがキャンセルされたセッションには戻れない問題。

**直近のアクティビティ:** `pwang347` が追加再現として「最初のメッセージがキャンセルされたセッションはクリックしても何も起きない」と報告。

**次のアクション:** 修正。

---

### ![Issue](icons/issue-opened.svg) [AHP/Copilot attachments enablement](https://github.com/microsoft/vscode/issues/315193) `#315193`
**ステータス:** Open | **ラベル:** feature-request, copilot-cli-agent, agent-host

**概要:** AHP/Copilot で attachment ピッカーを有効化する要望。

**直近のアクティビティ:** `anthonykimseattle` が「Files & Folders…」など Copilot CLI 既存ピッカーの状態に応じた採用チェックリストを提示。

**次のアクション:** 設計。

---

### ![Issue](icons/issue-opened.svg) [CodeLens widgets aren't tappable on mobile devices](https://github.com/microsoft/vscode/issues/166650) `#166650`
**ステータス:** Open | **ラベル:** bug, code-lens, safari

**概要:** モバイル（Safari）で CodeLens ウィジェットをタップできない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正。

---

### ![Issue](icons/issue-opened.svg) [Agent plugins - PluginMarketplaceService unknown source kind 'git-subdir'](https://github.com/microsoft/vscode/issues/310850) `#310850`
**ステータス:** Closed | **ラベル:** feature-request, unreleased, agent-plugins

**概要:** marketplace ファイルが Claude Code の `git-subdir` ソース種別を未サポートだった問題。

**直近のアクティビティ:** `abi-jey` が「+1」。

**次のアクション:** Closed（#317362 で対応）。

---

### ![Issue](icons/issue-opened.svg) [Add call back when user initiates a plan upgrade from VS Code](https://github.com/microsoft/vscode/issues/270428) `#270428`
**ステータス:** Closed | **ラベル:** feature-request, upstream, unreleased, plg, UBB

**概要:** Free ユーザーがクォータ枯渇前に VS Code 上からプランアップグレードを開始したときのコールバック要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Security: minimumReleaseAge setting for mitigating supply chain attacks on extensions](https://github.com/microsoft/vscode/issues/316867) `#316867`
**ステータス:** Open

**概要:** サプライチェーン攻撃緩和のため、拡張機能の「リリースから N 日経過してから有効化」設定の要望。

**直近のアクティビティ:** `barthuijgen` が「最近の nx console 拡張侵害で感染した。これは受け入れがたい状況で、防御策がない」と緊急性を訴える。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [Ctrl+backspace and Ctrl+Del discards ALL Chat changes instead of deleting word (when text prompt is empty)](https://github.com/microsoft/vscode/issues/317320) `#317320`
**ステータス:** Open

**概要:** プロンプトが空の状態で `Ctrl+Backspace` / `Ctrl+Del` を押すと、単語削除ではなく全 Chat 変更を破棄してしまい時間の作業が失われる回帰。#280443 で議論済みだった件。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正要。

---

### ![Issue](icons/issue-opened.svg) [Insiders prioritizing Copilot CLI hooks over vscode extension hooks](https://github.com/microsoft/vscode/issues/315830) `#315830`
**ステータス:** Open | **ラベル:** chat-hooks, agent-plugins

**概要:** Insiders で Copilot CLI hooks が vscode extension hooks より優先される問題。

**直近のアクティビティ:** `pwang347` が「Copilot CLI は `-ExecutionPolicy Bypass` を使わず `pwsh` を優先するが、デフォルトでは利用不可の場合がある」と分析（コメント 7 件）。

**次のアクション:** #317383 で修正中。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: local AH sessions don't show progress in vscode](https://github.com/microsoft/vscode/issues/316543) `#316543`
**ステータス:** Open | **ラベル:** bug, agent-host

**概要:** ローカル Agent Host セッションが chat リストで進捗を表示しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正。

---

### ![Issue](icons/issue-opened.svg) [AgentHost: WSL and Dev Containers support](https://github.com/microsoft/vscode/issues/317380) `#317380`
**ステータス:** Open | **ラベル:** feature-request, agent-host

**概要:** Agent Host を WSL / Dev Containers でもサポートしたい。SSH のリモート AH と同様の仕組み案。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン。

---

### ![Issue](icons/issue-opened.svg) [bug_report_template](https://github.com/microsoft/vscode/issues/317377) `#317377`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** `Arkafileasadi` の単発コメントのみ。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Report](https://github.com/microsoft/vscode/issues/317378) `#317378`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [bug4](https://github.com/microsoft/vscode/issues/317372) `#317372`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [bug2](https://github.com/microsoft/vscode/issues/317369) `#317369`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [bug1](https://github.com/microsoft/vscode/issues/317368) `#317368`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [bug3](https://github.com/microsoft/vscode/issues/317371) `#317371`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [bug 3](https://github.com/microsoft/vscode/issues/317370) `#317370`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [VS Code - Copilot Fix](https://github.com/microsoft/vscode/issues/317367) `#317367`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容なしの spam issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Cmd.default prototype in cmd.pyi should be changed.](https://github.com/microsoft/vscode/issues/317361) `#317361`
**ステータス:** Open

**概要:** Python の `cmd.pyi` 内 `Cmd.default` の戻り値型注釈が pylance で誤判定される。

**直近のアクティビティ:** `tedzo` が「拡張リストを公開すべきだった。pylance が `default` メソッドの戻り値に文句を言っている」と補足。

**次のアクション:** Python 拡張側で対応。

---

### ![Issue](icons/issue-opened.svg) [Agents: Act on feedback is broken (with data loss) when used in a CCA session](https://github.com/microsoft/vscode/issues/317379) `#317379`
**ステータス:** Open

**概要:** CCA エージェントセッションで diff にコメントして送ったとき、PR には `@copilot /act-on-feedback` だけが投稿され、コメント本体が消失する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 修正。

---

### ![Issue](icons/issue-opened.svg) [Collapsible tab groups (Chrome/Firefox-style)](https://github.com/microsoft/vscode/issues/317364) `#317364`
**ステータス:** Open

**概要:** Chrome / Firefox 風の折りたたみ可能タブグループの要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン。

---

### ![Issue](icons/issue-opened.svg) [fdsfsf](https://github.com/microsoft/vscode/issues/317375) `#317375`
**ステータス:** Closed

**概要:** 内容なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [avdfd](https://github.com/microsoft/vscode/issues/317374) `#317374`
**ステータス:** Closed

**概要:** 内容なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Sync GitHub Copilot Chat History Across Devices](https://github.com/microsoft/vscode/issues/306502) `#306502`
**ステータス:** Closed | **ラベル:** feature-request, *out-of-scope, panel-chat

**概要:** GitHub Copilot のチャット履歴をデバイス間で同期する要望。

**直近のアクティビティ:** `4piu` が「ワークアラウンドとして保存パスをカスタマイズさせてほしい。リモート SSH ワークフローが壊れている」と再要望。

**次のアクション:** Closed（out-of-scope）。

---

### ![Issue](icons/issue-opened.svg) [feauture request](https://github.com/microsoft/vscode/issues/317373) `#317373`
**ステータス:** Closed

**概要:** 内容なし。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [1.121.0 Endgame](https://github.com/microsoft/vscode/issues/316639) `#316639`
**ステータス:** Open | **ラベル:** endgame-plan

**概要:** 1.121.0 リリースの endgame プラン issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** リリース進行。

---

### ![Issue](icons/issue-opened.svg) [Extension causes high cpu load](https://github.com/microsoft/vscode/issues/315958) `#315958`
**ステータス:** Open | **ラベル:** info-needed

**概要:** `copilot-chat` 0.45.1 が高 CPU 負荷を引き起こすパフォーマンス問題。

**直近のアクティビティ:** `billyjacobs2014` が 1.120.0 環境情報を共有。

**次のアクション:** 情報収集。

---

### ![Issue](icons/issue-opened.svg) [VSCode Updater - gives error that vscode is open. it should ideally close Vscode, apply update and then restart vscode](https://github.com/microsoft/vscode/issues/317332) `#317332`
**ステータス:** Open | **ラベル:** feature-request, install-update, windows

**概要:** Windows アップデータが「vscode が開いている」エラーで止まる。Insiders の `old_Code - Insiders.exe` のファイルハンドル取得で失敗。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Updater 改善。

---

### ![Issue](icons/issue-opened.svg) [Model Management UX Exploration](https://github.com/microsoft/vscode/issues/317128) `#317128`
**ステータス:** Open

**概要:** モデル発見・整理・選択に関する重複コンセプトの整理 RFC。

**直近のアクティビティ:** `pwang347` が `Auto` / その他項目への意見を投稿。

**次のアクション:** デザイン議論。

---

### ![Issue](icons/issue-opened.svg) [希望 Copilot Chat 模型选择器中加入 DeepSeek 模型选项](https://github.com/microsoft/vscode/issues/317312) `#317312`
**ステータス:** Open | **ラベル:** ai-translated

**概要:** Copilot Chat のモデル選択に DeepSeek を追加してほしいという中国語の要望（GPT-4o / GPT-5 mini / GPT-4.1 のみ表示されている状況）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** プロダクト判断。

---

### ![Issue](icons/issue-opened.svg) [TypeScript and intellisense often randomly opens with "if" snippet selected ignoring any properties](https://github.com/microsoft/vscode/issues/77453) `#77453`
**ステータス:** Closed

**概要:** TypeScript の IntelliSense で `if` スニペットがプロパティを差し置いて選択される長年のバグ。

**直近のアクティビティ:** `lloydjatkinson` が「5 年かかったがついに直ったようだ」とスクショ付きコメント。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [In Agents Window, the Claude picker doesn't show up over AHP in chat widget](https://github.com/microsoft/vscode/issues/316643) `#316643`
**ステータス:** Closed | **ラベル:** bug, agent-host

**概要:** Agents Window で送信後に Claude ピッカーが表示されない問題。

**直近のアクティビティ:** `roblourens` が「`agentHostChatInputPicker.ts` の問題は残るが、本質は vscode と Agents Window の不整合」とコメント。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [Caret smoothing make cursor not snappy](https://github.com/microsoft/vscode/issues/317157) `#317157`
**ステータス:** Open | **ラベル:** info-needed

**概要:** Caret smoothing でカーソルが鈍く感じる問題。

**直近のアクティビティ:** `liel-almog` が `code_status.txt` を添付。

**次のアクション:** 情報精査。

---

### ![Issue](icons/issue-opened.svg) [Fetch structured JSON for GH PR review URLs in prompts](https://github.com/microsoft/vscode/issues/316752) `#316752`
**ステータス:** Closed | **ラベル:** feature-request

**概要:** GitHub PR レビュー URL の構造化 JSON 取得要望。

**直近のアクティビティ:** `westurner` が「`github.com/*/*/pull/*` の URI 正規表現を skill 側で扱えるかも」と議論。

**次のアクション:** Closed。

---

### ![Issue](icons/issue-opened.svg) [SSH to windows host: VS Code Server failed to start](https://github.com/microsoft/vscode/issues/317348) `#317348`
**ステータス:** Open

**概要:** Windows ホストへの SSH で VS Code Server が起動失敗。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 情報収集。

---

