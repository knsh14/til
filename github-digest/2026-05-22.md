# GitHub ダイジェスト — 2026-05-22

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[feat(terminal): replace `libvterm` with `libghostty-vt`](https://github.com/neovim/neovim/pull/39773)** (neovim/neovim) — Neovim 内蔵ターミナルの基盤を libvterm から libghostty-vt に置き換える 71 ファイル規模の大改修。`gpanders` から承認済みで、コミット単位で常に動作する形のまま段階的にポートされている。
2. ![PR](icons/git-pull-request.svg) **[Reject locked malware installations](https://github.com/astral-sh/uv/pull/18936)** (astral-sh/uv) — `uv sync` などのプロジェクト同期処理時に OSV の `MAL-` レポートと照合し、ロックされたマルウェアパッケージのインストールを拒否する大型セキュリティ機能。25 ファイル、レビュー 32 件を経てマージ済み。
3. ![PR](icons/git-pull-request.svg) **[Add browser emulation toolbar](https://github.com/microsoft/vscode/pull/317831)** (microsoft/vscode) — VS Code 内蔵ブラウザに Chrome DevTools 風のデバイスエミュレーションツールバーを追加する 11 ファイル +1459 行の機能 PR。ビューポートや DPR、UA、モバイル切り替えに対応。マージ済み。
4. ![PR](icons/git-pull-request.svg) **[feat(macos): add tab support for QuickTerminal - continued](https://github.com/ghostty-org/ghostty/pull/9857)** (ghostty-org/ghostty) — macOS の Ghostty QuickTerminal にタブ機能を追加する継続作業 PR。20 ファイル、20 件のレビューが付き、視覚区別や macOS 15.6.2 でのフォーカス問題などが議論されている。
5. ![PR](icons/git-pull-request.svg) **[bump msgq](https://github.com/commaai/openpilot/pull/38057)** (commaai/openpilot) — `msgq` を更新して macOS 上で `process_replay` を動かせるようにする変更。UI プレビューも `mici`/`big` 双方で差分なしを確認しており、CI も全パス。

---

## sunnypilot/sunnypilot

> upstream 同期 PR と Kia Niro EV ユーザーからの ICBM/SCC 系機能要望が中心。

### ![PR](icons/git-pull-request.svg) [Sync: `commaai/openpilot:master` → `sunnypilot/sunnypilot:master`](https://github.com/sunnypilot/sunnypilot/pull/1843) `#1843`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 上流 `commaai/openpilot:master` を sunnypilot に取り込む定例同期 PR。100 ファイルにわたる大規模差分 (+4535/-90901)。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/`、`Jenkinsfile`、`SConstruct`、`.vscode/settings.json` など 600 ファイル相当。

**次のアクション:** CI 通過確認と upstream 差分のレビュー。

---

### ![Issue](icons/issue-opened.svg) [SCC-L: Longitudinal-based ICBM](https://github.com/sunnypilot/sunnypilot/issues/1846) `#1846`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 2023 Kia Niro EV は alpha long に未対応のため、ICBM (Intelligent Cruise Behavior Manager) で C4 に縦制御を任せている。現状の SCC-V (Vision) と SCC-M (Map) は競合しがちなので、モデルの縦制御出力をソースとして利用する SCC-L (Longitudinal) を追加してほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナによるトリアージとモデル縦出力の安定性評価。

---

### ![Issue](icons/issue-opened.svg) [Sunnylink: Custom ACC Increment allow 0 value](https://github.com/sunnypilot/sunnypilot/issues/1845) `#1845`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** Kia Niro EV の短押し (+1)、長押し (端数丸め) を考慮し、Custom ACC Speed Interval に 0 を許容したいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。継続要望があれば再オープン。

---

## commaai/openpilot

> macOS 対応の `msgq` 更新、modeld の対称サイズコンパイル、リモートテレオプ系のエンコーダー統合など、ビルドとリモート操作の地盤整備が同時進行。

### ![PR](icons/git-pull-request.svg) [bump msgq](https://github.com/commaai/openpilot/pull/38057) `#38057`
**ステータス:** Open | **ラベル:** なし

**概要:** `msgq` を更新して macOS 上で `process_replay` が動作するようにする。`cereal/messaging`、`modeld`、UI カメラビュー周りに小さな修正が伴う。

**直近のアクティビティ:** UI プレビューは mici/big とも動画が同一、process replay も 66 セグメント全通過。`commaci-public` がモデルリプレイ参照を付与。

**変更ファイル:** `cereal/messaging/__init__.py`、`msgq_repo`、`modeld.py`、`cameraview.py` など 6 ファイル。

**次のアクション:** マージ向けの最終確認。

---

### ![PR](icons/git-pull-request.svg) [Compile modeld warps with symbolic camera sizes](https://github.com/commaai/openpilot/pull/38078) `#38078`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `modeld` のワープを対称的なカメラサイズでコンパイルするように改修。`compile_modeld.py`、`compile_dm_warp.py`、`dmonitoringmodeld.py` を中心に書き直す。

**直近のアクティビティ:** process replay は 0 件差分で 66 通過。

**変更ファイル:** `selfdrive/modeld/SConscript`、`compile_modeld.py`、`dmonitoringmodeld.py` ほか 5 ファイル。

**次のアクション:** ドラフト解除に向けた整理。

---

### ![PR](icons/git-pull-request.svg) [remote teleop multi-stream encoder](https://github.com/commaai/openpilot/pull/38013) `#38013`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** 各カメラごとに別個のエンコーダーを持っていたライブストリーム実装を、単一エンコーダー側でフレームバッファを切り替える方式に変更。`livestreamSwitchCamera` イベントを WebRTC データチャネルで切り替え可能にする。

**直近のアクティビティ:** `adeebshihadeh` がレビュー中。process replay は差分 0。

**変更ファイル:** `cereal/log.capnp`、`encoderd.cc`、`loggerd.h`、`webrtcd.py` ほか 8 ファイル。

**次のアクション:** レビュー指摘の反映。

---

### ![PR](icons/git-pull-request.svg) [remote body teleop from connect support](https://github.com/commaai/openpilot/pull/38011) `#38011`
**ステータス:** Open | **ラベル:** なし

**概要:** connect 経由でジョイスティックライブストリームを扱えるようにする変更。athena 側で `getNotCar` と `startStream` をディスパッチし、ストリームの後処理を改善する。

**直近のアクティビティ:** `sshane` から 3 件のコメントレビュー、`stefpi` が応答。

**変更ファイル:** `athenad.py`、`webrtcd.py` (2 ファイル)。

**次のアクション:** sshane のフィードバック対応。

---

### ![PR](icons/git-pull-request.svg) [remote teleop frame timing](https://github.com/commaai/openpilot/pull/38014) `#38014`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** フレームデータにタイミング情報を付与し、connect アプリ側がストリーム遅延を詳細に把握できるようにする。

**直近のアクティビティ:** process replay 通過のみ。

**変更ファイル:** `system/webrtc/device/video.py`、`webrtcd.py`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Op model16 deep](https://github.com/commaai/openpilot/pull/38073) `#38073`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** modeld 周りの再構成。新しい `driving_off_policy.onnx`/`driving_on_policy.onnx` モデルを導入し、`compile_modeld.py` などを更新する作業 PR。本文には「DOESNT WORK」と注記あり。

**直近のアクティビティ:** process replay 通過、`commaci-public` がモデルリプレイ参照を投稿。

**変更ファイル:** `selfdrive/modeld/` 配下 13 ファイル。

**次のアクション:** 動作させた上でのドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [scons: enforce max file size for build products](https://github.com/commaai/openpilot/pull/38077) `#38077`
**ステータス:** Open | **ラベル:** なし

**概要:** ビルド成果物に最大ファイルサイズを SCons で強制する変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `SConstruct` のみ。

**次のアクション:** メンテナのレビュー。

---

### ![Issue](icons/issue-opened.svg) [Cannot build on Ubuntu (MATE) 24.04.3 with supplied setup script](https://github.com/commaai/openpilot/issues/36414) `#36414`
**ステータス:** Open | **ラベル:** bug

**概要:** Ubuntu MATE 24.04.3 のセットアップスクリプトでビルドが失敗する。`commonmodel_pyx.pyx` の Cython 化と clang++ コンパイル時にエラーが出る既知の問題。

**直近のアクティビティ:** 起票者は panda 用組み込みビルドを除外することで cabana のみビルド成功。`PeterYFPeng` が openpilot v0.11.0 でも同様のエラーを報告し解決策を求めている。

**次のアクション:** Ubuntu 24.04 系での組み込みビルド分離手順のドキュメント化。

---

### ![PR](icons/git-pull-request.svg) [2026.05.20 fix compressed_vipc.py by using pyzmq for zmq client](https://github.com/commaai/openpilot/pull/38072) `#38072`
**ステータス:** Open | **ラベル:** tools

**概要:** `tools/camerastream/compressed_vipc.py` が `cereal.messaging` 経由で `msgq.ipc_pyx` の C++ 実装を呼び出すが、`zmq_impl` 削除後に動作しなくなった問題に対し、`pyzmq` を直接使って実装し直すバグ修正。

**直近のアクティビティ:** process replay 通過のみ。

**変更ファイル:** `tools/camerastream/compressed_vipc.py` (+123/-24)。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [I compiled modeld failed](https://github.com/commaai/openpilot/issues/38076) `#38076`
**ステータス:** Open | **ラベル:** PC

**概要:** VirtualBox 上の Ubuntu 24.04 で `scons -u` を実行すると `modeld` のコンパイルが失敗するという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 詳細ログの追加調査。

---

### ![PR](icons/git-pull-request.svg) [modeld: zero copy npy inputs](https://github.com/commaai/openpilot/pull/38075) `#38075`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `compile_modeld.py` での npy 入力のゼロコピー化を試す 1 行変更の探索 PR。

**直近のアクティビティ:** `Armandpl` がモデルリプレイビルドの偽グリーンを指摘。

**変更ファイル:** `compile_modeld.py` (+1/-1)。

**次のアクション:** モデルリプレイ CI の真のステータス確認。

---

## commaai/opendbc

> VW ID.4 のカーポート開発、Hyundai Elantra 2024-25 対応、横方向セーフティのリファクタが進行。

### ![PR](icons/git-pull-request.svg) [VW ID.4](https://github.com/commaai/opendbc/pull/3366) `#3366`
**ステータス:** Open (Draft) | **ラベル:** car, volkswagen, car safety

**概要:** Volkswagen ID.4 のカーポート WIP。`InfiniteCable`/`DaHansi` のカーポートと `lukasloetkolben` のハーネスがクレジット。MEB CAN 定義 (`mebcan.py`) を新規追加。

**直近のアクティビティ:** `github-actions[bot]` の car behavior report が VW 系 130 セグメントの再生結果を投稿。

**変更ファイル:** `carcontroller.py` (+141)、`carstate.py` (+122)、`mebcan.py` (+279) ほか 21 ファイル。

**次のアクション:** behavior report の精査と再生差分の評価。

---

### ![PR](icons/git-pull-request.svg) [Hyundai: Car Port for Elantra 2024-25](https://github.com/commaai/opendbc/pull/3318) `#3318`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, car safety

**概要:** Hyundai Elantra 2024-25 のカーポート。`#3320`、`#3319` を前提とし、Issue `#1119` をクローズする。

**直近のアクティビティ:** `sshane` と `sunnyhaibin` のレビューコメント。

**変更ファイル:** `fingerprints.py`、`values.py`、`hyundai.h` ほか 13 ファイル。

**次のアクション:** 前提 PR のマージ後の最終レビュー。

---

### ![PR](icons/git-pull-request.svg) [curvature steering safety](https://github.com/commaai/opendbc/pull/3399) `#3399`
**ステータス:** Closed (Draft) | **ラベル:** car, car safety

**概要:** 曲率ベースのステアリングセーフティ実装。`elkoled` は ID.4 ポートに統合する旨をコメントし PR をクローズ。

**直近のアクティビティ:** 作者自身が #3366 への統合宣言。

**変更ファイル:** `safety/lateral.h` (+52)、`safety/tests/common.py` (+156) ほか 7 ファイル。

**次のアクション:** ID.4 PR (#3366) で継続評価。

---

### ![Issue](icons/issue-opened.svg) [[$300 Bounty] Toyota: cancel cruise without chime](https://github.com/commaai/opendbc/issues/1173) `#1173`
**ステータス:** Open | **ラベル:** enhancement, car, toyota

**概要:** openpilot がトヨタ車でクルーズをキャンセルする際に発生する余計なチャイム音を、Lexus IS で使われている別キャンセルメッセージを応用して取り除く $300 賞金タスク。

**直近のアクティビティ:** `zhaog100` が賞金を請求する宣言コメント。

**次のアクション:** 実装 PR の提出待ち。

---

## commaai/panda

> ヘッダー＋ソース分割リファクタが正式に進行開始、`bootkick: wake on CAN` はクローズ、MISRA 強化と CAN comms 分割の小規模変更が並走。

### ![Issue](icons/issue-opened.svg) [[$500 Bounty] Refactor panda to use source + header file structure](https://github.com/commaai/panda/issues/2171) `#2171`
**ステータス:** Closed | **ラベル:** refactor, cleanup, bounty

**概要:** panda 全体をヘッダーオンリー構造からソース＋ヘッダー構造に変更する $500 賞金タスク。

**直近のアクティビティ:** `adeebshihadeh` が AI スロップが多すぎるとして Issue 自体はクローズし、既存の PR から賞金対象を選別すると表明。`nguyencaoky1121-dev` が形式的な「攻略ロードマップ」コメントを投稿し、`zhaog100` も賞金請求を宣言。

**次のアクション:** 既存 PR の精査（#2404 など）。

---

### ![PR](icons/git-pull-request.svg) [bootkick: wake on CAN](https://github.com/commaai/panda/pull/2393) `#2393`
**ステータス:** Closed | **ラベル:** なし

**概要:** Tesla 向けに SOM 起動をイグニッションから分離し、CAN 経由で SOM を早期起動できるようにする提案。

**直近のアクティビティ:** `adeebshihadeh` は、具体的なメリットを見出しにくく、誤検出も多発しうる懸念からクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [free replay, no jungle necessary](https://github.com/commaai/panda/pull/2397) `#2397`
**ステータス:** Closed | **ラベル:** なし

**概要:** `board/debug/can_replay.h` および `can_replay_data.bin` 等を追加し、jungle なしで CAN リプレイを行えるようにする 8 ファイル変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add MISRA check for integer bool literals](https://github.com/commaai/panda/pull/2405) `#2405`
**ステータス:** Open | **ラベル:** なし

**概要:** bool に対する 0/1 整数リテラル割り当てを検出する cppcheck アドオンを追加し、既存の MISRA チェックと併走させる。既存のリテラルを修正し変異テストも追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `bool_literals.py`、`checkers.txt`、`test_mutation.py` ほか 6 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Split CAN comms into source and header files](https://github.com/commaai/panda/pull/2404) `#2404`
**ステータス:** Open | **ラベル:** なし

**概要:** Issue #2171 の方針に沿い、`can_comms.h` の実装を `can_comms.c` に分離する機械的なソース／ヘッダー分割。`main_definitions.h` のグローバル状態定義も `main_state.c` に移動。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `can_comms.c` (+174)、`can_comms.h` (+6/-142) ほか 11 ファイル。

**次のアクション:** メンテナによる動作差分の検証。

---

## neovim/neovim

> ターミナル基盤の libghostty-vt への乗り換え、LSP 重複排除、`vim.log` のシンク化、`nvim_on` 移行、`bufdelete` フォーカス問題など、API 内部刷新と LSP/UI まわりの改善が同時並行。Dependabot を含む 48 件が更新された。

### ![PR](icons/git-pull-request.svg) [feat(terminal): replace `libvterm` with `libghostty-vt`](https://github.com/neovim/neovim/pull/39773) `#39773`
**ステータス:** Open | **ラベル:** build, defaults, tui, terminal, ci, AI assisted

**概要:** 内蔵ターミナルの基盤を libvterm から libghostty-vt に置き換える 71 ファイル規模の大改修。フォーカス報告、ブラケットペースト、入力エンコード、属性レンダリングなどを段階的に新 API へ移行し、各コミット時点で動作する形を保つ。

**直近のアクティビティ:** `kylesower` が libghostty がリリースモードでビルドされているか確認すべきと指摘。`gpanders` が一通り眺めた上で承認、ローカルで動作確認したとコメント。

**変更ファイル:** `cmake.deps/`、`build.zig`、CI ワークフローなど 71 ファイル。

**次のアクション:** パフォーマンス計測の確認と最終レビュー。

---

### ![PR](icons/git-pull-request.svg) [refactor: introduce nvim_on internally](https://github.com/neovim/neovim/pull/39883) `#39883`
**ステータス:** Merged | **ラベル:** defaults, refactor, lsp, treesitter, diagnostic, snippet, plugin-editorconfig, ux-visibility, image, ui2

**概要:** 冗長な `nvim_create_autocmd` の代わりに内部用の `nvim_on` を導入し、Neovim 内部のオートコマンド利用箇所を順次置き換える。

**直近のアクティビティ:** `superatomic` が `group` 引数の取り回しについて、インデクサ的に渡せる API 案を提案。`justinmk` らがコメントレビュー。

**変更ファイル:** `defaults.lua`、`ui2/messages.lua` ほか 32 ファイル。

**次のアクション:** マージ済み。公開 API 化に向けた使用感の評価。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): deduplicate location results](https://github.com/neovim/neovim/pull/39941) `#39941`
**ステータス:** Open | **ラベル:** lsp

**概要:** 複数 LSP クライアント (`basedpyright` + `ty` + `ruff` など) を併用したとき、`get_locations()` が同一位置の重複エントリを quickfix に出してしまう問題を、ファイル名と範囲のキーで重複除去して修正。Issue #39926 を fix。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/lsp/buf.lua`、`buf_spec.lua`。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Feature request: move cursor after indented text](https://github.com/neovim/neovim/issues/39940) `#39940`
**ステータス:** Open | **ラベル:** なし

**概要:** `gp` と `p` の関係のように、インデント操作後にカーソルを移動できる新しいモーション (`g<`/`g>` や `<g`/`>g`) を追加してほしいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [feat(runtime): add directory buffer for editing directories](https://github.com/neovim/neovim/pull/39723) `#39723`
**ステータス:** Open | **ラベル:** runtime

**概要:** `:edit <dir>` と `nvim <dir>` の挙動を netrw 依存から切り離し、Lua 製の組み込みディレクトリリスト (`filetype=directory`) を新設する。netrw は `:Explore` やリモート操作向けに残す。

**直近のアクティビティ:** `justinmk` と `superatomic` で多数のレビューコメントが進行中。

**変更ファイル:** `runtime/lua/nvim/directory.lua` (+328)、`directory_spec.lua` ほか 9 ファイル。

**次のアクション:** 設計面のレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): advertise that CompletionItem.detail can be resolved](https://github.com/neovim/neovim/pull/39934) `#39934`
**ステータス:** Merged | **ラベル:** lsp

**概要:** `CompletionItem.detail` を解決できるにもかかわらず capability を広告していない問題を 1 行修正。

**直近のアクティビティ:** `MariaSolOs` から承認、マージ済み。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(autoread): use file watchers for real-time change detection](https://github.com/neovim/neovim/pull/37971) `#37971`
**ステータス:** Open | **ラベル:** なし

**概要:** `'autoread'` の検出を `FocusGained` などのイベントに依存する反応的方式から、`vim._watch` を用いたファイル監視によるリアルタイム検出へ変更する長年の要望 (#1380) の実装。

**直近のアクティビティ:** `OleksandrChekhovskyi` がレビュー指摘を反映し、`justinmk` の「過度に粒度の細かい `it()` ブロックは統合してほしい」要請に対し、価値の薄いテストを 1 件削除し 2 件を統合した。

**変更ファイル:** `runtime/lua/nvim/autoread.lua` (+149)、`autoread_spec.lua` (+236) ほか 8 ファイル。

**次のアクション:** justinmk による次ラウンドレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(vim.net): add response status code and headers in on_response callback](https://github.com/neovim/neovim/pull/39685) `#39685`
**ステータス:** Open | **ラベル:** なし

**概要:** `vim.net` の `on_response` コールバックに `status`/`headers` を追加し、`vim.net.status` という HTTP ステータスコード列挙を新設する。

**直近のアクティビティ:** `deathbeam`、`yochem`、`justinmk` らがレビューコメントを連投。

**変更ファイル:** `vim/net.lua` (+96/-10) ほか 4 ファイル。

**次のアクション:** レビュー指摘の反映。

---

### ![PR](icons/git-pull-request.svg) [fix(fs): use uv.fs_lstat fallback when uv.fs_scandir_next returns no type in M.dir](https://github.com/neovim/neovim/pull/39749) `#39749`
**ステータス:** Open | **ラベル:** filesystem

**概要:** Btrfs/ext2/ext3/ext4 以外の Linux ファイルシステムで `uv.fs_scandir_next` が type を返さないケースに対し、`uv.fs_lstat` をフォールバックとして利用する修正。

**直近のアクティビティ:** `echasnovski` がテスト追加場所を案内、`abdulahmoda` が反映、`justinmk` も同意。

**次のアクション:** テスト追加後のマージ。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): populate popup menu and info popup consistently](https://github.com/neovim/neovim/pull/39939) `#39939`
**ステータス:** Open | **ラベル:** lsp

**概要:** `completionItem/resolve` のサポート可否や初期 `CompletionItem` のフィールド有無で、補完ポップアップと info ポップアップの表示が一貫しない問題を整理する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `lsp/completion.lua` (+66/-73)、`completion_spec.lua` (+227/-154)。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(lsp): resolve lsp config](https://github.com/neovim/neovim/pull/39391) `#39391`
**ステータス:** Open | **ラベル:** lsp

**概要:** `vim.lsp` の設定解決ロジックの整理。前 PR `#33578` の後継。

**直近のアクティビティ:** `justinmk`、`TheRealLorenz`、`superatomic` のコメントレビュー。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): reject process exit in fast callbacks](https://github.com/neovim/neovim/pull/39937) `#39937`
**ステータス:** Open | **ラベル:** lua

**概要:** libuv の fast callback 中に `os.exit()` を呼ぶとクラッシュする Issue #39783 に対し、`E5560` で fast コンテキストエラーを返して拒否する修正。

**直近のアクティビティ:** 作者の自己レビュー。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [ui2 + diagnostic: `E1159: Cannot open a float when closing the buffer`](https://github.com/neovim/neovim/issues/39938) `#39938`
**ステータス:** Open | **ラベル:** ui2

**概要:** ui2 を有効化した状態で別タブの LSP 接続バッファを削除すると、`vim/_core/ui2:105` で `E1159` がスローされるという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ui2 ハンドラのバッファクローズ時挙動の確認。

---

### ![PR](icons/git-pull-request.svg) [docs: misc](https://github.com/neovim/neovim/pull/39908) `#39908`
**ステータス:** Open | **ラベル:** documentation

**概要:** ドキュメントの誤字・整形などの修正。

**直近のアクティビティ:** `SebasF1349` が `vim.pos.cursor()` の例にあるタイポを指摘 (`vim.pos.lsp` → `vim.pos.cursor`)。

**次のアクション:** タイポ修正の取り込み。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): defer exit from fast callbacks](https://github.com/neovim/neovim/pull/39895) `#39895`
**ステータス:** Closed (Draft) | **ラベル:** lua

**概要:** 上記 #39937 の前段案。`os.exit()` をスケジュール経由にする。

**直近のアクティビティ:** `justinmk` がクラッシュ修正は基本必ず行うべきという立場を表明。`phanen` は `vim.cmd.cquit()` への置換でワークアラウンドできたと報告。`barrettruth` は workaround 採用で当面 not planned としてクローズした旨を説明。

**次のアクション:** クローズ済み。代替 PR #39937 にフォーカス。

---

### ![Issue](icons/issue-opened.svg) [headless instance crash after 9432e6c1e2e93ffffabeeeaeb6ccc0cbdf9609dd](https://github.com/neovim/neovim/issues/39783) `#39783`
**ステータス:** Open | **ラベル:** bug-crash, bug-regression

**概要:** fzf-lua の CI でヘッドレス nvim が当該コミット以降クラッシュする。スタックトレースから `os.exit()` が原因と推定される。

**直近のアクティビティ:** `justinmk` がアサーションを追加する形での修正を提案、`barrettruth` は workaround で十分という立場。

**次のアクション:** #39937 が決着すれば解決見込み。

---

### ![PR](icons/git-pull-request.svg) [docs(lsp): update generated lsp documentation](https://github.com/neovim/neovim/pull/37273) `#37273`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** LSP メタモデルから自動生成される `_meta/protocol.lua` を最新化する PR。

**直近のアクティビティ:** `tris203` がメタモデル再生成待ちでまだブロックされていると報告、`justinmk` は上流問題解決済みのはずと指摘。

**次のアクション:** メタモデル再生成。

---

### ![PR](icons/git-pull-request.svg) [fix(health): look for tags in help tags files](https://github.com/neovim/neovim/pull/39742) `#39742`
**ステータス:** Merged | **ラベル:** ci:backport release-0.12

**概要:** `:checkhealth` バッファが help シンタックスを使うのに `CTRL-]` でヘルプジャンプできない問題を、help タグファイルを検索して解決する 1 ファイル修正。

**直近のアクティビティ:** `justinmk`、`marcuscaisey` のコメントレビュー後マージ。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(float): simplify :bufdelete focus retry](https://github.com/neovim/neovim/pull/39928) `#39928`
**ステータス:** Merged | **ラベル:** なし

**概要:** PR #39858 のフォローアップ。`close_windows` がホルダーウィンドウを返す設計を改め、`do_buffer_ext` 側で `one_window` を直接確認する形に簡素化。

**直近のアクティビティ:** `seandewar` がさらに簡素化できる差分を提示。`glepnir` が現テストで十分との立場。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [fold: confusing `foldcolumn` behavior](https://github.com/neovim/neovim/issues/39916) `#39916`
**ステータス:** Open | **ラベル:** folds

**概要:** `foldcolumn` を 2 にしたとき、複数 fold が重なる行の表示 (例: `23`) が直感的でないという指摘。

**直近のアクティビティ:** `luukvbaal` が「`-` は fold 開始、`2` は追加で含まれる fold 数」と説明。`matystroia` は line 4 の `2` が依然不明確と返答。改善案として「スピル指標」を first column に置く案も提示された。

**次のアクション:** 関連 PR #39917 で改善試行中。

---

### ![PR](icons/git-pull-request.svg) [ci: bump zizmorcore/zizmor-action from 0.5.3 to 0.5.5](https://github.com/neovim/neovim/pull/39933) `#39933`
**ステータス:** Open | **ラベル:** ci, dependencies, github_actions

**概要:** Dependabot による GitHub Actions の小規模バージョンアップ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** CI 通過後マージ。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(net): set buftype=nofile (again)](https://github.com/neovim/neovim/pull/39932) `#39932`
**ステータス:** Open | **ラベル:** target:release

**概要:** PR #39930 を `release-0.12` にバックポートする自動 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(net): set buftype=nofile (again)](https://github.com/neovim/neovim/pull/39930) `#39930`
**ステータス:** Merged | **ラベル:** ci:backport release-0.12

**概要:** 24e00f2844bc の再適用。`runtime/plugin/net.lua` に `buftype=nofile` を 1 行追加。

**直近のアクティビティ:** Bot がバックポート PR #39932 の生成を通知。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Support counts for the line (`l`) text object](https://github.com/neovim/neovim/issues/39872) `#39872`
**ステータス:** Open | **ラベル:** editor

**概要:** PR #39845 で追加された `l` テキストオブジェクトを、count 指定で複数行に伸張させたいという要望。

**直近のアクティビティ:** `mikejmcguirk` が `[count]il` の挙動について作者案に同意。

**次のアクション:** 仕様検討。

---

### ![PR](icons/git-pull-request.svg) [fix(fold): confusing fill](https://github.com/neovim/neovim/pull/39917) `#39917`
**ステータス:** Open (Draft) | **ラベル:** needs:response

**概要:** Issue #39916 の修正試行。`drawline.c` の foldcolumn 描画ロジックに変更を加える。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナとの仕様合意。

---

### ![PR](icons/git-pull-request.svg) [docs(BUILD.md): fix duplicate "the"](https://github.com/neovim/neovim/pull/39929) `#39929`
**ステータス:** Open (Draft) | **ラベル:** documentation, typo

**概要:** `BUILD.md` の重複した「the」を修正する 1 行 PR。本文には、Cursor/Claude の AI エージェントが余剰クレジット消費目的で自動作成した PR だと明記されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナの判断。

---

### ![PR](icons/git-pull-request.svg) [feat(pos,range): supports store any additional data on pos and range](https://github.com/neovim/neovim/pull/39571) `#39571`
**ステータス:** Open | **ラベル:** needs:response

**概要:** `vim.range` などに任意の追加データを保持できるようにし、比較などの機能をフル活用できるようにする提案。

**直近のアクティビティ:** `justinmk` のコメントレビュー。

**次のアクション:** API 設計の合意。

---

### ![Issue](icons/issue-opened.svg) [logging: log messages emit LogMsg event](https://github.com/neovim/neovim/issues/39927) `#39927`
**ステータス:** Open | **ラベル:** logging, events, messages

**概要:** ログメッセージを autocmd の `LogMsg` イベントとして発行し、複数シンクをイベントエミッタ経由で実装できるようにする提案。

**直近のアクティビティ:** `justinmk` が `nvim_echo` を transport に流用する現状の問題点を分析。

**次のアクション:** 関連 PR #39750 との合意。

---

### ![PR](icons/git-pull-request.svg) [refactor(json)!: drop "eval" impl of JSON de/serialization](https://github.com/neovim/neovim/pull/39181) `#39181`
**ステータス:** Open (Draft) | **ラベル:** refactor, breaking-change

**概要:** Vimscript⇔JSON のラウンドトリップを担う `eval/decode.c`/`eval/encode.c` を削除し、JSON ロジックを Lua 側に集約する探索 PR。`#28683` の fix を含む。

**直近のアクティビティ:** `zeertzjq` のコメントレビュー。

**次のアクション:** 削除影響の評価。

---

### ![PR](icons/git-pull-request.svg) [refactor(lsp): message stream abstraction for message framing](https://github.com/neovim/neovim/pull/39000) `#39000`
**ステータス:** Open | **ラベル:** refactor, lsp

**概要:** JSON-RPC クライアントから LSP の「base protocol」相当のメッセージフレーミングを `vim.MessageStream` として抽出するリファクタ。

**直近のアクティビティ:** `lewis6991` が承認。`mfussenegger`、`ofseed`、`justinmk` のコメント。命名 (`format` vs `decode/encode`) の議論。

**次のアクション:** 命名議論決着後マージ。

---

### ![PR](icons/git-pull-request.svg) [fix(float): `:bufdelete` in floatwin may change focus](https://github.com/neovim/neovim/pull/39858) `#39858`
**ステータス:** Merged | **ラベル:** floatwin

**概要:** フロートウィンドウから別バッファを削除するとフォーカスがホルダーウィンドウに移ってしまう問題を、`switch_win_noblock()` を使う形で修正。`#39827` を fix。

**直近のアクティビティ:** `seandewar` のドライブバイコメント、`justinmk` が AI 生成パッチの改善案を提示。

**次のアクション:** マージ済み。フォローアップ PR #39928 でさらに簡素化。

---

### ![PR](icons/git-pull-request.svg) [fix(logging): vim.log followup](https://github.com/neovim/neovim/pull/39727) `#39727`
**ステータス:** Open (Draft) | **ラベル:** lsp, logging

**概要:** `vim.log.new()` の `name` を必須位置引数化し、`format_func` の改修で失われた「ログ可否」判定を復活させるフォローアップ。

**直近のアクティビティ:** `justinmk` のコメントレビュー多数。

**次のアクション:** 設計確定。

---

### ![PR](icons/git-pull-request.svg) [refactor(log)!: extract log sink as an interface](https://github.com/neovim/neovim/pull/39750) `#39750`
**ステータス:** Open | **ラベル:** refactor, lsp, logging, breaking-change

**概要:** `vim.log` のファイル書き込み専用実装を `vim.log.Sink` インタフェース化し、`vim.log.FileSink` を提供する。バッファリングや非同期書き込みなど将来拡張を見据えた変更。

**直近のアクティビティ:** `justinmk` が autocmd イベントベースのアーキテクチャ案を提示。

**次のアクション:** Issue #39927 との方針整合。

---

### ![PR](icons/git-pull-request.svg) [feat(remote): make --remote wait by default with +async opt-out](https://github.com/neovim/neovim/pull/37951) `#37951`
**ステータス:** Open (Draft) | **ラベル:** needs:response, remote, AI assisted

**概要:** `--remote` をデフォルトで wait させ、`+async` で明示的にオプトアウトする変更で、`neovim-remote` 不要のフローを提供する。

**直近のアクティビティ:** `justinmk` の複数コメントレビュー。

**次のアクション:** 互換性とドキュメント整理。

---

### ![PR](icons/git-pull-request.svg) [fix(ui2): unable to stop display of very long message](https://github.com/neovim/neovim/pull/39823) `#39823`
**ステータス:** Merged | **ラベル:** ui2

**概要:** ui2 の長大メッセージ表示処理で進捗フィードバックも中断手段もないため、100ms 超で部分表示し CTRL-C 中断を許す改修。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [fix(lsp): deduplicate location results from multiple LSP clients in get_locations()](https://github.com/neovim/neovim/issues/39926) `#39926`
**ステータス:** Open | **ラベル:** lsp

**概要:** PR #39941 の元 Issue。複数 LSP クライアント環境で `vim.lsp.buf.references()` が結果を N 重に返す。

**直近のアクティビティ:** `justinmk` が `on_list` の存在を案内しつつ、警告との両立は別 PR で対応すべきとコメント。

**次のアクション:** PR #39941 でフォロー。

---

### ![PR](icons/git-pull-request.svg) [feat(api): add operation field to nvim_set_option_value](https://github.com/neovim/neovim/pull/39849) `#39849`
**ステータス:** Open (Draft) | **ラベル:** api

**概要:** `nvim_set_option_value` に `operation` フィールド ("set"/"append"/"prepend"/"remove") を追加し、`:set opt+=` などと同等の API を提供。`#38420` 対応。

**直近のアクティビティ:** `justinmk` が LeakSanitizer のリーク検出ログを共有。

**次のアクション:** メモリリーク修正。

---

### ![Issue](icons/issue-opened.svg) [`nvim_buf_delete()` can permanently change current window](https://github.com/neovim/neovim/issues/39827) `#39827`
**ステータス:** Closed | **ラベル:** api, bug-regression, has:bisected, floatwin

**概要:** フロートウィンドウ内で `nvim_buf_delete()` するとフォーカスウィンドウが恒久的に変わる回帰バグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #39858 でフィックス済み。

---

### ![PR](icons/git-pull-request.svg) [feat(diagnostics): add ui2 msg area as diagnostic handler](https://github.com/neovim/neovim/pull/39871) `#39871`
**ステータス:** Open (Draft) | **ラベル:** diagnostic, ui2

**概要:** ui2 のメッセージ領域を diagnostic ハンドラとして使えるようにする WIP。命名 (ui2 / message / cmd / echo) や `fmt`/`severity`/`severity_sort` の設定オプション設計を検討中。

**直近のアクティビティ:** `bfredl` の COMMENTED レビュー。

**次のアクション:** ハンドラ名と設定スキーマの確定。

---

### ![Issue](icons/issue-opened.svg) [LSP: purescript-language-server "Sort/reformat imports" code action crashes](https://github.com/neovim/neovim/issues/39884) `#39884`
**ステータス:** Closed | **ラベル:** ux, lsp

**概要:** PureScript LS の Sort/reformat imports コードアクション実行時に `workspace_edit` の userdata index でクラッシュする。

**直近のアクティビティ:** `MariaSolOs` は LSP 仕様で nil が来ない場面で過剰防衛は不要との立場、`justinmk` は実例があれば 1 行 assert 程度は入れて良いと反論。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [refactor(gen): use terminfo field data to generate tui.txt](https://github.com/neovim/neovim/pull/39836) `#39836`
**ステータス:** Merged | **ラベル:** refactor, tui

**概要:** `$NVIM_TERMDEFS` フィールド変更で `tui.txt` がドリフトする問題を、`gen_vimdoc.lua` で `brief_xform` を導入し docs を自動生成する形で解決。

**直近のアクティビティ:** `justinmk` と `kylesower` のコメントレビュー。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [feat(pack): code action for updating all plugins](https://github.com/neovim/neovim/pull/39848) `#39848`
**ステータス:** Closed | **ラベル:** packages

**概要:** `vim.pack` に「全プラグイン更新」のコードアクションを追加する提案。

**直近のアクティビティ:** `echasnovski` と `MariaSolOs` がドキュメントとアクションの示し方を議論。最終的に作者がクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/39776) `#39776`
**ステータス:** Open (Draft) | **ラベル:** vim-patch

**概要:** vim 側 `cmdline-history` 更新の取り込み。

**直近のアクティビティ:** `zeertzjq` が対応 vim-patch を案内。

**次のアクション:** 取り込み。

---

### ![Issue](icons/issue-opened.svg) [put_spec.lua occasionally fails ...](https://github.com/neovim/neovim/issues/36250) `#36250`
**ステータス:** Closed | **ラベル:** test, needs:response

**概要:** put_spec.lua のフラッキーテスト。

**直近のアクティビティ:** Bot が情報不足のため自動クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(tui): can not set titlestring if TERM=wezterm](https://github.com/neovim/neovim/pull/39226) `#39226`
**ステータス:** Closed | **ラベル:** tui, needs:response

**概要:** WezTerm でタイトル更新が効かない問題への対応。

**直近のアクティビティ:** 情報待ち期限切れによる自動クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0506: home_replace() function can be improved](https://github.com/neovim/neovim/pull/39925) `#39925`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** vim 9.2.0506 のバックポート。`home_replace()` のリファクタを取り込み、関連する `set_b0_fname()`/`findswapname()` も整理。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: runtime file updates](https://github.com/neovim/neovim/pull/39924) `#39924`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** ランタイムファイル更新 (rust, mbsync) のバックポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): flush change notifications on save](https://github.com/neovim/neovim/pull/39894) `#39894`
**ステータス:** Merged | **ラベル:** lsp

**概要:** サーバが `textDocumentSync.save` capability を広告しない場合 (`ruff` など) に保存時の `didChange` がフラッシュされず diagnostic が古いまま残る問題を修正。

**直近のアクティビティ:** `MariaSolOs` が #29792 との関連を整理、`justinmk` 承認。

**次のアクション:** マージ済み。

---

## nvim-telescope/telescope.nvim

> Plenary アーカイブ化を見据えた依存切り出し作業が中心。

### ![PR](icons/git-pull-request.svg) [feat(neoplen): restore minimal async.uv](https://github.com/nvim-telescope/telescope.nvim/pull/3669) `#3669`
**ステータス:** Open | **ラベル:** なし

**概要:** PR #3647 で削除予定の `neoplen.async.uv` を、`telescope-frecency` が使う 7 関数 (`fs_stat`、`fs_open`、`fs_close`、`fs_read`、`fs_write`、`fs_unlink`、`fs_realpath`) のラッパーに最小化して復活させる。

**直近のアクティビティ:** `clason` が「むしろ関数を直接追加する形のほうが簡単では」と提案。

**変更ファイル:** `lua/neoplen/async/init.lua`、`uv_async.lua`。

**次のアクション:** ラッパー方式か直接追加か方針決定。

---

### ![PR](icons/git-pull-request.svg) [feat: vendor and clean up plenary](https://github.com/nvim-telescope/telescope.nvim/pull/3647) `#3647`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Plenary アーカイブ化を踏まえ、Telescope が使う部分を切り出して内製化し、可能なら現行の Neovim API に置き換える調査 PR。73 ファイル、+2959/-367 行。

**直近のアクティビティ:** `delphinus` が #3669 でミニマル `async.uv` を切り出した旨を共有、`clason` が同意。`async_job` の `class.lua` モジュール処理が今後の課題。

**変更ファイル:** `lua/neoplen/async/` 配下を中心に 73 ファイル。

**次のアクション:** #3669 のマージ後に統合継続。

---

## ghostty-org/ghostty

> macOS QuickTerminal のタブ、Win32 アプリランタイム、GTK occlusion、Galician 翻訳など、新機能とプラットフォーム拡張が活発。

### ![PR](icons/git-pull-request.svg) [feat(macos): add tab support for QuickTerminal - continued](https://github.com/ghostty-org/ghostty/pull/9857) `#9857`
**ステータス:** Open | **ラベル:** os/macos

**概要:** PR #8469 を引き継ぎ、QuickTerminal にタブ機能を追加。`cmd+w` の動作改善、マージコンフリクトの解消などを含み、20 ファイル +2130 行の大規模変更。

**直近のアクティビティ:** `iwex` が `bo2themax` にレビュー依頼。`bo2themax` はフォーカス時のタブ視覚区別が弱い点などインラインコメント多数で `CHANGES_REQUESTED`。`aeroith`/`bugrauluyurt` は既に承認。

**変更ファイル:** `QuickTerminalController.swift` (+136/-38)、`QuickTerminalTabBarView.swift` (+386)、`QuickTerminalTabDragDelegate.swift` (+267) ほか 20 ファイル。

**次のアクション:** `bo2themax` 指摘の反映と最終レビュー。

---

### ![PR](icons/git-pull-request.svg) [macos: remove tiny 1px gray border in non-native fullscreen](https://github.com/ghostty-org/ghostty/pull/11325) `#11325`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS 26 でウィンドウシャドウ有効時に描画される 1px の灰色ボーダーを、非ネイティブフルスクリーンで非表示にする修正。

**直近のアクティビティ:** `mitchellh` と `bo2themax` がそれぞれ `CHANGES_REQUESTED`。

**変更ファイル:** `TerminalWindow.swift`、`Fullscreen.swift`。

**次のアクション:** 指摘反映。

---

### ![Issue](icons/issue-opened.svg) [Add `toggle_background_opacity` keybind action](https://github.com/ghostty-org/ghostty/issues/5047) `#5047`
**ステータス:** Open | **ラベル:** gtk

**概要:** ペアプロやスクリーン共有のために、背景透過をキーバインドでトグルできるアクションを追加してほしい要望 (iTerm の Toggle Transparency 相当)。

**直近のアクティビティ:** `leejunho-com` が `sed` で `background-opacity` を書き換える bash スクリプトを共有。

**次のアクション:** GTK 側実装の検討。

---

### ![PR](icons/git-pull-request.svg) [i18n: add Galician Translation](https://github.com/ghostty-org/ghostty/pull/12674) `#12674`
**ステータス:** Open | **ラベル:** translations

**概要:** Galician (ガリシア語) ロケール `gl.po` を追加。

**直近のアクティビティ:** `00-kat` がメタデータの修正を確認、内容のレビュアーは別途必要と返答。`README_TRANSLATORS.md` に沿った `CODEOWNERS` と locales 登録が必要だった点を案内。

**変更ファイル:** `CODEOWNERS`、`po/gl.po`、`i18n_locales.zig`。

**次のアクション:** ガリシア語話者によるレビュー。

---

### ![PR](icons/git-pull-request.svg) [apprt: minimal Win32 skeleton (window + WGL + CoreSurface wiring)](https://github.com/ghostty-org/ghostty/pull/12403) `#12403`
**ステータス:** Open | **ラベル:** なし

**概要:** Ghostty の Win32 apprt 上流化シリーズ第 1 弾。`.win32` バリアントを Runtime enum に追加し、`zig build -Dtarget=native-native-gnu` で `cmd.exe` を ConPTY 上で動かせる最低限のターミナルを実現。

**直近のアクティビティ:** `mattn` がレビュー継続依頼。

**変更ファイル:** `src/apprt/win32/App.zig` (+294)、`Surface.zig` (+192) ほか 11 ファイル。

**次のアクション:** メンテナの再レビュー。

---

### ![PR](icons/git-pull-request.svg) [gtk: wire up occlusionCallback for non-focused tabs](https://github.com/ghostty-org/ghostty/pull/12760) `#12760`
**ステータス:** Open | **ラベル:** gtk

**概要:** Issue #12745 で議論されていた、フォーカスされていないサーフェスのレンダリング挙動をプラットフォーム間で統一する作業の Linux/GTK 実装。既存の `glareaMap`/`glareaUnmap` に `updateOcclusion` を追加する形。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `surface.zig` (+9)、`renderer/Thread.zig` (+14/-4)。

**次のアクション:** メンテナレビュー。

---

## astral-sh/uv

> 0.11.16 リリース、ロックファイルマルウェア拒否のマージ、`uv audit` の OSV エラー耐性改善、Renovate Dashboard の更新など多数。Renovate 含む 46 件。

### ![Issue](icons/issue-opened.svg) [Package conflicts do not include extras](https://github.com/astral-sh/uv/issues/19525) `#19525`
**ステータス:** Open | **ラベル:** bug

**概要:** `[tool.uv].conflicts` で `{ package = "pkgB" }` と書いても extras (`pkgB[e]`) との競合が除外されないバグ。0.11.14、Python 3.12.11、Windows/Linux。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.16](https://github.com/astral-sh/uv/pull/19522) `#19522`
**ステータス:** Merged | **ラベル:** なし

**概要:** uv 0.11.16 リリース。CHANGELOG とクレート群のバージョン更新を含む 153 ファイル。

**直近のアクティビティ:** `zanieb` のコメントレビュー多数。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix uv auth login/logout keyring provider help](https://github.com/astral-sh/uv/pull/19520) `#19520`
**ステータス:** Open | **ラベル:** なし

**概要:** `native` プロバイダ廃止後のヘルプ文言を修正。Issue #19205 fix。

**直近のアクティビティ:** `zanieb` が「いっそ無視されているオプション自体を隠すべき」と提案。

**次のアクション:** オプション非表示化への方針切替。

---

### ![PR](icons/git-pull-request.svg) [Add a `uv lock` check to CI](https://github.com/astral-sh/uv/pull/19524) `#19524`
**ステータス:** Merged | **ラベル:** internal

**概要:** `uv lock` のドリフト検知ワークフローを CI に追加。

**直近のアクティビティ:** `woodruffw` 承認、`zanieb` のコメント。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate の依存関係ダッシュボード。Hidden Unicode 警告などを含む定期更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 依存更新の継続。

---

### ![PR](icons/git-pull-request.svg) [Improve crates.io new crate error message](https://github.com/astral-sh/uv/pull/19523) `#19523`
**ステータス:** Merged | **ラベル:** internal

**概要:** `scripts/setup-crates-io-publish.py` のエラーメッセージ改善。

**直近のアクティビティ:** `woodruffw` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Improve the 403 forbidden hint to suggest `ignore-error-codes` when applicable](https://github.com/astral-sh/uv/pull/19521) `#19521`
**ステータス:** Open | **ラベル:** error messages

**概要:** プロキシ環境などで 403 が返るインデックスに対し、`ignore-error-codes` を提案するヒントを追加。

**直近のアクティビティ:** Bot がテストインベントリ変更を報告。

**変更ファイル:** `registry_client.rs`、`index_url.rs`、`pubgrub/report.rs` ほか 4 ファイル。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Define extra per `--package` with `uv sync`](https://github.com/astral-sh/uv/issues/19506) `#19506`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv sync --package=A --package=B --extra=E` が両パッケージに E を適用するため、`--package=A[E]` のような書式で指定したいという要望。

**直近のアクティビティ:** `zanieb` は `--package` で extras を許容するより `--extra` で `package[key, ...]` 形式を受け入れるべきかもと回答。

**次のアクション:** API 設計検討。

---

### ![PR](icons/git-pull-request.svg) [Generate PEP 740 attestations](https://github.com/astral-sh/uv/pull/19489) `#19489`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `uv publish` 内で PEP 740 アテステーションを自動生成し、`attest-action` 等を不要にする。`chrono` から `jiff` への依存縮小など TODO 多数。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `uv-pep740/` 新設ほか 15 ファイル。

**次のアクション:** 依存トリミング後の WIP 解除。

---

### ![PR](icons/git-pull-request.svg) [Handle missing archives for URL- and path-specified wheels](https://github.com/astral-sh/uv/pull/19518) `#19518`
**ステータス:** Open | **ラベル:** なし

**概要:** Issue #6147 の修正。URL/パス指定の wheel が再度参照されたときにアーカイブが消えているケースを正しく扱う。

**直近のアクティビティ:** Bot のテストインベントリレポート。

**変更ファイル:** `distribution_database.rs`、`installer/src/plan.rs`、`pip_install.rs` (+113)。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [`uv lock` Improvement: a consistent environment for `prepare_metadata_for_build_wheel`](https://github.com/astral-sh/uv/issues/7390) `#7390`
**ステータス:** Closed | **ラベル:** needs-decision

**概要:** `uv lock` 時に使われる Python 環境が `uv venv` 既定環境と一致しない問題。

**直近のアクティビティ:** `charliermarsh` がクローズ、`sharifhsn` が宣言的ビルド環境 (`tool.uv.extra-build-dependencies` 等) として既に出荷済みと案内。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Reject locked malware installations](https://github.com/astral-sh/uv/issues/18781) `#18781`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** ロックファイルがマルウェアを含む場合にインストールを拒否する機能。

**直近のアクティビティ:** `kam193` が MAL アドバイザリ発行への協力を表明。`woodruffw` がドキュメント側 Issue #19519 を作成。

**次のアクション:** PR #18936 マージで実装済み。

---

### ![Issue](icons/issue-opened.svg) [Document malware check behavior](https://github.com/astral-sh/uv/issues/19519) `#19519`
**ステータス:** Open | **ラベル:** documentation

**概要:** Preview 解除後にマルウェアチェック挙動を公開ドキュメントに記載する追跡 Issue。OSV を使うこと、`MAL` アドバイザリが PyPI とは独立に発行されること、ロック解決時のみチェックすることなどを明示する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ドキュメント執筆。

---

### ![Issue](icons/issue-opened.svg) [Relative path inconsistency in uv.lock for cache_dir at different depths](https://github.com/astral-sh/uv/issues/15417) `#15417`
**ステータス:** Closed | **ラベル:** bug

**概要:** ユーザーごとに `UV_CACHE_DIR` 相対パスが変わる結果、`uv.lock` の差分が無駄に発生する問題。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Add support for direct archive dependencies in Git](https://github.com/astral-sh/uv/pull/10072) `#10072`
**ステータス:** Merged | **ラベル:** enhancement

**概要:** Git リポジトリ内の wheel/sdist を直接参照するソースをサポートする 53 ファイル変更。

**直近のアクティビティ:** `xcorp` が確認を依頼、`konstin` が承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Speedup discovery of large workspaces](https://github.com/astral-sh/uv/pull/18311) `#18311`
**ステータス:** Open | **ラベル:** performance

**概要:** ワークスペースルートとメンバの `pyproject.toml` を複数回読み込まないようにし、`uv run python -V` を airflow ワークスペースで約 1.9 倍高速化。

**直近のアクティビティ:** `EliteTK` が概ね良いが `update_project` 周辺は再確認したい旨をコメント。`konstin`、`zanieb` の議論も継続。

**変更ファイル:** `workspace.rs` (+312/-140) ほか 13 ファイル。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [Add a benchmark interface to the `uv` crate](https://github.com/astral-sh/uv/pull/19486) `#19486`
**ステータス:** Open (Draft) | **ラベル:** performance, testing

**概要:** `uv-bench` クレートを新設し、コマンド呼び出しのベンチマークを 19 ファイルで整備。CodSpeed では 5 ベンチ無変化、68 ベンチ新規。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [Add first-party line wrapping](https://github.com/astral-sh/uv/pull/17535) `#17535`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** miette 撤去 (#17110) で失う行ラップ機能を自前実装する `uv-errors` クレートを追加。

**直近のアクティビティ:** `zanieb` の自己レビューコメント多数。

**次のアクション:** miette 撤去 PR と連動。

---

### ![PR](icons/git-pull-request.svg) [Remove miette](https://github.com/astral-sh/uv/pull/17110) `#17110`
**ステータス:** Open (Draft) | **ラベル:** no-build

**概要:** `miette` 依存を撤去する大規模変更。28 ファイル +1868/-1961。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #17535 マージ後に進める。

---

### ![Issue](icons/issue-opened.svg) [Skip 403 Errors instead of raising](https://github.com/astral-sh/uv/issues/19517) `#19517`
**ステータス:** Open | **ラベル:** enhancement

**概要:** プロキシ環境で 403 を返すパッケージを自動スキップして古いバージョンに解決し直すオプションがほしいという要望。

**直近のアクティビティ:** `zanieb` が `PR #12805` を案内。

**次のアクション:** 既存 PR で対応継続。

---

### ![Issue](icons/issue-opened.svg) [`uv` changes the name of the project to dash-case even if the `--name` option was given](https://github.com/astral-sh/uv/issues/19509) `#19509`
**ステータス:** Closed | **ラベル:** なし

**概要:** `uv init --package --name undescore_projectname` で snake_case 指定してもダッシュ化されてしまう挙動への異議。

**直近のアクティビティ:** `zanieb`、`woodruffw`、`zsol` がいずれも「`project.name` は識別子として正規化される設計」「`--name` は逐語的指定ではない」と説明。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Reject locked malware installations](https://github.com/astral-sh/uv/pull/18936) `#18936`
**ステータス:** Merged | **ラベル:** enhancement, preview

**概要:** `uv sync`/`uv add` 等の同期処理時に OSV へ 1 HTTP リクエストでマルウェア (`MAL-`) チェックを行い、ヒットしたらインストールを拒否する大型機能。

**直近のアクティビティ:** Bot のテストインベントリと CodSpeed レポート。`zanieb` のコメントレビュー多数。

**変更ファイル:** `uv-audit/src/service/osv.rs` (+217/-50)、`uv-resolver/src/lock/mod.rs`、`uv-settings/` ほか 25 ファイル。

**次のアクション:** マージ済み。Preview から本番化に向けた #19519 でのドキュメント化が続く。

---

### ![Issue](icons/issue-opened.svg) [uv audit fails with error decoding response body when OSV returns empty range events](https://github.com/astral-sh/uv/issues/19492) `#19492`
**ステータス:** Closed | **ラベル:** bug, external, uv audit

**概要:** OSV `PYSEC-2026-89` が `affected.ranges.events` に空の `{}` を含み、`uv audit` のデコードが失敗する問題。

**直近のアクティビティ:** `swantzter` が「他のエントリも影響を受ける可能性がある旨を表示し、CI を落としつつ警告も出す」案を提案。OSV 側の修正が完了し `angryfoxx` がクローズ。

**次のアクション:** クローズ済み。エラーメッセージ改善は #19515 でマージ。

---

### ![PR](icons/git-pull-request.svg) [Vendor packse scenarios and serve them from an in-memory index](https://github.com/astral-sh/uv/pull/18084) `#18084`
**ステータス:** Open (Draft) | **ラベル:** testing, build:skip-release, build:skip-docker

**概要:** packse シナリオを Rust 製ジェネレータでベンダリングし、wiremock のインメモリインデックスから配信する 177 ファイル変更。packse 依存を排除。

**直近のアクティビティ:** `zanieb` の自己レビューコメント多数。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [Add support for `uv audit --fix`](https://github.com/astral-sh/uv/pull/19497) `#19497`
**ステータス:** Open (Draft) | **ラベル:** preview, uv audit

**概要:** Issue #19428 対応。`uv.lock` を minor/patch 互換の修正バージョンに更新する `--fix` フラグの MVP。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 複数脆弱性のマージ規則、ロックファイル更新ロジックの実装。

---

### ![Issue](icons/issue-opened.svg) [add pypi vulnerability service as option to uv audit](https://github.com/astral-sh/uv/issues/19505) `#19505`
**ステータス:** Open | **ラベル:** enhancement

**概要:** OSV 単独障害時にも `uv audit` を運用可能にするため、PyPI などの代替脆弱性サービスを選択肢として追加してほしい要望。

**直近のアクティビティ:** `woodruffw` が #18506 で追跡中、PyPI API の特性 (JSON 応答が大きいなど) から短期対応は躊躇すると説明。`charliermarsh` も PyPI データは OSV のサブセットでは、と確認。

**次のアクション:** #18506 で継続。

---

### ![PR](icons/git-pull-request.svg) [feat(uv-audit): introduce pypi service support](https://github.com/astral-sh/uv/pull/19507) `#19507`
**ステータス:** Closed | **ラベル:** なし

**概要:** #19505 への対応試行。PyPI を audit サービスとして使う実装。

**直近のアクティビティ:** `woodruffw` が #19505 の議論に基づき導入を見送る方針を共有。

**次のアクション:** 参考実装として保持。

---

### ![Issue](icons/issue-opened.svg) [Support a `--fix` type flag for `audit`](https://github.com/astral-sh/uv/issues/19428) `#19428`
**ステータス:** Open | **ラベル:** enhancement, uv audit

**概要:** `uv audit --fix` の要望 Issue。

**直近のアクティビティ:** `datalogics-kam` が `constraint-dependencies` を使ったロック更新案にコメント。

**次のアクション:** #19497 で実装中。

---

### ![PR](icons/git-pull-request.svg) [Allow environment variables that take a list to be empty](https://github.com/astral-sh/uv/pull/19503) `#19503`
**ステータス:** Merged | **ラベル:** bug

**概要:** 0.11.15 で `UV_NO_BINARY_PACKAGE=""` のような空リスト値がエラーになっていた問題 (#19499) を、空文字列を空リストとして許容する形で修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Validation on empty environment variables](https://github.com/astral-sh/uv/issues/19499) `#19499`
**ステータス:** Closed | **ラベル:** bug

**概要:** 上記 PR #19503 の元 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Use `UV_NO_SYSTEM_CONFIG` in tests to avoid reading machine-global config](https://github.com/astral-sh/uv/pull/19516) `#19516`
**ステータス:** Merged | **ラベル:** testing

**概要:** テストでマシン全体の設定を読み込まないように `UV_NO_SYSTEM_CONFIG` を使う。

**直近のアクティビティ:** `EliteTK` が他テストへの展開を示唆し承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [distribution-filename: fix source dist parse using normalized name length as stem offset](https://github.com/astral-sh/uv/pull/19514) `#19514`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** `SourceDistFilename::parse` が正規化済み名前のバイト長を raw ファイル名のオフセットに使っていた問題を、raw 側で名前長を計算し直すように修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [uv audit: specialize malformed OSV error](https://github.com/astral-sh/uv/pull/19515) `#19515`
**ステータス:** Merged | **ラベル:** preview, uv audit

**概要:** #19492 のエラーメッセージを利用者が理解しやすいよう専用化する修正。OSV 側の問題を可視化することを優先し、自動スキップ的な救済は行わない。

**直近のアクティビティ:** `zanieb` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Test `create_junction` changes from #19402](https://github.com/astral-sh/uv/pull/19487) `#19487`
**ステータス:** Merged | **ラベル:** internal

**概要:** Windows での SMB 共有 (`C:/uv-smb`) を `ALT_FS`/`SMB_FS` として CI に公開し、`create_junction` のテストカバレッジを拡大。

**直近のアクティビティ:** `zanieb` と `EliteTK` のコメント。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Refuse uv venv --clear on a path that is not a virtual environment](https://github.com/astral-sh/uv/pull/19511) `#19511`
**ステータス:** Closed | **ラベル:** なし

**概要:** `uv venv . --clear` がプロジェクトディレクトリを丸ごと削除する事故を防ぐため、`is_virtualenv_base` 相当のガードを `OnExisting::Remove` 経路にも追加。

**直近のアクティビティ:** `konstin` が「自動化 PR は受け付けない」(astral-sh AI ポリシー) としてクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Document uv.lock merge-conflict resolution](https://github.com/astral-sh/uv/pull/19513) `#19513`
**ステータス:** Closed | **ラベル:** なし

**概要:** `uv.lock` のマージコンフリクト解消手順をドキュメント化する PR。

**直近のアクティビティ:** `konstin` が AI ポリシーを理由にクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Document marker-platform limit on required-environments](https://github.com/astral-sh/uv/pull/19512) `#19512`
**ステータス:** Closed | **ラベル:** なし

**概要:** `required-environments` が `implied_platform_markers` の固定マッピングに依存し Windows/Linux/macOS 以外のターゲットを扱えない制約をドキュメント化する PR。

**直近のアクティビティ:** `konstin` が AI ポリシーを理由にクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Avoid unwrapping from `OnceMap`](https://github.com/astral-sh/uv/pull/19510) `#19510`
**ステータス:** Merged | **ラベル:** internal

**概要:** `OnceMap` の `register`/`wait` を統合して `unwrap` を排除する内部リファクタ。

**直近のアクティビティ:** `charliermarsh` 承認。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [`uv lock` pins single-platform wheel when upstream's release is mid-upload](https://github.com/astral-sh/uv/issues/19508) `#19508`
**ステータス:** Open | **ラベル:** bug, external

**概要:** `uv lock` が greenlet 3.5.1 のアップロード途中状態を捉え、macOS フリースレッド版だけしかない時点で固定してしまうため、後の `uv sync` で他プラットフォームから「ABI タグ不一致」と誤認される問題。

**直近のアクティビティ:** `k0pernikus` が「wheel 数が 19→1 へ急減した場合に警告を出すヒューリスティクスはどうか」と提案。`zanieb` からは一般化できる方法がなく PEP 694 を本筋とする回答があった。

**次のアクション:** PEP 694 対応待ち。当面は DX 改善案を検討。

---

### ![Issue](icons/issue-opened.svg) [How to make uv inherit global package?](https://github.com/astral-sh/uv/issues/17215) `#17215`
**ステータス:** Open | **ラベル:** question

**概要:** グローバルにインストールした PyTorch を `uv venv --system-site-packages` で継承したいが効かない、という質問。

**直近のアクティビティ:** 起票者が `--system-site-packages` でも継承できない旨を再確認。

**次のアクション:** 仕様確認と回答。

---

### ![Issue](icons/issue-opened.svg) [Write migration guides](https://github.com/astral-sh/uv/issues/5200) `#5200`
**ステータス:** Open | **ラベル:** documentation

**概要:** Poetry や pip-tools から uv への移行ガイドを書く Issue。

**直近のアクティビティ:** スパム的なコメントが投稿された。

**次のアクション:** ドキュメント執筆。

---

### ![Issue](icons/issue-opened.svg) [Debian/Ubuntu Package](https://github.com/astral-sh/uv/issues/13640) `#13640`
**ステータス:** Open | **ラベル:** なし

**概要:** `apt` で uv をインストール/更新したいという要望。

**直近のアクティビティ:** `wetneb` が `python3-uv-build` 0.9.17 が Debian unstable で利用可能になった旨を共有。

**次のアクション:** Debian パッケージング継続。

---

### ![PR](icons/git-pull-request.svg) [uv-netrc: fix multi-word no-space comment lines causing parse errors](https://github.com/astral-sh/uv/pull/19494) `#19494`
**ステータス:** Merged | **ラベル:** なし

**概要:** `#word1 word2 word3` のような `#` 直後にテキストが続くコメント行を正しくスキップできない問題を修正。Python や wget の挙動と一致。

**直近のアクティビティ:** `charliermarsh` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Cache `pyproject.toml` parsing in workspace discovery](https://github.com/astral-sh/uv/pull/19007) `#19007`
**ステータス:** Open (Draft) | **ラベル:** performance

**概要:** ワークスペース探索時の `pyproject.toml` パース結果をキャッシュする 11 ファイルの内部最適化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #18311 と整合。

---

### ![PR](icons/git-pull-request.svg) [Reject unsafe entry points in `uv-build`](https://github.com/astral-sh/uv/pull/19495) `#19495`
**ステータス:** Merged | **ラベル:** error messages

**概要:** `uv-build` のホイール生成で不正なコンソールエントリポイント (予約名や `.` のみなど) を拒否し、PyPI のバリデーションと整合させる変更。

**直近のアクティビティ:** `konstin` 承認。`zsol` がさらに「`.` のみのエントリポイント」も拒否する検証を追加した旨をコメント。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [python builds are missing AF_VSOCK](https://github.com/astral-sh/uv/issues/19491) `#19491`
**ステータス:** Open | **ラベル:** bug

**概要:** uv が配布する python-build-standalone に `socket.AF_VSOCK` が含まれず、systemd/mkosi などが動かない。

**直近のアクティビティ:** `Jannik2099` が `AF_VSOCK=40` の固定値での回避を提案。`jjhelmus` がビルドに使う Linux kernel ソースの古さが原因と説明し、ヘッダを別途含める案を検討中。

**次のアクション:** python-build-standalone 側の調整。

---

## microsoft/vscode

> Copilot/Agent Window 関連の機能追加とリリース 1.121 系のリグレッション対応が同時多発。BYOK、エージェントホスト、サンドボックスツール、CLI、Live Share、Remote SSH、ターミナル統合など広域に変更が入り、Dependabot を含む 100 件が更新された。

### ![PR](icons/git-pull-request.svg) [Fix background todo policy for current-turn activity](https://github.com/microsoft/vscode/pull/316052) `#316052`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Agent の background todo 機能で、過去ターンの未処理コンテキストが新ターンに影響しないよう、ポリシーを現ターンに限定するスコープ化。

**直近のアクティビティ:** `connor4312` 承認。

**次のアクション:** マージ準備。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Support custom third-party LLMs (BYOK) for core Agent sessions](https://github.com/microsoft/vscode/issues/317683) `#317683`
**ステータス:** Open | **ラベル:** feature-request, model-byok

**概要:** Agents Window のコア機能 (Claude Agent や長時間エージェントなど) でも BYOK のサードパーティ LLM を使いたいという要望。

**直近のアクティビティ:** `dmitrivMS` が「直近で実装されたのでは」と確認。

**次のアクション:** 実装状況の確認。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] [release/1.121] Allow sandbox writes to workspace storage](https://github.com/microsoft/vscode/pull/317281) `#317281`
**ステータス:** Open | **ラベル:** cherry-pick-artifact

**概要:** PR #317202 を 1.121 にバックポートするチェリーピック。`terminalSandboxService.ts` でコンフリクトが残っており手動解消が必要。

**直近のアクティビティ:** `connor4312` が `CHANGES_REQUESTED`。

**次のアクション:** コンフリクト解消後の再レビュー。

---

### ![Issue](icons/issue-opened.svg) [Add long context pricing to relevant models](https://github.com/microsoft/vscode/issues/317575) `#317575`
**ステータス:** Closed | **ラベル:** UBB

**概要:** モデル選択ホバーに「ロングコンテキスト」価格情報を追加して料金体系を透明化したい。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317820 で実装済み。

---

### ![PR](icons/git-pull-request.svg) [Long context pricing](https://github.com/microsoft/vscode/pull/317820) `#317820`
**ステータス:** Merged | **ラベル:** なし

**概要:** Issue #317575 の修正。長コンテキスト用の段階価格メタデータを Chat UI ホバーで表示する 19 ファイル変更。

**直近のアクティビティ:** `connor4312` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump @nevware21/ts-utils 0.11.6 → 0.14.0 in /extensions/merge-conflict](https://github.com/microsoft/vscode/pull/317864) `#317864`
**ステータス:** Open | **ラベル:** javascript, dependencies

**概要:** Dependabot による依存更新。

**直近のアクティビティ:** Copilot レビュアー Bot のエラー。

**次のアクション:** CI 後マージ。

---

### ![PR](icons/git-pull-request.svg) [Add browser emulation toolbar](https://github.com/microsoft/vscode/pull/317831) `#317831`
**ステータス:** Merged | **ラベル:** なし

**概要:** 内蔵ブラウザにデバイスエミュレーションツールバーを追加し、ビューポート、DPR、ユーザーエージェント、モバイル切り替えを行えるようにする 11 ファイル +1459 行。

**直近のアクティビティ:** `dmitrivMS` が一度 dismiss した後に最終承認。

**変更ファイル:** `browserView/electron-main/browserViewEmulator.ts` (+112)、`browserEditorEmulationFeatures.ts` (+1004) ほか 11 ファイル。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: add a confirm step to Restore Checkpoint](https://github.com/microsoft/vscode/issues/317861) `#317861`
**ステータス:** Open | **ラベル:** なし

**概要:** Restore Checkpoint を誤クリックして作業を取り消してしまわないよう、確認ダイアログを入れたいという要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Scope tool search to request tools and tighten deferred tool handling](https://github.com/microsoft/vscode/pull/317707) `#317707`
**ステータス:** Open | **ラベル:** なし

**概要:** リクエスト単位の "tool manifest" を導入し、deferred ツールの分類を整合化。`ToolSearchTool` を request-scoped スナップショットに揃え、Anthropic の仮想 `activate_*` グループ復活なども含む 28 ファイル変更。

**直近のアクティビティ:** 作者が GPT/Claude 双方で動作確認済みと報告。Copilot Reviewer Bot が 5 件の指摘。

**次のアクション:** 指摘反映後のマージ。

---

### ![Issue](icons/issue-opened.svg) [PWSH Terminal Integration startup failure](https://github.com/microsoft/vscode/issues/299703) `#299703`
**ステータス:** Closed | **ラベル:** bug, install-update, windows, regression, insiders-released, terminal-shell-pwsh

**概要:** PowerShell プロファイル経由の VS Code シェル統合が起動失敗する古い回帰バグ。

**直近のアクティビティ:** `silkfire` が 1.121.0 でも未解決と報告。`BernhardMarconato` は手動の `& "$(code --locate-shell-integration-path pwsh)"` で回避できる旨を共有し、`dmitrivMS` は PATH 重複の確認を依頼した。

**次のアクション:** クローズ済み。新規 Issue で再起票が必要。

---

### ![Issue](icons/issue-opened.svg) [Missing Bearer token error when connecting to MCP behind OAuth](https://github.com/microsoft/vscode/issues/311305) `#311305`
**ステータス:** Open | **ラベル:** なし

**概要:** OAuth 保護下の MCP に接続するとき Bearer トークンが付かず 401 になる。

**直近のアクティビティ:** `kdwink` が初回認可リクエストが scopes パラメータを送らずデフォルトスコープにフォールバックしている、と原因仮説を提示。

**次のアクション:** `extHostAuthentication.ts` の scopes 処理確認。

---

### ![Issue](icons/issue-opened.svg) [allowAnonymousAccess requires restart but doesn't tell the user](https://github.com/microsoft/vscode/issues/317786) `#317786`
**ステータス:** Open | **ラベル:** model-byok

**概要:** `chat.allowAnonymousAccess` を有効化してもウィンドウ再読み込みが必要だが UI 上で通知されない。

**直近のアクティビティ:** `dmitrivMS` が期待挙動を確認。

**次のアクション:** UX 改善検討。

---

### ![PR](icons/git-pull-request.svg) [Fix entitlements rendering incorrectly](https://github.com/microsoft/vscode/pull/317857) `#317857`
**ステータス:** Merged | **ラベル:** なし

**概要:** 安全な整数ではないが有限な数値クォータスナップショットの描画問題を修正。

**直近のアクティビティ:** `anthonykim1` 承認。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Inline Suggestions completely block IntelliSense suggestions](https://github.com/microsoft/vscode/issues/317863) `#317863`
**ステータス:** Open | **ラベル:** new release

**概要:** 1.121 リリース以後、Inline Suggestions を無効化しないと IntelliSense が表示されないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [agent-host: stop config pickers flashing during resolveSessionConfig](https://github.com/microsoft/vscode/pull/317856) `#317856`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host で session-config の picker を変更した際、`resolveSessionConfig` の往復中に既存のスキーマが空に置換されて picker が点滅する問題を修正。

**直近のアクティビティ:** `anthonykim1` が一度 dismiss。

**次のアクション:** 再レビュー。

---

### ![Issue](icons/issue-opened.svg) [Use copilot with local models completely offline](https://github.com/microsoft/vscode/issues/246551) `#246551`
**ステータス:** Closed | **ラベル:** feature-request, verification-needed, insiders-released, model-byok

**概要:** Ollama 等のローカルモデルで Copilot を GitHub ログインなしに使いたい要望。

**直近のアクティビティ:** `digitarald` が Insiders で実装されテスト可能になった旨を告知。`dmitrivMS` は Embeddings について別モデル要件のため未対応と回答。

**次のアクション:** Insiders での検証。

---

### ![PR](icons/git-pull-request.svg) [fix(chat): enhance language model reset logic for CopilotCLI session type](https://github.com/microsoft/vscode/pull/317859) `#317859`
**ステータス:** Open | **ラベル:** なし

**概要:** チャット入力部の言語モデルリセット時に CopilotCLI セッション向けの追加処理を入れる修正。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [copilot: truncate long lines in read_file results](https://github.com/microsoft/vscode/pull/317862) `#317862`
**ステータス:** Open | **ラベル:** なし

**概要:** `read_file` ツールが超長文を返すケースで、prompt-tsx が prune した結果エージェントが「結果が空」と誤認して同ファイルを何度も再取得するループが発生し、セッションが 450MB+ に膨張した事例への修正。長い行をツール側でトランケートする。

**直近のアクティビティ:** Copilot レビュアー Bot のエラー。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Cloud agents: preserve archive state across v1↔v2 backend flip](https://github.com/microsoft/vscode/pull/317860) `#317860`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Cloud Agent バックエンドが Jobs API (v1) と Task API (v2) で切り替わると同じ PR セッションの URI が変わってアーカイブ状態が失われる問題を、PR 解決可能時は常に `/<prNumber>` を発行する形で安定化。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** ドラフト解除。

---

### ![Issue](icons/issue-opened.svg) [Remote-SSH Extension Host Keeps Crashing after latest release](https://github.com/microsoft/vscode/issues/317596) `#317596`
**ステータス:** Closed | **ラベル:** *duplicate, new release

**概要:** 1.121.0 で Remote-SSH 接続後にエクステンションホストが頻繁にクラッシュする。

**直近のアクティビティ:** 4 件の同症状報告。`roblourens` が #317714 と同件と特定。

**次のアクション:** #317714 で対応。

---

### ![PR](icons/git-pull-request.svg) [Add GHCP telemetry correlation fields for tool responses and response turns](https://github.com/microsoft/vscode/pull/317855) `#317855`
**ステータス:** Open | **ラベル:** なし

**概要:** `agent.tool.responseLength` テレメトリに相関フィールドを追加し、`response.success` で現在のターンを記録する。

**直近のアクティビティ:** Copilot レビュアー Bot がライセンス未設定エラーで停止。`connor4312` が承認。

**次のアクション:** マージ準備。

---

### ![Issue](icons/issue-opened.svg) [Remote SSH in Agent Window is not working?](https://github.com/microsoft/vscode/issues/317549) `#317549`
**ステータス:** Open | **ラベル:** bug, info-needed, agent-host, new release

**概要:** Agent Window で Remote SSH をクリックしても反応せず、`+` ボタンもローカルパスを指してしまう。

**直近のアクティビティ:** `roblourens` が出力チャンネルのエラー有無を確認。

**次のアクション:** ログ取得待ち。

---

### ![Issue](icons/issue-opened.svg) [`vsce publish` validation doesn't check engine vs `@types/vscode` dependency correctly](https://github.com/microsoft/vscode/issues/316431) `#316431`
**ステータス:** Open | **ラベル:** triage-needed, stale

**概要:** `vsce` のバージョン検証が `engines.vscode` よりも新しい `@types/vscode` を許容するなど誤判定する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in storageMainService](https://github.com/microsoft/vscode/pull/317841) `#317841`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** `storageMainService` でウィンドウ読み込み時に初期化されたプロファイル/ワークスペースストレージが破棄されないリークを修正。

**直近のアクティビティ:** 作者が自己レビューコメント多数。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in ipc.electron.ts](https://github.com/microsoft/vscode/pull/317846) `#317846`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** クライアント切断時に `Server.Clients` からエントリが削除されないリークを修正。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Remote `ssh` connection fails from Debian -> MacOS](https://github.com/microsoft/vscode/issues/317714) `#317714`
**ステータス:** Open | **ラベル:** bug, important, ssh, new release

**概要:** 1.121 リリース以降 Debian 11 から SSH リモートが繋がらなくなった。Windows/WSL → Ubuntu pod でも同症状、1.120 へのダウングレードで回避可能。

**直近のアクティビティ:** `roblourens` が `remote.SSH.useExecServer: false` を回避策として提示。複数ユーザーが追加で同症状を報告。`$PLATFORM` 未定義警告も観測されている。

**次のアクション:** Exec Server 経路の修正。

---

### ![Issue](icons/issue-opened.svg) [Add `Assisted-by` instead of `Co-authored-by` for AI agents](https://github.com/microsoft/vscode/issues/313962) `#313962`
**ステータス:** Open | **ラベル:** feature-request, git

**概要:** Copilot 用に追加された `Co-authored-by` を AI 用途と区別するため、`Assisted-by` 等に変更してほしい要望。

**直近のアクティビティ:** `0zitro` が「人と共同制作した時に使うタグなので、AI 名義は宣伝になる」と強く主張。

**次のアクション:** ポリシー検討。

---

### ![Issue](icons/issue-opened.svg) [No tooltips in agents window sessions list](https://github.com/microsoft/vscode/issues/317858) `#317858`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents Window のセッション一覧でフォルダーパスやセッションタイトルがホバーでツールチップ表示されない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Browser: support element selection in subframes](https://github.com/microsoft/vscode/pull/317405) `#317405`
**ステータス:** Merged | **ラベル:** なし

**概要:** 内蔵ブラウザの要素検査をクロスオリジン iframe (OOPIF) でも動作するように、フレーム認識インスペクタへ刷新。Issue #317137 を fix。

**直近のアクティビティ:** `dmitrivMS` 承認。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Can't add content from iframe in integrated browser to chat](https://github.com/microsoft/vscode/issues/317137) `#317137`
**ステータス:** Closed | **ラベル:** bug, unreleased, browser-integration

**概要:** PR #317405 の元 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Security: minimumReleaseAge setting for mitigating supply chain attacks on extensions](https://github.com/microsoft/vscode/issues/316867) `#316867`
**ステータス:** Open | **ラベル:** feature-request, extensions

**概要:** サプライチェーン攻撃対策として、公開後一定日数経過した拡張機能だけインストール/更新できる `minimumReleaseAge` 設定を追加してほしい要望。

**直近のアクティビティ:** `seanamos` が当面 `extensions.autoUpdate: false` しか対策がないと共有。MDM/GPO で制御できないこと、機能リリース停止すべきという意見も。

**次のアクション:** Issue #317830 と統合検討。

---

### ![Issue](icons/issue-opened.svg) [Claude Opus4.6 can't even write a 40 character html file](https://github.com/microsoft/vscode/issues/317852) `#317852`
**ステータス:** Open | **ラベル:** info-needed

**概要:** Claude Opus 4.6 がファイル書き込みに失敗するという報告。

**直近のアクティビティ:** Bot が情報不足と古いバージョンを指摘。

**次のアクション:** 情報待ち。

---

### ![PR](icons/git-pull-request.svg) [Bump Agent host Copilot CLI to 1.0.49](https://github.com/microsoft/vscode/pull/317241) `#317241`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent host / non-agent host の Copilot CLI を 1.0.49 にバンプ。`buildCopolotChatToken`/`getCopilotSmokeTestEnv` 共通化。

**直近のアクティビティ:** `rzhao271`、`deepak1556`、`anthonykim1` のコメント。

**次のアクション:** スモークテスト再実施。

---

### ![Issue](icons/issue-opened.svg) [Bad hiding behavior of pickers in toolbar](https://github.com/microsoft/vscode/issues/317345) `#317345`
**ステータス:** Open | **ラベル:** なし

**概要:** Claude Agent Host を選んだ状態でサイドバーをリサイズすると permission picker が早期に隠れる。

**直近のアクティビティ:** `benibenj` が picker 表示自体ができていないため後で再調査と回答。

**次のアクション:** 再現環境整備。

---

### ![PR](icons/git-pull-request.svg) [feat(editor): Chrome-like grouped tabs](https://github.com/microsoft/vscode/pull/315366) `#315366`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code のマルチエディタタブバーに Chrome 風のグループ化機能を追加 (Issue #100335)。色付きヘッダ、ドラッグ＆ドロップ、コンテキストメニュー対応など、11 ファイル +1635 行。

**直近のアクティビティ:** `mostafa` が `benibenj` に進捗確認。Copilot レビュアー Bot が複数回コメント。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Hide anonymous access setting from settings ui](https://github.com/microsoft/vscode/pull/317854) `#317854`
**ステータス:** Open | **ラベル:** なし

**概要:** `chat.allowAnonymousAccess` を `included: false` に変更し設定 UI から隠す。

**直近のアクティビティ:** `rzhao271` 承認。

**次のアクション:** マージ準備。

---

### ![Issue](icons/issue-opened.svg) [Status dashboard styling](https://github.com/microsoft/vscode/issues/317853) `#317853`
**ステータス:** Open | **ラベル:** polish

**概要:** ステータスダッシュボードの「codebase semantic indexing」「session sync」セクションが冗長で折りたたみもできない、というデザイン指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** デザイン調整。

---

### ![PR](icons/git-pull-request.svg) [itemProvider.provideCustomAgents to get agents from SessionCustomization.agents](https://github.com/microsoft/vscode/pull/317848) `#317848`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host セッション用のカスタムエージェントを `ICustomizationItemProvider.provideCustomAgents` 経由で提供できるよう API 追加。

**直近のアクティビティ:** `DonJayamanne` が一度 dismiss。

**次のアクション:** 再レビュー。

---

### ![PR](icons/git-pull-request.svg) [Enhance feedback widget behavior with auto-hide logic](https://github.com/microsoft/vscode/pull/317850) `#317850`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent フィードバックウィジェットが入力中に消えないよう、テキストがあれば auto-hide を抑止する。Issue #314909 を close。

**直近のアクティビティ:** `connor4312` 承認。

**次のアクション:** マージ準備。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot Chat fails with thinking or redacted_thinking blocks cannot be modified error](https://github.com/microsoft/vscode/issues/317849) `#317849`
**ステータス:** Closed | **ラベル:** new release

**概要:** Copilot Chat の会話継続で Anthropic API から 400 が返るエラー。

**直近のアクティビティ:** Bot が #253136 の重複としてクローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Chronicle: Add cost-tips command](https://github.com/microsoft/vscode/pull/317809) `#317809`
**ステータス:** Merged | **ラベル:** なし

**概要:** Chronicle に `cost-tips` コマンドを追加し、セッション履歴からトークン使用と Copilot コストの削減策を提案する。

**直近のアクティビティ:** `vijayupadya` が `digitarald` にメンション、Copilot レビュアー Bot が 2 件指摘。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [TitleBarAccountWidget leak](https://github.com/microsoft/vscode/issues/317851) `#317851`
**ステータス:** Open | **ラベル:** なし

**概要:** `TitleBarAccountWidget` まわりで `FunctionDisposable` が解放されないリーク報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** リーク追跡。

---

### ![Issue](icons/issue-opened.svg) [Agents Window unable to start a session with Copilot CLI](https://github.com/microsoft/vscode/issues/316511) `#316511`
**ステータス:** Open | **ラベル:** なし

**概要:** ghe.com 利用環境で VS Code 1.121.0 + Copilot CLI 1.0.51 の組合せで Agents Window セッションが開始できない。

**直近のアクティビティ:** `JohanLindqvistR`、`ArduousKnight` が同症状を報告。

**次のアクション:** GHE 環境固有の調査。

---

### ![PR](icons/git-pull-request.svg) [Proxy browser requests in remote workspaces](https://github.com/microsoft/vscode/pull/315843) `#315843`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** リモートワークスペースで内蔵ブラウザのネットワークリクエストをリモート接続経由でプロキシする 25 ファイル変更。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** ドラフト解除。

---

### ![Issue](icons/issue-opened.svg) [Menu bar goes greyed out](https://github.com/microsoft/vscode/issues/308635) `#308635`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** メニューバーがグレーアウトして反応しなくなる古いバグ。

**直近のアクティビティ:** `mdb-rad` が 1.121 で発生したと報告。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump uuid, @azure/cosmos, @azure/identity and @azure/msal-node in /build](https://github.com/microsoft/vscode/pull/317843) `#317843`
**ステータス:** Open | **ラベル:** javascript, dependencies

**概要:** Dependabot による Azure SDK 系の大規模アップデート。

**直近のアクティビティ:** Copilot レビュアー Bot のエラー。

**次のアクション:** CI 後マージ。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot - #changes variable no longer available in chat?](https://github.com/microsoft/vscode/issues/316240) `#316240`
**ステータス:** Open | **ラベル:** なし

**概要:** チャットで `#changes` 変数が使えなくなったという報告。

**直近のアクティビティ:** `markm77` が 1.121 でも依然問題と報告。`devned` は最新更新で復活したと共有。`AxelMolina1` は依然使えないと再報告。

**次のアクション:** 環境差分の調査。

---

### ![Issue](icons/issue-opened.svg) [[chat agent sandbox] .NET telemetry `deviceid` and other `~`-relative files written into workspace root on macOS](https://github.com/microsoft/vscode/issues/317636) `#317636`
**ステータス:** Open | **ラベル:** なし

**概要:** chat agent サンドボックスを macOS で有効化すると、`~` 展開がホームではなくワークスペースルートを指し、.NET の `deviceid` などが書き込まれてしまう。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** サンドボックスのホーム解決見直し。

---

### ![PR](icons/git-pull-request.svg) [Show a warning when showing the rendered markdown diff](https://github.com/microsoft/vscode/pull/317829) `#317829`
**ステータス:** Merged | **ラベル:** なし

**概要:** レンダリング済みマークダウン差分で変更が隠れる可能性を警告で明示する。

**直近のアクティビティ:** `vijayupadya` 承認。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [GitHub Copilot stopping GPT and resubmitting prompt shows ongoing mid-response output](https://github.com/microsoft/vscode/issues/317845) `#317845`
**ステータス:** Open | **ラベル:** なし

**概要:** GPT-5.5 で生成中にキャンセル→編集→再送信すると、前の応答の途中から続いて出力されてしまう。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![PR](icons/git-pull-request.svg) [Test: withCustomShellTool](https://github.com/microsoft/vscode/pull/317833) `#317833`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタムシェルツール有効時の eval 確認用テスト PR (マージしない)。

**直近のアクティビティ:** Bot のビルド/評価キュー通知。

**次のアクション:** 評価結果取得のみ。

---

### ![PR](icons/git-pull-request.svg) [Test: noCustomShellTool](https://github.com/microsoft/vscode/pull/317834) `#317834`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** カスタムシェルツール無効時の eval 確認用テスト PR (マージしない)。

**直近のアクティビティ:** Bot のビルド/評価キュー通知。

**次のアクション:** 評価結果取得のみ。

---

### ![Issue](icons/issue-opened.svg) [Fetch tool broken in sandbox with no clear way to know it's because sandbox](https://github.com/microsoft/vscode/issues/316624) `#316624`
**ステータス:** Closed | **ラベル:** unreleased, agent-sandbox

**概要:** サンドボックス有効時に Fetch ツールが壊れているのに、原因がサンドボックスだと分からない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #317199 でリバート完了。

---

### ![PR](icons/git-pull-request.svg) [[cherry-pick] Reverting fetch web tool changes for sandboxing](https://github.com/microsoft/vscode/pull/317199) `#317199`
**ステータス:** Merged | **ラベル:** cherry-pick-artifact

**概要:** 1.121 系へのチェリーピック。Issue #316624 を fix。

**直近のアクティビティ:** `dileepyavan`、`bryanchen-d` 承認。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Should we allow switching folder branch when there are no uncommitted changes and no active session](https://github.com/microsoft/vscode/issues/317844) `#317844`
**ステータス:** Open | **ラベル:** agents-window

**概要:** フォルダー選択中にブランチ切替を許容するかどうかの仕様議論。

**直近のアクティビティ:** `benibenj` が「やはり複雑なので一旦クローズで構わない」とセルフコメント。

**次のアクション:** クローズ判断。

---

### ![Issue](icons/issue-opened.svg) [Visual Studio Live Share fails to start or join collaboration session in VS Code 1.121.0](https://github.com/microsoft/vscode/issues/317842) `#317842`
**ステータス:** Open | **ラベル:** new release

**概要:** Live Share 1.1.122 で 1.121.0 上のセッション開始/参加が失敗する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Allow local models in Agents mode](https://github.com/microsoft/vscode/issues/317591) `#317591`
**ステータス:** Open | **ラベル:** なし

**概要:** 通常チャットと同じく、Agents モードでもローカルモデルを選択できるようにしてほしい要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Custom Endpoint BYOK provider sends wrong auth header for Azure OpenAI-compatible endpoints behind APIM](https://github.com/microsoft/vscode/issues/317810) `#317810`
**ステータス:** Open | **ラベル:** model-byok

**概要:** Custom Endpoint BYOK が `Authorization` と `X-API-Key` の両ヘッダを送るため、DeepSeek など `X-API-Key` のみを要求する API で問題が起きる。

**直近のアクティビティ:** `psycho-neon` が DeepSeek でも同症状と共有。

**次のアクション:** プロバイダ別ヘッダ制御の改修。

---

### ![PR](icons/git-pull-request.svg) [Integrating MXC for windows sandboxing](https://github.com/microsoft/vscode/pull/317669) `#317669`
**ステータス:** Merged | **ラベル:** なし

**概要:** Windows サンドボックスに `@microsoft/mxc-sdk` を統合し、`wxc-exec` 経由で Windows コンテナを実行可能に。20 ファイル +750 行。ネットワーク分離や環境変数注入は未実装。

**直近のアクティビティ:** `vijayupadya` 承認。

**次のアクション:** ネットワーク分離は後続対応。

---

### ![Issue](icons/issue-opened.svg) [BYOK Azure Foundry /responses fails with invalid input type](https://github.com/microsoft/vscode/issues/317534) `#317534`
**ステータス:** Open | **ラベル:** bug, model-byok

**概要:** Azure Foundry の `/responses` エンドポイントを BYOK で使うと `input[n].type` 検証エラー。

**直近のアクティビティ:** `cf-rdegregory` が Azure OpenAI Endpoint を使うべきと案内。

**次のアクション:** ドキュメント補強。

---

### ![Issue](icons/issue-opened.svg) [Enable sandboxing in windows using MXC SDK](https://github.com/microsoft/vscode/issues/317711) `#317711`
**ステータス:** Closed | **ラベル:** unreleased, agent-sandbox

**概要:** PR #317669 の元 Issue。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Live share not working session start failed with code 503 or 500](https://github.com/microsoft/vscode/issues/317838) `#317838`
**ステータス:** Open | **ラベル:** info-needed

**概要:** Live Share でセッション開始時に 503/500 エラーが返るという報告。

**直近のアクティビティ:** Bot が情報不足と指摘。

**次のアクション:** 情報待ち。

---

### ![Issue](icons/issue-opened.svg) [Agents: Isolation picker may be switching to folder even when a user is using work trees](https://github.com/microsoft/vscode/issues/317840) `#317840`
**ステータス:** Open | **ラベル:** agents-window

**概要:** Isolation picker が利用者の意図に反して `Folder` に切り替わる不具合の可能性。`Open in Agents` ボタンが値を上書きしている疑い。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現条件の特定。

---

### ![Issue](icons/issue-opened.svg) [Text search within GitHub Copilot Chat Window](https://github.com/microsoft/vscode/issues/309012) `#309012`
**ステータス:** Open | **ラベル:** feature-request

**概要:** Copilot Chat のウィンドウ内 Ctrl+F 検索の要望。

**直近のアクティビティ:** `JacobRochford` が進捗の可視化を強く求める。`justschen` はバックログ入りしたが ETA はないと回答。

**次のアクション:** バックログ。

---

### ![PR](icons/git-pull-request.svg) [feat(debug): add 'Copy All' action to Watch view context menu](https://github.com/microsoft/vscode/pull/307886) `#307886`
**ステータス:** Merged | **ラベル:** なし

**概要:** Watch ビューに「Copy All」アクションを追加し、`name: value` の形式で 1 行ずつコピーできるようにする。Issue #306116 を fix。

**直近のアクティビティ:** `connor4312` と `Yoyokrazy` 承認。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Add "Copy All" or "Copy as Key-Value Pairs" to Watch Window](https://github.com/microsoft/vscode/issues/306116) `#306116`
**ステータス:** Closed | **ラベル:** feature-request, debug, unreleased

**概要:** PR #307886 の元 Issue。

**直近のアクティビティ:** Bot が「20 票に満たないが、機能リクエストとして許容」のテンプレコメント。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [creating a fork of an AHP chat will create duplicate session, and none get the `forked:` prefix](https://github.com/microsoft/vscode/issues/317839) `#317839`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host ペア (AHP) のチャットを fork すると、同タイトルのセッションが 4 つ出来て `forked:` プレフィックスも付かない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [bug report](https://github.com/microsoft/vscode/issues/317836) `#317836`
**ステータス:** Closed | **ラベル:** spam

**概要:** ガイドラインに沿わないため Bot がスパム扱いでクローズ。

**直近のアクティビティ:** Bot のテンプレ応答のみ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat OTLP metrics do not expose cached input token usage](https://github.com/microsoft/vscode/issues/317837) `#317837`
**ステータス:** Open | **ラベル:** new release

**概要:** OTLP テレメトリにキャッシュされた入力トークン使用量が含まれないという報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** テレメトリ仕様の確認。

---

### ![Issue](icons/issue-opened.svg) [Problem to see mermaid](https://github.com/microsoft/vscode/issues/317717) `#317717`
**ステータス:** Open | **ラベル:** info-needed, new release

**概要:** 1.121 で Markdown プレビューの mermaid 図が描画されなくなったという報告。

**直近のアクティビティ:** `bmurphy1976` も同症状。`mjbvz` が Insiders で再現確認を依頼。

**次のアクション:** 情報待ち。

---

### ![Issue](icons/issue-opened.svg) [add inline browser viewer for php files](https://github.com/microsoft/vscode/issues/317835) `#317835`
**ステータス:** Open | **ラベル:** なし

**概要:** PHP ファイル向けにもインラインブラウザビューアを追加してほしい要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [Agents: Don't attempt to start a terminal when opening a Done/worktree session](https://github.com/microsoft/vscode/issues/317832) `#317832`
**ステータス:** Open | **ラベル:** agents-window

**概要:** Done 状態の worktree セッションを開くと、削除済み CWD でターミナル起動を試みて失敗ダイアログが出る。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** worktree 削除済みケースのショートサーキット実装。

---

### ![Issue](icons/issue-opened.svg) [Windows cursor sometimes becomes invisible in VS Code](https://github.com/microsoft/vscode/issues/317757) `#317757`
**ステータス:** Open | **ラベル:** info-needed, triage-needed

**概要:** Windows でカーソルが見えなくなる既知症状。

**直近のアクティビティ:** `ntysdd` が Issue #217357 と同件と推測。`connor4312` が GIF の提供を依頼。

**次のアクション:** 動画資料待ち。

---

### ![Issue](icons/issue-opened.svg) [Agent window: other modes should be disabled in sub agent if it is not supported](https://github.com/microsoft/vscode/issues/317827) `#317827`
**ステータス:** Open | **ラベル:** bug, copilot-cli-agent, agents-window

**概要:** Copilot CLI のサブエージェントセッションで Local や Claude を選んでも動かない場合、ドロップダウンから外すか無効化すべきという指摘。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UI 制御の修正。

---

### ![Issue](icons/issue-opened.svg) [Feature request: minimum release age policy for extension installs and updates](https://github.com/microsoft/vscode/issues/317830) `#317830`
**ステータス:** Open | **ラベル:** なし

**概要:** Issue #316867 と同趣旨。拡張機能の最小リリース経過日数ポリシーを企業ポリシーで管理可能にしてほしい要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #316867 と統合検討。

---

### ![PR](icons/git-pull-request.svg) [worktrees: add command-center switcher and per-worktree state management](https://github.com/microsoft/vscode/pull/317806) `#317806`
**ステータス:** Open | **ラベル:** なし

**概要:** Agents アプリの worktree 切替体験を通常のワークベンチに持ち込み、コマンドセンターにスイッチャー UI を追加する 11 ファイル +1381 行。

**直近のアクティビティ:** 作者が CLA 同意。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Render chat attachments as request tiles](https://github.com/microsoft/vscode/pull/317817) `#317817`
**ステータス:** Open | **ラベル:** なし

**概要:** 明示的なファイル/画像添付を、ピルや要約テキストではなく、ユーザーメッセージの上にスタンドアロンタイルとして表示する変更。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [Request Failed: 400](https://github.com/microsoft/vscode/issues/306958) `#306958`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot Chat で 400 (画像サイズ超過) エラー。

**直近のアクティビティ:** `jdschuitemaker` が Auto モード時に Claude モデルでのみ発生すると報告。

**次のアクション:** モデル振り分けロジック確認。

---

### ![Issue](icons/issue-opened.svg) [Inconsistency in 1M context size strings](https://github.com/microsoft/vscode/issues/317828) `#317828`
**ステータス:** Open | **ラベル:** bug

**概要:** 1M コンテキストサイズの表記が `1M` と `1000K` で混在しているので統一すべき。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UI 表記統一。

---

### ![PR](icons/git-pull-request.svg) [version-control: respect push remotes with 'sync changes'](https://github.com/microsoft/vscode/pull/293100) `#293100`
**ステータス:** Open | **ラベル:** なし

**概要:** `git config remote.pushDefault` と `--set-upstream-to` を併用するトライアングルフローを Sync changes で尊重するよう拡張。

**直近のアクティビティ:** 作者が `bpasero` らに再度レビュー依頼。

**次のアクション:** メンテナレビュー。

---

### ![Issue](icons/issue-opened.svg) [stale typescript errors after editing](https://github.com/microsoft/vscode/issues/317522) `#317522`
**ステータス:** Open | **ラベル:** bug, important, typescript

**概要:** vscode リポジトリ作業中に古い TypeScript エラーが残り続ける。

**直近のアクティビティ:** `aeschli` が typescript-native LSP 出力には該当エラーがないと指摘し、クライアント側がクリアできていない可能性を提示。`DanielRosenwasser` が完全なサーバーログを求める。

**次のアクション:** ログ取得継続。

---

### ![Issue](icons/issue-opened.svg) [Inconsistency in cost related strings](https://github.com/microsoft/vscode/issues/317826) `#317826`
**ステータス:** Open | **ラベル:** bug

**概要:** モデルメニューとモデルホバーで cost 表記の順序や文言が異なる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** UI 文言統一。

---

### ![PR](icons/git-pull-request.svg) [Enhance session customization handling and simplify sync logic](https://github.com/microsoft/vscode/pull/317700) `#317700`
**ステータス:** Merged | **ラベル:** なし

**概要:** Agent Host のセッションカスタマイズがセッション作成直後から利用可能になるよう、同期ロジックを改善。

**直近のアクティビティ:** Copilot レビュアー Bot が複数コメント、`connor4312` のコメント。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [cli: skip bare top-level directory entries when extracting archives](https://github.com/microsoft/vscode/pull/317792) `#317792`
**ステータス:** Merged | **ラベル:** なし

**概要:** PR #317393 によって Remote-SSH サーバインストールが「extraction path resolves outside root」で失敗していた回帰を、空エントリを安全に扱う形で修正。

**直近のアクティビティ:** `rzhao271` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [refactor and add tests for agentCustomizationContentExpander](https://github.com/microsoft/vscode/pull/317773) `#317773`
**ステータス:** Open | **ラベル:** なし

**概要:** `agentCustomizationContentExpander.ts` の抽出とテスト追加。

**直近のアクティビティ:** `roblourens` が一度 dismiss。

**次のアクション:** 再レビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(copilot): opt-in HTTP cache for the Node fetch fetcher](https://github.com/microsoft/vscode/pull/317721) `#317721`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot の Node fetch 経路に opt-in の HTTP キャッシュ (undici interceptor) を追加。プロキシや CA 注入とも共存。

**直近のアクティビティ:** `roblourens` 承認、`chrmarti` も LGTM。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Revert: Use copilot/copilot-utility-small for terminal-tool steering messages](https://github.com/microsoft/vscode/pull/317812) `#317812`
**ステータス:** Merged | **ラベル:** なし

**概要:** PR #317389 のリバート。ターミナルツールのステアリングメッセージを `copilot/copilot-utility-small` に流す変更を取り消す。

**直近のアクティビティ:** `rzhao271` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Handle tap activation for markdown links on mobile](https://github.com/microsoft/vscode/pull/317815) `#317815`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Agents のマークダウンリンクがタッチデバイスで開けない問題を、共通の markdown renderer 経路でタップ起動を扱うように修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [Exclude Enterprise FSI Users from Proxy](https://github.com/microsoft/vscode/pull/317193) `#317193`
**ステータス:** Closed (Draft) | **ラベル:** なし

**概要:** カスタムモデルが許可リストにないとき、デフォルトのメインエージェントモデルを検索サブエージェントに使う変更。

**直近のアクティビティ:** Copilot レビュアー Bot のエラー。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [build: persist ADO buildId/definitionId on published build records](https://github.com/microsoft/vscode/pull/317803) `#317803`
**ステータス:** Merged | **ラベル:** なし

**概要:** Cosmos DB `builds` コンテナの各ビルドドキュメントに ADO の `Build.BuildId`/`System.DefinitionId` を保存し、リリースダッシュボードから ADO ランへ遡れるようにする。

**直近のアクティビティ:** `vritant24` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Allow sending chat requests with attachments only](https://github.com/microsoft/vscode/pull/317776) `#317776`
**ステータス:** Open | **ラベル:** なし

**概要:** テキスト未入力でも添付のみのチャットリクエスト送信を可能にする変更。Issue #317775 を fix。

**直近のアクティビティ:** スクリーンショット差分 Bot が 20 件の変更を報告。作者が UI 文言改善を検討中とコメント。`dbaeumer` が一度 dismiss。

**次のアクション:** UI 文言調整と再レビュー。

---

### ![PR](icons/git-pull-request.svg) [Smoke test for Copilot CLI via chat extension in editor window](https://github.com/microsoft/vscode/pull/317140) `#317140`
**ステータス:** Open | **ラベル:** なし

**概要:** Copilot CLI を `workbench.action.chat.openNewSessionEditor.copilotcli` 経由でエディタウィンドウから開き、モック LLM 応答までを検証するスモークテスト。

**直近のアクティビティ:** `anthonykim1` がトークン構築ヘルパーの共通化を共有、`sandy081` が関連 PR #317545 を案内。`connor4312` 承認。

**次のアクション:** マージ準備。

---

### ![PR](icons/git-pull-request.svg) [FIx: show error message when running npm install without open workspace](https://github.com/microsoft/vscode/pull/317657) `#317657`
**ステータス:** Open | **ラベル:** なし

**概要:** ワークスペース未オープン時の `_runNpmInstall()` 無音失敗にエラーメッセージを表示する 1 行修正。

**直近のアクティビティ:** `mjbvz`、`roblourens` 承認。

**次のアクション:** マージ準備。

---

### ![PR](icons/git-pull-request.svg) [Add code-oss-dev skill](https://github.com/microsoft/vscode/pull/317811) `#317811`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** Code OSS の開発ビルドを使い捨てプロファイルとデバッグポート割り当てで起動する Copilot Skill (`code-oss-dev`) を追加する。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [More robust css rules for issue reporter](https://github.com/microsoft/vscode/pull/317804) `#317804`
**ステータス:** Merged | **ラベル:** なし

**概要:** Issue Reporter オーバーレイの CSS スコーピングを強化し、`.spacer` などの汎用セレクタや衝突しやすいアニメーション名を排除。

**直近のアクティビティ:** `roblourens` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: guard _resize against disposed _resizeDebouncer (fixes #317787)](https://github.com/microsoft/vscode/pull/317791) `#317791`
**ステータス:** Merged | **ラベル:** errors-fix, *error-fix-driving

**概要:** ターミナル teardown の最中に configuration change イベントが発火し、破棄済み `_resizeDebouncer` を参照してクラッシュする問題を修正。

**直近のアクティビティ:** `vijayupadya` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Make CI artifacts reproducible](https://github.com/microsoft/vscode/pull/317601) `#317601`
**ステータス:** Open (Draft) | **ラベル:** なし

**概要:** CI のアーカイブ生成を `reproducible-tar.sh`/`reproducible-zip.sh` 等で正規化し、同一 SHA から生成される成果物のバイト一致を目指す 16 ファイル変更。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** ドラフト解除。

---

### ![PR](icons/git-pull-request.svg) [fix(terminal): catch ENOPRO in getCwdResource when file:// provider absent in VS Code web](https://github.com/microsoft/vscode/pull/317780) `#317780`
**ステータス:** Open | **ラベル:** なし

**概要:** VS Code Server (Web) で `getCwdResource()` が `ENOPRO` (No file system provider found) で例外を投げ、Copilot ターミナルツールが毎回失敗する問題を修正。

**直近のアクティビティ:** Copilot レビュアー Bot のコメント。

**次のアクション:** メンテナレビュー。

---

### ![PR](icons/git-pull-request.svg) [Temporarily disable agenthost.enabled again by default](https://github.com/microsoft/vscode/pull/317797) `#317797`
**ステータス:** Merged | **ラベル:** なし

**概要:** `chat.agentHost.enabled` をプロダクトクオリティ全体でデフォルト無効に戻す 1 行変更。

**直近のアクティビティ:** `bryanchen-d` 承認。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: remove unsupported decommit_pooled_pages flag](https://github.com/microsoft/vscode/pull/317794) `#317794`
**ステータス:** Merged | **ラベル:** なし

**概要:** Chromium M134 更新後にサポートされなくなった V8 フラグを Electron ブートストラップから削除する。Issue #302602 を fix。

**直近のアクティビティ:** `justschen` 承認。

**次のアクション:** マージ済み。

---

## qmk/qmk_firmware

> スリープ復帰時のキーリピートバグ、Pro Micro to Halcyon コンバータ追加など小規模アクティビティ。

### ![Issue](icons/issue-opened.svg) [Keys are held down and keep repeating after waking from sleep](https://github.com/qmk/qmk_firmware/issues/23523) `#23523`
**ステータス:** Open | **ラベル:** bug, help wanted

**概要:** AKKO 5075S VIA / Bauer Lite (wt65_h2) などの VIA キーボードで、PC スリープ復帰時に押下したキーが押しっぱなしになる。

**直近のアクティビティ:** `polaire801` が macOS (Apple Silicon) でも Bauer Lite で同症状と報告。

**次のアクション:** USB ホストとの再接続シーケンス調査。

---

### ![PR](icons/git-pull-request.svg) [converter: Add Pro micro to Halcyon adapter and remove Imera](https://github.com/qmk/qmk_firmware/pull/26226) `#26226`
**ステータス:** Open | **ラベル:** core, documentation

**概要:** Halcyon コントローラを任意の Pro Micro ベースボードで使えるようにする「Pro Micro to Halcyon」アダプタを追加し、リリースされなかった Imera プロジェクトを削除する。

**直近のアクティビティ:** `drashna` が承認。

**変更ファイル:** `feature_converters.md`、`promicro_to_halcyon/_pin_defs.h` (+36) ほか 4 ファイル。

**次のアクション:** マージ準備。

---
