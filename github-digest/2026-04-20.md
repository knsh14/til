# GitHub ダイジェスト — 2026-04-20

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Run `LookaheadResolver` when resolving PEP 517 build requirements](https://github.com/astral-sh/uv/pull/19076)** (astral-sh/uv) — ワークスペース内の URL 依存をビルド依存リゾルバが認識しない問題（#19074）の修正 PR。`BuildDispatch::resolve` にもプロジェクト側と同じルックアヘッドパスを走らせる方針で、原因分析と再現テストまで添えて issue 報告から数時間で到着。
2. ![PR](icons/git-pull-request.svg) **[Retry failed partial downloads with HTTP Range header when supported.](https://github.com/astral-sh/uv/pull/19075)** (astral-sh/uv) — PyPI での接続切断に遭遇し続けたコントリビュータが、`pip` 同様に HTTP Range で部分再開するように書き直した個人的痛みドリブンの PR。#16934 #13359 #18642 #18538 など複数の既存 issue を解決候補に挙げる。
3. ![PR](icons/git-pull-request.svg) **[refactor(json)!: drop C-impl JSON de/serialization](https://github.com/neovim/neovim/pull/39181)** (neovim/neovim) — `eval/decode.c` / `eval/encode.c` の C 実装 JSON シリアライザを削除する破壊的変更の探索 PR。1603 行削除で、Vimscript↔JSON ラウンドトリップの存在意義を問う内容。
4. ![PR](icons/git-pull-request.svg) **[fix(smoothscroll): crash when resizing to textoff with showbreak](https://github.com/neovim/neovim/pull/39201)** (neovim/neovim) — `showbreak` 併用時のリサイズで発生するクラッシュを修正しマージ。即座にバックポート PR #39234 も出て 0.12 系にも反映。
5. ![PR](icons/git-pull-request.svg) **[Fix disposed SCM source control retention](https://github.com/microsoft/vscode/pull/311284)** (microsoft/vscode) — dispose された `SCMSourceControl` が extension host の管理簿に残り続けるメモリリーク（#311282）を修正してマージ。

---

## commaai/openpilot

> テスト高速化系と UI 周りの PR が多く、ステイル自動クローズも目立つ一日。eSIM UI と tizi/mici 基盤、`CarState.brake` 廃止の下準備が並行進行。

### ![PR](icons/git-pull-request.svg) [tools/lib/route: dedupe segment file mapping logic](https://github.com/commaai/openpilot/pull/37862) `#37862`
**ステータス:** Open | **ラベル:** tools

**概要:** `tools/lib/route.py` の `Route` クラスでファイルパス生成（`log_paths`、`qlog_paths`、`camera_paths` ほか）に重複していたロジックを、ローカル／リモート分岐を含む単一ヘルパーに集約するリファクタ。挙動（ローカルは先勝ち、リモートは後勝ち）と公開シグネチャは維持し、`tools/lib/tests/test_route_library.py` に回帰テストを追加。

**直近のアクティビティ:** process replay diff report が 0 変更・66 passed・0 errors を報告。レビューコメントはなし。

**変更ファイル:** `tools/lib/route.py` と同テストファイルで +139 / −72 の 2 ファイル変更。

**次のアクション:** 人によるレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [lpa: add is_euicc()](https://github.com/commaai/openpilot/pull/37847) `#37847`
**ステータス:** Merged | **ラベル:** (なし)

**概要:** `LPABase` に `is_euicc()` を追加、`TiciLPA` では `AT+CCHO` で ISD-R アプレットを問い合わせて eUICC かどうかを判定する実装。UI 側で eUICC 限定機能をゲートするのが目的で、`_acquire_lock` を `_acquire_channel` から分離して `open_isdr` のリトライ経路を避けて共用する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `system/hardware/base.py` と `system/hardware/tici/lpa.py`（+37 / −8）。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [esim: comma four profile management UI](https://github.com/commaai/openpilot/pull/37844) `#37844`
**ステータス:** Open (Draft) | **ラベル:** ui

**概要:** comma four 向け eSIM 管理 UI を追加するドラフト PR。バックグラウンドスレッドで LPA 操作を行う `CellularManager` バックエンドと、プロファイルカード・スライド確認による削除・切り替え UI を MICI 設定内に実装する。

**直近のアクティビティ:** process replay diff report は問題なし、UI preview では mici と big の両方で差分が検出されドラフト段階であることを示している。

**変更ファイル:** MICI ネットワーク設定まわり 6 ファイル（+619 / −4）、`esim_ui.py` と `cellular_manager.py` が新規追加。

**次のアクション:** レビュー前にドラフトから通常 PR への昇格と動作検証。

---

### ![PR](icons/git-pull-request.svg) [Lazy load libraries to speed up pytest collection](https://github.com/commaai/openpilot/pull/37861) `#37861`
**ステータス:** Open | **ラベル:** CI / testing, ui

**概要:** `pytest --collect-only` を 1 秒未満に短縮することを狙い、pytest 設定から `-n auto --dist=loadgroup` を外し、`test_widget_leaks.py` の UI 系重量 import（raylib.InitWindow を含む）をテスト関数内に遅延化する PR。ベンチは平均 1.02s → 0.89s（12% 改善）。

**直近のアクティビティ:** UI preview で mici / big ともに差分検出、process replay は 0 変更で問題なし。

**変更ファイル:** `selfdrive/ui/mici/tests/test_widget_leaks.py` のみ（+24 / −21）。

**次のアクション:** pyproject.toml 変更を反映した同著者の #37860（クローズ済）と統合する形でレビューを進める。

---

### ![Issue](icons/issue-opened.svg) [Improve pytest collection time to <1s](https://github.com/commaai/openpilot/issues/32611) `#32611`
**ステータス:** Open | **ラベル:** enhancement, CI / testing, bounty

**概要:** 2024 年から開いている bounty 付き issue で、4011 テストの collection を現在の 6〜7 秒から 1 秒未満に短縮するのが目的。`time pytest --collect-only` の改善前後比較が検証方法。

**直近のアクティビティ:** rohanbennur43 が最適化 PR #37861 を提出したことを報告し、平均 1.02s→0.89s の 12% 改善を示した。別の atguuuia も興味表明のコメントを投稿。

**次のアクション:** 提出された PR のレビューと、目標である 1 秒未満到達に向けたさらなる測定。

---

### ![PR](icons/git-pull-request.svg) [remove CarState.brake refs](https://github.com/commaai/openpilot/pull/37857) `#37857`
**ステータス:** Open | **ラベル:** car

**概要:** `CarState.brake` が多くの car 実装で誤って設定されており実質未使用であるため廃止する前準備として、`test_models.py` の 4 年物 Honda ペダル割り込み回避と GM 車コードの `brakePressed` への移行を行う。opendbc 側の #3338 と連動。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `selfdrive/car/car_specific.py`、`selfdrive/car/tests/test_models.py`（+4 / −15）。

**次のアクション:** opendbc/#3338 のマージを待ってから取り込む流れ。

---

### ![PR](icons/git-pull-request.svg) [loggerd/deleter: handle non-directory entries in log root](https://github.com/commaai/openpilot/pull/37736) `#37736`
**ステータス:** Open | **ラベル:** stale

**概要:** `os.listdir` と `shutil.rmtree` が通常ファイルに対しては失敗するため、log root にセグメントディレクトリ以外のファイルが混入するとディスク容量を永久に消費してしまう問題を修正。`isdir` で判定して `os.remove` で外れ値を削除する。#30102 の fix。

**直近のアクティビティ:** 24 日間活動がないとの自動警告コメントが投稿され、あと 7 日で自動クローズ予定。

**変更ファイル:** `system/loggerd/deleter.py`（+9 / −0）。

**次のアクション:** 自動クローズを避けるため、レビューもしくは著者の活動が必要。

---

その他、stale 自動クローズ（#37692 シェルスクリプトのクォート修正、#37693 tizi リプレイ、#37694 pytest collection、#37860 / #37858 / #37859 rohanbennur43 の試行）と、WIP の ModemManager 置き換え #37713 が存在。

---

## commaai/opendbc

> 新規プラットフォーム追加（GWM Haval H6、Honda Fit TW、Acura Integra）と Tesla まわりの調整が中心。`CarState.brake` 廃止の横断対応も進行。

### ![PR](icons/git-pull-request.svg) [GWM Haval H6: Initial Platform Support](https://github.com/commaai/opendbc/pull/3263) `#3263`
**ステータス:** Open | **ラベル:** CI / testing, DBC signals, car, car safety

**概要:** GWM Haval H6 を対応車種として新規追加する大規模 PR。AlexandreSato 本人に加え複数のユーザー（Conrado、otaviobonder）のテストルートが共有され、ハーネス（コネクタ 1376352-1 / 1565804-1）の Discord リンクと回路図、V1/V3 の写真、カメラカバー分解工具まで含む包括的な解説。

**直近のアクティビティ:** car_diff は変更なしで通過。直近 24 時間のレビューコメントはなし。

**変更ファイル:** 20 ファイル（+1019 / −42）、fingerprint・DBC 追加を含む。

**次のアクション:** メンテナーの一次レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Honda: Acura Integra 2023-25](https://github.com/commaai/opendbc/pull/2432) `#2432`
**ステータス:** Open | **ラベル:** DBC signals, car, honda, car safety

**概要:** Acura Integra 2023–2025 を Honda Bosch ラインに追加する長期 PR（2025 年 4 月オープン）。11 ファイルで +159 / −9。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 通常の車種追加と同様、実車確認とメンテナーレビュー。

---

### ![PR](icons/git-pull-request.svg) [VW MQB: work around standstill limit](https://github.com/commaai/opendbc/pull/2764) `#2764`
**ステータス:** Open (Draft) | **ラベル:** DBC signals, car, volkswagen

**概要:** EPB のない VW MQB 車で ESP が長時間停止ブレーキを保持したときの fault を、停止確定前に starting 状態へ遷移することで回避する大規模ワークアラウンド。急な坂道での後退リスクを抑えるため、最低速度しきい値を超えたら強制停止する仕組みや、ACC type 1 で運転者がブレーキを踏んでいる間の longitudinal 制御抑制を組み込む。

**直近のアクティビティ:** RJWadley と jyoung8607 を中心に 20 件超のレビューコメント往復。直近 24 時間の新規コメントはないが、更新が行われている。

**変更ファイル:** 7 ファイル（+459 / −17）、VW carcontroller/carstate/テスト/DBC を横断。

**次のアクション:** openpilot #37800 および opendbc #3322 の依存関係を解消してから本レビュー。

---

### ![PR](icons/git-pull-request.svg) [Tesla: smooth transition after gas pedal override](https://github.com/commaai/opendbc/pull/3249) `#3249`
**ステータス:** Open | **ラベル:** car, tesla

**概要:** アクセル解除直後に jerkMin/jerkMax を 0 から通常値までランプさせて、DI rate limiter が 0 から跳ね上がる急変を避ける改修。dongle `0f79c454f812791a` と該当ルートでの検証記載あり。

**直近のアクティビティ:** sshane がレビューコメントを 2 件付与。24 時間以内の新規コメントはない。

**変更ファイル:** 3 ファイル（+15 / −6）。

**次のアクション:** レビューコメントへの対応と追加検証。

---

### ![PR](icons/git-pull-request.svg) [deprecate CarState.brake](https://github.com/commaai/opendbc/pull/3338) `#3338`
**ステータス:** Open | **ラベル:** car, chrysler, ford, gm, honda, hyundai, mazda, tesla, volkswagen, rivian

**概要:** ほとんどの車種実装が `CarState.brake` を「未設定」「0 固定」「単位不明の整数」のいずれかで誤って埋めており、openpilot も実質使っていないため廃止する PR。前提 PR として openpilot/#37857 が必要。

**直近のアクティビティ:** github-actions による bot コメントのみ。

**変更ファイル:** 10 ファイル（+3 / −17）で各車種 carstate から該当フィールドを除去。

**次のアクション:** バリデーション用 dongle と route の記入、openpilot 側との同期マージ。

---

### ![PR](icons/git-pull-request.svg) [CI: Increase car_diff timeout to 20 minutes](https://github.com/commaai/opendbc/pull/3339) `#3339`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** car_diff ジョブが 10 分では足りずにタイムアウトする事例（リンク先ランを参照）があったため、タイムアウトを 20 分に引き上げる 1 行変更。

**直近のアクティビティ:** car_diff は変更なしで通過。

**変更ファイル:** `.github/workflows/*` 関連 1 ファイル（+1 / −1）。

**次のアクション:** 小さい変更でありメンテナーのマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Add new fingerprint data for Kia Niro EV](https://github.com/commaai/opendbc/pull/3307) `#3307` / ![PR](icons/git-pull-request.svg) [Honda: Fit 2025 (Taiwan)](https://github.com/commaai/opendbc/pull/3113) `#3113`
**ステータス:** Open (Kia), Draft (Fit TW)

**概要:** Kia Niro EV の fingerprint 追加、および Honda Fit 2025 台湾仕様のサポート追加。

**次のアクション:** 通常の車種追加と同様の検証とレビュー。

---

### ![PR](icons/git-pull-request.svg) [feat(Tesla): add setup note pointing to wiki](https://github.com/commaai/opendbc/pull/2122) `#2122`
**ステータス:** Merged

**概要:** Tesla セットアップ手順を GM/Nissan と同様に openpilot wiki に集約し、HW3/HW4 の情報分離、HW3 セットアップガイド、nightly-dev サポート、MrOne ストア参照リンクの修正を行う。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [Regression on Tesla Model 3 Highland (HW4) After Tesla OTA 2026.2.9.1](https://github.com/commaai/opendbc/issues/3295) `#3295`
**ステータス:** Open | **ラベル:** bug, car

**概要:** Tesla Model 3 Highland (2025 MY, HW4) に Tesla 側 OTA 2026.2.9.1 が入った後、comma 3X が「Car Unrecognized / Dashcam Mode」に落ちる車両側 OTA 互換性リグレッション。openpilot 側は 0.11.0 release-tizi のまま変化なし。

**直近のアクティビティ:** marchwill が Model 3 2022 で 2026.8.6.1 へ上げた後に同様症状、さらに「Autopilot」表記が「Self Driving」に変更されたと報告。

**次のアクション:** 影響車両の再フィンガープリント、あるいは新 OTA への対応調査。

---

## commaai/panda

> 直近 24 時間の更新はありません。

---

## neovim/neovim

> ui2（新 UI レイヤ）関連の修正とバックポートが多く、treesitter の `@noconceal` や `vim.pack` など機能追加も並走。LSP まわりの小修正やドキュメント整備もまとめて入っている。

### ![PR](icons/git-pull-request.svg) [refactor(json)!: drop C-impl JSON de/serialization](https://github.com/neovim/neovim/pull/39181) `#39181`
**ステータス:** Open (Draft) | **ラベル:** refactor, breaking-change

**概要:** `eval/decode.c` / `eval/encode.c` の C 実装 JSON シリアライザを落とす探索 PR。Vimscript↔JSON のラウンドトリップは優秀だが Lua↔JSON では活用されていないという結論で、#28683 の fix を狙う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 12 ファイル（+229 / −1603）。

**次のアクション:** 大規模 breaking-change のためメンテナーとの方向性合意が先。

---

### ![PR](icons/git-pull-request.svg) [feat(ui): implement :detach! to disconnect other UIs](https://github.com/neovim/neovim/pull/39216) `#39216`
**ステータス:** Open | **ラベル:** ui

**概要:** 従来「bang (!) not supported yet」でエラーを返していた `:detach!` を実装し、呼び出し元以外の UI を全て切り離してサーバーは生存させる挙動に。UI が 1 つだけなら no-op、UI が無い場合は `E5768: No UI attached` を返す。#39213 の close。

**直近のアクティビティ:** COMMENTED レビューが 2 件。

**次のアクション:** レビューコメント対応。

---

### ![PR](icons/git-pull-request.svg) [feat(options): add 'winpinned' to pin a window](https://github.com/neovim/neovim/pull/39157) `#39157`
**ステータス:** Merged | **ラベル:** options, window

**概要:** ウィンドウを「固定」して `:fclose` や `:only` の対象外にする window-local オプション `'winpinned'` を追加。ui2 の cmdline ウィンドウをデフォルトで pin することで意図せぬクローズを防ぐ。`:fclose` の hidden window 対象を見直し、#36123 と #36130 を解決。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(smoothscroll): crash when resizing to textoff with showbreak](https://github.com/neovim/neovim/pull/39201) `#39201` / [backport](https://github.com/neovim/neovim/pull/39234) `#39234`
**ステータス:** Merged (両方)

**概要:** `showbreak` を使っている状態で `textoff` までウィンドウを縮小したときのクラッシュを修正。承認を経てマージされ、バックポート PR も即座に取り込まれた。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter/extmark): support removing a conceal highlight](https://github.com/neovim/neovim/pull/35087) `#35087`
**ステータス:** Open | **ラベル:** treesitter

**概要:** `highlights.scm` で設定した `@conceal` を範囲内で打ち消せないという制限を解消するため、`@nospell` と同じ要領で `@noconceal` を導入、あるいは conceal メタデータを `\f` にセットすることで既存 conceal を上書き除去できるようにする。#35085 を close。

**直近のアクティビティ:** 14 件の COMMENTED レビュー往復。

**次のアクション:** レビュアーとの細部調整。

---

### ![PR](icons/git-pull-request.svg) [fix(api): expose fg_indexed/bg_indexed in nvim_get_hl](https://github.com/neovim/neovim/pull/39210) `#39210` / [backport](https://github.com/neovim/neovim/pull/39240) `#39240`
**ステータス:** Merged

**概要:** `nvim_get_hl` が `fg_indexed` / `bg_indexed` を返せるようにする修正。レビュー後に APPROVED でマージ、安定ブランチへのバックポートも完了。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [archive.lua to replace zipPlugin.vim](https://github.com/neovim/neovim/pull/39246) `#39246`
**ステータス:** Open

**概要:** 旧来の `zipPlugin.vim` を Lua 実装 `archive.lua` に置き換える提案。

**直近のアクティビティ:** COMMENTED レビューが 2 件。

**次のアクション:** レビュー継続。

---

### ![PR](icons/git-pull-request.svg) [fix(options): default titlestring shows PWD path](https://github.com/neovim/neovim/pull/39233) `#39233`
**ステータス:** Open | **ラベル:** options

**概要:** デフォルトの `titlestring` に PWD のパスが混入してしまう不具合を修正。

**直近のアクティビティ:** COMMENTED レビューが 1 件。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): don't strip debuginfo in precompile module](https://github.com/neovim/neovim/pull/39191) `#39191` / ![PR](icons/git-pull-request.svg) [perf(lsp): clear table by table.clear](https://github.com/neovim/neovim/pull/39222) `#39222` / ![PR](icons/git-pull-request.svg) [fix(snippet): cancel session on `<Esc>` in Select mode](https://github.com/neovim/neovim/pull/39238) `#39238`

**概要:** Lua precompile モジュールのデバッグ情報保持、LSP でのテーブルリセットに `table.clear` を使う性能改善、Select モードでの `<Esc>` によるスニペットセッション取り消しなど、パフォーマンスと UX の細かな改善群。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): check for `vim.NIL` in `vim.lsp.util.symbols_to_items()`](https://github.com/neovim/neovim/pull/39225) `#39225` / [fix(lsp): notify when maximum created `hl` groups is reached](https://github.com/neovim/neovim/pull/39231) `#39231` / [docs(lsp): include plain English description for `on_list` example](https://github.com/neovim/neovim/pull/39230) `#39230`

**概要:** LSP ユーティリティの `vim.NIL` 対応、highlight group 最大到達時の通知、`on_list` 例のドキュメント整備。

---

### ![PR](icons/git-pull-request.svg) [fix(drawline): hang while redrawing diff filler above fold](https://github.com/neovim/neovim/pull/4041) `#4041 (他)`
**ステータス:** Merged + backport

**概要:** fold 上の diff filler を再描画中にハングする問題を修正。バックポートも実施。

---

### ![PR](icons/git-pull-request.svg) [Zig0.16](https://github.com/neovim/neovim/pull/39076) `#39076`
**ステータス:** Open | **ラベル:** ci

**概要:** Zig 0.16 への移行を見据えた CI 関連準備 PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Zig 0.16 リリース／安定化を待っての取り込み。

---

### ![Issue](icons/issue-opened.svg) [treesitter: support `syntax=treesitter`, deprecate `treesitter.start()`](https://github.com/neovim/neovim/issues/39009) `#39009`
**ステータス:** Open | **ラベル:** treesitter, filetype, options

**概要:** `syntax=treesitter` を正式にサポートし、`treesitter.start()` 呼び出しを非推奨化する方向性の議論 issue。

**次のアクション:** 方針合意ののち実装。

---

### ![Issue](icons/issue-opened.svg) [`:restart` uses Nvim's current directory instead of shell's](https://github.com/neovim/neovim/issues/39239) `#39239` / [`vim.pack.add()` with `VersionRange` version is too permissive](https://github.com/neovim/neovim/issues/39214) `#39214` / [ui2: E212 (and probably others) are hidden on `:w`](https://github.com/neovim/neovim/issues/39235) `#39235`

**概要:** `:restart` がシェルの作業ディレクトリではなく Nvim の cwd を使う問題、`vim.pack.add` で `VersionRange` 指定が緩すぎる挙動、ui2 で書き込み時の `E212` が隠れるなど、細かな UX バグ報告が揃って投稿された。

---

その他、`vim-patch` による Vim 系パッチ取り込み（`9.2.0365`、`9.2.0368`、`8.2.4912` など）、`ci(backport)` ラベル運用変更、`nvim_echo` の progress percent 区別（#39029）、`delmarks` での `MarkSet` autocmd 発火（#4596 / backport #4545）、`vim.fs` 検索の `opts.skip` 追加（#4703）などの小粒だが着実な改善が多数マージされている。

---

## nvim-telescope/telescope.nvim

> plenary.nvim のアーカイブ予告に伴う自前ベンダリング／置き換え PR が進行中。

### ![PR](icons/git-pull-request.svg) [feat: vendor and clean up plenary](https://github.com/nvim-telescope/telescope.nvim/pull/3647) `#3647`
**ステータス:** Open (Draft)

**概要:** 2026-06-30 以降アーカイブ予定の plenary.nvim から必要な部分だけを `neoplen` として取り込み直す大規模リファクタ。`class`、`path`、`scandir` は削除し、`border`/`popup`、`job`、`log`、`async` を整理して拡張にも公開する方針。後続 PR で `job` を `vim.system` の薄いラッパーに書き換え、`log` / `async` は上流（`vim.log` / `vim.async`）安定後に移行予定。`plenary.busted` と `test_harness` は外部の `plentest.nvim` に移動。

**直近のアクティビティ:** 著者 clason による COMMENTED レビューを 3 回重ね、自己の作業メモ的な状況共有を継続。

**変更ファイル:** 72 ファイル（+2954 / −363）。

**次のアクション:** 著者以外による外部テスト協力募集中。

---

### ![Issue](icons/issue-opened.svg) [Consider dropping plenary.nvim dependency (it will be unmaintained/archived after Q2/2026)](https://github.com/nvim-telescope/telescope.nvim/issues/3651) `#3651`
**ステータス:** Closed | **ラベル:** enhancement

**概要:** plenary.nvim が 2026-06-30 以降アーカイブされる旨を受け、依存を外すことを提案した issue。

**直近のアクティビティ:** clason が「重複のため既存 issue / PR を確認してから出してほしい（ちなみにそのアーカイブ通知を書いたのは自分だ）」とコメントし即クローズ。

**次のアクション:** #3647 の進捗で対応中。

---

## ghostty-org/ghostty

> macOS まわりの不具合修正（アプリアイコン、スプリットドラッグ）と翻訳・ドキュメント系 PR が中心。Android / POSIX shared memory 対応も入る。

### ![PR](icons/git-pull-request.svg) [macOS: fix App Icon update in Finder](https://github.com/ghostty-org/ghostty/pull/12344) `#12344`
**ステータス:** Open | **ラベル:** os/macos

**概要:** `NSWorkspace.shared.setIcon` がメインアプリからしか呼べず、sandboxed な `DockTilePlugin` では `file-write-finderinfo` 権限が無いため Finder 上のアプリアイコンが更新されない問題を修正。release ビルドでのみ再現し、debug では正常だった。#11489 の fix。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+30 / −62）、`DockTilePlugin.swift` を削除して `AppIcon.swift` に処理を集約。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [macOS: fix surface focus/render state after dragging in to to another window/tab](https://github.com/ghostty-org/ghostty/pull/12338) `#12338`
**ステータス:** Open | **ラベル:** os/macos

**概要:** 2 つのバグを修正する PR。(1) 非フォーカス surface をウィンドウ A から B へすばやくドラッグし B を key window にしなかった場合、A 側のフォーカス surface が `keyDown` を受け取らない問題。(2) 同一ウィンドウ内でタブ A→B に surface をドラッグすると、drop 後の occlusion state が visible に戻らず入力は通るが描画されない問題（#12343）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `BaseTerminalController.swift`、`SurfaceView_AppKit.swift`（+19 / −3）。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [Dragging a split into a different tab causes split to function incorrectly](https://github.com/ghostty-org/ghostty/issues/12343) `#12343`
**ステータス:** Open | **ラベル:** os/macos

**概要:** macOS Tahoe 26.2 + Ghostty 1.3.0-main で、タブ間でスプリットをドラッグすると移動後に画面が更新されず入力だけ通る現象。タブタイトルは両方に反映されるなど奇妙な挙動を伴う。

**次のアクション:** #12338 で対応中。

---

### ![PR](icons/git-pull-request.svg) [android: Avoid referencing POSIX shared memory functions](https://github.com/ghostty-org/ghostty/pull/12341) `#12341`
**ステータス:** Merged

**概要:** Android の libc には `shm_open` などが無いため、`libghostty-vt` を Android にロードすると `dlopen failed: cannot locate symbol "shm_open"` で失敗する問題を修正。`src/terminal/kitty/graphics_image.zig` で Android 上では POSIX shared memory を呼ばないようにする。

**直近のアクティビティ:** jcollie と 00-kat による APPROVED レビューを経てマージ。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [macOS: fix #12266 by using the correct coordinates for the hitTest](https://github.com/ghostty-org/ghostty/pull/12322) `#12322`
**ステータス:** Merged | **ラベル:** os/macos

**概要:** 3 分割にして左の境界をドラッグまたは Equalize Splits 実行後、右端の split をクリックするとフォーカスが真ん中と右でジャンプする問題（#12266）の修正。hitTest に正しい座標を渡すように変更。#11872 の回帰。

**直近のアクティビティ:** mitchellh が APPROVED してマージ。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [i18n: add Belarusian translation (be)](https://github.com/ghostty-org/ghostty/pull/12284) `#12284` / [Translation documentation-related typos + CODEOWNERS update](https://github.com/ghostty-org/ghostty/pull/12336) `#12336` / [docs,ci: clarify that users can never open issues](https://github.com/ghostty-org/ghostty/pull/12335) `#12335`
**ステータス:** Merged (12284, 12336) / Open (12335)

**概要:** ベラルーシ語（80 文字列）ローカライズ追加、翻訳ドキュメント類の typo 修正と CODEOWNERS 更新、vouch 制による issue オープン制限の文言明確化など翻訳・CI まわりの一連整備。

**直近のアクティビティ:** 12284 は 00-kat から be_BY チームへの参加意思確認があり、vasilmytsyk が受諾して APPROVED。12336 は trag1c が APPROVED。

**次のアクション:** 12335 はレビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [update zon2nix](https://github.com/ghostty-org/ghostty/pull/12337) `#12337`
**ステータス:** Open

**概要:** Zig 0.16 互換性向上のために `zon2nix` を更新。

**次のアクション:** レビュー。

---

### ![Issue](icons/issue-opened.svg) [macOS: Global keybind stops in the app BUT not outside the app](https://github.com/ghostty-org/ghostty/issues/12294) `#12294`
**ステータス:** Open | **ラベル:** os/macos, input

**概要:** `keybind = global:cmd+space=toggle_visibility` 設定下で Ghostty 起動直後は global キーバインドが動くが、一度別アプリに切り替えた後は Ghostty に戻せない現象。Ghostty 再起動で一時的に復旧。

**直近のアクティビティ:** marko-markovic-space42-ai が macOS Tahoe 26.4.1 + Ghostty 1.3.1 でも同じ現象を確認したと追加報告。

**次のアクション:** 原因調査継続。

---

## astral-sh/uv

> renovate[bot] による依存更新の大量 PR に混じって、ワークスペース build 解決のバグ修正、retry ロジックのリファクタ、HTTP Range による再開ダウンロードなど効果の大きい改善が登場している。

### ![PR](icons/git-pull-request.svg) [Move retry logic into its own file](https://github.com/astral-sh/uv/pull/19073) `#19073`
**ステータス:** Merged | **ラベル:** internal

**概要:** リトライ処理が十分複雑になったため `base_client.rs` から切り出して独立モジュール化するリファクタ。

**直近のアクティビティ:** charliermarsh が APPROVED してマージ。

**変更ファイル:** 4 ファイル（+368 / −356）。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [Run `LookaheadResolver` when resolving PEP 517 build requirements](https://github.com/astral-sh/uv/pull/19076) `#19076`
**ステータス:** Open

**概要:** build dispatch がルックアヘッドなしで manifest を組み立てており、ワークスペースメンバーを `build-system.requires` 経由で参照する場合に推移的 URL 依存を認識できなかった問題を修正。プロジェクトリゾルバと同じく `LookaheadResolver` を先行実行し、`Manifest::with_lookaheads` に渡す。main で fail して本 PR で pass する回帰テストを追加。#19074 を close。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** 5 ファイル（+179 / −4）、`crates/uv-dispatch/src/lib.rs` 中心。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Retry failed partial downloads with HTTP Range header when supported.](https://github.com/astral-sh/uv/pull/19075) `#19075`
**ステータス:** Open

**概要:** PyPI がしょっちゅうコネクションをリセットするため現状の uv では retry が枯渇するが、pip は HTTP Range リクエストで再開してダウンロードを続行できる。1 年近くこの問題に困っていた apaz-cli が業を煮やして書いた PR。`cargo insta test` は `python_upgrade::python_upgrade_implementation`（main で既に壊れている）以外 pass。#16934 #13359 #18642 #18538 ほか多数の issue 解消を狙う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-distribution/src/distribution_database.rs`（+120 / −24）。

**次のアクション:** 以前の類似 PR (#17656) が「複雑すぎる」として却下された経緯があるため、コア開発者の評価待ち。

---

### ![PR](icons/git-pull-request.svg) [Add an environment variable for `UV_NO_PROJECT`](https://github.com/astral-sh/uv/pull/19052) `#19052` + ![Issue](icons/issue-opened.svg) [env var for `--no-project`](https://github.com/astral-sh/uv/issues/19020) `#19020`
**ステータス:** Merged / Closed | **ラベル:** configuration

**概要:** `--no-project` に対応する環境変数 `UV_NO_PROJECT` を追加。Issue #19020 を解決。

**直近のアクティビティ:** zsol が「自分もローカルブランチで書いていた」とコメントし、APPROVED レビューで `setup-uv` 側に `project: require|ignore|auto` を持たせる代案を提示。

**次のアクション:** 完了（setup-uv 側の改善は別議論）。

---

### ![PR](icons/git-pull-request.svg) [docs: disambiguate duplicate top-level setting anchors](https://github.com/astral-sh/uv/pull/19046) `#19046`
**ステータス:** Open

**概要:** トップレベル設定のアンカーが重複していたためドキュメント側を曖昧でないように書き換え。

**直近のアクティビティ:** alokshukla631 が APPROVED。

**次のアクション:** メンテナーのマージ判断。

---

### ![Issue](icons/issue-opened.svg) [URL Dependency Issue in Workspace Packages with Build Dependencies on Other Workspace Packages](https://github.com/astral-sh/uv/issues/19074) `#19074`
**ステータス:** Open | **ラベル:** bug

**概要:** ワークスペース内で `my-tool` が `my-backend` を build-dependency にし、`my-backend` が `my-util` に依存する構成で `uv build --wheel --package my-tool` が「Package `my-util` was included as a URL dependency」で失敗する。build-constraints やトランジティブ依存の明記で回避可能。

**直近のアクティビティ:** nooscraft が原因を特定し、`BuildDispatch::resolve` のマニフェストに lookahead が通っていないことを `crates/uv-resolver/src/resolver/urls.rs` まで追って解析、同日中に #19076 を投稿。

**次のアクション:** #19076 のレビュー／マージ。

---

### ![Issue](icons/issue-opened.svg) [uvx silently ignores requires-python, fails with confusing build error](https://github.com/astral-sh/uv/issues/19079) `#19079`
**ステータス:** Open

**概要:** `uvx open-webui serve` がデフォルト Python 3.14 のまま進んでしまい、`pyarrow==20.0.0` の cp314 wheel が無いためソースビルドに落ちて `cmake` なしでクラッシュする。実際はパッケージの `requires-python` が `<3.13` だが `uvx` が警告も出さずに続行する点が本質。`uvx --python 3.12` で回避可能。警告もしくは互換 Python への自動選択を提案。

**次のアクション:** トリアージ。

---

### ![Issue](icons/issue-opened.svg) [UV cache/sync issues?](https://github.com/astral-sh/uv/issues/19078) `#19078`
**ステータス:** Open | **ラベル:** bug

**概要:** `uv cache clean` 後でも `uv add tortoise-orm` が古いバージョンを入れ、`uv pip install` では正しいバージョンが入るのに `uv sync --upgrade` ではダウングレードされるという現象を POP_OS 24.04 / uv 0.11.7 / Python 3.14.2 で報告。

**次のアクション:** 再現最小ケースの追加確認。

---

### ![Issue](icons/issue-opened.svg) [`uv add --bounds=major` puts upper bound on minor version for 0.y.z packages](https://github.com/astral-sh/uv/issues/19047) `#19047`
**ステータス:** Open | **ラベル:** bug

**概要:** `uv add --bounds=major whenever` が `whenever>=0.10.0,<0.11.0` を生成し（本来 `<1.0` 相当を期待）、`--bounds=minor` では `<0.10.1` と事実上ピンになる。0.y.z パッケージでの bounds 解釈の不整合。

**次のアクション:** 仕様議論と修正。

---

### ![Issue](icons/issue-opened.svg) [Suppress timing output from `uv sync`](https://github.com/astral-sh/uv/issues/19050) `#19050`
**ステータス:** Open | **ラベル:** enhancement

**概要:** direnv で自動 `uv sync` を走らせているので「Resolved … in N ms」は不要だが、`--quiet` だと有用な info も消える。verbose と info の中間レベルで timing だけ抑制したい。

---

### ![Issue](icons/issue-opened.svg) [Can uv uninstall support -y flag ?](https://github.com/astral-sh/uv/issues/19077) `#19077` / [(Optional) Whitelist for uv-add](https://github.com/astral-sh/uv/issues/19049) `#19049` / [Exclude own version from uv.lock file?](https://github.com/astral-sh/uv/issues/17815) `#17815`

**概要:** pip 互換性のための `uv pip uninstall -y`（実質 no-op）サポート要望、`uv add` にホワイトリスト機構を入れたいという issue、自プロジェクトのバージョンを `uv.lock` から除外したい要望（Docker レイヤキャッシュ無効化を引き起こすため）など、UX 系の要望が活発。

---

### ![PR](icons/git-pull-request.svg) renovate[bot] による自動更新 PR 群 (#19054, #19071, #19062, #19055, #19072, #19070, #19069, #19068, #19067, #19066, #19065, #19064, #19063, #19061, #19060, #19059, #19058, #19057, #19056, #19053)

**概要:** `actions/upload-artifact` v7、`taiki-e/install-action` v2.75.9、`PyO3/maturin-action` v1.51.0、Python 3.14.4、`tokio` v1.51.1、`indexmap` v2.14.0、`hashbrown` 0.17.0 など多数の依存を一斉に bump。

**次のアクション:** CI 結果と互換性確認後にメンテナーがマージ。

---

## microsoft/vscode

> 1.118.0 エンドゲーム中でエンドゲームテストプラン項目がまとめて登録され、Copilot / Agents / Chat まわりの PR と cherry-pick が多数。SCM のメモリリーク修正と vscode-remote パス破損の連続再投稿が目立つ。

### ![PR](icons/git-pull-request.svg) [Fix disposed SCM source control retention](https://github.com/microsoft/vscode/pull/311284) `#311284`
**ステータス:** Merged | **ラベル:** scm

**概要:** dispose 済みの SCM source control が extension host 側の管理簿に残り続けるリーク（#311282）を解消する修正。

**直近のアクティビティ:** COMMENTED を経て APPROVED でマージ。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [fix(sessions): use non-empty path for unknown-workspace fallback URI to prevent joinPath crashes](https://github.com/microsoft/vscode/pull/311286) `(#310777 関連)` / 関連 issue [Error UriError: joinPath on URI without path](https://github.com/microsoft/vscode/issues/310777)

**概要:** Agents の不明ワークスペース fallback URI が空パスのまま `joinPath` に渡されてクラッシュする問題を、非空パスを使うように修正。

**次のアクション:** マージ進行。

---

### ![PR](icons/git-pull-request.svg) [Add `APPLICATION_SHARED` storage scope for cross-app state sharing](https://github.com/microsoft/vscode/pull/311317) `#311317`
**ステータス:** Open

**概要:** 複数 VS Code ベースアプリ間で state を共有するための新しいストレージスコープ `APPLICATION_SHARED` を追加する提案。sandy081 によるコア storage の拡張。

**直近のアクティビティ:** COMMENTED レビューが多数入り議論中。

**次のアクション:** スコープの定義とセキュリティ/権限方針の確認。

---

### ![PR](icons/git-pull-request.svg) [Refactor auto mode hints logic to include 'auto' in model hints for editor location](https://github.com/microsoft/vscode/pull/311331) `#311331` / [cherry-pick #311348](https://github.com/microsoft/vscode/pull/311348)
**ステータス:** Merged + cherry-pick Merged | **ラベル:** cherry-pick-artifact

**概要:** editor 位置での model hints に `auto` を含めるよう auto mode hints ロジックをリファクタ。Copilot API 側の github/copilot-api#31385 の対応。

**直近のアクティビティ:** cherry-pick が問題なくマージ完了。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [inline chat: add experiment-controlled reasoning effort and thinking settings](https://github.com/microsoft/vscode/pull/311350) `#311350`
**ステータス:** Open

**概要:** inline chat 向けに reasoning effort と thinking の設定を experiment 経由で制御できるようにする PR（jrieken）。

**直近のアクティビティ:** COMMENTED レビュー進行中。

**次のアクション:** レビューと experimentation 設定の確定。

---

### ![PR](icons/git-pull-request.svg) [Agents - disable indicators in the diff editor](https://github.com/microsoft/vscode/pull/311338) `#311338`
**ステータス:** Merged

**概要:** Agents 機能で diff editor 上のインジケーターを無効化する修正（lszomoru）。

**次のアクション:** 完了。

---

### ![PR](icons/git-pull-request.svg) [Hide review button when chat plan is collapsed](https://github.com/microsoft/vscode/pull/311352) `#311352`
**ステータス:** Open

**概要:** chat plan が折り畳まれているときにレビューボタンを非表示にする UI 修正。#311328 の fix。

**直近のアクティビティ:** COMMENTED レビュー中。

---

### ![PR](icons/git-pull-request.svg) [prompts: fix vscode-remote path corruption in PromptPathRepresentationService](https://github.com/microsoft/vscode/pull/311353) `#311353` / `#311351` / `#311354`
**ステータス:** いずれも Closed

**概要:** chencyu が `PromptPathRepresentationService` での vscode-remote パス破損修正を繰り返し再投稿し、いずれも Close されている状況。

**次のアクション:** 重複 PR の整理と維持者との擦り合わせ。

---

### ![PR](icons/git-pull-request.svg) [CopilotCLI: Hide plan when switching sessions](https://github.com/microsoft/vscode/pull/311331) `#311336 (cherry-pick #311255)` / [Bump version to 1.118.0](https://github.com/microsoft/vscode/pull/311336) `#311336 / #311330`
**概要:** セッション切り替え時に CopilotCLI のプランを隠す改修、1.118.0 へのバージョン bump、endgame ノートブックのマイルストーン更新など、1.118.0 リリース直前作業。

**直近のアクティビティ:** レビュー完了してマージ。

---

### ![PR](icons/git-pull-request.svg) [Fix #305740: avoid sync subprocess wait in TS node path resolution](https://github.com/microsoft/vscode/pull/311270) `#311270`
**ステータス:** Merged

**概要:** TypeScript Node.js パス解決で同期的な `subprocess.wait` を呼ばないように修正。#305740 の fix。

**次のアクション:** 完了。

---

### ![Issue](icons/issue-opened.svg) テストプラン項目群 (1.118.0 endgame)
- [Agents: Reuse VS Code auth](https://github.com/microsoft/vscode/issues/311359) #311359
- [Agent Host self-hosting: basic scenarios](https://github.com/microsoft/vscode/issues/311105) #311105
- [Test: Chronicle - local and cloud syncing](https://github.com/microsoft/vscode/issues/311360) #311360
- [Enable Chronicle - local and remote syncing](https://github.com/microsoft/vscode/issues/311361) #311361
- [Test GH auth secret migration](https://github.com/microsoft/vscode/issues/309737) #309737

**概要:** 1.118.0 リリースに向けたテストプラン項目の一連。Chronicle の同期、Agents の Reuse auth、Agent Host self-hosting、GitHub auth secret migration の再検証などを含む。

---

### ![Issue](icons/issue-opened.svg) [Completely disable refactoring TS/JS on Ctrl+/](https://github.com/microsoft/vscode/issues/311132) `#311132` / [Select model for git commit message generator](https://github.com/microsoft/vscode/issues/307953) `#307953` / [Agents app doesn't respect Autopilot](https://github.com/microsoft/vscode/issues/307963) `#307963` / [Extremely slow search in Keyboard shortcut panel](https://github.com/microsoft/vscode/issues/308734) `#308734` / [Keep/Undo widget sits over code text](https://github.com/microsoft/vscode/issues/307613) `#307613`

**概要:** Ctrl+/ に対する TS/JS リファクタリング完全無効化、git コミットメッセージ用モデル選択、Agents アプリの Autopilot 尊重、Keyboard shortcut パネルの検索遅延、Keep/Undo ウィジェットがコード上に被る現象などのユーザー要望・バグ報告。

---

### ![Issue](icons/issue-opened.svg) [Code review feature error](https://github.com/microsoft/vscode/issues/311148) `#311148` など

**概要:** Agents App での multi-root ワークスペース＋ worktree をまたぐ変更追跡要望、`Code disappears in editor tabs intermittently`、`Chat: Plan Mode immediately starts implementation when in Autopilot mode`、`Extension Signature Verification Failed: GitHub Copilot Chat`、`restart doesn't open window in the same workspace` など、Agents / Chat まわりの不具合・要望が大量に投稿されている。

---

### ![PR](icons/git-pull-request.svg) [Use `tsgo` to run type checks on client for watch](https://github.com/microsoft/vscode/pull/3938) `#3938` / [Align run_in_terminal output cap to 60kb and remove dead sanitizeTerminalOutput](https://github.com/microsoft/vscode/pull/3735) `#3735` / [Implement clearTodoList function and hide widget when no todos are present](https://github.com/microsoft/vscode/pull/3809) `#3809` / [Fix markdown preview deep navigation when fragment contains literal spaces](https://github.com/microsoft/vscode/pull/3889) `#3889`

**概要:** watch 時のクライアント型チェックに `tsgo` を使う変更、terminal 出力キャップを 60KB に揃えて不要な `sanitizeTerminalOutput` を削除、`clearTodoList` 実装、markdown preview の fragment スペース対応など、開発体験に関連する PR が並走。

---

## qmk/qmk_firmware

> 新キーボード追加 PR（Numpad_V2、Volt 80、Totem）が中心。Caps Word と Chordal Hold まわりのユーザーサポート issue も動きあり。

### ![PR](icons/git-pull-request.svg) [Adding Volt 80 Custom PCB Support](https://github.com/qmk/qmk_firmware/pull/26158) `#26158`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** Percent Studio aftermarket PCB「Volt 80」の新規追加。`keyboards/percent_studio_aftermarket/` 配下に設定と default keymap を新設（+336 / −0、4 ファイル）。

**直近のアクティビティ:** drashna が COMMENTED→APPROVED、著者 Jubakuba が追加コメント。

**次のアクション:** マージ手続き。

---

### ![PR](icons/git-pull-request.svg) [Add New Keyboard - Numpad_V2](https://github.com/qmk/qmk_firmware/pull/26164) `#26164`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** XBowsTech から 21 キーの Numpad_V2 新規追加（+262 / −0、5 ファイル）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add support for geigeigeist/totem](https://github.com/qmk/qmk_firmware/pull/25877) `#25877`
**ステータス:** Open | **ラベル:** keyboard, keymap, awaiting review

**概要:** 38 キーの column-staggered スプリットキーボード Totem（Seeed XIAO RP2040）を追加。keymap は miryoku をゆるく踏襲。

**直近のアクティビティ:** 著者 zelezarof が「レビューが長期間進んでおらず、5 月 10 日までに動きがなければクローズする」と宣言。過去に waffle87 と drashna からレビューがあり、drashna は 2026-01-10 に APPROVED していた。

**次のアクション:** メンテナーによる最終判断と merge。

---

### ![PR](icons/git-pull-request.svg) [Crkbd eehands](https://github.com/qmk/qmk_firmware/pull/26165) `#26165`
**ステータス:** Closed

**概要:** Crkbd の VIA 用 keymap に OLED フレーム生成スクリプトなどを含んで新規追加する PR。

**直近のアクティビティ:** 著者 chikulla が「誤って開いたのでクローズする」と投稿しクローズ。

---

### ![Issue](icons/issue-opened.svg) [When Caps Word is active, mouse clicks select a range of text](https://github.com/qmk/qmk_firmware/issues/26086) `#26086`
**ステータス:** Open | **ラベル:** question, help wanted

**概要:** Caps Word 有効中はキー間で Shift を弱く保持するため、マウスクリックが範囲選択になってしまう挙動報告。

**直近のアクティビティ:** getreuer が Caps Word の idle timeout（既定 5 秒）を短くすることで部分緩和できると案内、`config.h` で `#define CAPS_WORD_IDLE_TIMEOUT 3000` の例を提示。

**次のアクション:** 挙動自体の是非は継続議論。

---

### ![Issue](icons/issue-opened.svg) [[Feature Request] Chordal Hold handedness: L, R, Neither, Both](https://github.com/qmk/qmk_firmware/issues/26162) `#26162`
**ステータス:** Open | **ラベル:** enhancement, help wanted

**概要:** Chordal Hold の handedness 設定に、`*`（例外）の逆で「常に逆の手ルールの対象になる」値（例: `!`）を追加したい要望。Repeat キーがホームロウ MOD と干渉する際に mod-tap を tapped として確定させたい、という実使用ケース。

**直近のアクティビティ:** getreuer が `get_chordal_hold()` コールバックで per-chord にカスタムする実装例を提示し、ハンドネス設定より優先して `false` を返せば同等の挙動を userspace で得られると案内。

**次のアクション:** コア側で値追加するかはメンテナー判断。

---

## 生成メモ

- 今回 `microsoft/vscode` と `neovim/neovim` は更新件数が非常に多いため、ハイライトと傾向ベースのまとめを中心にし、全件個別の章立ては省略している。
- `commaai/panda` は直近 24 時間の更新なし。
