# GitHub ダイジェスト — 2026-05-30

## 本日のハイライト

1. ![Issue](icons/issue-opened.svg) **[getDisplayMedia called at workbench boot → KDE screen-share dialog on every new window](https://github.com/microsoft/vscode/issues/317948)** (microsoft/vscode) — VS Code 1.122 系で起動時に Wayland/KDE の画面共有ダイアログが出る回帰が複数 Issue（#317948, #319132, #319101, #318030 ほか）で同時多発的に報告され、コメントでも不安の声が集中している。
2. ![Issue](icons/issue-opened.svg) **[More actions button is easily confused for minimize button](https://github.com/microsoft/vscode/issues/319104)** (microsoft/vscode) — タイトルバーに追加された Agents 関連ボタンが最小化ボタンと紛らわしいという指摘から、AI 機能の押し付けへの強い反発を含む 7 件のコメント議論に発展。PR #319119 で非表示設定が追加された。
3. ![PR](icons/git-pull-request.svg) **[Async API](https://github.com/neovim/neovim/pull/40056)** (neovim/neovim) — Lua 向けにチャネルベースの非同期 API（`vim.async.spawn` / `vim.async.chan`）を新規実装する大型 PR。コルーチンモデルで `vim._async` プロトタイプを置き換える。
4. ![Issue](icons/issue-opened.svg) **[[$3000 bounty] BYD port](https://github.com/commaai/opendbc/issues/2017)** (commaai/opendbc) — BYD 車種対応の高額バウンティで、複数コントリビューターの動向や追加バウンティ（Sealion 7 への +$500）など調整コメントが活発。
5. ![PR](icons/git-pull-request.svg) **[Add `uv check` to run `ty` from uv](https://github.com/astral-sh/uv/pull/19605)** (astral-sh/uv) — uv から型チェッカー `ty` を取得・実行する `uv check` の最小実装がマージされ、後続 PR #19609 で設計の方向性をさらに議論中。

---

## commaai/openpilot

> リリース 0.11.1 の準備が進む中、usbgpu/tinygrad 周りのモデル実行系の整理と remote teleop（connect 連携）関連の PR、ファン制御やバウンティ Issue が動いている。

### ![Issue](icons/issue-opened.svg) [Reduce kernel boot time to <1s on comma 3X](https://github.com/commaai/openpilot/issues/30894) `#30894`
**ステータス:** Open | **ラベル:** enhancement, bounty, comma three

**概要:** comma 3X の最新 AGNOS で約 3.65 秒かかっているカーネル起動時間を、openpilot 本体の起動を遅延させない形で 1 秒未満に短縮するバウンティ。NFC や Bluetooth など openpilot が使わない機能を無効化する案が示されている。

**直近のアクティビティ:** @jeromeyhq がバウンティを請け負いたいと表明し、Linux カーネルの深い経験があり即着手できるとコメント。

**次のアクション:** バウンティ担当者の正式アサインと実装方針の確認。

---

### ![Issue](icons/issue-opened.svg) [Match stock Hyundai button logic](https://github.com/commaai/openpilot/issues/30950) `#30950`
**ステータス:** Open | **ラベル:** enhancement, bounty, car, hyundai

**概要:** openpilot（alpha）の縦方向制御使用時に純正と挙動が異なり、ユーザーが engage 方法に戸惑う問題への対応。panda と openpilot で純正の set/resume/main on ボタンのロジックに合わせる。

**直近のアクティビティ:** @jeromeyhq が Hyundai 車両システムの経験を挙げてバウンティを請け負いたいとコメント。

**次のアクション:** 純正ボタンロジックの実装と検証。

---

### ![PR](icons/git-pull-request.svg) [test models: check angle matches safety](https://github.com/commaai/openpilot/pull/38110) `#38110`
**ステータス:** Open | **ラベル:** CI / testing, car

**概要:** モデルテストで操舵角が safety と一致するか検査するテストを追加。opendbc#3423 を捕捉するが、Tesla の失敗も検出している模様。

**直近のアクティビティ:** Process replay diff レポート（bot）で 66 件パス・変化なしを確認。

**変更ファイル:** `selfdrive/car/tests/test_models.py`（+16/-0）。

**次のアクション:** Tesla 側の失敗要因の切り分け。

---

### ![PR](icons/git-pull-request.svg) [parameterized_class: fix class name](https://github.com/commaai/openpilot/pull/38109) `#38109`
**ステータス:** Merged

**概要:** PR #37280 以降 `-k TOYOTA` での絞り込みが壊れていた問題を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `common/parameterized.py`（+8/-1）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [modeld/SConscript: tg_devices depends on tg_backend only](https://github.com/commaai/openpilot/pull/38108) `#38108`
**ステータス:** Merged

**概要:** usbgpu 非検出で起動すると `tg_input_devices.json` が上書きされ、その後 GPU をホットプラグすると入力デバイスのメタデータが失われる問題を修正。このメタデータは将来 pkl に折り込むべきとされる。

**直近のアクティビティ:** model replay のアーティファクト参照（commaci-public）と、Process replay diff で 66 件パスを確認（bot）。

**変更ファイル:** `selfdrive/modeld/SConscript`（+2/-2）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [remote teleop sound requests](https://github.com/commaai/openpilot/pull/38012) `#38012`
**ステータス:** Closed（ドラフト）

**概要:** WebRTC 接続経由で要求された音を再生するための `soundRequest` capnp イベントを追加する変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `cereal/services.py`、`selfdrive/ui/soundd.py`、`system/athena/athenad.py`。

**次のアクション:** クローズ済み（#38106 へ集約と推測されるが本文には明記なし）。

---

### ![PR](icons/git-pull-request.svg) [teleop in connect](https://github.com/commaai/openpilot/pull/37796) `#37796`
**ステータス:** Closed

**概要:** connect 向けの body ジョイスティック teleop サポート。athenad での getNotCar と startJoystickStream、単一ビデオチャネルへの統合、データチャネル経由のカメラ切替や音再生、デバッグ用のフレームヘッダーへのタイミング情報付与などを含む。

**直近のアクティビティ:** 作者 @stefpi のコメントレビューあり（本文なし）。

**変更ファイル:** `cereal/log.capnp`、`system/athena/athenad.py`、`system/loggerd/encoderd.cc`、`system/webrtc/device/video.py` ほか 9 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [turn off dm on notCar](https://github.com/commaai/openpilot/pull/37856) `#37856`
**ステータス:** Closed

**概要:** notCar（body）でドライバーモニタリングを無効化。WebRTC 接続の切替時に commIssues を起こしていたため remote teleop の助けになり、非車載デバイスでは不要でもある。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `selfdrive/selfdrived/selfdrived.py`、`system/manager/process_config.py`。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [param controlled adaptive bitrate in hardware encoder](https://github.com/commaai/openpilot/pull/38095) `#38095`
**ステータス:** Merged

**概要:** param に基づいて v4l ハードウェアエンコーダのビットレートを変更できるようにする。

**直近のアクティビティ:** Process replay diff で 66 件パスを確認（bot）。

**変更ファイル:** `system/loggerd/encoder/v4l_encoder.cc`、`system/loggerd/encoderd.cc`、`common/params_keys.h` ほか 8 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [lateral maneuver runner bugs](https://github.com/commaai/openpilot/issues/38107) `#38107`
**ステータス:** Open

**概要:** Altima で lateral maneuver runner を試したが実用に至らず、開始直後に「road not straight」で頻繁に中断する。gasPressed を開始抑止条件に加えるなど複数のバグがリスト化されている。

**直近のアクティビティ:** @elkoled が報告に感謝し、各 maneuver は longitudinal report と同様に開始音が鳴ること、他の問題は来週調査する旨をコメント。

**次のアクション:** gasPressed 条件の追加と「road not straight」誤判定の調査。

---

### ![Issue](icons/issue-opened.svg) [Fan running full speed](https://github.com/commaai/openpilot/issues/38087) `#38087`
**ステータス:** Open | **ラベル:** bug

**概要:** 0.11 更新後、comma 3X のファンが温度に過敏になり、外気 70°F 以上で走行中はほぼ 100%で回るという報告。車を切ると即停止する。

**直近のアクティビティ:** @elkoled が setpoint を引き上げた PR #38100 を案内し、master で改善するか試すよう促した。

**次のアクション:** 報告者による master での確認。

---

### ![PR](icons/git-pull-request.svg) [manager: remove obsolete tinygrad `MainProcess` workaround](https://github.com/commaai/openpilot/pull/38104) `#38104`
**ステータス:** Merged

**概要:** 不要になった tinygrad の `MainProcess` 回避策を削除する。

**直近のアクティビティ:** Process replay diff で 66 件パスを確認（bot）。

**変更ファイル:** `system/manager/process.py`（+1/-5）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [sound requests from webrtc for body](https://github.com/commaai/openpilot/pull/38106) `#38106`
**ステータス:** Closed（ドラフト）

**概要:** body 向けに WebRTC からの音再生要求を扱う変更。

**直近のアクティビティ:** UI Preview で動画が一致（bot）、Process replay diff で 66 件パスを確認（bot）。

**変更ファイル:** `cereal/log.capnp`、`selfdrive/ui/soundd.py`、`system/athena/athenad.py` ほか。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [migration: fix missing and wrong fields in driverMonitoringStateDEPRECATED](https://github.com/commaai/openpilot/pull/38105) `#38105`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** `driverMonitoringStateDEPRECATED` のマイグレーションで欠落・誤りのあるフィールドを修正。

**直近のアクティビティ:** Process replay diff で 66 件パスを確認（bot）。

**変更ファイル:** `selfdrive/test/process_replay/migration.py`（+9/-4）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [usbgpu: fallback on small model](https://github.com/commaai/openpilot/pull/38098) `#38098`
**ステータス:** Open（ドラフト）

**概要:** usbgpu が使えない場合に small model にフォールバックする実装。ソフトディスエンゲージのトリガーや modelv2 の重複回避、odom/drivingModelData の扱いなどが TODO として残る。

**直近のアクティビティ:** @adeebshihadeh と作者 @Armandpl のコメントレビュー、Process replay diff で 66 件パスを確認（bot）。

**変更ファイル:** `selfdrive/modeld/modeld.py`、`selfdrive/modeld/router.py`、`selfdrive/modeld/big_modeld.py` ほか 7 ファイル。

**次のアクション:** TODO 項目の解消とレビュー継続。

---

### ![PR](icons/git-pull-request.svg) [comma four: warning for enabling experimental mode](https://github.com/commaai/openpilot/pull/37961) `#37961`
**ステータス:** Open（ドラフト）| **ラベル:** ui

**概要:** comma four で実験的モードを有効化する際の警告を追加する UI 変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `selfdrive/ui/mici/layouts/settings/toggles.py`、`selfdrive/ui/mici/layouts/home.py`、`selfdrive/ui/ui_state.py`。

**次のアクション:** ドラフト完成とレビュー。

---

### ![Issue](icons/issue-opened.svg) [openpilot 0.11.1 release](https://github.com/commaai/openpilot/issues/38004) `#38004`
**ステータス:** Open

**概要:** 0.11.1 リリースを追跡するチェックリスト Issue。staging への投入手順（リリース master ブランチ作成、リスクのあるコミットの revert、staging へのビルド push など）が項目化されている。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** チェックリストの残項目の消化。

---

## sunnypilot/sunnypilot

> upstream（openpilot/opendbc）由来の挙動に関する小さな修正・移植が中心。

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open（ドラフト）| **ラベル:** car

**概要:** Hyundai/Kia/Genesis の ccNC（non-HDA2）移植。本文は関連 opendbc PR（sunnypilot/opendbc#94）へのリンクのみ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `opendbc_repo`（サブモジュール参照、+1/-1）。

**次のアクション:** opendbc 側の対応とドラフト完成。

---

### ![Issue](icons/issue-opened.svg) [Scrolling causes a click on lists](https://github.com/sunnypilot/sunnypilot/issues/1528) `#1528`
**ステータス:** Closed

**概要:** リストでスクロールするとカテゴリを展開する意図がなくてもクリック扱いになる、という upstream 由来の挙動。別 PR で修正予定とされている。

**直近のアクティビティ:** 別 Issue #1495 のコメントを引用し、upstream 由来の挙動として別 PR で対応する方針を記載。

**次のアクション:** 修正 PR での対応。

---

## commaai/opendbc

> 各車種の port（VW ID.4、Honda、Hyundai/Kia、Tesla、GM など）が多数並行し、Ford/Tesla の lateral 安全制御（MAX_LAT_ACCEL/JERK の統一、native curvature）周りの refactor が活発。stale ラベルが付いた長期 PR も多い。

### ![PR](icons/git-pull-request.svg) [Fix: Decouple dual usage of tireStiffnessFactor to allow additional car support](https://github.com/commaai/opendbc/pull/2378) `#2378`
**ステータス:** Open | **ラベル:** car, stale

**概要:** `tireStiffnessFactor` が Civic 基準定数のスケーリングと paramsd のライブパラメータの両方に使われている問題を分離する。openpilot#25389 の paramsd エラーを修正し、Ram HD トラックを dashcam モードから外すために必要（ただし十分ではない）とされる。

**直近のアクティビティ:** Car behavior report で変化なし（bot）。コントリビューションのチェック項目を案内する自動レビュー（bot）。

**変更ファイル:** `opendbc/car/interfaces.py`、`opendbc/car/__init__.py`。

**次のアクション:** メンテナーレビュー。

---

### ![Issue](icons/issue-opened.svg) [[$3000 bounty] BYD port](https://github.com/commaai/opendbc/issues/2017) `#2017`
**ステータス:** Open | **ラベル:** enhancement

**概要:** 任意モデルで可の BYD 対応バウンティ。ハーネス設計、高品質な lateral port、可能なら longitudinal を要件とする。

**直近のアクティビティ:** @MyTH-zyxeon が調整状況をまとめてコメント。Sealion 7 への lateral+longitudinal 追加に +$500 を出す申し出（@Philan014）や、BYD が既に動作しているという報告などの新情報を共有。

**次のアクション:** 既存ドラフトの集約とメンテナー側の調整。

---

### ![PR](icons/git-pull-request.svg) [Honda: fix radarless braking](https://github.com/commaai/opendbc/pull/2050) `#2050`
**ステータス:** Open | **ラベル:** DBC signals, car, honda, stale

**概要:** 2025/2026 Civic Hybrid と 2025 Acura ADX が要求どおりの強さで制動しない問題。stock ACC の制動中は idlestop メッセージが常時オンになっている点を再現（制動時に常にビットを立てる）したところ制動強度が改善した。

**直近のアクティビティ:** Car behavior report（bot）と、60 日無活動による自動クローズ予告（bot）。@sshane らのコメントレビューあり。

**変更ファイル:** `opendbc/car/honda/hondacan.py`、`opendbc/dbc/generator/honda/honda_bosch_radarless.dbc`。

**次のアクション:** レビュー対応と stale 解消。

---

### ![PR](icons/git-pull-request.svg) [Honda: Dashcam support for Acura MDX 3G_MMR - serial steering](https://github.com/commaai/opendbc/pull/1938) `#1938`
**ステータス:** Open | **ラベル:** car, honda, stale, car safety

**概要:** serial steering 車（linbus 操舵のためコミュニティ製変換ボードが必要）を dashcam モードでサポートする一連の PR の第一弾。北米販売統計で 120 万台の追加 dashcam 対応につながるとされ、本 PR では 2020 MDX を対象とする。

**直近のアクティビティ:** 60 日無活動の自動クローズ予告（bot）と Car behavior report（bot）。@jyoung8607 が dashcam 専用モードはマージ基準を緩められるが各車の CI ルートは必要とコメント（CHANGES_REQUESTED）。

**変更ファイル:** `opendbc/car/honda/values.py`、`opendbc/car/honda/interface.py`、`opendbc/car/honda/fingerprints.py` ほか 11 ファイル。

**次のアクション:** CI ルートの追加とレビュー対応。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Genesis GV80 2021-22 (Middle East only)](https://github.com/commaai/opendbc/pull/1136) `#1136`
**ステータス:** Open | **ラベル:** car, hyundai ほか多数, stale

**概要:** 中東向け Genesis GV80 2021-22 の車種 port。チェックリスト（CarInfo 追加、テストルート、ハーネスタイプ等）は完了済み。234 ファイル・+12220/-509 の大規模変更。

**直近のアクティビティ:** 60 日無活動の自動クローズ予告（bot）。

**変更ファイル:** `docs/CARS.md`、各種 workflow、`opendbc/car/__init__.py` ほか 234 ファイル。

**次のアクション:** 大規模差分のリベース・レビュー。

---

### ![PR](icons/git-pull-request.svg) [HKG: Car Port for Hyundai Palisade and Kia Telluride 2023-25 (non-HDA2)](https://github.com/commaai/opendbc/pull/1273) `#1273`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, stale, car safety

**概要:** Hyundai Palisade / Kia Telluride 2023-25（non-HDA2）の port。stock/openpilot 双方のテストルートを記載。

**直近のアクティビティ:** 60 日無活動の自動クローズ予告（bot）。@sshane らのコメントレビューあり。

**変更ファイル:** `opendbc/car/hyundai/hyundaican.py`、`opendbc/car/hyundai/carcontroller.py`、`opendbc/car/hyundai/values.py` ほか 8 ファイル。

**次のアクション:** レビュー対応と stale 解消。

---

### ![PR](icons/git-pull-request.svg) [HKG: Elantra 2024-25 Port](https://github.com/commaai/opendbc/pull/1537) `#1537`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, stale, car safety

**概要:** Hyundai Elantra 2024-25、Elantra Hybrid 2024-25、i30 Hybrid 2024 をサポートする CAN アーキテクチャの port。ハーネスは Hyundai K。

**直近のアクティビティ:** 60 日無活動の自動クローズ予告（bot）。

**変更ファイル:** `opendbc/car/hyundai/fingerprints.py`、`opendbc/car/hyundai/values.py`、`opendbc/dbc/hyundai_kia_generic.dbc` ほか 7 ファイル。

**次のアクション:** レビューと stale 解消。

---

### ![PR](icons/git-pull-request.svg) [GM: FPv3 based on VIN](https://github.com/commaai/opendbc/pull/1624) `#1624`
**ステータス:** Open | **ラベル:** car, gm, stale, rivian

**概要:** VIN に基づく GM の FPv3 対応（本文記載なし）。

**直近のアクティビティ:** 60 日無活動の自動クローズ予告（bot）。@jyoung8607 が GM lateral 制御の最近の進展のため再オープンしたとコメント（CHANGES_REQUESTED）し、本 PR が改善適用のブロッカーになっていたと説明。

**変更ファイル:** `opendbc/car/gm/values.py`、`opendbc/car/vin.py`、`opendbc/car/rivian/values.py` ほか。

**次のアクション:** GM lateral 改善との整合とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Volkswagen ID.4 lateral](https://github.com/commaai/opendbc/pull/3366) `#3366`
**ステータス:** Open | **ラベル:** DBC signals, car, volkswagen, car safety

**概要:** VW MEB プラットフォーム初の lateral port。AEB 無効化を防ぎ stock の Emergency Assist エスカレーションをサポートするため、当面ゲートウェイハーネスのみ対応。MEB EPS は curvature target を使い、`lateral.py` に新しいネイティブヘルパーを追加。検証完了まで Dashcam モード。

**直近のアクティビティ:** Car behavior report で多数 Ford セグメントをテスト（bot）。@elkoled のコメントレビューあり。

**変更ファイル:** `opendbc/car/volkswagen/carcontroller.py`、`opendbc/car/lateral.py`、`opendbc/car/ford/carcontroller.py` ほか 27 ファイル。

**次のアクション:** EA エスカレーション検証後の Dashcam 解除（別 PR）。

---

### ![PR](icons/git-pull-request.svg) [Add new fingerprint Tesla 2022 Model Y LR](https://github.com/commaai/opendbc/pull/3411) `#3411`
**ステータス:** Open | **ラベル:** car, tesla, fingerprint

**概要:** Tesla 2022 Model Y LR の fingerprint を追加。Dongle ID とルートで検証。

**直近のアクティビティ:** Car behavior report で 20 セグメントがパス・変化なし（bot）。自動レビュー（bot）。

**変更ファイル:** `opendbc/car/tesla/fingerprints.py`（+1/-0）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Ford native curvature](https://github.com/commaai/opendbc/pull/3405) `#3405`
**ステータス:** Open | **ラベル:** car, ford, car safety

**概要:** Ford を汎用の `apply_std_curvature_limits` に移行し、Tesla の angle VM safety と同じく lateral acceleration/jerk 空間で動作させる。curvature に直接適用するため vehicle model 不要。手動の 3 つの curvature rate ブレークポイントを物理定数 MAX_LATERAL_ACCEL=3.6 m/s²、MAX_LATERAL_JERK=3.6 m/s³ の 2 つに置換。base ブランチは `unify-lat`。

**直近のアクティビティ:** Car behavior report で 111 Ford セグメントをテスト（bot）。@sshane らのコメントレビューあり。

**変更ファイル:** `opendbc/safety/lateral.h`、`opendbc/car/ford/carcontroller.py`、`opendbc/safety/modes/ford.h` ほか 12 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Volkswagen ID.4 longitudinal](https://github.com/commaai/opendbc/pull/3424) `#3424`
**ステータス:** Open | **ラベル:** DBC signals, car, volkswagen, car safety

**概要:** VW MEB プラットフォーム初の longitudinal port。lateral 版と同様にゲートウェイハーネスのみ・Dashcam モードで開始。

**直近のアクティビティ:** Car behavior report で多数の Ford セグメントをテスト（bot）。

**変更ファイル:** `opendbc/car/volkswagen/carcontroller.py`、`opendbc/car/lateral.py`、`docs/CARS.md` ほか 28 ファイル。

**次のアクション:** 検証後の Dashcam 解除とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Car docs - Limit stop light and stop sign sentence to SNG-capable cars](https://github.com/commaai/opendbc/pull/3156) `#3156`
**ステータス:** Open | **ラベル:** car, stale

**概要:** SNG 非対応車について、Web サイトで stop light / stop sign に言及しないようにする。例として 2018-2020 Honda Odyssey は 25mph 超のみ ACC 対応のため停止信号の扱いに触れるべきでない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `opendbc/car/docs_definitions.py`（+1/-1）。

**次のアクション:** レビューと stale 解消。

---

### ![PR](icons/git-pull-request.svg) [Honda: Dashcam support for Accord 9G - serial steering](https://github.com/commaai/opendbc/pull/2795) `#2795`
**ステータス:** Open | **ラベル:** DBC signals, car, honda, stale, car safety

**概要:** serial steering 車を dashcam モードでサポートする一連の PR の一部。本 PR は 16-17 Accord（ACCORD_9G）を対象。

**直近のアクティビティ:** Car behavior report（bot）。@jyoung8607 の CHANGES_REQUESTED と作者のコメントあり。

**変更ファイル:** `opendbc/car/honda/values.py`、`opendbc/car/honda/fingerprints.py`、`opendbc/car/honda/hondacan.py` ほか 15 ファイル。

**次のアクション:** レビュー対応と CI ルート整備。

---

### ![PR](icons/git-pull-request.svg) [Honda: CANFD long control](https://github.com/commaai/opendbc/pull/2569) `#2569`
**ステータス:** Open | **ラベル:** car, honda, stale, car safety

**概要:** Honda CANFD モデルの長手制御を追加。PR2162 を基に最新 opendbc へリベースし safety コードを追加。長手チューニングは別 PR、起動時の cruise fault 回避策も別 PR で対応済み。

**直近のアクティビティ:** Car behavior report（bot）。

**変更ファイル:** `opendbc/safety/modes/honda.h`、`opendbc/safety/tests/test_honda.py`、`opendbc/car/honda/interface.py` ほか 6 ファイル。

**次のアクション:** cruise fault 問題の解消とレビュー。

---

### ![PR](icons/git-pull-request.svg) [car diff: add show errors tab](https://github.com/commaai/opendbc/pull/3422) `#3422`
**ステータス:** Merged | **ラベル:** car

**概要:** car diff にエラー表示タブを追加し、メッセージを重複除去する。

**直近のアクティビティ:** Car behavior report で変化なし（bot）。

**変更ファイル:** `opendbc/car/tests/car_diff.py`（+5/-2）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Reapply "Nissan: use high precision steering angle"](https://github.com/commaai/opendbc/pull/3423) `#3423`
**ステータス:** Open | **ラベル:** car, nissan

**概要:** opendbc#3420 のリバートを再適用（Nissan の高精度操舵角の利用）。本文には「なぜ test_models が失敗しないのか」という疑問が記されている。

**直近のアクティビティ:** Car behavior report で Nissan 50 セグメント中 50 件が changed（bot）。

**変更ファイル:** `opendbc/car/nissan/carstate.py`（+2/-2）。

**次のアクション:** test_models が変化を検出しない理由の調査。

---

### ![PR](icons/git-pull-request.svg) [unify MAX_LAT_ACCEL and JERK](https://github.com/commaai/opendbc/pull/3414) `#3414`
**ステータス:** Open | **ラベル:** car, ford, tesla

**概要:** すべての vehicle model および curvature ベースの車で使う MAX_LAT_ACCEL と MAX_LAT_JERK を統一する。

**直近のアクティビティ:** @elkoled が「@sshane good to go?」とレビューを促し、双方のコメントレビューあり。

**変更ファイル:** `opendbc/car/lateral.py`、`opendbc/car/tesla/values.py`、`opendbc/car/ford/carcontroller.py`。

**次のアクション:** @sshane のレビュー判断。

---

### ![PR](icons/git-pull-request.svg) [Honda - add 24 CRV Canada fingerprint](https://github.com/commaai/opendbc/pull/3421) `#3421`
**ステータス:** Open | **ラベル:** car, honda, fingerprint

**概要:** 2024 CR-V カナダ仕様の fingerprint を追加（本文はルート ID のみ）。

**直近のアクティビティ:** Car behavior report（bot）。

**変更ファイル:** `opendbc/car/honda/fingerprints.py`（+1/-0）。

**次のアクション:** マージ判断。

---

### ![Issue](icons/issue-opened.svg) [[$1000 bounty] Kia EV9 lateral port](https://github.com/commaai/opendbc/issues/1175) `#1175`
**ステータス:** Open | **ラベル:** car, hyundai, car port

**概要:** Kia EV9 の lateral 専用 port（longitudinal は概念実証）をマスターへマージするバウンティ。最大 lateral acceleration の保証、フォルト/予期せぬアラートなし、滑らかで安全なユーザー操舵ブレンドが要件。

**直近のアクティビティ:** @MyTH-zyxeon が状況確認をコメント。既存ドラフト PR #1674（Kia EV9 2024 HDA2 & LFA2）や、別ユーザーのバウンティ請負表明について質問している。

**次のアクション:** 既存ドラフトとの関係整理と担当調整。

---

## commaai/panda

> リポジトリ構造のリファクタに関するバウンティ Issue が1件。

### ![Issue](icons/issue-opened.svg) [[$500 Bounty] Refactor panda to use source + header file structure](https://github.com/commaai/panda/issues/2171) `#2171`
**ステータス:** Closed | **ラベル:** refactor, cleanup, bounty

**概要:** 現状の「すべてがヘッダーファイル」のスタイルから、ソースとヘッダーを分離する構造へ移行するバウンティ。import 順序や `*_definitions.h` の整理、ドライバ追加の容易化が狙い。最小限の変更でクリーンな PR を求める。

**直近のアクティビティ:** @varshneydevansh が、ボットをそのまま使っているのか単なるコピペなのかと問い、LLM による低品質な投稿（slop）が多いとコメント。

**次のアクション:** クローズ済み。

---

## neovim/neovim

> Lua 非同期 API の新規提案や net/transport ログの分離（LSP ログ改善）、treesitter/filetype のドキュメント・ランタイム更新、release-0.12 へのバックポートが活発。floatwin やビルド（zig 0.17、luajit）周りの修正も並ぶ。

### ![PR](icons/git-pull-request.svg) [Async API](https://github.com/neovim/neovim/pull/40056) `#40056`
**ステータス:** Open

**概要:** Lua 向けにチャネルベースの非同期 API を実装する。`vim._async` プロトタイプを置き換え、より整理されたコルーチンモデルを提供。`vim.async.spawn` で追跡されるデタッチコルーチンを生成し、resume 境界を越えた yield を安全にする。`vim.async.chan` でバッファ付き通信チャネルを作る。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/async.lua`（+430）、`runtime/doc/lua.txt`（+226）、`test/functional/lua/async_spec.lua` ほか 6 ファイル。

**次のアクション:** レビューと API 設計の確認。

---

### ![PR](icons/git-pull-request.svg) [docs: misc](https://github.com/neovim/neovim/pull/39908) `#39908`
**ステータス:** Merged | **ラベル:** documentation

**概要:** ドキュメントの細かな修正をまとめた PR。Issue #39921 を修正。

**直近のアクティビティ:** @justinmk と @ofseed のコメントレビューあり。

**変更ファイル:** `runtime/doc/lua.txt`、`runtime/doc/api.txt`、`runtime/doc/diagnostic.txt` ほか 18 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Doc: `vim.diagnostic.GetOpts` doesn't match `vim.diagnostic.Set`](https://github.com/neovim/neovim/issues/39921) `#39921`
**ステータス:** Closed | **ラベル:** documentation

**概要:** `vim.diagnostic.GetOpts` の `lnum` は 0-indexed だが、`vim.diagnostic.Set` の説明と矛盾し、明示されていない。`nvim_win_get_cursor` が返す行（1-indexed）との整合で混乱が生じる。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #39908 でドキュメント修正されクローズ）。

**次のアクション:** 完了済み（修正・クローズ）。

---

### ![PR](icons/git-pull-request.svg) [fix(float): title/footer shows printable control chars](https://github.com/neovim/neovim/pull/40047) `#40047`
**ステータス:** Open | **ラベル:** floatwin

**概要:** float の title/footer に含まれるリテラルなタブ（など制御文字）が印字可能化されず正しく表示されない問題を修正。`'statusline'` 同様に表示用の正規化（タブを `^I` に）を行い、`parse_virt_text()` を untab でパラメータ化して幅を再計算する。Issue #40030 の修正。

**直近のアクティビティ:** @superatomic が `:set display+=uhex` 使用時に出力が壊れる（`<<09><09>c`）と指摘し、@glepnir がパース時に正規化しており再評価はしないと説明、最終的に @superatomic は「良さそう」と納得。@justinmk のコメントレビューあり。

**変更ファイル:** `src/nvim/api/win_config.c`、`src/nvim/api/extmark.c`、`test/functional/ui/float_spec.lua` ほか 5 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [feat(keymap): add `force` to control whether to override present mapping](https://github.com/neovim/neovim/pull/36394) `#36394`
**ステータス:** Open | **ラベル:** lua

**概要:** 既存マッピングがない場合のみマッピングを作る簡便な手段がない問題に対応。プラグインがユーザーのマッピングを上書きしないようにするのに有用。`vim.fn.maparg()` はバッファ/グローバルを区別せず信頼できないという背景がある。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/keymap.lua`、`runtime/doc/lua.txt`、`test/functional/lua/vim_spec.lua` ほか。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [refactor(net): parameterize transport logging](https://github.com/neovim/neovim/pull/40022) `#40022`
**ステータス:** Open | **ラベル:** refactor, lsp

**概要:** `vim.net.transport` のロギングが LSP ロギングに結合している問題を解消し、`vim.Log` オブジェクトをパラメータ化して `lsp.log` 以外にも記録できるようにする。将来 LSP サーバーごとにログを分離する布石。#38525 が必要とするため切り出した。

**直近のアクティビティ:** @justinmk が asan のメモリリーク検出ログを共有しつつ APPROVED（リベースが必要とコメント）。

**変更ファイル:** `runtime/lua/vim/lsp/rpc.lua`、`runtime/lua/vim/lsp/log.lua`、`runtime/lua/vim/net/_transport.lua`。

**次のアクション:** メモリリーク対応とリベース。

---

### ![PR](icons/git-pull-request.svg) [fix(autocmd): never show autocommand windows](https://github.com/neovim/neovim/pull/40032) `#40032`
**ステータス:** Merged | **ラベル:** display, events

**概要:** autocommand window を使う autocmd 内での再描画でちらつきが発生する問題を、autocommand window を非表示にすることで解決。

**直近のアクティビティ:** @justinmk と @luukvbaal のコメントレビューあり。

**変更ファイル:** `src/nvim/grid.c`、`src/nvim/autocmd.c`、`src/nvim/window.c` ほか。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(lua): support table for `lhs` of `vim.keymap.*`](https://github.com/neovim/neovim/pull/39948) `#39948`
**ステータス:** Merged | **ラベル:** lua

**概要:** 同じ動作を複数キーに割り当てるのが冗長な問題に対応し、`vim.keymap.set` の `lhs` にテーブルを渡せるようにする（例: `{ 'j', '<Down>' }`）。

**直近のアクティビティ:** @justinmk がドキュメント更新は PR #39908 で行うとコメント。@seandewar のレビューあり。

**変更ファイル:** `runtime/lua/vim/keymap.lua`、`runtime/doc/lua.txt`、`test/functional/lua/vim_spec.lua` ほか。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Extra blank line at top and bottom of window when tmux status-position top is set](https://github.com/neovim/neovim/issues/40049) `#40049`
**ステータス:** Closed

**概要:** Neovim 0.12 を tmux split 内で `status-position top` 設定時に使うと、各ウィンドウ上部に空行、ステータスライン下に灰色の行が出る。フルスクリーン時や `status-position bottom` では発生しない。

**直近のアクティビティ:** 報告者 @stoddart は再現できなかったことを認め、@mikejmcguirk も Mint で再現せずとコメント。

**次のアクション:** 再現不能のためクローズ。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump luajit to 8f421c81e](https://github.com/neovim/neovim/pull/39994) `#39994`
**ステータス:** Open | **ラベル:** build

**概要:** luajit を更新。macOS の enum 呼び出し規約修正、各種 sanitizer 警告の抑制、プロファイラスレッドのレース回避、FFI の ABI/呼び出し規約修正などを含む。

**直近のアクティビティ:** @clason が新コミットで別の macOS unittest 失敗が出たとコメント。

**変更ファイル:** `cmake.deps/deps.txt`（+2/-2）。

**次のアクション:** macOS unittest 失敗の調査。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: runtime file updates](https://github.com/neovim/neovim/pull/40054) `#40054`
**ステータス:** Merged | **ラベル:** vim-patch, filetype

**概要:** vim のランタイムファイル更新を取り込む。karel 構文項目の追加、Kawasaki Robots ファイル（`*.pg`）の filetype 検出などを含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/syntax/karel.vim`、`runtime/lua/vim/filetype.lua`、`runtime/doc/filetype.txt` ほか 9 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40055) `#40055`
**ステータス:** Open（ドラフト）| **ラベル:** vim-patch

**概要:** vim パッチ取り込み状況を `version.c` に反映する自動生成 PR（GTK4 スクロールバー、libvterm の XTQMODKEYS 等の項目）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/nvim/version.c`（+25/-33）。

**次のアクション:** 関連 vim-patch のマージに伴う更新。

---

### ![PR](icons/git-pull-request.svg) [refactor(globals): group search state into SearchState struct](https://github.com/neovim/neovim/pull/39485) `#39485`
**ステータス:** Open | **ラベル:** refactor

**概要:** `globals.h` が約 283 個の素の `EXTERN` シンボルを全翻訳単位に公開している問題に対し、`search_defs.h` を導入して検索/ハイライト系の 8 グローバルを `SearchState` 構造体にまとめる。#39481 の最初の段階的スライス。

**直近のアクティビティ:** 30 日間情報未提供のため自動クローズ（bot）。@justinmk は方向性を評価しつつ説明の簡潔化を要望（APPROVED）、@zeertzjq は新コメントが旧来より分かりにくいと DISMISSED。

**変更ファイル:** `src/nvim/ex_getln.c`、`src/nvim/drawline.c` ほか 17 ファイル。

**次のアクション:** コメントの可読性改善と再オープン判断。

---

### ![PR](icons/git-pull-request.svg) [feat(validate): show |nil in error message for optional params](https://github.com/neovim/neovim/pull/40043) `#40043`
**ステータス:** Open | **ラベル:** needs:response, lua

**概要:** `vim.validate` のエラーメッセージが optional パラメータで誤解を招く問題に対応。`expected string, got number` を `expected string|nil, got number` のように `nil` も許容される旨を示す。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/_core/shared.lua`、各種 spec ファイル。

**次のアクション:** レビュー（needs:response）。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): N/A test files](https://github.com/neovim/neovim/pull/40045) `#40045`
**ステータス:** Merged | **ラベル:** build, vim-patch

**概要:** Neovim に該当しない vim のテストファイルを N/A リストに追加。

**直近のアクティビティ:** @zeertzjq が APPROVED。

**変更ファイル:** `scripts/vim_na_files.txt`、`scripts/vim_na_regexp.txt`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(runtime): add directory buffer for editing directories](https://github.com/neovim/neovim/pull/39723) `#39723`
**ステータス:** Open | **ラベル:** runtime

**概要:** `:edit <dir>` や `nvim <dir>` が netrw に依存している点に対し、`filetype=directory` のビルトイン Lua ディレクトリ一覧を追加する。netrw は `:Explore`・リモートパス・アーカイブ・ファイル操作向けに残す。

**直近のアクティビティ:** @justinmk が `dir.lua` への改名を提案し、上記 UI に関する議論の解決を求めている。30 件のレビューあり。

**変更ファイル:** `runtime/lua/nvim/directory.lua`（+328）、`runtime/plugin/directory.lua`、`runtime/doc/plugins.txt` ほか 9 ファイル。

**次のアクション:** 命名と UI 議論の解決。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(vim.fs): fs.dir() may return nil "type" on some filesystems](https://github.com/neovim/neovim/pull/40053) `#40053`
**ステータス:** Merged | **ラベル:** filesystem, target:release

**概要:** #39749 を release-0.12 にバックポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/fs.lua`、`test/functional/lua/fs_spec.lua`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(treesitter): conceal backslash in markdown inline](https://github.com/neovim/neovim/pull/39993) `#39993`
**ステータス:** Open | **ラベル:** needs:response, treesitter

**概要:** Markdown inline のバックスラッシュエスケープが視覚的に表示される問題に対し、`@conceal` クエリで隠す。エスケープは `@string.escape` を使わなくなる。

**直近のアクティビティ:** @justinmk が CI 失敗の調査を依頼し、`@conceal` は Nvim 固有機能なので変更自体は問題なさそうとコメント。

**変更ファイル:** `runtime/queries/markdown_inline/highlights.scm`、`runtime/doc/news.txt`、`test/functional/treesitter/highlight_spec.lua`。

**次のアクション:** CI 失敗の解消。

---

### ![PR](icons/git-pull-request.svg) [fix(vim.fs): fs.dir() may return nil "type" on some filesystems](https://github.com/neovim/neovim/pull/39749) `#39749`
**ステータス:** Merged | **ラベル:** filesystem

**概要:** 一部のファイルシステムで `uv.fs_scandir_next` がエントリ型に `nil` を返し、`M.del` などの下流処理が誤動作する問題に対応。`etype` が `nil` の場合 `uv.fs_lstat` にフォールバックし、それも失敗すれば "unknown" にする。

**直近のアクティビティ:** バックポート PR #40053 の作成成功を bot が報告。@echasnovski のレビュー（CHANGES_REQUESTED 含む）あり。

**変更ファイル:** `runtime/lua/vim/fs.lua`、`test/functional/lua/fs_spec.lua`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [vim.pack prompts to reinstall all plugins on every launch](https://github.com/neovim/neovim/issues/39612) `#39612`
**ステータス:** Closed | **ラベル:** filesystem, packages

**概要:** `vim.pack` が起動のたびに全プラグインの再インストールを促す問題。`vim.fs.dir()` が libuv 経由で取得したエントリ型に依存しており、型が取れないと未インストール扱いになっていた。

**直近のアクティビティ:** 直近 24 時間のコメントなし（#39749 の修正に伴いクローズ）。

**次のアクション:** 完了済み（修正・クローズ）。

---

### ![PR](icons/git-pull-request.svg) [fix(cmdline): boolean prompt for cmdline_show event](https://github.com/neovim/neovim/pull/40052) `#40052`
**ステータス:** Open（ドラフト）

**概要:** `cmdline_show` イベントで空のプロンプトとプロンプトなしを区別できない問題に対し、プロンプト未アクティブ時に `cmdline_show.prompt` を false にする。空の `input()` の前のメッセージが表示されない問題も併せて修正。

**直近のアクティビティ:** @luukvbaal が、新パラメータを追加すべきか、イベントパラメータ型の変更が #31903 の範囲かを判断しかねるとコメント。

**変更ファイル:** `runtime/lua/vim/_core/ui2/cmdline.lua`、`src/nvim/ex_getln.c`、`runtime/doc/api-ui-events.txt` ほか 8 ファイル。

**次のアクション:** API 設計判断。

---

### ![Issue](icons/issue-opened.svg) [[doc] vim.filetype.add should clarify how extension works](https://github.com/neovim/neovim/issues/40044) `#40044`
**ステータス:** Open | **ラベル:** documentation, filetype

**概要:** `vim.filetype.add` の extension テーブルで「service.erb」のような複数要素拡張子が効かないことが分かりにくい、というドキュメント改善要望。

**直近のアクティビティ:** @justinmk が @clason @gpanders にメンション。

**次のアクション:** ドキュメント整備。

---

### ![Issue](icons/issue-opened.svg) [Windows: non-admin install (without an administrator account)](https://github.com/neovim/neovim/issues/40034) `#40034`
**ステータス:** Open | **ラベル:** platform:windows, distribution, has:plan

**概要:** Windows インストーラがどのパスでも管理者アカウントを要求する問題。現在ユーザーへのインストールを選べるようにしてほしいという要望。

**直近のアクティビティ:** @justinmk が過去のユーザーローカルインストール試行（PR #22949 で技術的理由により revert）と `ALLUSERS=1` 案に触れつつ、報告内容の正確な再現手順を求めた。

**次のアクション:** 再現手順の確認と方針検討。

---

### ![PR](icons/git-pull-request.svg) [fix(build.zig): do not depend on b.install_path on configure time](https://github.com/neovim/neovim/pull/40048) `#40048`
**ステータス:** Open

**概要:** zig 0.17 で `b.install_path` が `pub fn build()` 内で参照できなくなることに備える。ランタイムパスをバイナリにハードコードするビルドは明示的に指定する形にする。

**直近のアクティビティ:** @justinmk が APPROVED。

**変更ファイル:** `build.zig`、`test/run_tests.zig`、`test/harness.lua` ほか。

**次のアクション:** 完了見込み（承認済み）。

---

### ![Issue](icons/issue-opened.svg) [Quickfix list incremented after deleting error line and undoing deletion](https://github.com/neovim/neovim/issues/40038) `#40038`
**ステータス:** Open | **ラベル:** bug-vim, needs:vim-patch, quickfix

**概要:** quickfix のエラー行をバッファで削除して undo すると、当該 quickfix エラーの行番号がインクリメントされ正しい行を指さなくなる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** vim-patch の必要性確認。

---

### ![PR](icons/git-pull-request.svg) [feat(img): relative images (editor & buffer) using unicode placeholder](https://github.com/neovim/neovim/pull/39496) `#39496`
**ステータス:** Open | **ラベル:** ci:skip-news

**概要:** `vim.ui.img.Opts` に `relative`（`ui`/`editor`/`buffer`）と `buf` フィールドを追加。#39432 をクローズ。#39484 の上に構築しているため先にそちらの完成が必要。

**直近のアクティビティ:** @justinmk がレビューの時間を取れておらず、YOLO マージには慎重だとコメント。11 件のレビューあり。

**変更ファイル:** `runtime/lua/vim/ui/img/_kitty.lua`、`runtime/lua/vim/ui/img/_util.lua`、`test/functional/ui/img_spec.lua` ほか。

**次のアクション:** #39484 の完成とレビュー。

---

### ![Issue](icons/issue-opened.svg) [Option to force status height to 100%](https://github.com/neovim/neovim/issues/33052) `#33052`
**ステータス:** Open | **ラベル:** ui, statusline

**概要:** ステータス記号として縦線を使い git signs で差分を見たいが、ウィンドウセパレータとの間に隙間が出て見栄えが悪い。ステータス高さを 100%にする選択肢が欲しいという要望。

**直近のアクティビティ:** @justinmk が `"100%"` や行数指定をサポートする `'statuslineheight'` の導入を検討しつつ、まずは `"fill"` が良いかもしれないとコメント。

**次のアクション:** 具体的な設計案の検討。

---

### ![PR](icons/git-pull-request.svg) [refactor: rename `vim.transport` to `vim.net.transport`](https://github.com/neovim/neovim/pull/40021) `#40021`
**ステータス:** Merged | **ラベル:** lsp

**概要:** #39000 で `vim.transport` を `vim.net.transport` へ移動した際に忘れていたクラス名のリネームを行う。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/lua/vim/lsp/rpc.lua`、`runtime/lua/vim/net/_transport.lua`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Hide the vertical split bar](https://github.com/neovim/neovim/issues/5275) `#5275`
**ステータス:** Closed | **ラベル:** api, ui

**概要:** 分割ウィンドウ間の縦バーを消すオプションが欲しいという古い機能要望。

**直近のアクティビティ:** @JJJHolscher が、横分割はステータスラインで分離できるが縦分割のバーは無骨だとコメント。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [backport: docs(treesitter): describe pattern_id of captures](https://github.com/neovim/neovim/pull/40051) `#40051`
**ステータス:** Merged | **ラベル:** treesitter, target:release

**概要:** #40046 を release-0.12 にバックポート。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `runtime/doc/treesitter.txt`、`runtime/lua/vim/treesitter.lua`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [docs(treesitter): describe pattern_id of captures](https://github.com/neovim/neovim/pull/40046) `#40046`
**ステータス:** Merged | **ラベル:** treesitter

**概要:** `vim.treesitter.get_captures_at_pos` が返す `pattern_id` が未ドキュメントだった点を文書化。

**直近のアクティビティ:** バックポート PR #40051 の作成成功を bot が報告。

**変更ファイル:** `runtime/doc/treesitter.txt`、`runtime/lua/vim/treesitter.lua`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [ci: bump github/codeql-action from 4.35.5 to 4.36.0](https://github.com/neovim/neovim/pull/40050) `#40050`
**ステータス:** Open | **ラベル:** ci, dependencies, github_actions

**概要:** dependabot による github/codeql-action の更新（4.35.5 → 4.36.0）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/codeql.yml`（+2/-2）。

**次のアクション:** CI 確認とマージ。

---

## nvim-telescope/telescope.nvim

> winfixbuf ウィンドウや LSP シンボルフィルタに関する小さな Issue が動いている。

### ![Issue](icons/issue-opened.svg) [Avoid opening in winfixbuf windows](https://github.com/nvim-telescope/telescope.nvim/issues/3135) `#3135`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `winfixbuf` のウィンドウから telescope を開くとそのウィンドウでファイルを編集できない。`winfixbuf` のウィンドウなら別のウィンドウを選ぶようにしてほしいという要望。

**直近のアクティビティ:** @Kaiser-Yang が `get_selection_window` で固定でないウィンドウを探す実装例を共有。

**次のアクション:** 設定または本体での対応検討。

---

### ![Issue](icons/issue-opened.svg) [`lsp_document_symbols` and `lsp_workspace_symbols`: Default keybind `<C-l>` erroneously including icon](https://github.com/nvim-telescope/telescope.nvim/issues/3675) `#3675`
**ステータス:** Open | **ラベル:** bug

**概要:** `lsp_document_symbols` / `lsp_workspace_symbols` のデフォルトキー `<C-l>`（型でフィルタ）がアイコンを含んでしまいフィルタが壊れる。アイコンを除くか、フィルタ側でアイコンを無視すれば直る。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** フィルタ処理の修正。

---

## ghostty-org/ghostty

> GTK の quick-terminal 関連バグ修正や Kitty 由来の新プロトコル対応提案、エージェント向けの AGENTS.md シンボリックリンク追加が見られる。

### ![Issue](icons/issue-opened.svg) [Implement the Kitty Drag-and-Drop Protocol (OSC 72)](https://github.com/ghostty-org/ghostty/issues/12852) `#12852`
**ステータス:** Open | **ラベル:** vt

**概要:** Kitty が新たに公開したドラッグ＆ドロップ・プロトコル（OSC 72）の実装提案。プロトコルはまだ安定していないが、パーサーの作成から始め apprt と統合していく方針。大きなペイロードのために状態機械でパケットを連結する必要がある。

**直近のアクティビティ:** @AnirudhG07 が yazi も Kitty 向けにこの機能をサポートしたと触れ、Ghostty への実装に期待を寄せた。

**次のアクション:** パーサー実装の着手。

---

### ![PR](icons/git-pull-request.svg) [gtk: fix context menu hiding quick-terminal](https://github.com/ghostty-org/ghostty/pull/12843) `#12843`
**ステータス:** Merged | **ラベル:** gtk

**概要:** quick-terminal 内で右クリックのコンテキストメニューを開くと、autohide 有効時に quick-terminal が隠れてしまう問題（#12783）を修正。GDK のフォーカス変更イベントによる一時的なフォーカス喪失が原因。

**直近のアクティビティ:** @jcollie が一度 CHANGES_REQUESTED の後 APPROVED、作者 @dkinzler のコメントレビューあり。

**変更ファイル:** `src/apprt/gtk/class/window.zig`（+68/-19）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [agents: symlink CLAUDE.md to AGENTS.md](https://github.com/ghostty-org/ghostty/pull/12861) `#12861`
**ステータス:** Merged

**概要:** Claude Code が AGENTS.md をサポートしないため、多くのリポジトリで行われているように CLAUDE.md を AGENTS.md にシンボリックリンクする。

**直近のアクティビティ:** @bo2themax と @jcollie が APPROVED。

**変更ファイル:** `CLAUDE.md`（+1/-0）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(cursor): add vintage cursor style with configurable height](https://github.com/ghostty-org/ghostty/pull/12860) `#12860`
**ステータス:** Closed

**概要:** セルを下から埋める部分高さブロックの新カーソルスタイル `vintage`（MS-DOS など旧来端末風）を追加。`cursor-style-vintage-height`（1-100%、既定 25）で高さを設定。

**直近のアクティビティ:** ghostty-vouch bot が、本プロジェクトは PR 作者の vouch を必要とし作者が未登録のため自動クローズする旨をコメント。

**変更ファイル:** `src/config/Config.zig`、`src/font/sprite/draw/special.zig`、`src/renderer/cursor.zig` ほか 12 ファイル。

**次のアクション:** vouch 取得後の再提出。

---

## astral-sh/uv

> `uv check`（`ty` 実行）の導入と認証（logout、Google Artifact Registry）周りの機能追加、リリースビルドの高速化（mold/ICF）と CI ランナー整備、テストハーネス分割など内部改善が活発。

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate の更新と検出依存関係を一覧する自動 Issue。長期更新のない（unmaintained の可能性がある）35 の放置依存関係などが列挙される。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 依存関係の棚卸し（自動管理）。

---

### ![PR](icons/git-pull-request.svg) [Split uv integration tests into smaller test harnesses](https://github.com/astral-sh/uv/pull/19603) `#19603`
**ステータス:** Open（ドラフト）

**概要:** test-edit-test ループ改善の試作。統合テストを専用の `uv-integration` crate に移し、本番変更で統合テストハーネスを再リンクせずに済むようにする。さらに `uv-integration` を 12 の実行ファイルに分割し、テスト変更時の再リンクを該当コマンドのみに限定（例: `tool_list.rs` テスト編集が 7.05 秒→3.39 秒）。

**直近のアクティビティ:** @charliermarsh と @konstin が分割粒度のトレードオフを議論。test inventory bot が追加 2920・削除 2918 テストを報告。

**変更ファイル:** `crates/uv-integration/Cargo.toml`、`.config/nextest.toml`、`CONTRIBUTING.md` ほか 108 ファイル。

**次のアクション:** 分割粒度の合意と本実装。

---

### ![PR](icons/git-pull-request.svg) [Log out without `--username` if username is unambiguous](https://github.com/astral-sh/uv/pull/18975) `#18975`
**ステータス:** Open | **ラベル:** documentation, enhancement

**概要:** ユーザー名が一意に定まる場合は `--username` なしでログアウトできるようにする。当面はプレーンテキストバックエンドのみ対応（システムネイティブは (service, username) でキー付けされるため）。#18973 をクローズ。

**直近のアクティビティ:** @zanieb と @charliermarsh のコメントレビューあり。

**変更ファイル:** `crates/uv/src/commands/auth/logout.rs`、`crates/uv-auth/src/store.rs`、`crates/uv/tests/it/auth.rs` ほか。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix(requirements): detect uv lockfiles in -r and show actionable error](https://github.com/astral-sh/uv/pull/19617) `#19617`
**ステータス:** Open

**概要:** `.lock` ファイルを `-r` に渡したとき（例: `uv pip install -r action.py.lock`）、requirements.txt として解析され分かりにくい pep508 エラーになる問題を修正。`from_requirements_txt` 等に `.lock` 拡張子チェックを追加し実用的なエラーを出す。#16192 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-requirements/src/sources.rs`（+27/-0）。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix file locks on Android](https://github.com/astral-sh/uv/pull/18323) `#18323`
**ステータス:** Merged

**概要:** Android でのファイルロックを修正。#14574 と termux 側の問題の一部で、termux の downstream パッチを不要にする。

**直近のアクティビティ:** @RalfJung が、Rust 1.98 リリース後は標準のファイルロックが Android でも動くはずとコメント。@zsol が APPROVED。

**変更ファイル:** `crates/uv-fs/src/locked_file.rs`（+83/-2）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [uv python install fails with 'os error 448' on Windows with OneDrive Files On-Demand](https://github.com/astral-sh/uv/issues/19616) `#19616`
**ステータス:** Open

**概要:** Windows で OneDrive Files On-Demand が有効なとき、インストール先が OneDrive フォルダ外でも `uv python install` が「Python minor version link directory の作成失敗（os error 448、信頼されないマウントポイント）」で失敗する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** マウントポイント検出ロジックの調査。

---

### ![PR](icons/git-pull-request.svg) [Use ICF in release builds](https://github.com/astral-sh/uv/pull/19615) `#19615`
**ステータス:** Open（ドラフト）

**概要:** リリースビルドで ICF（同一コードの折り畳み）を使う。base ブランチは `zb/use-mold-in-release-builds`。

**直近のアクティビティ:** @zsol がより積極的な ICF の効果や、クラッシュレポート混乱のリスクとのトレードオフについて議論。@zanieb が計測スクリーンショットを共有。

**変更ファイル:** `.github/workflows/build-release-binaries.yml`（+8/-0）。

**次のアクション:** ICF 強度とリスクの判断。

---

### ![Issue](icons/issue-opened.svg) [Add xonsh support for the uv venv command (add activate.xsh)](https://github.com/astral-sh/uv/issues/19606) `#19606`
**ステータス:** Closed

**概要:** `uv venv` が virtualenv にある `activate.xsh`（xonsh 用）を生成しないので追加してほしい要望。

**直近のアクティビティ:** @zhcn000000 がテンプレート追加方法を提案。@woodruffw は tab 補完が `clap_complete` 由来のため、xonsh 対応は上流（clap）での導入が先と説明し、上流追跡に委ねてクローズ。

**次のアクション:** 上流（clap）での対応待ち。

---

### ![PR](icons/git-pull-request.svg) [Update dependencies](https://github.com/astral-sh/uv/pull/18980) `#18980`
**ステータス:** Open | **ラベル:** internal, build:skip-release

**概要:** Renovate によるコンテナイメージ（alpine、amazonlinux、pyston）の digest ピン更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test-smoke.yml`、`.github/workflows/test-system.yml`。

**次のアクション:** マージ。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/19531) `#19531`
**ステータス:** Open（ドラフト）

**概要:** Python リリースの自動同期更新。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-python/download-metadata.json`（+30/-64）。

**次のアクション:** マージ。

---

### ![Issue](icons/issue-opened.svg) [uvx offline fallback mode?](https://github.com/astral-sh/uv/issues/10380) `#10380`
**ステータス:** Open | **ラベル:** enhancement, cache

**概要:** オフライン環境で代替インデックスを使う際、インデックス停止時に `uvx` を呼ぶスクリプトが `--offline` を渡せず失敗する。途中でレジストリが落ちたらキャッシュにフォールバックしてほしい要望。

**直近のアクティビティ:** @timlehr が `uvx --offline-fallback` や `UV_OFFLINE_FALLBACK=1` といった狭いスコープの案に +1。

**次のアクション:** フォールバックモードの設計検討。

---

### ![PR](icons/git-pull-request.svg) [Add Google Artifact Registry credential retrieval](https://github.com/astral-sh/uv/pull/19611) `#19611`
**ステータス:** Open（ドラフト）

**概要:** Google Artifact Registry の認証情報取得に対応する（本文記載なし）。

**直近のアクティビティ:** test inventory bot が追加 14 テストを報告。@zanieb のコメントレビューあり。

**変更ファイル:** `crates/uv-auth/src/providers.rs`（+575）、`crates/uv-auth/src/middleware.rs`、`docs/guides/integration/google.md` ほか 9 ファイル。

**次のアクション:** ドラフト完成とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Complete PEP 751 multi-use lock file for pylock.toml export](https://github.com/astral-sh/uv/pull/14728) `#14728`
**ステータス:** Closed（ドラフト）

**概要:** `pylock.toml` エクスポート時に、これまで extras・依存グループ・環境フォーク等を省いた single-use ロックファイルを生成していたのを、完全な multi-use ロックファイルに変更する。extras/グループを合成 extra 名として内部表現する。

**直近のアクティビティ:** @gaborbernat と @konstin のコメントレビューあり。

**変更ファイル:** `crates/uv-resolver/src/lock/export/pylock_toml.rs`、`crates/uv-resolver/src/lock/export/marker_conversion.rs`（+287）ほか 16 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Use mold in release builds](https://github.com/astral-sh/uv/pull/19613) `#19613`
**ステータス:** Open（ドラフト）

**概要:** リリースビルドで mold リンカを使う。主に #19615 のためだが、Linux リリースビルドで約 10%の高速化も得られる（aarch64 で約 19%短縮など）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/ci.yml`、`scripts/install-mold.sh`、`scripts/install-cargo-extensions.sh`。

**次のアクション:** ドラフト完成。

---

### ![PR](icons/git-pull-request.svg) [[check] you're not special, ty](https://github.com/astral-sh/uv/pull/19609) `#19609`
**ステータス:** Open | **ラベル:** enhancement

**概要:** #19605 の後続で、ty を「uv check が行うことの 1 つ」と位置づける設計へ進める。`--` での任意引数転送のサポートを削除するのが主。

**直近のアクティビティ:** test inventory bot が追加 3 テストを報告。@Gankra のコメントレビューあり。

**変更ファイル:** `crates/uv/src/commands/project/check.rs`、`crates/uv/src/commands/project/check/ty.rs`、`crates/uv-cli/src/lib.rs` ほか 7 ファイル。

**次のアクション:** レビューと設計合意。

---

### ![PR](icons/git-pull-request.svg) [Enable safe ICF for macOS arm64 release builds](https://github.com/astral-sh/uv/pull/19614) `#19614`
**ステータス:** Closed（ドラフト）

**概要:** macOS arm64 リリースビルドで safe ICF を有効化する。

**直近のアクティビティ:** @zanieb が #19615 で作業中のため WIP を push したとコメント、@charliermarsh が macOS arm64 限定の理由を要調査とコメント。

**変更ファイル:** `.github/workflows/build-release-binaries.yml`（+4/-0）。

**次のアクション:** クローズ済み（#19615 に集約）。

---

### ![PR](icons/git-pull-request.svg) [Use runners from namespace](https://github.com/astral-sh/uv/pull/19604) `#19604`
**ステータス:** Open（ドラフト）

**概要:** namespace のランナーを使うよう CI を変更する（本文記載なし）。

**直近のアクティビティ:** @zanieb のコメントレビューあり。

**変更ファイル:** `.github/workflows/build-release-binaries.yml`、`.github/workflows/release.yml`、`.github/workflows/test-integration.yml` ほか 13 ファイル。

**次のアクション:** ドラフト完成。

---

### ![PR](icons/git-pull-request.svg) [Use Rust 1.94.0 features](https://github.com/astral-sh/uv/pull/19612) `#19612`
**ステータス:** Merged | **ラベル:** internal

**概要:** Rust 1.94.0 の機能を利用するよう更新（#19600 の続き）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `crates/uv-fs/src/path.rs`、`crates/uv-resolver/src/lock/mod.rs` ほか 7 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add `uv check` to run `ty` from uv](https://github.com/astral-sh/uv/pull/19605) `#19605`
**ステータス:** Merged | **ラベル:** enhancement

**概要:** `ty check` を取得・実行する最小実装。現状はメンバー選択情報を ty に渡さず、cwd で ty をダウンロード・実行し任意のフラグを渡せる、というセマンティクス。今後の「モノリシックな linter コマンド」構想には遠いが反復のため先行マージ。

**直近のアクティビティ:** CodSpeed bot が性能変化なしを報告、test inventory bot が追加 10 テストを報告。@zanieb のコメントレビューあり。

**変更ファイル:** `crates/uv/src/commands/project/check.rs`（+347）、`crates/uv-cli/src/lib.rs`（+192）、`crates/uv-bin-install/src/lib.rs` ほか。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [`uv run --python 3.12 python` trashes my project .venv directory](https://github.com/astral-sh/uv/issues/19610) `#19610`
**ステータス:** Open | **ラベル:** question

**概要:** `uv run --python 3.X python` を一時 REPL として使っていたが、`pyproject.toml` 管理のプロジェクトディレクトリで実行したところプロジェクトの `.venv` が指定版（3.12）で黙って置き換えられ驚いた、という報告。

**直近のアクティビティ:** @zanieb が、uv は通常 `.venv` を自分が管理する前提だと説明しつつ、uv が作成していない仮想環境を検出するのは妥当かもしれないとコメント。報告者はその検出が重要と返答。

**次のアクション:** uv 非管理の仮想環境検出の検討。

---

### ![PR](icons/git-pull-request.svg) [Add test coverage for both WSL1 and WSL2](https://github.com/astral-sh/uv/pull/19608) `#19608`
**ステータス:** Merged | **ラベル:** testing

**概要:** #19604 の作業中に WSL2 のみテストしており、Windows 2022 で使えない WSL1 が未カバーと気づいたため両方のテストを追加。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/test-integration.yml`（+11/-2）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Run a dependency audit when the user changes their lockfile with `uv add`](https://github.com/astral-sh/uv/issues/19607) `#19607`
**ステータス:** Open | **ラベル:** cli

**概要:** `uv add` でロックファイルを更新するたびに `uv audit` に相当する処理を実行する案。同期時は邪魔せず（`uv audit` は opt-in のまま）、依存追加時にリアルタイムのフィードバックを与える。設定可能かつ opt-in を想定。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** opt-in 設計の検討。

---

### ![Issue](icons/issue-opened.svg) [uv --locked looks in the wrong place for lockfile when symlinked](https://github.com/astral-sh/uv/issues/19222) `#19222`
**ステータス:** Open | **ラベル:** bug

**概要:** `.lock` を伴う uv スクリプトをシンボリックリンク経由で実行すると、uv が権威あるスクリプト位置でなくシンボリックリンクの位置/名前で `.lock` を探す。

**直近のアクティビティ:** @solace-rcampbell が、読み込みは常に canonicalize しフォールバックなし・書き込みは現状維持、という案ではセキュリティは解決するが書き込み挙動が紛らわしいと指摘。

**次のアクション:** 読み書きのパス解決方針の確定。

---

### ![Issue](icons/issue-opened.svg) [Don't we still have something like `uv sync --no-venv` or `uv sync --system`?](https://github.com/astral-sh/uv/issues/19602) `#19602`
**ステータス:** Closed | **ラベル:** question

**概要:** Docker コンテナ内で venv を使いたくない場合の方法に関する質問。

**直近のアクティビティ:** @JustBipin が、Django のチュートリアルに従い Docker 内で venv 不要と学んだが uv が venv を作ってしまうと説明。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [ci: use `uv publish` for astral-test-pypa-gh-action](https://github.com/astral-sh/uv/pull/19526) `#19526`
**ステータス:** Closed

**概要:** `pypa/gh-action-pypi-publish` への依存をやめ、`uv publish` を直接使う。CI 実行のたびに `uv publish` をドッグフーディングでき、Renovate の回避策も不要になる。#15276 をクローズ予定だった。

**直近のアクティビティ:** github-advanced-security bot のコメントレビューあり。

**変更ファイル:** `.github/workflows/ci.yml`（+12/-8）。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Add in preview](https://github.com/astral-sh/uv/pull/17841) `#17841`
**ステータス:** Open | **ラベル:** enhancement, preview

**概要:**（preview の）`uv init` の既定を src パッケージに切り替える。パッケージ名のエントリポイントを持ち `uv init foo` で実行可能。エントリポイント付きパッケージ／純ライブラリ／フラットな `main.py` アプリの 3 レイアウトを既存フラグから選択する。

**直近のアクティビティ:** @konstin のコメントレビュー、@zsol が APPROVED（数点の対応が必要とコメント）。

**変更ファイル:** `crates/uv/src/commands/project/init.rs`、`crates/uv/src/settings.rs`、`crates/uv-cli/src/lib.rs` ほか 6 ファイル。

**次のアクション:** 指摘事項の対応。

---

## microsoft/vscode

> 起動時に Wayland/KDE で画面共有ダイアログが出る回帰（1.122 系）が複数 Issue で多発。dmitrivMS による dispose/タイマー整理の perf PR 群、Agents/Sessions ウィンドウ周りの benibenj の修正、Copilot Chat・BYOK（独自モデル）・MCP に関する Issue が大半を占める。info-needed の自動クローズ Issue も多数。

### ![PR](icons/git-pull-request.svg) [Clear timeouts/intervals in dispose in core](https://github.com/microsoft/vscode/pull/319126) `#319126`
**ステータス:** Open | **ラベル:** perf

**概要:** core 側で dispose 時にタイマー（timeout/interval）を確実に解除し、リソースリークを防ぐ perf 改善。

**直近のアクティビティ:** CODENOTIFY bot が関係者へ通知。Copilot レビュー bot が 11 ファイルをレビューしコメント。

**変更ファイル:** `src/vs/editor/contrib/suggest/browser/suggestWidget.ts`、`src/vs/platform/quickinput/browser/quickInputList.ts` ほか 11 ファイル。

**次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [VS코드](https://github.com/microsoft/vscode/issues/319137) `#319137`
**ステータス:** Closed | **ラベル:** spam

**概要:** テンプレートのみで内容のない投稿。

**直近のアクティビティ:** bot がスパム/ガイドライン違反として自動クローズ。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [BYOK fails with `apiType: "responses"` but works with `apiType: "completion"`](https://github.com/microsoft/vscode/issues/319136) `#319136`
**ステータス:** Closed

**概要:** VS Code Chat の BYOK で `apiType` を `responses` にするとリクエストが失敗し、`completion` では成功する。失敗は Copilot 拡張側に起因しているように見える。

**直近のアクティビティ:** bot が #253136 の重複としてクローズ。

**次のアクション:** クローズ済み（#253136 を参照）。

---

### ![PR](icons/git-pull-request.svg) [Dispose implicit context attachment label in render disposables](https://github.com/microsoft/vscode/pull/319133) `#319133`
**ステータス:** Open | **ラベル:** perf, chat-input

**概要:** chat 入力の暗黙コンテキスト添付ラベルを再レンダー前にクリアし、DOM/リソースリークを防ぐ。

**直近のアクティビティ:** Copilot レビュー bot がコメント（新規指摘なし）。

**変更ファイル:** `src/vs/workbench/contrib/chat/browser/attachments/implicitContextAttachment.ts`（+1/-1）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Avoid leaking listeners in notebook search results](https://github.com/microsoft/vscode/pull/319130) `#319130`
**ステータス:** Open | **ラベル:** search, perf

**概要:** notebook 検索結果のリスナーを手動の `IDisposable` から親 store に登録する `MutableDisposable` に置き換え、リスナーリークを防ぐ。

**直近のアクティビティ:** Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/workbench/contrib/search/browser/searchTreeModel/searchResult.ts`（+5/-9）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Avoid leaving detached DOM elements in Getting Started](https://github.com/microsoft/vscode/pull/319128) `#319128`
**ステータス:** Open | **ラベル:** perf, getting-started

**概要:** Getting Started のエディタペインで disposable な UI コンポーネントを適切に管理し、切り離された DOM 要素の残留を防ぐ。

**直近のアクティビティ:** Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/workbench/contrib/welcomeGettingStarted/browser/gettingStarted.ts`（+23/-27）。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Follow up on Copilot comments to a previous PR](https://github.com/microsoft/vscode/pull/319135) `#319135`
**ステータス:** Open | **ラベル:** perf

**概要:** PR #319125 への Copilot レビューコメントへのフォローアップ。PHP 検証プロバイダのキャンセル処理などを追加。

**直近のアクティビティ:** Copilot レビュー bot がコメント。

**変更ファイル:** `extensions/php-language-features/src/features/validationProvider.ts`、`extensions/copilot/src/platform/review/vscode/reviewServiceImpl.ts` ほか。

**次のアクション:** マージ判断。

---

### ![Issue](icons/issue-opened.svg) [Mermaid diagrams fail to render after 1.121 upgrade when bierner.markdown-mermaid is still installed](https://github.com/microsoft/vscode/issues/317870) `#317870`
**ステータス:** Open

**概要:** 1.121 へのアップグレード後、`bierner.markdown-mermaid` 拡張が入ったままだと Mermaid 図がレンダリングされない。

**直近のアクティビティ:** @syuuha が、mermaid-chart 拡張（v2.6.6）でも 1.121.0 でレンダー失敗すると報告し関連 Issue を共有。

**次のアクション:** 内蔵 Mermaid と拡張の競合調査。

---

### ![Issue](icons/issue-opened.svg) [ssh remote stuck](https://github.com/microsoft/vscode/issues/319134) `#319134`
**ステータス:** Open | **ラベル:** new release

**概要:** VS Code でリモートプロジェクトを開くとターミナルがフリーズし、ボタン操作に反応しなくなる（macOS、1.122.1）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** リモート接続のハング調査。

---

### ![Issue](icons/issue-opened.svg) [getDisplayMedia called at workbench boot → KDE screen-share dialog on every new window](https://github.com/microsoft/vscode/issues/317948) `#317948`
**ステータス:** Closed | **ラベル:** bug, linux

**概要:** Linux KDE Plasma 6 Wayland で、拡張なしのクリーンプロファイルでも workbench 起動完了時に KDE の「共有内容を選択」ダイアログが出る。明示的に画面録画を起動していないのにプロンプトが出る。

**直近のアクティビティ:** テスト担当の @rzhao271 が VM と新しめのディストロでの再現の教訓を共有。@coredump11 はハッキングを疑うほど怖い不具合だったとコメント。

**次のアクション:** クローズ済み（修正対応）。

---

### ![PR](icons/git-pull-request.svg) [Add BrowserChatToolsAllowedDomains policy to restrict browser chat tools to specific domains](https://github.com/microsoft/vscode/pull/315471) `#315471`
**ステータス:** Open

**概要:** ブラウザ Chat ツールのドメイン許可リストポリシーを追加し、組織が信頼ドメインに限定して利用できるようにする（fetch ツールには影響しない）。設定 `workbench.browser.chatTools.allowedDomains` とエンタープライズポリシー `BrowserChatToolsAllowedDomains` を追加。#315458 を修正。

**直近のアクティビティ:** CODENOTIFY bot が通知、Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/platform/networkFilter/common/domainMatcher.ts`、`src/vs/workbench/contrib/browserView/...` ほか 21 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Fix BYOK models disappearing during startup](https://github.com/microsoft/vscode/pull/319123) `#319123`
**ステータス:** Open

**概要:** 起動時のタイミングレースで、設定済みのカスタムエンドポイント/BYOK モデルが `chat.hasByokModels.lastKnown` が false で復元されることによりモデルピッカーから隠れる問題を修正。非 Copilot の言語モデルプロバイダグループを即時の BYOK シグナルとして扱い、コールドスタートを回帰テストでカバー。#319121 を修正。

**直近のアクティビティ:** Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/workbench/contrib/chat/browser/hasByokModelsContribution.ts`、`src/vs/sessions/contrib/providers/localChatSessions/...` ほか 7 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Avoid creating progress codicon in status bar until needed](https://github.com/microsoft/vscode/pull/319127) `#319127`
**ステータス:** Merged | **ラベル:** perf, workbench-status

**概要:** ステータスバーで進捗 codicon を、実際に進捗表示が有効化されたときだけ生成し、不要な DOM 確保を減らす。

**直近のアクティビティ:** @benibenj が APPROVED、Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/workbench/browser/parts/statusbar/statusbarItem.ts`（+5/-3）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Wayland: VS Code 1.122.0 triggers xdg-desktop-portal ScreenCast permission dialog on startup, 1.121.0 does not](https://github.com/microsoft/vscode/issues/319132) `#319132`
**ステータス:** Open

**概要:** 1.122.0 で起動時に xdg-desktop-portal の ScreenCast 許可ダイアログが出る回帰。1.121.0 では発生しない。

**直近のアクティビティ:** 報告者 @surajskrv が「アプリが画面を共有しようとしています」というダイアログが起動直後に出ること、回帰範囲（1.121.0 動作・1.122.0 不具合）をコメント。

**次のアクション:** ScreenCast 起動の回帰修正（#317948 関連）。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Native LM Studio Integration (like Ollama)](https://github.com/microsoft/vscode/issues/308442) `#308442`
**ステータス:** Open | **ラベル:** feature-request

**概要:** VS Code は Ollama のネイティブ統合を持つが、LM Studio は第三者拡張＋手動設定が必要。ローカル LLM の選択肢として LM Studio のネイティブ統合を求める要望。

**直近のアクティビティ:** @calvinwonghk が賛同。

**次のアクション:** 機能要望のトリアージ。

---

### ![Issue](icons/issue-opened.svg) [Copilot agent model settings silently ignored when model ID format doesn't match internal ID](https://github.com/microsoft/vscode/issues/319131) `#319131`
**ステータス:** Open

**概要:** `github.copilot.chat.askAgent.model` 等に内部モデル ID と一致しない名前を設定すると、警告なしに無視されシステム既定にフォールバックする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 設定不一致時の警告表示の検討。

---

### ![Issue](icons/issue-opened.svg) [Copilot Sessions right-pane (Files) disappears immediately on first message when workspace root is a non-repo multi-root folder](https://github.com/microsoft/vscode/issues/319129) `#319129`
**ステータス:** Open

**概要:** マルチルートワークスペースで、セッションの作業ディレクトリが非 git リポジトリのルートフォルダにスコープされていると、最初のメッセージ送信と同時に右側の「Files」ペインが消え、スピナーが回り続ける。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 非リポジトリのマルチルート時の挙動修正。

---

### ![Issue](icons/issue-opened.svg) [Meta: Request rate limiting](https://github.com/microsoft/vscode/issues/253124) `#253124`
**ステータス:** Open | **ラベル:** under-discussion, chat-rate-limiting

**概要:** chat リクエストがレート制限でブロックされるシナリオを追跡するメタ Issue。

**直近のアクティビティ:** @ifrankwang と @KonradHEXANT が、独自インフラの自前モデル（BYOK）でもセッションがレート制限されるのはおかしいと不満をコメント。

**次のアクション:** BYOK 時のレート制限挙動の整理。

---

### ![Issue](icons/issue-opened.svg) [Screen Sharing Dialog Pops Up on Start Up](https://github.com/microsoft/vscode/issues/319101) `#319101`
**ステータス:** Closed

**概要:** Debian で VS Code を起動すると画面共有ダイアログが出る（1.122.0）。

**直近のアクティビティ:** @gavarial が修正で解決したと報告。bot が最新版（1.122.1）へのアップグレードを案内。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix separator for open in agents window action](https://github.com/microsoft/vscode/pull/319124) `#319124`
**ステータス:** Merged

**概要:**「Open in Agents」ウィンドウアクションのツールバーに一貫してセパレータを入れるよう修正。CSS ベースの縦線スタイリングを廃止。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `src/vs/workbench/browser/parts/titlebar/titlebarPart.ts`、`.../media/openInAgents.css`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Clear timeouts/intervals on dispose in extensions](https://github.com/microsoft/vscode/pull/319125) `#319125`
**ステータス:** Merged | **ラベル:** perf

**概要:** 拡張側でサービス dispose 時にスケジュール済みタイマーをクリアし、ライフサイクルのクリーンアップを改善。

**直近のアクティビティ:** CODENOTIFY bot が通知、Copilot レビュー bot がコメント、@benibenj が APPROVED。

**変更ファイル:** `extensions/git/src/askpass.ts`、`extensions/copilot/src/platform/survey/vscode/surveyServiceImpl.ts` ほか 8 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [preselectModelFromSessionHistory overwrites stored model preference during session type switches](https://github.com/microsoft/vscode/issues/316290) `#316290`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** チャットセッション種別（Panel ↔ CLI 等）を切り替えると、明示選択したモデルが永続ストレージで黙って上書きされる。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Problem to see mermaid](https://github.com/microsoft/vscode/issues/317717) `#317717`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** md プレビューで Mermaid が描画されない、別の拡張か最新更新の影響か分からないという報告（1.121.0、macOS）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Terminal Not Responding](https://github.com/microsoft/vscode/issues/317813) `#317813`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** CS50 関連の操作でターミナルが反応しなくなり、style50 でコンパイル済みなのに未コンパイルと表示される（codespaces、1.121.0）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [vs code crashes with following info](https://github.com/microsoft/vscode/issues/317816) `#317816`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** クラッシュ報告（テンプレートのみで詳細情報が不足）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Live share not working session start failed with code 503 or 500](https://github.com/microsoft/vscode/issues/317838) `#317838`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** Live Share のセッション開始がコード 503 または 500 で失敗する報告。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Agents: Isolation picker may be switching to folder even when a user is using work trees](https://github.com/microsoft/vscode/issues/317840) `#317840`
**ステータス:** Closed | **ラベル:** info-needed, agents-window

**概要:** Worktree セッションしか使っていないのに isolation picker が `Folder` にランダムに切り替わるという @Yoyokrazy の報告。`Open in Agents` ボタンが値を変えている可能性を検討。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Claude Opus4.6 can't even write a 40 character html file](https://github.com/microsoft/vscode/issues/317852) `#317852`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** モデルがファイルを書き込まないという簡素な報告（1.119.0、Windows）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Can we add a translation function to the update instructions?](https://github.com/microsoft/vscode/issues/317919) `#317919`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 更新手順に翻訳機能を追加できないかという要望（1.121.0、Windows）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [auto model mode not visible](https://github.com/microsoft/vscode/issues/317943) `#317943`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 新規チャットや再起動時は auto model が見えるが、セッションを選ぶと表示されなくなる（1.121.0、Windows）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Crash](https://github.com/microsoft/vscode/issues/317952) `#317952`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 開くたびにバックグラウンドの PowerShell 画面を再読み込みし約 10GB のメモリを使い、しばらくするとクラッシュしてホームチャットの進捗が消える（1.121.0）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Unable to see code suggestions](https://github.com/microsoft/vscode/issues/317964) `#317964`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** `Ask` モードでチャットのコード変更提案が見えず空白になることがある（1.113.0、Linux）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [not opening](https://github.com/microsoft/vscode/issues/317990) `#317990`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** HTML が開かないという簡素な報告（1.121.0、Windows）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [node js is not being recognised even after adding path of nodejs to environmental variables](https://github.com/microsoft/vscode/issues/318037) `#318037`
**ステータス:** Closed | **ラベル:** info-needed

**概要:** 環境変数に Node.js のパスを追加しても認識されないという報告（1.121.0、Windows）。

**直近のアクティビティ:** info-needed のまま追加情報なく自動クローズ（bot）。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Auto-completion disabled for languages with ID >= 256 due to LanguageID bits overlapping with TokenType bits](https://github.com/microsoft/vscode/issues/319118) `#319118`
**ステータス:** Open

**概要:** LanguageID のビットが TokenType のビットと重なるため、ID が 256 以上の言語で自動補完が無効化される。

**直近のアクティビティ:** @RedCMD が類似 Issue（#284742）を共有。bot が最新版へのアップグレードを案内。

**次のアクション:** ビット割り当ての見直し。

---

### ![Issue](icons/issue-opened.svg) [Copilot just kept going and wouldn't stop](https://github.com/microsoft/vscode/issues/291713) `#291713`
**ステータス:** Open | **ラベル:** bug, important, chat-agent

**概要:** Copilot のコード生成を止めたいのに停止できず、VS Code を物理的に再起動するしかなかった。チャットの通常の停止オプションが出なかった。

**直近のアクティビティ:** @tamuratak が、stop ボタンが全く機能していないこと、課金開始時に停止できないのは問題だと指摘し PR #319098 に言及。

**次のアクション:** stop ボタンのキャンセル伝播修正（PR #319098）。

---

### ![Issue](icons/issue-opened.svg) [Security: minimumReleaseAge setting for mitigating supply chain attacks on extensions](https://github.com/microsoft/vscode/issues/316867) `#316867`
**ステータス:** Open | **ラベル:** feature-request, extensions

**概要:** 拡張のサプライチェーン攻撃が増えていることを背景に、リリースから一定期間経過した拡張のみ更新する `minimumReleaseAge` 設定を求める要望。

**直近のアクティビティ:** @DanielRuf が既存のエンタープライズ向けピン留め機能を共有。

**次のアクション:** 機能要望の検討。

---

### ![Issue](icons/issue-opened.svg) [VS code process memory leak, Python code does not terminate after running](https://github.com/microsoft/vscode/issues/319115) `#319115`
**ステータス:** Open | **ラベル:** triage-needed

**概要:** Python スクリプト実行後、ターミナルを閉じてもバックグラウンドの PowerShell プロセスが残り、約 9 プロセス・2.5GB ほどを占有する。

**直近のアクティビティ:** 報告者が、多数のプロセスが開かれ効率モードが始まるのは正常かと画像付きで追記。

**次のアクション:** トリアージとプロセス残留の調査。

---

### ![Issue](icons/issue-opened.svg) [[Linux] Orphaned zygote processes spam "Socket operation on non-socket" errors filling system logs after VS Code crash](https://github.com/microsoft/vscode/issues/288893) `#288893`
**ステータス:** Open

**概要:** VS Code が Linux でクラッシュすると、孤児となった zygote 子プロセスが死んだ親と通信しようとし続け、systemd journal を埋め尽くしディスクを消費する。

**直近のアクティビティ:** @montybot が同じ問題に遭遇したとコメント。

**次のアクション:** クラッシュ後の子プロセス後始末の修正。

---

### ![Issue](icons/issue-opened.svg) [Error with model azure foundry gpt nano etc, these models send temperature to 0.1](https://github.com/microsoft/vscode/issues/319122) `#319122`
**ステータス:** Open | **ラベル:** new release

**概要:** Azure の nano / codex mini モデルでエラーになるという報告（モデル設定 JSON を提示）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Azure モデルの temperature 送信挙動の調査。

---

### ![Issue](icons/issue-opened.svg) [Add Remote Control Support to GitHub Copilot Chat](https://github.com/microsoft/vscode/issues/298406) `#298406`
**ステータス:** Open | **ラベル:** feature-request

**概要:** Copilot Chat セッションが開始したウィンドウに固定される現状に対し、GitHub Mobile やブラウザから進行中のローカルセッションに接続できるリモートコントロール機能を求める要望。

**直近のアクティビティ:** @rs38 が、少なくとも Copilot CLI では既に実装済みではないかとコメント。

**次のアクション:** 既存実装との関係確認。

---

### ![PR](icons/git-pull-request.svg) [Agent feedback fixes](https://github.com/microsoft/vscode/pull/319117) `#319117`
**ステータス:** Merged

**概要:** agent feedback サービスにファイルからセッションを取得するメソッドを追加し、削除した取得ロジックの代わりに使うよう関連コンポーネントを更新。未使用の import/依存を削除。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `src/vs/sessions/contrib/agentFeedback/browser/agentFeedbackService.ts`、`.../agentFeedbackEditorInputContribution.ts` ほか 10 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Open in Agents Window should be hideable](https://github.com/microsoft/vscode/pull/319119) `#319119`
**ステータス:** Merged

**概要:** タイトルバーの「Open in Agents Window」ボタンの表示/非表示を切り替える設定とアクションを追加。#319104 をクローズ。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `src/vs/workbench/contrib/chat/browser/chat.shared.contribution.ts`、`.../agentSessionsActions.ts` ほか 4 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [More actions button is easily confused for minimize button](https://github.com/microsoft/vscode/issues/319104) `#319104`
**ステータス:** Closed | **ラベル:** feature-request, titlebar, unreleased

**概要:** 最小化ボタンの隣に追加された「...（more actions）」ボタンが、小さい画面で最小化ボタンとほぼ同じに見えて紛らわしいという指摘。

**直近のアクティビティ:** @benibenj が状況を整理し非表示設定を提案。@Rawalanche や @thereallyniceone が AI 機能を押し付けられていると強く反発（計 7 コメント）。`chat.disableAIFeatures` でアイコンが消えることも案内された。

**次のアクション:** クローズ済み（PR #319119 で対応）。

---

### ![Issue](icons/issue-opened.svg) ["External ingest is disabled by your organization's policy" in Copilot chat since 1.122 update](https://github.com/microsoft/vscode/issues/318726) `#318726`
**ステータス:** Open

**概要:** 1.122 更新後、Copilot chat のセマンティックインデックスで「External ingest is disabled by your organization's policy」が出て、`#` 検索でプロジェクトのシンボル結果が返らない。

**直近のアクティビティ:** 報告者 @isaacvardyn が、依然としてメッセージが出ること、インデックスは「準備完了」と出るのに検索が機能しないことを追記。

**次のアクション:** ポリシー判定とセマンティックインデックスの調査。

---

### ![Issue](icons/issue-opened.svg) [Custom Endpoint (BYOK) models disappear from model picker on VS Code restart - hasByokModels.lastKnown race condition](https://github.com/microsoft/vscode/issues/319121) `#319121`
**ステータス:** Open | **ラベル:** new release

**概要:** 再起動後、Custom Endpoint（BYOK）モデルが数秒間モデルピッカーから消え、時には Reload Window するまで出ない。`HasByokModelsContribution` の起動タイミングレースが原因。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** PR #319123 での修正。

---

### ![Issue](icons/issue-opened.svg) [MCP: Atlassian Remote MCP OAuth fails with "scope not added to app" — VS Code app missing write:all:twg scope](https://github.com/microsoft/vscode/issues/319120) `#319120`
**ステータス:** Open | **ラベル:** new release

**概要:** Atlassian Remote MCP サーバーを追加し OAuth を行うと、同意ページがエラーになり OAuth が完了せず MCP サーバーに接続できない。VS Code アプリに `write:all:twg` スコープが欠けていることが原因とされる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** OAuth スコープの追加検討。

---

### ![Issue](icons/issue-opened.svg) [Support for RTL languages](https://github.com/microsoft/vscode/issues/11770) `#11770`
**ステータス:** Open | **ラベル:** feature-request, editor-RTL

**概要:** Arabic/Hebrew など RTL 言語（ソースコード内のテキスト）のサポートを求める古い要望。

**直近のアクティビティ:** @ericchaindev が、Copilot Chat でも Persian/Arabic の自然言語と LTR のコードブロックが混在し RTL 対応が必要だとコメント。

**次のアクション:** RTL レンダリングの検討。

---

### ![Issue](icons/issue-opened.svg) [Messages about session rate limit event when BYOK is in play](https://github.com/microsoft/vscode/issues/319116) `#319116`
**ステータス:** Closed | **ラベル:** new release

**概要:** vLLM 上のローカル AI をカスタムモデルとして使っているのにセッションのレート制限警告が出る（1.122.1、macOS）。

**直近のアクティビティ:** bot が #253124 の重複としてクローズ。

**次のアクション:** クローズ済み（#253124 を参照）。

---

### ![Issue](icons/issue-opened.svg) [Allow to change the font size and font of the workbench](https://github.com/microsoft/vscode/issues/519) `#519`
**ステータス:** Open | **ラベル:** feature-request, workbench-fonts

**概要:** エディタのフォント/サイズしか変更できず、workbench は「ズーム」での回避が必要。設定で調整したいという古い要望。

**直近のアクティビティ:** @laicasaane が「10 年経ってもまだ workbench のフォントサイズを変えられないのは驚き」とコメント。

**次のアクション:** 機能要望の検討。

---

### ![Issue](icons/issue-opened.svg) [Some icons in VS Code - Insiders don't show correctly](https://github.com/microsoft/vscode/issues/319100) `#319100`
**ステータス:** Open | **ラベル:** info-needed

**概要:** VS Code Insiders で一部アイコンが正しく表示されないという報告（スクリーンショット付き）。

**直近のアクティビティ:** 報告者 @luan-kodeplus が @aeschli に修正方法を尋ねた。bot が基本情報不足としてガイドライン確認を依頼。

**次のアクション:** 追加情報待ち（info-needed）。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat extension causes extension host to constantly restart](https://github.com/microsoft/vscode/issues/318595) `#318595`
**ステータス:** Open

**概要:** Copilot Chat 拡張を有効にすると全 worktree を読み込み始め、ハングして extension host が再起動するループに陥る。`claudeAgent.enabled: false` でも大きな Claude Code セッションの読み込みでハングする模様。

**直近のアクティビティ:** @tyrsson が、これによりセッションが止まり再送が必要でリクエストを消費する、購読解約も検討していると不満をコメント。

**次のアクション:** worktree/セッション読み込みのハング修正。

---

### ![Issue](icons/issue-opened.svg) [Dates on chat sessions](https://github.com/microsoft/vscode/issues/319106) `#319106`
**ステータス:** Open | **ラベル:** feature-request

**概要:** チャット/セッションの日付が作成日のまま固定で、別日に続けても更新されない、という挙動についての要望/疑問。

**直近のアクティビティ:** @dvckyduck が「仕様のように見える」とコメント。

**次のアクション:** 仕様か改善かの判断。

---

### ![PR](icons/git-pull-request.svg) [Make more/less sections not selectable](https://github.com/microsoft/vscode/pull/319114) `#319114`
**ステータス:** Merged

**概要:** Agents/Sessions リストで「more」「less」セクションを選択不可にする（非選択グループ ID を導入）。

**直近のアクティビティ:** @lszomoru が APPROVED、Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/sessions/contrib/sessions/browser/views/sessionsList.ts`（+10/-1）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [fix: enable tool_search for BYOK custom Responses endpoints](https://github.com/microsoft/vscode/pull/319105) `#319105`
**ステータス:** Open

**概要:** BYOK の OpenAI 互換カスタムエンドポイント（Responses API 経路）で `tool_search` を有効化する。#313098 を修正。

**直近のアクティビティ:** 作者 @PenguinDOOM が ChatMock のログを共有し `tool_search` が含まれ呼び出されることを確認。prompt と分岐条件の不一致を別途見つけたが本 PR には含めないと説明。Copilot レビュー bot がコメント。

**変更ファイル:** `extensions/copilot/src/platform/endpoint/node/responsesApi.ts`、各種 spec ファイルほか 9 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Fix empty title in new session](https://github.com/microsoft/vscode/pull/319111) `#319111`
**ステータス:** Merged

**概要:** 新規セッションのタイトルをクエリの 1 行目から設定し、セッションタブに意味のあるタイトルを即時表示する。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@lszomoru が APPROVED。

**変更ファイル:** `src/vs/sessions/contrib/providers/agentHost/browser/baseAgentHostSessionsProvider.ts`（+8/-1）。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [Screen share picker gets triggered, every time I open the app or browse the marketplace](https://github.com/microsoft/vscode/issues/318030) `#318030`
**ステータス:** Open

**概要:** Arch Linux + Hyprland で、VS Code Insiders を開くたび・マーケットプレイスで拡張をクリックするたびに画面共有ピッカーが出る。1 つ前のリリースに戻すと解消する。

**直近のアクティビティ:** @luobolong が関連 Issue を共有しバグと指摘。@elable947 が EndeavourOS + Hyprland で同じ問題を、jupyter notebook クリック時にも発生するとコメント。

**次のアクション:** 画面共有ピッカー誘発の修正（#317948 関連）。

---

### ![Issue](icons/issue-opened.svg) [Initialising Git and extensions in WSL in VS Code Insiders takes a long time, and some extension icons do not display correctly](https://github.com/microsoft/vscode/issues/318730) `#318730`
**ステータス:** Open

**概要:** VS Code Insiders で WSL の Git・拡張の初期化に長時間かかり、一部拡張アイコンが正しく表示されない。

**直近のアクティビティ:** @eamodio が Git の問題は #318595 関連かもしれないこと、アイコン欠けは CORS の問題で Insiders でのみ再現することをコメント。

**次のアクション:** WSL 初期化遅延と CORS によるアイコン欠けの調査。

---

### ![Issue](icons/issue-opened.svg) [Safety Report: AI Coding Extensions Lack Enforceable Guardrails — AWS Account Destroyed](https://github.com/microsoft/vscode/issues/319113) `#319113`
**ステータス:** Open

**概要:** 規制環境で 56 日間 AI コーディングアシスタントを使い、ガードレールを全設定しても 32 件のワークフロー違反が起き、AI が Terraform を誤ったターゲットにデプロイして AWS 管理アカウントを破壊し、15 日以上業務停止・10 万ドル超の損失が出たという安全性報告。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** AI ガードレールの実効性に関するトリアージ。

---

### ![PR](icons/git-pull-request.svg) [Fix sessions maximize state handling](https://github.com/microsoft/vscode/pull/318983) `#318983`
**ステータス:** Merged

**概要:** Agents ウィンドウのグリッドで、アクティブセッションが別スロットに移動したときに最大化状態を解除するよう修正。`setActive` にセッションの任意引数を追加。

**直近のアクティビティ:** CODENOTIFY bot が通知、Copilot レビュー bot がコメント、@sandy081 が APPROVED。

**変更ファイル:** `src/vs/sessions/browser/parts/sessionsPart.ts`、`.../sessionsManagementService.ts` ほか 4 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![Issue](icons/issue-opened.svg) [workbench.action.findInFiles: accept file URIs to bypass argv-length limits](https://github.com/microsoft/vscode/issues/319112) `#319112`
**ステータス:** Open

**概要:** `workbench.action.findInFiles` / `search.action.openNewEditor` で `filesToInclude` に文字列しか渡せず、プログラムで構築した大きなファイル集合を glob として渡すと argv 長制限に当たる。file URI を受け付けてほしい要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** API 拡張の検討。

---

### ![Issue](icons/issue-opened.svg) [Linux: sandbox-runtime's cli.js opened in editor](https://github.com/microsoft/vscode/issues/312691) `#312691`
**ステータス:** Open | **ラベル:** agent-sandbox

**概要:** #312191 のテスト中、まず信頼を尋ねられた後に sandbox-runtime の `cli.js` がエディタで開かれてしまう（Linux）。

**直近のアクティビティ:** @Icemic が #314695 と同じ問題で、ターミナルを bash にしても解決せず、最新版でも再現すると報告。@dileepyavan がバージョン確認を依頼。

**次のアクション:** sandbox-runtime の起動挙動の調査。

---

### ![Issue](icons/issue-opened.svg) [every time i open vscode i get asked to share my screen](https://github.com/microsoft/vscode/issues/319110) `#319110`
**ステータス:** Closed

**概要:** VS Code を開くたびに画面共有を求められるという報告。

**直近のアクティビティ:** 報告者 @huseeiin がアップグレード後は発生しないとしてクローズ。bot が最新版を案内。

**次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [[testplan-item] Browser: 'Add Area Screenshot to Chat' and 'Add Full Page Screenshot to Chat'](https://github.com/microsoft/vscode/issues/319108) `#319108`
**ステータス:** Open | **ラベル:** testplan-item

**概要:** 統合ブラウザのスクリーンショット（領域選択、実験的なフルページスクロール取得）を Copilot chat に挿入する機能のテストプラン項目。Windows/macOS/Linux でのテストが未消化。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 各 OS でのテスト実施。

---

### ![Issue](icons/issue-opened.svg) [Add Multi-Mode Screenshot Capture Support in Integrated Browser for Copilot Context](https://github.com/microsoft/vscode/issues/299790) `#299790`
**ステータス:** Closed | **ラベル:** feature-request, browser-integration

**概要:** 統合ブラウザにフルページ/ビューポート/領域選択の複数モードのスクリーンショット取得と、Copilot chat への 1 クリック挿入を求める要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし（PR #318791 等の実装に伴いクローズ）。

**次のアクション:** クローズ済み（実装済み）。

---

### ![PR](icons/git-pull-request.svg) [feat(editor): Chrome-like grouped tabs](https://github.com/microsoft/vscode/pull/315366) `#315366`
**ステータス:** Open

**概要:** マルチエディタのタブバーに Chrome 風のタブグループを実装。名前付き・色分け・折りたたみ可能なグループ、ドラッグ＆ドロップによる並び替えやグループ間移動、タブ/グループヘッダーのコンテキストメニューなどを備える。#100335 の要望に対応。

**直近のアクティビティ:** Copilot レビュー bot が複数回レビューしコメント（8〜9 件の指摘）。

**変更ファイル:** `src/vs/workbench/browser/parts/editor/multiEditorTabsControl.ts`（+498）、`.../editorCommands.ts`（+291）ほか 11 ファイル。

**次のアクション:** レビュー指摘の対応。

---

### ![Issue](icons/issue-opened.svg) [czf](https://github.com/microsoft/vscode/issues/319109) `#319109`
**ステータス:** Closed | **ラベル:** spam

**概要:** 内容のないスパム投稿。

**直近のアクティビティ:** bot がスパムとして自動クローズ。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix language model event issues](https://github.com/microsoft/vscode/pull/319097) `#319097`
**ステータス:** Merged

**概要:** 言語モデルイベントの問題を修正。#318753、#318749（再帰、非対応シナリオではない）などを修正。

**直近のアクティビティ:** @dmitrivMS のコメントレビュー、@vritant24 が APPROVED。

**変更ファイル:** `src/vs/workbench/api/common/extHostLanguageModels.ts`、`src/vs/base/common/collections.ts` ほか 5 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [[Refining + Validating] Preserve selected Copilot CLI model in Agents window](https://github.com/microsoft/vscode/pull/318594) `#318594`
**ステータス:** Open（ドラフト）

**概要:** Agents UX の Copilot CLI セッションで、タスク発行後にセッションを復元すると選択モデルが失われる問題を修正。選択モデルをセッションごとのメタデータとして永続化しモデルピッカーに復元する。#316044 を修正。

**直近のアクティビティ:** 作者 @Chulong-Li のコメントレビューあり。

**変更ファイル:** `extensions/copilot/src/extension/chatSessions/copilotcli/node/copilotcliSessionService.ts`、`.../chatSessionMetadataStore.ts` ほか 11 ファイル。

**次のアクション:** 検証とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Browser: 'Add Area Screenshot to Chat' and 'Add Full Page Screenshot to Chat (Experimental)'](https://github.com/microsoft/vscode/pull/318791) `#318791`
**ステータス:** Merged

**概要:** 統合ブラウザに「領域スクリーンショットを Chat に追加」と「フルページスクリーンショットを Chat に追加（実験的）」を実装。「Add Element to Chat」で正確な領域が取れない場合に有用。フルページは実験設定でゲートし既定オフ。#299790 をクローズ。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@DonJayamanne が APPROVED、@jruales のコメントレビューあり。

**変更ファイル:** `src/vs/platform/browserView/electron-main/browserViewInspector.ts`、`.../preload-browserView.ts` ほか 7 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Browser: 'Add screenshot to chat'](https://github.com/microsoft/vscode/pull/317679) `#317679`
**ステータス:** Merged

**概要:** 統合ブラウザのスクリーンショットを Chat に追加する機能。#318065 をクローズし #299790 に部分対応、#318058 も修正。

**直近のアクティビティ:** @kycutler が APPROVED、@jruales のコメントレビューあり。

**変更ファイル:** `src/vs/workbench/contrib/browserView/electron-browser/features/browserEditorChatFeatures.ts` ほか 3 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix: CancellationToken not propagating to `provideLanguageModelChatResponse` on chat stop](https://github.com/microsoft/vscode/pull/319098) `#319098`
**ステータス:** Open

**概要:** stop ボタンがプロバイダの `CancellationToken` にキャンセルを伝播しない問題を修正。(1) `$startChatRequest` の RPC cancel ハンドラがプロバイダ復帰前に削除される、(2) MainThread と ExtensionHost でリクエスト ID が異なり単一 CTS で橋渡しできない、の 2 点が原因。両側にローカルの CancellationTokenSource を導入。#291713、#277872 をクローズ。

**直近のアクティビティ:** Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/workbench/api/browser/mainThreadLanguageModels.ts`、`src/vs/workbench/api/common/extHostLanguageModels.ts` ほか 3 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix(agentHost): gracefully handle disconnected client during plugin sync](https://github.com/microsoft/vscode/pull/318988) `#318988`
**ステータス:** Open

**概要:** 既に切断されたクライアントに対してプラグイン同期が呼ばれると、authority が見つからず生のエラーが未処理のまま伝播する問題を修正。エラーを捕捉して明確なエラーに再スローし、残りのプラグインの同期を継続する。#318907 を修正。

**直近のアクティビティ:** @DonJayamanne が #318931 で修正済みかもしれないとコメント、作者は構造化エラー検出への置き換えやエラー保持の対応を報告。Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/platform/agentHost/node/agentPluginManager.ts`、`.../agentPluginManager.test.ts`。

**次のアクション:** #318931 との重複確認とレビュー。

---

### ![PR](icons/git-pull-request.svg) [Add "Copy Filename" / "Copy Folder Name" commands](https://github.com/microsoft/vscode/pull/315073) `#315073`
**ステータス:** Open

**概要:** `Copy Filename`（フォルダ向けの `Copy Folder Name`）コマンドを追加し、`Copy Path` / `Copy Relative Path` がある各メニューに配置する。コピー値はビュー表示と一致するよう `ILabelService.getUriBasenameLabel` を使う。#301362 をクローズ。

**直近のアクティビティ:** Copilot レビュー bot が複数回コメント、@matrush のコメントレビューあり（19 件のレビュー）。

**変更ファイル:** `src/vs/workbench/contrib/files/browser/fileCommands.ts`、`.../searchActionsCopy.ts` ほか 5 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Enabling compaction_trigger for openAI endpoints](https://github.com/microsoft/vscode/pull/318648) `#318648`
**ステータス:** Open

**概要:** 対象の gpt-5.4/gpt-5.5 系モデル向けに、従来の Responses API コンテキスト管理しきい値フローを compaction-trigger ベースのフローに置き換える。リクエスト整形、リプレイ処理、バックグラウンド compaction のオーケストレーションを含む。

**直近のアクティビティ:** @bhavyaus のコメントレビューあり。

**変更ファイル:** `extensions/copilot/src/extension/intents/node/agentIntent.ts`、`.../oaiLanguageModelServer.ts` ほか 16 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Azure BYOK: support configurable Entra auth provider and scopes](https://github.com/microsoft/vscode/pull/318118) `#318118`
**ステータス:** Open

**概要:** Azure BYOK にプロバイダレベルの `entraAuthProvider`（VS Code の `microsoft-sovereign-cloud` 認証への opt-in）と `entraScopes`（ソブリンクラウドや APIM/カスタムスコープの要求）を追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `extensions/copilot/src/extension/byok/vscode-node/azureProvider.ts`、`.../customEndpointProvider.ts` ほか 6 ファイル。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Disable execution subagent by default](https://github.com/microsoft/vscode/pull/319095) `#319095`
**ステータス:** Merged

**概要:** execution subagent を既定オフに戻す。最近既定オンにしたが、実験経由で展開する方針に変更。`chat.executionSubagent.enabled` の既定を true→false に。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `extensions/copilot/src/platform/configuration/common/configurationService.ts`、`extensions/copilot/package.json`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Cache malicious extensions list for enablement checks](https://github.com/microsoft/vscode/pull/319096) `#319096`
**ステータス:** Merged

**概要:** 有効化チェック用の悪意ある拡張リストをキャッシュし、起動時間を約 50〜80ms 短縮する。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@bhavyaus が APPROVED。

**変更ファイル:** `src/vs/workbench/services/extensionManagement/browser/extensionEnablementService.ts`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [agents: fix duplicate workspace created tasks firing](https://github.com/microsoft/vscode/pull/319078) `#319078`
**ステータス:** Open

**概要:** `WorktreeCreatedTaskDispatcher` の autorun が、実行中に自分の store を `deleteAndDispose` で同期的に削除し、タスクが重複発火する問題を修正。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@TylerLeonhardt が APPROVED。

**変更ファイル:** `src/vs/sessions/contrib/chat/browser/worktreeCreatedTaskDispatcher.ts`。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Agent Host Copilot CLI: Support async shell completion notifications](https://github.com/microsoft/vscode/pull/318511) `#318511`
**ステータス:** Merged

**概要:** カスタムターミナルツールが無効で SDK が通知する場合に、Agent Host Copilot セッションから Copilot SDK の `system.notification` イベントを購読し、非同期シェル補完通知をサポートする。#318505 を解決。

**直近のアクティビティ:** @rzhao271 が APPROVED、@roblourens は DISMISSED、@anthonykim1 のコメントレビューあり。

**変更ファイル:** `src/vs/platform/agentHost/common/state/protocol/...` ほか 43 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Prevent cache timeouts during long tool calls](https://github.com/microsoft/vscode/pull/316277) `#316277`
**ステータス:** Merged

**概要:** 長いツール呼び出し（特に subagent 実行）中にメインエージェントのキャッシュが失効する問題に対し、4 分ごとにダミーのユーザーリクエスト（最大 3 回のプローブ）を送ってキャッシュを保つ。#313748 の継続。

**直近のアクティビティ:** @bhavyaus が eval 評価を依頼するコメント、@bhavyaus と @dmitrivMS が APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/intents/node/toolCallingLoop.ts`、`.../defaultIntentRequestHandler.ts` ほか 5 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Enable rubber duck and /rubber-duck command](https://github.com/microsoft/vscode/pull/318662) `#318662`
**ステータス:** Merged

**概要:** Agent Host で Rubber Duck critic subagent を有効化。補完的なモデルファミリ（同等ティアの Claude↔GPT）を使う独立レビュアーで、出荷前にバグ・論理エラー・設計上の欠陥を捕捉する。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@connor4312 が APPROVED。

**変更ファイル:** `src/vs/platform/agentHost/node/copilot/copilotAgent.ts`、`.../agentService.ts` ほか 9 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Live streaming for Task API cloud agent sessions](https://github.com/microsoft/vscode/pull/319077) `#319077`
**ステータス:** Merged

**概要:** v2 Task API クラウドエージェントバックエンド向けにライブストリーマーを追加し、進行中のターンでツールカード・進捗・最終応答を段階的に表示する（v1 の SSE 挙動に合わせる）。新規 `TaskTurnStreamer` を導入。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/chatSessions/vscode-node/taskTurnStreamer.ts`、`.../copilotCloudSessionsProvider.ts` ほか 4 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add gemini exp setting to disable GetChangedFiles tool](https://github.com/microsoft/vscode/pull/319079) `#319079`
**ステータス:** Merged

**概要:** GetChangedFiles ツールを無効化する Gemini 用の実験設定を追加する。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/tools/vscode-node/toolsService.ts`、`.../configurationService.ts` ほか 4 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Add header request id for ghosttext edit types](https://github.com/microsoft/vscode/pull/319044) `#319044`
**ステータス:** Merged

**概要:** ghosttext 編集にリクエスト ID が欠けており CAPI 情報との突き合わせが難しい問題に対し、ヘッダーにリクエスト ID を追加。テレメトリ出力で自分のリクエストが見えることを検証済み。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@roblourens が（DISMISSED の後）APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/completions-core/vscode-node/lib/src/ghostText/ghostText.ts`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Throttle initial Git repository.status() across repositories](https://github.com/microsoft/vscode/pull/318862) `#318862`
**ステータス:** Merged

**概要:** マルチルートで多数の git リポジトリ（50 以上）があると、起動 25〜30 秒後に extension host が kill され再起動・クラッシュのループに陥る問題を修正。初期の `repository.status()` をリポジトリ間でスロットリングする。#318279、#318764 を修正。

**直近のアクティビティ:** @dmitrivMS が 30 リポジトリ・Windows で良好と確認しつつ顧客環境での確認を依頼。@lszomoru が APPROVED。

**変更ファイル:** `extensions/git/src/model.ts`。

**次のアクション:** 完了済み（マージ済み、顧客確認待ち）。

---

### ![PR](icons/git-pull-request.svg) [Update settings and docs for Execution Subagent rollout based on Gemini-3-Flash](https://github.com/microsoft/vscode/pull/319023) `#319023`
**ステータス:** Merged

**概要:** Execution Subagent を既定で有効化し既定モデルを `gemini-3-flash` に設定。gemini-3-flash が使えない場合は subagent を無効化する。

**直近のアクティビティ:** @vikramnitin9 と @bhavyaus がエンドポイント解決ロジックについて議論。@bhavyaus が APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/intents/node/agentIntent.ts`、`.../configurationService.ts` ほか 4 ファイル。

**次のアクション:** 完了済み（マージ済み、ただし #319095 で既定オフへ再変更）。

---

### ![PR](icons/git-pull-request.svg) [Update GitHub Copilot CLI to version 1.0.56](https://github.com/microsoft/vscode/pull/319076) `#319076`
**ステータス:** Open

**概要:** GitHub Copilot 依存を最新版（CLI 1.0.56）に更新し、CLI セッション処理とトークン管理を改善。SDK 更新への対応とテスト追加を含む。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@dmitrivMS が APPROVED。

**変更ファイル:** `extensions/copilot/package.json`、`extensions/copilot/src/extension/chatSessions/copilotcli/common/utils.ts` ほか 15 ファイル。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Fix command center debug hover color](https://github.com/microsoft/vscode/pull/319070) `#319070`
**ステータス:** Open

**概要:** デバッグ中の command center のテーマ上書きを修正し、ホバー/アクティブ時に通常のタイトルバー背景にフォールバックせずデバッグ色を保つようにする。`commandCenter.debuggingActiveBackground` を追加。#193789 を修正。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `src/vs/workbench/contrib/debug/browser/statusbarColorProvider.ts`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Fix: Linkify markdown links with no space before `[` when link text contains spaces](https://github.com/microsoft/vscode/pull/319073) `#319073`
**ステータス:** Open

**概要:** マークダウンリンクの直前にテキストがあり（例: `abc[src/main.ts 17](src/main.ts#L17)`）、リンクテキストに空白が含まれる場合に Linkifier の空白ベースのトークン化が入力を分割してしまう問題を修正。#318866 をクローズ。

**直近のアクティビティ:** Copilot レビュー bot が複数回コメント。

**変更ファイル:** `extensions/copilot/src/extension/linkify/common/linkifier.ts`、`.../linkifier.spec.ts`。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [chore: update copilot dependencies and improve session management](https://github.com/microsoft/vscode/pull/318655) `#318655`
**ステータス:** Open

**概要:** `@github/copilot` を 1.0.49→1.0.54、`@github/copilot-sdk` を beta.4→beta.7 に更新。reasoning effort 形式の説明を明確化し、セッション管理をリファクタ。

**直近のアクティビティ:** Copilot レビュー bot が複数回コメント、@chrmarti が APPROVED。

**変更ファイル:** `extensions/copilot/package.json`、`.../copilotcli/common/utils.ts` ほか 15 ファイル。

**次のアクション:** マージ判断。

---

### ![PR](icons/git-pull-request.svg) [Browser: Playwright API metrics](https://github.com/microsoft/vscode/pull/316473) `#316473`
**ステータス:** Open（ドラフト）

**概要:** 統合ブラウザの Playwright API メトリクスを追加する（本文記載なし）。

**直近のアクティビティ:** Copilot レビュー bot が複数回コメント。

**変更ファイル:** `src/vs/platform/browserView/node/playwrightService.ts`、`.../runPlaywrightCodeTool.ts` ほか 3 ファイル。

**次のアクション:** ドラフト完成。

---

### ![PR](icons/git-pull-request.svg) [Fix proxy auth attempt default](https://github.com/microsoft/vscode/pull/270025) `#270025`
**ステータス:** Closed

**概要:** プロキシ認証の試行の既定値を修正する。

**直近のアクティビティ:** @deepak1556 が CHANGES_REQUESTED。

**変更ファイル:** `src/vs/platform/native/electron-main/auth.ts`。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [win32(installer): Normalize PATH handling and fix uninstall filtering](https://github.com/microsoft/vscode/pull/264219) `#264219`
**ステータス:** Closed

**概要:** Windows インストーラの PATH 処理を正規化し、アンインストール時のフィルタリングを修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build/win32/code.iss`。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix missing reasoningEffort telemetry for Claude models](https://github.com/microsoft/vscode/pull/308325) `#308325`
**ステータス:** Closed

**概要:** `reasoningEffort` テレメトリが Responses API 経路（OpenAI）の `requestBody.reasoning?.effort` のみ読んでおり、Messages API を使う Claude モデルで欠落する問題を修正しようとした PR。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@bhavyaus が APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/prompt/node/chatMLFetcherTelemetry.ts`。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix crash on null entries in launch.json configurations array](https://github.com/microsoft/vscode/pull/308235) `#308235`
**ステータス:** Merged

**概要:** `launch.json` の configurations 配列に null エントリがあるとクラッシュする問題を修正。有効なエントリは解析しつつ無効な null を無視する。#307691 を修正。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@roblourens と @Yoyokrazy が APPROVED。

**変更ファイル:** `src/vs/workbench/contrib/debug/browser/debugConfigurationManager.ts`、`.../debugConfigurationManager.test.ts`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Post Update Widget Polish](https://github.com/microsoft/vscode/pull/308064) `#308064`
**ステータス:** Closed（ドラフト）

**概要:** 更新情報ペイロードから JSON/frontmatter メタデータを解析する `parseUpdateInfoInput` を追加し、カスタムマークダウンやボタン定義をサポートする更新ウィジェットの磨き込み。

**直近のアクティビティ:** Copilot レビュー bot がコメント。

**変更ファイル:** `src/vs/workbench/contrib/update/browser/updateTooltip.ts`、`.../updateTitleBarEntry.ts` ほか 5 ファイル。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Refactor Copilot CLI session management and improve folder handling](https://github.com/microsoft/vscode/pull/309779) `#309779`
**ステータス:** Merged

**概要:** `ICopilotCLISessionService` の `onDidChangeSessions` を非推奨化し、セッション削除ロジックを強化。新ブランチ作成のサポートやセッションオプショングループ処理の改善を含む。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@alexr00 が APPROVED。

**変更ファイル:** `extensions/copilot/src/extension/chatSessions/copilotcli/node/copilotcliSessionService.ts` ほか 7 ファイル。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Empty sessions view: cannot call joinPath](https://github.com/microsoft/vscode/pull/309777) `#309777`
**ステータス:** Merged

**概要:** 空のセッションビューで `joinPath` を呼べない問題を修正。解決不能な `unknown:///` ロケーションの扱いについてレビュー依頼を含む。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@sandy081 が APPROVED。

**変更ファイル:** `src/vs/base/common/uri.ts`、`.../copilotChatSessionsProvider.ts`。

**次のアクション:** 完了済み（マージ済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix #309650](https://github.com/microsoft/vscode/pull/309767) `#309767`
**ステータス:** Merged

**概要:** Issue #309650 の修正（本文記載なし）。

**直近のアクティビティ:** Copilot レビュー bot がコメント、@chrmarti が APPROVED。

**変更ファイル:** `src/vs/sessions/contrib/chat/browser/newChatViewPane.ts`。

**次のアクション:** 完了済み（マージ済み）。

---

## qmk/qmk_firmware

> ステノ用の Plover HID プロトコルや split キーボードの RGB EEPROM 同期・watchdog/OS 検出リセットの統一など core 機能 PR と、キーボード追加 PR、モジュール名重複検出の CLI 改善が並ぶ。長期 WIP の XAP 実装も継続。

### ![PR](icons/git-pull-request.svg) [Custom plover HID report type](https://github.com/qmk/qmk_firmware/pull/26018) `#26018`
**ステータス:** Open | **ラベル:** core, keyboard, cli, stale ほか

**概要:** Plover が追加したカスタム HID プロトコルに対応。COM ポートベースの Gemini/Bolt の代替で、インターフェース数が限られる MCU では 1 つ節約できる。

**直近のアクティビティ:** @dnaq がマージを妨げているものはあるかと質問。bot が 45 日無活動で stale マークし 30 日後クローズ予定と通知。@drashna が APPROVED。

**変更ファイル:** `quantum/keycodes.h`、`quantum/plover_hid.h`、`docs/features/stenography.md` ほか 18 ファイル。

**次のアクション:** マージ判断と stale 解消。

---

### ![PR](icons/git-pull-request.svg) [rgb_matrix: add optional split EEPROM sync](https://github.com/qmk/qmk_firmware/pull/26027) `#26027`
**ステータス:** Open | **ラベル:** core, documentation

**概要:** split キーボードで RGB 設定を両半に永続化するオプションを追加。どちらの半分を master にしてもスレーブ側も RGB 設定を保存できるようにする EEPROM フラグを追加。

**直近のアクティビティ:** @optix2000 が stale タグ除去のため bump コメント。

**変更ファイル:** `quantum/rgb_matrix/rgb_matrix.c`、`quantum/split_common/transactions.c`、`quantum/split_common/rgb_matrix_split_eeprom_sync.h` ほか 7 ファイル。

**次のアクション:** レビュー。

---

### ![PR](icons/git-pull-request.svg) [Handle split keyboard watchdog and os detection resets in a unified way](https://github.com/qmk/qmk_firmware/pull/25964) `#25964`
**ステータス:** Open | **ラベル:** core

**概要:** split キーボードの watchdog と OS 検出のリセットをメインタスクに委譲して統一的に扱う。リセット前に `pre_mcu_reset_user` のユーザーコードを実行し、単一箇所での graceful shutdown を可能にする。

**直近のアクティビティ:** 作者 @hydra が、今は追える余裕がないのでマージして適宜修正してよいとコメント。@zvecr が CHANGES_REQUESTED。

**変更ファイル:** `quantum/keyboard.c`、`quantum/os_detection.c`、`quantum/split_common/split_util.c` ほか 6 ファイル。

**次のアクション:** メンテナー側での引き継ぎ判断。

---

### ![PR](icons/git-pull-request.svg) [add BitDeck10 macro keyboard](https://github.com/qmk/qmk_firmware/pull/26184) `#26184`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** BitDeck10 マクロキーボードの追加。

**直近のアクティビティ:** @drashna と @zvecr のコメントレビューあり。

**変更ファイル:** `keyboards/morisoba/bitdeck10/bitdeck10.c`、`.../keyboard.json`、`.../keymaps/default/keymap.c` ほか 8 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [[Keyboard Addition] Support for Infinity 87.5 Rev 2](https://github.com/qmk/qmk_firmware/pull/26239) `#26239`
**ステータス:** Open | **ラベル:** keyboard, keymap, dd

**概要:** Infinity 87.5 PCB の第 2 リビジョンのサポート追加。#20249 を参考に PR チェックリストと最新テンプレートに沿って変更。

**直近のアクティビティ:** @zvecr が CHANGES_REQUESTED で、`data/mappings/keyboard_aliases.hjson` への alias 追加を依頼。

**変更ファイル:** `keyboards/mechlovin/infinity875/info.json`（+2503）、`data/mappings/keyboard_aliases.hjson` ほか 17 ファイル。

**次のアクション:** keyboard_aliases.hjson の更新。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open（ドラフト）| **ラベル:** core, breaking_change, feature ほか

**概要:** #11567 に基づく XAP（eXtensible Application Protocol）の実装を追跡する WIP PR。96 ファイル・+7548 の大規模変更。

**直近のアクティビティ:** @t-8ch と @tzarc のコメントレビュー、@jock070 が APPROVED。

**変更ファイル:** `builddefs/xap.mk`、`data/constants/lighting/...`、`data/schemas/definitions.jsonschema` ほか 96 ファイル。

**次のアクション:** WIP 継続。

---

### ![PR](icons/git-pull-request.svg) [Fail when a duplicate module name is detected](https://github.com/qmk/qmk_firmware/pull/26238) `#26238`
**ステータス:** Merged | **ラベル:** python, cli

**概要:** モジュール名の重複（例: `["foo/hello_world", "bar/hello_world"]`）は未サポートで、生成コードが `hello_world` 部分を一意と仮定し重複シンボル/定義を生むため、検出時に失敗させるようにする。

**直近のアクティビティ:** @tzarc のコメントレビューと APPROVED。

**変更ファイル:** `lib/python/qmk/info.py`（+14/-1）。

**次のアクション:** 完了済み（マージ済み）。

---
