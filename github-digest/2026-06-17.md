# GitHub ダイジェスト — 2026-06-17

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[font/shaper: add RTL shaping for Arabic and Hebrew](https://github.com/ghostty-org/ghostty/pull/11079)** (ghostty-org/ghostty) — アラビア語・ヘブライ語の双方向テキストシェイピングを純 Zig 製 UAX #9 実装で追加する大型 PR。利用者からの要望が強く議論も活発。
2. ![PR](icons/git-pull-request.svg) **[Terminal command risk assessment](https://github.com/microsoft/vscode/pull/313952)** (microsoft/vscode) — チャットのツール確認に LLM ベースのターミナルコマンドリスク評価を追加しマージ済み。エージェント実行の安全性に直結する。
3. ![PR](icons/git-pull-request.svg) **[Remove dismissing of the trust dialog since workspace is now untrusted by default](https://github.com/microsoft/vscode/pull/321854)** (microsoft/vscode) — 新規ワークスペースを既定で Restricted Mode（untrusted）に切り替える方針変更がテストにも波及。セキュリティ既定の重要な転換。
4. ![PR](icons/git-pull-request.svg) **[Centralised environment storage](https://github.com/astral-sh/uv/pull/18214)** (astral-sh/uv) — プロジェクト venv をキャッシュへ集中保存する設計大改修。zanieb・EliteTK による多数のレビューが進行中。
5. ![Issue](icons/issue-opened.svg) **[[Bug] STM32G0B1 USB HID broken after ChibiOS update](https://github.com/qmk/qmk_firmware/issues/26265)** (qmk/qmk_firmware) — ChibiOS 更新後に USB HID が認識されない回帰。報告者が ChibiOS 側に根本原因を特定。

---

## astral-sh/uv

> `uv tree` の出力強化（JSON・flatten・workspace フィルタ）と `uv audit` のロードマップ進展、Windows 環境のトラブル報告が中心。

### ![PR](icons/git-pull-request.svg) [Centralised environment storage](https://github.com/astral-sh/uv/pull/18214) `#18214`
**ステータス:** Open | **ラベル:** enhancement, preview

**概要:** プロジェクトの仮想環境を `.venv` ではなくキャッシュの `environments-v2` バケットに集中保存する `centralized-envs` プレビュー機能を追加する。互換性のため `.venv` シンボリックリンクを維持し、#1495 を解決し #7642 に部分対応する。

**直近のアクティビティ:** zanieb と EliteTK が複数のレビューコメントを残し、設計を詰めている。

**変更ファイル:** 29ファイル、+1578/-178 の大規模変更。

**次のアクション:** レビュー指摘への対応を経てプレビュー機能としてマージ。

---

### ![PR](icons/git-pull-request.svg) [Add JSON output for uv tree](https://github.com/astral-sh/uv/pull/19904) `#19904`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `uv tree` に `--format json` を追加し、依存グラフを機械可読な NetworkX node-link 形式で出力する。既定のテキスト形式は維持する。Issue #13276 の要望に対応する。

**直近のアクティビティ:** テストインベントリのボットコメントのみ。

**変更ファイル:** 6ファイル、+1020/-6。

**次のアクション:** ドラフトを完成させレビューへ。

---

### ![Issue](icons/issue-opened.svg) [uv tree support for json output](https://github.com/astral-sh/uv/issues/13276) `#13276`
**ステータス:** Open | **ラベル:** enhancement

**概要:** ツリー形式の出力に加えて、依存グラフを JSON 等で書き出すオプションが欲しいという要望。PR #19904 で対応中。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR #19904 のマージで解決見込み。

---

### ![PR](icons/git-pull-request.svg) [Add `--depends-on` to `uv workspace list`](https://github.com/astral-sh/uv/pull/19902) `#19902`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `uv workspace list` に `--depends-on <PACKAGE>` を追加し、既存の `uv.lock` を読んで対象パッケージへ依存経路を持つワークスペースメンバーを一覧する。lock を変更しない。

**直近のアクティビティ:** テストインベントリのボットコメントのみ。

**変更ファイル:** 6ファイル、+448/-3。

**次のアクション:** ドラフト完成後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add `--only-emit-workspace` to `uv tree`](https://github.com/astral-sh/uv/pull/19901) `#19901`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `uv tree` 出力でサードパーティパッケージを省略しワークスペースメンバーのみ表示する `--only-emit-workspace` を追加する。グラフ全体は走査用に保持する。

**直近のアクティビティ:** テストインベントリのボットコメントのみ。

**変更ファイル:** 6ファイル、+281/-69。

**次のアクション:** ドラフト完成後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add `--flatten` to `uv tree`](https://github.com/astral-sh/uv/pull/19900) `#19900`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `uv tree` に `--flatten` を追加し、選択された依存グラフを走査して到達可能な全パッケージを決定的順序で一度ずつ列挙する。同名異ソースにはソース修飾子を付ける。

**直近のアクティビティ:** テストインベントリのボットコメントのみ。

**変更ファイル:** 8ファイル、+350/-3。

**次のアクション:** ドラフト完成後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Preserve attribution in inverted dependency trees](https://github.com/astral-sh/uv/pull/19899) `#19899`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** 反転ツリー表示時に依存の出所を保持する。逆方向走査で必要な extra やマーカー条件を各逆経路に引き継ぎ、依存グループ経路をそれを所有するワークスペースメンバーで止める。

**直近のアクティビティ:** テストインベントリのボットコメントのみ。

**変更ファイル:** 2ファイル、+484/-49。

**次のアクション:** ドラフト完成後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Respect dependency selection for synthetic tree roots](https://github.com/astral-sh/uv/pull/19905) `#19905`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `uv tree` の合成ルートに直接付与された要件を、ワークスペースパッケージの依存と同様に扱う。プロジェクトレスなワークスペース依存グループを選択されたグループでフィルタし、要件の extra を保持する。

**直近のアクティビティ:** テストインベントリのボットコメントのみ。

**変更ファイル:** 2ファイル、+178/-15。

**次のアクション:** ドラフト完成後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Improve mirror error for custom Python download URLs](https://github.com/astral-sh/uv/pull/19898) `#19898`
**ステータス:** Open | **ラベル:** なし

**概要:** 設定済みの CPython インストールミラーが `python-build-standalone` の想定 URL 接頭辞に一致しないときのエラーメッセージを改善し、環境変数名の繰り返しではなく問題の URL を含めるようにする。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2ファイル、+19/-1。

**次のアクション:** レビューを経てマージ。

---

### ![PR](icons/git-pull-request.svg) [Fix transparent Python upgrades in project environments](https://github.com/astral-sh/uv/pull/19890) `#19890`
**ステータス:** Merged | **ラベル:** bug

**概要:** アップグレード可能な Python 要求時にプロジェクト環境へ `<major>.<minor>` 版を保存し、参照する Python がアップグレードされても環境を再利用できるようにした。従来はフル版を書いていたため常に古いと判定されていた。

**直近のアクティビティ:** zanieb が承認。

**変更ファイル:** 2ファイル、+88/-2。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix UV_NO_CACHE=false not overriding no-cache=true in config file](https://github.com/astral-sh/uv/pull/19895) `#19895`
**ステータス:** Open | **ラベル:** なし

**概要:** `CacheSettings::resolve()` が `no-cache` の CLI/環境変数と設定ファイル値を `||` で結合していたため、設定ファイルで `no-cache = true` だと `UV_NO_CACHE=false` が効かない問題を修正する。`--offline` と同じフラグ対の方式を採用する。

**直近のアクティビティ:** tdhopper と zanieb がレビューコメント。

**変更ファイル:** 5ファイル、+89/-13。

**次のアクション:** レビュー対応後マージ。

---

### ![PR](icons/git-pull-request.svg) [Replace dashmap with papaya](https://github.com/astral-sh/uv/pull/19532) `#19532`
**ステータス:** Open | **ラベル:** performance

**概要:** デッドロック条件の多い `DashMap` を papaya に置き換える。性能目的ではなく並行アクセスでのデッドロック（CI が6時間ハングする等）を避けるため。

**直近のアクティビティ:** konstin が残りの dashmap 利用も除去し、`Mutex` 追加後も性能が維持されたと報告。ibraheemdev が承認。

**変更ファイル:** 11ファイル、+147/-107。

**次のアクション:** マージへ。

---

### ![PR](icons/git-pull-request.svg) [Add a benchmark interface to the `uv` crate](https://github.com/astral-sh/uv/pull/19896) `#19896`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** 一度きりのプロセス初期化と繰り返し可能なプロセス内 uv 呼び出しを分離する隠し・フィーチャゲート付きベンチマークインタフェースを追加する。#19486 を置き換える。

**直近のアクティビティ:** CodSpeed ボットが10.58%の性能劣化を警告（実行環境差の可能性）。zanieb がコメント。

**変更ファイル:** 5ファイル、+177/-46。

**次のアクション:** 性能警告を確認しドラフト完成へ。

---

### ![PR](icons/git-pull-request.svg) [Clean up ImplementationName](https://github.com/astral-sh/uv/pull/19891) `#19891`
**ステータス:** Merged | **ラベル:** internal

**概要:** `uv_python::implementation::ImplementationName` のリファクタ。`From` 実装を廃して長短名を返す関数に統一し、`iter_all` を活用して情報を集約する。

**直近のアクティビティ:** EliteTK と zanieb が `From` を使うべきかで議論したが、現状の形で進めた。

**変更ファイル:** 3ファイル、+36/-39。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Modernize predicate checks with `is_some_and` and `is_none_or`](https://github.com/astral-sh/uv/pull/19889) `#19889`
**ステータス:** Merged | **ラベル:** internal

**概要:** MSRV が Rust 1.94 になったのを機に、`map(...).unwrap_or(...)` を `is_some_and`・`is_none_or`・`is_ok_and` に置き換えて述語チェックを明瞭にする。

**直近のアクティビティ:** zsol が `map_unwrap_or` の allow 除去を検討（79件の警告）し、woodruffw が除去に賛成。woodruffw が承認。

**変更ファイル:** 18ファイル、+24/-60。

**次のアクション:** マージ済み。allow 除去は別途検討。

---

### ![PR](icons/git-pull-request.svg) [Support SARIF as a `uv audit` output](https://github.com/astral-sh/uv/pull/19872) `#19872`
**ステータス:** Merged | **ラベル:** enhancement, preview, uv audit

**概要:** `uv audit` に `--output-format=sarif` を追加する。SARIF モデルは当面 uv クレートに直接インライン化する。Issue #19660 を解決する。

**直近のアクティビティ:** charliermarsh が承認。

**変更ファイル:** 7ファイル、+965/-160。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [SARIF output on uv audit](https://github.com/astral-sh/uv/issues/19660) `#19660`
**ステータス:** Closed | **ラベル:** enhancement, uv audit

**概要:** govulncheck のように `uv audit` で SARIF ファイルを出力できるようにする要望。`uv audit --format sarif` を提案。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR #19872 で対応しクローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Roadmap: `uv audit`](https://github.com/astral-sh/uv/issues/18506) `#18506`
**ステータス:** Open | **ラベル:** tracking, uv audit

**概要:** `uv audit` の MVP・ポスト MVP のロードマップ追跡 Issue。CLI 雛形・OSV による一括監査・SARIF 出力などの進捗を管理する。

**直近のアクティビティ:** woodruffw がマルウェアチェックの設定項目（`tool.uv.audit.malware-check`）を提案。利用者が pip-audit からの移行を歓迎しつつマルウェアチェック有効化を要望。

**次のアクション:** マルウェアチェック等のポスト MVP 機能を継続実装。

---

### ![PR](icons/git-pull-request.svg) [Respect --no-workspace across project commands](https://github.com/astral-sh/uv/pull/19880) `#19880`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `--no-workspace` を各プロジェクトコマンドで尊重し、現在のプロジェクトを単独として扱う。`uv lock --no-workspace` がネストしたプロジェクトの lock を書くようにする。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 25ファイル、+829/-62。

**次のアクション:** ドラフト完成後レビュー。

---

### ![PR](icons/git-pull-request.svg) [Add hidden --show-version to uv check](https://github.com/astral-sh/uv/pull/19892) `#19892`
**ステータス:** Merged | **ラベル:** なし

**概要:** `uv format --show-version` に倣い `uv check --show-version` を追加し、`Using ty <version>` を報告する。

**直近のアクティビティ:** zanieb が承認。

**変更ファイル:** 6ファイル、+28/-12。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Use locked ty versions in uv check](https://github.com/astral-sh/uv/pull/19884) `#19884`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** `uv check` がプロジェクトの `uv.lock` の `dev` グループから到達可能な正確な `ty` バージョンを使うようにする。従来は uv 内蔵の互換範囲から解決していた。

**直近のアクティビティ:** zanieb が複数のレビューコメント。

**変更ファイル:** 9ファイル、+931/-17。

**次のアクション:** レビュー対応後マージ。

---

### ![PR](icons/git-pull-request.svg) [Take project environment lock when using `uv venv` in a project](https://github.com/astral-sh/uv/pull/19837) `#19837`
**ステータス:** Merged | **ラベル:** bug

**概要:** プロジェクト環境を操作する `uv venv` でグローバルワークスペースロックを取るようにする。他のプロジェクト系コマンドには既に入っていたが `uv venv` には抜けていた。

**直近のアクティビティ:** zsol が承認、EliteTK がコメント。

**変更ファイル:** 3ファイル、+98/-27。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Respect explicit NETRC paths](https://github.com/astral-sh/uv/pull/19865) `#19865`
**ステータス:** Closed | **ラベル:** breaking

**概要:** `NETRC` で指定したパスが存在しないとき既定の `.netrc` にフォールバックする挙動を改め、`NETRC` が設定されていればそのパスを権威とする。破壊的変更とみなされクローズされた。

**直近のアクティビティ:** zanieb と charliermarsh が破壊的変更か・バグ修正かを議論。EliteTK は承認していた。

**次のアクション:** Issue #19894 で挙動を再検討。

---

### ![Issue](icons/issue-opened.svg) [Consider erroring if `NETRC` is set to a missing file](https://github.com/astral-sh/uv/issues/19894) `#19894`
**ステータス:** Open | **ラベル:** needs-decision, breaking

**概要:** `NETRC` が存在しないファイルを指すとき既定ファイルへフォールバックする現挙動が望ましいか検討する。PR #19865 から派生。

**直近のアクティビティ:** zaniebot が curl など他ツールの挙動を調査し、明示的な netrc パスが無いとき既定にフォールバックするツールは見当たらないと報告。

**次のアクション:** 挙動の方針を決定する。

---

### ![Issue](icons/issue-opened.svg) [Consider a short name for Pyodide](https://github.com/astral-sh/uv/issues/19897) `#19897`
**ステータス:** Open | **ラベル:** enhancement, internal

**概要:** CPython・PyPy・GraalPy には長短名があるが Pyodide には無く、マーカー短名 "cp" が cpython と衝突する問題。Pyodide は CPython のディストリビューションで Emscripten が対象プラットフォームのため扱いが難しい。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 短名の付け方を検討。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate による依存更新と検出依存を一覧する自動管理 Issue。長期間更新のない放置依存（34件）も列挙する。

**直近のアクティビティ:** 直近24時間のコメントなし（自動更新）。

**次のアクション:** 自動管理のため対応不要。

---

### ![Issue](icons/issue-opened.svg) [Enterprise CA `UnsupportedCriticalExtension` TLS Failure](https://github.com/astral-sh/uv/issues/19903) `#19903`
**ステータス:** Open | **ラベル:** bug

**概要:** 企業の SSL インスペクション環境で、critical な `X509v3 Certificate Policies` 拡張を持つ中間証明書を rustls/webpki が拒否し HTTPS が失敗する。OpenSSL・curl・Git・ブラウザは同じチェーンを受け入れる。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** rustls/webpki の証明書ポリシー拡張の扱いを検討。

---

### ![Issue](icons/issue-opened.svg) [uv python list shows error regarding UV_PYTHON_INSTALL_MIRROR](https://github.com/astral-sh/uv/issues/19893) `#19893`
**ステータス:** Open | **ラベル:** bug

**概要:** Windows で `python-downloads-json-url` をローカルファイルや内部 URL に設定して `uv python list` を実行するとミラー形式エラーが出る。同じ設定が Linux では問題なく動く。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** Windows での URL 検証ロジックを調査。PR #19898 が関連。

---

### ![Issue](icons/issue-opened.svg) [failt to create venv in windows](https://github.com/astral-sh/uv/issues/19873) `#19873`
**ステータス:** Closed | **ラベル:** bug, windows, needs-mre

**概要:** Windows で `uv venv --seed` 実行時に Windows PE リソース更新失敗で seed パッケージインストールが失敗する。

**直近のアクティビティ:** woodruffw が #15968 の重複（AV 干渉）として整理。報告者がセキュリティツール停止で解決したと確認。

**次のアクション:** 重複としてクローズ済み。

---

### ![Issue](icons/issue-opened.svg) [uv run strips PATHEXT to .CPL on Windows](https://github.com/astral-sh/uv/issues/18222) `#18222`
**ステータス:** Open | **ラベル:** question, windows, needs-mre, external

**概要:** Windows で `uv run` が仮想環境を有効化する際に PATHEXT が `.CPL` だけに切り詰められ、子プロセスが `.exe`/`.bat` 等を名前解決できなくなる。MCP サーバ等に影響する。

**直近のアクティビティ:** sparrowt が PyCharm や Copilot プラグインの mcp.json 設定が原因の可能性を指摘。

**次のアクション:** 外部要因（IDE 設定）の切り分けを継続。

---

### ![Issue](icons/issue-opened.svg) [uv trampoline failed to spawn Python child process. entity not found (os error 2)](https://github.com/astral-sh/uv/issues/19390) `#19390`
**ステータス:** Open | **ラベル:** bug, windows

**概要:** 特定バージョンの Windows で、Python を要する全 uv コマンドが「uv trampoline failed to spawn Python child process」で失敗する。最新の Windows 10/11 では問題ない。

**直近のアクティビティ:** woodruffw が最新版での再現確認と AV/EDR 利用を質問。報告者がデバッグ協力を申し出て -vvv ログを提供。

**次のアクション:** 詳細ログを基に原因を特定。

---

### ![Issue](icons/issue-opened.svg) [uv with the Databricks Extensions in VSCode](https://github.com/astral-sh/uv/issues/17728) `#17728`
**ステータス:** Open | **ラベル:** question

**概要:** VSCode の Databricks 拡張のために venv を作り依存を入れる手順についての確認質問。

**直近のアクティビティ:** Kaszanas が同様の環境構築を試みており解決法を尋ねた。

**次のアクション:** 利用手順の回答待ち。

---

### ![Issue](icons/issue-opened.svg) [Declarative management of installed Python versions and tools](https://github.com/astral-sh/uv/issues/19888) `#19888`
**ステータス:** Open | **ラベル:** enhancement

**概要:** uv 管理の Python バージョンとグローバルツールを、宣言した正確な一覧へ一括収束（不足を入れ余分を消す）させる機能の要望。Nix/Ansible 等の構成管理での宣言的運用が動機。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 宣言的同期（`--exact`）の設計を検討。

---

### ![Issue](icons/issue-opened.svg) [Windows: git+file:///D:/... URLs are rewritten to file:///d/... and fail](https://github.com/astral-sh/uv/issues/19887) `#19887`
**ステータス:** Open | **ラベル:** bug

**概要:** Windows でローカル git リポジトリを `file:///D:/...` 形式で参照すると `file:///d/...` に書き換えられて解決に失敗する。`git ls-remote` では有効なリポジトリ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** Windows のファイル URL 正規化を修正。

---

## astral-sh/uv（続き）

### ![Issue](icons/issue-opened.svg) [uv add downloads macOS wheel instead of Windows amd64 wheel on Win64](https://github.com/astral-sh/uv/issues/15654) `#15654`
**ステータス:** Open | **ラベル:** question

**概要:** Windows amd64 環境で `uv add` が Windows 用ではなく macOS の wheel をダウンロードする一方、`uv pip install` は正しく動く。test.pypi ミラーが macOS 版を常に含まないことが一因とみられる。

**直近のアクティビティ:** lnzeta が `uv sync` でも同様の問題に遭遇し、platform 明示でも直らないと報告。

**次のアクション:** ミラーに版が欠ける場合のプラットフォーム解決を調査。

---

### ![Issue](icons/issue-opened.svg) [The term 'package' is problematically overloaded](https://github.com/astral-sh/uv/issues/17235) `#17235`
**ステータス:** Open | **ラベル:** documentation

**概要:** Python パッケージングで "package" が import package と distribution package の双方を指し混乱を招くという指摘。ドキュメントでの用語整理を求める。

**直近のアクティビティ:** Btocode が PyPA の用語解説ページへのリンク追加と、`--upgrade-package` 説明箇所への補足を提案。

**次のアクション:** ドキュメントへの用語明確化を検討。

---

### ![PR](icons/git-pull-request.svg) [Add a full "lockfile" to tool receipts](https://github.com/astral-sh/uv/pull/18937) `#18937`
**ステータス:** Open（ドラフト） | **ラベル:** enhancement

**概要:** ツールのレシートに `uv.lock` 相当の完全な解決結果を追加する。lock を実質的にレシートへインライン化し、他フィールドは `[tool]` 配下に残す。

**直近のアクティビティ:** charliermarsh と woodruffw がレビュー。woodruffw が承認しつつ zanieb の確認も推奨。

**変更ファイル:** 17ファイル、+4913/-400 の大規模変更。

**次のアクション:** zanieb のレビューを経てマージ。

---

## commaai/opendbc

> Volkswagen MQB の起動時警告修正と Nissan Leaf のフィンガープリント追加が進行中。

### ![PR](icons/git-pull-request.svg) [VW MQB: fix temporary "front assist not available" on startup](https://github.com/commaai/opendbc/pull/3267) `#3267`
**ステータス:** Open | **ラベル:** car, volkswagen

**概要:** VW MQB 車で起動直後にクラスタが「Front assist not available」と表示する問題を、`ACC_02` のカウンタをシードして修正する。#3228 を置き換える2本のうち1本目。`ACC_06`/`ACC_07`/`ACC_10` のカウンタもシードする。

**直近のアクティビティ:** JaCzekanski が正しいアプローチと評価し、long モード有効起動で DTC が保存され Front Assist/ACC が動かなくなる場合があると補足。

**変更ファイル:** 2ファイル、+12/-0。

**次のアクション:** レビューを経てマージ。

---

### ![PR](icons/git-pull-request.svg) [I fingerprinted my 2018 Leaf G (IC)](https://github.com/commaai/opendbc/pull/3292) `#3292`
**ステータス:** Open | **ラベル:** car, nissan, fingerprint

**概要:** 日本国内向けで後にニュージーランドへ輸出された 2018 Nissan Leaf G のフィンガープリントを追加する。ソフトのインストールと起動はできるが LKAS エラーが残る。

**直近のアクティビティ:** github-actions ボットが PR を draft にする等のチェックリストを案内。

**変更ファイル:** 1ファイル、+3/-1。

**次のアクション:** LKAS エラーの切り分けとレビュー。

---

## commaai/openpilot

> webrtc ライブストリームや UI/replay ツールの改善が中心で、多数の stale PR が自動クローズされている。autonomy 系のモデル更新やセンサ関連の整理も進む。

### ![PR](icons/git-pull-request.svg) [webrtc: REMB-based livestream bitrate controller](https://github.com/commaai/openpilot/pull/38189) `#38189`
**ステータス:** Open | **ラベル:** なし

**概要:** ライブストリームのビットレート制御を、パケット損失ヒューリスティックからブラウザの REMB（Receiver Estimated Maximum Bitrate）駆動の制御へ置き換える。推定値が下がるとエンコーダのビットレートを下げ、安定後に上げ直す。

**直近のアクティビティ:** process replay 差分レポートは 66 件パスで変化なし。

**変更ファイル:** 1ファイル、+46/-44。

**次のアクション:** レビューを経てマージ。

---

### ![PR](icons/git-pull-request.svg) [webrtc: do not replace connection on prewarm](https://github.com/commaai/openpilot/pull/38190) `#38190`
**ステータス:** Merged | **ラベル:** なし

**概要:** prewarm 状態（未有効）で開いた接続が既存の有効接続を置き換えないようにし、`StreamRequestBody` でパラメータ受け渡しを整理する。

**直近のアクティビティ:** process replay 差分は変化なし。

**変更ファイル:** 3ファイル、+21/-28。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Full RL](https://github.com/commaai/openpilot/pull/38164) `#38164`
**ステータス:** Open | **ラベル:** autonomy

**概要:** 運転モデルに関わる変更（driving_supercombo.onnx の更新を伴う）。

**直近のアクティビティ:** process replay 差分は変化なし。Model Review ボットが master と PR ブランチのモデルを比較。

**変更ファイル:** 1ファイル、+2/-2。

**次のアクション:** モデル評価結果を確認しレビュー。

---

### ![PR](icons/git-pull-request.svg) [New sounds](https://github.com/commaai/openpilot/pull/38154) `#38154`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** soundd の挙動変更で、保留アラートが無いときループ音を途中で切らず最後まで再生する。

**直近のアクティビティ:** UI プレビューは同一。process replay は2件変化。nickorie と elkoled がレビューコメント。

**変更ファイル:** 24ファイル、+190/-30。

**次のアクション:** 変化した挙動の確認とレビュー。

---

### ![Issue](icons/issue-opened.svg) [mici: add prime / account page](https://github.com/commaai/openpilot/issues/36673) `#36673`
**ステータス:** Open | **ラベル:** enhancement

**概要:** mici UI に prime ステータスとペアリング中アカウントを表示するアカウントページを追加する。

**直近のアクティビティ:** varshneydevansh が着手を希望し、`PrimeState` の取得エンドポイントに触れつつ表示内容を質問。

**次のアクション:** 表示項目を確定し実装。

---

### ![PR](icons/git-pull-request.svg) [add USB logging](https://github.com/commaai/openpilot/pull/38186) `#38186`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** 基本的な USB ロギングを追加する。カーネルエラーカウンタや usb3 設定の cloudlog、LTSSM リンク状態追跡を TODO とする。

**直近のアクティビティ:** adeebshihadeh が低レベル USB 状態と固有ペリフェラルの両方をログすべきとコメント。

**変更ファイル:** 3ファイル、+121/-0。

**次のアクション:** ログ設計の方針を反映しドラフト完成へ。

---

### ![PR](icons/git-pull-request.svg) [desire cleanups](https://github.com/commaai/openpilot/pull/38188) `#38188`
**ステータス:** Open | **ラベル:** なし

**概要:** desire 関連コードの整理。

**直近のアクティビティ:** process replay 差分は変化なし。

**変更ファイル:** 1ファイル、+18/-41。

**次のアクション:** レビューを経てマージ。

---

### ![PR](icons/git-pull-request.svg) [Desire keep pulsing is dead](https://github.com/commaai/openpilot/pull/38187) `#38187`
**ステータス:** Merged | **ラベル:** なし

**概要:** 使われていない desire keep pulsing のコードを削除する。

**直近のアクティビティ:** process replay 差分は変化なし。

**変更ファイル:** 1ファイル、+0/-11。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [bump tinygrad](https://github.com/commaai/openpilot/pull/38176) `#38176`
**ステータス:** Merged | **ラベル:** なし

**概要:** tinygrad のバージョンを更新する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2ファイル、+3/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [replay: fix current time](https://github.com/commaai/openpilot/pull/38179) `#38179`
**ステータス:** Merged | **ラベル:** tools

**概要:** replay TUI で `route_date_time_` が未初期化で ctime が NULL を返す問題と、スケール誤りによる誤タイムスタンプを修正する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2ファイル、+2/-2。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Yaw is zero](https://github.com/commaai/openpilot/pull/37825) `#37825`
**ステータス:** Open | **ラベル:** なし

**概要:** NED フレームでは絶対ヨー姿勢が観測不能なため、pose KF で追跡する状態数を減らしログの S/N を高める。共分散が長距離ルートで緩やかに増える問題に対し、ヨーを安全に無視できるか問う。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2ファイル、+21/-13。

**次のアクション:** ヨー無視の妥当性を議論。

---

### ![PR](icons/git-pull-request.svg) [fix: webjoystick connection from firefox](https://github.com/commaai/openpilot/pull/38130) `#38130`
**ステータス:** Closed | **ラベル:** tools

**概要:** Firefox での mDNS 問題解決のため STUN サーバを追加し、カメラを wideRoad に切り替える。

**直近のアクティビティ:** adeebshihadeh が変更要求。

**変更ファイル:** 3ファイル、+6/-4。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [ui: fix false "NO PANDA" flash on screen wake](https://github.com/commaai/openpilot/pull/37404) `#37404`
**ステータス:** Merged | **ラベル:** ui, stale

**概要:** 画面をタップして起こす際に一瞬「NO PANDA」が表示される問題を修正する。

**直近のアクティビティ:** andiradulescu がコメント、adeebshihadeh が一度変更要求。

**変更ファイル:** 内容軽微。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [ui diff: Chunk frame diffs and group in report with thumbnails](https://github.com/commaai/openpilot/pull/37328) `#37328`
**ステータス:** Open | **ラベル:** stale

**概要:** `difflib` でリプレイ動画のフレームハッシュを差分し、変更・追加・削除をグループ化してサムネイル付き HTML レポートに表示する。

**直近のアクティビティ:** TheSecurityDev がマージ競合を解消すると表明しレビューコメント。

**変更ファイル:** 3ファイル、+352/-77。

**次のアクション:** 競合解消後レビュー。

---

### ![PR](icons/git-pull-request.svg) [add theme toggle in jotpluggler](https://github.com/commaai/openpilot/pull/37985) `#37985`
**ステータス:** Open（ドラフト） | **ラベル:** tools

**概要:** jotpluggler にテーマ切り替え（ダークモード）を追加する。Issue #37964 に対応。

**直近のアクティビティ:** CodeMan62 が定数を theme.cc へ移動。adeebshihadeh が変更要求。

**変更ファイル:** 6ファイル、+196/-53。

**次のアクション:** 変更要求への対応。

---

### ![PR](icons/git-pull-request.svg) [ui diff: add multithreading and improve logging (split from #37328)](https://github.com/commaai/openpilot/pull/37474) `#37474`
**ステータス:** Merged | **ラベル:** ui, stale

**概要:** #37328 からマルチスレッド化とリファクタを切り出し、差分動画生成とフレームハッシュ生成を並列化して長尺動画の処理時間を短縮する。

**直近のアクティビティ:** TheSecurityDev と Copilot レビューボットがコメント。

**変更ファイル:** 1ファイル、+59/-47。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix(ui): fill and center onroad camera if too small](https://github.com/commaai/openpilot/pull/37673) `#37673`
**ステータス:** Merged | **ラベル:** ui, stale

**概要:** `BIG=1` で c4 ルートを再生する際にカメラ映像が画面を埋めず中央寄せされない問題を修正し、内側コンテンツ矩形を尊重する。

**直近のアクティビティ:** TheSecurityDev と Copilot レビューボットがコメント。

**変更ファイル:** 2ファイル、+11/-5。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Compile modeld warps with symbolic camera sizes](https://github.com/commaai/openpilot/pull/38078) `#38078`
**ステータス:** Open（ドラフト） | **ラベル:** stale

**概要:** modeld の warp をシンボリックなカメラサイズでコンパイルする。

**直近のアクティビティ:** 24日間活動がなく7日後に自動クローズ予定とボットが通知。

**変更ファイル:** 6ファイル、+168/-85。

**次のアクション:** 活動を再開しないと自動クローズ。

---

### ![PR](icons/git-pull-request.svg) [wifi: no NetworkManager](https://github.com/commaai/openpilot/pull/37902) `#37902`
**ステータス:** Closed（ドラフト） | **ラベル:** stale

**概要:** NetworkManager の WiFi 管理を wpa_supplicant ctrl-socket クライアント + udhcpc + dnsmasq に置き換える大規模変更。デーモンが UI 再起動を生き延びる設計。#37752 を解決し #37662 の継続。

**直近のアクティビティ:** 非活動により自動クローズ。

**変更ファイル:** 27ファイル、+6085/-1521。

**次のアクション:** 再開時に re-open。

---

### ![PR](icons/git-pull-request.svg) [build(openpilot): replace panda submodule with pip-distributed panda dependency](https://github.com/commaai/openpilot/pull/37908) `#37908`
**ステータス:** Closed | **ラベル:** stale

**概要:** panda サブモジュールを pip 配布の pandacan 依存に置き換え、SConstruct で panda パスを動的解決する。commaai/panda#2392 の検証 PR。

**直近のアクティビティ:** 非活動により自動クローズ。

**変更ファイル:** 5ファイル、+63/-6。

**次のアクション:** 再開時に re-open。

---

### ![PR](icons/git-pull-request.svg) [Revert "Revert "DM: Lancia Delta HF Integrale model (#37696)""](https://github.com/commaai/openpilot/pull/37976) `#37976`
**ステータス:** Closed（ドラフト） | **ラベル:** stale

**概要:** #37971 の revert を再 revert する（Lancia Delta HF Integrale モデルの再投入）。

**直近のアクティビティ:** 非活動により自動クローズ。

**変更ファイル:** 8ファイル、+24/-53。

**次のアクション:** 再開時に re-open。

---

### ![PR](icons/git-pull-request.svg) [ui: real kernel touch event timestamps](https://github.com/commaai/openpilot/pull/38019) `#38019`
**ステータス:** Closed（ドラフト） | **ラベル:** ui, stale

**概要:** カーネルの実タッチイベントタイムスタンプを使い、ポーリングを廃止する。

**直近のアクティビティ:** 非活動により自動クローズ。

**変更ファイル:** 1ファイル、+99/-5。

**次のアクション:** 再開時に re-open。

---

### ![PR](icons/git-pull-request.svg) [dreaming about a clean root](https://github.com/commaai/openpilot/pull/38036) `#38036`
**ステータス:** Closed（ドラフト） | **ラベル:** stale

**概要:** リポジトリのルート構成を整理する構想。サブモジュールを実 Python パッケージ化し、`common/`・`selfdrive/` を `openpilot/` 配下へ移し、`launch_chffrplus.sh` を廃止する必要がある。

**直近のアクティビティ:** 非活動により自動クローズ。

**変更ファイル:** 1138ファイル、+92/-1236。

**次のアクション:** 構想として保留。

---

## commaai/panda

> 直近は静的解析（cppcheck）の bounty Issue がクローズされた程度の動き。

### ![Issue](icons/issue-opened.svg) [cppcheck: catch ints as bools](https://github.com/commaai/panda/issues/2105) `#2105`
**ステータス:** Closed | **ラベル:** enhancement, bounty

**概要:** int を bool として扱う箇所を cppcheck で検出する。cppcheck への対応追加・上流化・有効化と違反修正の3段階。#1876 が背景。

**直近のアクティビティ:** varshneydevansh が完了とみなせると提案。

**次のアクション:** クローズ済み。

---

## ghostty-org/ghostty

> macOS の QuickTerminal タブや クリップボードトースト、RTL（アラビア・ヘブライ）シェイピングなど機能追加が活発で、GTK のクラッシュ修正や翻訳追加も進む。

### ![PR](icons/git-pull-request.svg) [font/shaper: add RTL shaping for Arabic and Hebrew](https://github.com/ghostty-org/ghostty/pull/11079) `#11079`
**ステータス:** Open | **ラベル:** font

**概要:** アラビア語・ヘブライ語が LTR 強制で分断・反転表示される問題に対し、Unicode 双方向アルゴリズム（UAX #9）による双方向シェイピングを追加する。スコープはシェイピングのみで、カーソル位置・選択は後続作業とする。純 Zig 製 itijah ライブラリを用いる。

**直近のアクティビティ:** d-knafo と zughayyar が必須機能だと支持。pluiedev が Fribidi でなく純 Zig 採用を評価しつつ変更要求。DiaaEddin がレビュー。

**変更ファイル:** 10ファイル、+1802/-298。

**次のアクション:** レビュー指摘に対応。

---

### ![PR](icons/git-pull-request.svg) [feat(macos): add tab support for QuickTerminal - continued](https://github.com/ghostty-org/ghostty/pull/9857) `#9857`
**ステータス:** Open | **ラベル:** os/macos

**概要:** QuickTerminal にタブ対応を追加する。#8469 を引き継ぎ、マージ競合を解消し、複数タブ時の cmd+w での閉じ方を改善する。タブが1つなら QuickTerminal を隠す現挙動を維持する。

**直近のアクティビティ:** bo2themax と wammy67 が承認。

**変更ファイル:** 19ファイル、+2129/-88。

**次のアクション:** マージへ。

---

### ![PR](icons/git-pull-request.svg) [Add macOS clipboard copy toast](https://github.com/ghostty-org/ghostty/pull/13037) `#13037`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** 既存の `app-notifications = clipboard-copy` 挙動に macOS 対応を追加する。GTK は Adwaita トーストを出すが macOS は無反応だったため、`didWriteClipboard` 通知を発行し短命の SwiftUI オーバーレイを表示する。

**直近のアクティビティ:** jagoff がトースト色をミュートグリーンに変更し、GTK のクリップボード意味論（標準クリップボード書き込みのみ）を維持する方針を説明。

**変更ファイル:** 6ファイル、+96/-5。

**次のアクション:** GTK との視覚整合を詰めドラフト完成へ。

---

### ![PR](icons/git-pull-request.svg) [Kitty dnd parser](https://github.com/ghostty-org/ghostty/pull/13029) `#13029`
**ステータス:** Open | **ラベル:** なし

**概要:** 新しい kitty ドラッグ&ドロップ（dnd）プロトコルのパーサを実装する（#12852）。アクションのフックはまだ行わず、`kitty_clipboard_protocol.zig` を参考に形式を組む。テストは Claude Opus 4.8 で作成した。

**直近のアクティビティ:** jcollie が承認しコメント追加を希望。ajr-khll がレビュー。

**変更ファイル:** 4ファイル、+381/-0。

**次のアクション:** コメント追加後、アクション実装へ進む。

---

### ![PR](icons/git-pull-request.svg) [Add Serbian translation](https://github.com/ghostty-org/ghostty/pull/13030) `#13030`
**ステータス:** Open | **ラベル:** translations

**概要:** `sr` ロケールのセルビア語翻訳を追加する。

**直近のアクティビティ:** 00-kat が `src/os/i18n_locales.zig` へのロケール追加が必要と指摘し、kostich が対応。

**変更ファイル:** 3ファイル、+321/-0。

**次のアクション:** ロケール登録を確認しマージ。

---

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/13036) `#13036`
**ステータス:** Merged | **ラベル:** なし

**概要:** ディスカッションのコメントを契機に VOUCHED リストを更新する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 1ファイル、+1/-0。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [macOS: Not possible to cmd+c/cmd+x from rename tab, search, other text fields](https://github.com/ghostty-org/ghostty/issues/10749) `#10749`
**ステータス:** Open | **ラベル:** os/macos, gui, input

**概要:** macOS で「Change Tab Title」ダイアログや検索などのテキストフィールドで Cmd+C/Cmd+X が効かず、ショートカットが黙って飲み込まれる。右クリックメニューでは操作できる。

**直近のアクティビティ:** Shagon94 が関連 PR が複数あるがどれを追うべきか質問。

**次のアクション:** 対応 PR を集約し修正。

---

### ![Issue](icons/issue-opened.svg) [GTK: typing in scrollback search overlay (Ctrl+Shift+F) crashes with SIGSEGV](https://github.com/ghostty-org/ghostty/issues/13035) `#13035`
**ステータス:** Closed | **ラベル:** gtk

**概要:** Linux（GTK ビルド）で Ctrl+Shift+F のスクロールバック検索オーバーレイに文字入力すると毎回 SIGSEGV でクラッシュする。`SearchOverlay.searchChanged` 経由の null ポインタ書き込みが原因。

**直近のアクティビティ:** ghostty-vouch ボットが非メンテナは Issue でなく Discussion を作るルールを案内しクローズ。

**次のアクション:** Discussion 経由で再提起。

---

### ![Issue](icons/issue-opened.svg) [Scrollbar Appearance Triggers Resize Overlay Loop](https://github.com/ghostty-org/ghostty/issues/12161) `#12161`
**ステータス:** Closed | **ラベル:** なし

**概要:** スクロールバーのオーバーレイ出現がリサイズイベントを誘発し、リサイズオーバーレイが約1秒ごとに無限に明滅する。`resize-overlay = never` 等で回避可能。

**直近のアクティビティ:** kepstin が #11970 の重複で既に修正済み（未リリース）と指摘。

**次のアクション:** 重複としてクローズ済み。

---

## neovim/neovim

> ライフサイクル（`:restart` の `v:startreason`）や TUI クラッシュ修正、メッセージ/UI2 周りの改善、vim-patch 取り込みとセキュリティ修正が並ぶ。GSoC の WASM ビルドや charwise range など大型機能も進行中。

### ![PR](icons/git-pull-request.svg) [feat(:restart): v:startreason](https://github.com/neovim/neovim/pull/40186) `#40186`
**ステータス:** Open | **ラベル:** AI assisted 🤖

**概要:** nvim が再起動から起動されたかを判別する `v:startreason` を導入する。Issue #40184 を解決し、Codex による AI 支援で実装した。

**直近のアクティビティ:** nathanzeng と superatomic がレビューコメント。

**変更ファイル:** 12ファイル、+69/-3。

**次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [`v:startreason` for `:restart`](https://github.com/neovim/neovim/issues/40184) `#40184`
**ステータス:** Open | **ラベル:** lifecycle

**概要:** oil.nvim 等のプラグインがセッション復元付き再起動時に挙動を変えられるよう、`vim.v.startreason == "restart"` を判別する vvar を要望。#39282 のフォローアップ。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR #40186 で対応中。

---

### ![Issue](icons/issue-opened.svg) [build(wasm): Add WebAssembly/Emscripten build target [Tracking issue]](https://github.com/neovim/neovim/issues/39987) `#39987`
**ステータス:** Open | **ラベル:** gsoc, wasm-webassembly, platform:web

**概要:** GSoC 2026 で WASM/Emscripten を公式ビルドターゲットに加え、ブラウザで完全な編集機能（シンタックスハイライト・Lua・プラグイン・xterm.js 端末）を持つ nvim.wasm を生成する取り組み。#35567 に対応。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** ビルドパイプライン・リンクのマイルストーンを進行。

---

### ![PR](icons/git-pull-request.svg) [feat(charwise_range): initial implementation [no ci]](https://github.com/neovim/neovim/pull/32816) `#32816`
**ステータス:** Open（ドラフト） | **ラベル:** AI assisted 🤖

**概要:** 文字単位の範囲指定（backtick 範囲や `x.y` 範囲で列情報をコマンドへ渡す）の初期実装。`nvim_parse_cmd()`/`nvim_cmd()` を更新する。#22297 を解決。

**直近のアクティビティ:** 616b2f と justinmk がレビューコメント。

**変更ファイル:** 43ファイル、+2427/-561。

**次のアクション:** ドキュメント更新と残タスクの完成。

---

### ![PR](icons/git-pull-request.svg) [feat(column): per row click handlers for 'statuscolumn'](https://github.com/neovim/neovim/pull/40265) `#40265`
**ステータス:** Open | **ラベル:** mouse, side-column

**概要:** 'statuscolumn' の行ごとのクリックハンドラを実装する。各行に一意な `v:virtnum` を与え、クリックされた行に応じて `v:virt/lnum` を設定する。#40210 を解決。

**直近のアクティビティ:** echasnovski がレビューコメント。

**変更ファイル:** 13ファイル、+228/-97。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix: crash when bad escape sequence enter SS3 handler](https://github.com/neovim/neovim/pull/40296) `#40296`
**ステータス:** Open | **ラベル:** tui

**概要:** `handle_csi_ss3_full` で cmd 引数の範囲チェックを追加し、不正なエスケープシーケンスによるクラッシュ（#40295）を修正する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 1ファイル、+5/-1。

**次のアクション:** レビューを経てマージ。

---

### ![Issue](icons/issue-opened.svg) [cmd arg in handle_csi_ss3_full in driver-csi.c can be outside of range leading to UB](https://github.com/neovim/neovim/issues/40295) `#40295`
**ステータス:** Closed | **ラベル:** terminal, bug-crash

**概要:** `driver-csi.c` の `handle_csi_ss3_full` で cmd 引数が範囲外になり未定義動作を起こす。0.12 更新後に顕在化。

**直近のアクティビティ:** justinmk が PR 作成を依頼し、trobicho が PR #40296 を提出。

**次のアクション:** PR #40296 で対応しクローズ済み。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.{0662,0663}](https://github.com/neovim/neovim/pull/40291) `#40291`
**ステータス:** Merged | **ラベル:** vim-patch, spell, AI assisted 🤖

**概要:** `:spelldump` で細工された spell ファイルが `dump_prefixes()` の MAXWLEN サイズ配列を越えるスタック境界外書き込み（セキュリティ）を修正する vim-patch を取り込む。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 4ファイル、+51/-4。

**次のアクション:** マージ済み。

---

### ![Issue](icons/issue-opened.svg) [OptionSet foldminlines callback in treesitter/_fold.lua crashes with Invalid buffer id](https://github.com/neovim/neovim/issues/40290) `#40290`
**ステータス:** Open | **ラベル:** needs:response, treesitter, folds

**概要:** プラグインがウィンドウに `foldminlines` を設定すると `OptionSet` autocmd が無効なバッファ id で `FoldInfo.new(bufnr)` を呼び「Invalid buffer id」でクラッシュする。

**直近のアクティビティ:** justinmk が最新の development 版での確認を依頼。

**次のアクション:** 最新版での再現確認待ち。

---

### ![Issue](icons/issue-opened.svg) [`TermProperty` event (was: "Expose terminal alternate-screen state")](https://github.com/neovim/neovim/issues/40293) `#40293`
**ステータス:** Open | **ラベル:** terminal, has:plan, events

**概要:** Neovim が内部で持つ端末の代替スクリーン状態（`in_altscreen`）を Lua/プラグインへ公開する要望。TUI（nvim/less/fzf 等）が動作中かを知り `<Esc>` マッピング上書きを避けるため。

**直近のアクティビティ:** justinmk が `nvim_get_chan_info()` への追加や `TermProperty` イベントの案を提示。

**次のアクション:** 公開方法（バッファプロパティ/イベント）を設計。

---

### ![Issue](icons/issue-opened.svg) [multigrid UI: message grid not cleared when a shorter message overwrites a longer one](https://github.com/neovim/neovim/issues/40292) `#40292`
**ステータス:** Open | **ラベル:** ui, messages

**概要:** `ext_multigrid` と `ext_cmdline` 有効（`ext_messages` 無効）時、短いメッセージが長いメッセージを上書きすると前のメッセージの末尾がメッセージグリッドに残る。`grid_clear`/`grid_scroll` が送られない。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** メッセージグリッドのクリア処理を修正。

---

### ![Issue](icons/issue-opened.svg) [nvim_get_var() can return `nil` for `v:null` values](https://github.com/neovim/neovim/issues/40299) `#40299`
**ステータス:** Open | **ラベル:** api, vimscript, encoding, lua

**概要:** `b:/v:/t:/w:` 変数で `v:null` が `vim.NIL`（Lua で truthy）として返る問題に対し、`vim.json.decode` 同様の `{ luanil }` パラメータを `nvim_get_var()` に追加する要望。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** API 拡張を検討。

---

### ![Issue](icons/issue-opened.svg) [`nvim_exec2()` accepts args (like `nvim_exec_lua`)](https://github.com/neovim/neovim/issues/40159) `#40159`
**ステータス:** Open | **ラベル:** api, vimscript, has:workaround, lua

**概要:** `nvim_exec_lua` は args を受けるが `nvim_exec2()` は受けないため、Vimscript へ引数を渡す手軽な方法がない。バッファローカル変数更新などで不便。

**直近のアクティビティ:** justinmk が再帰的 dict マージのワークアラウンドを提示。

**次のアクション:** `nvim_exec2` への args 追加を検討。

---

### ![Issue](icons/issue-opened.svg) [position of `syntax on` in `init.vim` affects eol/noeol/fixeol options](https://github.com/neovim/neovim/issues/40170) `#40170`
**ステータス:** Open | **ラベル:** has:workaround, lifecycle, treesitter, events

**概要:** `syntax on` の記述位置によって、`.md`/`.lua` 等の特定拡張子ファイルで末尾改行が意図せず削除される。treesitter の ftplugin が関係。

**直近のアクティビティ:** v0lker がワークアラウンド（ftplugin 削除）が一時的・限定的だと指摘。

**次のアクション:** treesitter ftplugin と eol オプションの相互作用を調査。

---

### ![PR](icons/git-pull-request.svg) [fix(spell): restore z= state in invoking window](https://github.com/neovim/neovim/pull/40298) `#40298`
**ステータス:** Open（ドラフト） | **ラベル:** spell, AI assisted 🤖

**概要:** `z=` がスペル候補を `vim.ui.select()` に委譲する際、fzf-lua 等のピッカーで `curwin` が変わりピッカー側に無効なカーソル行が残る問題を修正する。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 2ファイル、+111/-7。

**次のアクション:** ドラフト完成へ。

---

### ![PR](icons/git-pull-request.svg) [fix(messages): "progress" kind for busy messages](https://github.com/neovim/neovim/pull/39280) `#39280`
**ステータス:** Merged | **ラベル:** ui-extensibility, messages, ui2

**概要:** "Scanning:" 補完や bufwrite、indent などの進捗メッセージに "progress" kind を付与し、UI が置き換え可能なよう msg id を設定する。

**直近のアクティビティ:** release-0.12 へのバックポートがチェリーピック失敗。superatomic と luukvbaal がレビュー。

**変更ファイル:** 10ファイル、+75/-35。

**次のアクション:** 手動バックポート（#39473 で対応）。

---

### ![PR](icons/git-pull-request.svg) [fix(cmdline): investigate repeated line redraws](https://github.com/neovim/neovim/pull/40240) `#40240`
**ステータス:** Open（ドラフト） | **ラベル:** cmdline-mode

**概要:** lualine 有効時にコマンドライン入力が折り返し後に繰り返し再描画される問題を、`redrawcmd()` 後に `cmdline_was_last_drawn` を true に戻して解消する。

**直近のアクティビティ:** luukvbaal が修正方針を妥当とし、`msg_puts_display` 経由の間接を不要と指摘。

**変更ファイル:** 1ファイル、+1/-0。

**次のアクション:** 簡素化案を反映しドラフト完成へ。

---

### ![Issue](icons/issue-opened.svg) [statusline disappears on cmdline message with `vim._core.ui2` + `cmdheight=0`](https://github.com/neovim/neovim/issues/40282) `#40282`
**ステータス:** Closed | **ラベル:** closed:question, ui2

**概要:** `vim._core.ui2` 有効時に yank やプラグインの cmdline 出力でステータスラインが消え、カーソル移動で戻る。

**直近のアクティビティ:** clason が「UI2 の設計どおりの挙動」と説明。pudep が nightly でも再現すると報告。

**次のアクション:** 仕様としてクローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Let's add Tokyonight-moon as the Built-in theme!](https://github.com/neovim/neovim/issues/40288) `#40288`
**ステータス:** Closed | **ラベル:** defaults

**概要:** Tokyonight Moon を組み込みテーマに加える要望。

**直近のアクティビティ:** clason がデフォルト以外の同梱テーマは Vim runtime 由来で外部テーマは同梱しない方針と説明。AI 支援の Issue ではという疑義も出た。

**次のアクション:** 方針によりクローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Lua can check if bracketedpaste is enabled in a `:terminal`](https://github.com/neovim/neovim/issues/35414) `#35414`
**ステータス:** Open | **ラベル:** clipboard, terminal

**概要:** `:terminal` で bracketedpaste が有効かを Lua から判別できるようにする要望。vim-slime 的用途で有用。

**直近のアクティビティ:** justinmk が `nvim_get_chan_info()` への追加と `TermProperty` イベントに賛意。

**次のアクション:** チャネル情報への追加を検討。

---

### ![PR](icons/git-pull-request.svg) [vim-patch: runtime file updates](https://github.com/neovim/neovim/pull/40228) `#40228`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** pfmain・beancount などの runtime ファイル更新を上流から取り込む。

**直近のアクティビティ:** ambroisie と zeertzjq がレビュー。

**変更ファイル:** 9ファイル、+540/-0。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [build(zig): add formatc and lintc steps](https://github.com/neovim/neovim/pull/38546) `#38546`
**ステータス:** Open | **ラベル:** ci:skip-news

**概要:** build.zig に formatc と lintc のステップを追加する。

**直近のアクティビティ:** dchinmay2 が複数のレビューコメント。

**変更ファイル:** 8ファイル、+219/-14。

**次のアクション:** レビュー対応。

---

### その他の neovim の更新

- ![PR](icons/git-pull-request.svg) [fix(runtime): don't use vim.bindeval()](https://github.com/neovim/neovim/pull/40297) `#40297`（Open）— ランタイムでの `vim.bindeval()` 利用を排除。zeertzjq・janlazo がレビュー。
- ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0567: dict function name allocation failure not handled](https://github.com/neovim/neovim/pull/40287) `#40287`（Merged）— dict 関数名のメモリ確保失敗を処理。zeertzjq 承認。
- ![PR](icons/git-pull-request.svg) [fix(lsp): allow specifying bufnr and client_id when enabling capabilities](https://github.com/neovim/neovim/pull/37690) `#37690`（Merged）— LSP capability 有効化の bufnr/client_id 排他制約を撤廃。#40294 として 0.12 へバックポート。
- ![PR](icons/git-pull-request.svg) [backport: fix(lsp): allow specifying bufnr and client_id](https://github.com/neovim/neovim/pull/40294) `#40294`（Open）と [backport: fix(lsp): catch another nil buffer state](https://github.com/neovim/neovim/pull/40286) `#40286`（Merged）— LSP 修正の release-0.12 バックポート。
- ![PR](icons/git-pull-request.svg) [backport: message kind for :recover and swapfile attention](https://github.com/neovim/neovim/pull/39473) `#39473`（Merged）— #39280/#39444 の 0.12 バックポート。
- ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40289) `#40289`（ドラフト）、[vim-patch:partial doc style](https://github.com/neovim/neovim/pull/40300) `#40300`（Merged）、[docs: reorder mailmap entry](https://github.com/neovim/neovim/pull/40301) `#40301`（Open）— ドキュメント・GTK4 系 vim-patch・mailmap 整理。
- ![PR](icons/git-pull-request.svg) [ci: bump github/codeql-action 4.36.1→4.36.2](https://github.com/neovim/neovim/pull/40192) `#40192`（Merged）— CI 依存更新。
- ![Issue](icons/issue-opened.svg) [lsp: inlay_hint decoration provider crashes "Invalid 'col'"](https://github.com/neovim/neovim/issues/39772) `#39772`（Closed）— 行短縮後の inlay hint 列位置クラッシュ。marcinwojtach が修正確認。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新はありません。

---

## qmk/qmk_firmware

> ChibiOS 更新後の USB HID 回帰と、エンドポイントサイズ検証・repeat_key 実装の修正が進む。

### ![Issue](icons/issue-opened.svg) [[Bug] STM32G0B1 USB HID broken after ChibiOS update #25730](https://github.com/qmk/qmk_firmware/issues/26265) `#26265`
**ステータス:** Closed | **ラベル:** bug, help wanted

**概要:** #25730 の ChibiOS 更新後、STM32G0B1CBT6 で USB HID デバイスが認識されず Windows/Linux で HID として列挙されない。

**直近のアクティビティ:** thelaurenliu が ChibiOS 側に根本原因を特定し、そちらへ直接報告すると表明。

**次のアクション:** ChibiOS 側での対応へ移管しクローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Endpoint sizing validation.](https://github.com/qmk/qmk_firmware/pull/26268) `#26268`
**ステータス:** Open（ドラフト） | **ラベル:** bug, core

**概要:** エンドポイントサイズが4の倍数である必要があるため、ChibiOS（RP2040 以上）で Plover HID 使用時のサイズ起因の非ブート障害を修正する検証を追加する。joystick 定義を `sizeof` ベースに変更する。

**直近のアクティビティ:** tzarc が V-USB の整理中のためドラフトとコメント。

**変更ファイル:** 2ファイル、+82/-15。

**次のアクション:** V-USB 対応後にドラフト完成へ。

---

### ![PR](icons/git-pull-request.svg) [repeat_key.c: add implementation for get_last_record](https://github.com/qmk/qmk_firmware/pull/26263) `#26263`
**ステータス:** Open | **ラベル:** core

**概要:** `repeat_key.h` に宣言があるが `repeat_key.c` に実装がなく、`get_last_record` 利用時にコンパイルエラーになる問題を修正する。

**直近のアクティビティ:** drashna が承認、tzarc と precondition がコメント。

**変更ファイル:** 2ファイル、+5/-1。

**次のアクション:** レビューを経てマージ。

---

## sunnypilot/sunnypilot

> HKG（現代・起亜）の ccNC ポート（非 HDA2）対応が進行中。

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/sunnypilot/sunnypilot/pull/1306) `#1306`
**ステータス:** Open（ドラフト） | **ラベル:** car

**概要:** 現代・起亜車の ccNC（非 HDA2）ポートを追加する。sunnypilot/opendbc#94 と連携。

**直近のアクティビティ:** 直近24時間のコメントなし。

**変更ファイル:** 1ファイル、+1/-1。

**次のアクション:** opendbc 側の対応と合わせてレビュー。

---

## microsoft/vscode

> 直近24時間は Issue・PR ともに取得上限の50件ずつに達する高活動。大半が Copilot / Agent Host（AH）関連で、エージェントセッションのコスト表示・ツールピッカー・モデル別プロンプト、ワークスペース信頼の既定 Restricted Mode 化やサンドボックスのネットワーク制御といったセキュリティ変更が中心。多数のユーザー報告 Issue（Copilot 不具合・スパム）はクローズされている。

### ![PR](icons/git-pull-request.svg) [AH: align autopilot and assisted approvals with CLI](https://github.com/microsoft/vscode/pull/321865) `#321865`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** Agent Host のセッション設定を Copilot CLI に合わせ、Autopilot をセッションのモード軸へ移し「Assisted Approvals」権限を新設する。モード+権限の既定設定も追加する。

**直近のアクティビティ:** Copilot レビューボットと DonJayamanne がコメント。

**変更ファイル:** 22ファイル、+691/-288。

**次のアクション:** レビュー対応後マージ。

---

### ![PR](icons/git-pull-request.svg) [Add per-model system-prompt registry for Copilot agent-host sessions](https://github.com/microsoft/vscode/pull/321864) `#321864`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** Copilot CLI の Agent Host セッションにモデル別のシステムプロンプトレジストリを追加し、固定定数でなくセッションのモデルに応じて SDK の `systemMessage` を解決する。Claude Opus 4.8 向けのプロンプトカスタマイズをオプトインで前方転送する。

**直近のアクティビティ:** Copilot レビューボットがコメント。

**変更ファイル:** 13ファイル、+686/-13。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Enable tool picker in Agent Host sessions](https://github.com/microsoft/vscode/pull/321825) `#321825`
**ステータス:** Open | **ラベル:** chat-tools, agent-host

**概要:** Agent Host セッションでローカルチャットと同じツールピッカー UI から VS Code 組み込み・拡張寄与の言語モデルツールを選べるようにする。従来は静的な `chat.agentHost.clientTools` 設定で固定だった。

**直近のアクティビティ:** DonJayamanne が `problems` や notebook 系ツールを AH から除外すべきと指摘し、変更要求の後に承認。

**変更ファイル:** 13ファイル、+520/-144。

**次のアクション:** 除外対象ツールの反映を経てマージ。

---

### ![PR](icons/git-pull-request.svg) [Show per-turn costs for Claude in Agent Host](https://github.com/microsoft/vscode/pull/321686) `#321686`
**ステータス:** Merged | **ラベル:** なし

**概要:** Claude Agent Host の SDK→プロトコルマッパーがターンごとのコスト情報を `chat/usage` メタデータに載せ、ワークベンチがターン単位のコストを表示できるようにする。#317387・#317196 を解決。

**直近のアクティビティ:** DonJayamanne が承認。

**変更ファイル:** 8ファイル、+317/-11。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Display session cost + persist context widget](https://github.com/microsoft/vscode/pull/321803) `#321803`
**ステータス:** Merged | **ラベル:** なし

**概要:** Copilot のクレジット消費（ターン単位とセッション合計）をモデル/シリアライズパイプラインに加え、コンテキスト利用詳細 UI に表示し、コンテキストウィジェットを永続化する。#315283・#303357 を実装。

**直近のアクティビティ:** eleanorjboyd が承認。

**変更ファイル:** 21ファイル、+189/-48。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [chat: add viewUnreviewedComments agent-host tool with confirmation UI](https://github.com/microsoft/vscode/pull/321804) `#321804`
**ステータス:** Merged | **ラベル:** なし

**概要:** 未受理（created 状態）の PR/コードレビューコメントをエージェントへ提示する `viewUnreviewedComments` ツールを追加する。`listComments` 結果に件数の注記を付け、確認 UI を介して表示する。

**直近のアクティビティ:** alexdima が承認。スクリーンショット差分ボットが新規画像を生成。

**変更ファイル:** 23ファイル、+973/-25。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Terminal command risk assessment](https://github.com/microsoft/vscode/pull/313952) `#313952`
**ステータス:** Merged | **ラベル:** なし

**概要:** チャットのツール確認に LLM ベースのターミナルコマンドリスク評価を追加する。新しいチャット設定キーとブラウザサービスを導入する。#313991 に対応。

**直近のアクティビティ:** roblourens が承認。

**変更ファイル:** 16ファイル、+721/-7。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Sandbox: AllowNetwork option to allow outgoing traffic in sandbox mode](https://github.com/microsoft/vscode/pull/313912) `#313912`
**ステータス:** Merged | **ラベル:** なし

**概要:** サンドボックスモードで外向き通信を許可する `AllowNetwork` オプションを追加する。#313276 を解決。

**直近のアクティビティ:** vijayupadya と pwang347 が承認。

**変更ファイル:** 24ファイル、+214/-134。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Guide terminal tool to request sandbox network/unsandboxed access proactively](https://github.com/microsoft/vscode/pull/321650) `#321650`
**ステータス:** Open | **ラベル:** なし

**概要:** ターミナルツールが、ネットワークを要するコマンド（git fetch/pull/push、パッケージ導入等）や非サンドボックス実行を要する書き込みで、初回試行から `requestAllowNetwork`/`requestUnsandboxedExecution` を立てるようサンドボックスガイダンスを改修する。失敗→再試行を避ける。

**直近のアクティビティ:** Copilot レビューボットが1件指摘。pwang347 と dileepyavan が承認。

**変更ファイル:** 15ファイル、+630/-106。

**次のアクション:** マージへ。

---

### ![PR](icons/git-pull-request.svg) [Remove dismissing of the trust dialog since workspace is now untrusted by default](https://github.com/microsoft/vscode/pull/321854) `#321854`
**ステータス:** Merged | **ラベル:** engineering

**概要:** 新規ワークスペースが既定で untrusted になったのに合わせ、サニティ UI テストで信頼ダイアログを自動承認する処理を削除する。vscode-engineering#3086 を修正。

**直近のアクティビティ:** vritant24 と jruales が承認。dmitrivMS がビルドのテスト通過後にマージすると表明。

**変更ファイル:** 1ファイル、+0/-10。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Fix Linux sanity UI test for Restricted-Mode-first workspace trust flow](https://github.com/microsoft/vscode/pull/321708) `#321708`
**ステータス:** Closed（ドラフト） | **ラベル:** なし

**概要:** untrusted ワークスペースが直接 Restricted Mode で開くようになり（`workspace.trust.startupPrompt` 既定 `'never'`）、起動時の信頼モーダルが出なくなったため、サニティ UI テストの待機処理を新しいアフォーダンスへ更新する。

**直近のアクティビティ:** rwoll と dmitrivMS がコメント。

**変更ファイル:** 1ファイル、+14/-5。

**次のアクション:** クローズ済み（#321854 に集約）。

---

### ![PR](icons/git-pull-request.svg) [Default `git.addAICoAuthor` to `off`](https://github.com/microsoft/vscode/pull/313725) `#313725`
**ステータス:** Closed | **ラベル:** なし

**概要:** ユーザー通知なしに AI の Co-authored-by トレーラーを自動付与することへの批判（HN 等で議論）を受け、組み込み Git 拡張で既定をオフにする提案。

**直近のアクティビティ:** Copilot レビューボットがコメント。

**変更ファイル:** 2ファイル、+2/-2。

**次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [Suppress Copilot-Integration-Id on CAPI models discovery](https://github.com/microsoft/vscode/pull/321855) `#321855`
**ステータス:** Merged | **ラベル:** なし

**概要:** プレミアムモデル探索で `Copilot-Integration-Id` ヘッダを抑制できるようにする。これがないと `no_auth_limited_copilot` SKU が `vscode-nl` 統合扱いとなりプレミアムモデルが拒否される。#321858 の修正候補。

**直近のアクティビティ:** TylerLeonhardt と dmitrivMS がコメント・承認。

**変更ファイル:** 5ファイル、+23/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Instrument Cloud Agent backend v1/v2 rollout behind an experiment](https://github.com/microsoft/vscode/pull/321818) `#321818`
**ステータス:** Merged | **ラベル:** なし

**概要:** Cloud Agent バックエンドの新版（Task API v2）を既存（Jobs API v1）と並行展開し健全性を比較できるよう、`chat.cloudAgentBackend.version` を実験駆動にし共通計測層を追加する。

**直近のアクティビティ:** pwang347 が承認。

**変更ファイル:** 11ファイル、+499/-71。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Avoid unneccesarily deleting dirs when syncing customizations](https://github.com/microsoft/vscode/pull/321860) `#321860`
**ステータス:** Merged | **ラベル:** なし

**概要:** `SyncedCustomizationBundler` が同期カスタマイズのプラグインツリーを毎回削除・再生成するのを避けて最適化する。#320303 に対応。

**直近のアクティビティ:** justschen が承認、DonJayamanne がコメント。

**変更ファイル:** 2ファイル、+181/-25。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: handle pending_confirmation signals when no active protocol turn exists](https://github.com/microsoft/vscode/pull/321843) `#321843`
**ステータス:** Open | **ラベル:** なし

**概要:** Agent Host セッションで、アクティブなプロトコルターンがないときに `pending_confirmation` シグナルが届くと「thinking」で永久に止まる問題を修正する。サブエージェント完了後の継続でツールが許可承認を要する場合に発生。

**直近のアクティビティ:** Copilot レビューボットと roblourens がコメント。

**変更ファイル:** 2ファイル、+81/-4。

**次のアクション:** レビュー対応後マージ。

---

### ![PR](icons/git-pull-request.svg) [fix: memory leak in ipc](https://github.com/microsoft/vscode/pull/293913) `#293913`
**ステータス:** Merged | **ラベル:** triage-needed

**概要:** ipc の `onDidClientConnect` で登録した2つの disposable がクライアント切断時に破棄されないメモリリークを修正する（ウィンドウ開閉のたびに発生）。

**直近のアクティビティ:** joaomoreno と bpasero が承認。

**変更ファイル:** 1ファイル、+10/-3。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [skip sandbox approvals for autopilot/bypass](https://github.com/microsoft/vscode/pull/313940) `#313940`
**ステータス:** Merged | **ラベル:** なし

**概要:** AutoApprove/Autopilot（承認バイパス）モードのセッションでは、サンドボックスの自動アンサンドボックス再試行確認を出さないよう調整する。#313903 を修正。

**直近のアクティビティ:** joshspicer が承認。

**変更ファイル:** 2ファイル、+11/-2。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [Add Space key to toggle breakpoint checkboxes in breakpoints view](https://github.com/microsoft/vscode/pull/313972) `#313972`
**ステータス:** Merged | **ラベル:** なし

**概要:** ブレークポイントサイドバーでフォーカス中の項目に Space キーで有効/無効を切り替えるキーハンドラを追加する。#313859 に対応。

**直近のアクティビティ:** justschen が承認、roblourens がコメント。

**変更ファイル:** 1ファイル、+16/-1。

**次のアクション:** マージ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: keyword.control and storage.modifier colors in 2026 Dark](https://github.com/microsoft/vscode/pull/315356) `#315356`
**ステータス:** Open | **ラベル:** なし

**概要:** `2026-dark.json` が keyword/storage を赤に設定する一方、include する `dark_modern.json` の `keyword.control`（紫）・`storage.modifier`（青）がより詳細で優先され、`return`/`async` 等が意図と違う色になる問題を修正する。

**直近のアクティビティ:** Copilot レビューボットがコメント。

**変更ファイル:** 1ファイル、+12/-0。

**次のアクション:** レビューを経てマージ。

---

### ![PR](icons/git-pull-request.svg) [Add Responses API cache control markers](https://github.com/microsoft/vscode/pull/321850) `#321850`
**ステータス:** Open（ドラフト） | **ラベル:** なし

**概要:** Responses API の入力アイテムへ生のキャッシュブレークポイントを伝播し、メッセージ内容・関数呼び出し・ツール検索アイテム等に `cache_control` マーカーを付与する。

**直近のアクティビティ:** Copilot レビューボットがコメント。

**変更ファイル:** 2ファイル、+243/-1。

**次のアクション:** ドラフト完成へ。

---

### ![Issue](icons/issue-opened.svg) [MCP OAuth: VS Code does not evict a stale dynamically-registered client / expired token on HTTP 401](https://github.com/microsoft/vscode/issues/321834) `#321834`
**ステータス:** Open | **ラベル:** なし

**概要:** APISIX + Keycloak の背後のリモート MCP サーバで、HTTP 401 時に VS Code が失効した動的登録クライアントや期限切れトークンを破棄せず再登録・再認証をトリガしない。401 がトークン期限切れ・失効・登録陳腐化のいずれかで曖昧なことが原因。

**直近のアクティビティ:** Ben-Home が `WWW-Authenticate` の `error="invalid_token"` 利用などクライアント側検出の難しさを詳説。

**次のアクション:** 401 後のトークン/登録ライフサイクル処理を改善。

---

### ![Issue](icons/issue-opened.svg) [Enterprise MCP OAuth: support stronger client assurance than public client_id + loopback redirect](https://github.com/microsoft/vscode/issues/321838) `#321838`
**ステータス:** Open | **ラベル:** なし

**概要:** OAuth で保護されたリモート MCP サーバ向けに、公開クライアント（client_id + PKCE + ループバック）より強いエンタープライズ級の認証モデルを VS Code に求める要望。

**直近のアクティビティ:** Ben-Home がプライベートクライアントメタデータなど強いクライアント保証の3パターンを提案。

**次のアクション:** エンタープライズ向け認証モデルの検討。

---

### ![Issue](icons/issue-opened.svg) [Agents Window Agent Harness with BYOK Support](https://github.com/microsoft/vscode/issues/320339) `#320339`
**ステータス:** Open | **ラベル:** feature-request, model-byok

**概要:** VS Code の Agents Window で BYOK（自前キー）モデルとカスタムエージェントをサポートし、ローカル開発・テスト用の使えるエージェントハーネスを提供する要望。

**直近のアクティビティ:** hu3bi と gtcgn が BYOK モデル追加が非直感的で、opencode プラグインへ移った経緯を共有。

**次のアクション:** BYOK 対応の設計を検討。

---

### ![Issue](icons/issue-opened.svg) [Linux agent sandboxing broken when `allowNetwork` mode is set](https://github.com/microsoft/vscode/issues/320504) `#320504`
**ステータス:** Open | **ラベル:** info-needed, agent-sandbox

**概要:** Linux で `allowNetwork` モード設定時にエージェントサンドボックスが壊れる。

**直近のアクティビティ:** dileepyavan が `chat.editor.localAgent.enabled` の利用を提案したが、hawkticehurst は Insiders 1.126 でも同じ結果だと報告。

**次のアクション:** Linux サンドボックスの allowNetwork 不具合を調査。

---

### ![Issue](icons/issue-opened.svg) [Freeze when opening any dialog V1.123+](https://github.com/microsoft/vscode/issues/321312) `#321312`
**ステータス:** Open | **ラベル:** なし

**概要:** 1.123 以降、任意のダイアログを開くとフリーズする。

**直近のアクティビティ:** qianlifeng が 1.125.0（Windows）でフリーズでなくクラッシュすると確認し、`window.dialogStyle` を custom にすると改善すると報告。

**次のアクション:** ダイアログ描画の不具合を調査。

---

### ![Issue](icons/issue-opened.svg) [Compact conversation doesnt work in AH](https://github.com/microsoft/vscode/issues/321826) `#321826`
**ステータス:** Open | **ラベル:** context-widget, agent-host

**概要:** コンテキストウィジェットの「Compact Conversation」アクションが Agent Host セッションで動作しない（クリックしても無反応）。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** AH での compact 動作を実装。

---

### ![Issue](icons/issue-opened.svg) [Model selected by Auto does not show in agent host sessions](https://github.com/microsoft/vscode/issues/321032) `#321032`
**ステータス:** Open | **ラベル:** agent-host

**概要:** Agent Host で Auto を選んでリクエストを実行しても、完了後に選ばれたモデルが表示されない（ローカルセッションでは表示される）。

**直近のアクティビティ:** dmitrivMS が再現できず、アカウント固有の要因か質問。

**次のアクション:** 再現条件の切り分け。

---

### ![Issue](icons/issue-opened.svg) [Agents + AHP: Use generated session title for worktree name](https://github.com/microsoft/vscode/issues/321801) `#321801`
**ステータス:** Open | **ラベル:** agents-window, agent-host

**概要:** 現在はチャットセッションの worktree 名に冒頭の数語を使うが、生成されたセッションタイトルを worktree 名にも使いたいという要望。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** PR #321866（worktree 名生成）と連携。

---

### ![Issue](icons/issue-opened.svg) [Add a font family setting for code blocks in Markdown Preview](https://github.com/microsoft/vscode/issues/321712) `#321712`
**ステータス:** Open | **ラベル:** なし

**概要:** Markdown プレビューのコードブロックに、エディタのフォントとは別のフォントファミリーを選べる設定を追加する要望。

**直近のアクティビティ:** 直近24時間のコメントなし。

**次のアクション:** 設定追加を検討。

---

### その他の主な PR

- ![PR](icons/git-pull-request.svg) [Add support for Copilot CLI quotas in agent host](https://github.com/microsoft/vscode/pull/321867) `#321867`（ドラフト）と [agentHost: generate Copilot worktree branch names](https://github.com/microsoft/vscode/pull/321866) `#321866`（ドラフト）— AH のクォータ伝播と worktree ブランチ名の自動生成。
- ![PR](icons/git-pull-request.svg) [Restore model config for chat sessions](https://github.com/microsoft/vscode/pull/321672) `#321672`（Merged）— 選択モデルの thinking effort 等をセッションへ復元。#321405 修正。
- ![PR](icons/git-pull-request.svg) [Refine OTel monitoring UI](https://github.com/microsoft/vscode/pull/321848) `#321848`（Open）— OTel 監視を Copilot ステータスダッシュボードへ移しユーザースコープに限定。roblourens 承認。
- ![PR](icons/git-pull-request.svg) [Add telemetry for chat quota notification banners](https://github.com/microsoft/vscode/pull/321793) `#321793` / [Add chat quota trajectory nudge](https://github.com/microsoft/vscode/pull/320683) `#320683` / [track tool used](https://github.com/microsoft/vscode/pull/321847) `#321847` / [Add SKU to enhance GH telemetry events](https://github.com/microsoft/vscode/pull/321046) `#321046`（Merged）— クォータ通知・ツール使用・SKU のテレメトリ整備。
- ![PR](icons/git-pull-request.svg) [Show session types as disabled with `Sign in` button when user is offline](https://github.com/microsoft/vscode/pull/321861) `#321861`（ドラフト）— オフライン時に Copilot 系セッション種別を無効表示。#321805 修正。pwang347 承認。
- ![PR](icons/git-pull-request.svg) [Auto-continue old local chat in Copilot CLI agent host](https://github.com/microsoft/vscode/pull/321811) `#321811`（Open）— ローカルチャットを AH へ自動継続。
- ![PR](icons/git-pull-request.svg) [Fix context window widget context mismatch for agent host](https://github.com/microsoft/vscode/pull/321862) `#321862`（ドラフト）— AH のコンテキスト窓ウィジェットの不一致を修正。
- ![PR](icons/git-pull-request.svg) [Document GitHub Copilot managed settings in add-policy skill](https://github.com/microsoft/vscode/pull/321851) `#321851`（ドラフト）— エンタープライズ管理設定のドキュメント追加。
- ![PR](icons/git-pull-request.svg) 設定検索の同義語追加 [Add 'hint'/'info' to editor.hover.enabled](https://github.com/microsoft/vscode/pull/313491) `#313491`、[Add 'pane' to editor group settings](https://github.com/microsoft/vscode/pull/313490) `#313490`（ともに Merged）。
- ![PR](icons/git-pull-request.svg) [Fix open router prompt caching with messages API](https://github.com/microsoft/vscode/pull/312435) `#312435`（Merged）、[sessions: fix theme import and refactor preview API](https://github.com/microsoft/vscode/pull/313965) `#313965`（Merged、オンボーディングでのテーマ誤アンインストール修正）。
- ![PR](icons/git-pull-request.svg) UI 微調整 [past tense for optimizing tool selection](https://github.com/microsoft/vscode/pull/313989) `#313989`、[make border progress default true](https://github.com/microsoft/vscode/pull/313980) `#313980`、[include theme preview svgs](https://github.com/microsoft/vscode/pull/313958) `#313958`（いずれも Merged）。
- ![PR](icons/git-pull-request.svg) 依存更新 [Bump form-data 3.0.4→3.0.5](https://github.com/microsoft/vscode/pull/321533) `#321533`、[Bump webpack-dev-server and @rspack/cli](https://github.com/microsoft/vscode/pull/321856) `#321856`。
- ![PR](icons/git-pull-request.svg) クローズ済み提案 [feat: /cwd slash command](https://github.com/microsoft/vscode/pull/313955) `#313955`・[#313949](https://github.com/microsoft/vscode/pull/313949)（セッション別 CWD）、[Support validating all markdown files](https://github.com/microsoft/vscode/pull/300688) `#300688`、チャットリソース保持の修正 [#302472](https://github.com/microsoft/vscode/pull/302472)・[#302476](https://github.com/microsoft/vscode/pull/302476)、[swc sourcemaps](https://github.com/microsoft/vscode/pull/163660) `#163660`、[docs fix](https://github.com/microsoft/vscode/pull/306065) `#306065`。

### クローズされた主な Issue

- 解決確認: [Terminal: Selection background appears on Backspace/Space/Esc (Win10)](https://github.com/microsoft/vscode/issues/318910) `#318910`（PowerShell 更新で解決）、["Open in Integrated Browser" Does Not Work](https://github.com/microsoft/vscode/issues/317765) `#317765`（1.125.0 で修正）、[Cancelled Copilot/Claude CLI turns show no metadata](https://github.com/microsoft/vscode/issues/317385) `#317385`・[#317387](https://github.com/microsoft/vscode/issues/317387)・[#317196](https://github.com/microsoft/vscode/issues/317196)（#321686 で修正）。
- AH/サンドボックス: [Agent host claude harness model list issues](https://github.com/microsoft/vscode/issues/321858) `#321858`（#321855 で対応）、[In Sandbox mode, approval window shows when auto approved](https://github.com/microsoft/vscode/issues/313903) `#313903`（#313940 で修正）、[Duplicate chat session after host disconnects](https://github.com/microsoft/vscode/issues/313360) `#313360`、各種 verified バグ [#313859](https://github.com/microsoft/vscode/issues/313859)・[#313934](https://github.com/microsoft/vscode/issues/313934)。
- Copilot 利用者報告（多くは info-needed/duplicate でクローズ）: Ollama 403 [#313984](https://github.com/microsoft/vscode/issues/313984)、[EMFILE too many open files #305816](https://github.com/microsoft/vscode/issues/305816)、エージェントが「完了」と言うが未完了 [#305159](https://github.com/microsoft/vscode/issues/305159)・[#305078](https://github.com/microsoft/vscode/issues/305078)、askモードの白枠生成バグ [#305069](https://github.com/microsoft/vscode/issues/305069)、ほか [#313976](https://github.com/microsoft/vscode/issues/313976)・[#313967](https://github.com/microsoft/vscode/issues/313967)・[#302572](https://github.com/microsoft/vscode/issues/302572)・[#302148](https://github.com/microsoft/vscode/issues/302148)・[#300906](https://github.com/microsoft/vscode/issues/300906)・[#299422](https://github.com/microsoft/vscode/issues/299422)。
- スパム/ガイドライン違反でクローズ: [#321857](https://github.com/microsoft/vscode/issues/321857)・[#313928](https://github.com/microsoft/vscode/issues/313928)・[#313919](https://github.com/microsoft/vscode/issues/313919)・[#313915](https://github.com/microsoft/vscode/issues/313915)・[#313913](https://github.com/microsoft/vscode/issues/313913) ほか。
- その他の機能要望・確認: [UBB support #313932](https://github.com/microsoft/vscode/issues/313932)、[Chronicle reindexing command #312825](https://github.com/microsoft/vscode/issues/312825)、[AHP selfhost shortlist #312224](https://github.com/microsoft/vscode/issues/312224)、[Add buttons for suggested tasks #309357](https://github.com/microsoft/vscode/issues/309357)、ポジティブフィードバック [#313908](https://github.com/microsoft/vscode/issues/313908)。

---

