# GitHub ダイジェスト — 2026-06-10

## 本日のハイライト

1. ![PR](icons/git-pull-request.svg) **[Upgrade dependency constraints during `uv upgrade`](https://github.com/astral-sh/uv/pull/19738)** (astral-sh/uv) — 新しい `uv upgrade` プレビュー機能の中核で、`pyproject.toml` の制約を安全に緩めて更新する処理を追加。レビューが 23 件と活発。
2. ![PR](icons/git-pull-request.svg) **[fix(treesitter): give each wasm parser its own TSWasmStore](https://github.com/neovim/neovim/pull/40163)** (neovim/neovim) — 共有 TSWasmStore が二重解放され SIGSEGV を起こすクラッシュ修正。WASM のメンテ方針をめぐり 10 件のコメントで議論。
3. ![Issue](icons/issue-opened.svg) **[Heap corruption (corrupted size vs. prev_size) when tree-sitter-haskell highlights a Markdown code block in hover float](https://github.com/neovim/neovim/issues/40169)** (neovim/neovim) — LSP ホバー内の Haskell コードブロックでヒープ破壊が再現するクラッシュ報告。
4. ![PR](icons/git-pull-request.svg) **[Add latcontrol for curvature](https://github.com/commaai/openpilot/pull/38141)** (commaai/openpilot) — 曲率指令を直接受け付ける EPS 向けの横方向コントローラを追加してマージ。新しい車種サポートの基盤となる機能追加。
5. ![PR](icons/git-pull-request.svg) **[Custom plover HID report type](https://github.com/qmk/qmk_firmware/pull/26018)** (qmk/qmk_firmware) — ステノ入力 Plover の新しい HID プロトコル対応。インターフェース節約のメリットがあり、breaking change ラベル付きで 12 件のレビュー。

---

## sunnypilot/sunnypilot

> UI 関連の2件の PR が更新。いずれも raylib UI への機能追加で、CI の UI プレビューによる動作確認が進行中。

### ![PR](icons/git-pull-request.svg) [visuals: hide camera toggle](https://github.com/sunnypilot/sunnypilot/pull/1627) `#1627`
**ステータス:** Open | **ラベル:** enhancement, ui, raylib

**概要:** オンロード UI からカメラ映像を非表示にする「Hide Camera」機能を追加する PR。永続パラメータ `HideCamera`（デフォルト "0"）を `params_keys.h` に追加し、メインと MICI 双方の設定 UI にトグル・説明・アイコンを実装する。

**直近のアクティビティ:** `github-actions[bot]` の UI プレビューでは mici 版は変化なし、big 版は映像差分を検出。`sunnyhaibin` から 2 件のレビューコメントが付いている。

**変更ファイル:** `params_keys.h`、`augmented_road_view.py`（mici/通常）、`visuals.py`、`ui_state.py`、設定メタデータ（json/yaml）の計 7 ファイル。

**次のアクション:** big 版の映像差分の妥当性確認と、`sunnyhaibin` のレビューコメントへの対応。

---

### ![PR](icons/git-pull-request.svg) [ui: external storage](https://github.com/sunnypilot/sunnypilot/pull/1524) `#1524`
**ステータス:** Open（Draft）| **ラベル:** ui, raylib

**概要:** 開発者向け設定に外部ストレージ用のレイアウトを追加する Draft PR。`developer.py` と新規の `external_storage.py` を変更し 267 行を追加する。

**直近のアクティビティ:** UI プレビューでは mici/big とも映像に変化なし。`sentry[bot]` のレビューコメントのみ。

**変更ファイル:** `selfdrive/ui/sunnypilot/layouts/settings/developer.py`、`external_storage.py` の 2 ファイル。

**次のアクション:** Draft 解除に向けた実装の完成。

---

## commaai/openpilot

> サウンド刷新・横方向制御・CI 高速化など多岐にわたる更新。多くの古い PR は stale ボットにより自動クローズが予告されている一方、サウンド系・curvature 制御・Jenkins/CI 改善はマージされている。

### ![PR](icons/git-pull-request.svg) [usbgpu: fallback on small model](https://github.com/commaai/openpilot/pull/38098) `#38098`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** モデル実行に関するフォールバックを small model で行うための変更。`modeld` 系・`log.capnp`・`services.py`・ハードウェア/プロセス設定など 12 ファイルにわたる。

**直近のアクティビティ:** UI プレビューは差分なし、process replay diff も「0 changed, 66 passed」。`adeebshihadeh`・`Armandpl` がレビューコメント。

**変更ファイル:** `big_modeld.py`、`modeld.py`、`routerd.py`、`events.py`、`hardwared.py`、`process_config.py` ほか。

**次のアクション:** Draft の完成とレビュー対応。

---

### ![PR](icons/git-pull-request.svg) [New sounds](https://github.com/commaai/openpilot/pull/38154) `#38154`
**ステータス:** Open | **ラベル:** なし

**概要:** soundd の挙動変更で、保留中のアラートがないときにループ音を途中で切らず最後まで再生するようにする。各種 wav アセットと `soundd.py`・`events.py` を更新。

**直近のアクティビティ:** `elkoled` が「process replay diff は alertSoundDEPRECATED 変更による想定内」とコメント。big 版 UI プレビューに映像差分あり。`adeebshihadeh`・`elkoled`・`nickorie` がレビュー。

**変更ファイル:** sounds 配下の wav 8 ファイル、`events.py`、`soundd.py`、`test_soundd.py` など 13 ファイル。

**次のアクション:** 映像差分の確認とレビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix: MetaDrive simulator on macOS (spawn context, lazy imports)](https://github.com/commaai/openpilot/pull/38152) `#38152`
**ステータス:** Open | **ラベル:** なし

**概要:** macOS で MetaDrive シミュレータが正しく動くよう修正し、`test_metadrive_bridge.py` を通す（#33207 をクローズ）。macOS の fork ではなく spawn を使うべき点、`dist_threshold` が厳しすぎる点、合成 `eof` がカメラ鮮度チェックを通らない点などが根本原因。

**直近のアクティビティ:** process replay diff は「0 changed, 66 passed」。

**変更ファイル:** `manager.py`、`metadrive_process.py`、`metadrive_world.py`、`camerad.py`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Yaw is zero](https://github.com/commaai/openpilot/pull/37825) `#37825`
**ステータス:** Open | **ラベル:** stale

**概要:** NED フレームでは絶対ヨー方位が観測不能であることを踏まえ、pose KF の状態数（および共分散）を削減してログの S/N 比を向上させる。

**直近のアクティビティ:** 24 日間活動がなく、stale ボットが 7 日後の自動クローズを予告。

**変更ファイル:** `locationd.py`、`pose_kf.py`。

**次のアクション:** 自動クローズ回避には再アクティブ化が必要。

---

### ![PR](icons/git-pull-request.svg) [New orange](https://github.com/commaai/openpilot/pull/37831) `#37831`
**ステータス:** Open | **ラベル:** stale

**概要:** 新しいアラート UI とアニメーション（engaged/disengaged のオレンジ・レッドアラート、常時表示の confidence dot、DM アラートの新スタイルなど）を参照用にまとめた PR（マージ目的ではない）。

**直近のアクティビティ:** stale ボットによる自動クローズ予告のみ。

**変更ファイル:** `alert_renderer.py`、`confidence_ball.py`、`hud_renderer.py`、`soundd.py` ほか 9 ファイル。

**次のアクション:** 参照用のため対応不要。

---

### ![PR](icons/git-pull-request.svg) [tools/lib/route: dedupe segment file mapping logic](https://github.com/commaai/openpilot/pull/37862) `#37862`
**ステータス:** Open | **ラベル:** tools, stale

**概要:** `route.py` のセグメントファイルマッピングの重複ロジックを、公開メソッドのシグネチャと挙動（local は first-match、remote は last-match）を保ったままリファクタ。回帰テストも追加。

**直近のアクティビティ:** stale ボットによる自動クローズ予告のみ。

**変更ファイル:** `tools/lib/route.py`。

**次のアクション:** レビューと再アクティブ化。

---

### ![PR](icons/git-pull-request.svg) [ui: add software panel for comma four](https://github.com/commaai/openpilot/pull/37894) `#37894`
**ステータス:** Open | **ラベル:** ui, stale

**概要:** comma four 向けのソフトウェアパネルを設定 UI に追加する（本文記載なし）。

**直近のアクティビティ:** stale ボットによる自動クローズ予告のみ。

**変更ファイル:** mici の `device.py`、`settings.py`、`software.py` など 4 ファイル。

**次のアクション:** 再アクティブ化が必要。

---

### ![PR](icons/git-pull-request.svg) [wifi: no NetworkManager](https://github.com/commaai/openpilot/pull/37902) `#37902`
**ステータス:** Open（Draft）| **ラベル:** stale

**概要:** NetworkManager による WiFi 管理を wpa_supplicant の ctrl-socket クライアント + udhcpc + dnsmasq に置き換える大型 PR（#37752 を解決、#37662 の継続）。UI 再起動後もデーモンが生存し、テザリングの引き継ぎにも対応。+6085/-1521 行。

**直近のアクティビティ:** stale ボットによる自動クローズ予告のみ。

**変更ファイル:** `networkmanager.py`、`dhcp_client.py`、`wifi_ui.py`、`hardware.py` ほか多数。

**次のアクション:** フィードバックと再アクティブ化。

---

### ![PR](icons/git-pull-request.svg) [build(openpilot): replace panda submodule with pip-distributed panda dependency](https://github.com/commaai/openpilot/pull/37908) `#37908`
**ステータス:** Open | **ラベル:** stale

**概要:** panda サブモジュールを廃止し、pip 配布の panda 依存（fork ブランチ固定）に置き換える。SConstruct を更新してインストール済み Python パッケージから panda パスを動的解決する。commaai/panda#2392 の検証用。

**直近のアクティビティ:** stale ボットによる自動クローズ予告のみ。

**変更ファイル:** `.gitmodules`、`SConstruct`、`panda`、`pyproject.toml`、`uv.lock`。

**次のアクション:** 検証完了と再アクティブ化。

---

### ![PR](icons/git-pull-request.svg) [jp: add dark mode](https://github.com/commaai/openpilot/pull/37917) `#37917`
**ステータス:** Open | **ラベル:** tools, stale

**概要:** jotpluggler ツールにダークモードを追加する（スクリーンショットのみの本文）。

**直近のアクティビティ:** stale ボットによる自動クローズ予告のみ。

**変更ファイル:** `tools/jotpluggler/` 配下 11 ファイル。

**次のアクション:** 再アクティブ化が必要。

---

### ![PR](icons/git-pull-request.svg) [Show offroad alert for ignored uploads](https://github.com/commaai/openpilot/pull/37930) `#37930`
**ステータス:** Open | **ラベル:** stale

**概要:** 無視されたアップロードに対してオフロードアラートを表示する（#36534 をクローズ）。

**直近のアクティビティ:** stale 予告に加え、`adeebshihadeh`・`sshane` がレビューコメント。

**変更ファイル:** `params_keys.h`、`alerts_offroad.json`、`prime_state.py`。

**次のアクション:** レビュー対応と再アクティブ化。

---

### ![PR](icons/git-pull-request.svg) [ci: reliable MetaDrive test via relaxed tolerances and CPU yielding (#30693)](https://github.com/commaai/openpilot/pull/37900) `#37900`
**ステータス:** Open | **ラベル:** なし

**概要:** 4 コアの無料 runner 上で MetaDrive CI テストを CPU を枯渇させずに安定実行する。`launch_openpilot.sh` の `BLOCK` オーバーライドを外し、ダミーオーディオシンクで `soundd` のクラッシュを防ぐなど（#30693 を修正）。

**直近のアクティビティ:** `FuZoe` が「マージコンフリクトは解消したのでレビュー再開を」とコメント。process replay diff は問題なし。

**変更ファイル:** `selfdrived.py`、`common.py`、`metadrive_process.py`、`launch_openpilot.sh` ほか。

**次のアクション:** レビュー再開。

---

### ![PR](icons/git-pull-request.svg) [op.sh: fix arg quoting so special chars survive eval](https://github.com/commaai/openpilot/pull/37967) `#37967`
**ステータス:** Merged | **ラベル:** tools, stale

**概要:** `op.sh` で `$` などのシェル特殊文字が現行のパース処理で壊れる問題を修正し、引数のクォート処理を堅牢化する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `tools/op.sh`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [feat(webrtc): addIceCandidate dispatcher](https://github.com/commaai/openpilot/pull/38161) `#38161`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** athenad を経由して inbound ICE を trickle するための dispatcher を追加する。

**直近のアクティビティ:** process replay diff は問題なし。

**変更ファイル:** `athenad.py`、`webrtcd.py`。

**次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [fix(athena): outbound rpc stuck behind low prio queue timeout](https://github.com/commaai/openpilot/pull/38160) `#38160`
**ステータス:** Open | **ラベル:** なし

**概要:** high/low の 2 つの送信キューを使う `ws_send` の隠れたバグ（low キューが空のとき high キューに新規メッセージが来ても 1 秒のタイムアウト待ちになる）を、`PriorityQueue` に置き換えて解消する。

**直近のアクティビティ:** process replay diff は問題なし。

**変更ファイル:** `athenad.py`、`test_athenad.py`。

**次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [lateral maneuver runner bugs](https://github.com/commaai/openpilot/issues/38107) `#38107`
**ステータス:** Open | **ラベル:** なし

**概要:** Altima で横方向マニューバを試したが「road not straight」で中断するなど使い物にならなかったというバグ報告。gasPressed を開始条件に加えるなど複数の改善項目がチェックリスト化されている。

**直近のアクティビティ:** `elkoled` が改良レポート（#38159）の確認を依頼。

**次のアクション:** #38159 によるクローズ予定。

---

### ![PR](icons/git-pull-request.svg) [Improve lateral maneuvers](https://github.com/commaai/openpilot/pull/38159) `#38159`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** 横方向マニューバのレポート生成を改善し、新しい Jitter マニューバを追加する（#38102 を取り込み、#38107 をクローズ）。

**直近のアクティビティ:** UI プレビューは差分なし、process replay diff も問題なし。

**変更ファイル:** `developer.py`（通常/mici）、`generate_report.py`、`lateral_maneuversd.py`。

**次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [CI: incremental device checkout](https://github.com/commaai/openpilot/pull/38158) `#38158`
**ステータス:** Open | **ラベル:** CI / testing

**概要:** デバイスのチェックアウトをインクリメンタル化し、safe/unsafe 双方で大幅に高速化する（safe checkout が 33s→3-4s など）。worktree を消さず fetch + `checkout --force` + delta rsync を行う。

**直近のアクティビティ:** process replay diff は問題なし。

**変更ファイル:** `selfdrive/test/setup_device_ci.sh`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [radar errors: only gate enable if openpilot long](https://github.com/commaai/openpilot/pull/38145) `#38145`
**ステータス:** Merged | **ラベル:** なし

**概要:** レーダーを解析するが利用しない場合（openpilot 縦方向制御でなく TSS-P のみ）に enable をレーダーでゲートしないようにする。純正 ACC が処理するため。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `selfdrived.py`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [soundd: fix long wav playback and mic feedback](https://github.com/commaai/openpilot/pull/38155) `#38155`
**ステータス:** Merged | **ラベル:** ui

**概要:** soundd の長尺 wav 再生を修正し、再生中にボリュームを更新しないことでマイクのフィードバック増幅を防ぐ。

**直近のアクティビティ:** UI プレビュー・process replay diff とも問題なし。

**変更ファイル:** `selfdrive/ui/soundd.py`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Jenkinsfile: use SSH ControlMaster to multiplex device connections](https://github.com/commaai/openpilot/pull/38157) `#38157`
**ステータス:** Merged | **ラベル:** CI / testing

**概要:** SSH の ControlMaster で接続を多重化し、デバイスへの SSH 接続を大幅高速化（中央値 0.38s→0.03s）。各ステップでも有意な短縮を計測。

**直近のアクティビティ:** process replay diff は問題なし。

**変更ファイル:** `Jenkinsfile`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add latcontrol for curvature](https://github.com/commaai/openpilot/pull/38141) `#38141`
**ステータス:** Merged | **ラベル:** なし

**概要:** 角度やトルクではなく車体曲率指令を直接受け付ける EPS 向けの横方向コントローラを追加。`CP.lateralTuning.pid` による任意の PID チューニングに対応し、`SteerControlType.curvature` と `LateralCurvatureState` の deprecation を解除。

**直近のアクティビティ:** `sshane`・`elkoled` のレビューコメント計 5 件（コメント本文はなし）。

**変更ファイル:** `latcontrol_curvature.py`、`controlsd.py`、`log.capnp`、`torque_bar.py` ほか 7 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix: MetaDrive sim on macOS (#33207)](https://github.com/commaai/openpilot/pull/38156) `#38156`
**ステータス:** Open | **ラベル:** なし

**概要:** macOS で `test_metadrive_bridge.py` を通す別アプローチの修正（#33207 を修正、commaai/metadrive#15 が必要）。spawn 時に ui モジュールの事前 import をスキップ、`eof` を `time.monotonic_ns()` で打刻するなど 3 点を対処。

**直近のアクティビティ:** process replay diff は問題なし。

**変更ファイル:** `modeld/SConscript`、`process.py`、`camerad.py`。

**次のアクション:** レビュー待ち（#38152 と重複領域）。

---

### ![PR](icons/git-pull-request.svg) [vendor acados via commaai/dependencies](https://github.com/commaai/openpilot/pull/37932) `#37932`
**ステータス:** Closed | **ラベル:** stale

**概要:** acados を commaai/dependencies 経由でベンダリングし、third_party から大量のバイナリ（-59338 行）を削除する PR。クローズ済み。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Speed up Jenkins device checkout](https://github.com/commaai/openpilot/pull/38024) `#38024`
**ステータス:** Closed | **ラベル:** CI / testing, stale

**概要:** Jenkins のデバイスチェックアウト高速化を狙った PR（本文なし）。クローズ済みで、#38158 に引き継がれたとみられる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [mici ui: add branch switcher](https://github.com/commaai/openpilot/pull/38153) `#38153`
**ステータス:** Open | **ラベル:** ui

**概要:** mici UI の設定にブランチ切り替え機能を追加する（本文なし）。

**直近のアクティビティ:** UI プレビューで mici/big とも映像差分あり。process replay diff は問題なし。

**変更ファイル:** mici の `device.py`、`settings.py`、`software.py`。

**次のアクション:** 映像差分の確認とレビュー。

---

## commaai/opendbc

> 車種フィンガープリント追加と Toyota/Subaru の安全モデル更新が中心。多数のフィンガープリント PR は60日無活動で stale クローズ予告が出ている一方、Toyota レーダー関連や VW CUPRA などはマージされた。

### ![PR](icons/git-pull-request.svg) [Add Hyundai Custin 2025 Fingerprint](https://github.com/commaai/opendbc/pull/3272) `#3272`
**ステータス:** Open | **ラベル:** car, hyundai, stale

**概要:** Hyundai Custin 2025 のフィンガープリントを追加する。

**直近のアクティビティ:** stale 予告と car behavior report（Hyundai 各車種 260 セグメントをテスト）。`github-actions[bot]` がレビューチェックリストをコメント。

**変更ファイル:** `hyundai/fingerprints.py`、`hyundai/values.py`。

**次のアクション:** 再アクティブ化が必要。

---

### ![PR](icons/git-pull-request.svg) [Toyota: check radar status signal](https://github.com/commaai/opendbc/pull/3452) `#3452`
**ステータス:** Merged | **ラベル:** car, toyota

**概要:** TSS2 では `RADAR_PRE_FAULT` が非ゼロのセグメントが見当たらず TSS-P 用と判明したため、`RADAR_STATUS != 1` のチェックを採用してレーダー障害/遮蔽を検知する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `toyota/radar_interface.py`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Subaru: Outback 22 Engine FW](https://github.com/commaai/opendbc/pull/3309) `#3309`
**ステータス:** Open | **ラベル:** car, subaru, fingerprint, stale

**概要:** Subaru Outback 20-22 のエンジン ECU フィンガープリントを追加する。

**直近のアクティビティ:** stale 予告とレビューチェックリストのみ。

**変更ファイル:** `subaru/fingerprints.py`。

**次のアクション:** 再アクティブ化が必要。

---

### ![PR](icons/git-pull-request.svg) [chore: Add a finger print of Mazda6](https://github.com/commaai/opendbc/pull/3312) `#3312`
**ステータス:** Open | **ラベル:** car, mazda, fingerprint, stale

**概要:** Mazda6 2022/12（6BA-GJ5FW、Wagon 2.5 25S L-Package JP）のフィンガープリントを追加する。

**直近のアクティビティ:** stale 予告とレビューチェックリストのみ。

**変更ファイル:** `mazda/fingerprints.py`。

**次のアクション:** 再アクティブ化が必要。

---

### ![PR](icons/git-pull-request.svg) [Simplify GM siglin to use tanh](https://github.com/commaai/opendbc/pull/3313) `#3313`
**ステータス:** Open | **ラベル:** car, gm, stale

**概要:** GM の siglin を、数学的に等価でより短い `tanh` を使う形に簡略化する。abs/sign を使う必然性があったのかという問いを含む。

**直近のアクティビティ:** stale 予告に加え `simontheflutist` のレビューコメント。

**変更ファイル:** `gm/interface.py`。

**次のアクション:** レビュー対応と再アクティブ化。

---

### ![PR](icons/git-pull-request.svg) [Hyundai CAN: split DBC for different messages definitions](https://github.com/commaai/opendbc/pull/3319) `#3319`
**ステータス:** Open | **ラベル:** DBC signals, car, hyundai, stale, car safety

**概要:** 新しいモデルイヤーの Hyundai 向けに別の DBC `hyundai_can_refresh_generated.dbc` を生成する。同じ CAN ID でも信号位置が異なるプラットフォームに対応するための準備で、本 PR では機能変更なし（#3320 が必要、#3318 の前提）。

**直近のアクティビティ:** stale 予告のみ。

**変更ファイル:** `hyundai/values.py`、各種 DBC、`test_hyundai.py`。

**次のアクション:** #3320 の取り込みと再アクティブ化。

---

### ![PR](icons/git-pull-request.svg) [HKG: ccNC port (non-HDA2)](https://github.com/commaai/opendbc/pull/1575) `#1575`
**ステータス:** Open | **ラベル:** car, hyundai, car safety

**概要:** ccNC 搭載の Hyundai/Kia 各車（Kona、Santa Cruz、Sonata、Tucson Hybrid、K4/K5/Sorento など、いずれも HDA II 非搭載）をサポートするポート。+398 行。

**直近のアクティビティ:** `sshane` が CHANGES_REQUESTED でレビュー。

**変更ファイル:** `hyundai/carcontroller.py`、`carstate.py`、`hyundaicanfd.py`、`hyundai_canfd.h` ほか 12 ファイル。

**次のアクション:** レビュー指摘への対応。

---

### ![Issue](icons/issue-opened.svg) [Toyota: check radar fault/dirty/status](https://github.com/commaai/opendbc/issues/3446) `#3446`
**ステータス:** Closed | **ラベル:** なし

**概要:** TSS-P RAV4 のレーダーに foil を貼って `ACC_CONTROL` の RADAR_DIRTY ビットの挙動を調査した記録。ダーティビットだけでは ground truth に使えず、0x123 の別信号を調べた内容。

**直近のアクティビティ:** `sshane` が #3452 へのリンクをコメント。

**次のアクション:** #3452 で対応済みのためクローズ。

---

### ![PR](icons/git-pull-request.svg) [Toyota: mark when radar is initializing or unavailable](https://github.com/commaai/opendbc/pull/3442) `#3442`
**ステータス:** Closed | **ラベル:** car, toyota

**概要:** 起動時や TSS-P/非 US TSS2 の停止時など、レーダーが初期化中・利用不可な状態をマークする（#3441 の拡張）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `toyota/radar_interface.py`。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [[bot] Update uv.lock](https://github.com/commaai/opendbc/pull/3456) `#3456`
**ステータス:** Merged | **ラベル:** bot, car safety

**概要:** uv.lock 依存関係の週次自動更新。

**直近のアクティビティ:** car behavior report は「No changes detected」。

**変更ファイル:** `coverage_table`、`uv.lock`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [cppcheck: replace bool 0 assignment with false](https://github.com/commaai/opendbc/pull/3444) `#3444`
**ステータス:** Merged | **ラベル:** car safety

**概要:** commaai/panda#2105 のクリーンアップに関連し、cppcheck 2.21.0 で残った rule 10.3 の指摘に対応して Toyota safety の `tx = 0;` を `tx = false;` に置き換える。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `safety/modes/toyota.h`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Honda - 2026 Pilot - mid-model refresh](https://github.com/commaai/opendbc/pull/3330) `#3330`
**ステータス:** Open | **ラベル:** car, honda

**概要:** 2026 Honda Pilot をサポート。制御は 2023-2025 と同じだが、マイナーチェンジで EPS が大幅に強化されたため別プラットフォームとトルク値が必要。

**直近のアクティビティ:** car behavior report（Honda 各車種 154 セグメント）を実行。

**変更ファイル:** `honda/fingerprints.py`、`interface.py`、`values.py`、`override.toml` ほか。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Subaru: LKAS Angle support and safety model](https://github.com/commaai/opendbc/pull/3454) `#3454`
**ステータス:** Open | **ラベル:** car, subaru, car safety

**概要:** Subaru の LKAS angle サポートと安全モデルを追加。Crosstrek の PR を参考に LKAS safety ロジックを切り出し、角度ベースとトルクベースの操舵入力を分離するようリファクタ。+179 行。

**直近のアクティビティ:** `bravochar` が #2864 のコメントに対応している旨をコメント。car behavior report（Subaru 各車種 79 セグメント）を実行。

**変更ファイル:** `subaru/carcontroller.py`、`carstate.py`、`subaru.h`、`test_subaru.py` ほか 7 ファイル。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [add laneChange to AudibleAlert](https://github.com/commaai/opendbc/pull/3455) `#3455`
**ステータス:** Closed | **ラベル:** car

**概要:** ユーザーが車線変更を確定したときに音を鳴らせるよう、`AudibleAlert` enum に `laneChange` を追加する。

**直近のアクティビティ:** car behavior report は「No changes detected」。`adeebshihadeh` のレビューコメント。

**変更ファイル:** `car/car.capnp`。

**次のアクション:** クローズ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [[$1000 bounty +community] Rivian: read set speed](https://github.com/commaai/opendbc/issues/1833) `#1833`
**ステータス:** Open | **ラベル:** enhancement, rivian

**概要:** Rivian の縦方向制御は準備済みだが、設定速度やボタン押下を車から読めない問題への $1000 バウンティ。コミュニティの GoFundMe も組まれている。

**直近のアクティビティ:** `gerrylum` が「この issue は解決済みでクローズ可能」とコメントする一方、`adeebshihadeh` が方法を問い、`Ishant5436` が CAN ログの公開を依頼するなど計 5 件のやり取り。

**次のアクション:** 解決の可否と公開ログの提供を確認。

---

### ![PR](icons/git-pull-request.svg) [VW ID.4 radar parser](https://github.com/commaai/opendbc/pull/3437) `#3437`
**ステータス:** Open | **ラベル:** enhancement, car, volkswagen

**概要:** ID.4 ポートから切り出したレーダーパーサ。トラックの生データ性が不明なため、ソースと品質の調査が必要。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `volkswagen/interface.py`、`radar_interface.py`。

**次のアクション:** トラックの品質調査とレビュー。

---

### ![PR](icons/git-pull-request.svg) [ADD Volvo V60](https://github.com/commaai/opendbc/pull/3453) `#3453`
**ステータス:** Closed | **ラベル:** なし

**概要:** Volvo V60/S60（P3 プラットフォーム 2015-2017、ACC と LKA 装備車）を sunnypilot に追加する PR。カスタムハーネスが必要。+13878 行で 100 ファイルに及ぶ大規模変更。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add new fingerprint for CUPRA Born 2023 (Volkswagen MEB Gen 1)](https://github.com/commaai/opendbc/pull/3451) `#3451`
**ステータス:** Merged | **ラベル:** car, volkswagen

**概要:** VW MEB プラットフォーム向けに CUPRA Born のフィンガープリントを追加する。

**直近のアクティビティ:** `elkoled` が謝意をコメント。`elkoled` は CHANGES_REQUESTED、`infiniteCable` がレビューコメント。

**変更ファイル:** `routes.py`、`override.toml`、`volkswagen/fingerprints.py`、`values.py`。

**次のアクション:** マージ済みのため対応不要。

---

## commaai/panda

> 直近24時間の更新は cppcheck クリーンアップの PR 1件のみ。

### ![PR](icons/git-pull-request.svg) [cppcheck: replace bool 0/1 assignments with false/true](https://github.com/commaai/panda/pull/2409) `#2409`
**ステータス:** Open | **ラベル:** なし

**概要:** panda 側の cppcheck クリーンアップ（#2105）を完了させる。cppcheck 2.21.0 の bool リテラル処理に合わせ、`bool ret = 0;` を `= false;`、`ret = 1;` を `= true;` に置き換える。CI 用に依存ブランチを一時的に参照。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `can_common.h`、`coverage_table`。

**次のアクション:** 依存ブランチの解消とレビュー。

---

## neovim/neovim

> vim-patch の取り込みと CI/ビルド更新が多数マージされ、Windows の 8.3 短縮パス問題の修正がリリースブランチへバックポート。treesitter WASM のクラッシュ修正や LSP 補完/コードレンズの改善も進行中で、複数の flaky テスト・クラッシュ報告が挙がっている。

### ![PR](icons/git-pull-request.svg) [chore(lsp): bump protocol/meta to 3.18](https://github.com/neovim/neovim/pull/37273) `#37273`
**ステータス:** Open | **ラベル:** なし

**概要:** LSP の protocol/meta を 3.18 に更新し、docs を再生成する。

**直近のアクティビティ:** `tris203` が「上流の issue が修正され 3.18 でブロック解除された」とコメント。

**変更ファイル:** `lsp/_meta/protocol.lua`、`lsp/protocol.lua`。

**次のアクション:** レビューとマージ。

---

### ![Issue](icons/issue-opened.svg) [Heap corruption (corrupted size vs. prev_size) when tree-sitter-haskell highlights a Markdown code block in hover float](https://github.com/neovim/neovim/issues/40169) `#40169`
**ステータス:** Open | **ラベル:** bug-crash

**概要:** `vim.lsp.buf.hover` の Markdown ホバーウィンドウに Haskell コードブロックが含まれ、tree-sitter-haskell が入っているとき、フロート閉鎖後に glibc ヒープ破壊で Neovim がクラッシュする。最小構成で再現し、haskell パーサがないと発生しない。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** 再現調査と修正。

---

### ![Issue](icons/issue-opened.svg) [difftool: weird behaviour with process substitution](https://github.com/neovim/neovim/issues/40151) `#40151`
**ステータス:** Open | **ラベル:** has:workaround

**概要:** `nvim -u NONE -d <(xxd one) <(xxd two)` は動くが、`packadd nvim.difftool` を付けると片方のファイルしか読み込まれない。

**直近のアクティビティ:** `gx089` がプロセス置換はパイプでシーク不能だと指摘、`e-kwsm` が #18557 等の重複と指摘、`PEMessage` がファイルサイズに依存すると報告するなど計 4 件。

**次のアクション:** `nvim.difftool` 側でのデフォルト改善の検討。

---

### ![Issue](icons/issue-opened.svg) [vim.fn.stdpath() can return unsupported 8.3 DOS filenames](https://github.com/neovim/neovim/issues/25019) `#25019`
**ステータス:** Open | **ラベル:** platform:windows

**概要:** Windows が長いユーザー名を `6ch~N` に短縮するため、`cmd.exe` 下で `stdpath("cache")` が 8.3 短縮名を含むパスを返し、RTP に追加すると `nvim_get_runtime_file` がファイルを見つけられない。

**直近のアクティビティ:** `ntdiary`・`justinmk` が、`fs_realpath` での「unshorten」案や、#40144 は緩和策で根本の `stdpath` 修正が別途必要との議論を計 4 件。

**次のアクション:** `stdpath` が短縮パスを返す根本原因の修正。

---

### ![PR](icons/git-pull-request.svg) [fix(option): set related options based on detected shell](https://github.com/neovim/neovim/pull/40031) `#40031`
**ステータス:** Open | **ラベル:** なし

**概要:** `'shellcmdflag'` がドキュメント通りに `'shell'` の値に応じて設定されない問題を、一般的なシェルを検出する簡易ルールテーブルで対処する（#31528, #28384, #16957 をクローズ予定）。

**直近のアクティビティ:** `justinmk`・`ntdiary` によるレビューコメント計 13 件。

**変更ファイル:** `option.c`、`options.lua`、`path.c`、`shell_spec.lua` ほか 7 ファイル。

**次のアクション:** レビュー指摘への対応。

---

### ![PR](icons/git-pull-request.svg) [backport: fix(path): `nvim_get_runtime_file` fails on DOS 8.3 filename](https://github.com/neovim/neovim/pull/40168) `#40168`
**ステータス:** Merged | **ラベル:** target:release

**概要:** #40144 を `release-0.12` にバックポートする。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `path.c`、`vim_spec.lua`。

**次のアクション:** マージ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [unreliable test: `pack_spec: vim.pack get() opts.offline can fetch new updates`](https://github.com/neovim/neovim/issues/40167) `#40167`
**ステータス:** Open | **ラベル:** test, ci, packages

**概要:** Windows CI で `pack_spec.lua` が「process still exists」で flaky に失敗する。

**直近のアクティビティ:** `echasnovski` が、`vim.system({'git','init'})` が pid のクローズ前に終了している可能性をトレースから推測しコメント。

**次のアクション:** pid クローズの待機処理の見直し。

---

### ![PR](icons/git-pull-request.svg) [fix(path): `nvim_get_runtime_file` fails on DOS 8.3 filename](https://github.com/neovim/neovim/pull/40144) `#40144`
**ステータス:** Merged | **ラベル:** ci:backport release-0.12

**概要:** `stdpath()` が返す 8.3 短縮名で `nvim_get_runtime_file` がファイルを見つけられない問題を、`do_path_expand` で `~` をリテラル文字として扱うことで修正する。

**直近のアクティビティ:** `neovim-backports[bot]` がバックポート PR #40168 作成を報告。`justinmk` が #25019 の Problem/Solution を更新した経緯などをコメント（計 4 件）。

**変更ファイル:** `path.c`、`vim_spec.lua`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [build(wasmtime): don't require exact patch version](https://github.com/neovim/neovim/pull/40166) `#40166`
**ステータス:** Merged | **ラベル:** build

**概要:** wasmtime が LTS リリースを導入し tree-sitter が追従したため、パッチ版を厳密固定せず major.minor の互換で済むようにする。

**直近のアクティビティ:** `justinmk` が APPROVED（:+1:）。

**変更ファイル:** `src/nvim/CMakeLists.txt`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(treesitter): give each wasm parser its own TSWasmStore](https://github.com/neovim/neovim/pull/40163) `#40163`
**ステータス:** Open | **ラベル:** treesitter, AI assisted 🤖

**概要:** 全 wasm パーサがグローバルな TSWasmStore を共有しており、最初のパーサ破棄時にストアが解放され、次のパーサ生成で解放済みメモリを参照して SIGSEGV する問題を、各パーサに専用ストアを持たせて修正する。

**直近のアクティビティ:** `clason` が WASM サポートはベストエフォートでバックポート未定・テストカバレッジが必要とし、`CGamesPlay` が wasmtime v36.0.6 で動作する旨を報告するなど計 10 件で議論。

**変更ファイル:** `src/nvim/lua/treesitter.c`。

**次のアクション:** WASM のテストワークフロー追加の検討。

---

### ![Issue](icons/issue-opened.svg) [Active statusline window detection randomly breaking](https://github.com/neovim/neovim/issues/40153) `#40153`
**ステータス:** Open | **ラベル:** bug-regression, lsp, has:bisected, events

**概要:** nightly のあるコミット以降、LSP（主に TypeScript LS）動作中にステータスラインのアクティブウィンドウ判定が壊れ、全ウィンドウに表示されたりどこにも表示されなくなる回帰。

**直近のアクティビティ:** `glepnir` が最小テスト、`luukvbaal` が `g:actual_curwin` の信頼性、`kristijanhusak` が最小再現構成、`justinmk` が `nvim_exec_autocmds` での更新漏れの可能性を問うなど計 11 件で議論。

**次のアクション:** 最小再現を用いた原因特定。

---

### ![Issue](icons/issue-opened.svg) [zig build fails on a aarch64-linux machine](https://github.com/neovim/neovim/issues/40149) `#40149`
**ステータス:** Open | **ラベル:** build

**概要:** Apple M1（Asahi Linux、zig 0.16.0）上で `aarch64-linux` ターゲットのビルドが失敗する。x86_64 マシンでは成功し、`x86_64-linux` ターゲットは aarch64 上でも成功する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** aarch64 でのビルド失敗の調査。

---

### ![PR](icons/git-pull-request.svg) [build(deps): bump tree-sitter to 519d51148](https://github.com/neovim/neovim/pull/40165) `#40165`
**ステータス:** Merged | **ラベル:** build

**概要:** tree-sitter を `519d51148` に更新する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `build.zig.zon`、`cmake.deps/deps.txt`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(autocmds): 'autocompletedelay' adds actual delay to 'TextChangedP autocmds](https://github.com/neovim/neovim/pull/40150) `#40150`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** `autocompletedelay` の遅延ループが `TextChangedP` autocmds の発火を阻害する問題（#40064 関連）。ブロック前に `ins_redraw(true)` で autocmds を flush する案を検討。

**直近のアクティビティ:** `glepnir` が「busy-wait を捨てタイマーで pum を出す」案や vim 側で先に直すべきとコメント、`Mrpaoo` が `os_delay()` が main_loop.events を排出しないのが根本と同意（計 3 件）。

**変更ファイル:** `insexpand.c`、`completion_spec.lua`。

**次のアクション:** vim 側での先行修正と非同期化の検討。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.1.0581: Various lines are indented inconsistently](https://github.com/neovim/neovim/pull/40161) `#40161`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** `test_taglist.vim` の不揃いなインデントを retab で修正する vim-patch（vim/vim#15259）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test/old/testdir/test_taglist.vim`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.1.{944,1142}](https://github.com/neovim/neovim/pull/40162) `#40162`
**ステータス:** Open | **ラベル:** test, vim-patch

**概要:** vim-patch 9.1.944 と 9.1.1142 の取り込み（本文なし）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test_registers.vim`、`test_startup.vim`。

**次のアクション:** レビューとマージ。

---

### ![PR](icons/git-pull-request.svg) [docs: update version.c](https://github.com/neovim/neovim/pull/40108) `#40108`
**ステータス:** Open（Draft）| **ラベル:** vim-patch

**概要:** `version.c` の vim-patch 取り込み状況を更新する。

**直近のアクティビティ:** `janlazo` が vim-patch:8.2.4087 をコメント。

**変更ファイル:** `src/nvim/version.c`。

**次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.0.2082,9.1.{95,342}](https://github.com/neovim/neovim/pull/40160) `#40160`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** vim-patch 9.0.2082、9.1.95、9.1.342 の取り込み（本文なし）。

**直近のアクティビティ:** `zeertzjq` が APPROVED。

**変更ファイル:** `shared.vim`、`test_taglist.vim`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [build(vim-patch): test_mswin_event() is N/A](https://github.com/neovim/neovim/pull/40158) `#40158`
**ステータス:** Merged | **ラベル:** build, vim-patch

**概要:** Windows 固有のキーコード/イベント処理に依存するため `test_mswin_event()` を N/A として扱う。

**直近のアクティビティ:** `zeertzjq` が APPROVED。

**変更ファイル:** `scripts/vim_na_files.txt`。

**次のアクション:** マージ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [Wrong text rendered in buffer](https://github.com/neovim/neovim/issues/39713) `#39713`
**ステータス:** Closed | **ラベル:** needs:response, needs:repro

**概要:** `cursorcolumn` と `linebreak` を有効にした状態でカーソル移動時に誤ったテキストがレンダリングされるという報告。

**直近のアクティビティ:** `github-actions[bot]` が、情報提供がなく 30 日経過したためクローズした旨をコメント。

**次のアクション:** 情報提供があれば再オープン。

---

### ![Issue](icons/issue-opened.svg) [`nvim_exec2()` accepts args (like `nvim_exec_lua`)](https://github.com/neovim/neovim/issues/40159) `#40159`
**ステータス:** Open | **ラベル:** api, vimscript, lua

**概要:** `nvim_exec_lua` は args を受け取れるが `nvim_exec2()` は受け取れず、Vimscript へ引数を渡す簡便な方法がない。`v:args` 特殊グローバル導入の検討を含む要望。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** API 設計の検討。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:595d0a7: wrong {str} length limit in matchfuzzy() docs](https://github.com/neovim/neovim/pull/40157) `#40157`
**ステータス:** Merged | **ラベル:** vim-patch

**概要:** `matchfuzzy()` の docs では `{str}` の上限を 256 と記載しているが、実際は 1024 であり、超過分は拒否でなく切り詰めとなる点を修正する vim-patch（vim/vim#20453）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `vimfn.txt`、`vimfn.gen.lua`、`eval.lua`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0605: Test_screenpos() is flaky in GUI](https://github.com/neovim/neovim/pull/40156) `#40156`
**ステータス:** Merged | **ラベル:** test, vim-patch

**概要:** GUI で `getwininfo()` のウィンドウ高さが stale になり `Test_screenpos()` が間欠的に失敗するため、アサーション時に `winheight(winid)` を使う vim-patch（vim/vim#20457）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test_cursor_func.vim`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [vim-patch:9.2.0610: cindent: closing brace in a comment affects the next line's indent](https://github.com/neovim/neovim/pull/40155) `#40155`
**ステータス:** Merged | **ラベル:** vim-patch, AI assisted 🤖

**概要:** `//` 行コメント内の `}` が enum/struct 内の次行インデントに影響する問題を、行コメント到達でスキャンを停止することで修正する vim-patch（vim/vim#20458）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `indent_c.c`、`test_cindent.vim`。

**次のアクション:** マージ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [put_spec.lua occasionally fails "should ring the bell when deleting if not appropriate" test](https://github.com/neovim/neovim/issues/36250) `#36250`
**ステータス:** Open | **ラベル:** test

**概要:** `put_spec.lua` の特定テストが `screen.lua` の nil index で間欠的に失敗する flaky テスト。

**直近のアクティビティ:** `zeertzjq` が CI で再発した旨をコメント。

**次のアクション:** flaky 原因の調査。

---

### ![PR](icons/git-pull-request.svg) [test: virt_lines_overflow=wrap with virt_lines_leftcol](https://github.com/neovim/neovim/pull/40147) `#40147`
**ステータス:** Merged | **ラベル:** test

**概要:** `virt_lines_overflow=wrap` と `virt_lines_leftcol` の組み合わせのテストを追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `decorations_spec.lua`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): refresh codelens despite pending debounce](https://github.com/neovim/neovim/pull/40154) `#40154`
**ステータス:** Open | **ラベル:** lsp, AI assisted 🤖

**概要:** #38801 で導入した重複抑止のため、`workspace/codeLens/refresh` がクライアント側の編集 debounce に負けて保存後の CodeLens が古いまま残る問題を、refresh を debounce より高優先で扱うことで修正する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `lsp/codelens.lua`、`codelens_spec.lua`。

**次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [fix(lsp): requery empty isIncomplete completion lists](https://github.com/neovim/neovim/pull/40100) `#40100`
**ステータス:** Open | **ラベル:** lsp

**概要:** 空の `{ isIncomplete = true, items = {} }` が補完を終了させてしまう問題を、空リストでも isIncomplete を保持しキー入力で再クエリするよう修正する（#40096 を修正）。

**直近のアクティビティ:** `glepnir`・`MariaSolOs` によるレビューコメント計 8 件。

**変更ファイル:** `lsp/completion.lua`、`completion_spec.lua`。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [build(zig): add formatc and lintc steps](https://github.com/neovim/neovim/pull/38546) `#38546`
**ステータス:** Open | **ラベル:** ci:skip-news

**概要:** `build.zig` に formatc と lintc ステップを追加する。

**直近のアクティビティ:** `bfredl`・`dchinmay2` によるレビューコメント計 35 件。

**変更ファイル:** `build.zig`、`lint.zig`、`.clang-tidy`、`.clangd` ほか 8 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix(mappings): add multibyte character support to handle_mapping()](https://github.com/neovim/neovim/pull/40148) `#40148`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** エストニア語/スウェーデン語等の非 US 配列で `[]` が打ちにくい問題に対し、`langmap` でマルチバイト文字をマップできるよう `handle_mapping()` をバイト単位ではなく文字単位で反復するよう修正する（#2420, #27776 関連）。

**直近のアクティビティ:** `justinmk` がクローズ対象 issue の明記やタイトルへの 'langmap' 追加を依頼（計 4 件）。

**変更ファイル:** `getchar.c`、`getchar.h`、`langmap_spec.lua`。

**次のアクション:** PR 説明の更新と Draft の完成。

---

### ![Issue](icons/issue-opened.svg) [MANPAGER mode is not working properly if MANPATH is set.](https://github.com/neovim/neovim/issues/40129) `#40129`
**ステータス:** Closed | **ラベル:** closed:question, plugin, runtime

**概要:** Neovim を MANPAGER に使うと、`K` や `:Man` で別の man ページを開けず `man -w` エラーが出る。MANPATH が変更されるのも問題。

**直近のアクティビティ:** `kurth4cker` が bash/MANPATH 設定の回避策の動作と、設定起因の挙動について計 2 件コメント。

**次のアクション:** 設定起因のためクローズ。

---

### ![PR](icons/git-pull-request.svg) [fix(lua): vim.iter totable() handles packed multivalues and advances iterator state](https://github.com/neovim/neovim/pull/40134) `#40134`
**ステータス:** Open | **ラベル:** lua, AI assisted 🤖

**概要:** `vim.iter()` の配列処理で packed multivalue の扱いが不正な 2 点（#39705）を修正する。`totable()` が先頭要素しかチェックしない問題と、drain 後に head/tail が進まず再 yield される問題。

**直近のアクティビティ:** `mvanhorn` が、全走査を避けるため `_has_packed` フラグを `map()`/`enumerate()` のみが立てる方式に変更した旨をコメント。`alf171`・`gpanders` がレビュー。

**変更ファイル:** `vim/iter.lua`、`iter_spec.lua`。

**次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [tracking: terminal buffer persistence](https://github.com/neovim/neovim/issues/40017) `#40017`
**ステータス:** Open | **ラベル:** terminal, gsoc, editor-state

**概要:** GSoC 2026 の `:terminal` バッファ永続化のトラッキング issue。再起動やセッション再読み込みで端末バッファの内容が失われる問題に対し、`:write foo.mpack` での状態エクスポート/リストア等を計画。

**直近のアクティビティ:** `justinmk` がスクロールバック復元の意図、`Willaaaaaaa` が拡張子を `.mpack` にする件を計 2 件コメント。

**次のアクション:** 仕様の確定と実装。

---

### ![PR](icons/git-pull-request.svg) [ci: bump the github-actions group across 1 directory with 2 updates](https://github.com/neovim/neovim/pull/40152) `#40152`
**ステータス:** Merged | **ラベル:** ci, dependencies, github_actions

**概要:** `actions/checkout` を 6.0.2→6.0.3、`github/codeql-action` を更新する Dependabot PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/workflows/` 配下 17 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

## nvim-telescope/telescope.nvim

> 直近24時間の更新は ripgrep 不要の live_grep 要望 issue 1件のみ。

### ![Issue](icons/issue-opened.svg) [live_grep without ripgrep](https://github.com/nvim-telescope/telescope.nvim/issues/2083) `#2083`
**ステータス:** Open | **ラベル:** enhancement

**概要:** ripgrep への依存なしで live_grep ピッカーを使いたいという要望。grep には `--column` がないため `-b`（バイトオフセット）で代用する必要がある。

**直近のアクティビティ:** `ignacypekala` が、rg がなければ grep にフォールバックするスニペットをコメント。

**次のアクション:** フォールバック対応の検討。

---

## ghostty-org/ghostty

> vouched ユーザー以外の PR/issue は自動クローズされる運用が機能しており、3件が bot により処理。macOS のディスプレイ間移動による pty 寸法問題と fontconfig クラッシュの報告が注目。

### ![PR](icons/git-pull-request.svg) [Update VOUCHED list](https://github.com/ghostty-org/ghostty/pull/12978) `#12978`
**ステータス:** Merged | **ラベル:** なし

**概要:** discussion での要請を受け、VOUCHED リストに `@jamesarch` を追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.github/VOUCHED.td`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [macos: stream Vietnamese preedit to terminal](https://github.com/ghostty-org/ghostty/pull/12976) `#12976`
**ステータス:** Closed | **ラベル:** os/macos

**概要:** ベトナム語入力では区切り文字までシェルに中間テキストが渡らない問題に対し、preedit の変更をテキスト/バックスペースとしてストリーム送出する。

**直近のアクティビティ:** 投稿者 `bennv14` が「フォークでなく本リポジトリに誤って PR を開いた」と謝罪。`ghostty-vouch[bot]` が vouched 外のため自動クローズする旨をコメント。

**変更ファイル:** `SurfaceView_AppKit.swift`、`KeyboardLayout.swift` ほか。

**次のアクション:** クローズ済み（フォークで再提出が必要）。

---

### ![Issue](icons/issue-opened.svg) [macOS: Dragging window between displays with different backing scale factors transiently shouldn't change pty dimensions](https://github.com/ghostty-org/ghostty/issues/12925) `#12925`
**ステータス:** Open | **ラベル:** os/macos

**概要:** backing scale factor の異なるディスプレイ間でウィンドウをドラッグすると pty 寸法が一時的に誤った値になり、TUI（Claude Code 等）のスクロールバックが破壊される。

**直近のアクティビティ:** `adam0white` が、2 画面の quick terminal でトグル時に同様の破壊が起きると報告。

**次のアクション:** pty 寸法の一時変化の修正。

---

### ![Issue](icons/issue-opened.svg) [Linux: SIGSEGV in FcCompare when fontconfig initialization fails](https://github.com/ghostty-org/ghostty/issues/12968) `#12968`
**ステータス:** Closed | **ラベル:** os/linux

**概要:** fontconfig の初期化が NULL を返すとき（キャッシュ破損や設定欠落）、フォールバックフォント探索の `FcCompare` で SIGSEGV する。15 件の coredump が同一スタックトレースを示す。

**直近のアクティビティ:** `ghostty-vouch[bot]` が、非メンテナは issue を作れず discussion を先に作る運用である旨をコメント。

**次のアクション:** discussion での再提出が必要（issue はクローズ）。

---

### ![PR](icons/git-pull-request.svg) [font: add null checks for fontconfig C API return values](https://github.com/ghostty-org/ghostty/pull/12967) `#12967`
**ステータス:** Closed | **ラベル:** font

**概要:** #12968 のクラッシュ修正で、`FcInitLoadConfigAndFonts` 等の fontconfig C 関数が NULL を返す場合の null チェックを Zig ラッパーに追加する。

**直近のアクティビティ:** `ghostty-vouch[bot]` が vouched 外のため自動クローズする旨をコメント。

**変更ファイル:** `pkg/fontconfig/` 配下と `font/` 配下の計 7 ファイル。

**次のアクション:** クローズ済み（vouched 後に再提出が必要）。

---

## astral-sh/uv

> 0.11.20 をリリースしつつ、`uv upgrade` プレビュー機能と ty 連携（workspace metadata）の開発が活発。キャッシュの content-addressing（dirhash）系の大型 Draft が複数並行し、Renovate/自動 PR と多数の質問・バグ issue が更新。AI 自動生成 PR の拒否方針も明確化されている。

### ![PR](icons/git-pull-request.svg) [Upgrade dependency constraints during `uv upgrade`](https://github.com/astral-sh/uv/pull/19738) `#19738`
**ステータス:** Open | **ラベル:** preview

**概要:** #19678 の続きで、`uv upgrade <package>` が `pyproject.toml` の本番依存を実際に更新できるようにする。解決済みバージョンを除外する上限/等価制約のみ緩め、下限・除外・extras・markers 等は保持する。lockfile 同期は後続の `--tighten` で扱う。

**直近のアクティビティ:** `astral-sh-bot[bot]` がテストインベントリ変更（+18/-3）を報告。`konstin`・`zsol`・`zanieb` のレビューコメント計 23 件。

**変更ファイル:** `version.rs`、`lock/mod.rs`、`upgrade.rs`、`upgrade.rs`（テスト）。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Remove redundant tests](https://github.com/astral-sh/uv/pull/19766) `#19766`
**ステータス:** Open（Draft）| **ラベル:** internal

**概要:** codex に冗長なテストを探させて削除する PR（-532 行）。性能向上は小さいが安価なクリーンアップ。

**直近のアクティビティ:** `zanieb` が「一部は良いが、`tool_upgrade_with` を `tool_upgrade_pinned_hint` で置き換える価値には疑問」とコメント。テストインベントリは -15。`EliteTK` がレビュー。

**変更ファイル:** `expanded_tags.rs`、`pip_compile.rs`、`tool_upgrade.rs` ほか 12 ファイル。

**次のアクション:** 削除対象の取捨選択。

---

### ![PR](icons/git-pull-request.svg) [Add `environment.root` to `uv workspace metadata --sync`](https://github.com/astral-sh/uv/pull/19760) `#19760`
**ステータス:** Merged | **ラベル:** preview

**概要:** `ty` が関心を持つ `environment.root` フィールドをワークスペースメタデータに追加する。スキーマに先行導入し、後続で連携を拡充する。

**直近のアクティビティ:** `konstin` が APPROVED。

**変更ファイル:** `metadata.rs`、`workspace/metadata.rs`、`metadata.md`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Compute and pass `uv workspace metadata` payload in `ty check`](https://github.com/astral-sh/uv/pull/19763) `#19763`
**ステータス:** Open | **ラベル:** preview

**概要:** `uv check` 特有の uv→ty→uv 循環を、uv 側でメタデータを計算して ty に渡すことで解消する案。`--isolated`/`--index` 等を ty 側のコールバックなしで正しく扱える。

**直近のアクティビティ:** `codspeed-hq[bot]` が「性能が 30.33%劣化」と警告。テストインベントリは +2/-62。

**変更ファイル:** `check.rs`、`check/ty.rs`、`module_owners.rs` ほか 8 ファイル。

**次のアクション:** 性能劣化の解消とレビュー。

---

### ![Issue](icons/issue-opened.svg) [`uv self update` fails in MSYS2 environment](https://github.com/astral-sh/uv/issues/12686) `#12686`
**ステータス:** Open | **ラベル:** bug

**概要:** MSYS2（Windows 10）でスタンドアロンインストーラ経由の uv に対し `uv self update` が「standalone scripts でのみ利用可能」と警告して失敗する。

**直近のアクティビティ:** `eegli` が「cargo-dist 側では修正済み、uv の publish に v0.32+ を使えば解決」とコメント。

**次のアクション:** cargo-dist v0.32+ への更新。

---

### ![PR](icons/git-pull-request.svg) [Use srs for Linux and macOS CI](https://github.com/astral-sh/uv/pull/19754) `#19754`
**ステータス:** Open（Draft）| **ラベル:** test:macos

**概要:** srs `2026.06.09` リリースを Linux x86_64/macOS arm64 CI で使い、mold の代わりに srs/sld を使う。`Swatinem/rust-cache` を生の `actions/cache` に置き換える。

**直近のアクティビティ:** `zaniebot` が、warm 再実行で CI コストは改善せず合計 runner 時間が 93%増と報告。`codspeed-hq[bot]` が 65.32%劣化を警告。

**変更ファイル:** `.github/actions/` `.github/workflows/` 配下と各 crate の計 20 ファイル。

**次のアクション:** CI コスト増の評価。

---

### ![PR](icons/git-pull-request.svg) [Upgrade Windows allocator to mimalloc v3](https://github.com/astral-sh/uv/pull/19743) `#19743`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** Windows のアロケータを mimalloc v3（0.1.52）に更新する。warm cache の peak RSS は低下、empty cache の peak RSS は増加とほぼ中立。

**直近のアクティビティ:** `zaniebot` が、cold peak RSS が 93.9→108.6 MiB（15.60%増）と報告。

**変更ファイル:** `Cargo.lock`、`uv-performance-memory-allocator/Cargo.toml`。

**次のアクション:** cold RSS 増加の評価。

---

### ![Issue](icons/issue-opened.svg) [Dependency Dashboard](https://github.com/astral-sh/uv/issues/2658) `#2658`
**ステータス:** Open | **ラベル:** internal

**概要:** Renovate の更新と検出依存を一覧する自動ダッシュボード。長期更新のない abandoned 依存 35 件を含む。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** Renovate 自動管理のため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Support newer GraalPy release assets that include the Python version.](https://github.com/astral-sh/uv/pull/19458) `#19458`
**ステータス:** Merged | **ラベル:** なし

**概要:** GraalPy の新しいリリース成果物名（CPython 互換版名を含む）に対応するようメタデータ取得スクリプトを更新する。旧形式との両対応。

**直近のアクティビティ:** `zanieb` が APPROVED。

**変更ファイル:** `fetch-download-metadata.py`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add a synthetic large-workspace benchmark](https://github.com/astral-sh/uv/pull/19687) `#19687`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** 127 メンバーと大きな生成 TOML を持つ自己完結のワークスペース探索ベンチマークを追加する（キャッシュにより 15.41x 高速化を測定）。

**直近のアクティビティ:** テストインベントリ変更なし、`codspeed-hq[bot]` も性能変化なしと報告。

**変更ファイル:** `Cargo.lock`、`uv-bench/Cargo.toml`、`workspace_discovery.rs`。

**次のアクション:** Draft の完成。

---

### ![Issue](icons/issue-opened.svg) [Native-Auth issues with pip and Artifactory](https://github.com/astral-sh/uv/issues/18900) `#18900`
**ステータス:** Open | **ラベル:** bug

**概要:** `native-auth` プレビュー有効時、Artifactory への `uv auth login`/`token` は通るが、install/sync 系コマンドが動かないというバグ報告。

**直近のアクティビティ:** `guillaume-alliander` が、mac でも同様で、sync が URL→host の順に試している様子だとコメント。

**次のアクション:** native-auth の認証情報マッチングの調査。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Stabilize preview feature](https://github.com/astral-sh/uv/pull/19197) `#19197`
**ステータス:** Open（Draft）| **ラベル:** breaking

**概要:** package-by-default の安定化後の姿をプレビューする変更（次の breaking リリースでマージ予定）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `init.rs`、`settings.rs`、`build.rs`（テスト）ほか 7 ファイル。

**次のアクション:** breaking リリース時のマージ。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Update documentation](https://github.com/astral-sh/uv/pull/19026) `#19026`
**ステータス:** Open（Draft）| **ラベル:** documentation, breaking

**概要:** package-by-default の安定化に合わせ、アプリ/パッケージドアプリの位置づけを入れ替えるドキュメント更新（安定化リリース直前にマージ予定）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `docs/concepts/projects/` ほか計 7 ファイル。

**次のアクション:** 安定化リリース時のマージ。

---

### ![PR](icons/git-pull-request.svg) [Package by default: Add in preview](https://github.com/astral-sh/uv/pull/17841) `#17841`
**ステータス:** Merged | **ラベル:** enhancement, preview

**概要:** `uv init` の（プレビュー）デフォルトを src パッケージに切り替える。エントリポイント付きパッケージ/純ライブラリ/フラットアプリの 3 レイアウトを既存フラグで選択可能にする。

**直近のアクティビティ:** `codspeed-hq[bot]` は性能変化なし。`zsol`・`konstin` のレビューコメント計 15 件。

**変更ファイル:** `uv-cli/src/lib.rs`、`uv-preview/src/lib.rs`、`init.rs`、`settings.rs` ほか。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Bump version to 0.11.20](https://github.com/astral-sh/uv/pull/19765) `#19765`
**ステータス:** Merged | **ラベル:** releases

**概要:** 2026-06-10 リリースの 0.11.20。`uv export` への `--emit-index-url`/`--emit-find-links` 追加、`uv pip list` の `--find-links` 対応、macOS リリースで ICF によるバイナリ縮小などを含む。

**直近のアクティビティ:** `zsol` が APPROVED。

**変更ファイル:** `CHANGELOG.md`、`Cargo.toml`、各 crate の Cargo.toml/README など 100 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Reject Git revisions in `uv upgrade`](https://github.com/astral-sh/uv/pull/19742) `#19742`
**ステータス:** Merged | **ラベル:** preview

**概要:** `rev` 指定の Git 依存はレジストリのバージョン制約がなく `uv upgrade` で意味ある更新ができないため、専用エラーを返すようにする（#19678 の続き）。

**直近のアクティビティ:** `konstin` が APPROVED。

**変更ファイル:** `upgrade.rs`、`upgrade.rs`（テスト）。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add a small documentation section for malware checks](https://github.com/astral-sh/uv/pull/19680) `#19680`
**ステータス:** Merged | **ラベル:** documentation

**概要:** #18936 で導入したマルウェアチェックのドキュメントを追加する（#19519 をクローズ）。

**直近のアクティビティ:** `charliermarsh` が APPROVED。

**変更ファイル:** `sync.md`、`uv.lock`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [UV_PUBLISH_CHECK_URL papercut](https://github.com/astral-sh/uv/pull/19690) `#19690`
**ステータス:** Merged | **ラベル:** documentation

**概要:** `UV_PUBLISH_CHECK_URL` 関連の小さな改善（env_vars への追記）。

**直近のアクティビティ:** `charliermarsh` が APPROVED。

**変更ファイル:** `env_vars.rs`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Use ICF in macOS release builds to reduce binary sizes](https://github.com/astral-sh/uv/pull/19615) `#19615`
**ステータス:** Merged | **ラベル:** enhancement, releases

**概要:** macOS リリースで `--icf=safe`（Rust 同梱の Mach-O LLD 経由）を使いバイナリサイズを削減する。Linux はサイズ改善が乏しく除外。

**直近のアクティビティ:** `charliermarsh` が APPROVED。`zsol` が「ICF で逆に速くなるのは直感に反する、panic のスタックトレース崩れに注意すべきでは」とコメントしつつ APPROVED。

**変更ファイル:** `build-release-binaries.yml`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Reduce noise in the many-file sdist benchmark](https://github.com/astral-sh/uv/pull/19750) `#19750`
**ステータス:** Merged | **ラベル:** internal

**概要:** `unpack_sdist_many_files` ベンチを Linux で `/dev/shm` に展開し、`BatchSize::PerIteration` で各イテレーション後に破棄してノイズを削減する。

**直近のアクティビティ:** `codspeed-hq[bot]` が「性能が 50.59%改善」と報告。

**変更ファイル:** `uv-bench/benches/uv.rs`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Group executable install errors during `uv python install`](https://github.com/astral-sh/uv/pull/19691) `#19691`
**ステータス:** Merged | **ラベル:** error messages

**概要:** `uv python install` でデフォルト実行ファイルをリンクする際、未管理ファイルとの衝突を 1 件ずつではなくまとめて 1 つのエラーで報告する。

**直近のアクティビティ:** `LJX-xixi` のレビューコメント計 7 件。

**変更ファイル:** `python/install.rs`、`python_install.rs`（テスト）。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Fix compile error](https://github.com/astral-sh/uv/pull/19739) `#19739`
**ステータス:** Merged | **ラベル:** internal

**概要:** #19606 に取り組む前提として、`upgrade.rs` のコンパイルエラーを修正する。

**直近のアクティビティ:** `zanieb` が APPROVED（Thanks!）。

**変更ファイル:** `project/upgrade.rs`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Avoid following symlinks during cache prune](https://github.com/astral-sh/uv/pull/19543) `#19543`
**ステータス:** Merged | **ラベル:** bug

**概要:** `uv cache prune` が削除前にパスを `canonicalize()` するため、シンボリックリンクがあると外部のターゲットを削除してしまうバグを修正する（#19542 をクローズ）。

**直近のアクティビティ:** `charliermarsh` が APPROVED。

**変更ファイル:** `uv-cache/src/lib.rs`。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add initial hidden `uv upgrade` command](https://github.com/astral-sh/uv/pull/19678) `#19678`
**ステータス:** Merged | **ラベル:** preview

**概要:** `uv upgrade <package>` の隠し初期実装を追加し、依存選択・制約書き換え・リゾルバ統合を検証する。read-only で `pyproject.toml`/`uv.lock`/環境は変更しない。+1385 行。

**直近のアクティビティ:** `konstin` が APPROVED、`zsol`・`konstin` のコメント計 3 件。

**変更ファイル:** `uv-cli/src/lib.rs`、`pyproject_mut.rs`、`upgrade.rs` ほか 12 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add `--find-links` support for `uv pip list`](https://github.com/astral-sh/uv/pull/16103) `#16103`
**ステータス:** Merged | **ラベル:** enhancement

**概要:** `uv pip list --outdated` で `--find-links` の場所が最新版検索に反映されるよう修正する（#16095 を解決）。`--no-index` でも flat index を registry client に通す。

**直近のアクティビティ:** `terror`・`konstin`・`charliermarsh` のレビューコメント計 8 件。

**変更ファイル:** `registry_client.rs`、`index_url.rs`、`pip/latest.rs` ほか。

**次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update dependencies](https://github.com/astral-sh/uv/pull/18980) `#18980`
**ステータス:** Open | **ラベル:** internal, build:skip-release, build:skip-docker

**概要:** alpine/amazonlinux/pyston のコンテナダイジェストを更新する Renovate PR。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `test-smoke.yml`、`test-system.yml`。

**次のアクション:** 手動マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Infer `uv tool` Python requests from remote distributions with static metadata](https://github.com/astral-sh/uv/pull/19583) `#19583`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** リモート配布物の静的メタデータから `uv tool` の Python リクエストを推測する（#19577 の続き、#14110/#19079/#19676 をクローズ予定）。+1522 行。

**直近のアクティビティ:** `zanieb` のレビューコメント計 2 件。

**変更ファイル:** `source/mod.rs`、`tool/install.rs`、`tool/run.rs` ほか 8 ファイル。

**次のアクション:** レビュー対応と Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Sync latest Python releases](https://github.com/astral-sh/uv/pull/19764) `#19764`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** Python リリースのメタデータを自動更新する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `download-metadata.json`。

**次のアクション:** 自動更新のためマージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Ignore directories named pyproject.toml during settings and workspace discovery](https://github.com/astral-sh/uv/pull/19762) `#19762`
**ステータス:** Open | **ラベル:** なし

**概要:** パス上に `pyproject.toml` という名前のディレクトリがあると全コマンドが「Is a directory」で落ちるバグ（#14584）を修正する。親ディレクトリにある場合は黙ってスキップし、カレントにある場合は明確なエラーを出す。

**直近のアクティビティ:** `astral-sh-bot[bot]` がテストインベントリ +2 を報告。

**変更ファイル:** `uv-settings/src/lib.rs`、`workspace.rs`、`lock.rs`。

**次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Request for `uv.lock` to support different index urls across developer machines and CI](https://github.com/astral-sh/uv/issues/6349) `#6349`
**ステータス:** Open | **ラベル:** enhancement, needs-design

**概要:** 開発者マシンと CI で異なる index URL を `uv.lock` でサポートしてほしいという要望。

**直近のアクティビティ:** `axellpadilla` が、プロキシ対応のため versions を変えず再解決する sync フラグの案を支持。

**次のアクション:** 設計の検討。

---

### ![Issue](icons/issue-opened.svg) [`uv venv --seed` fails in Windows Docker containers when the virtualenv is on a volume](https://github.com/astral-sh/uv/issues/19761) `#19761`
**ステータス:** Open | **ラベル:** bug

**概要:** Windows Docker コンテナでマウントボリューム上の virtualenv にパッケージをインストールすると uv が失敗する（uv 0.11.19）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** ボリューム上での失敗原因の調査。

---

### ![Issue](icons/issue-opened.svg) [`uv add --index <name>=<url> <package>` can overwrite an index of the wrong format](https://github.com/astral-sh/uv/issues/19759) `#19759`
**ステータス:** Open | **ラベル:** bug

**概要:** `uv add --index <name>=<url>` が既存 index を `<name>` でマッチして `<url>` を上書きし、format の異なる index を黙って壊す。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** index 上書き挙動の修正。

---

### ![PR](icons/git-pull-request.svg) [Deduplicate binary payloads with archive manifests](https://github.com/astral-sh/uv/pull/19694) `#19694`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** ネイティブライブラリのペイロードを BLAKE3 ダイジェスト鍵の `archive-files-v0` バケットに分離し、サイドカーマニフェストでパスを共有オブジェクトにマッピングしてインストール時にリンクする。+1203 行。

**直近のアクティビティ:** `codspeed-hq[bot]` は性能変化なしと報告。

**変更ファイル:** `archive_file_manifest.rs`、`dirhash/`、`uv-fs/src/link.rs` ほか 32 ファイル。

**次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Content-address wheel archives with directory hashes](https://github.com/astral-sh/uv/pull/19693) `#19693`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** ZIP wheel 展開時に決定的なディレクトリダイジェストを計算し、それを archive ID に使う。同一ツリーが複数回保存されるのを防ぐ。+2196 行。

**直近のアクティビティ:** `codspeed-hq[bot]` は性能変化なしと報告。

**変更ファイル:** `uv-cache/src/archive.rs`、`dirhash/`、`stream.rs`、`sync.rs` ほか 16 ファイル。

**次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Content-address distributions in the cache by `dirhash`](https://github.com/astral-sh/uv/pull/19665) `#19665`
**ステータス:** Closed | **ラベル:** なし

**概要:** 展開した `.whl` を BLAKE3 ダイジェストで content-address する初期 PR。#19693 等に分割・継承されたとみられクローズ済み。+1933 行。

**直近のアクティビティ:** `codspeed-hq[bot]` は性能変化なしと報告。

**変更ファイル:** `archive.rs`、`dirhash/`、`stream.rs` ほか 12 ファイル。

**次のアクション:** クローズ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [`uv add --index <name>=<url> <package>` can rename indexes used for source pins](https://github.com/astral-sh/uv/issues/19758) `#19758`
**ステータス:** Open | **ラベル:** question

**概要:** 既存 index と同じ URL で別 name を指定すると index が rename され、`[tool.uv.sources]` の旧 name 参照が更新されず不整合になる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** rename 挙動の整理。

---

### ![Issue](icons/issue-opened.svg) [`uv add --index <name>=<url> <package>` can resolve differently to a subsequent `uv lock`](https://github.com/astral-sh/uv/issues/19757) `#19757`
**ステータス:** Open | **ラベル:** question

**概要:** explicit な index を `uv add --index` で更新すると、後続の `uv lock`（index 未指定）で異なる解決結果になりうる。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** explicit index の扱いの整理。

---

### ![PR](icons/git-pull-request.svg) [Fix Python downloads with explicit `+gil` requests](https://github.com/astral-sh/uv/pull/17439) `#17439`
**ステータス:** Open（Draft）| **ラベル:** bug

**概要:** 明示的な `+gil` リクエストでの Python ダウンロードを修正する（#17437 をクローズ）。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `discovery.rs`、`downloads.rs`、`installation.rs`、`python_install.rs`。

**次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Use performance allocator for benchmarks](https://github.com/astral-sh/uv/pull/19756) `#19756`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** `uv-bench` ベンチターゲットにパフォーマンスメモリアロケータをリンクし、CodSpeed とローカルで同じ設定を使う。

**直近のアクティビティ:** `codspeed-hq[bot]` が「性能が×3.1 改善」と報告。`konstin` が APPROVED（lmao）。

**変更ファイル:** `bench.yml`、`uv-bench/Cargo.toml`、`uv-bench/benches/uv.rs`。

**次のアクション:** Draft の完成。

---

### ![Issue](icons/issue-opened.svg) [Dependencies in packaged project with scripts (x4 questions)](https://github.com/astral-sh/uv/issues/15632) `#15632`
**ステータス:** Open | **ラベル:** question

**概要:** packaged project でメイン依存とスクリプト依存の扱いに関する初心者の質問。

**直近のアクティビティ:** `beanscg` が「Bean Labs が public-source パスを実施。次の有用なステップは scripts/package のドキュメント明確化」とコメント。

**次のアクション:** ドキュメントの明確化。

---

### ![Issue](icons/issue-opened.svg) [Parsing of `Dynamic:` field in PKG-INFO should be case insensitive](https://github.com/astral-sh/uv/issues/18667) `#18667`
**ステータス:** Open | **ラベル:** bug, compatibility

**概要:** `parse_pkg_info` の `Dynamic` フィールドのマッチが大文字小文字を区別するため、`Dynamic: requires-python` のような行が検出されず false negative になる。

**直近のアクティビティ:** `septatrix` が、アップロード日時で case-insensitive を切り替える案を提示。`konstin` がミラーで timestamp が無い場合に堅牢でないと指摘。`Marius1311` が `slepc4py` で install が壊れる実例を報告（計 5 件）。

**次のアクション:** 互換性を保った大文字小文字非依存化の設計。

---

### ![Issue](icons/issue-opened.svg) [Support RFC 8628 device auth flow with `uv auth login`](https://github.com/astral-sh/uv/issues/19136) `#19136`
**ステータス:** Open | **ラベル:** enhancement

**概要:** `uv auth login` で OAuth2 デバイス認可フロー（RFC 8628、「URL を開いてコード入力」方式）をサポートしてほしいという要望。

**直近のアクティビティ:** `beanscg` が「Bean Labs が public-source パスを実施。次のステップは auth flow の設計制約」とコメント。

**次のアクション:** auth フローの設計。

---

### ![PR](icons/git-pull-request.svg) [Split uv integration tests into smaller test harnesses](https://github.com/astral-sh/uv/pull/19603) `#19603`
**ステータス:** Closed | **ラベル:** performance

**概要:** 統合テストを専用 `uv-integration` crate に移し 12 のコマンド領域別ハーネスに分割して再リンクを削減する試作。

**直近のアクティビティ:** `charliermarsh` が「複雑さへの反発が大きく一旦クローズする。価値はあると思うが投資を続けたくない」とコメント。`konstin`・`charliermarsh` のレビュー計 10 件。

**次のアクション:** クローズ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [ruff Checks Files That are Not Included When Copying uv From Distroless Image](https://github.com/astral-sh/uv/issues/19681) `#19681`
**ステータス:** Open | **ラベル:** question

**概要:** distroless image から uv をコピーした GitHub workflow で、ruff が含まれないファイルをチェックしてしまう問題。キャッシュ位置も要因。

**直近のアクティビティ:** `beanscg` が「Bean Labs が public-source パスを実施。次のステップは workflow の cache/copy 再現」とコメント。

**次のアクション:** 再現と原因切り分け。

---

### ![Issue](icons/issue-opened.svg) [Docs for workspaces are missing a monorepo publish management guide](https://github.com/astral-sh/uv/issues/19391) `#19391`
**ステータス:** Open | **ラベル:** question

**概要:** ワークスペースのドキュメントに monorepo の publish 管理ガイドがなく、PyPI 名衝突時のリネーム連鎖などで実運用上の摩擦が生じる。

**直近のアクティビティ:** `beanscg` が「Bean Labs が public-source パスを実施。次のステップは publish 管理ガイドの骨子」とコメント。

**次のアクション:** ガイドの追加。

---

### ![Issue](icons/issue-opened.svg) [Best practice to install Python packages when developing on container with watch mode](https://github.com/astral-sh/uv/issues/14234) `#14234`
**ステータス:** Open | **ラベル:** question

**概要:** Docker 開発で `pyproject.toml` に追加した依存がリビルド後に見つからず、ローカルでの `uv add` が冗長な `.venv` を作る問題。

**直近のアクティビティ:** `beanscg` が「Bean Labs が public-source パスを実施。次のステップはコンテナ/開発ワークフローの明確化」とコメント。

**次のアクション:** ドキュメントの明確化。

---

### ![PR](icons/git-pull-request.svg) [fix(#9902): ModuleNotFoundError related to venv uv_cache.json when stored in iCloud](https://github.com/astral-sh/uv/pull/19752) `#19752`
**ステータス:** Closed | **ラベル:** なし

**概要:** REAPR による自動修正 PR（#9902）。`tool.rs` の import を変更する。

**直近のアクティビティ:** `konstin` が「自動 PR は不可（AI_POLICY 参照）」とコメント。

**次のアクション:** クローズ済み（自動 PR は不可）。

---

### ![PR](icons/git-pull-request.svg) [Update Rust crate tikv-jemallocator to 0.7.0](https://github.com/astral-sh/uv/pull/19735) `#19735`
**ステータス:** Open | **ラベル:** internal, do-not-merge, build:skip-docker

**概要:** `tikv-jemallocator` を 0.6.0→0.7.0 に更新する Renovate PR。

**直近のアクティビティ:** `codspeed-hq[bot]` は性能変化なし。`zaniebot` がアロケータ構成の比較を報告。`zanieb` がレビューコメント。

**変更ファイル:** `.cargo/config.toml`、`Cargo.lock`、`uv-performance-memory-allocator/Cargo.toml`。

**次のアクション:** do-not-merge のため評価継続。

---

### ![PR](icons/git-pull-request.svg) [Add performance reporting skill](https://github.com/astral-sh/uv/pull/19753) `#19753`
**ステータス:** Open（Draft）| **ラベル:** なし

**概要:** uv の PR 向けにベンチ計画・既存エビデンス監査・簡潔な性能レポート作成を行うリポジトリローカル skill を追加する。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.agents/skills/report-performance/` 配下 3 ファイル。

**次のアクション:** Draft の完成。

---

### ![Issue](icons/issue-opened.svg) [Use `uv run --with` while respecting index configuration?](https://github.com/astral-sh/uv/issues/19737) `#19737`
**ステータス:** Open | **ラベル:** question

**概要:** AWS CodeArtifact 上のプライベート依存の複数版を `uv run --with` でテストできない。explicit index からの `--with` 要件が現状サポートされていないことが核心。

**直近のアクティビティ:** `zanieb` が「`--with` 要件は explicit index から取れない、最善策は未定」とコメント。`ddluke` が #17455 が役立つかもと示唆。

**次のアクション:** explicit index サポートの設計。

---

### ![Issue](icons/issue-opened.svg) [Identical Files Appearing in `UV_CACHE_DIR`](https://github.com/astral-sh/uv/issues/19744) `#19744`
**ステータス:** Closed | **ラベル:** bug, external

**概要:** Podman で UBI 8 コンテナをオフライン再構築する特殊ワークフローで、`UV_CACHE_DIR` に同一ファイルが現れる問題。

**直近のアクティビティ:** `zanieb` が「この種のファイルシステムへの対策は当面しないが再発したら検討する」とコメント、報告者が了承。

**次のアクション:** クローズ済み（特殊事例のため対応見送り）。

---

### ![PR](icons/git-pull-request.svg) [Share Claude and Codex hook scripts](https://github.com/astral-sh/uv/pull/19755) `#19755`
**ステータス:** Merged | **ラベル:** internal

**概要:** Codex のリポジトリローカルフックを追加し、フック実装を `scripts/hooks` に移して Claude と Codex で共有する。Codex の `apply_patch` ペイロードにも対応。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**変更ファイル:** `.claude/hooks/`、`.codex/hooks.json`、`scripts/hooks/` ほか 7 ファイル。

**次のアクション:** マージ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [`XDG_BIN_HOME` common default not used](https://github.com/astral-sh/uv/issues/6937) `#6937`
**ステータス:** Closed | **ラベル:** configuration, releases

**概要:** `XDG_BIN_HOME` 未設定時に Linux で `~/.cargo/bin/` にインストールされるが、慣例の `$HOME/.local/bin` を使うべきという要望。

**直近のアクティビティ:** `HaleTom` が、標準なのは `$HOME/.local/bin` で `$XDG_BIN_HOME` 変数自体は非標準だと整理してコメント。

**次のアクション:** クローズ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [How to request for the GIL version of Python in `.python-version`?](https://github.com/astral-sh/uv/issues/17437) `#17437`
**ステータス:** Open | **ラベル:** bug

**概要:** GIL 版 Python を使いたいのに uv が freethreaded 版を選び、`.python-version` で `3.14+gil` が認識されない。

**直近のアクティビティ:** `nickurak` が「Python バージョン指定とは独立に gil/freethreading だけ指定できると良い」とコメント。

**次のアクション:** `+gil` 指定方法の改善。

---

## microsoft/vscode

> 1.124.0 のリリース直後で、Copilot/Agents Window/Agent Host 周りの機能追加・回帰修正が大量。多くは旧バージョン利用者からのバグ報告や `info-needed` クローズ、CLA 待ちで、`copilot-pull-request-reviewer[bot]` による自動レビューが大半の PR に付いている。アクセシビリティ SLA バグ群と Copilot クォータ周りの UX 修正が目立つ。

### ![Issue](icons/issue-opened.svg) [can't disable quickSuggestions](https://github.com/microsoft/vscode/issues/320783) `#320783`
**ステータス:** Open | **ラベル:** new release | **概要:** quickSuggestions を無効化できないというバグ報告。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 再現確認。

---

### ![PR](icons/git-pull-request.svg) [Copilot SH: Use workspace customizations as-is, without bundling](https://github.com/microsoft/vscode/pull/320780) `#320780`
**ステータス:** Open | **概要:** Copilot Session Host でワークスペースのカスタマイズをバンドルせずそのまま使うようにする。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **変更ファイル:** `copilotAgent.ts`、`copilotSessionLauncher.ts` ほか。 **次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Feature Request: Idle Timeout for MCP Servers](https://github.com/microsoft/vscode/issues/318559) `#318559`
**ステータス:** Open | **ラベル:** feature-request | **概要:** MCP サーバが起動後に無期限稼働しメモリ圧迫でレンダラがクラッシュするため、アイドルタイムアウトでの自動停止を要望。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 設計検討。

---

### ![Issue](icons/issue-opened.svg) [Microsoft Authentication is not persisted](https://github.com/microsoft/vscode/issues/320776) `#320776`
**ステータス:** Open | **概要:** MS 認証が永続化されず繰り返し認証を求められるバグ報告。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [selectable sort order for copilot chat sessions](https://github.com/microsoft/vscode/issues/320781) `#320781`
**ステータス:** Open | **概要:** Copilot Chat セッション一覧の並び順を時系列（古い順）にも切り替えられる設定の要望。 **直近のアクティビティ:** `vs-code-engineering[bot]` が最新版への更新を案内。 **次のアクション:** 設定追加の検討。

---

### ![Issue](icons/issue-opened.svg) [Empty terminal command for Claude in agent host](https://github.com/microsoft/vscode/issues/320784) `#320784`
**ステータス:** Open | **ラベル:** bug, agent-host | **概要:** Agent Host で Claude のターミナルコマンドが空表示になる（Copilot でも稀に発生）。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** #320782 で修正中。

---

### ![Issue](icons/issue-opened.svg) [markdown language errors](https://github.com/microsoft/vscode/issues/320773) `#320773`
**ステータス:** Open | **ラベル:** info-needed, new release | **概要:** WSL で markdown language features に 5 件の uncaught error が出る。 **直近のアクティビティ:** `flyinglyon` が拡張機能のアクティベーション失敗ログを貼付、`vs-code-engineering[bot]` が情報不足を案内。 **次のアクション:** 追加情報待ち。

---

### ![PR](icons/git-pull-request.svg) [Fix empty terminal command for terminal tools without input](https://github.com/microsoft/vscode/pull/320782) `#320782`
**ステータス:** Open（Draft）| **概要:** #320251 の回帰で、SDK 組み込み bash ツール実行時にコマンドが streaming 中だと空のターミナルコマンドが表示される問題を、`getTerminalInput(tc) !== undefined` で gate して修正する。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **変更ファイル:** `stateToProgressAdapter.ts` とそのテスト。 **次のアクション:** Draft の完成。

---

### ![Issue](icons/issue-opened.svg) [Agents Window Agent Harness with BYOK Support](https://github.com/microsoft/vscode/issues/320339) `#320339`
**ステータス:** Open | **概要:** Agents Window で BYOK モデルとカスタムエージェントをサポートしてほしいという要望。 **直近のアクティビティ:** `hu3bi` が既存 BYOK セッションも表示されないと報告、`mderazon` が claude がハードコードされている様子をコメント。 **次のアクション:** BYOK サポートの設計。

---

### ![Issue](icons/issue-opened.svg) [Registry only allowing manually added servers to run without blocking them](https://github.com/microsoft/vscode/issues/318722) `#318722`
**ステータス:** Open | **概要:** MCP の「Registry Only」ポリシーがレジストリ外のリモートサーバ追加・実行を制限しない。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** ポリシー挙動の確認。

---

### ![Issue](icons/issue-opened.svg) [Cant rename symbol due to Copilot No Credits Message](https://github.com/microsoft/vscode/issues/320778) `#320778`
**ステータス:** Open | **ラベル:** new release | **概要:** F2 のシンボル名変更時に Copilot のクォータ超過ダイアログが出て rename が中断される。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** #320775 で修正中。

---

### ![Issue](icons/issue-opened.svg) [Refactor de paquete](https://github.com/microsoft/vscode/issues/320779) `#320779`
**ステータス:** Open | **概要:** パッケージのリネーム時に変更ファイルが全て未保存で開かれ、確認が煩雑というスペイン語のバグ報告。 **直近のアクティビティ:** `vs-code-engineering[bot]` が最新版への更新を案内。 **次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [Subagents sometimes show up in session list](https://github.com/microsoft/vscode/issues/320777) `#320777`
**ステータス:** Open | **ラベル:** bug, agent-host | **概要:** サブエージェントがセッション一覧に表示されることがある（スクリーンショットのみ）。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 表示ロジックの修正。

---

### ![Issue](icons/issue-opened.svg) [Can't turn on Chat Session Sync (`chat.sessionSync.enabled`)](https://github.com/microsoft/vscode/issues/320335) `#320335`
**ステータス:** Open | **概要:** `chat.sessionSync.enabled` が有効表示なのにチェックボックスが無効で、組織管理の表示が出る。 **直近のアクティビティ:** `hu3bi` が、BYOK + Copilot free で同様、`/etc/vscode/policy.json` も存在しないと報告。 **次のアクション:** 設定状態の調査。

---

### ![Issue](icons/issue-opened.svg) [Weekly updates are too frequent](https://github.com/microsoft/vscode/issues/309223) `#309223`
**ステータス:** Open | **ラベル:** install-update, under-discussion | **概要:** 週次更新が頻繁すぎて作業を妨げるという不満。 **直近のアクティビティ:** 複数ユーザー（`hx41455`、`tamasarpad`、`MyNameIsHelloWorld` ほか）が更新頻度への不満を計 5 件コメント。 **次のアクション:** 更新頻度方針の議論継続。

---

### ![PR](icons/git-pull-request.svg) [copilot: don't show quota dialog for automatic rename suggestions](https://github.com/microsoft/vscode/pull/320775) `#320775`
**ステータス:** Open | **概要:** F2 時に自動 rename 提案がクォータ超過モーダルを出してフォーカスを奪い rename を妨げる問題を、自動トリガー時はダイアログを出さないよう修正する（#319414 を修正）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `renameSuggestionsProvider.ts` とそのテスト。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Auto: keep model cache across token refresh](https://github.com/microsoft/vscode/pull/320764) `#320764`
**ステータス:** Open | **概要:** トークンリフレッシュ時に Auto モデルキャッシュをクリアするとモデルが暗黙に切り替わる問題を、実験設定 `chat.advanced.autoModePreserveModelOnTokenRefresh`（既定 false）の背後でキャッシュ保持にする。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `configurationService.ts`、`automodeService.ts`。 **次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [Document Copilot Chat OpenTelemetry integration with LangSmith backend](https://github.com/microsoft/vscode/issues/317326) `#317326`
**ステータス:** Open | **概要:** Copilot Chat の OTel 連携ドキュメントに LangSmith の記載がないため追加してほしいという要望。 **直近のアクティビティ:** `emil-lc` が LangSmith 側の連携ドキュメントへのリンクをコメント。 **次のアクション:** ドキュメント追記。

---

### ![Issue](icons/issue-opened.svg) [Symbols of some extensions in VS Code left side bar disappeared](https://github.com/microsoft/vscode/issues/320763) `#320763`
**ステータス:** Closed | **概要:** 一部拡張のアクティビティバーアイコンが見えなくなる回帰報告。 **直近のアクティビティ:** `tfrank1971` が修正を確認、`gjsjohnmurray` らが 1.124 への更新を案内。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [`Checking for Markdown links to update` for every md files edit](https://github.com/microsoft/vscode/issues/320287) `#320287`
**ステータス:** Closed | **ラベル:** info-needed | **概要:** md 編集ごとにリンク更新チェックが走るという報告。 **直近のアクティビティ:** `M-Kepler` が foam プラグイン起因と判明した旨をコメント。 **次のアクション:** クローズ済み（プラグイン起因）。

---

### ![PR](icons/git-pull-request.svg) [Azure BYOK: support configurable Entra auth provider and scopes](https://github.com/microsoft/vscode/pull/318118) `#318118`
**ステータス:** Open | **概要:** Azure BYOK で Entra 認証プロバイダとスコープを設定可能にし、sovereign cloud/APIM/B2B 向けスコープに対応する。 **直近のアクティビティ:** `pbiasotti` が `TylerLeonhardt` にレビュー・マージを依頼。 **変更ファイル:** `azureProvider.ts`、`customEndpointProvider.ts` ほか 6 ファイル。 **次のアクション:** レビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Agents window: couldn't disable built-in Github mcp server in agents customization](https://github.com/microsoft/vscode/issues/318576) `#318576`
**ステータス:** Open | **概要:** Agents カスタマイズで組み込み GitHub MCP サーバを右クリックしても無効化できない。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** コンテキストメニュー追加の検討。

---

### ![Issue](icons/issue-opened.svg) [VSCode won't let me rename a variable](https://github.com/microsoft/vscode/issues/319414) `#319414`
**ステータス:** Open | **ラベル:** bug, rename | **概要:** F2 で rename しようとするとポップアップが出て rename が取り消される。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** #320775 で修正中。

---

### ![Issue](icons/issue-opened.svg) [Creating a new session in Agents window should inherit current session's workspace](https://github.com/microsoft/vscode/issues/320774) `#320774`
**ステータス:** Open | **ラベル:** feature-request, agents-window | **概要:** `cmd+n` で新規セッションを開く際、現在のセッションのワークスペースを引き継いでほしいという要望。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 仕様検討。

---

### ![Issue](icons/issue-opened.svg) [Visual Studio Code 窗口意外终止("crashed", -2147483645)](https://github.com/microsoft/vscode/issues/261779) `#261779`
**ステータス:** Open | **ラベル:** bug, ai-translated | **概要:** VS Code ウィンドウが crashed で予期せず終了する（中国語報告）。 **直近のアクティビティ:** `justspoicy` が Win11 タッチキーボード（TabTip.exe）の回避策が有効だったとコメント。 **次のアクション:** 再現確認。

---

### ![Issue](icons/issue-opened.svg) [MCP servers for custom agents from copilot CLI don't start](https://github.com/microsoft/vscode/issues/318589) `#318589`
**ステータス:** Open | **概要:** Copilot CLI のカスタムエージェント用 MCP サーバが起動しない。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 再現確認。

---

### ![PR](icons/git-pull-request.svg) [Fix raw markdown shown in image carousel caption](https://github.com/microsoft/vscode/pull/320754) `#320754`
**ステータス:** Open | **概要:** 画像カルーセルのキャプションに生 markdown（`Viewed image [](file://...)`）が表示される問題を、プレーンテキストとして抽出するよう修正する（#307446 を修正）。 **直近のアクティビティ:** `stasyu2009-ux` がウクライナ語で問題を主張、`arun-357` が再現できず詳細を依頼するやり取り計 6 件。 **変更ファイル:** `chatImageCarouselService.ts`、`chatImageExtraction.ts` とそのテスト。 **次のアクション:** 報告者からの具体的な問題提示待ち。

---

### ![Issue](icons/issue-opened.svg) [Meta: Usage based billing model](https://github.com/microsoft/vscode/issues/319403) `#319403`
**ステータス:** Open | **ラベル:** chat-billing | **概要:** 2026-06-01 開始の従量課金体験に関するトラッキング issue。 **直近のアクティビティ:** `Piesikot` が、従来は使い切らなかったクレジットが急に消費される旨をコメント。 **次のアクション:** 課金関連は GitHub Support 案内。

---

### ![Issue](icons/issue-opened.svg) [Copilot System prompt always trips Azure OpenAI Jailbreak filter](https://github.com/microsoft/vscode/issues/320236) `#320236`
**ステータス:** Open | **概要:** Azure OpenAI BYOK 利用時、Copilot のシステムプロンプトが Jailbreak フィルタを頻繁に誤検知して拒否される。 **直近のアクティビティ:** `twsl` が plan モードで特に顕著と同様の報告。 **次のアクション:** プロンプト調整の検討。

---

### ![Issue](icons/issue-opened.svg) [Agents: Removing and reinstalling a marketplace does not update it](https://github.com/microsoft/vscode/issues/319562) `#319562`
**ステータス:** Open | **概要:** プラグインマーケットプレイスのカタログが自動更新されず、手動でクローン済みリポジトリを削除するしかない。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** カタログ更新機構の追加。

---

### ![PR](icons/git-pull-request.svg) [fix padding of input part in sessions window](https://github.com/microsoft/vscode/pull/320769) `#320769`
**ステータス:** Open | **概要:** Sessions window の入力部のパディングを修正する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `chatInputPart.ts`。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix: exit process cleanly on fatal startup errors](https://github.com/microsoft/vscode/pull/320767) `#320767`
**ステータス:** Open | **概要:** 起動時の致命的エラーで Electron がハングするのを防ぐため、`onReady()` の catch に `app.exit(1)` を追加する（#320760 を修正）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]`・`SprTybi` がレビューコメント。 **変更ファイル:** `src/main.ts`。 **次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [VS Code native crash on Mac with error 5](https://github.com/microsoft/vscode/issues/267703) `#267703`
**ステータス:** Open | **ラベル:** bug | **概要:** 大量のファイル作成/削除中に Mac で「error 5」のクラッシュが発生する。 **直近のアクティビティ:** `ulut0002` が `atsutil` リセットや `--disable-gpu` 等の回避策をコメント。 **次のアクション:** クラッシュ原因の調査。

---

### ![Issue](icons/issue-opened.svg) [Support BYOK/Custom Models for Inline Code Completions](https://github.com/microsoft/vscode/issues/318545) `#318545`
**ステータス:** Open | **概要:** インラインコード補完で BYOK/カスタムモデルをサポートしてほしい（chat では可能だが補完では無視される）。 **直近のアクティビティ:** `hu3bi`・`Arifursdev` が #313468/#309009 との重複を指摘。 **次のアクション:** 重複 issue の整理と機能検討。

---

### ![Issue](icons/issue-opened.svg) [Unable to use certain fonts in the editor (Consolas) since the last update](https://github.com/microsoft/vscode/issues/320369) `#320369`
**ステータス:** Open | **ラベル:** linux, font-rendering, under-discussion | **概要:** 最終更新以降、Linux のエディタで Consolas 等のフォントが使えなくなった。 **直近のアクティビティ:** `HolyBlackCat` がファイル提供済みで追加情報の要否を確認。 **次のアクション:** フォントレンダリングの調査。

---

### ![Issue](icons/issue-opened.svg) [Add a conversation-level cache hit rate to cache explorer, remove utility models](https://github.com/microsoft/vscode/issues/320765) `#320765`
**ステータス:** Open | **概要:** Cache Explorer で課金対象外の utility model を非表示にし、会話全体のキャッシュヒット率を表示する 2 つの改善提案。 **直近のアクティビティ:** `kevin-m-kent` が `cwebster-99` の類似 issue #320137 に言及しコメント。 **次のアクション:** 改善対応。

---

### ![PR](icons/git-pull-request.svg) [fix(diffEditor): reset wordWrapOverride2 when switching from inline to side-by-side view](https://github.com/microsoft/vscode/pull/320761) `#320761`
**ステータス:** Open | **ラベル:** triage-needed | **概要:** inline→side-by-side 切替時に `wordWrapOverride2` がリセットされず word wrap が抑制され続ける問題を、`inherit` にリセットして修正する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]`・`andreyfel` がレビューコメント。 **変更ファイル:** `diffEditorEditors.ts`。 **次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [1.124.0 Endgame](https://github.com/microsoft/vscode/issues/320355) `#320355`
**ステータス:** Closed | **ラベル:** endgame-plan | **概要:** 1.124.0 のエンドゲーム計画 issue。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** クローズ済み。

---

### ![PR](icons/git-pull-request.svg) [fix: persist SCM repository visibility state across restarts](https://github.com/microsoft/vscode/pull/301029) `#301029`
**ステータス:** Open | **概要:** `isHidden` リポジトリ（Copilot worktree 等）が再起動時の表示復元を壊す問題を修正する（#301028 を修正）。 **直近のアクティビティ:** `aruizdesamaniego-sh`・`copilot-pull-request-reviewer[bot]` のレビューコメント計 14 件。 **変更ファイル:** `scmViewService.ts` ほか 9 ファイル。 **次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [Keyboard focus skips Quick Start section and re-enters unexpectedly](https://github.com/microsoft/vscode/issues/314974) `#314974`
**ステータス:** Open | **ラベル:** accessibility-sla | **概要:** Quick Start セクションのキーボードフォーカス順が不整合になる A11y バグ。 **直近のアクティビティ:** `meganrogge` が再現用拡張の例をコメント。 **次のアクション:** フォーカス順の修正。

---

### ![Issue](icons/issue-opened.svg) [Up arrow doesn't retrieve chat history on new session screen of Agents window](https://github.com/microsoft/vscode/issues/320691) `#320691`
**ステータス:** Closed | **ラベル:** bug, agents-window | **概要:** Agents window の新規セッション画面で上矢印が過去のプロンプトを呼び出さない。 **直近のアクティビティ:** `benibenj` が、履歴ロジックの変更で壊れたが修正を push 済みとコメント。 **次のアクション:** クローズ済み（修正済み）。

---

### ![PR](icons/git-pull-request.svg) [Fix image carousel showing UUID on hover in modal editor title](https://github.com/microsoft/vscode/pull/320739) `#320739`
**ステータス:** Open | **概要:** 画像カルーセルのモーダルタイトルにホバーすると生 UUID が表示される問題を、`title` オプションを渡して修正する（#301984 を修正）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]`・`arun-357` のレビューコメント。 **変更ファイル:** `modalEditorPart.ts`。 **次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [Background terminal completion notification loses originating agent](https://github.com/microsoft/vscode/issues/320611) `#320611`
**ステータス:** Closed | **ラベル:** bug, candidate | **概要:** バックグラウンド実行した `run_in_terminal` の完了通知が、どのチャットセッション由来か示さず会話が stall する。 **直近のアクティビティ:** `ulugbekna` が 1.124 のやり残しとして On Deck に移動とコメント。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Risk assessment should not show for `Uncommitted Changes` prompt](https://github.com/microsoft/vscode/issues/320766) `#320766`
**ステータス:** Open | **概要:** worktree でプロンプト送信時の「Uncommitted changes」プロンプトに、AI フローでないのにリスク評価が表示される。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 表示条件の修正。

---

### ![Issue](icons/issue-opened.svg) [Source Control (Git) just hangs and shows reload icon](https://github.com/microsoft/vscode/issues/320758) `#320758`
**ステータス:** Open | **概要:** Git プロジェクトを開くと Source Control がリロードアイコンのまま何も起きない。 **直近のアクティビティ:** `stumbaumr` が 1.124.0 でも再現と報告、`vs-code-engineering[bot]` が更新を案内。 **次のアクション:** ハング原因の調査。

---

### ![Issue](icons/issue-opened.svg) [Add ability to extend from other settings files](https://github.com/microsoft/vscode/issues/15909) `#15909`
**ステータス:** Open | **ラベル:** feature-request, config | **概要:** `.vscode/settings.json` で他の設定ファイルを `extends` できるようにしてほしいという長年の要望。 **直近のアクティビティ:** `Wiktorro`・`LukasWillin` がチーム共有設定での必要性を計 2 件コメント。 **次のアクション:** 機能検討。

---

### ![PR](icons/git-pull-request.svg) [version-control: respect push remotes with 'sync changes'](https://github.com/microsoft/vscode/pull/293100) `#293100`
**ステータス:** Open | **概要:** `remote.pushDefault` と upstream を分けている場合に「Sync changes」が origin/main へ直接 push してしまう危険な挙動を、push remote を尊重するよう修正する。 **直近のアクティビティ:** `HaraldNordgren` がレビュー依頼と重要性を計 5 件コメント、`copilot-pull-request-reviewer[bot]` もレビュー。 **変更ファイル:** `extensions/git/` 配下 8 ファイル。 **次のアクション:** メンテナのレビュー待ち。

---

### ![Issue](icons/issue-opened.svg) [Agent Picker is not pre-selected when creating a new Agent Host session](https://github.com/microsoft/vscode/issues/317944) `#317944`
**ステータス:** Closed | **ラベル:** bug, agent-host | **概要:** 新規 Agent Host セッション作成時にエージェントピッカーが事前選択されない回帰。 **直近のアクティビティ:** `aeschli` が #320609 の回帰で #320762/#320743 で修正済みとコメント、`ulugbekna` が On Deck 移動を報告。 **次のアクション:** クローズ済み（修正済み）。

---

### ![PR](icons/git-pull-request.svg) [Do not clear agent when switching from initial to committed session](https://github.com/microsoft/vscode/pull/320762) `#320762`
**ステータス:** Open | **概要:** 暫定（未保存）セッションが確定セッションに置き換わる際にエージェント選択が失われる回帰を修正する（#317944 を修正）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `baseAgentHostSessionsProvider.ts`。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Preserve image source provenance across chat reference API boundary](https://github.com/microsoft/vscode/pull/320624) `#320624`
**ステータス:** Merged | **ラベル:** triage-needed | **概要:** 画像添付が ChatPromptReference に変換される際 source provenance（clipboard/screenshot/file/url）を失う問題を、`isPasted`/`isURL` を持たせて修正する。 **直近のアクティビティ:** `merfanian` が CLA 同意、`federicobrancasi`・`alexr00` が APPROVED。 **変更ファイル:** `extHostTypeConverters.ts`、`extHostTypes.ts` ほか。 **次のアクション:** マージ済みのため対応不要。

---

### ![Issue](icons/issue-opened.svg) [Custom SVG icons in tree view do not respect theme foreground color](https://github.com/microsoft/vscode/issues/311339) `#311339`
**ステータス:** Open | **ラベル:** bug, tree-views | **概要:** ツリービューのカスタム SVG アイコンがテーマの前景色を尊重せず黒で描画される。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** #320391 で関連修正中。

---

### ![PR](icons/git-pull-request.svg) [Reinstate colors of symbol codicons when used in custom tree views](https://github.com/microsoft/vscode/pull/320391) `#320391`
**ステータス:** Open | **概要:** カスタムツリービューでシンボル codicon の色が失われる問題を修正する（#299479 を修正、#267787 起因）。 **直近のアクティビティ:** `n-gist` が #311339 への影響可能性を問い、`gjsjohnmurray` がレビューコメント。 **変更ファイル:** `treeView.ts`。 **次のアクション:** レビュー対応。

---

### ![Issue](icons/issue-opened.svg) [Scrollbar color contrast is less than 3:1 in Light 2026 and Dark 2026 themes](https://github.com/microsoft/vscode/issues/318703) `#318703`
**ステータス:** Open | **ラベル:** bug, accessibility, accessibility-sla | **概要:** Light/Dark 2026 テーマでスクロールバーのコントラスト比が 3:1 未満（WCAG 1.4.11）という A11y バグ。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** コントラスト改善。

---

### ![Issue](icons/issue-opened.svg) [Focus is lost after interacting with "Auto Update" checkbox using keyboard](https://github.com/microsoft/vscode/issues/318700) `#318700`
**ステータス:** Open | **ラベル:** accessibility | **概要:** 拡張機能の「Auto Update」チェックボックスをキーボード操作後にフォーカスが失われる A11y バグ（WCAG 2.4.3）。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** フォーカス管理の修正。

---

### ![Issue](icons/issue-opened.svg) [The Expand/Collapse control does not respond to a single number voice command](https://github.com/microsoft/vscode/issues/318719) `#318719`
**ステータス:** Open | **ラベル:** accessibility, confirmation-pending | **概要:** 展開/折りたたみコントロールが単一数字の音声コマンドに反応しない A11y バグ。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** 音声コマンド対応の確認。

---

### ![Issue](icons/issue-opened.svg) [Keyboard navigation is blocked between "Resources" and "Views and more actions" using VO navigation](https://github.com/microsoft/vscode/issues/320381) `#320381`
**ステータス:** Closed | **ラベル:** accessibility-sla | **概要:** VoiceOver ナビゲーションで「Resources」と「Views and more actions」間の移動がブロックされる A11y バグ。 **直近のアクティビティ:** `kapilvaishna` が VS Code 側の問題と調査結果を、`meganrogge` がキーボードでは到達可能なため VoiceOver 固有としてクローズを提案。 **次のアクション:** クローズ済み（キーボードでは到達可能）。

---

### ![Issue](icons/issue-opened.svg) [Toolbar actions inside tree grid item are not discoverable for screen reader users](https://github.com/microsoft/vscode/issues/320750) `#320750`
**ステータス:** Open | **ラベル:** new release | **概要:** ツリーグリッド項目内のツールバーアクションがスクリーンリーダーで発見できず、プログラム的に関連付けられていない A11y バグ。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** ARIA 関連付けの追加。

---

### ![Issue](icons/issue-opened.svg) [Copilot chat makes vscode unresponsive](https://github.com/microsoft/vscode/issues/281743) `#281743`
**ステータス:** Open | **ラベル:** bug, freeze-slow-crash-leak, chat-agent | **概要:** Linux/KDE で Copilot chat 使用中に VS Code が応答しなくなる。 **直近のアクティビティ:** `Rowdster` が「もう発生しなくなった」とコメント。 **次のアクション:** 再現の有無を確認。

---

### ![Issue](icons/issue-opened.svg) [Terminal Font Ligatures stopped working in version 1.108](https://github.com/microsoft/vscode/issues/286742) `#286742`
**ステータス:** Open | **概要:** 1.108 以降でターミナルのフォントリガチャが動かなくなった回帰。 **直近のアクティビティ:** `v-amorim` が 1.107.1→1.108.0 で回帰したと 10 件超の報告に言及してコメント。 **次のアクション:** #318992 で関連修正中。

---

### ![Issue](icons/issue-opened.svg) [Model keeps on changing to Auto](https://github.com/microsoft/vscode/issues/317454) `#317454`
**ステータス:** Closed | **ラベル:** info-needed | **概要:** 特定の Copilot モデルを選んでも毎回 Auto に戻ってしまう。 **直近のアクティビティ:** `vs-code-engineering[bot]` が info-needed のまま 7 日経過したためクローズとコメント。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Copilot Chat - Won't stay in session tries to re-create](https://github.com/microsoft/vscode/issues/319162) `#319162`
**ステータス:** Closed | **ラベル:** info-needed | **概要:** Copilot Chat がセッションに留まらず再作成しようとする。 **直近のアクティビティ:** `vs-code-engineering[bot]` が info-needed のままクローズとコメント。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [Agents: Editing prior message scrolls back to top of the session](https://github.com/microsoft/vscode/issues/319239) `#319239`
**ステータス:** Closed | **ラベル:** info-needed, agents-window | **概要:** 直前メッセージを編集するとセッションが最上部までスクロールしてしまう。 **直近のアクティビティ:** `vs-code-engineering[bot]` が info-needed のままクローズとコメント。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [无法执行任务](https://github.com/microsoft/vscode/issues/319568) `#319568`
**ステータス:** Closed | **ラベル:** info-needed | **概要:** VS Code 更新後にエージェントタスクが実行できないという中国語報告。 **直近のアクティビティ:** `vs-code-engineering[bot]` が info-needed のままクローズとコメント。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [code is not visible given by claude](https://github.com/microsoft/vscode/issues/319575) `#319575`
**ステータス:** Closed | **ラベル:** info-needed | **概要:** IDE でコードを書く際に表示されないという報告。 **直近のアクティビティ:** `vs-code-engineering[bot]` が info-needed のままクローズとコメント。 **次のアクション:** クローズ済み。

---

### ![Issue](icons/issue-opened.svg) [code is not visible given by claude](https://github.com/microsoft/vscode/issues/319576) `#319576`
**ステータス:** Closed | **ラベル:** info-needed | **概要:** #319575 と同内容の重複報告。 **直近のアクティビティ:** `vs-code-engineering[bot]` が info-needed のままクローズとコメント。 **次のアクション:** クローズ済み（重複）。

---

### ![PR](icons/git-pull-request.svg) [[Fix] Implement platform specific URL format](https://github.com/microsoft/vscode/pull/238890) `#238890`
**ステータス:** Closed | **概要:** マルチプラットフォーム拡張のビルドでプラットフォーム固有 URL 形式を実装する（#238886 を修正）。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **変更ファイル:** `build/lib/extensions.js`、`extensions.ts`。 **次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add a second turn to the chat smoke tests](https://github.com/microsoft/vscode/pull/319942) `#319942`
**ステータス:** Open（Draft）| **概要:** チャットの smoke テストに 2 ターン目を追加し自動化レイヤーを強化する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `agentsWindow.ts`、`chat.ts`、`chatSessions.test.ts` ほか。 **次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Agents window: Remove trailing separator from panel part configuration](https://github.com/microsoft/vscode/pull/320748) `#320748`
**ステータス:** Merged | **概要:** パネル部設定の末尾セパレータを除去しレイアウト一貫性を高める（#317695 に対応）。 **直近のアクティビティ:** `vs-code-engineering[bot]` の CODENOTIFY、`benibenj` が APPROVED。 **変更ファイル:** `layoutActions.ts`、`panelPart.ts`。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Improve the local to native and remote to local copy, paste, and DND experience](https://github.com/microsoft/vscode/pull/320685) `#320685`
**ステータス:** Open | **概要:** ローカル/リモート間のコピー・ペースト・ドラッグ&ドロップ体験を改善する（vscode-remote-release#2008 を修正）。+688 行。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]`・`mjbvz` がレビューコメント。 **変更ファイル:** `remoteFileSystemProxy*.ts`、`dnd.ts`、`clipboardService.ts` ほか 10 ファイル。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Consolidate authentication provider session options](https://github.com/microsoft/vscode/pull/320738) `#320738`
**ステータス:** Open | **概要:** `AuthenticationProviderSessionOptions` と `AuthenticationGetSessionOptions` の共通プロパティを新 interface に抽出して重複を削減する。 **直近のアクティビティ:** `microsoft-github-policy-service[bot]` が CLA 同意を要求。`copilot-pull-request-reviewer[bot]` はレビュー不可。 **変更ファイル:** `vscode.proposed.authIssuers.d.ts`。 **次のアクション:** CLA 同意待ち。

---

### ![PR](icons/git-pull-request.svg) [refactor: unify glob pattern types in search API](https://github.com/microsoft/vscode/pull/320740) `#320740`
**ステータス:** Open | **ラベル:** triage-needed | **概要:** text search API の `GlobString` エイリアスを廃止し `GlobPattern` に統一する（既存 TODO の実装、後方互換）。 **直近のアクティビティ:** `microsoft-github-policy-service[bot]` が CLA 同意を計 2 件要求。 **変更ファイル:** `vscode.proposed.textSearchProvider.d.ts`。 **次のアクション:** CLA 同意待ち。

---

### ![PR](icons/git-pull-request.svg) [Enhance isAgentSessionsWorkspace documentation](https://github.com/microsoft/vscode/pull/320736) `#320736`
**ステータス:** Open | **ラベル:** triage-needed | **概要:** `isAgentSessionsWorkspace` の説明を明確化し、エージェントワークスペースの具体例を追記する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` はレビュー不可。 **変更ファイル:** `vscode.proposed.agentSessionsWorkspace.d.ts`。 **次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Refactor chat action icons for consistency](https://github.com/microsoft/vscode/pull/320752) `#320752`
**ステータス:** Open | **概要:** チャットアクションのアイコンを `arrowUp` から `newLine` に変更して一貫性を高める（#314829 に対応）。 **直近のアクティビティ:** `github-actions[bot]` がスクリーンショット差分（42 件変化）を報告。 **変更ファイル:** `chatExecuteActions.ts`、`chatQueueActions.ts` ほか 7 ファイル。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Add local agent host sandbox test](https://github.com/microsoft/vscode/pull/319590) `#319590`
**ステータス:** Open（Draft）| **概要:** ローカル Agent Host（レンダラ側拡張ではなく）を使う Agents Window の smoke テストスイートを追加する。+1382 行。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `agentService.ts`、`sandboxConfigForSdk.ts` ほか 15 ファイル。 **次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Fixes builtin markdown editor paths](https://github.com/microsoft/vscode/pull/320753) `#320753`
**ステータス:** Open | **概要:** 実験的な組み込み markdown カスタムエディタの webview アセットパスを現在のビルド出力に合わせる。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `markdownEditorProvider.ts`、`esbuild.markdownEditor.mts` ほか。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix setting active session when new session is submitted](https://github.com/microsoft/vscode/pull/320743) `#320743`
**ステータス:** Merged | **概要:** 新規セッション送信時に Sessions グリッドの表示と `activeSession` の不一致を修正する。 **直近のアクティビティ:** `aeschli` が APPROVED、`copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `sessionsViewService.ts`、`sessionsManagementService.ts` ほか。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix(terminal): track ligatures addon config for change detection](https://github.com/microsoft/vscode/pull/318992) `#318992`
**ステータス:** Open | **概要:** ターミナルのリガチャ addon 設定の変更検知を、`enabled` を含む全体ではなく `ILigatureOptions` の形で比較するよう修正する（#318993 を修正）。 **直近のアクティビティ:** `anthonykim1`・`dmitrivMS` が APPROVED。 **変更ファイル:** `xtermTerminal.ts`。 **次のアクション:** マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Add chat quota trajectory nudge](https://github.com/microsoft/vscode/pull/320683) `#320683`
**ステータス:** Open（Draft）| **概要:** 月間使用率 10-35% かつ日次 3.5%以上の予測時に、対象 EDU/Pro/ProPlus ユーザーへクォータ軌道の nudge を `chatQuotaTrajectoryNudge` treatment の背後で表示する。 **直近のアクティビティ:** `rfeltis` が CLA 同意。`copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `chatQuotaNotification.ts` ほか 5 ファイル。 **次のアクション:** Draft の完成。

---

### ![PR](icons/git-pull-request.svg) [Fix workbench hover not scrolling when content overflows](https://github.com/microsoft/vscode/pull/316992) `#316992`
**ステータス:** Open | **概要:** ワークベンチのホバーがオーバーフロー時にスクロールバーなしで内容を切り捨てる問題（SCM Commit Graph のホバーで顕著）を、スクロールビューポートに高さ制約を与えて修正する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `hoverWidget.ts` とそのテスト。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [speed up vscode work tree flows](https://github.com/microsoft/vscode/pull/320729) `#320729`
**ステータス:** Merged | **概要:** worktree 作成時の install+watch 自動化と、新規 worktree にコピーするファイルを調整して開発フローを高速化する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` のレビュー、`alexr00` が APPROVED。 **変更ファイル:** `.vscode/settings.json`、`tasks.json`、`package.json`。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Fixed typo in cli update](https://github.com/microsoft/vscode/pull/245751) `#245751`
**ステータス:** Merged | **概要:** CLI の self-update コマンド出力のタイプミスを修正する。 **直近のアクティビティ:** `dmitrivMS`・`alexr00` が APPROVED、`copilot-pull-request-reviewer[bot]` がレビュー。 **変更ファイル:** `cli/src/commands/update.rs`。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Remove redundant session rename action](https://github.com/microsoft/vscode/pull/320728) `#320728`
**ステータス:** Merged | **概要:** セッションビューアクションの重複した rename アクションを除去する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` のレビュー、`alexr00` が APPROVED。 **変更ファイル:** `sessionsViewActions.ts`。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [No changes - problem statement missing requirements](https://github.com/microsoft/vscode/pull/291550) `#291550`
**ステータス:** Closed（Draft）| **概要:** Copilot coding agent が「hello」のみの曖昧な指示で作成したため変更なしの PR。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add .mpp and .mxx extensions to C++](https://github.com/microsoft/vscode/pull/286872) `#286872`
**ステータス:** Open | **概要:** C++20 モジュールファイルの一般的な拡張子 `.mpp`/`.mxx`（xmake/build2 のデフォルト）を C++ 言語定義に追加する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `extensions/cpp/package.json`。 **次のアクション:** レビュー待ち。

---

### ![PR](icons/git-pull-request.svg) [Revert ":lipstick:"](https://github.com/microsoft/vscode/pull/234492) `#234492`
**ステータス:** Closed | **概要:** #234417 を revert する PR。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **変更ファイル:** `sign.js`、`sign.ts`。 **次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Update LICENSE.txt](https://github.com/microsoft/vscode/pull/238110) `#238110`
**ステータス:** Closed | **概要:** LICENSE.txt の更新 PR（変更行なし）。 **直近のアクティビティ:** 直近 24 時間のコメントなし。 **次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [fix: Clone from Github doesn't rank org repos highly](https://github.com/microsoft/vscode/pull/320528) `#320528`
**ステータス:** Open | **概要:** `getUserRemoteSources` がクエリ有無に関わらず全 user/org リポジトリ（最大 100 件）を返すため、クエリに一致しない repo が上位に出る問題を修正する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `remoteSourceProvider.ts`。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix: Windows: Error auto-updating x64 version on ARM64](https://github.com/microsoft/vscode/pull/320545) `#320545`
**ステータス:** Open | **概要:** ARM64 Windows 上で x64 版 VS Code が x64 エミュレーションを検出できず誤って自動更新に失敗する問題を、`isRunningUnderARM64Translation()` を修正して解消する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **変更ファイル:** `nativeHostMainService.ts`、`updateService.win32.ts` ほか 5 ファイル。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [agentHost: complete skills after whitespace](https://github.com/microsoft/vscode/pull/320721) `#320721`
**ステータス:** Merged | **概要:** ユーザーメッセージ内で空白の後に来る `/` トークンでも skill のスラッシュ補完を出せるようにする（スラッシュコマンドは行頭限定を維持）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` のレビュー、`dmitrivMS` が APPROVED。 **変更ファイル:** `agentHostSkillCompletionProvider.ts` ほか。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [refactor: streamline agent host completion variable handling and improve attachment rendering](https://github.com/microsoft/vscode/pull/320717) `#320717`
**ステータス:** Merged | **概要:** Agent Host の補完参照（skills/commands）のチャット変数表現を整理し、添付レンダリングを調整する。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` のレビュー、`dmitrivMS` が APPROVED。 **変更ファイル:** `agentHostInputCompletions.ts`、`chatAttachmentsContentPart.ts` ほか 7 ファイル。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Add "Load more" paging for large folders in the Explorer](https://github.com/microsoft/vscode/pull/320536) `#320536`
**ステータス:** Open | **概要:** 巨大フォルダ展開時に Explorer がフリーズする問題に対し、`explorer.fileLimit`（既定 5000）を超えると先頭ページと「Load more」を表示するページングを追加する（#281507 を修正）。 **直近のアクティビティ:** `warptengood` がスクリーンショットとレビューコメント計 5 件超。 **変更ファイル:** `explorerView.ts`、`explorerModel.ts` ほか 8 ファイル。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Return a deep cloned VSBuffer when it might be detached and then accessed again](https://github.com/microsoft/vscode/pull/320190) `#320190`
**ステータス:** Open | **概要:** `ChunkStream._read()` の返す VSBuffer が iframe へ転送され backing storage が detach されると次回アクセスでエラーになる問題を、転送されうる場合にディープクローンを返して修正する（#316841 を修正）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]`・`er91` のレビューコメント計 4 件。 **変更ファイル:** `buffer.ts`、`ipc.net.ts`、`ipc.ts`。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Feat/add private vsx registry](https://github.com/microsoft/vscode/pull/313170) `#313170`
**ステータス:** Closed | **概要:** プライベート vsx レジストリを追加するフォークカスタマイズ PR（+12082 行、95 ファイル）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がクォータ超過でレビュー不可とコメント。 **次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [[CLOSED - MERGED INTERNALLY]](https://github.com/microsoft/vscode/pull/317894) `#317894`
**ステータス:** Closed | **概要:** TSCode/TestAgent 向けの VS Code フォークカスタマイズ PR（+12289 行、100 ファイル）。内部マージ済みとしてクローズ。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` がレビューコメント。 **次のアクション:** クローズ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [Block auto-approve for tilde and env-var expansions in file write paths](https://github.com/microsoft/vscode/pull/320676) `#320676`
**ステータス:** Merged | **概要:** `~/foo` や `%APPDATA%\foo` がワークスペース内と誤判定され auto-approve されてしまう問題を、変数展開ガードに `~`/`%` を含めて修正する（#320660 を修正、1.123 へ cherry-pick）。 **直近のアクティビティ:** `vs-code-engineering[bot]` の CODENOTIFY、`dileepyavan` が APPROVED。 **変更ファイル:** `commandLineFileWriteAnalyzer.ts` とそのテスト。 **次のアクション:** マージ済みのため対応不要。

---

### ![PR](icons/git-pull-request.svg) [agentHost: fix client tool auto-approval](https://github.com/microsoft/vscode/pull/320712) `#320712`
**ステータス:** Open | **概要:** Agent Host の bypass/autopilot 承認時にクライアント所有ツール（Playwright 等）が自動承認されるよう、ツール呼び出しメタデータを保持し `ToolConfirmKind.Setting` で自動確認する（#314149 を修正）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` のレビュー、`DonJayamanne` が APPROVED。 **変更ファイル:** `agentSideEffects.ts`、`sessionPermissions.ts` ほか 7 ファイル。 **次のアクション:** マージ待ち。

---

### ![PR](icons/git-pull-request.svg) [Validate redirect scheme and strip credentials on cross-origin redirects in MCP HTTP client](https://github.com/microsoft/vscode/pull/320347) `#320347`
**ステータス:** Open | **ラベル:** triage-needed | **概要:** MCP HTTP クライアントがリダイレクトの scheme を検証せず認証情報も剥がさないため、`unix://`/`pipe://` でローカルソケット（Docker daemon 等）にアクセスしうる脆弱性を修正する。 **直近のアクティビティ:** `g0w6y`・`connor4312`・`dmitrivMS` のレビューコメント計 7 件。 **変更ファイル:** `extHostMcp.ts`。 **次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [fix: use win.getBounds() instead of getPosition()/getSize() to prevent window drift on Linux](https://github.com/microsoft/vscode/pull/315493) `#315493`
**ステータス:** Open | **概要:** GNOME/X11 で `win.getPosition()` がコンテンツ領域座標を返しウィンドウが再起動ごとに下方へ drift する問題を、`win.getBounds()` を使って修正する（#117180 を修正）。 **直近のアクティビティ:** `harshagarwalnyu` が、上流 Electron 修正待ちの間 VS Code 側回避策が許容されるか `deepak1556` に確認。`copilot-pull-request-reviewer[bot]` もレビュー。 **変更ファイル:** `windowImpl.ts`。 **次のアクション:** 方針確認。

---

### ![PR](icons/git-pull-request.svg) [Make VSBuffer#slice return an owned buffer (fixes #316841)](https://github.com/microsoft/vscode/pull/316842) `#316842`
**ステータス:** Closed（Draft）| **概要:** Safari/iOS で webview が空白になる回帰を、`VSBuffer#slice` が view ではなく所有バッファを返すよう変更して修正する（#316841 を修正）。 **直近のアクティビティ:** `er91` が転送箇所を直す #320190 のレビューを依頼。`dop-amine` もコメント。 **次のアクション:** クローズ済み（#320190 に引き継ぎ）。

---

### ![PR](icons/git-pull-request.svg) [agentHost: describe empty attachment tool results](https://github.com/microsoft/vscode/pull/320707) `#320707`
**ステータス:** Merged | **概要:** クライアントツール結果がテキストを持たず埋め込みリソースのみの場合、`<empty />` でなく「Tool produced the attached image/file」等と説明する（#320678 のフォローアップ）。 **直近のアクティビティ:** `copilot-pull-request-reviewer[bot]` のレビュー、`DonJayamanne` が APPROVED。 **変更ファイル:** `copilotAgentSession.ts` とそのテスト。 **次のアクション:** マージ済みのため対応不要。

---

## qmk/qmk_firmware

> キーボード追加・ビルド修正に加え、Plover HID プロトコルや XAP といった breaking change を含む大型機能 PR が継続レビュー中。`drashna`・`zvecr` らメンテナがレビューを進めている。

### ![PR](icons/git-pull-request.svg) [Float65 Keyboard](https://github.com/qmk/qmk_firmware/pull/26197) `#26197`
**ステータス:** Open | **ラベル:** keyboard, keymap

**概要:** float65 キーボードを追加する（config.h、keyboard.json、keymap など 6 ファイル、+464 行）。

**直近のアクティビティ:** `drashna` のレビューコメント。

**変更ファイル:** `keyboards/yiancardesigns/float65/` 配下 6 ファイル。

**次のアクション:** レビュー対応。

---

### ![PR](icons/git-pull-request.svg) [Custom plover HID report type](https://github.com/qmk/qmk_firmware/pull/26018) `#26018`
**ステータス:** Open | **ラベル:** core, keyboard, keymap, documentation, awaiting review, python, cli, CI, no-ci, dd, breaking_changes_2026q3

**概要:** Plover v5.1.0 が追加したカスタム HID プロトコルに対応する。COM ベースの Gemini/Bolt より少ないインターフェースで済み、複合インターフェース化でさらに削減できる可能性がある。+606 行。

**直近のアクティビティ:** `drashna` が「マージには 2 つの承認が必要で、それを待っている状態」とコメント。`GCRev` のレビューコメントと `drashna` の APPROVED など計 12 件。

**変更ファイル:** `process_plover_hid.c`、`plover_hid.h`、`usb_main.c`、`stenography.md` ほか 18 ファイル。

**次のアクション:** もう 1 件の承認を待つ。

---

### ![PR](icons/git-pull-request.svg) [[WIP] XAP -- eXtensible Application Protocol](https://github.com/qmk/qmk_firmware/pull/13733) `#13733`
**ステータス:** Open（Draft）| **ラベル:** core, keyboard, keymap, documentation, breaking_change, python, cli, feature, CI ほか

**概要:** XAP（#11567）の実装を追跡する作業中の大型 PR（+7548 行、96 ファイル）。スキーマ・テンプレート・クライアント生成などを含む。

**直近のアクティビティ:** `t-8ch`・`tzarc` のレビューコメントと `jock070` の APPROVED（計 3 件）。

**変更ファイル:** `xap.jsonschema`、`xap_defaults.json`、各種テンプレートほか 96 ファイル。

**次のアクション:** WIP の継続。

---

### ![Issue](icons/issue-opened.svg) [[Bug] audio.pins in keyboard.json is not converted to C defines during build](https://github.com/qmk/qmk_firmware/issues/26106) `#26106`
**ステータス:** Closed | **ラベル:** bug, help wanted

**概要:** `keyboard.json` の `audio.pins` がビルド時に `AUDIO_PIN`/`AUDIO_PIN_ALT` の C define に変換されないバグ。

**直近のアクティビティ:** 直近 24 時間のコメントなし。

**次のアクション:** #26107 で修正済み（クローズ）。

---

### ![PR](icons/git-pull-request.svg) [Fix audio.pins in keyboard.json not being converted to C defines](https://github.com/qmk/qmk_firmware/pull/26107) `#26107`
**ステータス:** Merged | **ラベル:** python, cli, stale, dd

**概要:** `audio.pins`（keyboard.json）が C define に変換されない問題を修正する。レビュー指摘を受け、コードでなく `data/mappings/info_config.hjson` のマッピング更新で対応する方針に変更。

**直近のアクティビティ:** `zvecr` が当初 CHANGES_REQUESTED でマッピングでの実現を提案し、その後 `zvecr`・`drashna` が APPROVED（計 4 件）。

**変更ファイル:** `info_config.hjson`、`generate/config_h.py`、`generate/rules_mk.py`。

**次のアクション:** マージ済みのため対応不要。
